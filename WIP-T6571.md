```json
{
  "taskId": "T6571",
  "role": "implement-worker",
  "baseCommit": "abcacad0ae44cce09c6ed217fbe20181def4f2f3",
  "startedAt": "2026-09-17T13:40:00.220Z",
  "checkpoints": [
    { "name": "D491 reproduction", "status": "done" },
    { "name": "receipt and net-diff invariant", "status": "done" },
    { "name": "orchestration contract projections", "status": "done" },
    { "name": "focused acceptance checks", "status": "done" },
    { "name": "parent closure preflight", "status": "done" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## D491 reproduction <!-- cq:wip-checkpoint -->

Add both required form-(a) expected-failure tests with literal `tasks:T6571` provenance, record the expected receipt-path-union/net-diff failures, and keep the focused expected-failure suite green before production edits.

Observed before production edits:

- Plain D491 assertions: 0 pass / 2 fail. The broker verifier threw `broker receipt paths do not equal the actual base-to-result diff`; the repository-backed production verifier returned `receiptsVerified: false` for the same transient-path history.
- Restored marker file hashes exactly matched their pre-mutation hashes: `acddca2...15e1c5` and `9c18d34...c3d05`.
- Live form-(a) selector: 2 pass / 0 fail; expected-failure policy: 9 pass / 0 fail.
- The unfiltered three-file command reached 30 pass / 2 fail; both failures were unrelated H236 nested-Codex sandbox probes receiving exit 101 because the confined process could not open its synthetic `/tmp` mount-registry lock.

## Receipt and net-diff invariant <!-- cq:wip-checkpoint -->

Authenticate the complete durable receipt chain from the trusted origin through the exact clean tip, then validate `filesTouched` independently against the authoritative net diff.

Implemented one fused verifier invariant in both public verification paths:

- ordinary chains begin exactly at the authoritative base; guarded chains begin at the server-resolved rebased-start anchor;
- receipt identities and contiguous edges are checked through the exact result tip, with Git parent/tree/sorted-path/object verification;
- caller `filesTouched` is unique, sorted, and exactly equal to the ordinary base-to-result or guarded onto-to-result net diff, independently of historical receipt paths;
- the broker rejects dirty managed tips and false net-diff claims while retaining guarded exact-tip empty-suffix handling.

Mutation evidence: with all three broker guards disabled and transient-path rejection reinstated, the focused broker selector produced 0 pass / 4 fail. With ordinary origin/identity disabled and receipt-path-union equality reinstated, the repository-backed selector produced 0 pass / 4 fail. After restoration, the source hashes returned exactly to `2a3e586...28b87` and `855f52d...4fba`.

## Orchestration contract projections <!-- cq:wip-checkpoint -->

Update both canonical prompt sources, regenerate tracked projections, and verify packaged prompt surfaces.

Task provenance includes the canonical sources and both tracked projections:

- `nix/pkg/cq-assets/commands/cq/implement/advance.md`
- `nix/pkg/cq-assets/fragments/codex/implement-dispatch-workflow.md`
- `nix/pkg/cq-ledgers/packages/cq-config/src/promptCatalog.gen.ts`
- `nix/pkg/cq-ledgers/packages/ledger-web/src/agentsCatalogue.gen.ts`

The prompt-catalog projection is metadata-only and remained byte-identical. The agents catalogue was deterministically regenerated for the changed `implement/advance` role; a direct parser comparison confirmed that its projected prompt exactly equals the canonical role body. Official Nix-backed generation and authority checks could not connect to the Nix daemon from this confined worker (the host cache is read-only and the daemon socket is denied), so those exact commands remain recorded as environmental failures for the trusted parent.

## Focused acceptance checks <!-- cq:wip-checkpoint -->

Run the task-scoped test and typecheck commands specified by the bound acceptance criteria.

Measured outcomes:

- Required five-file focused test command: 94 pass / 2 fail / 953 assertions. Both failures are the unchanged H236 nested-Codex sandbox probes; their child process exits 101 because this already-confined worker cannot open the synthetic `/tmp` mount-registry lock. All T6571 and other selected tests passed.
- Broker file excluding only the two H236 confinement probes: 9 pass / 0 fail / 63 assertions.
- `bun run typecheck`: exit 0.
- Expected-failure policy: 9 pass / 0 fail; both T6571 markers and literal inventory entries were removed only after same-titled plain tests passed.
- Official prompt generation/authority/packaging commands failed before output comparison because Nix daemon access is denied in this sandbox. Direct canonical-to-generated role comparison and stale-contract checks passed.

## Parent closure preflight <!-- cq:wip-checkpoint -->

The parent independently repeated the closure preflight at exact candidate tip `42e8558d68bd91147e3ac9b142aa866cc295620b`: `bun test packages/ledger/test/gateClosureRepository.test.ts` completed with 1 pass / 0 fail / 1 assertion in 36.07 seconds. This worker did not repeat unchanged focused suites or run a canonical full gate.

- Before manifest repair, the exact `bun test packages/ledger/test/gateClosureRepository.test.ts` selector reproduced 0 pass / 1 fail because `implementationEvidenceRuntime.ts` has digest `855f52d763fe059803a42f169aabffe87db1cdbacb085035ca7874367d414fba` while its closure edge declared `d8e46f22313a50f7b76b8324602e058dd8ae2e991d7cc3ac149075e50a921671`.
- Correcting that entry exposed another stale edge, so a complete opaque-edge audit was performed. Six additional changed sources had stale declarations across ten entries; only their digest fields were updated, preserving every edge kind, source, target, and guard.
- A post-repair audit reported zero declared-source digest mismatches.
- The exact focused selector then completed with 1 pass / 0 fail / 1 assertion in 36.58 seconds.

## Trusted full gate <!-- cq:wip-checkpoint -->

Unmeasured in this child. The operator explicitly reserved the single canonical full gate for the parent queue.
