```json
{
  "taskId": "T2081",
  "role": "implement-worker",
  "baseCommit": "29da16f77a873e04980e4c817b27edac1f316b74",
  "startedAt": "2026-08-12T20:37:54.731Z",
  "checkpoints": [
    { "name": "reproduction", "status": "done" },
    { "name": "contract-and-storage", "status": "done" },
    { "name": "implementation", "status": "done" },
    { "name": "targeted-checks", "status": "done" },
    { "name": "full-gate", "status": "unmeasured" }
  ]
}
```

## reproduction <!-- cq:wip-checkpoint -->

Observed before implementation at the actual Codex workspace-write process boundary:

```text
$ cq gate run --worktree "$PWD" --command-cwd "$PWD/nix/pkg/cq-ledgers" -- bun --version
1.3.13
cq: fatal: cq gate: could not resolve the caller process group
exit=1
```

The child command ran, but runner registration failed across the confined process boundary. The
current implement-worker result contract also has no runner-owned evidence arm with which the
supervisor can attest the exact clean result tip after the child returns.

The failure-first schema regression produced `0 pass / 1 fail` and reported both expected
contract gaps: missing required `gateDurationMs` and forbidden additional property
`supervisedGateEvidence`.

## contract-and-storage <!-- cq:wip-checkpoint -->

Added the versioned exact-tip evidence arm and runner-owned storage enforcement. Focused tests
passed `7 pass / 0 fail`, covering green consumption plus fabricated, red, zero-pass,
nonzero-fail, dirty-tree, and replay rejection. The existing Git broker test also reproduced a
Bun 1.3 `node:child_process` stdin loss (`hash-object --stdin` received zero bytes); direct
immutable subprocess stdin restored the receipt path and its focused test passed.

## implementation <!-- cq:wip-checkpoint -->

The trusted store boundary now binds the prepared Codex worker, validates receipts and the
clean exact tip under the managed effect lock, runs the canonical gate, rechecks the tip/tree,
and attaches strict versioned evidence. Reviewer schemas, validation helpers, and source prompts
now pass that evidence through without a sandbox rerun; legacy gate paths remain available. The
installed Codex provider test exercises the native and process boundaries with caller gate fields
omitted, a trusted gate dummy, and assertions on the stored evidence. Timeout, cancellation, dirty
trees, moving tips, red counts, zero tests, fabricated evidence, and replay all fail closed.
The concurrent-store reproduction initially ran the trusted gate twice; re-resolving dispatch
liveness under the effect lock now reduces simultaneous stores to one active gate attempt.
The Bun 1.3 peer harness exhibited the same streamed-stdin loss as Git hashing; immutable stdin
now preserves both broker file bytes and peer request bytes, with mutation controls reproducing
the old-state digest failure and peer readiness timeout.

## targeted-checks <!-- cq:wip-checkpoint -->

Typecheck passed. Focused schema, storage, reviewer, prompt, version, Git broker, and generated
catalogue checks passed (64 tests plus one installed-only test skipped outside its packaged gate).
Deliberate mutants failed on red evidence, zero passes, post-gate tip motion, concurrent replay,
reviewer task substitution, sandbox self-gate prose, caller duration, zero-byte Git input, and
streamed peer input; every mutated file returned to its pre-mutation SHA-256 digest.

## full-gate <!-- cq:wip-checkpoint -->

Run the canonical full gate from the prepared worktree.
