```json
{
  "taskId": "T6410",
  "role": "implement-worker",
  "baseCommit": "f6159f48c291e2c80cf35f462994a046975ec8a7",
  "startedAt": "2026-09-02T18:34:24Z",
  "checkpoints": [
    { "name": "reproduction", "status": "done" },
    { "name": "implementation", "status": "done" },
    { "name": "focused verification", "status": "done" },
    { "name": "mutation evidence", "status": "done" },
    { "name": "guarded-rebase verification", "status": "done" },
    { "name": "lint correction", "status": "done" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## reproduction <!-- cq:wip-checkpoint -->

The required stale-term selector initially found FS/Git registrations in the
shared plan-lifecycle, predicate, task-adoption, goal-precondition, and
workset-owner matrices, plus outdated suite descriptions.

## implementation <!-- cq:wip-checkpoint -->

Removed FS/Git from shared contract matrices, retained provider-only durable
factory exports, and registered the goal-precondition contract for the
in-memory dummy, SQLite/XDG, and environment-gated PostgreSQL adapters.

## focused verification <!-- cq:wip-checkpoint -->

The specified 12-file `bun test` selector passed 357 tests and 2,073
assertions (71 PostgreSQL-gated skips). `bunx tsc -b --pretty false` passed.
Both required stale-term selectors returned no matches.

Reran at 2026-09-02T18:52:51Z in this dispatch: the selector again passed
357 tests and 2,073 assertions with 71 PostgreSQL-gated skips; TypeScript and
both stale-term selectors again exited zero.

On the guarded-rebase continuation, the specified selector passed 357 tests
and 2,073 assertions with 71 PostgreSQL-gated skips. `bunx tsc -b --pretty
false` passed, and both stale-term selectors returned no matches.

## mutation evidence <!-- cq:wip-checkpoint -->

Before mutation, `plan-lifecycle-store-conformance.test.ts` hashed to
`e3115bda850d1ebb09c3b5ba8beec8b644a491ca3d180a35ee90b276ce46ca1a`.
Changing both surviving SQLite registry expectations to
`SqliteLedgerStoreMutated` made the exact selector fail the two registry
assertions, reporting the actual `SqliteLedgerStore` value. Restoring the
expected values returned the file to the same SHA-256; the final exact
selector passed.

Repeated during this dispatch: both `SqliteLedgerStore` registry expectations
were mutated, producing the two expected registry failures; after restoration,
the file again hashed to `e3115bda850d1ebb09c3b5ba8beec8b644a491ca3d180a35ee90b276ce46ca1a`
and the exact selector passed 112 tests (44 PostgreSQL-gated skips).

On the guarded-rebase continuation, changing both SQLite registry expectations
to `SqliteLedgerStoreMutated` failed the two registry assertions. Restoring the
expected values returned the file to SHA-256
`e3115bda850d1ebb09c3b5ba8beec8b644a491ca3d180a35ee90b276ce46ca1a`; the
exact selector then passed 112 tests (44 PostgreSQL-gated skips).

## guarded-rebase verification <!-- cq:wip-checkpoint -->

Verified the rebased start commit against the supplied base and old-result
bridge, then repeated the acceptance checks on the rewritten history.

## lint correction <!-- cq:wip-checkpoint -->

`bunx eslint packages/ledger/test/taskAdoptionPersistentRaceWorker.ts`
reproduced the configured `@typescript-eslint/no-unused-vars` failure for the
unused `control` parameter on line 57. Renaming that parameter to `_control`
made the same focused lint command exit zero, matching the configured
underscore convention. The source SHA-256 changed from
`0e7e0790397783fa3a8d755bf4652a727d4f027dde03db5f698caa593279e2ee` to
`981c7daeebd97c6571c65689617a6350e60c7dcea853dbc194b32a587e9eed3b`.

## trusted full gate <!-- cq:wip-checkpoint -->

Pending trusted result-storage finalization.
