```json
{
  "taskId": "T6520",
  "role": "implement-worker",
  "baseCommit": "7476d205ed0a1acf9a809c521cfd6185104ea6e0",
  "startedAt": "2026-09-18T15:14:20Z",
  "checkpoints": [
    { "name": "reproduce fenced handoff defects", "status": "done" },
    { "name": "implement recoverable handoff", "status": "done" },
    { "name": "focused acceptance", "status": "done" },
    { "name": "guard mutations", "status": "done" },
    { "name": "reproduce coordinator successor handoff", "status": "done" },
    { "name": "correct coordinator successor handoff", "status": "done" },
    { "name": "verify real-backend candidate authority", "status": "done" },
    { "name": "reproduce persisted completion replay", "status": "done" },
    { "name": "correct persisted completion replay", "status": "done" },
    { "name": "reproduce packaged-role retained lease mismatch", "status": "done" },
    { "name": "correct packaged-role retained lease expectations", "status": "done" },
    { "name": "verify packaged-role correction", "status": "unmeasured" },
    { "name": "reproduce retained-lease successor rejection", "status": "done" },
    { "name": "correct retained-lease successor transfer", "status": "done" },
    { "name": "verify successor correction", "status": "done" },
    { "name": "reproduce bare reprepare successor path", "status": "done" },
    { "name": "correct public continuation transfer", "status": "done" },
    { "name": "verify guarded correction chain", "status": "done" },
    { "name": "reproduce canonical task fixture mismatch", "status": "done" },
    { "name": "correct canonical task fixture mismatch", "status": "done" },
    { "name": "verify canonical task fixture correction", "status": "done" },
    { "name": "reproduce managed gate closure mismatch", "status": "done" },
    { "name": "audit affected closure declaration edges", "status": "done" },
    { "name": "correct stale closure source digests", "status": "done" },
    { "name": "verify gate closure correction", "status": "done" },
    { "name": "reproduce authoritative candidate retention defect", "status": "done" },
    { "name": "correct authoritative candidate retention", "status": "done" },
    { "name": "complete canonical acceptance matrix", "status": "done" },
    { "name": "verify retention and acceptance corrections", "status": "done" },
    { "name": "round 9 guard mutations", "status": "done" },
    { "name": "reproduce canonical acceptance coverage gaps", "status": "done" },
    { "name": "add canonical gate and successor matrices", "status": "done" },
    { "name": "add durable cut-point fault matrix", "status": "done" },
    { "name": "round 11 guard mutations", "status": "done" },
    { "name": "reproduce retained-journal timing under controlled load", "status": "done" },
    { "name": "correct deterministic retained-journal timing", "status": "done" },
    { "name": "replace synthetic candidate matrices with production boundaries", "status": "done" },
    { "name": "round 13 guard mutations", "status": "done" },
    { "name": "round 13 focused acceptance", "status": "done" },
    { "name": "round 16 criticism review", "status": "done" },
    { "name": "D501 cancelled reprepare reproduction", "status": "done" },
    { "name": "D501 fixture correction", "status": "done" },
    { "name": "D502 hidden prompt reproduction", "status": "done" },
    { "name": "D502 lazy rendering correction", "status": "done" },
    { "name": "round 16 mutation evidence", "status": "done" },
    { "name": "non-PG fixture consumers", "status": "done" },
    { "name": "exact five-file acceptance", "status": "done" },
    { "name": "full web subtree", "status": "done" },
    { "name": "type and lint", "status": "done" },
    { "name": "round 17 D499 deterministic admission", "status": "done" },
    { "name": "round 17 D503 direct projection coherence", "status": "done" },
    { "name": "round 17 D504 publication cleanup settlement", "status": "done" },
    { "name": "round 17 guard mutations", "status": "done" },
    { "name": "round 17 focused acceptance", "status": "done" },
    { "name": "round 18 protected lease race reproduction", "status": "done" },
    { "name": "round 18 durable completion reservation", "status": "todo" },
    { "name": "round 18 mutation evidence", "status": "unmeasured" },
    { "name": "round 18 focused acceptance", "status": "unmeasured" },
    { "name": "round 19 restore cancelled draft", "status": "done" },
    { "name": "round 19 correct protected lease race reproduction", "status": "done" },
    { "name": "round 19 durable completion reservation", "status": "todo" },
    { "name": "round 19 mutation evidence", "status": "unmeasured" },
    { "name": "round 19 focused acceptance", "status": "unmeasured" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## round 19 restore cancelled draft <!-- cq:wip-checkpoint -->

Correction round 19 opened at 2026-09-19T01:54:01Z. The cancelled generation
19 production draft was preserved as an exact seven-file patch with SHA-256
`01ab0eb50e1c84edb51a98229eb9069ec7a4bd4b53caf6a23ff29a4844e3e078`,
then only those seven production paths were restored to retained `HEAD` through
`apply_patch`. The worktree was otherwise clean. The draft will not be applied
until the corrected production-boundary race is demonstrated fail-first.

## round 19 correct protected lease race reproduction <!-- cq:wip-checkpoint -->

The counted unchanged-candidate case again uses distinct base/result commits,
a complete receipt closure, descendant ancestry, and its original protected-ref
advance. It passes independently (1 test, 10 assertions).

A separate SQLite/public-capability interleaving pauses `shareWithGuardian`
after its second and final production `resolveImplementationCandidateAuthority`
call and before guardian share or ref movement. At that point the ordinary
public continuation legally resolved and allocated a successor; the regression
expectation failed with “Expected promise that rejects / Received promise that
resolved” (0 pass, 1 fail, 7 assertions). This reproduces the stale-holder
handoff defect at the disputed boundary without closed-over digest equality.

## round 19 durable completion reservation <!-- cq:wip-checkpoint -->

Pending a correct fail-first observation. If reproduced, refine the retained
draft into a replay-safe reservation that fences holder transfer through ref
settlement, evidence finalization, and idempotent lease release.

## round 19 mutation evidence <!-- cq:wip-checkpoint -->

Unmeasured. Every changed guard will be made to fail causally, then restored to
its final byte hash and rerun green.

## round 19 focused acceptance <!-- cq:wip-checkpoint -->

Unmeasured. Run the canonical five-file selector plus affected queue,
continuation, retention, reservation, type, lint, and closure checks. The
trusted parent owns the full gate.

## round 18 protected lease race reproduction <!-- cq:wip-checkpoint -->

Correction round 18 opened at 2026-09-19T01:36:11Z. Reproduce through public
boundaries whether a consumed continuation can transfer the queue lease after
final candidate authorization but before protected ref settlement and thereby
permit a stale holder to merge.

Observed fail-first with an exact-tip consumed candidate: the production merge
admission provider completed candidate authorization, then the public consumed
continuation path resolved and allocated its successor before admission or ref
movement. The regression guard expected a completion-reservation rejection but
the continuation promise resolved. An earlier non-exact fixture attempt was
discarded because its continuation live tip did not match the candidate result;
it did not exercise the disputed interleaving.

## round 18 durable completion reservation <!-- cq:wip-checkpoint -->

Pending the fail-first observation. If reproduced, bind merge/completion
authority to an explicit replay-safe lease reservation through ref settlement,
ledger recording, evidence finalization, and lease release.

## round 18 mutation evidence <!-- cq:wip-checkpoint -->

Unmeasured. Any changed guard must be made to fail causally and restored to its
final byte hash before completion.

## round 18 focused acceptance <!-- cq:wip-checkpoint -->

Unmeasured. The parent owns the required PostgreSQL/package gate and trusted
full gate; this worker will run the canonical five-file selector plus affected
queue, continuation, retention, reservation, type, lint, and closure checks.

## round 17 D499 deterministic admission <!-- cq:wip-checkpoint -->

Correction round 17 opened at 2026-09-19T00:32:13Z. Replace scheduler timing
as an ordering oracle with explicit durable-write, admission, and bootstrap
observations while retaining a bounded real-process smoke and deterministic
expired-cleanup control. Production deadlines remain unchanged.

The H354 proof now blocks and releases the durable evidence write explicitly,
then records durable-write completion, admission acquisition, and bootstrap
launch in exact order. Its real process remains bounded by a 1,000 ms launch
deadline; the separate never-resolving prelaunch case still proves typed
expiry, admission abandonment, zero bootstrap launches, and zero active
admissions. The focused H354 selector passed with 8 assertions.

## round 17 D503 direct projection coherence <!-- cq:wip-checkpoint -->

Move the 20,000-document coherence proof to the existing direct projection
seam with recorded commands. Preserve the exact one-delta, changed-key, and
self-version assertions; retain dedicated real-worker fault coverage.

The 20,000-document case now wraps `createDirectSearchProjection` with
per-store command recorders. It retains the one-delta, one changed key, peer
notification, retained-document count, and self-version assertions without a
1,000 ms worker boundary. The focused coherence case passed with 11
assertions; the dedicated queued-dummy and Bun-worker fault suite passed 9
tests with 108 assertions.

## round 17 D504 publication cleanup settlement <!-- cq:wip-checkpoint -->

Add deterministic fail-first controls for publication, expiry, cleanup, and
the already-rejecting/past-deadline branch before changing implementation.
Make cleanup ownership and I/O settlement explicit without broad ENOENT
suppression or deadline inflation.

Fail-first observed: an already-started publication rejected while
`awaitBeforeLaunchDeadline` evaluated an already-expired boundary. The helper
returned the typed deadline error, but Bun then reported the controlled
publication rejection as unhandled. The new Blackbox-Atomic regression guard
fails on that exact rejection rather than on an import or harness error.

`awaitBeforeLaunchDeadline` now observes an already-started rejection before
validating the boundary. Owned release publication uses a factory that does
not start after expiry and, once started, settles before the caller removes
its protocol directory. Four atomic deadline/publication cases, 18 real
registered-launch cases, and 9 broker cases passed. Nonexpired publication
failures remain exact; typed expiry and process/orphan settlement remain
covered by their existing suites.

## round 17 guard mutations <!-- cq:wip-checkpoint -->

Observed failures followed by exact byte restoration and green reruns:

- swapping the H354 admission-held/admission-start expectation failed on the
  exact ordering delta; restored `implementation-evidence.test.ts` to SHA-256
  `d97522c9300154d3fb7165627f9db4ae01d33675b8932cab2c2bdf26b66f5854`;
- suppressing peer command recording failed with zero writes instead of the
  required one delta; restored `sqlite-scoped-coherence.test.ts` to SHA-256
  `f21b98104b24b624756a4947bb87316566335c15249ad1c64b77cdc037c0be5c`;
- removing early rejection observation reproduced the controlled unhandled
  publication rejection;
- starting owned publication before expiry validation failed with one start
  instead of zero;
- removing I/O settlement made the cleanup outcome settle before the owned
  publication;
- converting a nonexpired I/O rejection to a deadline failed exact-error
  identity.

The D504 mutations were restored to `launchDeadline.ts` SHA-256
`eee16b51460bd4bab29e35e0c82ce7ca683b0c0a76ccb41dff7ad11a9f5373f5`;
the four-case guard file remained SHA-256
`ba11521ef4354396371abe1a8e229ac391049b8ce59c3f18b4ae0a05629fcdaf`.

## round 17 focused acceptance <!-- cq:wip-checkpoint -->

Observed green on restored source bytes:

- H354: 8 consecutive passes under two bounded same-CPU competitors, each
  with 8 assertions;
- 20,000-document coherence: 8 concurrent CPU-pinned passes, each with 11
  assertions;
- deadline and registered-launch regression selector: 22 pass, 0 fail, 89
  assertions;
- workset broker: 9 pass, 0 fail, 39 assertions;
- queued-dummy and real-worker fault coverage: 9 pass, 0 fail, 108 assertions;
- exact five-file acceptance: 51 pass, 0 fail, 277 assertions;
- all eight non-PostgreSQL queue fixture consumers: 55 pass, 0 fail, 247
  assertions;
- `bun run typecheck` and `bun run lint`;
- `git diff --check`;
- the repository closure guard first rejected the expected stale H354 source
  digest, then passed 1 test with 0 failures after refreshing only the two
  existing H354 and registered-launch path/spawn digest pairs. No target or
  execution edge changed.

## reproduce retained-journal timing under controlled load <!-- cq:wip-checkpoint -->

Correction round 13 opened at 2026-09-18T21:56:53Z. The parent reported that
the retained-journal real-process case passes when idle but expires its 50 ms
bootstrap handshake under a bounded same-CPU competitor. Reproduction must
separate bootstrap/admission ordering from scheduler latency and retain a
deterministic expiry control.

Observed fail-first: the exact selector passed idle, then failed with
`registered-launch bootstrap handshake` when both Bun and one bounded `yes`
competitor were pinned to CPU 0. The failure was the expected deadline path.

## correct deterministic retained-journal timing <!-- cq:wip-checkpoint -->

The case now records the exact ordering: retained-journal snapshot completes,
admission is acquired, the bootstrap launches, and the settlement snapshot
follows. The real-process success path uses a 1,000 ms launch margin. A separate
never-resolving prelaunch validation provides a deterministic deadline-expiry
negative control and proves that no bootstrap runs and the admission is
abandoned. The selector passes idle and under the same bounded CPU load.

## replace synthetic candidate matrices with production boundaries <!-- cq:wip-checkpoint -->

The unchanged-candidate case now carries one counted coordinator gate through
real panel preparation and retry, review-attempt finalization, completion
preparation, merge admission and settlement, ledger recording, idempotent
completion replay, and one queue release. Its gate history remains one at every
boundary and records zero post-merge launches.

The tip, tree, command, and packaged-environment rows now drive the production
queue coordinator. Each observes one initial callback, rejects stale dispatch
identity without another callback, then qualifies the fresh identity and
observes exactly one new gate. The command row also proves that a noncanonical
command cannot enter the queue before the canonical fresh attempt runs.

Guarded-successor coverage now restarts a real SQLite backend over a live
managed Git worktree and calls the public
`resolveImplementationCandidateAuthority`. The retired source and foreign task
are rejected; only the post-rebase successor reaches real review and completion
preparation. The earlier in-memory row-transition case remains as the cheap BG
leg, while the SQLite case supplies the Effectual-GoodCommunication leg.

## round 13 guard mutations <!-- cq:wip-checkpoint -->

Observed production-behavior mutations, followed by exact byte restoration:

- adding a redundant prelaunch evidence snapshot made the retained-journal
  ordering case fail with three snapshots instead of two;
- skipping the coordinator's `finalizeQualifiedFront` call made the changed
  candidate flow reject before completion;
- duplicating that call made the second gate claim reject before completion;
- bypassing the evidence service's candidate-authority resolver made the
  retired-source review unexpectedly resolve instead of reject.

Restored SHA-256: `implementationEvidence.ts`
`07db22fc609f572892686d5aa26627e3797143baeadfad7e5017436448d453f7` and
`implementationCandidateQueue.ts`
`df9265f6b3a640a973a8424b179cf5b895f5ad8c7d6e3e6881d185767d2a54e8`.

## round 13 focused acceptance <!-- cq:wip-checkpoint -->

Observed green after byte restoration:

- exact five-file acceptance selector: 51 pass, 0 fail, 275 assertions;
- `bun run typecheck`;
- `bun run lint`;
- repository closure guard: 1 pass, 0 fail after refreshing the changed
  implementation-evidence path/spawn digests and declaring the new SQLite
  successor test's path/spawn edge;
- retained-journal selector under one bounded same-CPU competitor: 1 pass,
  0 fail.

## reproduce fenced handoff defects <!-- cq:wip-checkpoint -->

Observed before implementation:

- `implementation-candidate-reuse.test.ts` failed because `confirmDispatchCompletionOn` changed the current queue row from `leased` to `released` and removed its lease.
- `implementation-review-attempts.test.ts` failed because replay after reservation expiry returned a synthesized `existing` abstention instead of reporting the still-running attempt as pending; the original reviewer verdict would be discarded.
- The current hermetic conflict continuation clears global Git configuration without supplying a trusted committer identity; a global-only repository therefore cannot complete the rebase.

## implement recoverable handoff <!-- cq:wip-checkpoint -->

The qualified candidate retains its exact live queue lease through gate attachment,
worker confirmation, review, and merge authorization. Protected evidence journals
the candidate receipt, revalidates task/goal/manifest/roster/worktree/gate/lease
identity before the ref update, and releases the lease idempotently only after the
merged completion is recorded. Once the protected ref equals the result commit,
the durable completion phase drives recovery without another gate or candidate
revalidation.

Expired live external-review reservations remain pending so the original verdict
can settle. Conflict continuation supplies a hermetic trusted committer identity
while retaining the rebased commit author.

## focused acceptance <!-- cq:wip-checkpoint -->

Observed green:

- `bun run typecheck`
- 36 focused queue, review, completion, and conflict-continuation tests
- the task's five-file acceptance selector: 42 pass, 0 fail

Correction round 3 observations:

- the task's five-file acceptance selector: 44 pass, 0 fail
- affected queue/coordinator suites: 46 pass, 0 fail
- completion recovery, review retry, and shared-store suites: 22 pass, 0 fail
- installed-boundary command selectors: 4 pass, 0 fail
- `bun run typecheck`
- `bun run lint`

## guard mutations <!-- cq:wip-checkpoint -->

Observed mutations from this run; each restored file matched its pre-mutation SHA-256:

- queue lease-state predicate: expected `confirm_dispatch_completion` rejection;
  restored `dispatchAttestation.ts` to `bcc8020d8b0866e8c9fa1f45d94c896b6c2f627c07b79c022f0ea3333cdb6777`.
- external-review pending status: expected `pending` versus `existing` assertion failure;
  restored `implementationEvidence.ts` to `78ab774de324b2a6e22008a6ee64af478a334606066d8c739a4af23dee233fb8`.
- candidate receipt equality fence: expected exact-current receipt rejection;
  restored the same implementation-evidence hash.
- trusted conflict committer identity: expected `Committer identity unknown` failure;
  restored `gitConflictContinuation.ts` to `ccf5f8082f4f6907a647181f40a7ba603353f38b49fec42b1cc0676955016629`.
- exact requested-candidate admission: disabling the expected-handle comparison
  leased the foreign front and failed the blocked-handoff assertion; restored
  `dispatchImplementationQueue.ts` to `bee22d4374d48d5c55e9756ee593302acdea459e06c594404887e0762c6c141d`.
- consumed retained-lease settlement: disabling the settled replay path retried
  the gate and reproduced `has no staged parent gate`; restored
  `implementationCandidateQueue.ts` to `df9265f6b3a640a973a8424b179cf5b895f5ad8c7d6e3e6881d185767d2a54e8`.
- consumed gate identity: disabling the task binding admitted tampered gate
  evidence and failed the rejection assertion; restored `dispatchCapability.ts`
  to `adbc86bd3abaff0cfda02dc49551efd9b98f04015458ccccd93ed12fe285e4fe`.
- SQLite uniqueness scope: reverting to a handle-scoped transaction reproduced
  the whole-namespace scan rejection; restored the same dispatch-capability hash.
- legacy fingerprint compatibility: disabling the legacy digest rejected the
  exact old-format merged record, while disabling the rejection admitted the
  tampered record; restored `implementationEvidence.ts` to
  `07db22fc609f572892686d5aa26627e3797143baeadfad7e5017436448d453f7`.
- retained candidate lease: changing the runnable source-fixture expectation to
  `released` failed with actual `leased`; restored
  `implementation-candidate-reuse.test.ts` to
  `729b322a4d92190f5f06f43b3fdd1b22eff206c997af3c9bc5548c6ba18ad85e`
  and observed 2 pass, 0 fail.
- completion-time release: changing the protected-completion release count from
  2 to 0 failed with actual 2; restored `implementation-evidence.test.ts` to
  `36af19ccf571c61024063555ce59c1ce4fa337f69ea827c31ac7ebf57259dac7`
  and observed the focused test pass.

## reproduce canonical acceptance coverage gaps <!-- cq:wip-checkpoint -->

Round 11 opened from the guarded-rebase exact tip. The canonical five-file
selector's existing cases are green, but its executable inventory has no named
case that carries one counted coordinator gate through review retry, exact
ff-only merge, completion, and replay. Its candidate-change loop only observes
old-receipt rejection; it never admits a fresh replacement or counts a new
gate. Its post-rebase coverage stops at a retired source rejection, and the
seven-cut matrix hides duplicate calls behind saturating counters instead of
recording durable operation-id effects. These are test-coverage defects rather
than a newly reproduced product failure.

## add canonical gate and successor matrices <!-- cq:wip-checkpoint -->

The canonical selector now has named Behavioral-Active Blackbox-Group cases
for the previously missing contract rows:

- the real queue coordinator seam counts one initial gate and remains at one
  through review retry, observed ff-only result head, completion release, and
  completion replay, with a zero post-merge delta;
- changed result tip, tree, gate command, and packaged environment each reject
  stale review evidence and admit a fresh candidate after exactly one new gate;
  roster plus current-lease refresh is review-only and retains the green gate;
- a consumed retained source allocates a true guarded-rebase successor, survives
  backend restart, and makes only that successor eligible for authenticated
  review and completion. The retired source and foreign task are rejected, while
  the adjacent canonical cases retain stale-generation, unqualified, and
  terminal-source rejection.

Observed affected selector: 22 pass, 0 fail, 166 assertions. The exact five-file
acceptance selector passes 49 tests and 245 assertions. `bun run typecheck`,
affected-file ESLint, Prettier verification, and `git diff --check` are green.
The repository closure guard first failed with the exact stale digest for the
changed implementation-evidence test, then passed after both declared path and
spawn edge digests were refreshed.

## add durable cut-point fault matrix <!-- cq:wip-checkpoint -->

The seven existing launch/ref/merge/ledger/evidence/release cuts now record
ledger and release side effects in request-digest-checked maps keyed by stable
operation ids. Every restart path asserts the exact single durable completion
key and exact single durable release key; repeated callback invocation can no
longer be hidden by saturating an integer counter.

## round 11 guard mutations <!-- cq:wip-checkpoint -->

Observed expected failures, then restored the exact pre-mutation bytes:

- unchanged-candidate gate history: changed the expected initial count from one
  to two and observed the `[1,1,1,1,1]` mismatch;
- changed-candidate matrix: required three gates and observed the actual two on
  the tip row;
- cut-point durability: substituted a foreign release operation id and observed
  the exact persisted `release-before-merge-launch` key;
- guarded successor completion: required `merge-required` and observed the
  authenticated `prepared` completion.

Restored SHA-256: `implementation-evidence.test.ts`
`823afe8b9ba3be95f4773b228d091072083a14b0dea2330f8b7177bd7553eebc`,
`implementation-candidate-reuse.test.ts`
`3bca76c974179a1c8d5b6c22af2533ca0b3875b8167ebc07a3ce8ad77550edb8`,
`implementation-candidate-stale-holder.test.ts`
`c309abcd523d9532a0de4b7f9fb5e93002940aa8357f5989f157479e2971f3fe`.

## reproduce coordinator successor handoff <!-- cq:wip-checkpoint -->

Correction round opened for the reported production-coordinator failure: the
installed successor boundary has no staged parent gate.

Parent-host fail-first evidence identified the exact assertion. In this confined
child the installed subprocess selector instead times out before Codex launch:
a minimal Bun `node:child_process` pipe-write probe receives sandbox `EPERM`.
The equivalent in-process public coordinator reproduced the causal replay: a
second coordination of the consumed retained lease attempted another gate.

## correct coordinator successor handoff <!-- cq:wip-checkpoint -->

Coordination is now candidate-exact. A request for a queued non-front candidate
returns blocked without leasing the foreign front, and a replay of the exact
consumed retained lease returns settled/empty without invoking the gate again.
The public coordinator test observes one gate, one consumed result, and the
same live lease retained for protected review and completion.

## verify real-backend candidate authority <!-- cq:wip-checkpoint -->

A real SQLite attestation backend now exercises the full public sequence:
stage, qualify, gate, confirm, retain the lease, and resolve candidate authority.
The namespace-wide uniqueness scan runs in a namespace-scoped transaction and
returns the exact task/result/holder/generation receipt.

## reproduce persisted completion replay <!-- cq:wip-checkpoint -->

Correction round 3 opened at 2026-09-18T16:34:09Z for the reported persisted
merged-record replay failure: reopening old-format evidence currently rejects
completion recording after the candidate-authority fingerprint field was added.
Fail-first regression reconstructed an authenticated old-format merged journal.
Current revalidation rejected it with `implementation evidence fingerprint
changed before recording`; unrelated-head and tampered controls rejected first.

## correct persisted completion replay <!-- cq:wip-checkpoint -->

Revalidation accepts the legacy fingerprint only when the persisted completion
has no candidate receipt and the complete old-format payload still authenticates.
The exact merged record writes the ledger once and replay returns existing;
tampered evidence and unrelated protected refs remain rejected.

## reproduce packaged-role retained lease mismatch <!-- cq:wip-checkpoint -->

Parent full-gate evidence at the authoritative starting commit reports two
failures in `packagedCodexRoleGitBroker.test.ts`: the fixtures expect a
`released` / terminal gate-complete queue row immediately after the worker gate,
while the production lifecycle now correctly retains the qualified `leased` row
for protected review and completion. The parent observed 44 passing tests and 2
failures in that file; this correction starts from the exact reported red tip.

## correct packaged-role retained lease expectations <!-- cq:wip-checkpoint -->

Both installed-role fixtures now require the exact retained lease holder,
generation, and durable qualification after the worker gate. The authentication
scenario intentionally leaves that authority live because it never records a
protected completion. The guarded-rebase scenario now resolves candidate
authority from the successor row, reads the task's actual sealed goal and
finalized manifest, performs the ff-only merge, records the protected ledger
completion, proves the task is done, observes queue release, replays completion
idempotently, and confirms the gate count remains three.

## verify packaged-role correction <!-- cq:wip-checkpoint -->

Observed in this confined correction:

- the task's five-file acceptance selector: 44 pass, 0 fail;
- retained-lease mutation: expected red, then 2 pass, 0 fail after restoration;
- completion-release mutation: expected red, then 1 pass, 0 fail after restoration;
- packaged-role source contract: 1 pass, 0 fail (3 installed tests filtered);
- `bun run typecheck`;
- ESLint on `packagedCodexRoleGitBroker.test.ts`.

The installed-process fixture itself is unmeasured here because its packaged
executables are not injected into the confined child and the known subprocess
pipe `EPERM` is not product evidence. The trusted parent owns that canonical
installed gate.

## reproduce retained-lease successor rejection <!-- cq:wip-checkpoint -->

Correction round 5 opened at 2026-09-18T17:32:50Z from the exact reported red
tip. Parent evidence identifies the failing boundary as
`claimStagedRebaseSuccessor`: a qualified consumed source whose lease remains
live for protected review cannot prepare its guarded post-rebase successor.
The updated persisted-store fixture first confirms the source remains exactly
`consumed` with its qualified live lease after worker confirmation, then
restarts the backend. The valid guarded successor fails at
`claimStagedRebaseSuccessor` with `the guarded-rebase source is not a retired
implementation queue enrollment`; foreign-result and foreign-manager controls
reject before it.

## correct retained-lease successor transfer <!-- cq:wip-checkpoint -->

Successor preparation now validates the consumed candidate's exact qualified
lease, continuation receipt lineage, managed binding, result, and finalized
guarded-rebase journal before atomically retiring that queue authority and
claiming it for one successor generation. The prior lease is removed in the
same store unit of work, so it cannot retain review or merge authority.

An ordinary criticism continuation uses its single-use authenticated
continuation claim to terminalize the consumed predecessor as `superseded`
before its successor is admitted. The queue accepts only that exact claimed
generation and ignores an older staged-rebase source after its already-claimed
successor advances through an ordinary correction.

Observed green after the correction: all 5 staged-rebase source contract tests,
including persisted-store restart, foreign result/manager rejection, unique
successor allocation, successor gate completion, and ordinary correction; the
workspace TypeScript build and affected-file ESLint also pass.

## verify successor correction <!-- cq:wip-checkpoint -->

Observed green:

- exact five-file acceptance selector: 44 pass, 0 fail, 158 assertions;
- staged-rebase, in-memory/SQLite shared-store, continuation, qualification,
  queue, and stale-rebase regressions: 151 pass, 0 fail, 932 assertions;
- complete staged-rebase source contract: 5 pass, 0 fail, 30 assertions;
- packaged installed-role source contract: 1 pass, 0 fail; 3 installed-process
  cases are intentionally unavailable in this confined child;
- `bun run typecheck` and `bun run lint`.

Observed causal mutations, each followed by restoration and the 5-test source
contract passing:

- disabling guarded retained-lease transfer reproduced `the guarded-rebase
  source is not a retired implementation queue enrollment`;
- disabling ordinary continuation transfer reproduced `dispatch continuation
  does not match the retained implementation queue authority`;
- disabling ordinary-continuation ancestry reproduced `terminal enrollment
  authority ... cannot be resurrected`;
- treating an older already-claimed staged source as current reproduced `a
  retired enrollment requires its exact staged-rebase source authority`;
- disabling the old-result fence admitted the foreign-result prepare and failed
  its rejection assertion;
- disabling the manager/repository fences admitted the foreign-manager prepare
  and failed its rejection assertion;

## reproduce bare reprepare successor path <!-- cq:wip-checkpoint -->

Correction round 6 starts from the exact parent-reported red tip. The required
fail-first probe will use the public dispatch capability with a bare
`reprepareOf` request, without injecting a private continuation claim.

Observed fail-first through the persisted backend: after an exact-tip guarded
successor was gated, confirmed, and retained its qualified lease, a round-2
same-bridge bare `reprepareOf` failed in `claimStagedRebaseSuccessor` with
`the guarded-rebase source is not a retired implementation queue enrollment`.
This is the same line and rejection reported by the parent installed scenario.

## correct public continuation transfer <!-- cq:wip-checkpoint -->

Bare same-bridge `reprepareOf` now derives and persists the consumed source's
single-use continuation claim inside the allocation transaction. It supersedes
the prior qualified queue lease only when managed binding, guarded bridge,
result, and the complete inherited receipt lineage match exactly. Queue
admission recognizes the immediate and transitive superseded generations while
requiring a continuous current claim, the same bridge/manager identity, and an
exact retained-receipt prefix.

The persisted-backend scenario now passes through exact-tip generation,
fresh-receipt correction, and a further inherited-plus-fresh correction. A
substituted receipt prefix rejects before authority transfer. The public
capability exact-tip/correction scenario also passes, and `bun run typecheck`
is green.

## verify guarded correction chain <!-- cq:wip-checkpoint -->

Observed green after restoration:

- exact five-file acceptance selector: 44 pass, 0 fail, 158 assertions;
- staged-rebase source lifecycle: 5 pass, 0 fail, 32 assertions;
- dispatch attestation, staged-rebase, and broker Git capability suites:
  139 pass, 0 fail, 1101 assertions;
- candidate queue, SQLite race, qualification, and staged qualification suites:
  28 pass, 0 fail, 92 assertions;
- packaged installed-role source contract: 1 pass, 0 fail; 3 installed-process
  cases remain intentionally unavailable in this confined child;
- `bun run typecheck` and `bun run lint`.

Observed causal mutations:

- disabling bare same-bridge transfer reproduced the exact
  `source is not a retired implementation queue enrollment` rejection;
- bypassing inherited receipt equality admitted the substituted receipt prefix
  and failed the negative assertion;
- omitting the server-derived continuation claim, current predecessor claim,
  manager identity, lineage bridge, or receipt prefix made queue admission fail
  with `terminal enrollment authority ... cannot be resurrected`;
- restricting admission to only the immediate predecessor made the further
  inherited-plus-fresh correction fail on its older superseded ancestor.

After each mutation, `dispatchAttestation.ts` returned to SHA-256
`da9c52578def800286c65478d7de8ddbe7c4ce58de17d51eda448a57ed898b12`
and `dispatchImplementationQueue.ts` returned to SHA-256
`674ffcbead44162da92ca5de41867a2bc3d92e5aba3413594fe9a16893919c49`.
- inverting receipt-lineage equality reproduced the retired-source rejection;
- changing the expected transferred source state to `leased` failed against
  actual `staged-rebase-retired`;
- changing the expected ordinary predecessor state to `leased` failed against
  actual `terminal` / `superseded`;
- requiring a retained source lease failed with actual `undefined`.

Restored SHA-256 digests:

- `dispatchAttestation.ts`:
  `ad87da69e1683aa0d1ebd626faf3a1cd29bd8722d341519f8aca399e913920de`;
- `dispatchImplementationQueue.ts`:
  `e6b0c6684babd058bd0248d3fb3d69fb64cb1d5a4ebe0ed26072d41cd74e5125`;
- `dispatchStagedRebaseSource.test.ts`:
  `5da29e20b48fa5d35ae8bd81213f4041cc69dbfa9a48173e112a0f7f786a3b4a`.

## reproduce canonical task fixture mismatch <!-- cq:wip-checkpoint -->

Correction round 7 starts from the exact parent-reported red tip. Reproduce the
packaged continuation fixture's mismatch between the sealed canonical task
specification and the task descriptions supplied by later worker rounds.

Observed the production digest probe fail with distinct SHA-256 identities:
canonical `cfd77a975dffce995d752925618bf121874b41435952e5b53baf03126c575766`,
round 0 `12aa6b8e5468818f1b22ace7269ef9def1814f8f54de8cfcca3c358e06f69bc7`,
and bridge `19587e707ee1f7574eb4e219f0ec3acc457254f2b33582bf8669c1ac943e3b4a`.

## correct canonical task fixture mismatch <!-- cq:wip-checkpoint -->

The fixture now defines one canonical task specification and uses it both when
sealing the finalized task and when dispatching round 0 and later guarded
continuations. A focused real-SQLite authority regression resolves the matching
candidate, temporarily substitutes a mismatched description to prove the
production guard rejects it, and restores the authenticated row. Production
task-specification binding remains unchanged.

## verify canonical task fixture correction <!-- cq:wip-checkpoint -->

Observed green on the final source tree:

- packaged source contract plus real-SQLite candidate-authority regression:
  2 pass, 0 fail, 23 assertions;
- exact five-file task acceptance selector: 44 pass, 0 fail, 158 assertions;
- `bun run typecheck`;
- `bun run lint`.

The first typecheck exposed loss of a narrowed JSON-object type in the initial
installed test placement; the runnable authority regression performs the
mutation inside the narrowing transaction and retains the original row for
restoration.

Observed guard mutation: replacing the deliberately mismatched candidate
description with the canonical description made the rejection assertion fail
with `Expected promise that rejects` and an actually resolved authority. After
restoration the focused test passed and
`supervisedWorkerGateStorage.test.ts` returned to SHA-256
`44aae13dba70e0b63878c1d68b3ee1124e9c358333542de4b54f3364361dd6c9`.

## reproduce managed gate closure mismatch <!-- cq:wip-checkpoint -->

Correction round 8 starts from the exact parent-reported red tip. The focused
closure repository test reproduced `source-digest-mismatch` before any
declaration changed: `dispatchCapability.ts` measured
`adbc86bd3abaff0cfda02dc49551efd9b98f04015458ccccd93ed12fe285e4fe`
but the manifest declared
`752b239c41c7a6b0fda497671760c72f54b280371e6d9c21dea9e198f82d34fb`.

## audit affected closure declaration edges <!-- cq:wip-checkpoint -->

All 380 declarations were measured. Exactly 15 declarations across the 10
parent-identified T6520 source files were stale. Their existing opaque-call
inventories still require the same `path` and/or `spawn` kinds; the task diffs
did not add or remove any declared opaque operation or repository target. All
15 target sets therefore remain unchanged and empty.

## correct stale closure source digests <!-- cq:wip-checkpoint -->

Updated only the 15 stale SHA-256 values in `cq-gate-closure.json`. The complete
managed closure then resolved: 1 pass, 0 fail.

## verify gate closure correction <!-- cq:wip-checkpoint -->

Observed guard mutation: replacing the corrected `dispatchCapability.ts`
digest with its stale declaration reproduced the exact
`source-digest-mismatch` failure. Restoring it returned the manifest to SHA-256
`31a9b695d17bfd115a947f390ebdef4577e44a6bf89d6d181f364a4a3d98c443`
and the focused closure test passed: 1 pass, 0 fail.

Final focused observations:

- exact five-file task acceptance selector: 44 pass, 0 fail, 158 assertions;
- `gateClosureRepository.test.ts`: 1 pass, 0 fail after restoration;
- `bun run typecheck`;
- `bun run lint`.

The trusted parent owns the canonical full gate.

## reproduce authoritative candidate retention defect <!-- cq:wip-checkpoint -->

Round 9 starts from the exact parent-reported red tip. Reproduce the sweep
failure and premature removal of a consumed, qualified candidate whose queue
lease remains authoritative for protected review and completion.

Observed through a closed-and-reopened SQLite backend: at exactly 24 hours from
dispatch consumption, `sweepAttestationsOn` throws `AttestationContractError:
implementationQueue.state: a terminal attestation cannot retain a runnable
queue enrollment`. The regression also pins preservation through the old
30-day horizon, idempotency-key exclusion while leased, and new 24-hour/30-day
boundaries measured from durable queue release.

## correct authoritative candidate retention <!-- cq:wip-checkpoint -->

Retention now derives from the queue's durable release/terminal provenance when
a terminal dispatch carries implementation queue authority. Runnable queue
states have no retention horizon: fetch, continuation resolution, idempotency
reclaim, and sweep all preserve them. Release starts fresh 24-hour envelope and
30-day reuse windows without changing the dispatch's original `terminalAt`.

The restart-backed SQLite regression passes: 3 tests, 0 failures, 16
assertions in `implementation-candidate-reuse.test.ts`.

## complete canonical acceptance matrix <!-- cq:wip-checkpoint -->

The exact selector now covers candidate invalidation for changed result commit,
tree, command, packaged environment, task, manifest, lease generation, and
managed worktree authority. It also rejects unqualified and staged-rebase-
retired rows after durable replay and exercises seven restart/fault cuts from
pre-launch through queue release. Every cut converges to one durable ledger
completion and one effective queue release.

Observed exact five-file selector: 47 pass, 0 fail, 221 assertions.

The managed closure first failed on the stale
`implementation-evidence.test.ts` digest, then resolved after both existing
path/spawn declarations were refreshed without changing their target sets:
1 pass, 0 fail.

## verify retention and acceptance corrections <!-- cq:wip-checkpoint -->

Observed on restored final source bytes:

- exact five-file acceptance selector: 47 pass, 0 fail, 221 assertions;
- retention/attestation/SQLite/staged-rebase focused suites: 170 pass, 0
  fail, 1354 assertions;
- managed gate closure repository: 1 pass, 0 fail;
- `bun run typecheck`;
- `bun run lint`.

## round 9 guard mutations <!-- cq:wip-checkpoint -->

Observed expected failures and restored passes:

- restoring the old dispatch-terminal retention clock reproduced the SQLite
  collapse `AttestationContractError`; `dispatchAttestation.ts` restored to
  SHA-256 `f0e3d7b0811c1e4abea2b0de904f8bed2102d85ddd498dc88dd77dc8034a2c7a`;
- bypassing candidate receipt equality admitted changed candidate identity and
  failed the rejection matrix; `implementationEvidence.ts` restored to SHA-256
  `07db22fc609f572892686d5aa26627e3797143baeadfad7e5017436448d453f7`;
- disabling post-recording candidate release failed the seven-cut convergence
  matrix with zero releases; the same implementation-evidence hash was restored;
- bypassing exact qualification changed the unqualified rejection into a null
  dereference and failed its boundary assertion; the dispatch-attestation hash
  above was restored.

## round 16 criticism review <!-- cq:wip-checkpoint -->

This correction must distinguish consumed continuations from cancelled or
aborted reprepare in the non-PostgreSQL fixture and must avoid rendering closed
role-prompt bodies while preserving their metadata, controls, and test ids.

## D501 cancelled reprepare reproduction <!-- cq:wip-checkpoint -->

The exact queue selector observed one pass and one failure. The later-generation
case threw `DispatchContinuationError` with reason `not-found` before reaching
its queue-authority assertions because the shared fixture attempted consumed-
continuation discovery for an aborted prior dispatch.

## D501 fixture correction <!-- cq:wip-checkpoint -->

The fixture now reads the exact prior dispatch row and requests a continuation
claim only when that row is consumed. Aborted/cancelled reprepare remains a
plain `reprepareOf`; missing prior rows still fail explicitly, and errors from
real consumed-continuation discovery are not caught. A prior row must also
remain a live envelope; a collapsed tombstone fails explicitly. The exact
selector then passed both cases with zero failures.

## D502 hidden prompt reproduction <!-- cq:wip-checkpoint -->

Added a synthetic-content guard that requires every closed prompt details
element to omit its body, requires an opened prompt to render only its own
body, and requires closing it to unmount that body. The production prompt
catalogue bodies are not inspected by the guard. Before correction the focused
test failed because the first closed panel already contained its synthetic
marker and all 200 repeated content tokens.

## D502 lazy rendering correction <!-- cq:wip-checkpoint -->

Each prompt details element now owns only its open state and mounts `Markdown`
only while open. The details element, summary, metadata, controls, and existing
test id remain present while closed. The focused synthetic-content guard passed
with 80 assertions after the correction.

## round 16 mutation evidence <!-- cq:wip-checkpoint -->

Observed both causal mutations fail, then restored exact bytes and observed the
focused passes:

- removing the prior-state discrimination reproduced
  `DispatchContinuationError: not-found`; the later-generation selector passed
  after restoring the condition;
- omitting the live-envelope narrowing made `bun run typecheck` fail with
  TypeScript error TS2339 on tombstone rows; the selector and typecheck passed
  after restoring `implementationCandidateQueueFixture.ts` to SHA-256
  `3db23c8f26efeab04504a555ca68f0cd1ea96d92ed9d8af0857fdb6721f13f91`;
- mounting `Markdown` unconditionally reproduced the closed synthetic-marker
  assertion failure; the 80-assertion selector passed after restoring `App.tsx`
  to SHA-256
  `b0786aff00c367045ce51b008063147b3209de8a468e3c0027feb32b9e198451`.

The unchanged final test file measures SHA-256
`1e494614e5b82ec4dbd4846755eba59b9fe89c2b9fe4ebf775ef79ec67eca41b`.

## non-PG fixture consumers <!-- cq:wip-checkpoint -->

All eight non-PostgreSQL fixture consumers passed: 55 tests, 0 failures, and
247 assertions on the final implementation tip, including the SQLite race and
restart-backed authority cases.

## exact five-file acceptance <!-- cq:wip-checkpoint -->

The task's exact five-file selector passed 51 tests with 0 failures and 275
assertions on the final implementation tip, including the versioned production
bootstrap, exact-gate reuse, successor authority, cut-point recovery, and
continuation suites.

## full web subtree <!-- cq:wip-checkpoint -->

Under eight bounded 25-second Bun CPU loops pinned with the selected tests to
CPU 0, the first Agents case passed in 3.44 seconds and both Memories cases
passed in 3.30 seconds. The focused synthetic prompt control also passed at
idle with 80 assertions. The complete Agents file passed 14 tests and 212
assertions on the final implementation tip.

The complete web subtree was attempted in the confined child: 587 passed, 23
were skipped, and 29 host-bound cases failed because loopback server creation
is denied here. Catalogue regeneration additionally reached Nix but could not
write the user fetcher cache outside the worktree. No Agents or Memories case
failed. The trusted parent gate owns the same checks with host capabilities.

## type and lint <!-- cq:wip-checkpoint -->

`bun run typecheck` and `bun run lint` passed. `git diff --check` passed. The
repository closure guard passed 1 test with 0 failures against the audited
current digests. A whole-file Prettier probe exposed substantial pre-existing
format drift in `App.tsx`; those unrelated mechanical changes were restored,
and only the surgical task diff remains.

## trusted full gate <!-- cq:wip-checkpoint -->

Delegated to trusted result storage after the final clean commit.
