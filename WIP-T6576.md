```json
{
  "taskId": "T6576",
  "role": "implement-worker",
  "baseCommit": "9272de7b3cd5cf8ebe41abb42c514b8421cb3f9b",
  "startedAt": "2026-09-20T11:16:42Z",
  "checkpoints": [
    { "name": "base verification", "status": "done" },
    { "name": "fail-first regressions", "status": "done" },
    { "name": "receipt reconciliation correction", "status": "done" },
    { "name": "composed enrollment correction", "status": "done" },
    { "name": "current-seal correction", "status": "done" },
    { "name": "mutation proof", "status": "done" },
    { "name": "focused checks", "status": "done" },
    { "name": "composition checks", "status": "done" },
    { "name": "round 1 lossless recovery seal parsing", "status": "done" }
  ]
}
```

## base verification <!-- cq:wip-checkpoint -->

Verified the prepared worktree, `implement/T6576` branch, exact starting commit,
base commit object, and base ancestry.

## fail-first regressions <!-- cq:wip-checkpoint -->

Added three independently titled public `test.failing` regressions with literal
`tasks:T6576` annotations and matching inventory entries. Plain-test probes
failed at the intended production boundaries:

- `durable continuation receipts do not have one chain start` for the second
  genuine guarded-rebase transaction;
- terminal enrollment authority `cannot be resurrected` for the composed
  gate-rejected, current-recovery, staged-retirement, guarded-successor lineage;
- `journal recovery successor does not inherit the preceding receipt closure`
  while recapturing a cancelled guarded successor across a staged-retired
  recovery source.

All markers were restored after the probes; the marker-contract and inventory
policy tests passed.

## receipt reconciliation correction <!-- cq:wip-checkpoint -->

Guarded-rebase finalization now selects the unique connected receipt component
bound to the journal's head, original tip, rebase target, conflict identity,
and observed live tip. Every durable receipt is still checked against its own
journal before component selection. The public regression passes as a plain
test (1 pass, 0 fail), including an exact replay, a second independent genuine
conflict, preservation of the first journal bytes, and a clean
completed-but-unrecorded recovery after historical conflicts.

## composed enrollment correction <!-- cq:wip-checkpoint -->

Queue ancestry now composes through a genuine gate-rejected correction and an
authenticated staged recovery edge without treating unrelated terminal rows as
authority. Staged retirement no longer erases an authentic relation, while the
exact source, successor, queue, result, receipt, repository, worktree, manager,
and guarded-rebase coordinates remain mandatory. The composed public regression
passes for both memory and SQLite backends (1 test body, 0 failures).

## current-seal correction <!-- cq:wip-checkpoint -->

Current-recovery seals now retain one authenticated guarded-tip transition
between the pre-rebase and post-rebase receipt components. That transition is
validated and propagated through persisted dispatch bindings, result evidence,
continuation confirmation, queue admission, and seal promotion. The regression
recaptures after staged retirement plus cancellation, then resumes through a
new receipt, qualification, and successful supervised completion (1 pass, 0
fail). All 24 dispatch-recovery-seal tests pass after preserving strict-maximal
selection semantics for stale candidates.

## focused checks <!-- cq:wip-checkpoint -->

The five requested files pass with only the independently diagnosed baseline
selector excluded: 110 pass, 1 skip, 0 fail. The exact unfiltered command was
also run. Its sole remaining failure is the installed-successor process harness,
which times out after 30 seconds before issuing a bridge request. The identical
selector extracted from exact base `9272de7b3cd5cf8ebe41abb42c514b8421cb3f9b`
has the same timeout, pass/filter counts, and one-assertion signature.

The prescribed closure sequence passes with 50 tests, followed by green
`bun run typecheck` and `bun run lint`.

## mutation proof <!-- cq:wip-checkpoint -->

Each changed invariant was disabled independently and then restored:

- replacing transaction-scoped receipt selection with global ordering failed
  the receipt regression with `durable continuation receipts do not have one
  chain start`; restored source SHA-256
  `c9cac4878348dff5cceba6b0095eb2b91c6809365a7af025a970ecceaf0b5034`;
- disabling gate-rejected correction ancestry failed with terminal enrollment
  resurrection rejection; restored source SHA-256
  `067be87cc243a61e4beab92afedadda97b1d938e58eb0f968ee74fd4e249063e`;
- disabling authenticated staged recovery edges failed with `journal recovery
  successor does not inherit the preceding receipt closure`; restored source
  SHA-256
  `0319792d91d4b6f6b1129eb825c2336d50ea24baacc3e97f005ffebdf2da4ce8`.

Every corresponding plain regression passed after restoration.

## composition checks <!-- cq:wip-checkpoint -->

Applied D510 commit `811e44c2632597ab7d9b80b0d0591727f7303756`
to G219 commit `eec3c325097f684c04098faa2e55bb5108901707`, then
three-way merged the T6576 base-to-tip delta. Overlaps were reconciled
explicitly so G219's retained guarded bridge does not get re-applied after a
T6576 receipt transition has superseded it, and staged-retired rows remain
ancestry intermediates without becoming resurrection blockers.

The reconciled composition passed 6 guarded-rebase tests; 50 queue,
recovery-seal, and runtime tests with 1 PostgreSQL skip; all 9 combined G219 and
T6576 supervised lineage scenarios; the repository closure test; typecheck;
and lint. The verified composed source-tree identity, excluding this
self-referential WIP evidence artifact, is
`394b7f8bcc9df4991ce79e1e040a62676d6228e2`.

## round 1 lossless recovery seal parsing <!-- cq:wip-checkpoint -->

Reproduce the authenticated-byte mismatch for retained recovery seals whose
`guardedTipTransition` field is absent, preserve the distinct absent and
explicit-null encodings, correct the format boundary without weakening digest
or reference verification, and rerun the focused and composition checks.

The same-titled plain regression failed for both the in-memory and filesystem
journal adapters with `recovery seal digest is not self-authenticating`; the
expected-failure marker, `tasks:T6576` annotation, and inventory entry were
restored, and the marker-policy suite remains green.

The schema now preserves an absent `guardedTipTransition` while semantic
consumers locally normalize absence to no transition. New seeds continue to
emit explicit null, non-null guarded transitions round-trip, and changed seed
fields or seal references remain rejected. The marker was replaced by the
same-titled plain test and its annotation/inventory entry removed.

Mutation proof restored the old `.default(null)` parser and failed only the
new selector in both adapters with the original self-authentication error.
Restoration returned 2/2 cases green; the production source SHA-256 returned to
`0b499322e33dbac39e350222bbc996699d58a04dc4362fa1c9404d2fe51b2560`
and the regression source SHA-256 returned to
`0df6eace2d5c569df1b59f68603c192d4e1d0b60e4f92bfa36c5ce716b5cc072`.

The retained-journal, recovery-epoch, and current-recovery suites pass 58 tests
with zero failures. The required marker-policy and closure sequence passes 50
tests, followed by green typecheck and lint. The unfiltered five-file matrix
passes 110 tests with one PostgreSQL skip; its sole failure remains the
installed-successor selector timing out before the controlled provider receives
its first acquire message. The selector reproduces in isolation under the
ambient, pinned-Node, minimal-PATH, direct-`/proc`, and CQ-clean environments,
before any recovery parser or successor bridge request executes.

The round-1 composition was reconstructed from G219 commit/tree
`eec3c325097f684c04098faa2e55bb5108901707` /
`d38f390e7a177c5de54e701632e3f56c54df0e05`, D510 commit/tree
`811e44c2632597ab7d9b80b0d0591727f7303756` /
`eebc692cd0c79141c148d83e5e9905bd8eaf3b44`, and this task's pre-evidence
tip/tree `10893a063a0608c7e5443ca537b800d3bb2eed00` /
`dd0f84bec5d64a635013d8bd353dae8a318c4d94`. The reconciled composition
passes 87 guarded-rebase, retained-seal, recovery-promotion, capture, and queue
tests with zero failures, plus 9 combined G219/T6576 supervised-lineage tests
with zero failures. Its deterministic source manifest SHA-256 (sorted paths and
file hashes, excluding `node_modules`, the closure manifest, and WIP evidence)
is `3687067a52367afd5c2284f00b3301eb625dc95a637b02d2f1127d92f5af03f2`.
The disposable copy intentionally has no workspace-package links, so a
whole-workspace TypeScript invocation there fails module resolution before
checking the changed files; the authoritative task worktree's typecheck and
lint remain green.
