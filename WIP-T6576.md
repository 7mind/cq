```json
{
  "taskId": "T6576",
  "role": "implement-worker",
  "baseCommit": "9272de7b3cd5cf8ebe41abb42c514b8421cb3f9b",
  "startedAt": "2026-09-20T11:16:42Z",
  "checkpoints": [
    { "name": "base verification", "status": "done" },
    { "name": "fail-first regressions", "status": "done" },
    { "name": "receipt reconciliation correction", "status": "done" },
    { "name": "composed enrollment correction", "status": "done" },
    { "name": "current-seal correction", "status": "done" },
    { "name": "mutation proof", "status": "done" },
    { "name": "focused checks", "status": "done" },
    { "name": "composition checks", "status": "done" },
    { "name": "round 1 lossless recovery seal parsing", "status": "done" },
    { "name": "round 2 fail-first regressions", "status": "done" },
    { "name": "round 2 retained staged and guarded ancestry", "status": "done" },
    { "name": "round 2 mutation proof", "status": "done" },
    { "name": "round 3 base verification", "status": "done" },
    { "name": "round 3 old-producer recovery regression", "status": "done" },
    { "name": "round 3 authenticated legacy recovery correction", "status": "done" },
    { "name": "round 3 mutation proof", "status": "done" },
    { "name": "round 3 focused checks", "status": "done" },
    { "name": "round 4 base verification", "status": "done" },
    { "name": "round 4 portable legacy fixture", "status": "done" },
    { "name": "round 4 guarded recovery diagnostics", "status": "done" },
    { "name": "round 4 mutation proof", "status": "done" },
    { "name": "round 5 base verification", "status": "done" },
    { "name": "round 5 cancelled guarded suffix regression", "status": "done" },
    { "name": "round 5 guarded suffix assembly correction", "status": "done" },
    { "name": "round 5 mutation proof", "status": "done" },
    { "name": "round 5 focused checks", "status": "done" },
    { "name": "round 5 composition checks", "status": "done" },
    { "name": "round 6 base verification", "status": "done" },
    { "name": "round 6 composed fail-first regressions", "status": "done" },
    { "name": "round 6 composed corrections", "status": "done" },
    { "name": "round 6 mutation proof", "status": "done" },
    { "name": "round 6 focused checks", "status": "done" },
    { "name": "round 6 composition checks", "status": "done" },
    { "name": "round 7 base verification", "status": "done" },
    { "name": "round 7 source reconciliation", "status": "done" },
    { "name": "round 7 mutation proof", "status": "done" },
    { "name": "round 7 focused checks", "status": "done" },
    { "name": "round 8 base verification", "status": "done" },
    { "name": "round 8 active-checklist reconciliation", "status": "done" },
    { "name": "round 8 WIP contract check", "status": "done" },
    { "name": "round 9 base verification", "status": "done" },
    { "name": "round 9 cancelled recovery fail-first regressions", "status": "done" },
    { "name": "round 9 current-seal recapture correction", "status": "done" },
    { "name": "round 9 mutation proof", "status": "done" },
    { "name": "round 9 focused checks", "status": "done" },
    { "name": "round 10 base verification", "status": "done" },
    { "name": "round 10 old-producer cancellation regression", "status": "done" },
    { "name": "round 10 receipt-closure correction", "status": "done" },
    { "name": "round 10 mutation proof", "status": "done" },
    { "name": "round 10 focused checks", "status": "done" },
    { "name": "round 13 base verification", "status": "done" },
    { "name": "round 13 fresh-manual-rebase reproduction", "status": "todo" },
    { "name": "round 13 inherited-and-fresh closure correction", "status": "todo" },
    { "name": "round 13 mutation proof", "status": "unmeasured" },
    { "name": "round 13 focused checks", "status": "unmeasured" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## base verification <!-- cq:wip-checkpoint -->

Verified the prepared worktree, `implement/T6576` branch, exact starting commit,
base commit object, and base ancestry.

## fail-first regressions <!-- cq:wip-checkpoint -->

Added three independently titled public `test.failing` regressions with literal
`tasks:T6576` annotations and matching inventory entries. Plain-test probes
failed at the intended production boundaries:

- `durable continuation receipts do not have one chain start` for the second
  genuine guarded-rebase transaction;
- terminal enrollment authority `cannot be resurrected` for the composed
  gate-rejected, current-recovery, staged-retirement, guarded-successor lineage;
- `journal recovery successor does not inherit the preceding receipt closure`
  while recapturing a cancelled guarded successor across a staged-retired
  recovery source.

All markers were restored after the probes; the marker-contract and inventory
policy tests passed.

## receipt reconciliation correction <!-- cq:wip-checkpoint -->

Guarded-rebase finalization now selects the unique connected receipt component
bound to the journal's head, original tip, rebase target, conflict identity,
and observed live tip. Every durable receipt is still checked against its own
journal before component selection. The public regression passes as a plain
test (1 pass, 0 fail), including an exact replay, a second independent genuine
conflict, preservation of the first journal bytes, and a clean
completed-but-unrecorded recovery after historical conflicts.

## composed enrollment correction <!-- cq:wip-checkpoint -->

Queue ancestry now composes through a genuine gate-rejected correction and an
authenticated staged recovery edge without treating unrelated terminal rows as
authority. Staged retirement no longer erases an authentic relation, while the
exact source, successor, queue, result, receipt, repository, worktree, manager,
and guarded-rebase coordinates remain mandatory. The composed public regression
passes for both memory and SQLite backends (1 test body, 0 failures).

## current-seal correction <!-- cq:wip-checkpoint -->

Current-recovery seals now retain one authenticated guarded-tip transition
between the pre-rebase and post-rebase receipt components. That transition is
validated and propagated through persisted dispatch bindings, result evidence,
continuation confirmation, queue admission, and seal promotion. The regression
recaptures after staged retirement plus cancellation, then resumes through a
new receipt, qualification, and successful supervised completion (1 pass, 0
fail). All 24 dispatch-recovery-seal tests pass after preserving strict-maximal
selection semantics for stale candidates.

## focused checks <!-- cq:wip-checkpoint -->

The five requested files pass with only the independently diagnosed baseline
selector excluded: 110 pass, 1 skip, 0 fail. The exact unfiltered command was
also run. Its sole remaining failure is the installed-successor process harness,
which times out after 30 seconds before issuing a bridge request. The identical
selector extracted from exact base `9272de7b3cd5cf8ebe41abb42c514b8421cb3f9b`
has the same timeout, pass/filter counts, and one-assertion signature.

The prescribed closure sequence passes with 50 tests, followed by green
`bun run typecheck` and `bun run lint`.

## mutation proof <!-- cq:wip-checkpoint -->

Each changed invariant was disabled independently and then restored:

- replacing transaction-scoped receipt selection with global ordering failed
  the receipt regression with `durable continuation receipts do not have one
  chain start`; restored source SHA-256
  `c9cac4878348dff5cceba6b0095eb2b91c6809365a7af025a970ecceaf0b5034`;
- disabling gate-rejected correction ancestry failed with terminal enrollment
  resurrection rejection; restored source SHA-256
  `067be87cc243a61e4beab92afedadda97b1d938e58eb0f968ee74fd4e249063e`;
- disabling authenticated staged recovery edges failed with `journal recovery
  successor does not inherit the preceding receipt closure`; restored source
  SHA-256
  `0319792d91d4b6f6b1129eb825c2336d50ea24baacc3e97f005ffebdf2da4ce8`.

Every corresponding plain regression passed after restoration.

## composition checks <!-- cq:wip-checkpoint -->

Applied D510 commit `811e44c2632597ab7d9b80b0d0591727f7303756`
to G219 commit `eec3c325097f684c04098faa2e55bb5108901707`, then
three-way merged the T6576 base-to-tip delta. Overlaps were reconciled
explicitly so G219's retained guarded bridge does not get re-applied after a
T6576 receipt transition has superseded it, and staged-retired rows remain
ancestry intermediates without becoming resurrection blockers.

The reconciled composition passed 6 guarded-rebase tests; 50 queue,
recovery-seal, and runtime tests with 1 PostgreSQL skip; all 9 combined G219 and
T6576 supervised lineage scenarios; the repository closure test; typecheck;
and lint. The verified composed source-tree identity, excluding this
self-referential WIP evidence artifact, is
`394b7f8bcc9df4991ce79e1e040a62676d6228e2`.

## round 1 lossless recovery seal parsing <!-- cq:wip-checkpoint -->

Reproduce the authenticated-byte mismatch for retained recovery seals whose
`guardedTipTransition` field is absent, preserve the distinct absent and
explicit-null encodings, correct the format boundary without weakening digest
or reference verification, and rerun the focused and composition checks.

The same-titled plain regression failed for both the in-memory and filesystem
journal adapters with `recovery seal digest is not self-authenticating`; the
expected-failure marker, `tasks:T6576` annotation, and inventory entry were
restored, and the marker-policy suite remains green.

The schema now preserves an absent `guardedTipTransition` while semantic
consumers locally normalize absence to no transition. New seeds continue to
emit explicit null, non-null guarded transitions round-trip, and changed seed
fields or seal references remain rejected. The marker was replaced by the
same-titled plain test and its annotation/inventory entry removed.

Mutation proof restored the old `.default(null)` parser and failed only the
new selector in both adapters with the original self-authentication error.
Restoration returned 2/2 cases green; the production source SHA-256 returned to
`0b499322e33dbac39e350222bbc996699d58a04dc4362fa1c9404d2fe51b2560`
and the regression source SHA-256 returned to
`0df6eace2d5c569df1b59f68603c192d4e1d0b60e4f92bfa36c5ce716b5cc072`.

The retained-journal, recovery-epoch, and current-recovery suites pass 58 tests
with zero failures. The required marker-policy and closure sequence passes 50
tests, followed by green typecheck and lint. The unfiltered five-file matrix
passes 110 tests with one PostgreSQL skip; its sole failure remains the
installed-successor selector timing out before the controlled provider receives
its first acquire message. The selector reproduces in isolation under the
ambient, pinned-Node, minimal-PATH, direct-`/proc`, and CQ-clean environments,
before any recovery parser or successor bridge request executes.

The round-1 composition was reconstructed from G219 commit/tree
`eec3c325097f684c04098faa2e55bb5108901707` /
`d38f390e7a177c5de54e701632e3f56c54df0e05`, D510 commit/tree
`811e44c2632597ab7d9b80b0d0591727f7303756` /
`eebc692cd0c79141c148d83e5e9905bd8eaf3b44`, and this task's pre-evidence
tip/tree `10893a063a0608c7e5443ca537b800d3bb2eed00` /
`dd0f84bec5d64a635013d8bd353dae8a318c4d94`. The reconciled composition
passes 87 guarded-rebase, retained-seal, recovery-promotion, capture, and queue
tests with zero failures, plus 9 combined G219/T6576 supervised-lineage tests
with zero failures. Its deterministic source manifest SHA-256 (sorted paths and
file hashes, excluding `node_modules`, the closure manifest, and WIP evidence)
is `3687067a52367afd5c2284f00b3301eb625dc95a637b02d2f1127d92f5af03f2`.
The disposable copy intentionally has no workspace-package links, so a
whole-workspace TypeScript invocation there fails module resolution before
checking the changed files; the authoritative task worktree's typecheck and
lint remain green.

## round 2 retained staged and guarded ancestry <!-- cq:wip-checkpoint -->

Reproduce the remaining public-runtime refusals for a real staged recovery
retirement followed by its exact guarded successor, including retained older
parent-lost or gate-rejected terminal history and a cancelled guarded successor
that must become the next authenticated current-seal epoch. Correct only the
authenticated transition predicates and preserve terminal causes and bindings.

Real queue retirement now preserves the authenticated recovery and continuation
claims while retaining the exact `aborted` / `staged-rebase` terminal cause.
Composed enrollment accepts that state only when the staged source binding is
self-authenticating and matches the queue, receipt closure, manager binding,
guarded journal, source/result/onto coordinates, and exact successor. The
legacy consumed-source transition remains admitted only with its durable
continuation closure.

The memory and SQLite public-runtime paths pass through a genuine stale-front
retirement and exact guarded successor, then a second clean guarded rebase with
exact replay and a changed brokered suffix. Current-seal capture likewise
authenticates an aborted staged source, its cancelled exact guarded successor,
and a later recovery worker; no recovery gate runs before that ordinary worker
reaches queue coordination.

## round 2 fail-first regressions <!-- cq:wip-checkpoint -->

Replaced the earlier synthetic consumed-source paths with public stale-front
coordination. The runtime now persists each staged source as `aborted` with
abort reason `staged-rebase`, and prepares the exact guarded successor through
the normal coordinator launcher.

With runner inversion temporarily removed, the historical-enrollment case
failed at qualification with `terminal enrollment authority ... cannot be
resurrected`; the cancelled-successor case failed during current-seal capture
with `staged recovery retirement does not authenticate its exact guarded
successor`. Both same-titled tests are restored as live `test.failing` markers
with `tasks:T6576` annotations and matching inventory entries.

## round 2 mutation proof <!-- cq:wip-checkpoint -->

Three independent mutations reproduced the protected refusals:

- dropping the retained journal-recovery claim failed the historical case at
  the earlier parent-lost enrollment with `cannot be resurrected`;
- removing aborted staged retirement from composed ancestry failed at the
  staged source with `cannot be resurrected`;
- withholding the aborted source receipt closure from current-seal progression
  failed with `staged recovery retirement does not authenticate its exact
  guarded successor`.

Restoration returned both same-titled selectors green. Final restored SHA-256
digests are
`68b08fe9c4479a55d414f85a808f3ffb2741c58066ecb3e9cc9dd0720a3bebec`
for `dispatchImplementationQueue.ts`,
`76223cb2e3774242b32710d536038095631e2bd4ded416b3e92f2a4ea837f656`
for `dispatchRecoverySeal.ts`, and
`b838c1b6542f1247a127f89f3008ae1f7692bf8117f6a08a36f4cb81db30c93c`
for the public regression file. Typecheck and lint pass after restoration.

## round 2 focused checks <!-- cq:wip-checkpoint -->

Unmeasured.

## round 2 composition checks <!-- cq:wip-checkpoint -->

Unmeasured.

## round 3 base verification <!-- cq:wip-checkpoint -->

Verified the prepared `implement/T6576` worktree at exact starting commit
`80c3b4bb1284f975f7e8b1da7193a4daf31e5b3a`, with the named main commit and
prior result both ancestors of the prepared head.

## round 3 old-producer recovery regression <!-- cq:wip-checkpoint -->

The pinned `04rs3iqgs02d5jb5awzrww84jphpgdr7` dispatch-capability factory now
drives the memory and SQLite scenarios through a real staged retirement. Both
legacy-produced aborted rows lack `dispatchContinuationClaim` and
`dispatchJournalRecoveryClaim`; after backend close/reopen, the current reader
rejects generation 2 with `terminal enrollment authority ... cannot be
resurrected`. The same-titled test is a live `test.failing` marker with its
`tasks:T6576` annotation and inventory entry, and the marker-policy tests pass.

## round 3 authenticated legacy recovery correction <!-- cq:wip-checkpoint -->

Composed ancestry now admits a legacy current-recovery intermediate without
the duplicated claim only when it is the next generation after an authentic
parent-lost terminal, its qualified result extends the exact inherited receipt
closure, its task/goal/manifest and manager coordinates match, and exactly one
authenticated staged-retirement journal binds it to its guarded successor.
The proof remains discoverable through later composed continuations. Mutations
to the receipt closure, guarded journal binding, repository identity, or
finalized-manifest authority all reject without allocating a queue row. The
same-titled plain memory/SQLite regression passes through the later clean
guarded rebase, and its temporary marker, annotation, and inventory entry are
removed.

## round 3 mutation proof <!-- cq:wip-checkpoint -->

Forcing the new legacy recovery-intermediate predicate to return false made the
plain public regression fail at the intended current-reader boundary with
`terminal enrollment authority ... #2 cannot be resurrected` (0 pass, 1 fail,
27 assertions before refusal). Restoring the predicate returned the same test
to 1 pass, 0 fail, 66 assertions. The restored production SHA-256 is
`6f221cc057da325a829daa4aa5321197dd478e7a2cfdb674f09f6fcc5293e5cb` and
the restored regression SHA-256 is
`15719844c1f63109f99a455805a3084096a156159fe8ecdcd78a22b23ed27f1e`.

## round 3 focused checks <!-- cq:wip-checkpoint -->

The five requested files pass 110 tests with one PostgreSQL skip when only the
known installed-successor process selector is excluded. The exact unfiltered
command also ran: the same 110 tests passed, while that selector alone timed
out at 30 seconds and its asynchronous bridge reader then observed the fixture
directory cleanup. This matches the prior confined-run signature; the process
fixture never reached the T6576 recovery predicate.

The prescribed marker-policy and closure sequence passes 50 tests with zero
failures after refreshing the two existing opaque-edge source digests for the
portable regression. `bun run typecheck` and `bun run lint` both exit zero.

## round 3 composition checks <!-- cq:wip-checkpoint -->

Unmeasured.

## round 4 base verification <!-- cq:wip-checkpoint -->

Verified the prepared `implement/T6576` worktree at exact starting commit
`1875a6b280f9e85fcc43f52f85191b4a71914284`; the named main commit is an
ancestor and the round-3 result is the exact current head.

## round 4 portable legacy fixture <!-- cq:wip-checkpoint -->

Replaced the machine-specific legacy queue source read with a portable JSON
round-trip fixture that carries the retained `04rs` producer path and SHA-256
as provenance. Substituting an absent source path had failed before either
backend ran with `ENOENT` (0 pass, 1 fail); the revised fixture has no host path
dependency and still removes only the two post-legacy claims before the current
reader reopens the backend.

## round 4 guarded recovery diagnostics <!-- cq:wip-checkpoint -->

Reproduce the remaining recovery receipt-closure refusal across staged
retirement and its exact guarded successor, then correct only the authenticated
bridge predicate and record its diagnostic subreason.

The same-titled public memory/SQLite regression now advances protected HEAD
before coordinating the first exact-tip guarded candidate, proves its retired
queue attempt has an empty fresh receipt suffix, fetches and cancels the exact
second guarded successor, and calls recovery at that successor's starting tip.
As a plain test it reaches `assertReceiptChain` and fails with `recovery receipt
closure must be non-empty` (0 pass, 1 fail, 32 assertions). Its `test.failing`
marker, `tasks:T6576` annotation, and inventory entry were restored for the
fail-first checkpoint.

Recovery seals now retain an ordered authenticated transition chain while
preserving the historical absent/null/single-transition encodings byte for
byte. Receipt validation walks receipts and guarded bridges as one event
sequence, so consecutive exact-tip rebases may share a receipt prefix without
fabricating a receipt. Staged edges append either a complete ordinary recovery
closure or the post-guarded receipt component and reject foreign, divergent,
reordered, gapped, or tip-mismatched components with bounded subreasons.

The transition chain propagates through persisted Git bindings, backend
validation, broker result validation, recovery completion, and implementation
enrollment. Composed enrollment matches the exact latest staged bridge while
the binding authenticates every earlier bridge. The public regression now
passes for both memory and SQLite through two guarded retirements, an empty
exact-tip component, cancellation, recovery recapture, a fresh ordinary
receipt, and queue qualification (1 test body, 70 assertions). Its marker,
annotation, and inventory entry are removed while the same-titled plain test
remains. Blackbox-Atomic journal controls cover fresh and empty intervening
components for both seal versions and both journal adapters, including changed
old tips, reordered transitions, gapped prefix lengths, and substituted latest
edges (25 tests, 0 failures for the journal file).

## round 4 mutation proof <!-- cq:wip-checkpoint -->

Four independent mutations failed at the protected boundary and were restored:

- treating a complete ordinary recovery closure as a fresh component made the
  public regression fail with `staged recovery receipt suffix 0 is foreign or
  does not continue its authenticated source tip`;
- disabling old-result continuity made the repeated-transition control accept
  a forged old tip, so both adapter cases failed because the expected rejection
  did not occur;
- withholding the multi-transition binding reduced recovery to the latest edge
  and failed preparation with `transition does not start at the preceding
  authenticated tip`;
- restoring task-wide last-receipt matching rejected ordinary qualification
  with `terminal enrollment authority ... cannot be resurrected`.
- sending a one-edge journal through the multi-edge broker field failed the
  existing staged-successor regression with `receipt-chain transition sequence
  does not end at its current edge`; restoration keeps historical single-edge
  bytes on the singular field and reserves the sequence field for two or more
  edges.

Restored SHA-256 digests are
`dc886ec64b07aa14a3461b32dc32d852673098f62e7ae3dcc06784993e9736a6`
for `dispatchRecoverySeal.ts`,
`ca0e79e279c492e22198bb611c1b7736c3b699c4e32fa62dba47f4457e9b5ac6`
for `currentRecoverySeal.ts`,
`2f74c6ee9a9db64436fda15d0d0dc9ec8dc8b47bfa91a49785f5669eddc968fa`
for `dispatchCapability.ts`, and
`8b6f9b01dcb72de5add6aa41e5078b8205b9cf7486c7ba650d0de2a00c7ccce0`
for `dispatchImplementationQueue.ts`.

The prescribed unfiltered five-file matrix ran once. It passed 109 tests with
one PostgreSQL skip. Its two failures were the previously recorded installed-
successor process timeout (including the same post-cleanup bridge-reader
`ENOENT`) and the newly exposed one-edge encoding regression. After restricting
the sequence field to two or more edges, the exact failing selector and the new
repeated-edge selector pass together (2 pass, 0 fail, 120 assertions). The
known installed-successor process selector is outside the recovery predicate
and still requires the parent-side unconstrained check recorded in the prior
round guidance.

## round 4 focused checks <!-- cq:wip-checkpoint -->

Unmeasured.

## round 4 composition checks <!-- cq:wip-checkpoint -->

Unmeasured.

## round 5 base verification <!-- cq:wip-checkpoint -->

Verified the prepared `implement/T6576` worktree at exact starting commit
`e48a2401dcbf9df08cdc19c41164d7c56953e74e`; the named main commit is an
ancestor and the round-4 result is the exact current head.

## round 5 cancelled guarded suffix regression <!-- cq:wip-checkpoint -->

The retained `04rs` historical-enrollment fixture now creates one brokered
receipt on the second guarded successor before cancellation. Both memory and
SQLite paths reopen their backend and reach the intended current-seal refusal;
the live `tasks:T6576` expected-failure marker and inventory entry keep that
known refusal green.

A separate same-boundary public regression runs zero, one, and two fresh
guarded receipts after backend reopen for both memory and SQLite. Those
single-bridge cases already pass (150 assertions), so they remain a plain test;
only the repeated-bridge historical case is a progression marker. Marker policy
and contract checks pass with the live marker.

## round 5 guarded suffix assembly correction <!-- cq:wip-checkpoint -->

The current-seal resolver now distinguishes an already-complete closure from a
fresh component contributed by the exact guarded successor. A component is
assembled with its authenticated prefix only after its task, generation,
attestation, old-head chain, live tip, guarded journal, onto, original result,
rebased start, and receipt-prefix length match the retained transition. The
assembled closure enters strict maximal-source selection only once.

Recovery preparation now derives the sealed live tip from the last authenticated
event: the current guarded start for an empty component, or the last receipt tip
when receipts follow that transition. The same-titled historical regression is
plain and passes for memory and SQLite (70 assertions); its marker, annotation,
and inventory entry are absent. The separate zero/one/two-receipt reopen matrix
passes for both adapters, including malformed, missing, foreign, and divergent
durable-journal component rejection with committed-journal preservation (150
assertions).

## round 5 mutation proof <!-- cq:wip-checkpoint -->

Three independent mutations failed at their protected boundary:

- bypassing pre-selection suffix assembly restored the original `recovery
  guarded-tip transition 0 exceeds the receipt closure` refusal (0 pass, 1
  fail, 32 assertions);
- deriving the sealed live tip from the guarded start even when receipts follow
  it rejected resumed preparation with `journal recovery seed receipt closure
  does not end at the live tip` (0 pass, 1 fail, 33 assertions);
- forcing the zero-receipt matrix arm into the fresh-journal control path failed
  immediately with `zero-receipt mutation observed`, proving the explicit zero
  component arm executes independently of the fresh component controls.

Restoration returned both public selectors green. Restored SHA-256 digests are
`d19262e0862ed8a0f57b09577bf5593e2a0684203dac8a6b0323ad934cd46ca1`
for `dispatchRecoverySeal.ts`,
`b41c6407c00eafe90770e4405d7a56952b677339abae02e4ceef920bda57ae8f`
for `dispatchCapability.ts`, and
`0f95e9fec902b733244dcb6fbdafd6cc2955dd7efea0c53b4245ea46a40a1612`
for the public regression file.

## round 5 focused checks <!-- cq:wip-checkpoint -->

The exact unfiltered five-file matrix ran once. All T6576 selectors passed; the
matrix reported 111 pass and one PostgreSQL skip. Its only failure was the
retained installed-successor process selector timing out at 30 seconds, followed
by the same bridge-reader `ENOENT` after fixture cleanup (1 fail, 1 asynchronous
error). This is the selector already A/B-reproduced on exact base
`9272de7b3cd5cf8ebe41abb42c514b8421cb3f9b` in prior rounds and is outside the
current recovery predicates.

The same five files with only that diagnosed selector excluded pass 111 tests
with one PostgreSQL skip and zero failures. The prescribed marker-policy and
closure sequence passes 50 tests with zero failures after refreshing the four
existing closure entries whose task-lineage sources changed. `bun run
typecheck` and `bun run lint` both exit zero. The lint pass required removing
the obsolete `previousHead` assignment left behind when T6576 moved chain
ordering into authenticated transition traversal; no receipt behavior changed.

## round 5 composition checks <!-- cq:wip-checkpoint -->

Reconstructed the prescribed disposable composition from rooted main
commit/tree `9272de7b3cd5cf8ebe41abb42c514b8421cb3f9b` /
`d0ad881c0385085ec44cae11f627b97f2e18c374`, G219 commit/tree
`eec3c325097f684c04098faa2e55bb5108901707` /
`d38f390e7a177c5de54e701632e3f56c54df0e05`, D510 commit/tree
`811e44c2632597ab7d9b80b0d0591727f7303756` /
`eebc692cd0c79141c148d83e5e9905bd8eaf3b44`, and the frozen round-5
task tip/tree `85d0d2f645837615a31492bd0fe99273e83f57e1` /
`bcd910548e18a67a8571a9d19a33fa30a6f837c2`.

The explicit overlap resolution keeps G219's authenticated retained bridge
until the first guarded transition supersedes it, then derives recovery base
and live tip from the transition without reapplying the old bridge. It also
retains D510's transaction-scoped receipt selection and both tasks' public
supervised-lineage controls.

On the exact composed bytes, the guarded-rebase journal suite passes 6 tests
with zero failures and 48 assertions. The ten combined G219/T6576 automatic,
manual, staged-retired, correction, and zero/one/two-receipt supervised
scenarios pass with zero failures and 542 assertions. The repository closure
and closure-contract suites pass 40 tests with zero failures and 355
assertions. `bun run typecheck` and `bun run lint` both exit zero.

The deterministic sorted path/SHA-256 source manifest, excluding copied
`node_modules`, generated `dist` output, the closure manifest, and this WIP
artifact, is
`938023e1e31478692117fa7049f71baa0bd8c257d70b0aa33f615c875fe57f7e`.

## round 6 base verification <!-- cq:wip-checkpoint -->

Verified the prepared `implement/T6576` worktree at exact starting commit
`75b686b765312a2e4e29b5739bb4c3ea48581f99`; the named main commit and the
round-5 result are ancestors of the prepared head.

## round 6 composed fail-first regressions <!-- cq:wip-checkpoint -->

The four operator-specified failures were reproduced in one disposable
composition before their corrections:

- the guarded-origin managed recovery regression failed with `input.baseCommit:
  journal recovery baseCommit differs from managed binding`;
- after the resumed worker persisted a broker receipt, aborting before result
  storage and resolving at the new head failed with `journal recovery successor
  input is not bound to preceding epoch` (0 pass, 1 fail, 21 assertions);
- a red G219 gate, changed correction, consumption, and ordinary continuation
  failed with `stage=continuation-ancestry; reason=ancestry-unverified` (0 pass,
  1 fail, 35 assertions);
- the whole supervised-storage file reproduced the SQLite
  `dispatch_attestations.backend, project_key, capability_hash` uniqueness
  collision. The collision followed from restart multipliers congruent modulo
  256, not from the independently retained installed-successor timeout.

The current-branch expected-failure protocol was also exercised for the
staged-retired case: the same title first failed as a plain test, then existed
as a live `test.failing` marker with `tasks:T6576` and a matching inventory
entry while the marker-contract/policy suite remained green. The correction
replaced it with the same-titled plain test and removed the annotation and
inventory entry.

## round 6 composed corrections <!-- cq:wip-checkpoint -->

Current recovery now derives the logical base from the latest authenticated
guarded transition, accepts the selected generation's receipt component when
it closes at the live tip, and carries that authenticated transition through
receipt-chain verification. This task branch contains that shared correction
in `currentRecoverySeal.ts` and `dispatchRecoverySeal.ts`, plus the public
staged-retired/cancelled regression.

The disposable exact composition additionally re-verifies the retained bridge
before managed base validation, evicts it after an authenticated current-epoch
transition supersedes it, persists and authenticates the gate-rejected
correction claim, validates the real staged-rebase predecessor before recursive
ancestry, and uses non-aliasing restart seed offsets. Those G219/D510-owned
overlaps are published in `/tmp/t6576-composition-r6-6VVKdf` for owner adoption;
they are not imported blindly into the narrower task branch.

## round 6 mutation proof <!-- cq:wip-checkpoint -->

Three independent production mutations on the current branch failed the
same-titled staged-retired regression and were restored:

- removing latest-transition `ontoCommit` precedence failed with `journal
  recovery successor input is not bound to preceding epoch`;
- withholding authenticated receipt-chain transition context failed with
  `inherited receipt chain diverges at entry 2`;
- restoring the stale predicate that required the selected source to be the
  transition successor failed with `recovery seal has inconsistent seed
  bindings`.

Each mutation reported 0 pass, 1 fail, and 21 assertions. Restoration returned
the selector green for memory and SQLite (1 pass, 0 fail, 46 assertions).
Restored SHA-256 digests are
`26dbbebdb488ff56bbe1ee3496e6be9e87fdc7f79ee7c227a9ba29cb5c2899ca`
for `dispatchRecoverySeal.ts`,
`0f90c9129c3e709e6ddddde0629de3f8bb9bd5b1a3ea40abdc06347300f3544b`
for `currentRecoverySeal.ts`, and
`1f4d5f4d78f32294f83547e7f5135f7b41b650746c3099152afdfa5ee43dd96d`
for the regression file. The closure manifest itself failed closed with each
stale declared digest and passed after both declarations were restored to these
bytes.

## round 6 focused checks <!-- cq:wip-checkpoint -->

On the task branch, the six-file guarded-rebase, queue, recovery-seal,
supervised-storage, runtime, and managed-recovery matrix passes 114 tests with
one PostgreSQL skip and zero failures when excluding only the previously
A/B-reproduced installed-successor process selector. The marker-policy and
repository-closure sequence passes 50 tests with zero failures and 597
assertions. `bun run typecheck` and `bun run lint` both exit zero.

## round 6 composition checks <!-- cq:wip-checkpoint -->

The exact composition starts from the retained read-only source
`/nix/store/mp15xrmb8mps2klc3cpcgqwpsdn90619-cq-verified-fused-recovery-source`
and is published at `/tmp/t6576-composition-r6-6VVKdf`. Its rooted identities
are main commit/tree `9272de7b3cd5cf8ebe41abb42c514b8421cb3f9b` /
`d0ad881c0385085ec44cae11f627b97f2e18c374`, G219 commit/tree
`eec3c325097f684c04098faa2e55bb5108901707` /
`d38f390e7a177c5de54e701632e3f56c54df0e05`, D510 commit/tree
`811e44c2632597ab7d9b80b0d0591727f7303756` /
`eebc692cd0c79141c148d83e5e9905bd8eaf3b44`, frozen round-5 task
commit/tree `85d0d2f645837615a31492bd0fe99273e83f57e1` /
`bcd910548e18a67a8571a9d19a33fa30a6f837c2`, and round-6 shared-correction
commit/tree `451f435f558cad5cc7826b949b1e2d82939f6a99` /
`c453f368be203b3854094ee8ce29e538705d0bd6`.

On those exact composed bytes, the six-file behavioral matrix passes 123 tests
with one PostgreSQL skip and zero failures; the retained-bridge, staged-retired,
and gate-rejected/ordinary-continuation selectors all pass in that ordering.
The whole supervised file no longer reports a capability-hash uniqueness
collision; its only red selector is the previously diagnosed
installed-successor timeout and ensuing cleanup `ENOENT`. The exact composed
repository closure passes 40 tests with zero failures and 355 assertions, the
expected-failure policy passes 10 tests with zero failures and 242 assertions,
and typecheck and lint exit zero.

The deterministic sorted path/SHA-256 source manifest, excluding copied
`node_modules`, generated `dist` output, the closure manifest, and WIP evidence,
is `457c0d2b799d98126e98c1a5d15e17a6f819e2cb965a4425f392599bac68f642`.

## round 7 base verification <!-- cq:wip-checkpoint -->

Verified the prepared `implement/T6576` worktree at exact starting commit
`a96daa2344fc3ede464f8f56b9a5d732e1a5c8e6`; the named main commit is an
ancestor and the round-6 result is the exact current head.

## round 7 source reconciliation <!-- cq:wip-checkpoint -->

The corrected flat source is published at
`/tmp/t6576-composition-r7-zM6tDd`. The parent-build context, including the
reconciled closure manifest, is published at
`/tmp/t6576-composition-context-r7-frPXZx`; its package root is
`/tmp/t6576-composition-context-r7-frPXZx/nix/pkg/cq-ledgers`.

Starting from the read-only flat source
`/nix/store/90spp7v3fzf2k4prrn07qd7s31hf3yix-cq-verified-fused-recovery-source`,
the committed G219 delta between `eec3c325097f684c04098faa2e55bb5108901707`
and `38e2093a742cf31caf189c5bc4e1a03e1f23a8a6` was reconciled manually at the
two shared source boundaries. The retained source already contained G219's
claim envelope/codec, non-aliasing seeds, and T6576 staged-predecessor
transition. The final composition adds only G219's authenticated legacy
correction-claim reconstruction and restarted-reader regression while
preserving T6576's guarded transition chain and current-seal recovery cases.

The base flat-source manifest is
`457c0d2b799d98126e98c1a5d15e17a6f819e2cb965a4425f392599bac68f642`,
matching the recorded round-6 convention. The corrected deterministic sorted
path/SHA-256 manifest, excluding `node_modules`, generated `dist`, the closure
manifest, and WIP evidence, is
`c7db54d17363bdac706eb5df2279eca9b38cd6877239ac65b119395916c5c78c`.
The same manifest is obtained from both published package roots. Corrected
source SHA-256 values are
`a90e21a64adf0fa9899aeee1ad8116dd47a2bcaaeab769d5b4e7ca8703a1e4f2`
for `dispatchCapability.ts` and
`f6d0b547fa23aa564d5ec5aaac9b05796c4be2cfb21e3efa78ed8a6c1f4e808b`
for `supervisedWorkerGateStorage.test.ts`. The reconciled
`cq-gate-closure.json` SHA-256 is
`62cc4999cb4712930e38dff78043929e8b3b920a40b7db89f5522d6d743e9e1d`.

The exact rooted identities are main commit/tree
`9272de7b3cd5cf8ebe41abb42c514b8421cb3f9b` /
`d0ad881c0385085ec44cae11f627b97f2e18c374`, G219 source commit/tree
`eec3c325097f684c04098faa2e55bb5108901707` /
`d38f390e7a177c5de54e701632e3f56c54df0e05`, G219 correction commit/tree
`38e2093a742cf31caf189c5bc4e1a03e1f23a8a6` /
`b632546498e7fe321a033e83d742fdde79677dda`, D510 commit/tree
`811e44c2632597ab7d9b80b0d0591727f7303756` /
`eebc692cd0c79141c148d83e5e9905bd8eaf3b44`, T6576 round-6 narrow product
commit/tree `451f435f558cad5cc7826b949b1e2d82939f6a99` /
`c453f368be203b3854094ee8ce29e538705d0bd6`, and round-6 evidence tip/tree
`a96daa2344fc3ede464f8f56b9a5d732e1a5c8e6` /
`fbdfaaf1791f3f173d962a01c07e12dd6500f477`. No G219, T6558, or T6560 branch
commit was imported into the managed T6576 branch.

## round 7 mutation proof <!-- cq:wip-checkpoint -->

With only the composed reader's legacy correction-claim fallback disabled, the
actual retained `r3fp` producer case failed at the intended current-reader
boundary with `stage=continuation-ancestry; reason=ancestry-unverified`: zero
passes, one failure, and 35 assertions before refusal. Restoring the final
production bytes returned the identical old-producer case to one pass, zero
failures, and 70 assertions across memory and SQLite. The temporary producer
import was then removed. The restored source hashes and deterministic manifest
exactly match the values recorded above.

## round 7 focused checks <!-- cq:wip-checkpoint -->

The committed G219 correction and case-C selectors pass together: 2 tests, 84
assertions, zero failures. A separate version-crossing probe used the actual
retained `r3fpiyyfqhh4qf4mx057pxj2jmr9h3sk-cq-0.0.1` dispatch-capability
factory as producer, reopened the durable memory and SQLite histories with the
composed reader, and passed the corrected ordinary continuation: 1 test, 70
assertions, zero failures. The temporary old-producer import was removed and
the published regression file was restored to the hash recorded above.

The post-reconciliation six-file guarded-rebase, queue, recovery-seal,
supervised-storage, production-runtime, and managed-recovery matrix passes 123
tests with one PostgreSQL skip, 1,519 assertions, and zero failures. Only the
operator-excluded installed-successor process selector was filtered; its known
sandbox timeout was not repeated. `bun run typecheck` and `bun run lint` both
exit zero. In the parent-build context, the exact marker-policy and closure
sequence passes 50 tests with 597 assertions and zero failures, including the
repository closure check against the reconciled manifest.

## trusted full gate <!-- cq:wip-checkpoint -->

Unmeasured. This focused-only worker will not invoke the canonical full gate;
the trusted parent owns the single queue-front gate for the frozen candidate.

## round 8 base verification <!-- cq:wip-checkpoint -->

Verified the prepared `implement/T6576` worktree at exact starting commit
`38afde08c3e6629f59024bbc735b92df67f8eddb`; the named main commit is an
ancestor and the round-7 result is the exact current head.

## round 8 active-checklist reconciliation <!-- cq:wip-checkpoint -->

The active checklist no longer carries the superseded round-2 focused and
composition checks, round-3 composition check, or round-4 focused and
composition checks. Their historical sections remain byte-for-byte present,
including the recorded unmeasured and red evidence; no unrun historical check
was relabelled as green.

No source composition or product check was repeated in this prose-only round.
The operator reports that the retained round-7 proof used immutable source
`mjfwmj4mq4gvmglii6s3ff8drbjpqqgw`, included an artifact/source/runtime byte
audit, passed a 12-file host matrix with 361 tests, one PostgreSQL skip, zero
failures, and 3,036 assertions, and passed a 46-test Nix check with 531
assertions. These are parent observations, not checks performed by this worker.
The retained parent log is
`logs/20260920T191000Z-R7-exact-bounded-composition-verification.md`.

## round 8 WIP contract check <!-- cq:wip-checkpoint -->

A production-parser preflight assessed the committed intermediate artifact and
found exactly the two expected round-8 checkpoints open: 1 pass, 0 failures.
After completing them, the only non-done checkpoint is the exact runner-owned
`trusted full gate` entry below; the final closure projection is checked again
on the finished bytes before this round is stored.

## round 9 base verification <!-- cq:wip-checkpoint -->

Verified the prepared `implement/T6576` worktree at exact starting commit
`db1e525fccd83ae0dd222485496d82497f2114ab`; the named main commit is an
ancestor and the round-8 result is the exact current head.

## round 9 cancelled recovery fail-first regressions <!-- cq:wip-checkpoint -->

Reproduce the zero-fresh-receipt cancellation refusal after an authenticated
staged-retired source, its exact guarded successor, and a later ordinary
recovery generation. Preserve the distinct seed-binding and composed-ancestry
refusals as independently titled public regressions.

The existing staged-retired public scenario now cancels the later ordinary
recovery generation without writing a fresh receipt and resolves recovery at
the unchanged guarded tip. As a plain test it failed at
`recovery seal has inconsistent seed bindings` with 0 passes, 1 failure, and
21 assertions. Its same-titled `test.failing` marker, `tasks:T6576` annotation,
and matching inventory entry are live while production remains unchanged.

An independent committed-epoch regression retains the historical shape in
which a consumed recovery intermediate still carries staged-retirement queue
state, then appends an ordinary cancelled successor with the exact inherited
receipt closure and no guarded bridge. As a plain test it failed at
`staged recovery retirement does not authenticate its exact guarded successor`
with 0 passes and 1 failure. Its same-titled `test.failing` marker,
`tasks:T6576` annotation, and matching inventory entry are also live.

## round 9 current-seal recapture correction <!-- cq:wip-checkpoint -->

Current-seal semantics now recognize a later generation of the same
authenticated attestation lineage as continuing the latest guarded transition
when the worktree tip and receipt closure do not advance. Receipt ownership,
lineage maximum, transition ordering, transition authority, and live-tip
validation remain unchanged.

Staged-retirement inspection now runs only when the intermediate actually has
staged-retirement queue state and its proposed successor actually carries a
guarded bridge. A historical consumed intermediate with stale staged state and
an ordinary successor therefore follows the ordinary continuation validator,
which still requires exact generation, input, binding, receipt-prefix, and
terminal evidence.

Both expected-failure markers were replaced by same-titled plain tests and
their annotations and inventory entries were removed. The staged-source case
passes across memory and SQLite with 46 assertions. The historical-intermediate
case passes with its unchanged receipt closure. The previously masked
zero/one/two-fresh-receipt matrix now exercises six memory/SQLite arms and
passes with 160 assertions; its result fixture derives the actual net diff and
supplies the schema-required mutation evidence.

## round 9 mutation proof <!-- cq:wip-checkpoint -->

Two independent restored mutations reached the documented refusals:

- requiring the selected source generation to equal the guarded successor
  generation failed the staged-source regression with `recovery seal has
  inconsistent seed bindings` after 21 assertions; restoration passed both
  backends with 46 assertions and restored `currentRecoverySeal.ts` SHA-256
  `b0d5d287dc3397968acaacd9d5866133df8f0f8281f438b7e6740281e64e5c1b`;
- treating staged source state or a guarded bridge as independently sufficient
  failed the historical-intermediate regression with `staged recovery
  retirement does not authenticate its exact guarded successor`; restoration
  passed and restored `dispatchRecoverySeal.ts` SHA-256
  `c37f47da79be9621585e9b8f41a54f223b84a7b034b98997b0d88c0eaaf300e6`.

## round 9 focused checks <!-- cq:wip-checkpoint -->

Completed by the round-10 focused matrix on the retained round-9 bytes plus the
narrow receipt-closure correction: 112 pass, one PostgreSQL skip, zero fail
after excluding only the independently reproduced installed-successor harness.

## round 10 base verification <!-- cq:wip-checkpoint -->

Verified the prepared `implement/T6576` worktree at exact starting commit
`9b199e23e77a34162e293daced9660344683386e`; the named main commit is an
ancestor and the round-9 result is the exact current head. Retained uncommitted
round-10 test and closure-manifest edits are present from the cancelled prior
generation and will be preserved.

## round 10 old-producer cancellation regression <!-- cq:wip-checkpoint -->

The actual retained `r3fpiyyfqhh4qf4mx057pxj2jmr9h3sk-cq-0.0.1` producer
completed, stored, and consumed the corrected case-C result in the round-7
G219/T6576 disposable composition. The current reader alone then prepared and
materialized its ordinary continuation, cancelled it without a new commit, and
resolved recovery at the unchanged correction tip. Before the production edit,
the selector failed after 36 assertions with `intermediate recovery
continuation receipt closure is incomplete, divergent, or foreign`; there was
no fixture or import failure.

## round 10 receipt-closure correction <!-- cq:wip-checkpoint -->

The current-seal reader now distinguishes an absolute continuation closure
from a post-guarded component. A component is admitted only when its persisted
guarded bridge exactly matches the authenticated transition, its prefix length
is exact, every receipt belongs to the intermediate generation and continues
the expected head, and it terminates at the continuation's bound live tip. The
same rules apply at successor inheritance, intermediate continuation, and final
cancelled-source assembly. The native recovery-seal suite passes 25 tests with
74 assertions. In the reconciled G219/T6576 composition, G219's separate
gate-rejected correction claim remains composition-only; no G219 branch commit
or claim model was imported into T6576.

## round 10 mutation proof <!-- cq:wip-checkpoint -->

Three independent mutations ran against the actual retained `r3fp` producer
case C in the disposable composition:

- removing post-guarded inherited-prefix admission failed with `journal
  recovery successor does not inherit the preceding receipt closure`;
- removing intermediate component extension failed with `intermediate recovery
  continuation receipt closure is incomplete, divergent, or foreign`;
- removing final component assembly failed with `recovery guarded-tip
  transition 0 does not start at the preceding authenticated tip`.

Restoring the exact source bytes (SHA-256
`1daf31a1b127f9e32d6bb63a90c186ba48583a9a9297ff57a2ff2885ecc5be4c`
in the reconciled composition) returned the selector to one pass, zero
failures, and 74 assertions across memory and SQLite.

## round 10 focused checks <!-- cq:wip-checkpoint -->

The exact five-file command ran and reproduced only the pre-existing confined
installed-successor harness signature: 112 tests passed, one PostgreSQL case
skipped, while that selector timed out before its bridge request and its
asynchronous reader then observed fixture cleanup. The same selector/signature
was already A/B reproduced on exact base
`9272de7b3cd5cf8ebe41abb42c514b8421cb3f9b` in prior rounds.

Excluding only that unrelated selector, the same five requested files pass 112
tests with one PostgreSQL skip and zero failures. The required marker-policy and
repository-closure sequence passes 50 tests with 597 assertions, followed by
green `bun run typecheck` and `bun run lint`. The exact old-producer composition
selector passes with 74 assertions and zero failures. No full gate was invoked.

The recorded rooted identities are main commit/tree
`9272de7b3cd5cf8ebe41abb42c514b8421cb3f9b` /
`d0ad881c0385085ec44cae11f627b97f2e18c374`, G219 source commit/tree
`eec3c325097f684c04098faa2e55bb5108901707` /
`d38f390e7a177c5de54e701632e3f56c54df0e05`, G219 correction commit/tree
`38e2093a742cf31caf189c5bc4e1a03e1f23a8a6` /
`b632546498e7fe321a033e83d742fdde79677dda`, D510 commit/tree
`811e44c2632597ab7d9b80b0d0591727f7303756` /
`eebc692cd0c79141c148d83e5e9905bd8eaf3b44`, and the round-10 T6576
narrow product commit/tree `60bfee920948939f0ded89b545741c8cef60fef7` /
`38ed5119bb17036498c4d41762c65c49c16d0eb6`. The reconciled composition
source and old-producer regression SHA-256 values are respectively
`1daf31a1b127f9e32d6bb63a90c186ba48583a9a9297ff57a2ff2885ecc5be4c` and
`2d1d9e464d6eed6bdc557db543b73e4e3d7f2c7c89c14fcdfce462ce1bf70005`.
No G219, T6558, or T6560 branch commit was imported into the managed task
branch.

## round 13 base verification <!-- cq:wip-checkpoint -->

Verified the prepared `implement/T6576` worktree at exact starting commit
`0e44378b4175c8be4a291d975726f500e3c5c86c`; dispatched base commit
`6f88f702829d28f183fdd6e30d77f1b3972878c3` is an ancestor and the supplied
round-12 result is the exact current head.

## round 13 fresh-manual-rebase reproduction <!-- cq:wip-checkpoint -->

Reproduce the retained public memory and SQLite refusal for a cancelled,
unenrolled recovered source followed by a manually prepared guarded successor
that also cancels after writing fresh receipts. Require the documented
recovery-claim-tip refusal rather than a fixture or import failure.

## round 13 inherited-and-fresh closure correction <!-- cq:wip-checkpoint -->

Pending reproduction.

## round 13 mutation proof <!-- cq:wip-checkpoint -->

Unmeasured.

## round 13 focused checks <!-- cq:wip-checkpoint -->

Unmeasured.
