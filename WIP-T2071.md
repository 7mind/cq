```json
{
  "taskId": "T2071",
  "role": "implement-worker",
  "baseCommit": "56f50619d3d64dd42b4adf5e06fe0fd3250358bb",
  "startedAt": "2026-08-12T14:57:12Z",
  "checkpoints": [
    { "name": "critic-reproduction", "status": "done" },
    { "name": "v1-layer-repair", "status": "done" },
    { "name": "targeted-verification", "status": "unmeasured" },
    { "name": "full-gate", "status": "unmeasured" }
  ]
}
```

## critic-reproduction <!-- cq:wip-checkpoint -->

Add failure-first real-Git regressions for staged modification reversed in the
worktree and staged addition deleted in the worktree.

Both cases reached the published candidate, then repair refused with
`journal-invalid: v1 journal lacks staged worktree postimage <path>`. The
focused run executed two tests and failed both for that expected reason.

## v1-layer-repair <!-- cq:wip-checkpoint -->

Reconstruct missing overlay postimages relative to legacy HEAD and verify the
published worktree before installing the candidate-relative index.

Absent-overlay staged paths now derive their worktree postimage from legacy
HEAD and reject a published filesystem state with a different mode or object.
The two focused real-Git cases pass, retain both staged and unstaged path
deltas, and remain stable across repeated repair.

## targeted-verification <!-- cq:wip-checkpoint -->

Run the dispatched focused tests, typecheck, and lint with observed test counts.

## full-gate <!-- cq:wip-checkpoint -->

Run the authoritative CQ gate command in the foreground and record its result.
