```json
{
  "taskId": "T2231",
  "role": "implement-worker",
  "baseCommit": "fb5fbc528769223a6136c25c3dca58f139e7903f",
  "startedAt": "2026-08-18T16:17:25.000Z",
  "checkpoints": [
    { "name": "Step 0 base verification", "status": "done" },
    { "name": "D326 named bounds + derivation assertions", "status": "done" },
    { "name": "D326 admission test rework (markers, hold, absence)", "status": "done" },
    { "name": "Paired form-(c) detector (blocking/prompt child)", "status": "done" },
    { "name": "Focused runs green", "status": "done" },
    { "name": "Mutation evidence", "status": "done" },
    { "name": "Ten consecutive D326 runs", "status": "done" },
    { "name": "Full gate", "status": "todo" }
  ]
}
```
## Step 0 base verification <!-- cq:wip-checkpoint -->
Verified 2026-08-18: toplevel
`/home/pavel/work/safe/flakes/cq/.claude/worktrees/01a01592-80a1-70e8-87a2-ed772bf1279c`,
branch `implement/T2231`, HEAD == startingCommit == baseCommit
`fb5fbc528769223a6136c25c3dca58f139e7903f` (`git cat-file -t` = commit,
`merge-base --is-ancestor` exit 0, relation equal), clean tree. Round 0, no
priorResultCommit. Fail-first: per task dispatch, D341's recorded unchanged-main
(`8752a23fa98544bb3356e9b0c73210d685b5ea1e`) instrumented 7/10 exact
host-execution-deadline series stands as the reproduction; the intermittent
rejection is not re-required.

## D326 named bounds + derivation assertions <!-- cq:wip-checkpoint -->
Done in `packages/ledger-mcp/test/supervisedWorkerGateStorage.test.ts`: named
bounds FIRST_EXECUTION_TIMEOUT_MS=15,000, QUEUED_EXECUTION_TIMEOUT_MS=5,000,
ADMISSION_HOLD_MS=6,000, QUEUED_ADMISSION_TIMEOUT_MS=90,000,
D326_TEST_TIMEOUT_MS=300,000. Plain derivation test exposes
90,000 = 30,000 + 6,000 + 40,000 + 14,000, the 220,000 serial enclosure
(2×60,000 launch handshakes + 15,000/5,000 executions + 2×40,000 cleanups),
the overlap (90,000 ≤ 60,000+15,000+40,000 first-run share), and the 80,000
margin under the 300,000 test timeout.

## D326 admission test rework (markers, hold, absence) <!-- cq:wip-checkpoint -->
Done: both children write separate non-empty PID/PGID marker files before any
blocking; phase bounds are first=15,000 / queued=5,000 execution with 90,000
queued admission; the queued row is observed gate-running continuously for the
complete 6,000 ms hold after finalize(second) and before runner admission;
both stores resolve result-stored with one terminal row each; fixture lock
absent after settlement; direct kill(0)/kill(-pgid,0) absence checks for every
recorded identity; env restoration preserved. Focused run: admission test
passes in ~6.9 s.

## Paired form-(c) detector (blocking/prompt child) <!-- cq:wip-checkpoint -->
Done: `D341 pairs a deadline-killed blocking child with a prompt child under
the D326 bounds` drives the production `nodeSupervisedWorkerGateRunner`.
Blocking child (15,000 admission / 5,000 execution) publishes its PID/PGID
marker before blocking (marker observed before the rejection), rejects with the
exact `supervised worker gate exceeded its host execution deadline` message,
and both recorded identities are absent after settlement. Prompt child
(15,000 execution) exits 0 with positive passes and zero failures. No marker,
no inventory entry (form c). Passes in ~5.2 s.

## Focused runs green <!-- cq:wip-checkpoint -->
`-t 'D326'`: 4 pass / 0 fail (275 expects). Full file: 18 pass / 0 fail
(390 expects), tsc -b clean.

## Mutation evidence <!-- cq:wip-checkpoint -->
sha256 before mutations: supervisedWorkerGateStorage.test.ts fb1887cb5dbe…;
identical after every restoration (git status clean).
- M1 named bound QUEUED_ADMISSION_TIMEOUT_MS 90,000→80,000: derivation test failed
  `toBe(90_000)` receiving 80,000. Restored.
- M2 enclosure arithmetic (dropped one 40,000 cleanup term): derivation test failed
  `toBe(220_000)` receiving 180,000. Restored.
- M3 exact diagnostic expectation mutated to "supervised worker gate timed out":
  detector failed, receiving the exact `supervised worker gate exceeded its host
  execution deadline`. Restored.
- M4 marker absence expectation flipped to `toBe(false)`: detector failed
  (received true — the recorded child PID is absent after settlement). Restored.
- M5 lifecycle: first child's release-wait removed from the fixture script; the
  admission test failed in 1.08 s — the queued row reached result-stored during
  the 6,000 ms hold, breaking the gate-running observation. Restored.
Post-restore focused runs: `-t 'D326'` 4 pass / 0 fail; `-t 'D341 pairs'` 1 pass.

## Ten consecutive D326 runs <!-- cq:wip-checkpoint -->
`for attempt in $(seq 1 10); do bun test packages/ledger-mcp/test/supervisedWorkerGateStorage.test.ts -t 'D326 lets a second terminal store' || exit 1; done`
— 10 (pass) lines, LOOP_EXIT=0.

## Full gate <!-- cq:wip-checkpoint -->
Todo.
