## M48

### H18 — confirmed

- createdAt: 2026-06-03T20:40:23.945Z
- updatedAt: 2026-06-03T20:40:23.945Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: "D28 root cause: readLog validates real=realpath(resolved) but reads the non-canonical resolved (check-then-read TOCTOU)"
- description: "The containment check resolves and validates `real` (canonical), but the read uses fs.readFile(resolved) where resolved still contains symlink components, so a swap between check and read escapes. Fix: read the canonical `real`."
- evidence: ["[correct] packages/ledger/src/store/FsLedgerStore.ts readLog (post-T158, on main 311b8a1) — `const real = await fs.realpath(resolved)` validated against realLogsDir inside the try block, then AFTER the try: `const buf = await fs.readFile(resolved)`. The validated path (`real`) and the read path (`resolved`) differ; resolved follows symlinks at read time. Verified by Read. `real` is scoped to the try and not reused for the read — confirming the gap.","[correct] threat-model note: read_log is read-only + confined to the server's own docs/logs; an actor able to swap a symlink inside docs/logs already controls the returned directory contents — no privilege escalation. Hence severity low. (Reviewer-validated; orchestrator-confirmed.)"]
- ledgerRefs: ["defects:D28"]
