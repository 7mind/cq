## M35

### H13 — confirmed

- createdAt: 2026-06-03T10:12:56.590Z
- updatedAt: 2026-06-03T10:23:41.904Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: advance() helper's fixed 1500ms Enter-replay settle budget starves under CPU contention → multi-step-form test flakes
- description: "ROOT-CAUSE CANDIDATE (single). packages/ledger-tui/test/app.test.tsx 'creates an item via the multi-step form' (~L450) drives the create-item overlay via the advance() helper (~L201), which uses a fixed internal ms=1500 Enter-replay settle budget. Under full-suite CPU contention (notably when navMemo's N=200 mount ran first in the same bun process) the overlay does not advance to the next step within 1500ms, so the assertion fails ~1/5; 0/2 in isolation. advance() + the test are byte-identical base vs T130's commit. TRUE if: the failure mode is a fixed-time settle budget (not a logic error), the helper is unchanged by T130, and the site is the same systemic ink-testing-library timing-budget class as D20 (poll-until-condition is the fix pattern)."
- ledgerRefs: ["defects:D23"]
- evidence: ["[correct] app.test.tsx:201-209 (orchestrator-verified) — advance() uses a fixed ms=1500 wall-clock deadline as its ONLY exit-or-fail budget; on a slow render it throws 'advance: <done> never appeared'. Fixed-time settle budget, not a logic error.","[correct] app.test.tsx:450-467 (orchestrator-verified) — 'creates an item via the multi-step form' drives the create-item overlay through FOUR sequential advance() calls with NO explicit per-test timeout (inherits bun's 5000ms default).","[correct] GIT VERIFICATION (orchestrator, Bash) — advance() (app.test.tsx:195-215) is BYTE-IDENTICAL between bfa70ed^ (pre-T130) and HEAD; the T130 (bfa70ed) diff touches neither advance() nor the multi-step test (grep count 0). Confirms D23 is a residual of the D20 timing-budget class in a site T130 did not touch.","[correct] app.test.tsx:173-190 + 554-578 (orchestrator-verified) — the file already carries poll-until-condition helpers waitFor/waitForFrame and a precedent explicit 20_000ms per-test timeout on the scroll test for the SAME CPU-contention reason; the fix is to fold that idiom into advance() and/or give the multi-step test an explicit generous timeout."]
