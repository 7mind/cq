## M138

### H34 — confirmed

- createdAt: 2026-06-09T22:28:00.962Z
- updatedAt: 2026-06-09T22:33:10.765Z
- author: "opus-4.8[1m]"
- session: 7e451a99-b692-4ea6-b078-7776ebb17ca0
- headline: The bootstrap idempotence/divergence guard does not assert the committed nix/pkg/cq-ledgers/docs/ledgers.yaml is byte-identical to a fresh regen, so source(constants.ts)→fixture drift accumulates silently
- description: "WHAT WOULD MAKE THIS TRUE: there is a test/guard (in packages/ledger or the bootstrap path) that boots an FsLedgerStore against the committed docs/ledgers.yaml fixture but only PARSE-checks it (or checks a subset like an order-significant divergence on SOME fields) rather than asserting the committed fixture equals a fresh `regen-bootstrap` output. If the guard does not do a full byte-identity (or canonical-equality) comparison of committed-vs-fresh-regen, then a prior task that edited constants.ts (adding sessionLogs fields / the handoffs user-action-required status) without re-running regen-bootstrap would leave the committed ledgers.yaml stale and the guard would NOT fail — which is exactly what T335's reviewer observed at base 155d378. CONFIRM by reading: the regen-bootstrap script + the bootstrap-divergence guard test (what it actually asserts), and whether a byte/canonical equality check exists."
- ledgerRefs: ["defects:D47"]
- evidence: ["[correct] package.json:7,12 — `check` = 'tsc -b && eslint . && bun test'; `regen-bootstrap` is a SEPARATE manual `bun run scripts/regen-bootstrap.ts`, NOT invoked by check → a stale committed fixture cannot fail check via the regen path.","[correct] scripts/regen-bootstrap.ts:30-37 — REGISTRY_PATH=docs/ledgers.yaml; writes serializeRegistry({version:1, ledgers: CANONICAL_LEDGERS.map(c=>({name,schema}))}); manual write-only, no read-back/compare.","[correct] FsLedgerStore.ts:430-454 + :200 — the only divergence guard is schemasEqual at init(); onSchemaDivergence DEFAULTS to 'backup-reinit' which SILENTLY backs up + reinitialises on divergence; 'abort' (throw BootstrapViolationError) is the opt-out, not the default.","[correct] FsLedgerStore.ts:1515-1542 — schemasEqual is a structural parsed-schema compare (idPrefix/statusValues(order)/terminalStatuses/fields/transitions); it never byte/canonical-compares the committed YAML against a fresh regen.","[correct] (ORCHESTRATOR-DISCOVERED, explorer missed it) packages/ledger/test/canonical-ledgers.test.ts:504-522 — an existing test 'repo docs/ledgers.yaml matches canon' DOES read the committed fixture (L506) + copy it to a temp dir (L511) + boot a store (L513). BUT it constructs `new FsLedgerStore({ root: dir })` WITHOUT onSchemaDivergence:'abort', so it runs in DEFAULT backup-reinit mode that SILENTLY self-heals; the test comment (L514 'init() throws BootstrapViolationError on any divergence') is WRONG for that mode. On a stale fixture, init() reinits the temp copy and store.enumerate() (L518) returns canon → the assertion PASSES, drift undetected. THIS is the precise mechanism behind the T335 observation.","[incorrect] explorer note 'no committed-vs-regen test exists' — REFUTED: canonical-ledgers.test.ts:504 IS a committed-fixture-vs-canon test; it is merely INEFFECTIVE (default backup-reinit mode + structural-only compare, no byte equality)."]
