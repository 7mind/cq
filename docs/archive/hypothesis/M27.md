## M27

### H9 — confirmed

- createdAt: 2026-06-03T10:12:33.983Z
- updatedAt: 2026-06-03T10:23:12.123Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: Per-cursor-move full App re-render + ink full-tree reconciliation is the dominant ~500ms cost
- description: "ROOT-CAUSE CANDIDATE (a). Every cursor move does patchTop({cursor})→setStack→a full App re-render; even though ScrollList windows the drawn slice, the surrounding App subtree re-renders and ink reconciles/diffs the entire output tree each render. TRUE if: cursor state lives in the top-of-stack such that moving it re-renders the whole App, there is NO React.memo boundary isolating the heavy subtrees, and the dominant cost is the breadth of the reconciled tree (not a single heavy op). Disjoint from the markdown-reparse (b), residual-O(N) (c), and ink-draw (d) candidates."
- ledgerRefs: ["defects:D13"]
- evidence: ["[correct] app.tsx:398-404 + 830-832 (orchestrator-verified) — a cursor up/down calls patchTop({cursor})→setStack, replacing the top-of-stack Frame; cursor lives on App state so every move re-renders the whole App.","[correct] app.tsx (full read, explorer + orchestrator) — NO React.memo/memo() anywhere; ScrollList/ContentPane/Markdown/Overlays are plain inline function components, so React re-executes the entire subtree on each cursor move.","[correct] app.tsx:743-760 (orchestrator-verified) — T85's itemsDerived useMemo memoizes only the O(N) DATA builders (deps exclude cursor); it returns the same component tree, so ScrollList/ContentPane still re-execute and ink reconciles the full tree. T85 introduced no memo BOUNDARY.","[correct] ORCHESTRATOR RUNTIME MEASUREMENT (debug/20260603-101700-d13-navperf.tsx, ink-testing-library, hypothesis ledger) — per-cursor-move wall-clock with an EMPTY description = 28.6ms @ N=400; this N-independent floor is the full unmemoized App re-render + full-screen ink layout/draw, confirming H9's full-re-render mechanism as a primary cost driver (the ~500ms real-terminal figure scales this floor up by real stdout/throttle/terminal I/O the harness omits)."]

### H10 — confirmed

- createdAt: 2026-06-03T10:12:38.038Z
- updatedAt: 2026-06-03T10:23:17.819Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: ContentPane re-parses markdown / re-computes detail layout on every cursor move
- description: "ROOT-CAUSE CANDIDATE (b). The detail pane (ContentPane) for the selected item re-renders on every cursor move and re-parses markdown / re-derives field layout unconditionally — an un-memoized per-keystroke parse is the dominant cost. TRUE if: ContentPane parses markdown or builds its field rows on each render with no memo keyed on the selected item, and that parse is non-trivial. Disjoint from full-tree reconciliation (a), residual list-O(N) (c), and ink draw (d)."
- ledgerRefs: ["defects:D13"]
- evidence: ["[correct] markdownText.tsx:142-146 (orchestrator-verified) — Markdown() calls parseBlocks(text) unconditionally in its render body, no useMemo, not React.memo; parseBlocks is a per-line tokenizer (L75-140) and renderInline a per-run regex loop (L23-64).","[correct] app.tsx:1389-1410 + 1485/1509 (orchestrator-verified) — ContentPane (plain function, not memoized) renders one <Markdown> per long field and also rebuilds field order + a per-field estimateLines loop each render; cur=allRows[top.cursor] (app.tsx:928-930 / 1013-1015) changes identity per cursor move so the detail re-parses every move.","[correct] ORCHESTRATOR RUNTIME MEASUREMENT (debug/20260603-101700-d13-navperf.tsx) — at fixed N=400, per-cursor-move wall-clock = 28.6ms with an EMPTY description vs 55.4ms with a ~1.4KB markdown description: the selected item's unconditional markdown re-parse+re-render roughly DOUBLES per-move cost (+27ms). This is the dominant per-keystroke amplifier, and it is per-selected-item (N-independent), exactly H10."]

### H11 — wrong

- createdAt: 2026-06-03T10:12:45.457Z
- updatedAt: 2026-06-03T10:23:28.749Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: A residual un-memoized O(N) cost remains on the per-keystroke path that T85 did not cover
- description: "ROOT-CAUSE CANDIDATE (c). T85 memoized filterVisibleRows/computeColumnLayout/buildItemEntries, but some other O(N)-in-item-count work still runs per cursor move — e.g. a parent-level .map over all rows, milestones-schema threading, a per-render reduce, or a measureElement pass over many nodes. TRUE if: a code path that scales with ledger item count executes on each cursor move outside T85's memo boundaries. Disjoint from full-tree reconciliation (a), markdown re-parse (b), and ink draw (d)."
- ledgerRefs: ["defects:D13"]
- evidence: ["[correct] app.tsx:1009 + 1389-1395 (orchestrator-verified) — the O(N) sites DO exist outside T85's memo: viewItems=allRows.map(r=>r.item) every render, and ContentPane's defectFixTaskIds/hypothesisRelationships/findItem over viewItems (relationships.ts:69, 113-114) on defects/hypothesis ledgers. The literal 'un-memoized O(N) path exists' is true.","[correct] ORCHESTRATOR RUNTIME MICRO-BENCH (debug/20260603-101700-d13-navperf.tsx) — hypothesisRelationships = 5.9µs@N=25, 10µs@N=100, 32µs@N=400, 156µs@N=1600; defectFixTaskIds <7µs; viewItems.map <9µs even @N=1600. The O(N) work is sub-millisecond — NEGLIGIBLE against a ~500ms (or harness ~55ms) per-move cost.","[correct] ORCHESTRATOR RUNTIME END-TO-END — per-cursor-move wall-clock is FLAT in N: 54.7ms@N=25, 57.8ms@N=100, 54.7ms@N=400. The residual latency does NOT grow with N, directly refuting H11 (the residual O(N) is NOT the dominant/perceptible cost). The N-growth the original report mentioned was the PRE-T85 builder cost that T85 already removed; the surviving residual is N-INDEPENDENT (H9+H10)."]

### H12 — wrong

- createdAt: 2026-06-03T10:12:50.879Z
- updatedAt: 2026-06-03T10:23:34.947Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: ink full-screen redraw / stdout write / measureElement cost per keystroke is the dominant cost (React-independent)
- description: "ROOT-CAUSE CANDIDATE (d). The dominant per-keystroke cost is ink's render-to-terminal: a full-screen redraw, a large stdout write, or a synchronous measureElement/yoga layout pass over the whole tree at large terminal sizes — i.e. cost in ink's commit/draw phase rather than React render or app-level derivations. TRUE if: ink redraws the whole frame per render and that draw (yoga layout + ANSI diff + stdout) dominates wall-clock independent of how much the React tree actually changed. Disjoint from (a) breadth-of-React-tree, (b) markdown parse, (c) app-level O(N)."
- ledgerRefs: ["defects:D13"]
- evidence: ["[correct] ink.js:302-306 + reconciler.js:92-118 (explorer-cited) — ink does run a FULL yoga calculateLayout() per React commit (synchronous), and App renders a full-screen frame (app.tsx:1031), so layout/draw IS per-keystroke. This part of H12 holds.","[correct] ink.js:176-197 + 734 (explorer-cited) — BUT the stdout write is throttled to 34ms (maxFps=30) and skipped on unchanged output; 34ms is ~10× too small to be the sole ~500ms cause. So ink draw cannot be the React-INDEPENDENT dominant cost H12 claims.","[correct] ORCHESTRATOR RUNTIME MEASUREMENT — the dominant per-move AMPLIFIER is React-side: an empty-description move costs 28.6ms and a long-markdown move costs 55.4ms @ N=400 (the +27ms is the React-side markdown parse+render, H10). The full-screen yoga layout/draw is a contributor folded into H9's N-independent floor, NOT a separately-dominant React-independent cost — refuting H12's specific claim."]
