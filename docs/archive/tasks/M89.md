## M89

### T223 — done

- createdAt: 2026-06-07T19:39:36.355Z
- updatedAt: 2026-06-07T20:44:59.506Z
- author: "opus-4.8[1m]"
- session: 994b02a0-7e3f-40df-81ed-b12b9ce6b13e
- headline: "Add the additive [tiers] table AND an agent-name->tier map to cq.toml, parse both in the @cq/ledger config layer"
- description: |
    Per Q127, the suggestedModel tiers (fast/standard/frontier) must resolve to concrete Pi provider+model pairs via a NEW `[tiers]` table in cq.toml. Today the config layer parses only `[aliases]` + reviewers/planners (the ledger MCP get_config returns { configured, aliases, reviewers, planners }; get_planners/get_reviewers resolve <harness>:<model> tokens).
    
    THIS TASK ADDS TWO additive config artifacts that together resolve R265's CRITICAL tier-source hole (the cq agent frontmatter is name/description/disallowedTools ONLY — there is NO suggestedModel/tier field on agents, and cq-assets must stay byte-identical per Q126/K44, so the tier source CANNOT live in agent frontmatter):
    1. `[tiers]` table mapping fast/standard/frontier -> a <harness>:<model> token (reusing the [aliases] vocabulary, e.g. frontier = "claude:opus-4.8[1m]" or "pi:grok-build"; standard/fast -> cheaper pi providers like "pi:minimax-m3"/"ollama-cloud").
    2. An additive `agent-name -> tier` map (e.g. an `[agent_tiers]` table: investigate-explorer = "frontier", plan-reviewer = "frontier", implement-worker = "standard", ...). This is the per-agent tier SOURCE: the dispatch extension resolves a named agent -> its tier (via [agent_tiers]) -> a concrete provider+model (via [tiers]). Agent markdown stays byte-identical (preserves Q126/K44).
    
    Extend the @cq/ledger config parser/schema to parse BOTH tables and expose them (extend get_config's return and/or add get_tiers / agent->tier resolvers), WITHOUT changing existing [aliases]/reviewers/planners behavior. Add both tables to the repo's cq.toml with documented defaults (an agent with no [agent_tiers] entry falls back to a documented default tier, e.g. standard). This is config/TOML/TypeScript work INDEPENDENT of the extension implementation and the child-session spike — it runs CONCURRENTLY (no dependency on T221).
- acceptance: "`bun test` for the config layer passes including NEW tests asserting: (a) `[tiers]` parses fast/standard/frontier into a resolved provider+model; (b) `[agent_tiers]` parses agent-name->tier and an UNLISTED agent resolves to the documented default tier; (c) a named agent resolves end-to-end agent-name -> tier -> provider+model; (d) a cq.toml WITHOUT either new table still yields configured:true with the existing aliases/reviewers/planners intact (additive, no regression). get_config (or new get_tiers/agent-tier resolvers) returns the parsed maps."
- suggestedModel: standard
- dependsOn: []
- ledgerRefs: ["goals:G28"]
- resultCommit: 92aae54
- completion: "Added additive cq.toml [tiers] + [agent_tiers] tables + @cq/config parser/resolvers (resolveAgentTier/resolveTierToken/resolveAgentModel) + 17 tests; bun run check green 1038/0; merged to main."
- sessionLogs: ["docs/logs/20260607-203143-a9a2104f90713afba.md","docs/logs/20260607-204327-ae1180d30433cc093.md","docs/logs/20260607-204327-pi-codex-T223.md","docs/logs/20260607-204327-pi-grok-T223.md"]

### T228 — done

- createdAt: 2026-06-07T19:56:53.625Z
- updatedAt: 2026-06-07T20:51:31.165Z
- author: "opus-4.8[1m]"
- session: 994b02a0-7e3f-40df-81ed-b12b9ce6b13e
- headline: "Pin the runtime config-access strategy for the store-path extension to reach cq.toml [tiers]/[agent_tiers]"
- description: |
    Resolve R265's runtime-config-access gap: the dispatch extension is a STANDALONE store-path .ts file loaded by the vendored pi-coding-agent runtime via programs.pi.settings.extensions — it is NOT part of the @cq/ledger Bun workspace, so it cannot rely on workspace-relative imports being resolvable at runtime. DECIDE and document HOW the extension obtains the cq.toml [tiers] + [agent_tiers] config (from T223) at runtime, choosing ONE strategy:
      (A) RECOMMENDED — locate + parse cq.toml directly: the extension finds the project root (the same root the ledger MCP uses; e.g. via the cwd pi runs in, or a CQ_PROJECT_ROOT/CQ_CONFIG env var set on piWrapped alongside CQ_AGENTS_DIR from T222) and parses cq.toml with a small TOML reader. Avoids any cross-workspace import. The tier-resolution LOGIC (agent-name -> tier -> provider+model) is duplicated minimally OR factored into a tiny dependency-free helper the extension can inline/copy.
      (B) cross-workspace import from @cq/ledger — only if T221/T224 confirm the pi runtime resolves an import of a built @cq/ledger module from a store path (likely NOT, given the standalone-store-path loading); reject unless proven.
      (C) inline the resolved map at build time — rejected: it would bake cq.toml values into the Nix store and break the 'edit cq.toml, re-resolve' expectation.
    
    ARTIFACT LOCATION (pinned, removes R266's underspecification): record the decision in BOTH places so it is consumable and durable — (i) a LOCKED `decisions` ledger item under M86 linked `goals:G28` whose `headline` names the chosen strategy + the exact env-var/path contract, and (ii) a companion design note at `docs/drafts/<YYYYMMDD-HHMM>-pi-runtime-config-access.md` capturing the full rationale + the helper-sharing decision. The `decisions` item is the AUTHORITATIVE contract; the draft is the long-form backing.
    
    CONSUMPTION CONTRACT (how T224/T225 reference it WITHOUT re-deciding): T224 implements the extension's config read directly against the pinned contract (the named env var + default path + chosen TOML reader + the named shared resolver helper) — it does NOT re-evaluate strategy A/B/C. T225 wires tier resolution using the SAME helper named here. Both cite the `decisions` item id in their implementation notes. The contract MUST be concrete enough that neither task makes a fresh access-strategy choice (e.g. 'extension reads $CQ_CONFIG (default <CQ_PROJECT_ROOT>/cq.toml), parses [tiers]+[agent_tiers] with <toml-lib>, resolves agent-name->tier->provider+model via <helper-name>'). This is config-access DESIGN, independent of the spike — runs concurrently within M89; depends on T223 (the config shape it must read).
- acceptance: "A LOCKED `decisions` ledger item exists under M86 (linked ledgerRefs goals:G28) naming: (a) the chosen runtime config-access strategy (A/B/C) with justification; (b) the exact contract the extension uses to find cq.toml — the env-var name (and/or path convention) consistent with T222's exposure var, plus the default path; (c) the chosen TOML reader; (d) the shared agent-name->tier->provider+model resolution helper (name + inline-vs-duplicated decision). A companion `docs/drafts/<ts>-pi-runtime-config-access.md` records the same with full rationale. The contract is concrete enough that T224 implements the extension's config read and T225 wires tier resolution against it WITHOUT re-deciding the access strategy (both cite the decisions item id). Verify: the decisions item is `locked` and resolvable, and the draft file exists."
- suggestedModel: standard
- dependsOn: ["T223"]
- ledgerRefs: ["goals:G28"]
- completion: "Locked decision K46 + backing note docs/drafts/20260607-2049-pi-runtime-config-access.md: the standalone store-path extension reads cq.toml at runtime via $CQ_CONFIG (default $CQ_PROJECT_ROOT/cq.toml, fallback cwd) — env set on piWrapped alongside CQ_AGENTS_DIR — with an INLINED flat-table TOML reader + INLINED agent→tier→provider+model resolver (no cross-workspace import; rejected B/C). T224/T225 implement against K46 without re-deciding."
- sessionLogs: ["docs/drafts/20260607-2049-pi-runtime-config-access.md"]
