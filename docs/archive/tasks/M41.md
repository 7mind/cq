## M41

### T136 — done

- createdAt: 2026-06-03T15:16:12.548Z
- updatedAt: 2026-06-03T16:11:14.588Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: Make the "'s' key is inert on an archived item" test regression-sensitive (content-pane assertion)
- description: |
    Test-only fix for D24 (root cause confirmed by H14, no product change). In packages/ledger-tui/test/app.test.tsx, the "the 's' key is inert on an archived item" test (~L959-986) currently asserts ONLY f.toContain('[archived]') (:982, path-HEADER string, app.tsx:934-939) and f.toContain('archived task') (:984, LIST-pane row, app.tsx:1069). Both are overlay-INSENSITIVE: the status SelectList overlay replaces ONLY the right-hand content-pane Box (app.tsx:1071-1073: `overlay !== null ? <Overlays/> : contentEl`), so both asserted strings persist even when the picker is open. Therefore if the `!cursorInArchive` guard on the 's' handler (app.tsx:803 content-focus / :838 list-focus) were removed, pressing 's' on an archived row would open the status picker yet the test would still pass — it cannot catch the regression.
    
    FIX (mirror the sibling 'e'-inert test at :988-1010, which asserts the content-pane 'read-only' badge and IS regression-sensitive):
    
    PRIMARY ASSERTION (required, simplest + unambiguous): after pressing 's' and the existing waitForFrame settle, assert the read-only badge '[archived · read-only]' (app.tsx:1424) is still PRESENT in the frame. This is verified sound + regression-sensitive: contentEl always renders ContentPane with readOnly={cursorInArchive} (app.tsx:1012-1021), so the badge shows whenever the cursor is on an archived row regardless of focus, while opening the status overlay swaps the content-pane Box to <Overlays/> (app.tsx:1071-1073), removing the badge — exactly mirroring the proven 'e'-inert test (app.test.tsx:1008).
    
    OPTIONAL (allowed, not required): additionally assert the SelectList '› ' cursor marker (app.tsx:1291-1296) is absent ONLY within a CONTENT-PANE-SCOPED slice — the complement of the listSide(frame) helper (~test L1257-1263), i.e. the text to the RIGHT of the second '│' separator. This catches the picker opening in the content pane directly.
    
    FORBIDDEN (R135, sound): do NOT assert '› ' absent from the WHOLE frame. The list-pane SelectList renders '› ' for the SELECTED row (app.tsx:1294: `sel ? "› " : "  "`); in this test the cursor sits on the archived row after A+DOWN, so the list pane always renders '› archived task' and '› ' is present in the whole frame regardless of overlay state. A whole-frame '› '-absent assertion is therefore RED on a correct codebase (never passes) — the opposite of regression-sensitive — and would also fail acceptance step 2. The listSide() helper exists precisely because a whole-frame substring check cannot tell a list COLUMN apart from a content FIELD; any '› '-absence check MUST be content-pane-scoped.
    
    Keep the existing waitForFrame settle. Scope: packages/ledger-tui/test/app.test.tsx ONLY — do NOT touch app.tsx. Do NOT mark D24 resolved here; the implement-flow merge-back owns closure.
- acceptance: "1) The augmented test FAILS if 's' opens the status overlay on an archived row — verify by a SCRATCH removal of the `!cursorInArchive` guard at app.tsx:803/838, confirm the test goes red for the RIGHT reason (the PRIMARY assertion fires: the '[archived · read-only]' badge is GONE because the overlay swapped the content-pane Box to <Overlays/>; and, if the optional content-pane-scoped check is included, '› ' now present in the content-pane slice), then RESTORE the guard (app.tsx left byte-identical). 2) With the guard restored, the test passes (the badge is present; the picker is absent from the content pane). 3) `bun run check` (bun test + typecheck + lint) passes from the repo root. NOTE: any '› '-absence assertion MUST be scoped to the content pane (complement of listSide); a whole-frame '› '-absent assertion is FORBIDDEN (it is red even on a correct codebase, so it would fail step 2)."
- suggestedModel: standard
- ledgerRefs: ["defects:D24","goals:G12"]
- resultCommit: b8df1c6
- completion: "'s'-inert archived-item test now asserts the content-pane '[archived · read-only]' badge (+ content-pane-scoped contentSide() picker-absence check), so it fails if 's' opens the status overlay on an archived row. Verified red-for-right-reason on scratch guard removal; app.tsx untouched. D24 fix."
