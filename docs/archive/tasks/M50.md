## M50

### T161 — done

- createdAt: 2026-06-03T20:42:08.863Z
- updatedAt: 2026-06-03T21:00:32.212Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: "readLog: read the validated canonical path (close D28 TOCTOU)"
- description: |
    Close the check-then-read TOCTOU in FsLedgerStore.readLog (packages/ledger/src/store/FsLedgerStore.ts, L1251-1302).
    
    PROBLEM (confirmed H18 / D28): readLog computes `const real = await fs.realpath(resolved)` and validates `real` against `realLogsDir` INSIDE the try block (L1276-1289), but `real` is block-scoped there and the actual read at L1296 is `await fs.readFile(resolved)` — the NON-canonical, symlink-bearing path. A symlink under `resolved` swapped between the realpath check and the readFile is re-followed at read time, escaping confinement (transient race; T158 closed the persistent-symlink escape but left this).
    
    FIX (~2-3 lines):
    1. Hoist `real` out of the try: declare `let real: string | undefined;` BEFORE the try (around L1276); inside the try assign `real = await fs.realpath(resolved);` (keep the existing containment re-assert on `real` unchanged at L1285-1289).
    2. Change the read at L1296 from `fs.readFile(resolved)` to read the VALIDATED CANONICAL path: `await fs.readFile(real ?? resolved)`. `real` has no symlink components, so the read follows nothing and validated-path === read-path, closing the TOCTOU.
    3. PRESERVE ENOENT: the existing catch (L1290-1294) already swallows ENOENT and falls through; when realpath(resolved) ENOENTs, `real` stays `undefined`, so `real ?? resolved` reads `resolved` and the normal not-found error still surfaces from readFile. Do NOT mask ENOENT. (Non-ENOENT realpath errors still rethrow as today.)
    
    REGRESSION TEST (add to the existing FsLedgerStore readLog / D26 suite in packages/ledger): assert that readLog reads the CANONICAL path — e.g. set up an in-logs symlink pointing to a valid in-root target, then after the realpath check swap it to an out-of-root target; the read must NOT return the out-of-root content (it reads the canonical `real`). At minimum, add a test that a file reachable only via a symlinked component is read through its canonical path and that a post-check swap does not escape confinement. Keep the existing D26 escape-rejection, symlinked-root (false-positive), and genuine-ENOENT tests green.
    
    SCOPE: packages/ledger/src/store/FsLedgerStore.ts (readLog only) + its test file. Surgical — no unrelated refactors.
- acceptance: 1. FsLedgerStore.readLog reads `real ?? resolved` (validated canonical path), not `resolved`; `real` is hoisted to a `let` outside the try. 2. New regression test demonstrates a post-check symlink swap (or symlinked-component path) does NOT escape confinement / the canonical path is read — and FAILS against the pre-fix code (reproduction-first). 3. Existing D26 escape-rejection + symlinked-root false-positive + genuine-ENOENT readLog tests remain green. 4. `bun run check` (bun test + tsc -b + eslint) green.
- suggestedModel: standard
- ledgerRefs: ["defects:D28","goals:G14"]
- resultCommit: 537017f
- completion: "D28 fixed: readLog reads the validated canonical path (real ?? resolved), closing the check-then-read TOCTOU; ENOENT unmasked. Deterministic TOCTOU regression test (realpath-spy swap; fails against pre-fix resolved-read). Merged 537017f; integrated check green 851/0."
