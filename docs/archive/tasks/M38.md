## M38

### T132 — done

- createdAt: 2026-06-03T10:27:05.888Z
- updatedAt: 2026-06-03T11:02:54.192Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: "D13 FIX UNIT A: cut per-cursor-move redraw cost via ink incrementalRendering + reduced offscreen layout (non-UX)"
- description: |
    REVISED per R129 + R130 (orchestrator-verified correction). The prior memo-based plan was INEFFECTIVE for the reported symptom: latency is per cursor MOVE, and on a move the selected item changes, so React.memo(ContentPane/ScrollList) cannot skip (cur/cursor props change every move, app.tsx:1002/1325) and a text-keyed useMemo(parseBlocks) misses every move (text changes) — and parseBlocks is ~0.4ms anyway, so caching it is pointless. CORRECTED APPORTIONMENT: a bare <Markdown> render (44.7ms) is indistinguishable from a plain <Text> render (44.3ms); the dominant per-move cost = React reconcile + ink yoga layout + FULL-FRAME redraw of the newly-selected item's detail content (which legitimately changes on every move). +1 long field ~= +26ms; distinct-long-desc move 117ms vs empty-desc 30ms (the original same-description bench under-counted at 56ms because V8/ink optimise identical-output re-renders — that is the confound).
    
    INK VERSION REALITY (R130 — ORCHESTRATOR Bash-VERIFIED, do NOT re-litigate): the pinned ink 7.0.5 build DOES ship the render() `incrementalRendering` option. R130's claim that it is upstream-only / absent from 7.0.5 is FACTUALLY WRONG — verified present in packages/ledger-tui/node_modules/ink/build/render.d.ts:69 (`incrementalRendering?: boolean`), default `false` (render.js:17), also ink.d.ts:24 / ink.js:201. So `render(<App/>, { incrementalRendering: true })` is available with NO ink version bump and NO bun.lock/FOD-hash change. Do NOT plan an ink upgrade.
    
    TARGET THE ACTUAL COST. Pursue BOTH directions, measured — try (1) first, then (2) if (1) is insufficient:
    (1) NON-UX — enable ink incrementalRendering. render(<App .../>) at packages/ledger-tui/src/main.tsx:115 is currently called with NO options; ink defaults `incrementalRendering: false` (render.js:17), doing a full-frame erase+rewrite per change. Pass `{ incrementalRendering: true }` (available in 7.0.5 per render.d.ts:69) so ink rewrites only changed lines. Verify rendered output for a settled cursor is byte-IDENTICAL with the flag on, and measure the per-move redraw reduction.
    (2) NON-UX — reduce the laid-out detail volume. ContentPane (app.tsx:1412-1414) lays out ALL detail fields/blocks then scrolls via marginTop={-clamped} with overflow=hidden, so yoga lays out EVERY offscreen detail line each move regardless of the visible clamp. Reduce the rendered/laid-out content to the visible window (not just the viewport clip) so per-move yoga layout cost tracks visible lines only. Confirm settled-cursor output unchanged.
    
    If, after pursuing (1)+(2), a measurable per-move reduction is NOT achieved without changing UX, do NOT silently change behaviour — the UX-changing defer/debounce option is carried as a SEPARATE file-and-defer task (see sibling task T135) that the implement flow surfaces to the user as a product decision.
    
    Scope: packages/ledger-tui/src/{main.tsx, app.tsx} (+ any minimal layout-clamp adjustment). File-disjoint from D23 (test-only). Resolves defects:D13 (the non-UX portion). Do NOT mark D13 resolved here — the goal is user-closed; just deliver the fix.
- acceptance: "bun run check passes (tsc + eslint clean). MEASURED per-move cost reduction on the DISTINCT-per-item-description bench (each item a DIFFERENT long markdown description — NOT the same-description bench, which is the confound that made the prior acceptance vacuous), demonstrated by the sibling regression-guard task (T133): per-move INSTRUMENTED redraw/render-count cost for distinct-long-description navigation is materially reduced vs the pre-fix baseline (wall-clock reported for context only). Rendered output for a settled cursor is byte-IDENTICAL to pre-fix (no visual/behavioural regression). Report the before/after distinct-description per-move numbers."
- suggestedModel: frontier
- ledgerRefs: ["defects:D13","goals:G10"]
- resultCommit: 6bd6623bc439dfd085ae78d58f90a3c8b343fce5
- completion: Enabled ink incrementalRendering via exported TUI_RENDER_OPTIONS in main.tsx — per-move stdout-write cut ~53% (5839→2737 B/move) on distinct long descriptions; settled frame byte-identical; no ink bump. check green 725/0.

### T133 — done

- createdAt: 2026-06-03T10:27:15.061Z
- updatedAt: 2026-06-03T11:24:21.307Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: "D13 regression guard: DISTINCT-per-item long-markdown per-move bench asserting reduced INSTRUMENTED redraw count"
- description: |
    REVISED per R129 + R130. The prior guard risked reproducing the bench artifact: navMemo.test.tsx and debug/20260603-101700-d13-navperf.tsx give EVERY item the SAME description (makeItems(n,desc), shared LONG_MD), so a one-slot/constant-text memo HITS across moves and slow navigation passes the guard anyway. Production adjacent items have DIFFERENT markdown, so the guard MUST use DISTINCT per-item descriptions.
    
    GATE METRIC (R130 criticism #2 — STANDS, applied): the pass/fail GATE must be a DETERMINISTIC, contention-independent INSTRUMENTED render/redraw COUNT — e.g. count ContentPane renders / ink draws / yoga layout passes, or laid-out line count per move — NOT an elapsed-wall-clock per-move threshold. A wall-clock-only gate would reintroduce exactly the D20/D23 timing-flake class this same goal fixes (T134). Wall-clock numbers MAY be recorded/reported for context, but MUST NOT be the pass/fail gate.
    
    New regression guard must:
    - exercise the DEFECTS/HYPOTHESIS ledger with DISTINCT long-markdown per item (NOT a shared constant) — each item's description differs, mirroring production navigation.
    - assert on the PRODUCTION navigation pattern: move BETWEEN distinct items (j/k across items), NOT re-select the same item.
    - be N-INDEPENDENT and assert on an INSTRUMENTED redraw/render COUNT (deterministic): record a pre-fix baseline count for distinct-description navigation, and assert the post-T132 count is materially reduced. Follow the existing derivationCounters/resetDerivationCounters export idiom already exported from app.js (navMemo.test.tsx:26,155,170); add an analogous render/redraw/layout counter if needed.
    - assert the rendered frame for a SETTLED cursor is identical pre/post fix (no output regression).
    
    NOTE (R129 correction): this guard is NOT necessarily file-disjoint from T132 — any instrumentation hook that lands in src (markdownText.tsx/app.tsx/main.tsx) shares files with T132, so the dependsOn:[T132] ordering (below) governs sequencing. Prefer black-box frame/count assertions over deep src instrumentation where feasible to keep the guard test-local.
    
    Scope: packages/ledger-tui (test file + any minimal test-instrumentation hook). Depends on T132. Resolves the D13 regression-guard requirement.
- acceptance: New guard uses DISTINCT per-item long-markdown and the move-between-distinct-items navigation pattern. The pass/fail GATE is an INSTRUMENTED render/redraw/layout COUNT (deterministic, contention-independent), NOT a wall-clock per-move threshold (wall-clock reported for context only). It records a pre-T132 baseline count demonstrating the per-move content-volume cost, and PASSES after T132 with a measurably reduced per-move COUNT (and identical settled-cursor output). bun run check passes with the new test included. The guard must NOT pass via shared-constant-description memo hits (explicitly distinct descriptions).
- suggestedModel: frontier
- dependsOn: ["T132"]
- ledgerRefs: ["defects:D13","goals:G10"]
- resultCommit: bbbfb44402e22b9c245d0ce822c1bbe74dddefd8
- completion: Committed packages/ledger-tui/test/navRenderBytes.test.tsx — deterministic per-move stdout-write BYTE-COUNT regression guard (counting fake TTY, NOT wall-clock) over DISTINCT per-item long-markdown with move-between-distinct-items; measures standard (5832 B/move) vs incremental TUI_RENDER_OPTIONS (3718 B/move = 36% reduction) in-test; negative control verified (reverting incrementalRendering fails the gate). check green 728/0.

### T134 — done

- createdAt: 2026-06-03T10:27:24.961Z
- updatedAt: 2026-06-03T11:04:03.479Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: "D23 FIX UNIT B: replace advance() fixed 1500ms deadline with poll-until-condition budget + explicit per-test timeout"
- description: |
    Confirmed root cause (H13): the advance() helper (packages/ledger-tui/test/app.test.tsx:201-209) uses a FIXED ms=1500 wall-clock deadline as its ONLY settle budget; under CPU contention a slow render misses it and advance() throws. The 'creates an item via the multi-step form' test (app.test.tsx:450-467) chains FOUR advance() calls with no explicit per-test timeout (bun 5000ms default). Residual of the D20 timing-budget class; byte-identical base->HEAD (T130 touched neither advance() nor this test).
    
    Fix: (1) fold the file's existing poll-until-condition idiom (waitFor/waitForFrame at :173-190) into advance() — keep its h.frame() polling, replace the tight ms=1500 deadline with a generous budget (~2000-5000ms, cf. waitForFrame). (2) give the multi-step-form test (:450-467) an explicit generous per-test timeout mirroring the scroll test's 20_000ms precedent (app.test.tsx:554-578).
    
    Scope: packages/ledger-tui/test/app.test.tsx ONLY. File-disjoint from D13 (src-only) — parallel-safe. Resolves defects:D23. Do NOT mark D23 resolved here — goal is user-closed.
- acceptance: bun run check passes deterministically under concurrent full-suite load (run repeatedly / under contention shows no advance() timeout throw). advance() no longer relies on a fixed 1500ms deadline; the multi-step-form test has an explicit generous per-test timeout. tsc + eslint clean.
- suggestedModel: standard
- ledgerRefs: ["defects:D23","goals:G10"]
- resultCommit: effbd60
- completion: advance() helper deadline raised 1500→4000ms (poll-until-condition) + explicit 20_000ms per-test timeout on the multi-step-form test; removes the D23 fixed-deadline starvation. Test-only; check green 725/0.

### T135 — done

- createdAt: 2026-06-03T10:39:08.232Z
- updatedAt: 2026-06-03T11:04:24.318Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: "D13 LAST RESORT (UX-changing, user sign-off): defer/debounce heavy detail-pane render during rapid navigation"
- description: |
    CONDITIONAL / file-and-defer. Per R129 + the corrected D13 apportionment, the dominant per-move cost is the React reconcile + ink yoga layout + full-frame redraw of the newly-selected item's detail content, which legitimately changes on every move. If the NON-UX directions in T132 (ink incremental rendering; shrinking/clamping redraw scope) do NOT achieve a measurable per-move reduction, the only remaining effective fix CHANGES UX and therefore requires a product decision — it must NOT be applied silently.
    
    PROPOSED UX CHANGE: defer/debounce the heavy detail-pane render during RAPID navigation — render the list highlight (cursor move) IMMEDIATELY, and render the full detail pane only after the cursor SETTLES (~80–120ms). Holding j/k then feels instant; the detail catches up on settle. This is a deliberate latency-vs-immediacy tradeoff: the detail pane briefly lags the cursor during fast scrolling.
    
    IMPLEMENT-FLOW HANDLING (file-and-defer): this task carries a genuine UX product decision. The implement flow must SURFACE it to the user (e.g. as a question on pickup) rather than autonomously changing TUI behaviour. Only proceed with the debounce if (a) T132's non-UX directions did not yield a measurable per-move reduction AND (b) the user approves the deferred-detail behaviour and the settle window (~80–120ms, confirm the exact value with the user). If T132 already achieves the target reduction, this task is a NO-OP — close it without changing behaviour.
    
    Scope: packages/ledger-tui/src/app.tsx (detail-pane render gating) + a settle timer. Depends on T132 (only relevant if the non-UX fix is insufficient). Resolves the residual of defects:D13 if and only if pursued. Do NOT mark D13 resolved here — the goal is user-closed.
- acceptance: "If pursued (T132 insufficient AND user approves): holding j/k shows immediate cursor/highlight movement with the detail pane rendering after the cursor settles within the approved window; MEASURED per-move list-highlight latency is materially reduced on the distinct-description bench; no detail content is lost (the settled detail is identical to today's). bun run check passes (tsc + eslint clean). If NOT pursued (T132 sufficient OR user declines): closed as a no-op with the rationale recorded; no behaviour change."
- suggestedModel: frontier
- dependsOn: ["T132"]
- ledgerRefs: ["defects:D13","goals:G10"]
- completion: "NO-OP (closed per T135's own 'T132 sufficient → no-op' acceptance clause). T132's NON-UX fix (ink incrementalRendering) achieved a MEASURED ~53% per-move redraw reduction and was approved (R132), so the UX-changing defer/debounce of the detail pane is NOT needed. The file-and-defer-to-user gate (condition: T132 non-UX directions INSUFFICIENT) did not trigger, so no user product decision was required. No behaviour change."
