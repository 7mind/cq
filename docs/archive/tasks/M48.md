## M48

### T158 — done

- createdAt: 2026-06-03T20:07:25.454Z
- updatedAt: 2026-06-03T20:37:09.771Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: "D26: harden FsLedgerStore.readLog against symlink escape (reproduce-first)"
- description: |
    Behavioural fix for D26 (CONFIRMED H16). FsLedgerStore.readLog (packages/ledger/src/store/FsLedgerStore.ts, ~lines 1257-1267) validates containment with a LEXICAL `path.resolve(logsDir, relPath)` + `startsWith(logsDir + path.sep)` check and never calls fs.realpath, so a symlink located inside docs/logs/ whose target escapes the root passes the guard and fs.readFile follows it.
    
    REPRODUCE FIRST (mandatory): add a regression test under the ledger package's test suite that creates a symlink inside docs/logs pointing OUTSIDE the confinement root, then asserts read_log/readLog REJECTS it with the escape error. Confirm this test FAILS before the fix (the escaping target is currently read) and fails for the RIGHT reason (content is read / no escape error), not an unrelated error.
    
    FIX: after the existing lexical guard, call fs.realpath(resolved) and re-assert `realpath === logsDir || realpath.startsWith(logsDir + path.sep)`; throw the SAME escape error otherwise. CATCH ENOENT specifically so a genuinely missing file still surfaces the normal not-found error path (do NOT mask a missing file as an escape, and do NOT swallow the ENOENT). ~3 lines around the existing guard.
    
    Scope: packages/ledger/src/store/FsLedgerStore.ts (readLog only) + one new regression test. Disjoint from D25 (ledger-tui) and D27 (llm/commands prompts) — no shared files. Frontends stay pure MCP clients.
- acceptance: (1) New regression test creates a symlink under docs/logs targeting outside the root and asserts readLog rejects it with the escape error; the test FAILS before the realpath re-assert is added (proving repro) and PASSES after. (2) A test (existing or added) confirms a genuinely missing log file still surfaces the normal not-found / ENOENT error and is NOT masked as an escape. (3) bun run check (test + typecheck + lint) green.
- suggestedModel: standard
- ledgerRefs: ["defects:D26","goals:G13"]
- resultCommit: 311b8a1
- completion: "D26 fixed: FsLedgerStore.readLog realpath-resolves both the target and the confinement root before containment, rejecting in-docs/logs symlinks whose target escapes while allowing legitimate reads under a symlinked-root parent; ENOENT not masked. +2 regression tests. r0 false-escape regression fixed in r1. Merged 311b8a1; integrated check green 850/0."

### T159 — done

- createdAt: 2026-06-03T20:07:31.561Z
- updatedAt: 2026-06-03T20:37:12.139Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: "D25: remove stale unused eslint-disable directive in navRenderBytes test"
- description: |
    Lint-hygiene fix for D25 (CONFIRMED H15). packages/ledger-tui/test/navRenderBytes.test.tsx:291 carries `// eslint-disable-next-line no-console` over the console.log at :292, but eslint.config.js enables no `no-console` rule, so the directive is unused → eslint 'Unused eslint-disable directive' warning (non-fatal; the lint script sets no --max-warnings 0).
    
    FIX: remove the stale `// eslint-disable-next-line no-console` comment at packages/ledger-tui/test/navRenderBytes.test.tsx:291. KEEP the console.log at :292. Trivial, single-line deletion (eslint --fix would also handle it).
    
    Scope: packages/ledger-tui/test/navRenderBytes.test.tsx only. Disjoint from D26 (ledger package) and D27 (llm/commands prompts).
- acceptance: "The stale `// eslint-disable-next-line no-console` comment is gone from navRenderBytes.test.tsx:291; the console.log it covered remains. eslint no longer emits the 'Unused eslint-disable directive' warning for that file. bun run check green."
- suggestedModel: fast
- ledgerRefs: ["defects:D25","goals:G13"]
- resultCommit: 311b8a1
- completion: "D25 fixed: removed the stale unused `// eslint-disable-next-line no-console` directive at navRenderBytes.test.tsx:291. Merged 311b8a1; check green."

### T160 — done

- createdAt: 2026-06-03T20:07:39.109Z
- updatedAt: 2026-06-03T20:37:14.929Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: "D27: reword CHAINED handoff trigger in all 3 */advance.md files"
- description: |
    Prompt-clarity fix for D27 (CONFIRMED H17). In each */advance.md §Handoff record, the CHAINED branch names only /advance (plan/advance.md:283, implement/advance.md:290); investigate/advance.md:282 additionally names /plan:advance. A /<flow>:start or /<flow>:follow-up that runs the advance pass INLINE is a wrapping flow command that is NEITHER /advance NOR /plan:advance, so neither branch's literal trigger covers it cleanly — correctness today rests on start.md/follow-up.md externally directing the inline pass to the STANDALONE branch.
    
    FIX (prompt-only): reword EACH of the 3 */advance.md CHAINED triggers to fire on ANY wrapping flow command, e.g. 'CHAINED INLINE by any wrapping flow command (/advance, /plan:advance, or a /<flow>:start / /<flow>:follow-up that runs this pass inline) → SUPPRESS; the outermost wrapper owns the single handoff write.' Keep one authoritative handoff write at the outermost wrapper. Adapt the wording to each file's existing phrasing so the surrounding STANDALONE/CHAINED structure stays consistent.
    
    Scope: llm/commands/plan/advance.md, llm/commands/implement/advance.md, llm/commands/investigate/advance.md (CHAINED-trigger text only). Disjoint from D25 (ledger-tui test) and D26 (ledger src). No code change.
- acceptance: "All 3 */advance.md CHAINED handoff triggers are reworded to fire on ANY wrapping flow command (explicitly covering a /<flow>:start or /<flow>:follow-up inline pass), keeping the 'outermost wrapper owns the single handoff write' invariant. No duplicate/omitted handoff semantics introduced. bun run check green (these are docs; check should be unaffected)."
- suggestedModel: standard
- ledgerRefs: ["defects:D27","goals:G13"]
- resultCommit: 311b8a1
- completion: "D27 fixed: reworded the CHAINED handoff trigger in all 3 */advance.md to fire on any wrapping flow command, and made the 4 start/follow-up wrappers the single handoff writer (inline pass suppresses) — one writer per invocation, no duplicate/omitted. Docs-only, 7 files. Merged 311b8a1; check green."
