## M87

### T221 — done

- createdAt: 2026-06-07T19:39:16.812Z
- updatedAt: 2026-06-07T20:47:27.021Z
- author: "opus-4.8[1m]"
- session: 994b02a0-7e3f-40df-81ed-b12b9ce6b13e
- headline: Read-only go/no-go spike on Pi's ExtensionAPI child-session primitive
- description: "Confirm the single genuine unknown before any implementation cost is sunk: whether Pi 0.78.0's `ExtensionAPI` can spawn an isolated child agent turn with a FILTERED toolset and capture its final output. Primarily READ-ONLY: resolve and read badlogic's pi-mono `examples/extensions` subagent example AND the `@earendil-works/pi-coding-agent` ExtensionAPI typings (resolve the package via the pi-coding-agent store path — the two existing extensions in `nix/pkg/pi-extensions/*.ts` import `import type { ExtensionAPI } from \"@earendil-works/pi-coding-agent\"`). Determine the exact supported API for the FIVE primitives: (1) `pi.registerTool`; (2) spawning a child session with its OWN context + a filtered toolset (a child that cannot see the dispatch tool, enforcing subagents-cannot-spawn-subagents); (3) pinning the child's model/provider; (4) capturing the child's final text/structured output; plus the registerTool surface itself counted as (1). If the typings are ambiguous, a MINIMAL discardable spike extension (`spike-child-session.ts`, removed afterward) MAY be loaded under pi to empirically confirm the primitive — but no production file is changed. Produce a written go/no-go. CONTINGENCY: if any primitive is absent, this is a NO-GO that REOPENS the Q125/Q130 mechanism lock (decision K-level revisit) and possibly G28 scope — the later milestones (M88–M91) MUST NOT proceed on an assumed GO."
- acceptance: "A written go/no-go decision citing exact file:line in badlogic's pi-mono examples/extensions subagent example AND in the @earendil-works/pi-coding-agent typings for EACH of the FIVE primitives {registerTool, child-session spawn, tool-filtering, model/provider pin, output capture}. GO = ALL FIVE primitives named and confirmed present in Pi 0.78.0 (note: the two existing nix/pkg/pi-extensions/*.ts prove registerProvider + pi.on, but NOT registerTool — so registerTool genuinely needs confirming, not assuming). NO-GO = the missing primitive named with its citation. CONTINGENCY (first-class, not a separate defect): a NO-GO does NOT let later milestones proceed on an assumed GO — it REOPENS the locked mechanism choice (Q125/Q130, decision lock) and possibly G28's scope, by filing a question/decision-revisit rather than starting T222/T223/T224. Only a GO unblocks the M88–M91 milestones. No production files changed; any empirical spike extension (spike-child-session.ts) is discarded after the probe."
- suggestedModel: frontier
- ledgerRefs: ["goals:G28"]
- resultCommit: bd6aa87
- completion: "Spike GO: Pi 0.78.0 ExtensionAPI supports all 5 primitives (registerTool, child-session spawn [subprocess Route A + in-process createAgentSession Route B], tool-filtering, model pin, output capture) — verified at exact file:line. Go/no-go note merged; M88–M91 unblocked. Recommended dispatch path: Route A (subprocess pi -p --mode json)."
- sessionLogs: ["docs/logs/20260607-203143-a97d456b51fabc948.md","docs/logs/20260607-204327-a587aeaaa80fb8b26.md","docs/logs/20260607-204327-pi-codex-T221.md","docs/logs/20260607-204327-pi-grok-T221.md"]
