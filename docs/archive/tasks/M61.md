## M61

### T1 — done

- createdAt: 2026-06-05T22:19:46.484Z
- updatedAt: 2026-06-05T22:49:14.063Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Merge get_reviewers + get_config into BOTH ledger-MCP tool surfaces (config capability)
- description: |
    Re-express the cq-config server's two tools on BOTH of the ledger MCP's tool-registration surfaces (R193 BLOCKER). The ledger MCP registers tools on TWO parallel surfaces, both keyed off LEDGER_TOOL_NAMES: (a) `createLedgerMcpTools` in packages/ledger/src/mcp/ledgerTools.ts (the @anthropic-ai/claude-agent-sdk `tool()` factory, used ONLY by the in-process Claude-SDK host); (b) `registerLedgerStdioTools` in packages/ledger/src/mcp/stdioLedgerTools.ts (raw @modelcontextprotocol/sdk `server.registerTool`). The STANDALONE @cq/ledger-mcp binary (`nix run .#ledger-mcp`, i.e. .mcp.json's `ledger` server, which plan/advance.md + implement/advance.md + reviewers.md all reach as mcp__ledger__*) AND the embedded TUI (in-memory transport) + web (co-hosted HTTP) servers ALL go buildServer() -> registerLedgerStdioTools (ledger-mcp/src/main.ts:247), NOT createLedgerMcpTools. So both surfaces MUST register the tools or PART 1's whole premise (consumers calling mcp__ledger__get_reviewers/get_config) is unmet on the real binary.
    
    WORK:
    (1) ledgerTools.ts: add `get_reviewers` and `get_config` via the claude-agent-sdk `tool()` builder (empty input schema `{} as Record<string,never>`, returning `jsonResult(...)`), SDK auto-prefixes them mcp__cq__* (surfacing as mcp__ledger__get_reviewers / mcp__ledger__get_config). Extend createLedgerMcpTools(store, readLog?) with an optional config-capability param (e.g. configCapability?: { computeReviewers(): GetReviewersResult; computeConfig(): GetConfigResult }) bound to the resolved store root, mirroring the readLog capability injection.
    (2) stdioLedgerTools.ts: register the SAME two tools via raw `server.registerTool` (same empty-input/JSON-result shape), AND thread the config capability as a NEW PARAM of the registration entrypoint: `registerLedgerStdioTools(server, store, readLog, configCapability?)`. T2's buildServer wiring passes the capability into THIS function.
    (3) Capability absence (in-memory store, no cq.toml-capable root): the two tools either are not registered or throw a not-implemented error on both surfaces (mirror ReadLogNotImplementedError).
    (4) Lift the REUSABLE payload logic from packages/cq-config-mcp/src/main.ts (computeReviewers/computeConfig + ResolvedReviewer/GetReviewersResult/GetConfigResult) into a small reusable module under packages/ledger-mcp (NOT @cq/ledger core — keep core config-agnostic). Do NOT add @cq/config as a dep of @cq/ledger — the capability is constructed in @cq/ledger-mcp (T2) and injected; the @cq/config import + loadConfig/resolveReviewers call live ONLY in the ledger-mcp capability implementation.
- acceptance: BOTH ledgerTools.ts (createLedgerMcpTools) AND stdioLedgerTools.ts (registerLedgerStdioTools) register get_reviewers + get_config behind the injected config capability; registerLedgerStdioTools's signature gains the configCapability? param; @cq/ledger has NO new @cq/config import (grep packages/ledger/src for '@cq/config' returns nothing — the capability is injected, not imported here); `bun run typecheck` green. (Tool-count/name + cross-surface drift-guard assertions are bumped in T3; the end-to-end stdio surfacing assertion is in T2.)
- suggestedModel: frontier
- ledgerRefs: ["goals:G18"]
- resultCommit: 194b1bd
- completion: get_reviewers+get_config registered on BOTH ledger-MCP surfaces (createLedgerMcpTools + registerLedgerStdioTools w/ configCapability? param) behind an injected config-agnostic ConfigCapability; @cq/config logic lifted into @cq/ledger-mcp (core stays config-agnostic); stdio surface lists all 20 tools. typecheck+lint green; count test left red for T3 (reproduce-first).
- sessionLogs: ["docs/logs/20260605-224413-a3f8bd35062689ff3.md","docs/logs/20260605-224814-a10ae95066958fef7.md"]

### T2 — done

- createdAt: 2026-06-05T22:19:57.765Z
- updatedAt: 2026-06-05T23:21:25.285Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Wire the @cq/config capability into ledger-mcp buildServer -> registerLedgerStdioTools (+ embedded TUI/web)
- description: "In packages/ledger-mcp/src/main.ts, construct the config capability the factory/registration expect (T1) and pass it as the new 4th arg into `registerLedgerStdioTools(server, store, readLog, configCapability)` inside buildServer (ledger-mcp/src/main.ts:247) — this is the surface the STANDALONE binary and the embedded TUI/web servers actually use. Also pass it into createLedgerMcpTools for the in-process Claude-SDK path. The capability closes over the resolved store root (the same --cwd>$LEDGER_ROOT>cwd value buildServer already resolves) and calls @cq/config loadConfig + the lifted computeReviewers/computeConfig builders, re-reading cq.toml on each call (no caching, mirroring the standalone server). Add @cq/config to packages/ledger-mcp/package.json dependencies (workspace:*). Drop the now-redundant $CQ_CONFIG_ROOT knob (Q99) — the config root IS the ledger root. Mirror the SAME capability construction into the embedded buildServer paths used by the TUI and web frontends (wherever they co-host the ledger MCP via buildServer), so get_reviewers/get_config work in embedded mode too. Resolve cq.toml at the store root for those paths as well."
- acceptance: "packages/ledger-mcp/package.json lists @cq/config; buildServer passes the config capability into registerLedgerStdioTools (4th arg) AND createLedgerMcpTools; an END-TO-END stdio MCP roundtrip test (spin up the stdio server / its registered tool set and list tools) asserts get_reviewers + get_config surface on the STDIO binary — NOT merely on the in-process tool() factory — returning {configured:false,...} when no cq.toml at the store root and the resolved reviewer set when a fixture cq.toml is present; the embedded TUI/web buildServer paths inject the capability too; `bun run typecheck` + `bun test` green for ledger-mcp."
- suggestedModel: frontier
- dependsOn: ["T1"]
- ledgerRefs: ["goals:G18"]
- resultCommit: 382f653
- completion: Wired the @cq/config config capability into ledger-mcp buildServer (bound to FsLedgerStore.rootDir) → registerLedgerStdioTools 4th arg; all four host paths funnel through buildServer; e2e stdio config roundtrip test.
- sessionLogs: ["docs/logs/20260605-230151-a2a233749a674e96d.md","docs/logs/20260605-230954-aeca586ec8effd1fb.md"]

### T3 — done

- createdAt: 2026-06-05T22:20:08.478Z
- updatedAt: 2026-06-05T23:21:29.046Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Bump the ledger-MCP tool-surface count (18->20) + LEDGER_TOOL_NAMES in BOTH files + drift-guard test
- description: |
    Update the documented tool surface to reflect the two merged tools on BOTH registration surfaces (R193: the count/name bump is under-scoped to one file). Changes:
    (1) packages/ledger/src/mcp/ledgerTools.ts — bump the header '18 tools' comment (~L7) to 20 and document the new get_reviewers + get_config entries in the surface listing.
    (2) packages/ledger/src/mcp/stdioLedgerTools.ts — it ALSO documents the tool count (header ~L4-5 AND the registerLedgerStdioTools doc-comment ~L149): bump those 18->20 too, and list the two new tools.
    (3) Update the shared LEDGER_TOOL_NAMES list (the canonical tool-name array both surfaces + the tests assert against — grep for LEDGER_TOOL_NAMES) to include the two new names.
    (4) The CROSS-SURFACE DRIFT-GUARD test (per stdioLedgerTools.ts ~L18: 'the schema-drift guard between the stdio path and the Claude path is the test suite') asserts BOTH surfaces register the SAME tool set — update its expectation to 20 and the new names. Reproduce-first: after T1 this drift-guard / count assertion FAILS (20 registered vs 18 asserted, and/or one surface updated but not the other — the guard catches the cross-surface mismatch); confirm that failure for the right reason FIRST, then update to make it pass.
- acceptance: The tool-count + cross-surface drift-guard test fails BEFORE the bump (20 vs 18, and the drift-guard flags a one-surface-only update) for the right reason, then passes after; the header count says 20 in BOTH ledgerTools.ts AND stdioLedgerTools.ts (header + the ~L149 doc-comment); LEDGER_TOOL_NAMES contains get_reviewers + get_config; the cross-surface drift-guard test is green; `bun test` for @cq/ledger green.
- suggestedModel: standard
- dependsOn: ["T1"]
- ledgerRefs: ["goals:G18"]
- resultCommit: 088cebf
- completion: Tool-count 18→20 across all surfaces (headers + ledger-mcp/src/main.ts L3/L159 + count test toBe(20) + get_reviewers/get_config containment + drift-guard); bun run check fully green 924/0.
- sessionLogs: ["docs/logs/20260605-230151-a7098844f31543a09.md","docs/logs/20260605-231650-a97b142e31a323838.md","docs/logs/20260605-231650-a8900f23126a3eeb4.md"]

### T4 — done

- createdAt: 2026-06-05T22:20:23.647Z
- updatedAt: 2026-06-05T23:21:33.473Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Delete the @cq/config-mcp standalone server package (dir + workspace entry)
- description: "Remove the whole packages/cq-config-mcp/ directory (the standalone server: src/main.ts, package.json, tsconfig, any tests). Remove its entry from the Bun workspace `packages` glob/members if listed explicitly (root package.json / workspace config) and from the root tsconfig project references (tsc -b) if referenced. Per Q98 this is an OUTRIGHT delete — no deprecation shim. The @cq/config PARSER package (packages/cq-config/) STAYS untouched. The reusable payload logic (computeReviewers/computeConfig) has already been lifted into ledger-mcp in T1; nothing in cq-config-mcp is still imported anywhere after T1/T2."
- acceptance: packages/cq-config-mcp/ is gone; grep across the repo for 'cq-config-mcp' and '@cq/config-mcp' returns ONLY removed/expected references (none in source, package.json members, or tsconfig refs); `bun run typecheck` + `bun test` green (no dangling import).
- suggestedModel: standard
- dependsOn: ["T1"]
- ledgerRefs: ["goals:G18"]
- resultCommit: 1fa0031
- completion: Deleted packages/cq-config-mcp/ (server) + root workspace/tsconfig/bun.lock prune; no dangling imports (logic lifted into @cq/ledger-mcp by T1).
- sessionLogs: ["docs/logs/20260605-230151-a83e9dabbfa46e9c3.md","docs/logs/20260605-230954-a85a9f2f3a19a1e9a.md"]

### T5 — done

- createdAt: 2026-06-05T22:20:36.127Z
- updatedAt: 2026-06-05T23:33:52.350Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Remove cq-config-mcp from flake.nix + add @cq/config symlink to ledger-mcp/embed closures
- description: "In flake.nix: delete the cqConfigMcp derivation (~L247-304), the packages.cq-config-mcp + apps.cq-config-mcp attrs (~L449,494), and the node-modules FOD fileset + installPhase entries for packages/cq-config-mcp (~L59,102,106). The @cq/config parser STAYS in the FOD fileset (it is now a dep of @cq/ledger-mcp). Because T2 makes @cq/config a dep of @cq/ledger-mcp, ADD a @cq/config workspace symlink under packages/ledger-mcp/node_modules/@cq/config in the ledger-mcp derivation installPhase (mirror the existing @cq/ledger workspace-link pattern), and add the same symlink in embedServerClosure so the TUI/web embedded servers resolve @cq/config at runtime. Do NOT refresh the FOD hash here — that is the dedicated next task after all dep-graph changes land."
- acceptance: flake.nix has no cqConfigMcp derivation / packages.cq-config-mcp / apps.cq-config-mcp / cq-config-mcp FOD entries; the ledger-mcp + embedServerClosure installPhases create the @cq/config symlink; `nix flake check`/eval does not reference the removed attrs (a `nix eval .#packages.x86_64-linux --apply builtins.attrNames` no longer lists cq-config-mcp).
- suggestedModel: frontier
- dependsOn: ["T2","T4"]
- ledgerRefs: ["goals:G18"]
- resultCommit: a5cc2c8
- completion: Removed cq-config-mcp from flake.nix (cqConfigMcp derivation + packages/apps attrs + FOD entries); added @cq/config symlink to ledger-mcp installPhase + embedServerClosure (verified resolving in nix build .#ledger-mcp). FOD hash refresh deferred to T11.
- sessionLogs: ["docs/logs/20260605-232847-afb0f68b46a96d11e.md","docs/logs/20260605-232847-a645bca44e053fff7.md"]

### T6 — done

- createdAt: 2026-06-05T22:20:43.558Z
- updatedAt: 2026-06-05T23:33:55.635Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Remove the cq-config server registration from dev-llm.nix
- description: In nix/hm/dev-llm.nix remove `cqConfigPkg = ledgerPkgs.cq-config-mcp` (~L105) and the `programs.mcp.servers.cq-config` entry (~L567-570). The piMcpJson keep-alive remap picks up `programs.mcp.servers` generically, so deleting the server entry removes it from the Pi-side mcp.json too (no separate Pi edit needed). Verify no other reference to cqConfigPkg remains in the module.
- acceptance: grep nix/hm/dev-llm.nix for 'cq-config' / 'cqConfigPkg' / 'cq-config-mcp' returns nothing; the home-manager module still evaluates (`nix eval` of the dev-llm output / a build that exercises it succeeds); ledger MCP server registration is unchanged.
- suggestedModel: standard
- dependsOn: ["T4"]
- ledgerRefs: ["goals:G18"]
- resultCommit: ee91a81
- completion: Removed cqConfigPkg binding + programs.mcp.servers.cq-config block from nix/hm/dev-llm.nix; servers.ledger + piMcpJson keep-alive remap intact; nix-instantiate --parse OK.
- sessionLogs: ["docs/logs/20260605-232847-a305e6abd89e3f266.md","docs/logs/20260605-232847-a724303ed7acf8e22.md"]

### T7 — done

- createdAt: 2026-06-05T22:20:52.869Z
- updatedAt: 2026-06-05T23:33:58.470Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Remove the cq-config server entry from this repo's .mcp.json
- description: In the repo-root .mcp.json delete the 'cq-config' server entry (~L7-10). The ledger MCP server entry already provides get_reviewers/get_config after T1/T2, so consumers reach those tools as mcp__ledger__*. Leave the 'ledger' server entry and all others untouched.
- acceptance: .mcp.json is valid JSON with no 'cq-config' server; the 'ledger' entry remains; a smoke check (connect the ledger MCP and list tools) shows get_reviewers + get_config present on the ledger server and no separate cq-config server.
- suggestedModel: fast
- dependsOn: ["T4"]
- ledgerRefs: ["goals:G18"]
- resultCommit: d3df878
- completion: Removed the cq-config server entry from repo-root .mcp.json; ledger entry intact; valid JSON.
- sessionLogs: ["docs/logs/20260605-232847-a0b795e35f9a2f146.md","docs/logs/20260605-232847-aee7f6bc1b30f3934.md"]

### T8 — done

- createdAt: 2026-06-05T22:21:02.206Z
- updatedAt: 2026-06-05T23:21:35.142Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Repoint commands/cq/reviewers.md from mcp__cq-config__* to mcp__ledger__* (all cq-config prose)
- description: "In nix/pkg/cq-assets/commands/cq/reviewers.md: change the frontmatter allowed-tools (L4) from `mcp__cq-config__get_reviewers, mcp__cq-config__get_config` to `mcp__ledger__get_reviewers, mcp__ledger__get_config`, and update ALL prose that names cq-config to call get_config/get_reviewers on the LEDGER MCP server. R193 (minor): there are MORE bare 'cq-config' references than the two patterns originally greps for — beyond 'mcp__cq-config__' and 'cq-config MCP server', the file also carries: the frontmatter `description` line ('from cq-config get_reviewers/get_config'), L15 '(from cq-config), falling back', L30 'Call get_config (from the cq-config MCP server)', and L44/L147 'the cq-config server' / '(cq-config MCP)'. Rewrite ALL of them to reference the ledger MCP. Keep the fallback alias map + SESSION-ONLY semantics unchanged. Independent file from plan/advance.md + implement/advance.md (parallel with T9/T10)."
- acceptance: reviewers.md frontmatter grants mcp__ledger__get_reviewers + mcp__ledger__get_config; a repo-wide grep of the file for the BARE token 'cq-config' (case-insensitive) returns ZERO hits (this subsumes 'mcp__cq-config__', 'cq-config MCP server', the description line, '(from cq-config)', 'the cq-config server', '(cq-config MCP)'); the command's documented steps reference the ledger MCP.
- suggestedModel: fast
- dependsOn: ["T1"]
- ledgerRefs: ["goals:G18"]
- resultCommit: 2254de2
- completion: Repointed commands/cq/reviewers.md cq-config → ledger MCP (frontmatter + all prose); grep cq-config=0.
- sessionLogs: ["docs/logs/20260605-230151-a43b14b0e77e4f7cd.md","docs/logs/20260605-230954-aa852f6fdcd50ddfd.md"]

### T9 — done

- createdAt: 2026-06-05T22:21:09.625Z
- updatedAt: 2026-06-05T23:21:38.964Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Repoint commands/implement/advance.md from mcp__cq-config__get_reviewers to mcp__ledger__get_reviewers
- description: "In nix/pkg/cq-assets/commands/implement/advance.md: change the frontmatter allowed-tools (L4) `mcp__cq-config__get_reviewers` -> `mcp__ledger__get_reviewers`, and in the reviewer-resolution block replace the 'cq-config MCP get_reviewers' call reference with the ledger MCP's get_reviewers (mcp__ledger__get_reviewers). Behaviour and reconciliation semantics are unchanged — only the tool namespace moves. Independent file from reviewers.md and plan/advance.md (parallel with T8/T10)."
- acceptance: implement/advance.md frontmatter grants mcp__ledger__get_reviewers; no 'mcp__cq-config__' / 'cq-config MCP' reference remains; the reviewer-resolution prose calls get_reviewers on the ledger MCP.
- suggestedModel: fast
- dependsOn: ["T1"]
- ledgerRefs: ["goals:G18"]
- resultCommit: 8dd57e5
- completion: Repointed commands/implement/advance.md cq-config → ledger MCP (frontmatter + §3a); grep cq-config=0. (Redone on correct base after a round-0 wrong-base disapproval.)
- sessionLogs: ["docs/logs/20260605-230151-a9105095d47ef3ac2.md","docs/logs/20260605-231650-a04aaca8b59cc590e.md","docs/logs/20260605-231650-a8f337257e8c0f743.md"]

### T10 — done

- createdAt: 2026-06-05T22:21:17.923Z
- updatedAt: 2026-06-05T23:21:41.817Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Repoint commands/plan/advance.md from mcp__cq-config__get_reviewers to mcp__ledger__get_reviewers
- description: "In nix/pkg/cq-assets/commands/plan/advance.md: change the frontmatter allowed-tools (L4) `mcp__cq-config__get_reviewers` -> `mcp__ledger__get_reviewers`, and in step 2.1 ('Resolve the active reviewer set. Call the cq-config MCP get_reviewers tool ...') replace the cq-config reference with the ledger MCP's get_reviewers. Reconciliation semantics unchanged. This is the PART 1 edit of plan/advance.md; PART 2 (T-multi-planner step) edits the SAME file, so that PART 2 task must dependsOn this one (same-file serialization, R137/R138/R139 precedent)."
- acceptance: plan/advance.md frontmatter grants mcp__ledger__get_reviewers; step 2.1 calls get_reviewers on the ledger MCP; no 'mcp__cq-config__' / 'cq-config MCP' reference remains.
- suggestedModel: fast
- dependsOn: ["T1"]
- ledgerRefs: ["goals:G18"]
- resultCommit: 84509c9
- completion: Repointed commands/plan/advance.md cq-config → ledger MCP (frontmatter + step 2.1); grep cq-config=0.
- sessionLogs: ["docs/logs/20260605-230151-ad16fd7116fe47304.md","docs/logs/20260605-230954-ae196b2e9666ad82c.md"]

### T11 — done

- createdAt: 2026-06-05T22:21:33.157Z
- updatedAt: 2026-06-05T23:43:10.086Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Refresh the Nix node-modules FOD hash + build all three Nix products green
- description: "The dependency graph changed (cq-config-mcp removed; @cq/config added as a dep of @cq/ledger-mcp) and bun.lock is regenerated, so the node-modules fixed-output derivation hash is stale. Per the repo convention: set flake.nix `outputHash` to 52 'A's, run `nix build .#node-modules` to get the `got:` hash, paste it back. Then build all three products to prove the merge + removal are sound: `nix build .#ledger-mcp`, `.#ledger-tui`, `.#ledger-web`. Confirm `.#cq-config-mcp` no longer exists as a buildable attr. This is the PART 1 closing gate — it depends on the package delete (T4), the flake edits + symlinks (T5), and the @cq/config dep wiring (T2)."
- acceptance: outputHash matches the freshly built node-modules; `nix build .#ledger-mcp .#ledger-tui .#ledger-web` all succeed; `nix build .#cq-config-mcp` fails with attr-not-found; the built ledger-mcp closure resolves @cq/config (get_reviewers/get_config callable). Also run `bun run check` from nix/pkg/cq-ledgers green.
- suggestedModel: frontier
- dependsOn: ["T2","T4","T5"]
- ledgerRefs: ["goals:G18"]
- resultCommit: 698e7df
- completion: Refreshed node-modules FOD outputHash (sha256-Ymy9b5It+yQLP9J0WtCFMgVk70ZD9NJYiZ7TOYmJGDw=) after cq-config-mcp removal + @cq/config dep; nix build .#node-modules/.#ledger-mcp/.#ledger-tui/.#ledger-web all green; .#cq-config-mcp attr-not-found; @cq/config resolves in ledger-mcp closure; bun run check 924/0. PART 1 closing gate passed.
- sessionLogs: ["docs/logs/20260605-233831-ae6ad820bbf4e4d96.md","docs/logs/20260605-233831-acb2103ec420aa076.md"]
