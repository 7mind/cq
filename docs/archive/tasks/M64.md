## M64

### T182 — done

- createdAt: 2026-06-06T00:36:50.328Z
- updatedAt: 2026-06-06T00:45:13.613Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Repoint cq-assets/README.md 'Configuration' section from cq-config MCP server to the ledger MCP
- description: |
    DOC-ONLY repoint of ONE section of ONE file: nix/pkg/cq-assets/README.md 'Configuration' section.
    
    Confirmed staleness (H23): README is the LAST live consumer documenting the removed standalone cq-config MCP server. Staleness is confined to:
    - L77 heading: '## Configuration — cq.toml and cq-config MCP'
    - L82-85 prose: 'The `cq-config` MCP server exposes `get_reviewers` over the `.mcp.json` interface ...'
    
    After G18 PART 1 (merged to main) the standalone cq-config server + its package + flake derivation + dev-llm.nix/.mcp.json registrations were deleted, and get_reviewers/get_config were merged into the ledger MCP (now surfaced as mcp__ledger__*; get_planners added in PART 2).
    
    FIX (verbatim suggestedFix):
    1. L77 heading → 'Configuration — cq.toml and the ledger MCP' (or drop the server name).
    2. L82-85 prose → state that cq.toml is parsed by the @cq/config PARSER package and surfaced by the LEDGER MCP server, which exposes get_reviewers/get_config (and get_planners) as mcp__ledger__* over .mcp.json — there is no standalone cq-config server. Optionally note that planners mirror reviewers via the shared [aliases] table (cq.toml.example).
    
    MODEL FORM: commands/cq/reviewers.md (T8-repointed) is the correct repointed reference form.
    
    SCOPE GUARD: the @cq/config PARSER lib (packages/cq-config) survives and is legitimate — any reference to the @cq/config PARSER must be KEPT, not removed. Only the standalone cq-config SERVER references are stale and must go. Edit the 'Configuration' section ONLY; touch no code.
    
    REPRODUCE-FIRST: satisfied by confirmed H23 evidence (README L77/L82-85 are the stale lines, validated against source). This is a documented doc edit; no test.
- acceptance: "(1) README 'Configuration' section (L77 heading + L82-85 prose) repointed to the ledger MCP: heading no longer names the standalone cq-config MCP server, prose states cq.toml is parsed by @cq/config parser and surfaced by the ledger MCP (get_reviewers/get_config/get_planners as mcp__ledger__*). (2) No standalone cq-config-SERVER reference remains anywhere in nix/pkg/cq-assets/README.md — verify e.g. `grep -ni 'cq-config' nix/pkg/cq-assets/README.md` returns only legitimate @cq/config PARSER-lib references (if any), and no 'cq-config MCP server' / standalone-server mention. (3) The @cq/config parser lib reference (if present) is preserved, NOT removed. (4) `bun run check` is green (no code touched)."
- suggestedModel: fast
- ledgerRefs: ["defects:D32","goals:G19"]
- resultCommit: 418b641
- completion: "Repointed nix/pkg/cq-assets/README.md 'Configuration' section off the removed standalone cq-config MCP server to the ledger MCP: heading → 'cq.toml and the ledger MCP'; prose → cq.toml parsed by @cq/config parser package, surfaced by the ledger MCP (get_reviewers/get_config/get_planners as mcp__ledger__*), planners mirror reviewers via shared [aliases]. @cq/config parser ref preserved; grep cq-config clean; bun run check 931/0. Fixes D32."
- sessionLogs: ["docs/logs/20260606-004218-aa6df8b4a497586c2.md","docs/logs/20260606-004501-abf629cdf1df480ad.md"]
