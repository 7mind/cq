## M62

### T12 — done

- createdAt: 2026-06-05T22:21:50.056Z
- updatedAt: 2026-06-05T23:56:59.183Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: "Add planners=[] to @cq/config (types + toml whitelist + parseConfig + resolvePlanners)"
- description: "Extend the @cq/config parser for a sibling top-level `planners = [...]` list of ALIAS names (Q102: SHARED [aliases] table, separate planners list). Changes: (1) packages/cq-config/src/types.ts — add `planners: readonly string[]` to CqConfig; (2) packages/cq-config/src/toml.ts — extend RawToml with `planners: readonly string[] | null` and whitelist the top-level `planners` key alongside `reviewers` (currently L139-143 throws on any non-'reviewers' top-level key); (3) packages/cq-config/src/config.ts — parseConfig populates `planners = raw.planners ?? []`; add `resolvePlanners(config): ReviewerToken[]` mirroring resolveReviewers (resolve each planners alias through [aliases], throw CqConfigError on a dangling alias); have loadConfig eagerly resolve planners too (so a dangling planner alias is caught at load); (4) packages/cq-config/src/index.ts — export resolvePlanners. Reuse ReviewerToken (a planner token is the same harness:model shape). Reproduce-first: add a failing parse test for a cq.toml carrying planners=[...] BEFORE extending the whitelist, confirm it fails on 'unexpected top-level key planners', then make it pass."
- acceptance: "A cq.toml with both reviewers=[...] and planners=[...] parses; resolvePlanners resolves planner aliases through [aliases] and throws on a dangling alias; loadConfig eagerly catches a dangling planner alias; new @cq/config unit tests cover parse + resolve + dangling cases (the whitelist test fails first for the right reason); `bun test` for @cq/config green."
- suggestedModel: standard
- ledgerRefs: ["goals:G18"]
- resultCommit: 84f1bfe
- completion: "Added planners=[] to @cq/config: CqConfig.planners + RawToml whitelist (reviewers+planners) + parseConfig + resolvePlanners (mirrors resolveReviewers, CqConfigError on dangling) + loadConfig eager resolve + index export. Reproduce-first whitelist test (round-1 fix: real assertions for unknown-key throw + planners acceptance). bun run check 931/0."
- sessionLogs: ["docs/logs/20260605-234840-a08f6196f1503d48b.md","docs/logs/20260605-234840-a8ec7c81273f99fb4.md","docs/logs/20260605-235512-ad92ef21623d13fd7.md","docs/logs/20260605-235512-a5d3920c5ea7b51cc.md"]

### T13 — done

- createdAt: 2026-06-05T22:22:01.505Z
- updatedAt: 2026-06-06T00:15:43.047Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Expose get_planners tool + planners on get_config in BOTH ledger-MCP surfaces (count 20->21)
- description: |
    Surface the new planner config in the merged ledger-MCP config tool on BOTH registration surfaces (R193 BLOCKER + count under-scoping apply to PART 2 too).
    (1) In the ledger-mcp config capability (added in T2) add a computePlanners() builder mirroring computeReviewers but calling @cq/config resolvePlanners (returns { configured, planners: [{harness,model,alias}] }); and extend computeConfig's payload with a `planners` field (the raw planner alias list), so get_config returns { configured, aliases, reviewers, planners }.
    (2) packages/ledger/src/mcp/ledgerTools.ts — add a `get_planners` tool() behind the config capability (same empty-input-schema/jsonResult shape as get_reviewers), and add `planners` to the get_config handler's returned object.
    (3) packages/ledger/src/mcp/stdioLedgerTools.ts — register the SAME get_planners tool via raw server.registerTool, and add `planners` to ITS get_config handler too (both surfaces must carry the tool or the standalone/embedded binary misses it — same R193 BLOCKER as T1).
    (4) Bump the tool-surface count 20->21 in BOTH the ledgerTools.ts header comment AND the stdioLedgerTools.ts header (~L4-5) + doc-comment (~L149); add get_planners to the shared LEDGER_TOOL_NAMES + the count/name test AND the cross-surface drift-guard test (both surfaces register the SAME set). SAME-FILE as T1/T3 — serialized via dependsOn. Reproduce-first: the count + cross-surface drift-guard assertion fails (21 vs 20, and flags a one-surface-only update) until both surfaces + the test are updated.
- acceptance: "get_planners surfaces on BOTH ledgerTools.ts and stdioLedgerTools.ts and returns the resolved planner set (configured:false when cq.toml absent or planners empty); get_config includes a planners field on BOTH surfaces; both headers (ledgerTools.ts + stdioLedgerTools.ts header + ~L149 doc-comment) say 21 tools; LEDGER_TOOL_NAMES + its count/name test AND the cross-surface drift-guard test include get_planners (test fails first 21 vs 20 / flags one-surface mismatch, then green); an end-to-end stdio roundtrip lists get_planners on the STDIO binary; `bun run typecheck` + `bun test` green."
- suggestedModel: frontier
- dependsOn: ["T12","T1","T2","T3"]
- ledgerRefs: ["goals:G18"]
- resultCommit: 7a3806d
- completion: "Exposed get_planners + planners-on-get_config across BOTH ledger-MCP surfaces (ledgerTools.ts tool() + stdioLedgerTools.ts registerTool); computePlanners added to ConfigCapability interface + impl (via @cq/config resolvePlanners); count 20→21 in both headers/doc-comments; LEDGER_TOOL_NAMES + count test toBe(21) + cross-surface drift-guard + e2e stdio roundtrip (configured:false / fixture planners). Round-1 fix: 5 stale '20-tool' doc/desc strings → 21. bun run check 931/0."
- sessionLogs: ["docs/logs/20260606-000540-a97be4a79798703b9.md","docs/logs/20260606-000540-a5a984688cc1bf754.md","docs/logs/20260606-001315-a1e705b98ecc858ce.md","docs/logs/20260606-001315-a33d24ff676791cdb.md"]

### T14 — done

- createdAt: 2026-06-05T22:22:13.984Z
- updatedAt: 2026-06-05T23:52:10.945Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Add a CANDIDATE MODE to the plan-advance agent (emit structured task-DAG JSON, write nothing)
- description: "Extend nix/pkg/cq-assets/agents/plan-advance.md with a CANDIDATE MODE branch used when the orchestrator launches the planner as one of N parallel planners (Q100 generate-N-then-judge; Q101 claude planners run as plan-advance subagents in this mode). In candidate mode the agent: grounds itself read-only (codegraph/Read/Grep/Glob, same as today), then for a goal whose state warrants a plan, EMITS a full candidate plan as a single fenced JSON block in its REPLY — shape { milestones: [{title, dependsOn?}], tasks: [{headline, description, acceptance, suggestedModel, dependsOn?, ledgerRefs}], rationale: '<why this DAG>' } — and WRITES NOTHING to the ledger (consistent with its existing disallowedTools Write/Edit/Bash; the candidate is returned, not persisted). The normal SINGLE-planner state-machine path (files questions / writes the plan / locks the decision) is UNCHANGED and remains the default; candidate mode is entered only when the orchestrator's prompt explicitly requests it (mirror how plan-reviewer has a 'configured mode' vs 'fallback mode'). Document the candidate JSON contract precisely so the orchestrator's synthesis judge and pi:* planners emit the SAME shape."
- acceptance: plan-advance.md documents both modes; candidate mode's JSON contract (milestones+tasks+rationale) is specified verbatim and matches the fields create_item needs; the default single-planner path is explicitly unchanged; the agent still writes nothing in candidate mode (no new tools granted).
- suggestedModel: frontier
- ledgerRefs: ["goals:G18"]
- resultCommit: 6f2397e
- completion: "Added mode-gated CANDIDATE MODE to agents/plan-advance.md: grounds read-only then emits a full task-DAG as one fenced JSON block {milestones,tasks,rationale} matching the tasks-ledger create_item schema (+ per-task `milestone` title-attachment for pre-persist refs), writing nothing; default single-planner state-machine path explicitly unchanged; entered only on explicit orchestrator request; no new tools."
- sessionLogs: ["docs/logs/20260605-234840-a30c47b88228fbda7.md","docs/logs/20260605-234840-adf84034426d6cc3d.md"]

### T15 — done

- createdAt: 2026-06-05T22:22:30.169Z
- updatedAt: 2026-06-06T00:23:52.926Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Add the multi-planner generate-N-then-JUDGE+SYNTHESIS step to plan/advance.md
- description: "Add a pluggable multi-PLANNER path to nix/pkg/cq-assets/commands/plan/advance.md, structurally mirroring the multi-reviewer step 2b but with the Q100 reconciliation model. Changes: (1) frontmatter allowed-tools — add mcp__ledger__get_planners (get_reviewers already present from T10). (2) New step at the planner stage (step 1 region): RESOLVE the active planner set — call mcp__ledger__get_planners; if absent/configured:false take the SINGLE-planner fallback (today's plan-advance subagent path, UNCHANGED); if configured:true take the MULTI-PLANNER path AND honor a session-only /cq:planners override (T16). (3) MULTI-PLANNER path: launch ALL active planners in PARALLEL as candidate-emitters — claude:<model> via Agent subagent_type plan-advance in CANDIDATE MODE (T14) returning candidate-plan JSON; pi:<model> via Bash `pi -p --no-tools --no-session --provider P --model M '<prompt>'` per K30 (strip code fence), fed the goal context + the candidate-plan JSON contract. Collect N candidate plans. (4) JUDGE+SYNTHESIS (Q100): a synthesis step (orchestrator-run, or a dedicated plan-synthesizer subagent) PICKS a strongest base candidate AND, critically, FOLDS IN valuable parts of the non-best candidates (it must NOT blindly discard them — when a non-best planner contributed something important, incorporate it) to produce ONE synthesized plan. (5) Only the ORCHESTRATOR writes that one synthesized plan to the ledger (create_milestone for work milestones + create_item the tasks under them, record milestone ids on the goal, transition clarifying->planning), so pi:* planners participate fully (Q101 — orchestrator owns all writes). Document that the synthesized plan then enters the SAME reviewer loop (step 2) unchanged. SAME-FILE as T10 (PART 1's plan/advance.md edit) — serialized via dependsOn."
- acceptance: "plan/advance.md documents resolve-planners + single-planner fallback + multi-planner generate-N-then-judge-with-SYNTHESIS (explicitly: judge folds in good parts of non-best plans, not pick-best); pi:* planners are candidate-emitters with the orchestrator owning all ledger writes; the synthesized plan flows into the existing reviewer loop; frontmatter grants mcp__ledger__get_planners; the single-planner path is explicitly preserved as the unconfigured fallback."
- suggestedModel: frontier
- dependsOn: ["T10","T13","T14"]
- ledgerRefs: ["goals:G18"]
- resultCommit: d949db6
- completion: "Added multi-planner generate-N-then-JUDGE+SYNTHESIS path to commands/plan/advance.md: step-1 resolve-planners (get_planners) → configured:false/absent keeps unchanged single-planner fallback → configured:true fans out claude (plan-advance CANDIDATE mode) + pi (K30 shellout) candidate-emitters → judge+synthesis folds in non-best (not pick-best) → orchestrator owns all writes → synthesized plan re-enters unchanged reviewer loop. Frontmatter grants get_planners. bun run check 931/0."
- sessionLogs: ["docs/logs/20260606-002059-a9b5c5cc39c67e136.md","docs/logs/20260606-002059-aa87f457459f24fec.md"]

### T16 — done

- createdAt: 2026-06-05T22:22:43.150Z
- updatedAt: 2026-06-06T00:23:58.648Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Add commands/cq/planners.md (session-only planner-set override, mirror reviewers.md)
- description: "Create nix/pkg/cq-assets/commands/cq/planners.md mirroring commands/cq/reviewers.md 1:1 for PLANNERS (Q102). It is the SESSION-ONLY override: parse a natural-language planner instruction (e.g. '/cq:planners use grok and opus only') into alias names, resolve each via cq.toml [aliases] (from mcp__ledger__get_config / get_planners) with the SAME hardcoded fallback map (grok->pi:grok-build, opus->claude:opus-4.8[1m], codex->pi:gpt-5.5), echo the active planner set, and state clearly the override is SESSION-ONLY (reverts on the next fresh /plan:advance). frontmatter allowed-tools: mcp__ledger__get_planners, mcp__ledger__get_config, Read. Writes NOTHING durable (no Write tool). Document the consistency note with /plan:advance exactly as reviewers.md does (the orchestrator reads the stated active planner set from run context; no override => get_planners default; configured:false => single native Claude planner). Depends on T13 (get_planners/get_config.planners must exist)."
- acceptance: "commands/cq/planners.md exists, mirrors reviewers.md structure (parse->resolve->echo->session-only note), grants only mcp__ledger__get_planners + get_config + Read, writes nothing durable, and references /plan:advance's planner-resolution step; the fallback alias map matches reviewers.md."
- suggestedModel: standard
- dependsOn: ["T13"]
- ledgerRefs: ["goals:G18"]
- resultCommit: b3278b3
- completion: "Added commands/cq/planners.md — session-only planner-set override, 1:1 mirror of reviewers.md (parse→resolve→echo→session-only→/plan:advance note). Frontmatter exactly get_planners+get_config+Read (writes nothing durable); fallback alias map identical to reviewers.md; implement-flow refs correctly dropped (plan-flow scope). bun run check 931/0."
- sessionLogs: ["docs/logs/20260606-002059-a1c4cb86ab04c0bb7.md","docs/logs/20260606-002059-a3c37c74fc93f48f3.md"]

### T17 — done

- createdAt: 2026-06-05T22:22:55.670Z
- updatedAt: 2026-06-06T00:31:39.161Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: "PART 2 closing gate: full check + cq-assets packaging picks up planners.md + planners docs"
- description: "Verify the whole PART 2 surface is sound and packaged. (1) Run `bun run check` (typecheck + lint + test) from nix/pkg/cq-ledgers green — includes the new @cq/config planners tests (T12) and the get_planners tool/count tests (T13). (2) Build the cq-assets product (the derivation that installs commands/ + agents/, e.g. `nix build .#<cq-assets-attr>`) and confirm commands/cq/planners.md + the modified plan/advance.md + agents/plan-advance.md are present in the built output (the new command file must be picked up by the asset fileset — add it to the fileset/glob if the packaging enumerates files explicitly). (3) If the repo carries an example/docs cq.toml or a config reference doc that lists `reviewers`, add a `planners = [...]` example + a one-line note that planners mirror reviewers (SHARED [aliases]); do NOT invent a docs file if none exists. This is the PART 2 convergence gate; depends on all PART 2 tasks."
- acceptance: "`bun run check` green; the cq-assets build output contains commands/cq/planners.md and the updated plan/advance.md + plan-advance.md; if an example cq.toml/config doc exists it shows a planners=[] sibling example; no stale mcp__cq-config__ reference remains anywhere (repo-wide grep clean)."
- suggestedModel: frontier
- dependsOn: ["T12","T13","T14","T15","T16"]
- ledgerRefs: ["goals:G18"]
- resultCommit: 7e403fe
- completion: "PART 2 convergence gate: bun run check green 931/0 (incl T12/T13 planner tests); verified cq-assets bundle = eval-time .#llmAssets glob (assets.nix collectMd recursive readDir) auto-includes commands/cq/planners.md + T15 plan/advance.md + T14 agents/plan-advance.md (confirmed via nix eval); added planners=[] sibling + shared-[aliases] note to cq.toml.example; git grep mcp__cq-config__ ZERO in live files (only docs archives/logs historical). README cq-config repoint = D32 (separately tracked)."
- sessionLogs: ["docs/logs/20260606-002814-aa74506521b3587a5.md","docs/logs/20260606-002814-ad5aae722ae7162ff.md"]
