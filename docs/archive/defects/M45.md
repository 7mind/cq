## M45

### D27 — resolved

- createdAt: 2026-06-03T19:05:59.100Z
- updatedAt: 2026-06-03T20:37:22.244Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: "advance.md handoff STANDALONE-trigger wording leaves a seam under /<flow>:start inline chaining"
- severity: low
- description: "Filed out-of-scope by the T154 reviewer. In each */advance.md §Handoff record, the STANDALONE branch triggers on 'user invoked /<flow>:advance directly, with no wrapping flow command' while the CHAINED branch names only /advance (investigate/advance.md also names /plan:advance). When /<flow>:start runs its advance pass INLINE, that pass HAS a wrapping flow command (the start command) that is NOT /advance, so neither branch's literal trigger fires cleanly. Correctness is preserved because each start.md/follow-up.md externally directs the inline pass to take the STANDALONE branch + anchors a single 'ONE handoffs record' write (no duplicate, no omission) — but advance.md's trigger text alone is ambiguous for /<flow>:start."
- suggestedFix: "Reword each */advance.md CHAINED trigger to fire on ANY wrapping flow command: 'CHAINED INLINE by any wrapping flow command (/advance, /plan:advance, or a /<flow>:start / /<flow>:follow-up that runs this pass inline) → SUPPRESS; the outermost wrapper owns the single handoff write.' Keeps one authoritative write at the outermost wrapper, removing reliance on start.md's external override. Prompt-only; all 3 advance.md files."
- ledgerRefs: ["tasks:T154","goals:G11"]
- rootCause: "CONFIRMED (H17). The */advance.md handoff CHAINED branch (plan/advance.md:283, implement/advance.md:290) names only /advance; investigate/advance.md:282 additionally names /plan:advance. A /<flow>:start or /<flow>:follow-up that runs the advance pass inline is a wrapping flow command that is NEITHER /advance NOR /plan:advance, so neither branch's literal trigger covers it. Correctness today rests on start.md/follow-up.md externally directing the inline pass to the STANDALONE branch — advance.md's trigger text alone is ambiguous."
- dependsOn: ["T160"]
- fix: "T160 (merged 311b8a1): reworded the CHAINED handoff trigger in all 3 */advance.md + made the 4 start/follow-up wrappers the single handoff writer (inline pass suppresses). One writer per invocation; no duplicate/omitted handoff; the start/follow-up trigger seam is closed self-containedly. Sole fix task done."
