## M48

### D28 — resolved

- createdAt: 2026-06-03T20:39:43.227Z
- updatedAt: 2026-06-03T21:00:34.595Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: TOCTOU window between realpath containment check and fs.readFile in readLog
- severity: low
- description: "Filed out-of-scope by the T158 reviewer (round 1). In FsLedgerStore.readLog, after T158 the containment is verified via real = fs.realpath(resolved) + startsWith(realLogsDir), but the subsequent read is fs.readFile(resolved) — the NON-canonical path that still contains the symlink(s). readFile re-follows symlinks, so a symlink at/under `resolved` swapped to an out-of-root target between the check and the read could bypass confinement. PRE-EXISTING (the check-then-read structure predates T158; T158 closed the persistent-symlink escape but left this transient race). Threat model is weak: read_log is a read-only capability confined to the server's OWN docs/logs; an actor able to swap a symlink inside docs/logs already controls the directory contents read_log returns, so the race grants no privilege escalation."
- suggestedFix: "Read the validated CANONICAL path: hoist `real` out of the try and `fs.readFile(real)` (not `resolved`). `real` has no symlink components, so the read follows nothing and the validated path === the read path, closing the TOCTOU. Preserve ENOENT behaviour: when realpath(resolved) throws ENOENT (missing file), fall back to reading `resolved` so the normal not-found error still surfaces (don't mask it). Add a regression test asserting the canonical path is read / a post-check symlink swap does not escape. ~2-3 lines + test."
- ledgerRefs: ["tasks:T158","goals:G13"]
- rootCause: "CONFIRMED (H18). FsLedgerStore.readLog (on main 311b8a1) computes `real = fs.realpath(resolved)` and validates `real` against realLogsDir INSIDE a try block, but the actual read is `fs.readFile(resolved)` after the block — reading the non-canonical, symlink-bearing `resolved`. A symlink swapped between the realpath check and the readFile follows to its new target at read time (TOCTOU). Pre-existing check-then-read structure; T158 closed the persistent-symlink escape but the read still targets `resolved`. Severity low: read-only tool confined to the server's own docs/logs; an actor able to swap an in-docs/logs symlink already controls the returned content (no privilege escalation)."
- dependsOn: ["T161"]
- fix: "T161 (merged 537017f): FsLedgerStore.readLog now reads the validated canonical path via fs.readFile(real ?? resolved) instead of the symlink-bearing resolved, so a symlink swapped between the realpath check and the read no longer escapes confinement; ENOENT for a missing file still surfaces normally. Deterministic TOCTOU regression test added (spies fs.realpath to swap the target at the check→use boundary; verified to fail against the pre-fix read). Sole fix task done."
