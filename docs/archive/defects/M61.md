## M61

### D32 — resolved

- createdAt: 2026-06-05T23:11:17.516Z
- updatedAt: 2026-06-06T00:45:21.150Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: cq-assets/README.md still references the removed cq-config MCP server (consumer missed by G18 PART 1 repoint)
- severity: low
- description: "Filed from T10's review (out-of-scope, file-and-defer). G18 PART 1's consumer-repoint tasks T8/T9/T10 covered the three command files (commands/cq/reviewers.md, commands/implement/advance.md, commands/plan/advance.md) but the plan MISSED nix/pkg/cq-assets/README.md, which still documents the cq-config MCP server / its get_reviewers/get_config tools. After T1-T7 merge (cq-config server merged into the ledger MCP + the standalone server/registrations removed), README's cq-config references are STALE. Fix: repoint README's cq-config references to the ledger MCP (get_reviewers/get_config now live there) and remove the standalone-cq-config-server mention. Low severity (doc-only). NOTE: gates M61 archival until resolved."
- suggestedFix: "Edit nix/pkg/cq-assets/README.md 'Configuration' section only: (1) L77 heading → 'Configuration — cq.toml and the ledger MCP' (or drop the server name); (2) L82-85 prose → state that cq.toml is parsed by the @cq/config parser and surfaced by the LEDGER MCP server, which exposes get_reviewers/get_config (and get_planners) as mcp__ledger__* over .mcp.json — there is no standalone cq-config server. Optionally note planners mirror reviewers via the shared [aliases] table (cq.toml.example). Doc-only; bun run check stays green (no code touched)."
- ledgerRefs: ["goals:G18","goals:G19"]
- rootCause: "Confirmed (H23): nix/pkg/cq-assets/README.md is the LAST live consumer still documenting the removed standalone cq-config MCP server. The staleness is confined to the 'Configuration' section: L77 heading '## Configuration — cq.toml and cq-config MCP' and L82-85 prose 'The `cq-config` MCP server exposes `get_reviewers` over the `.mcp.json` interface ...'. After G18 PART 1 (merged to main), the standalone server + its package + flake derivation + dev-llm.nix/.mcp.json registrations were deleted and get_reviewers/get_config were merged into the ledger MCP (now mcp__ledger__*; get_planners added in PART 2). Validated against source: .mcp.json registers only the 'ledger' server; the Bun workspace has no packages/cq-config-mcp (only the surviving @cq/config PARSER lib at packages/cq-config); commands/cq/reviewers.md (T8) is the correct repointed model form. No mcp__cq-config__ literal and no stale asset/consumer table row exist in README — only the L77-85 prose."
- dependsOn: ["T182"]
- fix: "Resolved via T182 (commit 418b641, merged to main): repointed nix/pkg/cq-assets/README.md 'Configuration' section off the removed standalone cq-config MCP server to the ledger MCP — heading 'cq.toml and the ledger MCP'; prose attributes cq.toml parsing to the @cq/config parser package and surfacing to the ledger MCP (get_reviewers/get_config/get_planners as mcp__ledger__*). README is no longer a stale consumer; grep cq-config clean. Confirmed root cause H23. D32.dependsOn=[T182], now done — orchestrator-owned closure."
