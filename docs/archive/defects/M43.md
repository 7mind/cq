## M43

### D25 — resolved

- createdAt: 2026-06-03T17:18:03.592Z
- updatedAt: 2026-06-03T20:37:16.796Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: Pre-existing unused eslint-disable directive in ledger-tui navRenderBytes test
- severity: low
- description: "Filed out-of-scope by the T145 reviewer. packages/ledger-tui/test/navRenderBytes.test.tsx:291 emits an ESLint WARNING 'Unused eslint-disable directive (no problems were reported from no-console)'. Pre-existing (introduced with the file by T133, not by any G11 task); does NOT fail `bun run check` (the lint script sets no --max-warnings 0). Cosmetic."
- suggestedFix: "Remove the stale `// eslint-disable-next-line no-console` comment at navRenderBytes.test.tsx:291 (keep the console.log). Trivial; auto-fixable via eslint --fix."
- ledgerRefs: ["tasks:T145","goals:G11"]
- rootCause: "CONFIRMED (H15). navRenderBytes.test.tsx:291 carries `// eslint-disable-next-line no-console` over the console.log at :292, but eslint.config.js enables no `no-console` rule, so the directive is unused → eslint 'Unused eslint-disable directive' warning. Non-fatal (lint script sets no --max-warnings 0), pre-existing (T133)."
- dependsOn: ["T159"]
- fix: "T159 (merged 311b8a1): removed the stale unused eslint-disable directive at navRenderBytes.test.tsx:291. Sole fix task done."

### D26 — resolved

- createdAt: 2026-06-03T17:46:49.469Z
- updatedAt: 2026-06-03T20:37:19.621Z
- author: "opus-4.8[1m]"
- session: ea0ee283-9e2d-4088-a61a-86fac464e29b
- headline: read_log does not resolve symlinks before its containment check (potential symlink escape)
- severity: low
- description: Filed out-of-scope by the T147 reviewer. FsLedgerStore.readLog (packages/ledger/src/store/FsLedgerStore.ts) validates containment via a lexical prefix check on path.resolve(logsDir, relPath) but never calls fs.realpath. A symlink located INSIDE <root>/docs/logs/ that targets a path outside the root (e.g. ../../etc/passwd) passes the string-prefix check (the link's own path is under logsDir) and fs.readFile follows it, reading content outside the confinement root. Out of scope for T147 (its acceptance scoped confinement to lexical `..`/absolute rejection; the dispatch brief explicitly marked symlinks out of scope), and low-likelihood (requires FS write access inside docs/logs), but a genuine latent path-confinement gap in a file-read tool.
- suggestedFix: "After the lexical guard, fs.realpath(resolved) and re-assert the realpath === logsDir || startsWith(logsDir + path.sep); throw the same escape error otherwise. Catch ENOENT so a genuinely missing file still surfaces the normal not-found error (do not mask it as an escape). ~3 lines. Add a test: create a symlink under docs/logs pointing outside, assert read_log rejects it."
- ledgerRefs: ["tasks:T147","goals:G11"]
- rootCause: "CONFIRMED (H16). FsLedgerStore.readLog (FsLedgerStore.ts:1257-1267) validates containment with a lexical `path.resolve(logsDir, relPath)` + `startsWith(logsDir + path.sep)` check and never calls fs.realpath, then fs.readFile(resolved) follows symlinks. A symlink located inside docs/logs/ whose target escapes the root passes the lexical guard (its own path is under logsDir) and its target is read. Genuine latent path-confinement gap; low likelihood (needs FS write inside docs/logs)."
- dependsOn: ["T158"]
- fix: "T158 (merged 311b8a1): FsLedgerStore.readLog now realpath-resolves both the target and the confinement root before the containment check, so an in-docs/logs symlink whose target escapes is rejected; ENOENT not masked; legitimate reads under a symlinked-root parent still succeed. +2 regression tests. Sole fix task done. (Residual pre-existing TOCTOU tracked separately as D28.)"
