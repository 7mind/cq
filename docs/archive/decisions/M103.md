## M103

### K52 — locked

- createdAt: 2026-06-08T11:20:02.514Z
- updatedAt: 2026-06-08T11:20:02.514Z
- author: "opus-4.8[1m]"
- session: $CLAUDE_CODE_SESSION_ID
- headline: "G32/D39 scope: defer cross-ledger open-question resolution + drained predicate-gate (T260)"
- rationale: "Up-front scope decision for G32 (D39 fix). The LOAD-BEARING enforcement is the per-status NON-EMPTY checks (assertHandoffInvariants: mixed/answers-required⇒non-empty blockingQuestions, user-action-required⇒non-empty handoffReasons), which by construction make the reproduced HO22/HO25/HO26 launder (each was `mixed` with an EMPTY blockingQuestions[]) UNWRITABLE. The two OPTIONAL hardenings from D39.suggestedFix are DEFERRED: (A) cross-ledger 'each blockingQuestions id resolves to an EXISTING `open` question' — deferred (needs F2-style cross-ledger store plumbing; a dangling/closed id is lower-likelihood + lower-severity; a non-empty blockingQuestions[] already closes the launder); (B) `drained` predicate-gate restatement — deferred to the prompt layer (W2's turn-vs-run framing covers the legitimate-drained discipline; no machine-checkable field carries it). If (A) is wanted later it is a separate goal."
- ledgerRefs: ["goals:G32","defects:D39"]
