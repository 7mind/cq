# T679 — Re-measuring the shipped ledger MCP response contract against the RS3 corpus

Task: `tasks:T679` (goal `goals:G93`, validates `researches:RS3`).
Date: 2026-07-25. Harness: `docs/drafts/20260725-2130-t679-rs3-remeasure/`.

## 0. What kind of number each figure is

Three different quantities appear below. They are never mixed, and none of them
is a billing figure.

| Label used below | Meaning |
|---|---|
| **measured wire shape** | UTF-8 bytes and `o200k_base` tokens of one serialized MCP request or response, produced by the code under measurement. |
| **RS3 simulated bound** | RS3's own hand-modelled "what a compact/ack contract could remove" number. RS3 called it an upper bound; it is quoted here only for direction, never as a realized result. |
| **scenario** | An arithmetic combination of measured quantities under an explicitly stated assumption (e.g. "the tool schema is paid once per subagent context"). A scenario is a conditional, not an observation. |

**No claim in this report converts any of the above into realized billing
reduction.** Realized cost depends on call mix, prompt/tool-schema caching, host
framing, and reasoning length — none of which this probe observes. `o200k_base`
is a reproducible accounting proxy, not the tokenizer Anthropic models bill
with; RS3 used the same proxy, which is what makes the two comparable.

## 1. Corpus, commit, tokenizer

| Field | Value |
|---|---|
| Corpus | 357 raw subagent JSONL transcripts, 95,152,796 bytes — byte-identical to the corpus RS3 states it measured |
| Corpus location | `$XDG_STATE_HOME/cq/projects/9faab3c136afe411b16a43206b14f834382ed440/logs/raw` (the project key whose `logs/` also holds the RS3 session logs) |
| Corpus pin | `docs/drafts/20260725-2130-t679-rs3-remeasure/corpus-manifest.json` — per-file name + size + sha256; the probe aborts on any drift |
| Parse result | 26,688 JSONL lines, 0 parse failures, 357/357 files, 710 ledger tool calls across 15 distinct tools |
| RS3 baseline commit | `3fe3b8a7935f3027218581e76bb9da2ce1b833e2` |
| Tree under measurement | this worktree at `337f6f00` (`nix/pkg/cq-ledgers`) |
| Tokenizer | `gpt-tokenizer@3.4.0`, encoding `o200k_base` — the tokenizer and version RS3 used |
| Bun | 1.3.13 |

**Corpus identity was recovered, not substituted.** RS3's probe script was never
committed (`debug/` at `3fe3b8a7` contains one unrelated shell script), so the
corpus was re-identified from RS3's own stated invariants: the set of `*.jsonl`
transcripts in that project's raw-log area predating the RS3 run is exactly 357
files totalling exactly 95,152,796 bytes. Two further coincidences confirm the
identification: RS3's cited transcript `20260621-144654-a2cfaa49e796ab47e.jsonl`
lives in that directory and no other, and the per-tool call counts reproduce
RS3's inventory (see §2).

An independent methodological cross-check: running this report's
`schema-overhead.ts` against the RS3-era tree yields **4,537** tokens for the
27-tool schema where RS3 reported **4,539** — a 2-token (0.04%) difference. The
measurement method used here therefore reproduces RS3's, so the before/after
comparison is between like quantities.

## 2. Method

RS3 simulated the "after" side. This task does not: for every captured tool
result, the after-shape is computed by passing the captured payload through the
functions the shipped server actually calls —
`projectItemDto`, `projectMilestoneItemGroupsDto`, `projectFtsSearchResultsDto`,
`projectFetchedMilestoneDto`, `projectItemMutationAckDto`,
`projectMilestoneMutationAckDto`, `projectLedgerMutationAckDto` from
`packages/ledger/src/mcp/wireResponseContract.ts` — inside the same envelopes
those handlers build (`{item: …}`, `{items: …}`, `{results: …}`,
`{milestone: …}`, `{ledger: {id}}`) and serialized as the same minified JSON.

Where the corpus is silent (five cutover-affected tools have zero captured
calls), §6 measures the real RS3-era server and the real current server side by
side on an identical operation script instead of modelling them.

Exact commands: `docs/drafts/20260725-2130-t679-rs3-remeasure/README.md`.

## 3. Per-tool result on the RS3 corpus (measured wire shapes)

`before` = captured response text as the RS3-era server produced it.
`after` = the shipped response for the same payload (compact projection for
projection tools, fixed acknowledgement for mutations, byte-identical for the
contractually unchanged classes). Bytes and tokens are both given, because they
are not interchangeable.

| Tool | Contract class | Calls | Replayed | Before bytes | After bytes | Before tok | After tok | Saved tok | Saved % | Median before/after tok |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `fetch_item` | mandatory projection | 366 | 360 | 995,049 | 145,402 | 262,466 | 49,515 | 212,951 | 81.13% | 592 → 133 |
| `list_milestone_items` | mandatory projection | 155 | 155 | 982,981 | 251,049 | 256,111 | 80,347 | 175,764 | 68.63% | 1272 → 380 |
| `fts_search` | mandatory projection | 21 | 19 | 228,380 | 55,105 | 61,719 | 17,270 | 44,449 | 72.02% | 2626 → 710 |
| `create_item` | fixed ack | 70 | 70 | 125,098 | 17,276 | 31,713 | 7,241 | 24,472 | 77.17% | 428 → 104 |
| `update_item` | fixed ack | 26 | 26 | 83,823 | 6,309 | 22,181 | 2,673 | 19,508 | 87.95% | 741 → 98 |
| `fetch_milestone` | mandatory projection | 26 | 19 | 13,196 | 10,288 | 3,564 | 2,924 | 640 | 17.96% | 134 → 134 |
| `create_milestone` | fixed ack | 4 | 4 | 2,020 | 532 | 568 | 220 | 348 | 61.27% | 142 → 55 |
| `get_config` | requested full content | 5 | 5 | 5,669 | 5,669 | 1,674 | 1,674 | 0 | 0% | 322 → 322 |
| `get_reviewers` | purpose-built small | 22 | 22 | 3,952 | 3,952 | 1,210 | 1,210 | 0 | 0% | 59 → 59 |
| `get_agent_models` | purpose-built small | 2 | 2 | 3,846 | 3,846 | 960 | 960 | 0 | 0% | 480 → 480 |
| `enumerate_ledgers` | purpose-built small | 1 | 1 | 1,578 | 1,578 | 438 | 438 | 0 | 0% | 438 → 438 |
| `fetch_ledger_archive` | requested full content | 2 | 2 | 476 | 476 | 159 | 159 | 0 | 0% | 79.5 → 79.5 |
| `derive_predicates` | purpose-built small | 3 | 3 | 515 | 515 | 149 | 149 | 0 | 0% | 48 → 48 |
| `fetch_ledger` | mandatory projection | 6 | 0 | — | — | — | — | — | — | not replayable |
| `fetch_prompt` | requested full content | 1 | 0 | — | — | — | — | — | — | captured result is an error |
| **Total (replayed)** | | **710** | **688** | **2,446,583** | **501,997** | **642,912** | **164,780** | **478,132** | **74.37%** | |

Accounting for all 710 calls: 688 replayed, 14 captured error results (excluded
— the contract does not change error text), 8 unreplayable (6 `fetch_ledger` and
2 `fts_search` whose captured "result" is a host oversize notice, the payload
having been elided before it reached the transcript; the saved-payload files the
notices point at no longer exist, so this is the same blind spot RS3 recorded).

### Direction versus RS3

| Tool | RS3 simulated saving (upper bound) | Measured wire-shape saving now |
|---|---:|---:|
| `fetch_item` | 69.89% | 81.13% |
| `list_milestone_items` | 56.75% | 68.63% |
| `fts_search` | 61.61% | 72.02% |
| `create_item` | 91.61% | 77.17% |
| `update_item` | 95.51% | 87.95% |
| `create_milestone` | 73.24% | 61.27% |

The five high-volume methods RS3 named retain material savings in the direction
RS3 predicted. The three read methods exceed RS3's simulated share because the
shipped compact projection drops more than RS3 modelled; the three mutation
methods fall short of it because the shipped acknowledgement retains more than
RS3 modelled (`milestoneId`, both timestamps, provenance, and canonicalized
reference arrays). Neither difference is a regression; both are consequences of
the contract that was actually chosen, and they are the reason a re-measurement
was required rather than reusing RS3's figures.

Aggregate framing, stated precisely: **on this corpus the shipped response
shapes serialize 478,132 fewer `o200k_base` tokens (74.37%) and 1,944,586 fewer
bytes (79.5%) than the responses the RS3-era server actually returned, for the
same 688 calls.** RS3's headline "419,366 tokens removed (65.6%)" was a
simulated bound over a slightly different candidate set; the two numbers are of
different kinds and are not netted against each other anywhere in this report.

## 4. Request-side (schema and input) overhead — measured

Response savings are not free. Both trees were booted as real stdio MCP servers
and asked for `tools/list`; the returned tool definitions were tokenized.

| Metric | RS3-era tree (`3fe3b8a7`) | This tree (`337f6f00`) | Δ |
|---|---:|---:|---:|
| Tools | 27 | 27 | 0 |
| `tools/list` bytes | 19,318 | 30,493 | +11,175 |
| `tools/list` tokens | 4,537 | 7,035 | **+2,498 (+55.1%)** |

Decomposition of the growth, by mechanically stripping each contract artefact
from the current schema (counterfactual strips of the measured schema, labelled
as such):

| Artefact | Tokens |
|---|---:|
| `projection` input property + its description sentence (6 tools) | 1,111 |
| Fixed-acknowledgement sentences in tool descriptions | 542 |
| `Authoritative response: …` block appended to every description | 1,162 |
| All three combined (they overlap) | **2,214** |
| Residual growth not attributable to the response contract | 284 |

Per-call input cost, measured over the corpus: adding the mandatory
`projection` argument costs **+4 tokens per call** (identical for `"compact"`
and `"full"`), i.e. **+2,296 tokens** across the 574 projection-bearing calls in
this corpus.

So the contract's request side costs a measured **2,214 tokens once per context
that is shown the tool list**, plus **4 tokens per projection-bearing call**.

## 5. Full-content control

Compact-only measurement would flatter the result, because `projection:"full"`
still exists and is used deliberately. Two controls:

1. **Corpus control.** For all 553 replayed projection-tool calls the probe also
   computed the `projection:"full"` response and compared it to the captured
   payload: `fullControlDeltaTokens = 0` for every tool, and a canonicalized
   deep-equality assertion (`full === captured payload`) passed on every call.
   The full path returns exactly what the RS3-era server returned — it saves
   nothing and loses nothing, and it now costs +4 input tokens per call.
2. **Requested-full-content class.** `read_log`, `fetch_prompt`,
   `fetch_ledger_archive`, `get_config` are declared `requested-full-content` /
   `purpose-built-small` in `LEDGER_RESPONSE_CONTRACTS`, and the corpus confirms
   byte-identical volume for those of them that appear with a non-error result
   (`get_config` and `fetch_ledger_archive`: 1,833 tokens over 7 calls,
   unchanged).

A useful consequence for call-mix reasoning: if a fraction *f* of today's
projection-bearing calls must use `"full"`, the response saving scales by
(1 − *f*) while the +4/call input cost is paid regardless. At *f* = 1 the
contract is a pure request-side cost. **This is a scenario, not a measurement of
how callers actually split** — the corpus predates the contract and therefore
contains no evidence about the future full/compact mix.

## 6. Low-volume cutover check (twin-server differential)

Q313 answered "apply the contract to every structurally eligible method", so
tools with no corpus usage were changed too. For these the corpus can say
nothing, so the RS3-era server and the current server were driven with an
**identical deterministic operation script** (in-memory store, pinned clock,
pinned ids) and their real responses compared.

| Operation | Before tok | After tok | Δ response | Δ args | Δ net |
|---|---:|---:|---:|---:|---:|
| `create_ledger` (typical schema) | 100 | 8 | −92 | 0 | −92 |
| `create_milestone` (small) | 71 | 55 | −16 | 0 | −16 |
| `update_milestone` (status only) | 71 | 55 | −16 | 0 | −16 |
| `create_item` (minimal: headline only) | 74 | 70 | −4 | 0 | −4 |
| `create_item` (narrative + refs) | 793 | 77 | −716 | 0 | −716 |
| `update_item` (status only, small item) | 75 | 71 | −4 | 0 | −4 |
| `update_item` (status only, narrative item) | 794 | 78 | −716 | 0 | −716 |
| `reopen_item` | 75 | 71 | −4 | 0 | −4 |
| `unarchive_item` | 793 | 77 | −716 | 0 | −716 |
| `search_items` | 868 | 158 | −710 | +4 | −706 |
| `fts_search` | 918 | 208 | −710 | +4 | −706 |
| `list_milestone_items` | 872 | 162 | −710 | +4 | −706 |
| `fetch_item` (narrative item) | 794 | 84 | −710 | +4 | −706 |
| `fetch_item` (narrative item, **full**) | 794 | 794 | 0 | +4 | **+4** |
| `fetch_item` (small item, compact) | 75 | 75 | 0 | +4 | **+4** |
| `fetch_item` (small item, **full**) | 75 | 75 | 0 | +4 | **+4** |
| `fetch_milestone` (small) | 102 | 96 | −6 | +4 | −2 |
| `snapshot` / `derive_predicates` / `archive_milestone` (unchanged controls) | 94 / 91 / 33 | 94 / 91 / 33 | 0 | 0 | 0 |
| `enumerate_ledgers` (unchanged control) | 402 | 435 | **+33** | 0 | **+33** |

**No acknowledgement or compact projection made any response larger** — not in
the 25 twin operations and not in any of the 688 corpus calls
(`worstRegressionTokens = 0` for every tool). This is also structural: every
acknowledgement and compact item is a key-subset of the entity the old server
returned, so it cannot exceed it. The uniform cutover's small acknowledgements
are genuinely small: 8 tokens for `create_ledger`, 55 for milestone acks, 70–78
for item acks regardless of item size.

Two non-savings are worth naming rather than averaging away:

* **Small items pay +4 tokens and save 0.** When an item carries only
  compact-allowlisted fields, compact ≡ full, so the mandatory parameter is pure
  cost. This is not hypothetical: **11 of 19** replayed `fetch_milestone` calls
  and **4 of 19** `fts_search` calls in the corpus were exactly break-even on the
  response side, which is why `fetch_milestone` saves only 17.96% overall. RS3
  had classified `fetch_milestone` as purpose-built-small and recommended *no
  change* for it; the uniform cutover made it projection-bearing anyway, and on
  this corpus that decision is roughly a wash (−640 response tokens against +104
  input tokens over 26 calls).
* **`enumerate_ledgers` grew 33 tokens between the two commits.** Investigated,
  and it is **not** a contract regression: the response shape is unchanged, and
  the growth is one extra bootstrap ledger (`upstream`) that exists in the
  current tree and did not exist at `3fe3b8a7`. Out of scope for T679, no action
  proposed.

## 7. Compact / acknowledgement correctness

Two independent checks, both green.

1. **Corpus assertions** (in `remeasure.ts`, run over all 688 replayed calls,
   `correctnessProblemCount = 0`):
   * compact retains `id`, `milestoneId`, `status`, `createdAt`, `updatedAt`,
     and `author`/`session` when the source item had them;
   * compact emits no field outside `COMPACT_ITEM_FIELD_NAMES`, and loses no
     allowlisted field that the source item had;
   * `fts_search` compact preserves `score` and `matchedFields`;
   * acknowledgements retain a non-empty allocated `id`, `milestoneId`,
     `status`, both timestamps, and the canonicalized `dependsOn` / `blockedBy`
     / `ledgerRefs` arrays, and leak no narrative field;
   * `projection:"full"` is canonically deep-equal to the captured full payload.
2. **Shipped test suite**: `bun test packages/ledger/test/wire-response-contract.test.ts
   packages/ledger/test/direct-read-projection.test.ts
   packages/ledger/test/cq-tool-response-contract.test.ts
   packages/ledger-mcp/test/responseContractDocs.test.ts` → 31 pass, 0 fail, 309
   expectations.

## 8. Net accounting, and what it does not say

Measured, on this corpus:

* response side: **−478,132** tokens (−1,944,586 bytes) over 688 calls;
* per-call request side: **+2,296** tokens over 574 projection-bearing calls;
* cold schema: **+2,214** tokens attributable to the contract, once per context
  that receives the tool list (+2,498 measured total growth between the two
  commits, of which 284 is unrelated).

The first two are directly comparable — same calls, same corpus — and net to
**−475,836 tokens**. The third is per-context, not per-call, so combining it
requires an assumption about how many contexts pay it. The corpus contains 357
transcripts, of which **198 make at least one ledger call**:

| Scenario (assumption stated) | Corpus-wide net |
|---|---:|
| Tool schema fully cached, or its cost ignored | **−475,836 tokens** (saving) |
| Schema charged once per transcript that calls the ledger (198) | **−37,464 tokens** (saving) |
| Schema charged once per transcript, called or not (357) | **+314,562 tokens** (cost) |

These are scenarios. Which one is closest to reality depends on prompt/tool
caching behaviour and on whether the ledger MCP server is attached to contexts
that never call it — neither of which this probe measures, and neither of which
should be guessed at here.

The distribution matters more than the aggregate: the **median** ledger-calling
transcript saves **1,461** response tokens, which is *less* than the 2,214-token
schema delta; only **76 of 198** calling transcripts save more than it. The
saving is concentrated in read-heavy orchestrator sessions (max observed:
15,211 tokens in one transcript). Break-even is roughly **4–5 narrative
`fetch_item` calls** (median saving 459 tokens/call) per context.

Explicitly **not** claimed: that any of this reduces a bill by any percentage;
that RS3's 419,366-token simulated bound was realized (it was a bound, over a
different candidate set, and this report neither confirms nor uses it as an
outcome); that a token saved on the wire is a token saved in a model's context
after host framing.

## 9. Verdict against the acceptance criterion

| Requirement | Status |
|---|---|
| Reproducible report recording corpus / commit / tokenizer | §1; corpus pinned by sha256 manifest, scripts committed under `docs/drafts/20260725-2130-t679-rs3-remeasure/`, exact commands in its README |
| Per-tool before/after bytes and tokens | §3 (corpus), §6 (twin-server) |
| Aggregate savings | §3 total row, §8 |
| Schema / input overhead | §4 — measured on both trees, decomposed |
| Representative full-content controls | §5 — zero delta, deep-equality verified |
| Compact/ack correctness checks | §7 — 0 problems over 688 calls, 31 shipped tests green |
| High-volume methods retain material savings in RS3's direction | §3 — yes, 68–88% per method |
| Regressions explained / fixed / surfaced | §6 and §8 — no response grew; the two non-savings and the one unrelated growth are named and explained |
| No claim treats the RS3 simulated bound as realized billing reduction | §0, §3, §8 |

### One item for plan review (not fixed here)

The response contract's **request side costs 2,214 cold tokens per context**,
which exceeds the median per-transcript response saving (1,461) on this corpus.
The contract is still clearly correct for read-heavy orchestrator sessions and
for narrative payloads, and no response shape regressed — so this is not a
proposal to revert anything. It is a request for a decision on two cheap
follow-ups that would make the win robust rather than distribution-dependent:

1. **Shorten the repeated schema prose.** `ITEM_PROJECTION_DESCRIPTION` (a
   ~40-word sentence) is emitted twice per projection tool — once as the
   parameter description and again inside the `Authoritative response:` block —
   across six tools. The `Authoritative response:` blocks alone measure 1,162
   tokens. Compressing these could plausibly return a four-figure token count to
   every context at zero behavioural cost; the exact figure needs measuring, not
   estimating.
2. **Decide whether `fetch_milestone` should carry the mandatory projection at
   all.** RS3 recommended leaving it alone; the measured outcome on this corpus
   is a wash (−640 response tokens, +104 input tokens, 11 of 19 calls
   break-even).

Both are measurable with the committed harness by re-running §4 and §3 after the
change.
