# plan-advance session log

- **Goal:** G1 — follow-up #2: defects/tasks separation + investigate:* flow
- **Role:** planner (plan-advance)
- **Agent:** a6738e7a850842658
- **Returned status:** awaiting-answers

---

### Session summary
- **Did:** Ran the single plan-advance step for goal G1 (phase `clarifying`, no open linked questions; #2 follow-up scope just appended) — rule 2(a), filed the next clarifying-question batch scoped to the #2 follow-up.
- **Achieved:** Created questions Q19–Q28 under milestone M1, each `open`, `ledgerRefs: ["goals:G1"]`, with context/suggestions/recommendation; G1 remains `clarifying` (open questions gate the `planning` transition). No phase change, no plan emitted.
- **Discovered:** The defects/hypothesis schemas ALREADY exist in `docs/ledgers.yaml` and carry the needed fields (defects: rootCause/suggestedFix/fix/severity(required)/dependsOn/ledgerRefs; hypothesis: parentHypothesis/evidence/rationale/status open|uncertain|confirmed|wrong) — a near-exact match for the research-loop's H-tree, so the core can plausibly be prompt-only (Q28). The follow-up's "just ledger names differ" understates a real status/required-field divergence between tasks and defects (Q19). research-loop is a markdown-ledger skill whose orchestrator must validate every citation; mapping it onto subagents-cannot-spawn-subagents forces the DFS loop into the `/investigate:advance` command body, mirroring how plan-flow keeps its loop in the command.
- **Issues:** None blocking. The #1 follow-up (M6, T26–T34) is already planned/approved and untouched this round; these questions are strictly the #2 follow-up. User must answer Q19–Q28 in the TUI/web, then run `/plan:advance G1`.

---

## Questions filed (Q19–Q28)
- **Q19** — tasks vs defects parametrization (schemas differ in status sets and required fields; "just ledger names differ" is not literally true).
- **Q20** — defect→task linkage representation (dependsOn vs ledgerRefs vs both) and defect-closure owner/trigger.
- **Q21** — user-reported defect intake command (new `/investigate:start` vs folding into `/plan:*`).
- **Q22** — reviewer-found defects: a third `defects[]` bucket, scoped to out-of-scope/pre-existing faults vs in-scope criticism.
- **Q23** — investigate:* command/subagent set and file names for `scripts/link-prompts.ts` LINKS.
- **Q24** — research-loop hypothesis tree mapped onto the `hypothesis` ledger and the subagents-cannot-spawn-subagents constraint (loop lives in `/investigate:advance`).
- **Q25** — investigation→planning handoff (reuse plan-flow's reviewed cycle vs inline mini-planning).
- **Q26** — integration into plan/implement: decoupled file-and-defer vs auto-invoke.
- **Q27** — investigate-explorer read-only contract (no worktree) and evidence validation/storage convention.
- **Q28** — scope boundary: prompt-only (existing schemas suffice) vs UI/schema expansion.
