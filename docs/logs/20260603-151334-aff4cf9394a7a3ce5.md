# investigate-explorer session log

- defect: D24 (ex-D22 — vacuous 's'-key-inert archived-item test)
- hypothesis: H14 (test asserts only overlay-insensitive frame content)
- role: explorer
- returned lean: supports → adjudicated CONFIRMED (orchestrator-validated)

### Session summary
- **Did:** gathered read-only evidence for H14 (the 's'-key-inert archived-item test asserts only overlay-INSENSITIVE frame content, so it cannot catch a regression where 's' opens the status overlay on an archived row).
- **Achieved:** 8 evidence items; lean supports.
- **Discovered:** the 's'-inert test (app.test.tsx:959-986) asserts only frame.toContain('[archived]') + frame.toContain('archived task'). The path-header '[archived]' (app.tsx:934-939/1040) and the list pane ({listEl}, app.tsx:1069) are top-level boxes; the status overlay (<Overlays> case 'status' SelectList, app.tsx:1820-1836) replaces ONLY the content-pane box (overlay!==null ? <Overlays/> : contentEl, app.tsx:1072-1122). So both asserted strings persist even with the picker open → the test passes regardless. The sibling 'e'-inert test (:988-1010) asserts 'read-only' = the content-pane badge '[archived · read-only]' (app.tsx:1424) the overlay WOULD replace → regression-sensitive. No contentPane(f) helper exists; the file has listSide(frame) (L1264); the SelectList cursor marker is '› ' (app.tsx:1291-1296). Guarded behavior = `!cursorInArchive` on the 's' handler (app.tsx:803 content-focus / 838 list-focus).
- **Issues:** the test asserts the bracketed '[archived]' (header) form, distinct from the content badge '[archived · read-only]' — confirms neither asserted string can detect an open overlay. Fix: assert content-pane-scoped that the '› ' picker marker is ABSENT and the read-only badge PRESENT.

## ORCHESTRATOR ADJUDICATION
All cited lines re-opened and validated [correct] (app.test.tsx:982-984/1008; app.tsx:1061-1073/803/838). H14 CONFIRMED → D24 root-caused → defect-seeded goal G12 (planning) under M39.
