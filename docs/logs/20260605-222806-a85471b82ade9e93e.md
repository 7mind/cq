# Plan-flow session log

- **Goal:** G18 (milestone M59) — Merge cq-config into ledger MCP + parallel planners
- **Role:** reviewer (plan-reviewer)
- **Verdict:** revise (review R193) — 3 criticisms

---

### Session summary
- **Did:** reviewed the emitted plan for plan-flow goal G18 (PART 1 cq-config merge + standalone removal across M61/T1–T11; PART 2 parallel planners across M62/T12–T17) against current main HEAD source.
- **Achieved:** verdict `revise`, review id R193, 3 criticisms / 0 new questions / 0 out-of-scope defects.
- **Discovered:** The load-bearing defect is a DUAL tool-registration surface the plan overlooked. The ledger MCP registers its tool surface TWICE off the same `LEDGER_TOOL_NAMES`: `createLedgerMcpTools` (`packages/ledger/src/mcp/ledgerTools.ts`, the claude-agent-sdk `tool()` factory, in-process host only) and `registerLedgerStdioTools` (`packages/ledger/src/mcp/stdioLedgerTools.ts`, raw `@modelcontextprotocol/sdk`). The standalone `@cq/ledger-mcp` binary — which `.mcp.json`'s `ledger` server runs, and which plan/advance.md, implement/advance.md, reviewers.md all call as `mcp__ledger__*` — and the embedded TUI/web servers all go through `buildServer()` → `registerLedgerStdioTools` (`ledger-mcp/src/main.ts:247`), NOT `createLedgerMcpTools`. T1/T13 add the new tools ONLY to `ledgerTools.ts`, so they would never surface on the binary every consumer actually talks to. T3/T13's "18→20→21" count bump is likewise scoped to one file while `stdioLedgerTools.ts` (header L4-5, doc-comment L149) shares the count and a cross-surface drift-guard test (L18). Verified-correct anchors: toml.ts L139-143 reject-non-reviewers, the readLog injection pattern, the cqConfigMcp derivation L247-304, the packages/cq-config-mcp workspace member, the plan-advance agent disallowedTools (candidate-mode feasible), Q97–Q102 faithfully encoded, same-file serialization chains + acyclic DAG.
- **Issues:** codegraph not indexed; Grep/Glob unavailable — verified via direct Read (sufficient). The stdioLedgerTools.ts comment naming a `@cq/cq-mcp` package is stale; the gap affects the `@cq/ledger-mcp` binary + embedded hosts.
