# Plan-flow candidate planner — G38 (grok, pi grok-build/grok-build CANDIDATE mode)

- Goal: G38; Role: candidate planner, alias `grok` (pi --provider grok-build --model grok-build)
- Outcome: candidate plan emitted (4 work milestones 1a/1b/2/3; 13 tasks). NOTE: used `defects:D38` placeholder for the item-3 defect — D38 is an already-RESOLVED real defect (verdict-enum); the orchestrator substitutes the real new defect id during synthesis.

### Verbatim captured stdout
```json
{
  "milestones": [
    {"title": "G38-1a: Implement-worker worktree auto-cleanup (prompt-only)", "dependsOn": []},
    {"title": "G38-1b: Ledger ~/.cache mirror backup + restore subcommand", "dependsOn": []},
    {"title": "G38-2: Flows help tab per-role ACTIONS diagrams (web-only)", "dependsOn": []},
    {"title": "G38-3: TUI focus paging keys + Home/End (defect-aware)", "dependsOn": []}
  ],
  "tasks": [
    "1a-1 start-of-pass orphan sweep (marker G38-1a-start-sweep) + canonical-ledgers.test.ts cell",
    "1a-2 post-done worktree remove+branch delete §7.3 (marker G38-1a-post-done-cleanup) + test cell (dep 1a-1)",
    "1b-1 onMutation ~/.cache mirror in FsLedgerStore (XDG path, sha256 slice12, atomic, non-blocking)",
    "1b-2 ledger-mcp restore --from-cache [--cwd] subcommand (dep 1b-1)",
    "1b-3 tests mirror-on-write/restore/path-scheme (dep 1b-1,1b-2)",
    "2-1 hand-authored role->actions catalogue TS module",
    "2-2 wire into HelpOverlay/DiagramSvg replacing state diagrams, preserve testids (dep 2-1)",
    "3-1 revise app.tsx useInput focus paging + Home/End raw ESC (ledgerRef defects:D38 [WRONG id])",
    "3-2 ink tests list/content focus paging + Home/End (dep 3-1)"
  ],
  "rationale": "one milestone per item; prompt edits carry G38- markers + canonical-ledgers test cells; 1b onMutation post-lock atomic mirror + restore CLI; 2 web-only catalogue+render; 3 defect-aware (placeholder defects id), raw ESC matching for Home/End."
}
```
