# plan-advance — G13 (defect-seeded: fix D25/D26/D27), token: review-requested
- Emitted plan (clarify-skipped, K8 pt4). Work milestone M48 recorded on G13.fields.milestones.
- T158 (D26/standard) — harden FsLedgerStore.readLog vs symlink escape; reproduce-first acceptance (failing symlink test before fs.realpath re-assert, passing after; ENOENT not masked). Scope: FsLedgerStore.ts + new regression test.
- T159 (D25/fast) — remove stale `// eslint-disable-next-line no-console` at navRenderBytes.test.tsx:291.
- T160 (D27/standard) — reword CHAINED handoff trigger in all 3 llm/commands/*/advance.md to fire on ANY wrapping flow command.
- Each task ledgerRefs ["defects:D<n>", "goals:G13"]; each defect's dependsOn now lists its fix task (Q20 bidirectional). bun run check in every acceptance. No inter-task deps (DAG-parallel). Goal stays `planning`, awaiting reviewer.
