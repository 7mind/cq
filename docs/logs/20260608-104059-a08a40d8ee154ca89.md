# implement-reviewer (opus) — T238 — approve (0/0/0)
Replica tokenToChildModel + parseCqToken are BYTE-IDENTICAL to the real cq-subagent-dispatch.ts (T233) functions — guard is faithful. Fixture table covers qualified-agree / bare-refuse-both / empty-half-refuse-both / claude. parseReviewerToken asserted to THROW on rejects + agree on provider+model on accepts. Claude null correctly scoped as parent-fallback outside the PI-only ACCEPT-iff invariant. Single-file diff, no scope creep. check green 1089/0.
