# investigate-explorer session log

- defect: D23 (multi-step-form test flakes via advance() helper 1500ms budget)
- hypothesis: H13 (advance() helper's fixed 1500ms Enter-replay settle budget starves under CPU contention)
- role: explorer
- returned lean: supports

### Session summary
- **Did:** gathered evidence for H13.
- **Achieved:** 5 evidence items; lean supports.
- **Discovered:** advance() (app.test.tsx:201-209) uses a fixed ms=1500 wall-clock deadline as its ONLY exit-or-fail budget — polls h.frame(), re-presses Enter while `still` shows, throws once Date.now()>end without `done`. Fixed-time settle budget, not a logic error. The multi-step-form test (app.test.tsx:450-467) drives the create-item overlay through FOUR sequential advance() calls with NO explicit per-test timeout (bun 5000ms default). The file already has the poll-until-condition helpers waitFor (L173-180) / waitForFrame (L183-190) that advance() is structurally a sibling of; the scrolling test got an explicit 20_000 timeout for the same CPU-contention reason (L554-578).
- **Issues:** could not verify byte-identical base-vs-T130 without Bash. [ORCHESTRATOR FOLLOW-UP: verified — advance() helper (L195-215) is byte-identical bfa70ed^→HEAD; T130 diff touches neither advance() nor the multi-step test (grep count 0). H13 fully confirmed.]
