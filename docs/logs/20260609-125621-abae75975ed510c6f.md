# implement-worker — T312 (opus)

- Task T312 (@cq/ledger ~/.cache mirror + shared cacheMirrorDir); agent abae75975ed510c6f; status **pass**; resultCommit 62340a1
- check green: tsc 0, eslint 0, 1300 tests pass/0 fail.

### Session summary
Added node-only ~/.cache mirror driven by onMutation. Shared pure `cacheMirrorDir(absRootDir) -> <cacheBase>/cq/ledgers/<basename>-<sha256[:12]>` (XDG_CACHE_HOME ?? ~/.cache), exported for T313. mirrorMutation copies docs/<ledger>.md per op + archive file(s)+ledgers.yaml on archive, via the store's atomic-write primitive (extracted to fsAtomic.ts, reused — no dup). Wired into fireMutation AFTER onMutation as guarded fire-and-forget (failures swallowed to stderr, never block/unwind), tracked in pendingMirrors + drained by dispose(). TDD tests: byte-equality, <basename>-<12hex> dir name, archive+registry mirroring, swallowed error, atomic writes. **Worktree-confined:** reset own worktree from stale base 242184f → 0109255 before starting (D43-correct).
