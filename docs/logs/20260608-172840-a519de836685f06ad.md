# Plan follow-up log — G34 (planner)

- Goal: G34 (milestone M108), follow-up clarifying round (privilege class RO/RW + exposed tools on agent cards)
- Role: planner (plan-advance)
- Returned status: awaiting-answers

### Session summary
- **Did:** Performed the single state-driven step for re-opened goal G34 (rule 2a): grounded read-only in the cq-assets frontmatter and the locked tasks T275/T276/T278/T281, then filed a follow-up-scoped clarifying batch.
- **Achieved:** Created Q151, Q152, Q153 (status `open`, milestone M108, `ledgerRefs: ["goals:G34"]`, author opus-4.8[1m]). Goal remains `clarifying`. No mutation of original Q145–Q150, the locked plan (T267–T281 / M109–M112), or decision K54.
- **Discovered:** The two asset kinds encode tools OPPOSITELY — subagents (`nix/pkg/cq-assets/agents/*.md`) carry a DENY-list `disallowedTools` (+ optional `isolation: worktree`) with no positive allow-list; orchestrator commands (`nix/pkg/cq-assets/commands/cq/*.md`) carry a positive `allowed-tools` allow-list, and the shared rubric prompts (implement-review.md, plan-review.md) carry no/partial tools frontmatter. RO/RW maps cleanly onto the subagent deny-list (RW = implement-worker, implement-conflict-resolver, investigate-prober; RO = plan-advance, plan-reviewer, implement-reviewer, investigate-explorer). The locked T275 parser extracts `disallowedTools`/`isolation` for agents but does NOT currently parse command `allowed-tools` — Q152 flags that as a likely small parser extension if option (a) is chosen.
- **Issues:** Codegraph is not indexed for this repo (errored "No CodeGraph project loaded"); fell back to Read + a one-shot Monitor invocation to list/grep the assets. No blockers.

### Questions filed
- Q151 — privilege class RO/RW: derive from `disallowedTools` (RW iff no mutating tool disallowed) vs author explicitly; RO/RW split per role enumerated.
- Q152 — "exposed tools": subagents carry `disallowedTools` deny-list while commands carry `allowed-tools` allow-list (and some carry neither) → needs a definition (raw-per-kind vs normalized resolved allow-list vs curated); flags extending the T275 parser to read command `allowed-tools`.
- Q153 — orchestrator commands: derive privilege/tools from their own `allowed-tools` vs a flat "orchestrator"/N-A label vs union of dispatched subagents.

awaiting-answers
