# Plan follow-up #2 log — G34 (planner)

- Goal: G34 (milestone M108), follow-up #2 clarifying round (Agents tab: show LIVE-configured models, resolved at runtime via MCP)
- Role: planner (plan-advance) ; Returned: awaiting-answers

### Session summary
- **Did:** DEFAULT-mode rule 2(a) — G34 re-opened for follow-up #2 (live-configured models on the Agents tab); filed a clarifying batch scoped ONLY to follow-up #2. No phase transition; no plan writes.
- **Achieved:** Created six `open` questions Q154–Q159 under M108, linked goals:G34: Q154 (capability shape — dedicated get_agent_models vs extend get_config), Q155 (drop build-time model fields vs keep static fallback), Q156 (candidate token set for per-harness mappings), Q157 (unconfigured/embedded-mode + 'default/not configured' label states), Q158 (agent-id roster ownership + join key), Q159 (acceptance/test bar).
- **Discovered:** get_config's current `tiers` wire field is a T268 MINIMAL BRIDGE (packages/ledger-mcp/src/configCapability.ts projectConfig) — it collapses the inverted classifier into the OLD per-slot {fast,standard,frontier} shape + exposes only alias-name planner/reviewer lists, so the client CANNOT reproduce the build-time derivation from get_config as-is → a server capability rework or new tool is required. Build-time model derivation (deriveModelClass/deriveModelMappings over cq.toml.example) lives in packages/ledger-web/scripts/gen-agents-catalogue.ts. The web LedgerClient interface (packages/ledger-web/src/types.ts) has NO config method today, and test/agentsTab.test.tsx is explicitly static-only — both (incl. FakeClient) must change.
- **Issues:** none blocking; codegraph not initialized (used Read).

awaiting-answers
