# Implement log — T276 (agents catalogue codegen)

- Task: T276 (M111 / G34 W3). Worker: opus (a5dc702331a95e784, commit ff45f37). Reviewers: opus[claude] (ab31107ab3ccc7563) + minimax[pi:ollama-cloud]; grok+codex[pi:grok-build] excluded (stall). Merged to main dca35f8. bun run check green 1201/0.

### Worker
Wrote packages/ledger-web/scripts/gen-agents-catalogue.ts (wired as `gen-agents` in package.json) — parses all 19 Q148 role assets via T275 parseAgentMarkdown, derives model class from cq.toml.example [agent_tiers]+classifyToken (N/A for the 12 commands), privilege via deriveSubagentPrivilege/deriveCommandPrivilege, exposedTools via formatExposedTools; emits committed agentsCatalogue.gen.ts (AGENT_ROLES, 19 entries; 7 real class — investigate-explorer+plan-reviewer=frontier, other 5 agents=standard; per-harness mappings frontier→{claude:["opus-4.8[1m]"]}, standard→{pi:["grok-build/grok-build"]}). Hard-fails on a missing ## Catalogue block. Reads cq.toml.example (committed) via readFileSync+parseConfig (loadConfig is hardcoded to cq.toml) → deterministic; REPO_ROOT 6 levels up from scripts/. Narrowed the stale T275 placeholder test (AGENT_ROLES===[] → non-empty). Spot-checks: implement-worker=>RW/standard, plan-reviewer=>RO/frontier.

### Reviews — both APPROVE
- opus: empirically re-ran gen-agents (byte-deterministic, zero diff), confirmed hard-fail-on-missing-Catalogue (exit 1, precise message), all 19 roles complete (non-empty description/inputs/outputs/ioSchema/promptTemplate/model/privilege/exposedTools), reads committed cq.toml.example, scope clean (4 files), check green 1201/0.
- minimax: verified script logic (6-level REPO_ROOT, parseConfig on committed example, deriveModelClass via resolveAgentTier, deriveModelMappings union+classify+group+sort, T275 privilege/exposedTools helpers), gen sample well-formed, placeholder-test narrowing acceptable (T277 owns the real freshness test), scope respected.
- Orchestrator independently confirmed byte-determinism (re-ran gen-agents → zero diff).
