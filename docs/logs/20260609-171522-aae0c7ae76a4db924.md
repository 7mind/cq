# implement-reviewer — T329 (opus); verdict approve (0/0/0)
agent aae0c7ae76a4db924. Verified at 2631bf1 in an isolated worktree: (1) cross-nav sets pendingAgentAnchor=`help-agent-${agentId}` + reuses T328's scrollToHelpSection (exact anchor matches the rendered section id/testid help-agent-<role.id>); (2) tab-flip-then-scroll sequencing correct (setTab('agents') + a [tab,pendingAgentAnchor] effect that only fires once tab==='agents', rAF-deferred + setTimeout(0) fallback for happy-dom); (3) abstract nodes inert (DiagramSvg gates on agentId!==undefined && onActivateAgent!==undefined; user node has no agentId); (4) FlowLegend maps Object.keys(ROLE_KIND_FILL) → swatches with backgroundColor=ROLE_KIND_FILL[kind]; (5) tests non-vacuous (real click+Enter on the plan-advance node, exact-id scrollIntoView spy, abstract-inert + zero-scroll, per-kind color + swatch count). Required-prop change sound (single always-supplying call site; DiagramSvg's prop stays optional). typecheck+lint clean, 1368/0, nix build .#ledger-web exit 0.
