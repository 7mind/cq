# Session log — T343 implement-worker (opus)

- **Task:** T343 (M139); status pass; resultCommit a6034be; bun run check green (1470/0)

### Session summary
Added fetch_prompt / validate_input / validate_output MCP tools backed by a new injected PromptCatalogCapability. fetch_prompt joins the @cq/config typed-catalog sidecar schemas (T341) onto the role's prompt body, re-read per call from cq-assets (agents/<id>.md for dispatched roles, commands/cq/<id>.md for orchestrator-commands, resolved under <root>/nix/pkg/cq-assets/); orchestrator-command roles return prompt-only (schemas absent). validate_input/output run Ajv2020 via a new @cq/config validateAgainstSchema helper, returning structured errors with the failing field path; both fail-fast on unknown roleId (UnknownRoleError) and on orchestrator-command roles (NoSchemaForRoleError). Tools registered in BOTH the stdio and Claude-SDK ledger-tool factories (22→25). DISCOVERED: the catalog is split — schemas in @cq/config DISPATCHED_ROLE_SIDECARS, prompt body only in cq-assets markdown (ledger-mcp can't depend on ledger-web), so the capability joins them directly by re-reading the asset. Ajv2020 already a @cq/config dep → no new dep/FOD. A non-cq ledger root gets a precise 'cannot read asset' fail-fast. filesTouched: cq-config/src/{validation.ts,index.ts}, ledger/src/mcp/{promptCatalogCapability.ts,ledgerTools.ts,stdioLedgerTools.ts}, ledger/src/index.ts, ledger/test/mcp-tools.test.ts, ledger-mcp/src/{promptCatalogCapability.ts,main.ts}, ledger-mcp/test/{promptCatalogCapability,http,main}.test.ts. Based on main edb5dd8 (has T336+T341).
