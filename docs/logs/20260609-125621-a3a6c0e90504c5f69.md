# implement-worker — T318 (opus)

- Task T318 (TUI LIST-focus paging + Home/End, remove no-Enter detail-scroll); agent a3a6c0e90504c5f69; status **pass**; resultCommit 04c5adb
- check green 1301/0.

### Session summary
LIST-focus PgUp/PgDn now page the cursor by listInnerH rows (scroll:0); Home/End jump first/last row; removed the no-Enter detail-scroll affordance + updated comment + hint bar. Added module-scope `matchHomeEnd` helper (CSI + numeric VT forms) shared with T319. 7 ink-testing-library LIST-focus tests feeding raw ESC via stdin, green. **Discovered (grounding correction):** the task premise "ink exposes no key.home/key.end" is OUTDATED for ink 7.0.5 — it DOES expose key.home/key.end/key.pageUp/key.pageDown and sets input="" (raw ESC never reaches input). Handler honors `key.home/key.end || matchHomeEnd(input)` so it works under ink 7.0.5 AND keeps matchHomeEnd as a correct raw-bytes fallback. Also made 3 pre-existing wrap-sensitive T87 hint assertions whitespace-robust (mirroring the L925 pattern) since the hint-string update shifted the 80-col wrap point.
