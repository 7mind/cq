# Session log — T348 implement-worker (opus)

- **Task:** T348 (M145, G43); status pass; resultCommit 7647744; bun run check green (1496/0)

### Session summary
Added `packages/ledger/src/store/git/GitPlumbing.ts`: an injectable wrapper (GitRunner exec seam; default `nodeGitRunner` via child_process.execFile) over the K66-PoC git invocations — hashObject (git hash-object -w --stdin), writeTree (isolated GIT_INDEX_FILE scratch index + update-index --cacheinfo + write-tree, NEVER the real index), commitTree (orphan when parent null, else -p), CAS updateRef (3-arg `git update-ref <ref> <new> <old>`, throws typed StaleRefError), readRef (null on missing), catFile (git cat-file -p <ref>:<path>), lsTree (git ls-tree -r --name-only), tagRef. Typed errors StaleRefError + GitCommandError. Test (packages/ledger/test/gitPlumbing.test.ts) mirrors the PoC proof against a throwaway /tmp repo: two writes advance an orphan ref two commits while HEAD/`git status --porcelain`/index stay byte-identical; stale-CAS throws StaleRefError; catFile/lsTree read back with no checkout. Exports added to @cq/ledger index.ts. DISCOVERED: exactOptionalPropertyTypes:true forbids forwarding `undefined` to an optional ctor field — withCwd branches to omit scratchDir. NOTED for T352: updateRef maps any non-zero 3-arg exit to StaleRefError (over-broad; can't arise within the seam's own contract since newSha always comes from a prior commitTree/hashObject). Based on main 604251e.
