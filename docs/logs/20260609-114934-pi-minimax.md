# Plan-flow candidate planner — G38 (minimax, pi ollama-cloud/minimax-m3 CANDIDATE mode)

- Goal: G38; Role: candidate planner, alias `minimax` (pi --provider ollama-cloud --model minimax-m3)
- Outcome: candidate plan emitted (4 work milestones 1a/1b/2/3; 14 tasks). Most thorough on 1a (adds §5-intact reinforcement + implement-worker.md alignment + gen-agents regen) and explicitly makes the defect-record a task; uses `defects:<TBD-G38-TUI-PAGING>` placeholder.

### Verbatim captured stdout (summary of the emitted JSON)
```json
{
  "milestones": [
    "G38.1a — Prompt-level worktree auto-cleanup",
    "G38.1b — Out-of-repo ledger backup under ~/.cache",
    "G38.2 — Web Flows help tab: per-role action diagrams",
    "G38.3 — TUI focus-respecting paging keys"
  ],
  "tasks": [
    "1a immediate worktree removal §7.3 (marker G38-WT-CLEANUP-AFTER-MERGE) + test cell",
    "1a verify §5 blocked-task intact (marker G38-BLOCKED-WT-INTACT) + test cell",
    "1a start-of-pass orphan sweep (marker G38-WT-ORPHAN-SWEEP) + test cell",
    "1a align implement-worker.md (marker G38-WORKER-WT-EPHEMERAL) + regen agentsCatalogue.gen.ts (dep §7.3)",
    "1b cache mirror subscriber FsLedgerStore (XDG path scheme + atomic + post-lock isolation)",
    "1b ledger-mcp restore --from-cache [--cwd] subcommand (dep mirror)",
    "1b consolidated tests: mirror-on-write/restore/path-hash/atomicity/post-lock isolation",
    "2 hand-authored role->actions catalogue TS module (flowActions.ts)",
    "2 replace state diagrams in HelpOverlay via elk DiagramSvg, preserve testids (dep catalogue)",
    "2 web happy-dom tests help-flow-<id> (dep render)",
    "3 file defects record (severity medium) — orchestrator creates",
    "3 refactor useInput focus paging + Home/End raw ESC (dep defect)",
    "3 ink-testing-library tests list/content focus (dep refactor)"
  ],
  "rationale": "four independent milestones; 1a markers + canonical-ledgers cells (eval-time-only guard); 1b coupled mirror subscriber then restore then consolidated tests; 2 catalogue->wire->tests; 3 defect-record first then useInput refactor (raw ESC \\x1b[H/\\x1b[F/\\x1b[1~/\\x1b[4~) then ink tests; defect severity medium."
}
```
