# Session log — T346 implement-worker (sonnet)

- **Task:** T346 (M142, fix D47); status pass; resultCommit 841151b; bun run check green (1488/0)

### Session summary
Test-only fix to packages/ledger/test/canonical-ledgers.test.ts. Added BootstrapViolationError to imports. (1) Fixed the existing committed-vs-canon boot test to use onSchemaDivergence:'abort' (was the default 'backup-reinit' which silently self-heals stale fixtures); fixed the comment. (2) Added a byte-equality test verifying the committed docs/ledgers.yaml matches serializeRegistry({version:1,ledgers:CANONICAL_LEDGERS.map(c=>({name,schema}))}) exactly (passes on the in-sync real fixture). (3) Added a reproduce-first test proving abort-mode THROWS on a deliberately-staled decisions schema (rationale.required flipped) while the old default mode does NOT — demonstrating the pre-fix gap. DISCOVERED: the staled-fixture corruption must be a PARSEABLE divergence (flipped a `required` flag) rather than removing a statusValues entry, because removing 'open' would also invalidate transitions['open'] → SchemaValidationError before reaching the schemasEqual divergence check. examples/sample-ledger excluded from byte-equality. No source code changes. filesTouched: ledger/test/canonical-ledgers.test.ts. Based on main 4af242e.
