# plan-advance candidate (opus) — G32 (D39 fix)

- goal: G32 (defect-seeded, resolves D39); single grounded candidate planner
- alias: opus (claude); outcome: 4 milestones, 9 tasks

### Session summary
Grounded in the @cq/ledger write path (core.ts applyCreateItem/applyUpdateItem/validateFields, the StatusChangePrecondition hook, both stores), HANDOFFS_SCHEMA, advance.md §Stop-condition, and the G30 precedents (T245/T255).
KEY DESIGN: load-bearing enforcement = a handoffs-specific PURE assertion `assertHandoffInvariants(itemId,status,fields)` modelled on assertQuestionAnswerPrecondition (D29) + assertGoalPhasePreconditions (F2) — NOT a generic LedgerSchema `requiredWhen` DSL (core.ts:790 explicitly records that as out-of-scope). Invoked in applyCreateItem (the load-bearing path — handoffs are terminal-on-create; BOTH stores route through applyCreateItem, so the create check needs ZERO per-store wiring) + applyUpdateItem. Pure non-empty checks (mixed/answers-required ⇒ blockingQuestions non-empty; user-action-required ⇒ handoffReasons non-empty) need no cross-ledger view. The cross-ledger open-question-resolution + drained predicate-gate are STRETCH (gated, mirror F2 plumbing if cheap).
9 tasks: W1 (helper / wire create+update / dual-adapter tests reproducing HO26 as asserted-throw / stretch-scope-decision); W2 (advance.md turn-vs-run clause); W3 (euphemism blocklist+self-check in advance.md / thread into 3 per-flow prompts); W4 (four-table grep-invariant + verify bun run check + nix build).
