# implement-worker — T158 round 1 (D26 fix, criticism fix), status: pass
- resultCommit 448b156; branch implement/T158r1; base 7575f5c (round-0); check green 844/0.
- Fixed the round-0 regression: readLog now realpath-resolves BOTH the target AND this.logsDir before comparing, so a symlinked parent of the store root no longer causes a false escape rejection; an in-docs/logs symlink whose target escapes is still rejected. ENOENT still surfaces normally. Added regression test: FsLedgerStore rooted via a symlinked dir → legitimate read succeeds (failed under round-0). filesTouched: FsLedgerStore.ts, test/read-log.test.ts.
