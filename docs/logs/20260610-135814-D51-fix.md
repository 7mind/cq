# Session log — D51 fix (worker opus a837bfed0af08bd98 + reviewer opus a4392bff8243bbe1b)

- **Defect:** D51 (low, M146, G43) — RESOLVED; resultCommit 435d302 → main

### Session summary
FIX: the embedded ledger-tui now uses the backend-selecting `startLedgerCoherenceWatcher` instead of the hardcoded FS `startLedgerWatcher`, so external changes refresh the embedded TUI under the git-object backend. EmbeddedContext (ledger-tui mcpClient.ts) now carries the resolved ResolvedLedgerStore descriptor (backend+branch+store), threaded through McpLedgerClient.embedded(); main.tsx wires startLedgerCoherenceWatcher(ctx.resolved, ctx.cwd, onChange) — ref-sha-watch under git-object, file-watch under fs (fs behaviour byte-identical, resolved.store === store), mirroring ledger-web/serve.ts. createEmbeddedStore already returned ResolvedLedgerStore (T357). New embeddedCoherence.test.ts proves backend exposure + onChange fires on an external orphan-ref advance under git-object (the previously-missed scenario) and on a docs write under fs. REVIEW (opus, approve): reran the new test against the OLD wiring — the git-object orphan-ref-advance case FAILS (onChange never fires), PASSES under the new wiring (fail-before/pass-after confirmed); fs path byte-equivalent; web serve.ts unaffected; remote --mcp-url mode untouched; check green 1596/0.
