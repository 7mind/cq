# Session log — T353 implement-reviewer round 1 (opus) — DISAPPROVE

- **Task:** T353 (M145, G43); verdict **disapprove**; 1 criticism / 0 questions / 0 defects

### Session summary
Reviewed T353 round 1 (diff b934cd2..0910d96). Production watcher logic CORRECT: R418 git-dir indirection verified (`git rev-parse --verify`, no `.git/refs` literal); main.ts is re-export-only (scope respected, no T357 wiring); first-poll undefined-sentinel prevents spurious fire. BUT `bun run check` RED: the new refWatcher.test.ts failed 2/4 deterministically under full-suite timing (cross-instance-coherence + git-dir-indirection tests). Diagnosed as a synchronization race between the test's coherence-gate and the trailing onChange push (the watcher loop does `await invalidate(id); onChange(id)` over sorted ledger ids; `widgets` sorts last; the fetch-coherence gate flips true the instant invalidate('widgets') resolves, one tick before the onChange('widgets') push). Acceptance ('bun run check green' + 'onChange invoked with the ledger id') unmet → disapprove. Reproduced deterministically in isolation + full suite (1539 pass / 2 fail).
