# Session log — D49 fix (worker sonnet ab7bb7825e12ca152 + reviewer opus aca88e59fd1ee3833)

- **Defect:** D49 (low, M145, G43) — RESOLVED; resultCommit 3143a2a → main (incl. D54 follow-on)

### Session summary
FIX: GitPlumbing.updateRef no longer maps EVERY non-zero 3-arg `git update-ref` exit to StaleRefError. Now `stderr.includes("cannot lock ref")` → StaleRefError (genuine CAS mismatch / stale-old / ref-already-exists); any OTHER non-zero exit → GitCommandError (e.g. malformed/nonexistent newSha, which exits 128 with 'nonexistent object'/'not a valid SHA1', NO lock phrase). Two new gitPlumbing tests: CAS-mismatch→StaleRefError; bad-newSha→GitCommandError. REVIEW (opus, approve): discriminator verified empirically against git 2.54.0 — all real CAS variants emit 'cannot lock ref' (StaleRefError preserved, backend lost-update guarantee intact), nonexistent-newSha does not (GitCommandError); the GitCommandError test genuinely fails under the OLD blanket mapping (fix-pinning teeth confirmed); check green 1594/0. Reviewer surfaced a latent nit → D54 (discriminator depends on unversioned git stderr wording/locale). ORCHESTRATOR folded in D54 directly: forced LC_ALL=C/LANG=C on nodeGitRunner so git stderr is locale-deterministic (commit on main; check green 1597/0). Combined: D49 discriminator now robust against locale translation.
