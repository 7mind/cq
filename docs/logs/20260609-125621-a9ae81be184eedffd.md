# implement-worker — T308 (sonnet)

- Task T308 (advance.md §7.3 worktree teardown); agent a9ae81be184eedffd; status **pass**; resultCommit 5c4c9de
- check green 1294/0. filesTouched: implement/advance.md + agentsCatalogue.gen.ts.

### Session summary
Replaced 'Claude: auto' worktree teardown in §7.3 with explicit `git worktree remove --force` / `git branch -D implement/<taskId>` / `git worktree prune`, documented root cause, embedded marker G38-1a-post-done-cleanup (×1). §5 'worktree INTACT' (L281) untouched. **Discovered:** a `bun run check` freshness-guard test runs gen-agents and asserts agentsCatalogue.gen.ts is byte-identical — so editing the catalogued advance.md REQUIRED regenerating gen.ts in this same task to stay green (worker ran gen-agents; pure regeneration). Note: this contradicts the T322 "do not regen here" instruction — the freshness guard forces per-task regen.
