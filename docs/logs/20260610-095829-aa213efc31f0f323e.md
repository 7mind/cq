# Session log — T350 implement-worker (opus)

- **Task:** T350 (M144, G43); status pass; resultCommit 82d00cf; bun run check green (1489/0)

### Session summary
Extracted `AbstractLedgerStore<P extends LedgerPersistence>` (new file) holding all persistence-agnostic logic — the in-memory `ledgers` Map, init() load loop + bootstrap + ambient-milestone seeding + legacy-pointer backfill, all read methods (fetch/fetchItem/fetchMilestone/listMilestoneItems/snapshot/search/ftsSearch), all mutation methods, the AsyncMutex + lockfile critical sections (withLock/withMilestonesLock/withRegistryLock), fireMutation/onMutation, the FTS index lifecycle, invalidate(), schema-divergence DETECTION + reinit ORCHESTRATION (backup BYTES delegated to the seam's backupCanonicalState), and dispose(). Every former fs.*/atomicWrite call-site in the moved code now routes through a LedgerPersistence seam method. Added `FsPersistence` (new file) as the FS seam impl; FsLedgerStore now `extends AbstractLedgerStore` wired to FsPersistence and keeps ONLY FS-only concerns (locks root, cache mirror via afterMutation/drainBackend, read_log, reset/ResetSummary, rootDir) — 1576 → 339 lines. lockfile.ts + cacheMirror.ts NOT moved (carve-outs honored). DISCOVERED: a subclass cannot reference `this` before super() to bind the registry-snapshot accessor the seam needs for the divergence backup — solved with a post-super() `FsPersistence.bindRegistrySnapshot()` (eslint forbids this-alias). Seam treats archive locators as the stored relative ArchivePointer.path, resolved + containment-checked inside FsPersistence (reproduces assertWithinDocsRoot exactly). No shared-method signature changed → no CHANGE-INVENTORY decision item needed. Based on main 96e2680.
