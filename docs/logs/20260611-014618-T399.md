# Session log — T399 implement-worker (sonnet) + 4-reviewer panel — UNANIMOUS approve (fixes D59)

- **Task:** T399 (M169, G48; fixes D59); worker a8e0e65b status **pass**; resultCommit c34fe39 (cherry-picked onto main 3f8c7f5); check 1688/0. Review: opus aeba9d4d (independently reproduced 26 tools at the commit) + codex/grok/minimax (pi) all approve.

### Worker
Fixed D59 (cq dispatcher tore down long-running modes). packages/cq-cli/src/main.ts: split the handler outcome into a new `SubcommandOutcome {exitCode}` (the 5 native handlers return this) and gave `DispatchOutcome` a `longRunning: boolean`. `dispatch()`: mode branch → `{exitCode:0, longRunning:true}`; usage/unknown → `{..., longRunning:false}`; native → `{...handlerOutcome, longRunning:false}`. `main()` → `const {exitCode, longRunning} = await dispatch(argv); if (longRunning) return; process.exit(exitCode);` — a mode governs its own process lifetime (the delegate's stdio transport / Ink / web server keeps the loop alive) exactly like the standalone bins, while native subcommands still process.exit to propagate non-zero codes. dispatch.test.ts updated to assert `longRunning` per branch (modes true; usage/unknown/native false) while keeping the T387/T389 verbatim-`argv.slice(1)` + "mode delegate never fires" assertions. check 1688/0; nix build .#cq green; `cq mcp` stdio → 26 tools; `cq mcp --http` → serverInfo.

### Review (UNANIMOUS approve)
opus (checked out c34fe39 in an isolated worktree — main was pre-T399): all 5 adversarial checks pass — longRunning set on EVERY return path; main() still propagates non-zero codes for natives; MODES and SUBCOMMANDS are disjoint const arrays so no native can be mis-flagged longRunning; the SubcommandOutcome/DispatchOutcome split is type-correct (spread places longRunning:false last, SubcommandOutcome has no longRunning key); test asserts longRunning per branch + T387/T389 delegation intact. Independently reproduced: check 1688/0, nix build .#cq exit 0, `cq mcp` stdio → 26 tools + serverInfo ledger-mcp. codex/grok/minimax concur.
```json
{"taskId":"T399","verdict":"approve","criticism":[],"questions":[],"defects":[],"rationale":"longRunning on all dispatch paths (mode=true; usage/unknown/native=false); main() skips process.exit only when longRunning (non-zero propagation intact); MODES/SUBCOMMANDS disjoint so no native mis-flag; test per-branch + delegation intact; opus reproduced check 1688/0 + nix build green + cq mcp stdio 26 tools.","summary":"T399 unanimous approve — D59 fixed."}
```
