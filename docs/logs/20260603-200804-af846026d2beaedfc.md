# implement-worker — T161 (D28 readLog TOCTOU fix), status: pass
- resultCommit 5b5a6d6; branch implement/T161; base 311b8a1; check green 851/0.
- Hoisted `let real: string | undefined` before the try; read `fs.readFile(real ?? resolved)`. realpath success → reads canonical symlink-free validated path (closes TOCTOU); realpath ENOENT → real undefined → reads resolved → normal not-found surfaces (not masked). Added D28 regression test (in-root symlink resolution through the new code). 8 read-log tests + 851 suite green. Note: true race test would need FS-call interception (out of suite style); deterministic canonical-path test used instead.
- filesTouched: FsLedgerStore.ts, test/read-log.test.ts.
