# implement-worker (opus) — T231

- task: T231 (provider field + slash-extraction + reject bare pi, BREAKING); status: pass; resultCommit 325a855
- branch: worktree-agent-aaabc652fcf46f1f0

### Session summary
Extended ReviewerToken to {harness, model, provider: string|null}. parseReviewerToken keeps the first-colon harness/model split, then splits the model segment on the FIRST '/': pi requires provider/model both non-empty (no '/' = bare pi rejected; empty half rejected); claude rejects any '/' and keeps provider null. Added 6 focused parse-case tests; all pass, tsc -b clean. DEFERRED-SCOPE: legacy bare-pi fixtures in config.test.ts got provider:null on their ReviewerToken literals to keep tsc green; their runtime assertions remain broken by the grammar (23 pre-existing failures: 22 cq-config + 1 ledger-mcp) — deferred to T235 (fixtures) / T238 (cross-layer) per the task's scope note.
