# implement-reviewer — T315 (opus)

- Task T315; agent ac6e5154210a5967d; verdict **approve** (0/0/0)

ROLE_FLOWS exports 4 flows in canonical order (plan, investigate, implement, advance); each model:DiagramModel genuinely type-assignable to the renderer shape (RoleNode extends DiagramNode, RoleEdge = DiagramEdge), green typecheck confirms; every edge from/to resolves to a declared node id (test-enforced); ≥1 role node + ≥1 labeled action edge per flow with meaningful labels ('dispatches planner','returns verdict','merges by SHA'); diff touches only the 2 new files, Flows tab/flowData.ts untouched (T316 scope preserved). check green 1301/0. Minor non-blocking note: the assignability test line is a compile-time no-op; the guarantee comes from the field/type declarations.
