# implement-worker — T154 (G11, per-flow handoff writes + /advance suppression)
- status: pass; resultCommit 21f8a48; branch implement/T154; check green 841/0 (prompt-only)
- Added a §Handoff record section to the per-flow advance prompts (investigate/plan/implement advance.md) instructing a STANDALONE handoffs write at the flow's stop (stop→status mapping drained|answers-required|mixed|illness-detected + full field set) and SUPPRESSION when chained. Concrete suppression signal: env var CQ_ADVANCE_RUN=1 (/advance SETS, sub-flows CHECK) — stated identically across 7 prompts; advance.md left unedited (T153/T156 own it). plan/start + plan/follow-up (single-shot bootstraps) got dedicated handoff steps; investigate/start + implement/start note they inherit the write from the advance pass they run inline. No duplicate handoffs per /advance run. filesTouched: llm/commands/{investigate,plan,implement}/*.md (7 files).
