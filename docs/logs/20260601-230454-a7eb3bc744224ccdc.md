# plan-advance session log

- **Goal:** G1 — Implement the /implement:* command family (follow-up: UI/schema improvements)
- **Role:** planner (plan-advance)
- **Agent:** a7eb3bc744224ccdc
- **Returned status:** awaiting-answers
- **Timestamp (UTC):** 20260601-230454

---

### Session summary
- **Did:** Rule 2a step — read the follow-up scope and the relevant code (TUI `app.tsx` list/`ScrollList`, web `App.tsx` `ItemTable`/filter/`DetailPanel`/`summarize`, `docs/ledgers.yaml` schemas, `LedgerStore`/`ledgerTools.ts` archive surface, web `mcpClient.ts`/`types.ts`, and both reviewer prompts), then filed a batch of clarifying questions scoped to the follow-up.
- **Achieved:** Created open questions Q11–Q18 under M1 (linked `goals:G1`): Q11–Q12 archived-items read contract + read-only/UI reach; Q13–Q14 per-milestone filter vs. subsections (overlap) and which views/ordering; Q15 TUI column-table columns + narrow-pane degradation; Q16–Q18 reviews `summary` schema add (+ examples/canonical tests), badge-wrapping/summary-cell CSS, and the prompt/orchestrator write-path scope. Goal remains `clarifying`.
- **Discovered:** Confirmed reviews schema has NO `summary` field (fields: new_questions[], criticism[], ledgerRefs[], tags[], sourceRefs[]); `summarize()` falls through `headline?title?question?summary?` to the first field (criticism) — the wrapping cause. Web `ItemTable` already has a milestone column and IS a real `<table>`; req 3 (subsections) overlaps req 2 (milestone filter). Archives exist only via per-ledger `archivePointers` + `fetch_ledger_archive(ledger_id, archive_id)`; the web `mcpClient` does not even implement `fetchLedgerArchive`, and neither UI has any archive affordance. implement-reviewer writes no ledger item — its JSON contract feeds `/implement:advance`, so req 5's prompt work spans plan-reviewer.md, implement-reviewer.md, and implement:advance.md. The questions counter is ahead (new ids Q11+, not Q9), implying earlier withdrawn/created questions.
- **Issues:** none — these are real user-only scope decisions; planning resumes once they are answered.
