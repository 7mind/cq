# Session log — G45 plan-advance (opus), clarifying round 1

- **Goal:** G45 ("Reusable ledger-MCP library + tool-name namespacing (prefix)"); role: planner; returned **awaiting-answers**

### Session summary
G45 fresh in `clarifying`; grounded read-only in the tool-registration surface — ledgerTools.ts (createLedgerMcpTools + the hardcoded LEDGER_TOOL_NAMES 26-name array; safeIdSchema `^[A-Za-z0-9_-]+$`), stdioLedgerTools.ts (registerLedgerStdioTools raw registerTool, same literal names), ledger-mcp/main.ts (buildServer + SERVER_INSTRUCTIONS naming tools in prose + the drift-guard), @cq/ledger index.ts + package.json exports. Filed the first batch of 7 clarifying questions (open, linked goals:G45, under M156):
- **Q205** — namespacing mechanism (prefix), separator, and the critical cq-default-unprefixed decision.
- **Q206** — where the prefix is configured (programmatic param vs CLI flag vs cq.toml).
- **Q207** — scope of "reusable component": prefix-only vs public builder + exports + example.
- **Q208** — prefix-propagation completeness across both factories, the drift-guard, and the SERVER_INSTRUCTIONS text; pure-name-transform confirmation.
- **Q209** — frontend/client impact (cq frontends unaffected) + whether a resolved name-list export is in scope.
- **Q210** — storage-customization confirmation (storage out of scope).
- **Q211** — operational acceptance bar (two-prefixed-servers collision test, cq drift-guard unchanged, builder doc/example, bun run check).

DISCOVERED: LEDGER_TOOL_NAMES is a hardcoded literal duplicated as registration literals across BOTH tool factories; tool names appear in 4 places (two factories + drift-guard array + SERVER_INSTRUCTIONS prose), all of which a prefix must touch. The cohesive buildServer wrapper is NOT exported from @cq/ledger (lives in @cq/ledger-mcp/main.ts) — so the "build-your-own-server" public surface is the main scope question. Storage already parameterized via createLedgerStore.
