# implement-reviewer (opus) — T255 — approve (0/0/0)
T255(a) schema assertions match constants.ts (token in statusValues+terminalStatuses+transitions=[] + terminal). T255(b) grep test reads the four REAL prompt files (path resolves correctly) with VERIFIED TEETH — empirically mutating implement/advance.md to drop the token made the test FAIL (then restored). Test-only +65 lines, no scope creep. check green 1071/0.
