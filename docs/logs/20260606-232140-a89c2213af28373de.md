# Explorer session log — D34 / H26 (round 1)

- Defect: D34 (milestone M79)
- Hypothesis: H26
- Role: investigate-explorer (claude:opus)
- Lean returned: supports (mechanism confirmed; live-data shape flagged for runtime probe — since settled from the orchestrator snapshot)

## Session summary (verbatim)
- **Did:** gathered evidence for hypothesis H26 (top-bar progress `38/39`: numerator counts only `answered` questions; denominator counts ALL active questions including the 1 terminal-but-not-answered `withdrawn`).
- **Achieved:** 8 numbered evidence items; lean supports.
- **Discovered:** The top-bar bar (`LedgerProgressBar`, App.tsx:980/1407-1433) renders `done/total` straight from the SERVER-computed `LedgerSummary.completedCount/itemCount` — the client does NO filtering. Server-side `computeCompletedCount` (ledgerTools.ts:73-86, mirrored in stdioLedgerTools.ts:119-132) returns `sc["answered"]` ONLY for the questions ledger, while `itemCount` (ledgerTools.ts:192-198) totals every active item. The questions schema (constants.ts:204-208) makes both `answered` AND `withdrawn` terminal, and `withdrawn ≠ answered`, so 1 withdrawn question sits in the denominator (39) but never the numerator (38). The existing test enumerate-ledgers-summary.test.ts:67-130 encodes EXACTLY this mechanism (2 answered + 1 open + 1 withdrawn → itemCount 4, completedCount 2). No contradicting evidence found.
- **Issues:** Could not directly read live ledger data (no Bash/Grep tools); the exact 38-answered/1-withdrawn shape was inferred. SETTLED by the orchestrator from the session snapshot (answered=38, withdrawn=1, open=0). Design comments (types.ts:157-158, ledgerTools.ts:66-71) frame the `answered`-only numerator as INTENTIONAL — adjudication of defect-vs-intended left to orchestrator.

## Orchestrator validation
All decisive citations validated against source (ledgerTools.ts:78-79, 191-200; constants.ts:205-206; App.tsx:1416-1430). Live data confirmed from snapshot. H26 → confirmed. Root cause: non-symmetric denominator (itemCount includes `withdrawn` while the numerator is answered-only).
