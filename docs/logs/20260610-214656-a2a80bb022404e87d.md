# Session log — T384 implement-worker (sonnet), round 1

- **Task:** T384 (M161, G46); worker r1; status **pass**; resultCommit 4362906; check 1675/0.

### Session summary
- Added `export const TOP_LEVEL_USAGE` (mirrors RESTORE_USAGE string-array-join style; covers default stdio + --cwd/--http/--tool-prefix/restore) and a `--help`/`-h` branch at the top of main() that writes it to stdout and returns before any server construction. New help.test.ts verifies both flags resolve, stdout contains the 4 tokens, no 'serving' on stderr (--help), and --help wins regardless of argv position.
- check 1675/0.
