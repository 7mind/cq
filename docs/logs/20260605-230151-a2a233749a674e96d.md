# Implement-flow session log
- **Task:** T2 (M61, G18) — worker — pass (resultCommit 382f653, branch implement/T2; ff-merged 194b1bd)

### Session summary
- Wired the @cq/config capability into ledger-mcp buildServer so get_reviewers/get_config surface on the stdio binary + all embedded (TUI/web) paths. pass; typecheck+lint green, 933 pass/1 fail (T3's count target).
- Discovered: ledger-mcp has NO in-process createLedgerMcpTools call site — ALL four paths (standalone stdio, HTTP, embedded TUI via mcpClient.embedded, embedded web via serve.ts) funnel through buildServer(store,displayName). Single injection point covers all. @cq/config already a ledger-mcp dep (T1); LEDGER_TOOL_NAMES already carried the names. $CQ_CONFIG_ROOT only existed in the standalone server — bound config root = ledger root via new FsLedgerStore.rootDir getter.
- Files: ledger-mcp/src/main.ts (createConfigCapability(store.rootDir) → 4th arg of registerLedgerStdioTools), ledger/src/store/FsLedgerStore.ts (rootDir getter), ledger-mcp/test/main.test.ts (e2e stdio config roundtrip: no-cq.toml→configured:false, fixture→resolved set).
