# Plan-flow session log — candidate planner (opus)

- **Goal:** G28 — Enable subagent support in the pi agent harness for the cq flow
- **Milestone:** M86 (coordination)
- **Role:** candidate planner (plan-advance, CANDIDATE mode), alias `opus` = claude:opus-4.8[1m]
- **Outcome:** candidate plan emitted (4 milestones / 7 tasks); used as synthesis base
- **Agent id:** aeca57f3db613a5b9

## Session summary
- **Did:** Grounded read-only against the real tree (G28 + answered Q125–Q130, nix/hm/dev-llm.nix Pi wiring, the two existing nix/pkg/pi-extensions/*.ts, investigate/advance.md dispatch convention, investigate-explorer.md frontmatter, cq.toml + get_config schema) and emitted ONE candidate task-DAG; wrote nothing to the ledger.
- **Achieved:** A 4-milestone, 7-task candidate honoring all locked constraints — Q130 bespoke in-repo extension with a first go/no-go spike on the child-session primitive; Q126 shared prompts unchanged (tool shaped to the existing named-agent+task convention); Q127 orchestrator-named agent + additive cq.toml [tiers] tier->Pi-model resolution; Q128 acceptance = one explorer + one reviewer dispatch returning parseable structured results on this repo non-sandboxed, sandbox/implement-worker deferred; Q129 no vendoring, reusable extension surface.
- **Discovered:** Pi materializes mergedCommands+mergedSkills but NOT mergedAgents (Claude gets agents at dev-llm.nix L535; Pi block L616-688 omits them) — so a dedicated task to project the agents bundle to a Pi-discoverable path is a hard prerequisite for the extension, made an explicit DAG edge. The subagents-cannot-spawn-subagents invariant already lives in agent frontmatter as the `Agent`/dispatch-tool exclusion in `disallowedTools`.
- **Issues:** Codegraph not indexed for this tree (fell back to Read). The exact @earendil-works/pi-coding-agent child-session API is intentionally left for the spike task — the one genuine unknown, the gating first task by design.
