# plan-advance session log

- **Goal:** G1 — follow-up #2: defects/tasks separation + investigate:* flow
- **Role:** planner (plan-advance)
- **Agent:** acad80523d4bc148e
- **Returned status:** review-requested

---

### Session summary
- **Did:** Rule #2(b) — Q19–Q28 (the #2 follow-up batch) were all `answered`, the answers are decisive, so I grounded against the repo, persisted a #2-specific grounding addendum on G1, transitioned G1 `clarifying → planning`, and emitted the #2-follow-up plan.
- **Achieved:** G1 now `planning`. Three new work milestones created and recorded on `G1.fields.milestones` (now `[M3, M6, M7, M8, M9]`): M7 (investigate:* assets), M8 (defect-awareness in plan/implement prompts, dependsOn M7), M9 (Full-scope defect/hypothesis relationship views in TUI+web). Fifteen tasks T35–T49 created with acceptance/suggestedModel/dependsOn/ledgerRefs. Key files in scope: `llm/commands/investigate/{start,advance}.md`, `llm/agents/investigate-explorer.md`, `scripts/link-prompts.ts`, the existing plan/implement prompts under `llm/`, and `packages/ledger-web/src/App.tsx` + `packages/ledger-tui/src/app.tsx`.
- **Discovered:** The Q28 "Full" scope does NOT force a `@cq/ledger` schema change for the defect↔task link — `dependsOn[]` and `ledgerRefs[]` already exist on both the defects and tasks schemas (and `parentHypothesis` on hypothesis), so the bidirectional link from Q20 needs no new field. "Full" therefore reduces to prompt edits (M7/M8) plus UI relationship-view work (M9); T45 is a spike that gates any schema change only if the UI work surfaces a real gap. The defects schema already has the required `severity` field and `tasks` carries an optional `severity` too. `/implement:advance.md` already references "defect D2" inline, confirming defect vocabulary is welcome there.
- **Issues:** none blocking. The #1 follow-up (M6, T26–T34) and the original /implement plan (M3) are untouched, as instructed. Next: the plan-reviewer should adversarially review M7/M8/M9.
