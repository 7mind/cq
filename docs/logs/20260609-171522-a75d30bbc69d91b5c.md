# implement-worker — T329 (opus); FU-4b/d wiring (final follow-up task)
agent a75d30bbc69d91b5c; status pass; resultCommit 2631bf1; check 1368/0; nix build .#ledger-web exit 0. FU-4b: threaded onActivateAgent from the Flows tab → FlowDiagram → DiagramSvg; activating an agentId node flips the help tab to 'agents' (via pendingAgentAnchor + a [tab,anchor] effect that rAF-defers) and scrolls help-agent-<agentId> via the exported scrollToHelpSection; abstract nodes inert. FU-4d: FlowLegend (one swatch per RoleKind from exported ROLE_KIND_FILL) + legend CSS. New test flowsCrossNav.test.tsx: click+Enter cross-nav to help-agent-plan-advance, inert abstract node, per-kind swatch background-color. Note: exactOptionalPropertyTypes forced FlowDiagram's onActivateAgent prop required (sole call site always supplies it).
