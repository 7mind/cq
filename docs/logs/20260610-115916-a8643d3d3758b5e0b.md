# Session log — T356 implement-worker (opus)

- **Task:** T356 (M149, G43); status pass; resultCommit 5449411 (worktree) → c369b20 (main); check 1547/0

### Session summary
Discovered T352 had ALREADY wired GitObjectLedgerBackend through the COMPLETE runStoreAbstractSuite — so the full conformance body already ran against all three backends (Fs, InMemory, Git). The one conformance facet that lived FS-only (N-parallel mutation, in concurrency.test.ts) was lifted into the shared store-abstract.ts as portable concurrency-parity assertions (no-lost-write, monotonic updatedAt, last-write-wins, unique monotonic ids) exercised purely through the LedgerStore surface — so reads/writes/archive/concurrency parity is now proven identical across Fs/InMemory/Git. PARITY FINDING (no gap): two-instance concurrent writes are fully serialised by the advisory lockfile, so the Git CAS update-ref conflict (StaleRefError) never fires in normal operation; in-process the AsyncMutex fronts it. The CAS-conflict path is a deliberately-different cross-process surface (GitPersistence caveat 1), correctly excluded from the shared body, NOT papered over. N lowered to 12 + explicit 30s git-aware per-test timeout (40 serialised git ref-advances timed out under full-suite load). No tmp-repo residue (reuses seedRepo()/afterAll). Single file touched: store-abstract.ts. Based on main 85b0f01.
