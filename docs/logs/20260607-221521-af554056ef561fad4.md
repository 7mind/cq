# Implement-flow session log — worker T224

- **Task:** T224 — Write the bespoke nix/pkg/pi-extensions dispatch-agent extension
- **Milestone:** M88 · **Role:** implement-worker (frontier→opus) · **Status:** pass
- **Branch/commit:** worktree-agent-af554056ef561fad4 / 9b7c874ff0c53a7b2df2498fa3dd2d9b92f9e28f (parent 7611867)
- **Agent id:** af554056ef561fad4

## Session summary
- **Did:** authored `nix/pkg/pi-extensions/cq-subagent-dispatch.ts` (the bespoke `dispatch_agent` extension, Route A subprocess per the T221 spike) and registered it in `programs.pi.settings.extensions` in `nix/hm/dev-llm.nix`.
- **Achieved:** PASS. resultCommit 9b7c874. `bun run check` GREEN (tsc + eslint + 1037 pass/0 fail); standalone `tsc -p` of the extension vs pi-coding-agent 0.78.0 types exit 0; `nix-instantiate --parse` OK. **Dispatch probe (live pi):** parent (grok-build) called `dispatch_agent` for `investigate-explorer` and the tool returned a non-empty `"PONG"` (child exitCode 0); a controlled pair proved the child LACKS the dispatch tool — with `--exclude-tools dispatch_agent,write,edit,bash` the child's tool list excludes `dispatch_agent` (control without exclusion includes it). cq-assets untouched.
- **Discovered:** (1) pi's exported `parseFrontmatter` is strict-YAML and REJECTS cq agent frontmatter (unquoted `description:` values contain colons → "Nested mappings not allowed in compact mappings"); since cq-assets is read-only, the extension parses the flat `key: value` frontmatter itself (split on first colon). (2) `--no-extensions` on the child is too aggressive — it disables the pi-xai package extension that registers the `grok-build` provider (child → "Unknown provider"); dropped it. Re-dispatch guard = `--exclude-tools` denylist (always includes `dispatch_agent`) + never passing `--extension cq-subagent-dispatch.ts` to the child. Child defaults to the parent's active model (`ctx.model`); cq.toml path computed per K46 but tier resolution deferred to T225.
- **Issues:** worktree was cut from the WRONG commit (HEAD `1d5c8ea`, an ancestor lacking T221/T222) → `git reset --hard 7611867` (1d5c8ea is a clean ancestor of 7611867). The non-fatal `patch-grok-build-context-window.ts` "baseUrl is required" stderr appears at session_start (out of scope, ignored); the new extension emits NO load error. `AgentToolResult` has no `isError` field under this repo's strict tsconfig → errors surfaced in result text + `details.exitCode`. pi `node-pty` native postinstall (node-gyp) fails — irrelevant to typecheck/tests; install completed, 1037 tests pass.
