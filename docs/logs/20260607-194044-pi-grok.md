# Plan-flow session log — candidate planner (grok via pi shellout)

- **Goal:** G28 — Enable subagent support in the pi agent harness for the cq flow
- **Role:** candidate planner, alias `grok` = pi:grok-build (`pi -p --no-tools --no-session --provider grok-build --model grok-build`)
- **Outcome:** candidate plan emitted (5 milestones / 5 tasks); folded into synthesis (parallelized [tiers], empirical-probe spike, dedicated integration milestone)
- **Exit:** 0

## Verbatim captured stdout (incl. pre-fence-strip noise)
```
added 1 package, and audited 214 packages in 1s
[npm funding/version notices]
Extension error (/nix/store/5pkhb56j17fn98kjakikp2sghr1n0nsq-patch-grok-build-context-window.ts): Provider grok-build: "baseUrl" is required when defining models.
```
```json
{
  "milestones": [
    { "title": "Spike: Validate Pi child-session primitive", "dependsOn": [] },
    { "title": "Implement Pi subagent dispatch extension", "dependsOn": ["Spike: Validate Pi child-session primitive"] },
    { "title": "Support [tiers] in cq config and ledger parser", "dependsOn": ["Spike: Validate Pi child-session primitive"] },
    { "title": "Wire agents and extension into pi via dev-llm.nix", "dependsOn": ["Implement Pi subagent dispatch extension", "Support [tiers] in cq config and ledger parser"] },
    { "title": "Demonstrate explorer and reviewer dispatches under pi", "dependsOn": ["Wire agents and extension into pi via dev-llm.nix"] }
  ],
  "tasks": [
    "Spike: validate Pi ExtensionAPI supports spawning filtered-tool child sessions and capturing structured output (fast)",
    "Implement the production cq subagent dispatch extension (standard)",
    "Add [tiers] table to cq.toml and extend ledger config parser (fast)",
    "Wire agents and the new extension into the pi harness in dev-llm.nix (standard)",
    "Demonstrate one read-only explorer dispatch and one reviewer dispatch under pi (standard)"
  ],
  "rationale": "Decomposition starts with the single genuine technical unknown as an isolated read-only spike; all subsequent work depends on the proven primitive. [tiers] support and the extension are parallelizable post-spike (additive, non-breaking). Wiring is the last integration step; terminal milestone is exactly the locked acceptance criteria."
}
```
(Full task descriptions/acceptance captured in the run task-output file; condensed here.)

Note: grok parallelized [tiers] off the spike (parallel to the extension) and bundled agent-projection into the wiring task — the synthesis kept the parallelization but promoted agent-projection to its own task per the opus base.
