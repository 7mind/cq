# implement-reviewer (opus) — G32 enforcement (T257+T258+T259 integrated) — approve (0/0/0)
assertHandoffInvariants (core.ts:891) pure — mixed/answers-required⇒non-empty blockingQuestions, user-action-required⇒non-empty handoffReasons, drained/illness exempt; throws SchemaValidationError (sibling type); no cross-ledger access; HANDOFFS_SCHEMA fields stay required:false. Wired into applyCreateItem (after validateFields, L356) + applyUpdateItem on EFFECTIVE merged status+fields BEFORE the in-place mutation loop (L289-301) — throw leaves the item untouched (no fail-after-commit). Both adapters route create/update through the shared apply helpers → single HANDOFFS_LEDGER keying covers both, no bypass. T259 dual-adapter matrix: HO26 repro (pre-fix-succeeded note) + 3 status×empty throws + valid + drained/illness succeed + update-path empty-field rejection, real .rejects.toThrow assertions. Fixed fixture made VALID not weakened. D39 files 34/0; full suite 1135/0.
