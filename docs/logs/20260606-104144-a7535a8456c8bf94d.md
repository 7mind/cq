# Plan-flow session log
- **Goal:** G20 (M65) — planner (plan-advance) bootstrap clarifying round — awaiting-answers

### Session summary
- **Did:** Grounded both G20 feature requests read-only against the repo, recorded a grounding summary on goal G20, and filed the first batch of clarifying questions (fresh `clarifying` goal, no prior questions, not defect-seeded). No phase transition.
- **Achieved:** Wrote G20.fields.grounding; filed 7 `open` questions under M65, each ledgerRefs ["goals:G20"]:
  - Q105 — F1: cq.toml [webui] schema; port as integer (extend parser) vs quoted string; host+port only vs future-proof table.
  - Q106 — F1: per-field precedence (CLI > cq.toml > default) for host/port; only port increments.
  - Q107 — F1: port auto-increment scan bound, always-on vs config-only, exact stdout report format/stream (today prints to stderr).
  - Q108 — F2: is `cq` a net-new package/bin packaged like ledger-mcp; bin name literally `cq`; root via --cwd > $LEDGER_ROOT > CWD.
  - Q109 — F2: precise relocation boundary for `cq reset` (keep FsLedgerStore.reset() library, move CLI wrapper, drop `ledger-mcp --reset`) + whether that breaking CLI change is acceptable.
  - Q110 — F2: pin init/reset/erase as non-overlapping; exact `erase` blast radius (esp. .backup/ + logs/) and confirmation policy.
  - Q111 — Coordination with in-flight G18 (same @cq/config parser files) + whether `cq init` should scaffold cq.toml.
- **Discovered:** "reset" already exists at two layers — library method FsLedgerStore.reset() in @cq/ledger (backup to docs/.backup/<ts>/ + reinit-empty) and CLI wrapper `ledger-mcp --reset/--yes` (runReset/ResetIo) in packages/ledger-mcp/src/main.ts; only the wrapper relocates. `init` maps to idempotent store.init() bootstrap; `erase` (destroy) has NO existing implementation — most ambiguous/destructive. ledger-web (packages/ledger-web/src/serve.ts) already parses --host/--port (defaults 127.0.0.1/5180), runs embedded MCP by default, prints serving line to STDERR (goal asks STDOUT), does NOT read cq.toml today. @cq/config TOML parser supports only quoted strings + string arrays (no numbers) and whitelists tables, so [webui] requires extending toml.ts/config.ts/types.ts — the SAME files G18 PART 2 edits for planners.
- **Issues / deviations flagged as questions:** stdout-vs-current-stderr startup print; removing `ledger-mcp --reset` is a breaking CLI change; same-file coordination risk with G18. None blocking.
- Returned token: awaiting-answers.
