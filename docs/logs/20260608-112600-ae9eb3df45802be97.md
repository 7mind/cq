# implement-worker (sonnet) — T234 (committed scope) — pass (4eb7018→49fd0b1)
Migrated cq.toml.example [aliases] to qualified pi tokens (grok-build→pi:grok-build/grok-build, minimax-m3→pi:ollama-cloud/minimax-m3; opus unchanged). Added cq-toml-example.test.ts parsing the real repo-root example via parseConfig(readFileSync) (not loadConfig, which reads the gitignored cq.toml) + asserting minimax→{pi,minimax-m3,ollama-cloud}, codex/grok→{pi,grok-build,grok-build}, and no bare-pi survivor. check green 1063/0. DEFERRED: the live gitignored cq.toml migration (activation-coupled — old MCP) is a separate operational step (T246-class).
