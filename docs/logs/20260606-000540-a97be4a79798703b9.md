# Implement-flow session log
- **Task:** T13 (M62, G18) — worker, round 0 — pass (resultCommit 5bb8991)

### Session summary
- **Did:** exposed get_planners + planners-on-get_config across BOTH ledger-MCP surfaces (Claude tool() factory + raw stdio registerTool), bumped tool-surface count 20→21.
- **Achieved:** pass; resultCommit 5bb89917a944d6a4262032dda03baf1bee0b95dd (branch implement/T13); bun run check green — typecheck + lint clean, 931 pass / 0 fail / 2854 expect.
- **Discovered:** @cq/config already exports resolvePlanners + CqConfig.planners (from T12), so computePlanners mirrors computeReviewers exactly. buildServer wiring needed no change (createConfigCapability returns the capability object; adding computePlanners flows through). The cross-surface drift-guard is main.test.ts L87 (expect(names).toEqual([...LEDGER_TOOL_NAMES].sort())).
- Reproduce-first: count test RED `expect(LEDGER_TOOL_NAMES.length).toBe(20)` → Expected 20 Received 21; drift-guard RED on a one-surface mismatch (get_planners removed from stdio only → stdio listTools diff '- get_planners'). Both green after both surfaces + tests updated.
- e2e stdio roundtrip (ledger-mcp/test/main.test.ts) lists get_planners on the real STDIO binary, asserts configured:false/planners:[] (no cq.toml) and resolved planners=["opus"] from fixture, + planners on get_config in both cases.
- Files: ledger/src/mcp/configCapability.ts, ledger/src/index.ts, ledger-mcp/src/configCapability.ts, ledger/src/mcp/ledgerTools.ts, ledger/src/mcp/stdioLedgerTools.ts, ledger/test/mcp-tools.test.ts, ledger-mcp/test/main.test.ts.
- **Issues:** none.
