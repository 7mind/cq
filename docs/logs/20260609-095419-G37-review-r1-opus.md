# Plan review log — G37 round 1 (reviewer: opus[claude], configured mode)

- Goal G37 (M122), defect-seeded D43 fix, multi-reviewer round 1. Verdict: **revise** (1 criticism). Returned JSON, wrote nothing (orchestrator writes the single aggregated reviews item). grok+codex abstained (no API key); panel = opus + minimax(pi).

### Verdict: revise

**Criticism (verbatim):** "T307 acceptance presumes `nix build .#llm-skills` validates the 4 edited cq-assets governance prompts (implement-worker.md + the 3 advance.md). This is false. `.#llm-skills` builds nix/pkg/llm-skills; it never reads nix/pkg/cq-assets/agents/ or commands/. cq-assets is consumed EVAL-TIME-ONLY (assets.nix via builtins.readFile/readDir at eval) — NO buildable derivation vendors the prompt files. So `.#llm-skills` exits 0 regardless of whether T301-T304's edits are present or correct, giving ZERO regression coverage. This is the SAME defect already adjudicated for G31 (R280 revise → R281 go-ahead). Fix: drop the claim that any nix build exercises the 4 files; make `bun run check` + the T306 grep-invariant the SUBSTANTIVE gate. Update the goal's own acceptance note."

**Otherwise confirmed:** T301 closes part-a (worktree-confinement Boundary, all 4 clauses); T302/T303/T304 close part-b non-regressively (the existing after-archive + run-stop commits stay; the new checkpoints override chained-suppression only for themselves); DAG acyclic (T301∥T302 → T303 → T304; T305 no-dep; T306 deps T301-T304; T307 deps T305+T306); T306 grep-invariant (T255/T264 pattern) is the right guard given cq-assets is eval-time-only.
