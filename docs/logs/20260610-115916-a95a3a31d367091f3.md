# Session log — T349 implement-worker (sonnet)

- **Task:** T349 (M146, G43); status pass; resultCommit 315874a (worktree) → 054a64c (main); check 1550/0

### Session summary
Extended @cq/config with an optional top-level `[ledger]` table: added `LedgerBackend = 'fs' | 'git-object'` union + `isLedgerBackend` guard + `LedgerConfig` interface (types.ts); `RawLedger` structural validation + whitelist (toml.ts); `parseLedger()` with default-application (branch 'cq-ledger', remote 'origin') + fail-fast unknown-backend rejection via CqConfigError (config.ts); `ledger` field added to CqConfig (null when [ledger] absent → fs default; git-object opt-in per Q189); exports (index.ts). Added a documented COMMENTED-OUT `[ledger]` block to CQ_TOML_TEMPLATE (cqTomlTemplate.ts) + cq.toml.example. 9 acceptance tests in config.test.ts (git-object resolution, fs default, branch/remote override, absent table, unknown-backend throw, non-string backend, unexpected key, inert commented block). Based on main 85b0f01.
