# Session log — T365 implement-worker (sonnet) + opus review (approve)

- **Task:** T365 (M155, G44); status pass; resultCommit bd3744f → 81fcc1e (main); check 1630/0; review **approve** (opus a96fe737b31a0488a, 0 findings)

### Session summary
WORKER: added a T365 grep-invariant guard (in packages/ledger/test/canonical-ledgers.test.ts, beside D39's T264 advance.md prose guard) — 5 per-string assertions reading the real advance.md + expect(content).toContain(token) for the G44 load-bearing enforcement strings: (1) §Bootstrap marker-drop `touch "${XDG_RUNTIME_DIR:-/tmp}/cq-advance-active-$CLAUDE_CODE_SESSION_ID"`; (2) §The one write marker-unlink `rm -f ...`; (3) external-signal escape `external-signal:`; (4) Stop-hook cross-ref `claudeStopGateHook`; (5) `mcp__ledger__derive_predicates`. Mirrors T264's structure exactly. check 1630/0.
REVIEW (opus approve): all 5 tokens present VERBATIM in advance.md (grep -F confirmed, no typos); TEETH proven empirically (removing `external-signal:` fails the matching it()); reads the real file (not a hardcoded copy); mirrors T264/D39 guard; check 1630/0.
