# Investigate explorer log — D43 / H31 (role: explorer, lean: supports)

- Defect D43, hypothesis H31 (two-part prompt gap). Explorer opus (a4b0d0d4f781c94c2). 9 evidence items; orchestrator validated the load-bearing citations against source (excerpts match exactly) → H31 confirmed.

### Session summary
- **Did:** gathered evidence for H31 (a: implement-worker not confined to its own worktree; b: deferred ledger-commit discipline leaves a large uncommitted-ledger window a stray `git reset --hard` erases) across the 4 cq-assets prompt files.
- **Achieved:** 9 numbered evidence items; lean supports. Orchestrator re-validated: implement-worker.md "Boundaries (hard rules)" (L47-55) forbid merge/push/rebase + scope but contain NO worktree-confinement / no-cross-checkout-git / reset-only-in-own-worktree rule; the worker's only sanctioned git mutation is `git add -A && git commit` on the task branch (L71); the base is PASSED IN by the harness (native isolation:worktree, L38-43) so the worker never establishes its own base → the stale-base trigger is harness-side and the missing guardrail lets a worker improvise cross-checkout git to 'fix' it. implement/advance.md (L395-405) commits the ledger ONLY after archive_milestone + at the standalone stop (suppressed when chained, L542-549); advance.md (L506-518) commits after every archive + at run-stop, NOT after every task merge; plan/advance.md (L717+) commits only at the standalone stop (suppressed when chained), no commit-after-planning-lock.
- **Discovered:** part (a) is a confirmed ABSENCE (permissive gap), not a prescriptive instruction to do harm. Fix leads: (1) add a worktree-confinement hard rule + "NEVER run git against any path outside your worktree; NEVER git reset --hard/checkout/cherry-pick to fix your base — report a stale base as status=fail" to implement-worker.md Boundaries; (2) commit-after-planning-lock + commit-after-every-merge checkpoints to shrink the uncommitted-ledger window.
- **Issues:** none — both parts confirmable statically; no probe needed.
