# implement-worker — T310 (sonnet) — ROUND 0 (fail)

- Task T310 (implement-worker.md alignment); agent ad38cc1669269c0e7; status **fail**; resultCommit fd49943
- check 1293 pass / 1 fail — the failure is `agentsCatalogue.gen.ts freshness guard (part b)`.

### Session summary
Added orchestrator-worktree-teardown note to implement-worker.md Boundaries region with marker G38-1a-worker-ephemeral (×1); markdown edit correct. **blockedReason:** task-spec contradiction — acceptance requires `bun run check` green, but the freshness guard fails on any cq-assets/agents/*.md edit until `bun run gen-agents` is run, which the task said to defer to T322. Resolution (orchestrator): the freshness guard FORCES per-task regen; re-dispatch T310 instructing it to ALSO run gen-agents + commit the regenerated gen.ts.
