# Implement-flow session log — worker T226

- **Task:** T226 — Acceptance demo: read-only explorer dispatch under Pi returning parseable evidence-json
- **Milestone:** M91 · **Role:** implement-worker (frontier→opus) · **Status:** pass
- **Branch/commit:** worktree-agent-aa0c624118a6e9655 / d6166755a3882575148f726689e0c70936615545 (parent cc2f326)
- **Agent id:** aa0c624118a6e9655

## Session summary
- **Did:** ran live pi dispatch demos under the wrapped grok-build harness from an UNCHANGED cq-convention prompt; captured evidence; ran the orchestrator-side parse; wrote+committed `docs/drafts/20260608-0029-T226-explorer-dispatch-demo.md` + a capture dir.
- **Achieved:** pass; resultCommit d616675. All 5 acceptance criteria demonstrated on THIS repo: (1) unchanged cq `subagent_type:"investigate-explorer"` dispatch wording from advance.md §3; (2) grok-build fired `dispatch_agent(agent="investigate-explorer")` from that prompt (not a manual call); (3) child `excludedTools=[dispatch_agent,write,edit,bash]` (NO Write/Bash/dispatch), matching investigate-explorer.md disallowedTools; (4) child returned a fenced-json evidence block (8 file:line citations + excerpts + relevance), live orchestrator-side JSON.parse + contract validation succeeded; (5) git diff of nix/ + nix/pkg/cq-assets clean. Docs-only.
- **Discovered (follow-ups):** (a) home-manager `~/.pi/agent/settings.json` registers a STALE T224 copy of the extension (nix-store path predating the T225 merge — HM activation not re-run), which conflicts with an explicit --extension of the merged file ("Tool dispatch_agent conflicts"); worked around via a cloned PI_CODING_AGENT_DIR swapping that one extension path for the repo's merged T225 file. Re-running HM activation refreshes the store path. (b) cq.toml.example frontier tier = a `claude:` token → can't drive a child pi → falls back to parent model (resolvedTier=frontier, modelSource=parent); explorer runs read-only regardless.
- **Issues:** none blocking; the stale-settings extension is a harness-activation follow-up out of scope for this docs task.
