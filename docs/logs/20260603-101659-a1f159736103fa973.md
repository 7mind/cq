# investigate-explorer session log

- defect: D13 (TUI ~500ms per cursor move)
- hypothesis: H11 (residual un-memoized O(N) cost on the per-keystroke path that T85 did not cover)
- role: explorer
- returned lean: supports (sites exist; magnitude NOT established — later REFUTED as dominant by orchestrator runtime measurement)

### Session summary
- **Did:** gathered evidence for H11 (residual un-memoized O(N) on the cursor-move path).
- **Achieved:** 6 evidence items; lean supports (existence of sites).
- **Discovered:** viewItems = allRows.map(r=>r.item) (app.tsx:1009) rebuilds an O(N) array every render OUTSIDE itemsDerived useMemo; ContentPane (un-memoized) calls defectFixTaskIds (app.tsx:1389) + hypothesisRelationships (app.tsx:1390) which are O(N) (relationships.ts:69, 113-114) on defects/hypothesis ledgers; findItem (app.tsx:1393-1395) O(N) .find per related id. navMemo test uses a TASKS ledger where isDefect/isHypothesis are false → this path never fires under the T85 guard, explaining how it slipped past.
- **Issues:** static evidence identifies the O(N) sites but does not establish they account for ~500ms. [ORCHESTRATOR FOLLOW-UP: runtime micro-bench measured hypothesisRelationships=32µs@N=400 / 156µs@N=1600, and end-to-end per-move latency FLAT in N (55ms @ N=25/100/400) → H11 is NEGLIGIBLE, NOT the dominant cost. Refuted as root cause.]
