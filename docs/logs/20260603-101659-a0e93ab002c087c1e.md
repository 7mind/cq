# investigate-explorer session log

- defect: D13 (TUI ~500ms per cursor move)
- hypothesis: H12 (ink full-screen redraw / stdout / measureElement cost dominates, React-independent)
- role: explorer
- returned lean: mixed

### Session summary
- **Did:** gathered evidence for H12 against ink 7.0.5 commit/draw path.
- **Achieved:** 8 evidence items; lean mixed.
- **Discovered:** render(<App/>) called with NO options (main.tsx:115) → defaults (maxFps=30). App renders full-screen frame width=cols height=rows (app.tsx:1031) so every frame lays out + writes the whole screen. Per React commit ink runs a FULL yoga calculateLayout() (reconciler.js:92-118 → ink.js:302-306) — synchronous, NOT throttled. BUT the stdout write IS throttled to 34ms (ink.js:176-197) and skipped on unchanged output (ink.js:734); default log-update does full-frame erase+rewrite (log-update.js:45-53). 34ms throttle is ~10× too small to be the sole ~500ms cause. ink already exposes onRender({renderTime}) (ink.js:320-322) — unused; the instrumentation lead.
- **Issues:** cannot apportion wall-clock statically. ~500ms implausibly large for one ink frame → points to many renders/keystroke or O(N) inside render. Runtime measurement REQUIRED. [ORCHESTRATOR FOLLOW-UP: the empty-desc N-independent floor (~28ms in harness) is the full re-render + full-screen layout; the dominant amplifier is the React-side markdown re-parse (H10). H12's "React-independent dominant" claim refuted; ink draw is a folded-in contributor to the H9 floor, not separately dominant.]
