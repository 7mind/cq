# investigate-explorer session log

- defect: D13 (TUI ~500ms per cursor move)
- hypothesis: H9 (per-cursor-move full App re-render + ink full-tree reconciliation is the dominant cost)
- role: explorer
- returned lean: supports (mechanism; magnitude needs runtime profiling)

### Session summary
- **Did:** gathered evidence for H9 against packages/ledger-tui/src/app.tsx + components + the T85 navMemo test.
- **Achieved:** 8 evidence items; lean supports (structurally), with explicit caveat that the magnitude claim requires runtime profiling.
- **Discovered:** cursor lives on top-of-stack Frame (app.tsx:293-307); every up/down move calls patchTop({cursor})→setStack (app.tsx:398-404, 830-832) → whole App re-renders. NO React.memo/memo( anywhere in app.tsx; ScrollList/ContentPane/ItemRowLabel/Overlays are plain inline function components → React re-renders the entire subtree per cursor move. T85's itemsDerived useMemo (app.tsx:743-760) stops the O(N) DATA builders re-running but returns the same component TREE — ScrollList/ContentPane still re-execute and ink reconciles the full tree. navMemo test (test/navMemo.test.tsx:177-181) asserts only derivationCounters==0, not render count/breadth/latency.
- **Issues:** static reading establishes the MECHANISM but cannot establish it is the ~500ms-dominant cost vs a single heavy op. Competing heavy-op: ContentPane re-parses markdown each render (app.tsx:1485,1509 + markdownText.tsx). Orchestrator must profile.
