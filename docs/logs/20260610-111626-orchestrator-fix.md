# Session log — T353 round-2 fix (orchestrator, opus-4.8[1m])

- **Task:** T353 (M145, G43); orchestrator-applied round-2 fix; resultCommit e4233c0

### Session summary
Round-1 review disapproved T353 (test flaky 2/4 under load). I reproduced the flakiness directly (2 fail / 1 fail / 1 fail across 3 isolated runs — confirming a real race, not the worker's lucky green). Applied the reviewer's suggested test-gating fix (gate tests 1/2 on `changedIds.includes("widgets")` rather than fetch-coherence) — but it STILL flaked (now a 5s TIMEOUT: onChange('widgets') genuinely never fired in some runs). Root cause was DEEPER than the round-1 ordering-race diagnosis: a **baseline race in the production watcher** — refWatcher.ts established its baseline `lastSha` only at the FIRST poll TICK (`setTimeout(poll, pollMs)`), so a ref advance in the start→first-tick window was captured AS the baseline and silently missed. Tests 1/2 start the watcher then IMMEDIATELY write through A with no delay; under full-suite load the first tick is delayed past A's write → baseline = post-write sha → no change ever observed → timeout. (Test 4 dodged this with a 120ms wait.) FIX (production, in T353 scope): call `poll()` IMMEDIATELY at startup to establish the baseline synchronously (the caller has already awaited store.init()), instead of deferring the first poll one interval. Verified: 8/8 isolated runs green + full `bun run check` 1541/0; round-2 re-review (opus) confirmed determinism (8/8 isolated + 3/3 full-suite) and the immediate-poll change correct (undefined-sentinel still guards spurious first fire; single timer schedule; safe close/poll race; test-4 baseline timing preserved). This was a genuine production defect in the watcher's startup semantics — a ref advance in the first poll window would have been silently swallowed in production too, not merely in the test.
