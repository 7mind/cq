# plan-reviewer session log

- goal: G10
- subagent: plan-reviewer
- verdict: revise (review R129) — 4 criticisms, 0 new questions

### Session summary
- **Did:** reviewed M38 (T132/T133/T134) for G10 against the confirmed root causes D13/D23, the investigation bench, and the actual repo (app.tsx, markdownText.tsx, app.test.tsx, navMemo.test.tsx).
- **Achieved:** verdict revise, review id R129, 4 criticisms / 0 new questions / 0 out-of-scope defects.
- **Discovered:** The D13 fix has a fix-to-symptom gap. The symptom is per-cursor-MOVE latency, but on a real move the selected item changes — ContentPane.row and ScrollList.cursor props both change (React.memo cannot skip them on a move) and the selected markdown `text` changes (so useMemo(()=>parseBlocks(text),[text]), a one-slot memo, misses every move). The bench debug/20260603-101700-d13-navperf.tsx confounds the measurement: makeItems(n,desc) gives every item the SAME description, so the one-slot memo hits across moves in the bench but not in production where adjacent items differ. T132's acceptance ("a pure cursor move no longer re-parses unchanged markdown") is vacuous for navigation; T133's guard risks reproducing the constant-text artifact. Also: ScrollList's inline getLabel/renderLabel closures (app.tsx:981-1001) make React.memo inert unless memoized; T133's parseBlocks instrumentation hook lands in markdownText.tsx (also edited by T132) so its "file-disjoint/test-only" scope note is wrong (dependsOn ordering covers it). D23/T134 is sound and correctly scoped.
- **Issues:** Codegraph MCP not reachable; verified all citations via Read. reviews schema rejects summary/defects fields; verdict folded into first criticism (matching prior reviews).
