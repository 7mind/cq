# investigate-explorer session log

- defect: D13 (TUI ~500ms per cursor move)
- hypothesis: H10 (ContentPane re-parses markdown / re-computes detail layout on every cursor move)
- role: explorer
- returned lean: supports

### Session summary
- **Did:** gathered evidence for H10 under the detail-pane lens.
- **Achieved:** 8 evidence items; lean supports.
- **Discovered:** Markdown (markdownText.tsx:142-143) calls parseBlocks(text) unconditionally in render body, no useMemo, not React.memo. parseBlocks (L75-140) is a per-line tokenizer; renderInline (L23-64) a per-run regex loop. ContentPane (app.tsx:1325) is a plain function component rendering one <Markdown> per long field (app.tsx:1485,1509) → parse runs once per long field per render. On cursor move top.cursor changes → cur=allRows[top.cursor] (app.tsx:928-930) is a different Row → ContentPane re-renders. ContentPane also rebuilds field layout each render: orderItemFields (L1369-1382) + per-field estimateLines loop (L1399-1408). T85 memo scoped to LIST derivations only (app.tsx:730-743), excludes detail pane.
- **Issues:** static evidence pins the structure (unconditional un-memoized per-field parse) decisively; magnitude not established statically. Runtime measurement needed.
