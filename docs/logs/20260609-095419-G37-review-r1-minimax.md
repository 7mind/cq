# Plan review log — G37 round 1 (reviewer: pi:ollama-cloud/minimax-m3, configured mode)

- Goal G37 (M122), defect-seeded D43 fix, multi-reviewer round 1. Verdict: **revise**. Returned JSON via pi shellout (exit 0); orchestrator writes the single aggregated reviews item. grok+codex abstained (no API key).

### Verdict: revise

minimax confirmed the plan structurally sound (both halves of the Q166 fix covered; DAG acyclic; reproduction-first T305 correct per §6a; grounding matches CLAUDE.md). Findings:

1. **[headline] Deploy/activation step.** T307 stops at `nix build .#llm-skills` + `bun run check`; the new prompts don't reach `~/.claude`/`~/.pi` until `home-manager switch` — so D43 stays live in the user's session post-verify. Either add an activation task or mark activation an explicit user follow-up. — ADJUDICATED: already flagged in T307 + G37 (the home-manager-switch live-activation note, like D37/D41); kept explicit. NOT made an autonomous task (the switch is a user/environment action the sandbox cannot reliably run — consistent with the D37/D41 user-action handoff precedent).

2. **[REAL] T303/T304 "same marker" vs T306's "4 markers" contradiction.** T304's dep rationale implied it reuses T303's marker, but T303 (implement/advance.md) and T304 (advance.md) are distinct files, and T306 asserts 4 distinct cells. Pick the 4-distinct-files reading and drop "same marker", OR merge T304 into T303 and reduce T306 to 3. — ACCEPTED: revised T304 to state it writes its OWN distinct file-scoped marker in advance.md (cell 4 of T306); the four cells are 4 distinct (file, marker) assertions (T303's and T304's phrasing may overlap but are asserted in two distinct files).

3. **T306 file-scoped specificity.** Make explicit that each assertion reads a SPECIFIC file path and asserts its marker in THAT file (not repo-wide grep). — ACCEPTED: tightened T306 wording.

4. **T301 Boundary non-exhaustive (rebase/push/fetch).** — PARTIALLY PRE-SATISFIED: implement-worker.md already bans merge/push/rebase globally, and T301 already generalizes via "(or any op, incl. git -C <other> / --git-dir) that switches or mutates a working tree other than its own". Tightened the generalization wording to be unambiguous.

5. **T305 reproduction-first narration.** Add a one-line note that the repro captures the failure on the UNFIXED code (pairs with T306's after-state assertion). — ACCEPTED: added.

minimax's `defects[]` entry restated finding #2 (no separate code defect).
