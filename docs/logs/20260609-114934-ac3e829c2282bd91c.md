# Plan-flow candidate planner — G38 (opus, claude plan-advance CANDIDATE mode)

- Goal: G38 (Flow hardening + UI: worktree cleanup, ~/.cache backup, flows-tab role-actions, TUI keybindings)
- Role: candidate planner (multi-planner generate-N-then-judge), alias `opus`
- Agent id: ac3e829c2282bd91c
- Outcome: candidate plan emitted (5 work milestones incl. cross-cutting verification; 9 tasks)

### Session summary
- **Did:** Grounded read-only against G38's ledger state (goal G38, milestone M126, the 7 answered questions Q167–Q173 — all "as recommended") and the actual repo loci for all four bundled work items, then composed ONE candidate task-DAG (CANDIDATE mode — no ledger writes).
- **Achieved:** 5-work-milestone candidate (W-a worktree-cleanup prompt edits; W-b ~/.cache mirror + restore CLI; W-c flows-tab role-actions; W-d TUI focus keybindings incl. a defect-fix task; W-e cross-cutting verification), operational acceptance on every task, verbatim grep-invariant markers on the prompt edits.
- **Discovered:** §7.3 worktree-removal text L310-311; §5 "Leave the worktree INTACT" L282-283; §K4 L47-55; §1 start-of-pass anchor L91. FsLedgerStore exposes rootDir (L200), fires onMutation(ledgerId,op) synchronously post-lock (L210-229) — op carries ledgerId NOT a file path, so the mirror must map ledgerId→ledgerPath internally and is best implemented INSIDE the store. reset()/backupAndReinit()→docs/.backup/<ts>/ is the symmetry precedent for restore. ledger-mcp main.ts has NO subcommand dispatch — restore needs new positional parsing. ledger-web has flowData.ts (state diagrams to REPLACE) + agentsCatalogue.ts (untouched Agents tab); the new role→actions catalogue is a THIRD module feeding the existing DiagramSvg/layoutDiagram. TUI item-3 loci: app.tsx LIST-focus L840-845, CONTENT-focus L802-834, listInnerH L896, no Home/End anywhere; ink exposes no key.home/key.end.
- **Issues:** No codegraph index / no Grep tool — exact test-file paths + precise Flows-tab JSX block not pinned; acceptance names renderer/testids/module rather than line numbers.

Key fold-in (architectural): one SHARED path-scheme function used by BOTH the CacheMirror writer and the restore CLI (writer/reader symmetry is the point of restore-without-replay). Cross-cutting verification milestone depends on all four item-milestones.
