# implement-worker — T154 round 1 (criticism fix)
- status: pass; resultCommit 7cbc8d1; branch implement/T154b; check green 841/0 (prompt-only); advance.md NOT edited
- Replaced the broken env-var (CQ_ADVANCE_RUN) suppression with a CONTEXTUAL signal: STANDALONE invocations write ONE handoffs record (stop→status mapping + HANDOFFS_SCHEMA field set); when CHAINED INLINE by /advance the write is SUPPRESSED, keyed on the executing agent's invocation context (no env var/process signal — /advance runs sub-flows inline in the same session). start/follow-up note they are never /advance-chained + inherit the standalone write; plan/advance + plan/start/follow-up chain investigate/advance inline (auto-investigate) — the suppression in investigate/advance covers that path too (no duplicate). Confirmed zero CQ_ADVANCE_RUN refs remain. Consistent with advance.md §Provenance single-writer model. filesTouched: llm/commands/{investigate,plan,implement}/*.md (7 files).
