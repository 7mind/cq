# Investigate-explorer session log

- **Defect:** D31 — web BatchAnswerModal premature dismissal on non-last answer
- **Hypothesis:** H22 (T163/D29 changed the modal advance-vs-close behavior)
- **Role:** explorer (read-only)
- **Returned lean:** contradicts

---

### Session summary
- **Did:** gathered evidence for hypothesis H22 (T163 changed BatchAnswerModal so a non-last answer dismisses instead of advancing).
- **Achieved:** 6 numbered evidence items; lean **contradicts**.
- **Discovered:** The advance/close decision lives in `batchSave` (App.tsx:543-571), which recomputes the remaining-open set from `batchAnsweredRef` and only calls `setBatchOpen(false)` when `remaining.length === 0`, otherwise advances `batchIndex` to the next unanswered row. The full T163 diff (commit 6629483) touches ONLY `answerHasText` initialization/re-sync and adds `disabled={!answerHasText}` to two submit buttons plus a `fireInput` typing step in tests — it does NOT modify `batchSave` or any close/advance code. The submit button (App.tsx:1735-1742) calls `onSave` and never `onClose`; the only other close paths are Escape and backdrop. The batchModalClose tests already cover the multi-question/non-last case and assert the modal stays open after a non-last answer plus progress advancing to "3 of 3" — and T163 preserved every one of those assertions.
- **Issues:** Source at HEAD does not reproduce D31. Drill suggestions: commits after 6629483 (NONE — orchestrator confirmed), the line-565 reload interaction, duplicate-row indexOf. Codegraph not indexed.

Orchestrator validation (2026-06-05): re-read App.tsx:543-571 (batchSave advance/close — exact), App.tsx:1695-1752 (ALL three save buttons — submit, "as recommended", per-suggestion "pick" — call the SAME `onSave`→`batchSave`, so the user's "as recommended" uncertainty resolves to the same correct path), batchModalClose.test.tsx:233-257 (mid-queue "Q2 of 3 advances to Q3" — exact). `git log 6629483..HEAD -- App.tsx` = EMPTY (no post-T163 change). `batchSchema` set once at open (App.tsx:528), never reset by `reload()` (469) — so reload cannot close the modal. RAN `bun test batchModalClose.test.tsx` at HEAD → 6 pass / 0 fail. The regression does NOT reproduce from current source; the test is a green reproduction harness showing the CORRECT (advance) behavior.
