# Investigate-flow session log
- **Defect:** D32 (M61, G18) — hypothesis H23 — explorer — lean: supports (confirmed)

### Session summary
- Gathered evidence for H23 (README still references the removed standalone cq-config MCP server). 5 numbered evidence items; lean confirmed.
- README section "Configuration — cq.toml and cq-config MCP" (L77 heading + L82-85 prose) is the last live stale consumer — names a `cq-config` MCP server exposing `get_reviewers`, which no longer exists. `.mcp.json` registers only `ledger`; the Bun workspace has no packages/cq-config-mcp (only the surviving packages/cq-config PARSER lib); commands/cq/reviewers.md (T8-repointed, allowed-tools mcp__ledger__get_reviewers/get_config) is the model form.
- No `mcp__cq-config__*` literal and no stale asset/consumer table row in this README; staleness confined to the L77-85 prose. The @cq/config parser package is NOT stale (survives) and is not separately mentioned in README.
- Exact stale lines needing repoint: L77 (heading), L82-85 (prose). Correct replacement: cq.toml is parsed by @cq/config and surfaced by the LEDGER MCP, which exposes get_reviewers/get_config (and get_planners) as mcp__ledger__* — no standalone cq-config server.
- Limitation: explorer had no Bash/Grep/codegraph; confirmed structural facts (no server package in workspaces, no cq-config in .mcp.json) by direct Read rather than git grep.
