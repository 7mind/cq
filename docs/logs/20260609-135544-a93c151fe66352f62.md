# investigate-explorer — H32 (D45 root cause); role: explorer; lean: supports
Agent a93c151fe66352f62.

### Session summary
Gathered evidence for H32 (cacheMirror omits docs/ledgers.yaml on the 'create' op). 4 evidence items, lean supports. Mechanism fully established by static read (no probe):
- cacheMirror.ts:82 `if (op !== "archive") return;` executes BEFORE `mirrorFile(..., layout.registryPath)` at L84 — so the registry is mirrored ONLY on 'archive', never on 'create'/'update'. (docstring L56-64 confirms: registry is an "archive op additionally" item.)
- FsLedgerStore.ts createLedger: writeRegistry() (L756) then fireMutation(name, "create") (L759); fireMutation→scheduleMirror forwards op='create' verbatim into mirrorMutation (L247/258-273) — registryPath IS plumbed in, so the omission is purely the op-gated early return.
- Consequence: after createLedger, the mirror's ledgers.yaml lags the in-repo registry until a later 'archive' op re-mirrors it.

Orchestrator validation: re-opened cacheMirror.ts L74-96 + FsLedgerStore.ts L754-761 — both citations match source verbatim. All 4 evidence items [correct]. H32 CONFIRMED.
