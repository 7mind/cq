# Session log — T398 implement-worker (sonnet) — D58 fix + surfaced D59

- **Task:** T398 (M169, G48; fixes D58); worker a2e936c5 status **pass**; resultCommit 7247888 (cherry-picked onto main e79d6d1); check 1688/0.

### Worker
Fixed D58 (cqCli cp-into-existing-dir nesting): flake.nix:294 `mkdir -p "$WORKSPACE/packages/cq-cli" $out/bin` → `mkdir -p "$WORKSPACE/packages" $out/bin` (1-line). The pre-created dest made `cp -r packages/cq-cli "$WORKSPACE/packages/cq-cli"` nest the package (`cq-cli/cq-cli/src/main.ts`); with only the parent pre-created the cp copies-as-dest correctly. Verified: `nix build .#cq` green; `result/share/cq/packages/cq-cli/src/main.ts` exists; `.../cq-cli/cq-cli` does NOT exist; the "Module not found" error is gone. check 1688/0.

### Surfaced a SECOND, distinct defect → filed D59
Worker observed that `cq mcp` stdio now reaches the server ("serving stdio MCP") but exits with 0 tools, while the direct ledger-mcp bin serves 26. Root-caused by the orchestrator (reproduced on the rebuilt .#cq): the cq dispatcher's `main()` calls `process.exit(0)` right after the mode delegate (ledger-mcp.main) resolves post-`server.connect()`, tearing the stdio channel down before any request — the standalone bin deliberately does NOT exit (comment at ledger-mcp main.ts:717 "exiting here would close stdin"). This is a SEPARATE root cause from D58 (dispatcher lifecycle, not cp staging) → filed as **D59** with fix task **T399**.

### Scope adjudication
T398's scope is **D58** — fixed and verified (build green + correct staging path + Module-not-found gone). Acceptance criterion 3 ("cq mcp → 26 tools") conflated the two defects; the 26-tools launch verification is reassigned to T399/D59. T398 → done; D58 → resolved.
