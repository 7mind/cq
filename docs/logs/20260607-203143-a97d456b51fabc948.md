# Implement-flow session log — worker T221

- **Task:** T221 — Read-only go/no-go spike on Pi's ExtensionAPI child-session primitive
- **Milestone:** M87
- **Role:** implement-worker (frontier→opus)
- **Returned status:** pass — verdict GO
- **Branch/commit:** worktree-agent-a97d456b51fabc948 / f052445ac296450fe6cadce7e891201c86b0a92d
- **Agent id:** a97d456b51fabc948

## Session summary
- **Did:** read-only go/no-go spike on Pi 0.78.0's ExtensionAPI child-session primitive; wrote the decision to docs/drafts/20260607-2022-T221-pi-childsession-spike.md (docs-only, no production code).
- **Achieved:** PASS. resultCommit f052445. **Verdict = GO** — all five primitives present in Pi 0.78.0:
  (1) registerTool — dist/core/extensions/types.d.ts:816 (exercised in examples/extensions/subagent/index.ts:455, structured-output.ts:64);
  (2) child-session spawn — Route A subprocess `pi -p` (subagent/index.ts:329) OR Route B in-process createAgentSession (dist/core/sdk.d.ts:108, re-exported dist/index.d.ts:16);
  (3) tool-filtering — `--tools` flag (subagent/index.ts:290) or SDK tools/excludeTools (sdk.d.ts:44-45);
  (4) model/provider pin — `--model` (subagent/index.ts:289) or SDK model (sdk.d.ts:21);
  (5) output capture — JSON message_end parse (subagent/index.ts:345-365) + getFinalOutput (:164), or in-process AgentSession.prompt() (agent-session.d.ts:326) + .messages (:289).
  The shipped upstream subagent example uses Route A (subprocess), NOT a method on the ExtensionAPI object (the `pi` factory arg has no spawnChildSession). The two existing pi-extensions only prove registerProvider+pi.on; this spike closes the registerTool/child-session gap.
- **Discovered:** GO does not reopen Q125/Q130. One design choice for T224: Route A (subprocess, proven, recommended) vs Route B (in-process SDK, lighter but coupled to internal SDK shape).
- **Issues:** worktree on the auto-named branch worktree-agent-a97d456b51fabc948 (harness artifact), not implement/T221 — reconciled at merge-back via cherry-pick of f052445.
