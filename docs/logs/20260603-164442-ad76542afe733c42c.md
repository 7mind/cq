# implement-worker — T143 (G11, cross-ledger snapshot helper)
- status: pass; resultCommit d232ec9; branch implement/T143; check green 733/0
- Added LedgerStore.snapshot() → { [ledger]: { [status]: { count, items:{id,status,summary}[] } } } over active ledgers; pure buildSnapshot(FetchedLedger[]); both stores delegate via enumerate()+fetch(); archived excluded; no long fields; dual-tested + 64KB size guard (300 items/6 ledgers). Extracted a canonical summarize()/fieldToString() into @cq/ledger (headline??title??question??summary via SUMMARY_SOURCE_FIELDS) instead of reimplementing; frontends left untouched (out of scope, future dedup). filesTouched: summarize.ts, snapshot.ts, index.ts, store/LedgerStore.ts, store/FsLedgerStore.ts, store/InMemoryLedgerStore.ts, test/snapshot.test.ts, test/store-abstract.ts.
- MERGE NOTE: worktree base was 5306944 (NOT a9b8603) → T141 reopen/unarchive ABSENT in this worktree; merge onto current main will conflict on the store trio (additive around the `// --- mutations` anchor) + index.ts (T137/T142 exports). No semantic conflict — keep both sides.
