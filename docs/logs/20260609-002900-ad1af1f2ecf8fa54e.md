# Implement worker log — T290 (worker: opus[claude], worktree)

- Task T290 (M117/G36). Worktree branch worktree-agent-ad1af1f2ecf8fa54e, commit ee04cca (cherry-picked to main 8a52419). base c03b851 (reset within worktree only — D43 git-safety honored). status: pass. bun run check 1272/0.

### Session summary
- reviewerTokensEqual now compares effort alongside harness/model/provider, normalizing undefined vs null to one equivalence class via `(a.effort ?? null) === (b.effort ?? null)`. classifyToken + the parseTiers D42 dup-guard both delegate to it → no change there (confirmed + tested). Exported reviewerTokensEqual (was private) so the Q162 identity assertions can call it directly. parseReviewerToken/formatReviewerToken untouched.
- Tests: high!=low, same-high==, effortless==, undefined==null; parseTiers accepts effort-differing [tiers] keys but throws D42 on the same effortful token (tested via alias-key+direct-key, since literal-identical TOML keys are collapsed by the parser); classifyToken distinct classes per effort.
- filesTouched: cq-config/src/{config,index}.ts + test/config.test.ts.
