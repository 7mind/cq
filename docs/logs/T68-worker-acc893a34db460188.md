# implement-worker — T68 (M14) TUI title — PASS

Agent acc893a34db460188. resultCommit d99891f, branch implement/T68. check 566 pass / 0 fail.

Derived `dn = client.displayName()` + `headerTitle = dn ? '[${dn}] LLM ledgers' : 'ledger-tui'`; replaced static 'ledger-tui' Text with {headerTitle}. displayName() synchronous (captured at connect per T66). ink test: FakeClient('cq1') → frame contains '[cq1] LLM ledgers'. Files: app.tsx, test/app.test.tsx.
