```json
{
  "taskId": "T2823",
  "role": "implement-worker",
  "baseCommit": "df7959c9a6c8b1b6aa61f4058cea2756d5c860fa",
  "startedAt": "2026-08-27T07:52:30Z",
  "checkpoints": [
    { "name": "legacy recovery reproduction", "status": "done" },
    { "name": "recovery identity migration", "status": "done" },
    { "name": "inherited WIP release reproduction", "status": "done" },
    { "name": "terminal release scoping", "status": "done" },
    { "name": "round 1 preservation correction", "status": "done" },
    { "name": "round 2 integration inheritance reproduction", "status": "done" },
    { "name": "round 2 authenticated release ownership", "status": "done" },
    { "name": "round 3 task-owned WIP reproduction", "status": "done" },
    { "name": "round 3 candidate inspection correction", "status": "todo" },
    { "name": "targeted checks", "status": "done" },
    { "name": "mutation evidence", "status": "done" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## legacy recovery reproduction <!-- cq:wip-checkpoint -->

Observed expected failure: the serialized pre-scheme generation-17 journal is rejected with
`journal-conflict` when production compares its lifecycle-sensitive digest to the current scheme
while a valid generation-18 successor is ready for promotion.

## recovery identity migration <!-- cq:wip-checkpoint -->

Observed expected failure: a clean terminal worktree based on an integration commit containing an
unchanged open `WIP-T9999.md` is refused instead of released.

## inherited WIP release reproduction <!-- cq:wip-checkpoint -->

New seeds persist `finalized-task-membership-v1`. Pre-scheme committed journals are validated
against the current finalized task specification and manifest, exact manager binding, selected
terminal source, receipt closure, snapshot, and live tip before a compare-and-swap migration.
Stable replay and promotion both migrate atomically; exact replay is idempotent.

## terminal release scoping <!-- cq:wip-checkpoint -->

Terminal release and supervised WIP closure now derive candidate artifacts from the stored task
starting commit through the immutable result tip, plus untracked root WIP files. Unchanged tracked
integration artifacts are ignored; task-owned open, foreign, malformed, dirty, and divergent state
remain preservation failures.

## round 1 preservation correction <!-- cq:wip-checkpoint -->

Reproduced both residual preservation failures. Root WIP candidate discovery now retains a root
artifact name when Git reports a descendant path, so a committed directory named like a WIP
artifact is parsed and refused as malformed. The guarded-merge regression now asserts the intended
foreign-artifact refusal for task-introduced T2234/T2235 artifacts. Gate-closure digests were
refreshed only after the behavioral suites passed.

## round 2 integration inheritance reproduction <!-- cq:wip-checkpoint -->

Observed expected failure: after prepare, integration retained `WIP-T2234.md`; the task incorporated
that authenticated integration commit and produced a clean terminal tip, but release refused it as
`wip-malformed` because candidate discovery compared `headAtPrepare..resultCommit`.

## round 2 authenticated release ownership <!-- cq:wip-checkpoint -->

Runner-owned gate projection now records the repository integration tip only when it is an ancestor
of the exact result. Pre-merge closure and terminal release compare WIP candidates from that sealed
tip; legacy projections retain the fail-closed prepare-head baseline. The new regression passes.

## round 3 task-owned WIP reproduction <!-- cq:wip-checkpoint -->

Observed expected failures in 2 focused tests: pre-merge closure resolved instead of rejecting the
open `implementation` checkpoint, and terminal release returned `released` instead of `wip-open`
when the unchanged task-owned artifact was absent from the integration-tip diff.

## round 3 candidate inspection correction <!-- cq:wip-checkpoint -->

Pending: include the current task artifact in candidate inspection while preserving diff-derived
introduced or modified foreign and malformed artifacts.

## targeted checks <!-- cq:wip-checkpoint -->

`bun run typecheck` and `bun run lint` passed after correction. Seven focused recovery, journal,
supervised-gate, managed-worktree, CLI merge, and production XDG terminal-release files passed in
round 1: 111 tests, 0 failures, 1041 assertions. The gate-closure suite passed separately: 38
tests, 0 failures, 353 assertions; all 359 declared opaque-edge digests match their source bytes.
Round 2 reran the complete supervised-gate and managed-worktree files: 57 tests, 0 failures, 649
assertions. Typecheck and lint passed. The supervised-gate, CLI merge, gate-closure, and repository
closure files then passed together: 76 tests, 0 failures, 806 assertions.

## mutation evidence <!-- cq:wip-checkpoint -->

- Fail-first legacy regression: pre-scheme promotion rejected with `journal-conflict`; restored
  implementation passes migration, promotion, generation-19 allocation, and replay.
- Fail-first release regression: unchanged inherited WIP caused `refused`; restored implementation
  releases the clean terminal tree.
- Bypassed finalized task-specification comparison: competing-authority control resolved instead of
  rejecting; restored hash
  `b7fb1a3c378f480f86d646cb980b0b67f82f51d950baaff1a2645e9aca0e7bb0` and control passed.
- Forced the journal migration predicate false: valid migration failed for both memory and
  filesystem adapters; restored hash
  `e52da699a7d60dbdb5bbfb02bde8941a1618b93cdf6432678fcdeb498f77be56` and both passed.
- Removed terminal-release candidate scoping: unchanged inherited WIP was refused; restored hash
  `67842a0ff427d4ca5f8777d0f0c0de12cf1a325a327a71c8c1e362faa6cbfb0a` and release passed.
- Dropped changed tracked WIP candidates: modified foreign WIP was released; restored the same
  managed-worktree hash and foreign/malformed preservation passed.
- Removed descendant-to-root WIP candidate projection: a committed `WIP-T9999.md` directory was
  released by both transports; restored managed-worktree hash
  `88ee0fc8f15adf5520848681932cd12040a75535bb289334ac10de7b05ec7a16` and both refused it as
  `wip-malformed`.
- Replaced the guarded-merge foreign-artifact assertion with the former open-checkpoint assertion:
  it failed against the observed foreign T2234/T2235 refusal; restored test hash
  `7512360d27f97d9018993d526501d384835633c74340044cac5fbe7b78696957` and the regression passed.
- Replaced terminal release's authenticated integration baseline with `headAtPrepare`: the new
  post-prepare integration regression failed with the observed foreign `WIP-T2234.md` refusal;
  restored managed-worktree hash
  `0323ee3476f2884199d2a61fc8d3e2794078532d45008836ebbb9734bce2a585` and it passed.
- Disabled the integration/result ancestry rejection: the divergent-integration control resolved
  instead of rejecting; restored the same managed-worktree hash and the control passed.

## trusted full gate <!-- cq:wip-checkpoint -->

Unmeasured; delegated to trusted result storage after final commit verification.
