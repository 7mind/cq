```json
{
  "taskId": "T2823",
  "role": "implement-worker",
  "baseCommit": "df7959c9a6c8b1b6aa61f4058cea2756d5c860fa",
  "startedAt": "2026-08-27T07:52:30Z",
  "checkpoints": [
    { "name": "legacy recovery reproduction", "status": "done" },
    { "name": "recovery identity migration", "status": "done" },
    { "name": "inherited WIP release reproduction", "status": "done" },
    { "name": "terminal release scoping", "status": "done" },
    { "name": "targeted checks", "status": "unmeasured" },
    { "name": "mutation evidence", "status": "unmeasured" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## legacy recovery reproduction <!-- cq:wip-checkpoint -->

Observed expected failure: the serialized pre-scheme generation-17 journal is rejected with
`journal-conflict` when production compares its lifecycle-sensitive digest to the current scheme
while a valid generation-18 successor is ready for promotion.

## recovery identity migration <!-- cq:wip-checkpoint -->

Observed expected failure: a clean terminal worktree based on an integration commit containing an
unchanged open `WIP-T9999.md` is refused instead of released.

## inherited WIP release reproduction <!-- cq:wip-checkpoint -->

New seeds persist `finalized-task-membership-v1`. Pre-scheme committed journals are validated
against the current finalized task specification and manifest, exact manager binding, selected
terminal source, receipt closure, snapshot, and live tip before a compare-and-swap migration.
Stable replay and promotion both migrate atomically; exact replay is idempotent.

## terminal release scoping <!-- cq:wip-checkpoint -->

Terminal release and supervised WIP closure now derive candidate artifacts from the stored task
starting commit through the immutable result tip, plus untracked root WIP files. Unchanged tracked
integration artifacts are ignored; task-owned open, foreign, malformed, dirty, and divergent state
remain preservation failures.

## targeted checks <!-- cq:wip-checkpoint -->

Unmeasured.

## mutation evidence <!-- cq:wip-checkpoint -->

Unmeasured.

## trusted full gate <!-- cq:wip-checkpoint -->

Unmeasured; delegated to trusted result storage after final commit verification.
