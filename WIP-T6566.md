```json
{
  "taskId": "T6566",
  "role": "implement-worker",
  "baseCommit": "e4c168b44ba32169376ce91da7263fa993879c22",
  "startedAt": "2026-09-16T11:20:54.518Z",
  "checkpoints": [
    { "name": "reproduce H354", "status": "done" },
    { "name": "implement cohesive launch contract", "status": "done" },
    { "name": "focused checks", "status": "done" },
    { "name": "reproduce delayed durable acquisition", "status": "done" },
    { "name": "correct durable admission deadline", "status": "done" },
    { "name": "correction focused checks", "status": "done" },
    { "name": "Q409 parent recovery", "status": "done" },
    { "name": "guarded rebase", "status": "done" },
    { "name": "restore inherited-output Node target", "status": "done" },
    { "name": "post-rebase focused checks", "status": "done" },
    { "name": "refresh gate-closure digests", "status": "done" },
    { "name": "round-4 focused closure check", "status": "done" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## reproduce H354 <!-- cq:wip-checkpoint -->

Added the plain real-process H354 regression in
`packages/process-control/test/worksetGitEffectGate.test.ts`. Before production
edits, the focused selector failed because the effect resolved successfully
after the delayed share and returned `null` instead of a deadline error; the
Git target therefore launched outside the requested boundary.

## implement cohesive launch contract <!-- cq:wip-checkpoint -->

The Git-effect gate establishes one absolute deadline; the registered launcher
and bootstrap share it through admission, registration, durable merge-started
preparation, guardian sharing, release, and acknowledgement. Phase failures
include the authenticated bootstrap exit outcome. Retained merge-started
records use a read-only replay path, and repository-head CAS is rechecked before
guardian sharing.

## focused checks <!-- cq:wip-checkpoint -->

`bun test packages/process-control/test/registeredLaunch.test.ts
packages/process-control/test/worksetGitEffectGate.test.ts
packages/ledger/test/implementation-evidence.test.ts
packages/cq-cli/test/gateRun.test.ts && bun run typecheck` passed with 53 tests,
0 failures, 206 assertions, and a clean TypeScript build.

## trusted full gate <!-- cq:wip-checkpoint -->

Reserved for parent-supervised finalization after guarded continuation.

## reproduce delayed durable acquisition <!-- cq:wip-checkpoint -->

Parent replay of exact candidate `620ec3f6e80b888bb6a5c4e3602b7c0c497a44f4`
exited 1 during durable admission acquisition. A delayed first evidence
snapshot reproduced the same phase before the correction: a 120 ms journal
authentication exhausted a 50 ms launch deadline before the target could run.
Paired actual-admission controls prove one launch under budget and zero target
launches, unchanged HEAD, and zero active admissions over deadline.

## correct durable admission deadline <!-- cq:wip-checkpoint -->

The provider factory now authenticates the complete merge journal before the
process-launch deadline starts. It retains the authenticated completion and
HEAD, then keeps actual admission acquisition, registration, prepared-journal
mutation, guardian sharing, release, and acknowledgement under the one absolute
deadline. Exact retained `merge-started` replay skips the redundant snapshot
append; prepared and already-at-result cases retain the bounded durable write.
New pre/post preparation HEAD CAS controls preserve ref identity. The prior
whole-file formatting rewrite was removed, and the exact-environment regression
again uses a real live stdin FIFO with argv/cwd/stdout/stderr verification.

Mutation checks inverted the prevalidation HEAD equality guard and the retained
no-rewrite branch. The focused controls failed respectively with the wrong
phase (`repository HEAD changed before merge launch`) and an extra evidence
snapshot (3 instead of 2), then passed after exact-byte restoration. Source and
test hashes restored to `ee780dc57407ec7abc4f10e3b02e290afef7d4bd9d6bca5c5886eb2c6bce6959`
and `ab6c59d92a3fb8f4447703b30cafedc3365be7ac79f710c166c57d7542a291d7`.

## correction focused checks <!-- cq:wip-checkpoint -->

Targeted correction selectors pass: 12 implementation-evidence cases, both
short/over-deadline acquisition cases, the slow journal diagnostic case, and
the restored exact-environment/live-pipe case. The exact required command
passed with 57 tests, 0 failures, 223 assertions, and a clean `tsc -b` build.
The parent owns the sole final full gate.

## Q409 parent recovery <!-- cq:wip-checkpoint -->

Parent replay retained T6518's existing completion reference and merge
operation id, advanced the protected record R6518 exactly once to approved
result `e4c168b44ba32169376ce91da7263fa993879c22`, and observed the canonical
acknowledgement. Public record-completion now reports the retained completion as
recorded/existing.

## guarded rebase <!-- cq:wip-checkpoint -->

The server-guarded continuation rebased this task onto protected head
`e4c168b44ba32169376ce91da7263fa993879c22`; the current rebased task tip is
`f52fce9b84a8898b77e20781f2e20664e74739fe`.

## restore inherited-output Node target <!-- cq:wip-checkpoint -->

The inherited-output test again launches the original `"node"` target. In this
prepared environment the inherited asynchronous writes reproducibly yielded
empty output (isolated and full-file 17/18), contrary to the parent A/B note.
Writing the same fixture payloads synchronously to inherited descriptors 1 and
2 removes that scheduling dependency while preserving the external Node target
and exact stdout/stderr coverage; the full file then passed 18/18.

Mutation replaced `writer-stdout` with `mutant-writer-stdout`; the isolated
control failed on the exact stdout assertion and passed after restoration.
The intended file hash was unchanged before and after mutation:
`611d6a3749d85d96b2319f8fafff720232bc2c2b2e31f684fd33a2a53c5d99d4`.

## post-rebase focused checks <!-- cq:wip-checkpoint -->

The exact required command passed on the rebased correction with 57 tests, 0
failures, 223 assertions, and a clean `tsc -b` build:

`bun test packages/process-control/test/registeredLaunch.test.ts packages/process-control/test/worksetGitEffectGate.test.ts packages/ledger/test/implementation-evidence.test.ts packages/cq-cli/test/gateRun.test.ts && bun run typecheck`

The parent owns the sole trusted full gate for the exact final commit.

## refresh gate-closure digests <!-- cq:wip-checkpoint -->

Round 4 starts from failed-gate candidate
`14e2277740d3d06650fdd57a97d2f9ebae96e821`. Parent closure verification found
that `registeredLaunch.test.ts` now hashes to
`611d6a3749d85d96b2319f8fafff720232bc2c2b2e31f684fd33a2a53c5d99d4`, while
`cq-gate-closure.json` still declares its prior digest. Refresh only stale
declarations attributable to the T6566 candidate; preserve closure edges,
guards, and the restored Node fixture.

The pre-change closure test failed 0/1 with `source-digest-mismatch` for that
exact file. A comparison limited to T6566-changed sources found seven stale
digests across 11 existing declarations. After those were refreshed, the
resolver exposed the new `path` and `spawn` capabilities in
`implementation-evidence.test.ts`; two explicit hashed declarations now bind
those capabilities without an exemption or target change. The closure check
then passed 1/1.

Mutation changed the new implementation-evidence `path` digest from
`ab6c59d92a3fb8f4447703b30cafedc3365be7ac79f710c166c57d7542a291d7` to
the deliberately wrong `bb6c...` value. The closure test failed with the exact
source digest mismatch, then passed after restoration. The manifest SHA-256
before mutation and after restoration was
`7459f71f21e63e1f959e658f45a02ed6a23d0860df77719fc9858e469807ef26`.

## round-4 focused closure check <!-- cq:wip-checkpoint -->

The parent observed all three timeout selectors pass 5/5 with the pinned
Nixpkgs Bun 1.3.13 and reproduce three failures with Bun 1.4.2. The trusted
runner uses the matching packaged 1.3.13 artifact, so the runtime-specific
1.4.2 failures are not a basis for another production correction. The focused
repository-closure check passed 1/1. On Bun 1.3.13, the exact changed-candidate
command passed with 57 tests, 0 failures, 223 assertions, and a clean `tsc -b`
build:

`bun test packages/process-control/test/registeredLaunch.test.ts packages/process-control/test/worksetGitEffectGate.test.ts packages/ledger/test/implementation-evidence.test.ts packages/cq-cli/test/gateRun.test.ts && bun run typecheck`
