```json
{
  "taskId": "T6518",
  "role": "implement-worker",
  "baseCommit": "8bdc0a95efe17e69ac7e621f925574115fca3f66",
  "startedAt": "2026-09-15T16:16:11Z",
  "checkpoints": [
    { "name": "stale-base reproduction", "status": "done" },
    {
      "name": "missing qualification persistence reproduction",
      "status": "done"
    },
    { "name": "queue and qualification contract", "status": "done" },
    { "name": "staged rebase authority contract", "status": "done" },
    { "name": "adapter implementations", "status": "done" },
    { "name": "targeted acceptance checks", "status": "done" },
    { "name": "round 1 gate rejection reproduction", "status": "done" },
    { "name": "round 1 correction", "status": "done" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## stale-base reproduction <!-- cq:wip-checkpoint -->

Observed before implementation with
`bun test packages/cq-config/test/dispatchStagedCompletionQualification.test.ts`:
the regression expected the unqualified gate claim to reject, but the promise
resolved and advanced the row to `gate-running`.

## missing qualification persistence reproduction <!-- cq:wip-checkpoint -->

The same pre-change run showed the durable gate-pending row carried staged
output bytes and their digest but no `stagedCompletionQualification` binding.
The run executed 2 tests; both failed at their intended behavioral assertions.

## queue and qualification contract <!-- cq:wip-checkpoint -->

Queue partition/enrollment/attempt records,
qualification, FIFO acquisition, lease-generation transitions, restart
recovery, and exact queued-gate admission are implemented. The shared
Behavioral-Active Blackbox contract passes against both the strict in-memory
dummy and SQLite for reopen, one-winner acquisition, lease fencing, and minimal
tombstone projection. The two reproduction tests pass (2 tests, 0 failures).

Mutation evidence:

- Disabling queued-gate admission made `stale-base gate-pending conflict is
refused before the attempt is qualified` fail because the claim resolved;
  restoring `dispatchAttestation.ts` to SHA-256
  `4eb64dedb049d8e281d29a0c8ce714e497567c106e8a7bf5ad5460677467211b`
  restored its pass.
- Omitting the top-level qualification persistence made `staged-completion
qualification is persisted before any gate can start` fail at the missing
  field; restoring `dispatchImplementationQueue.ts` to SHA-256
  `4e6290f55d168a65cd27f0a1f77dfb3c92512ed5c51ca8e4189768b9a1c00966`
  restored its pass.

## staged rebase authority contract <!-- cq:wip-checkpoint -->

An exact, clean, leased queue front can be retired into one server-bound
staged-rebase source. Retirement is replay-safe for identical bytes, altered
retirement is refused, the old parent-gate path is revoked, the source
survives terminal compaction, and only a successor carrying the finalized
guarded-rebase bridge can reuse the stable enrollment and ordinal.

Mutation evidence: removing the `ontoCommit` equality from the terminal replay
guard made `exact retirement is replay-safe, revokes old paths, and survives
tombstone projection` fail because the altered retirement resolved. Restoring
`dispatchImplementationQueue.ts` to SHA-256
`f06599c435d2576e127a3461c1ab840ebe2d920ccfdc7b49a0d50a11608c86f8`
restored both staged-rebase tests (2 tests, 0 failures).

## adapter implementations <!-- cq:wip-checkpoint -->

The ledger-MCP adapter composes queue enrollment with native-completion
qualification only after the attestation service accepts the staged result.
It exposes the parent-owned acquire, park, yield, resume, recovery, release,
cancellation, and staged-rebase retirement transitions over the configured
durable backend. Queue scans retain revision, enrollment, ordinal, and
staged-rebase successor authority across terminal tombstone compaction.

The Behavioral-Active Blackbox adapter tests pass for strict per-partition
FIFO, independent repository partitions, duplicate enrollment refusal,
lifecycle transitions, pre-storage refusal, and SQLite peer/restart races (5
tests, 0 failures). TypeScript project references also pass.

Mutation evidence:

- Reversing admission-ordinal comparison made `FIFO is strict inside one
partition while independent partitions lease concurrently` select the
  second attestation and fail.
- Allowing a second holder to overwrite a leased front made `peer processes
elect one lease winner and fence one stale recovery after reopen` observe
  two `leased` outcomes instead of one `blocked` and one `leased`.
- Restoring both guards produced SHA-256
  `ac8d625ac1e3e16611597a09d02e5fb4705e723b9505d28a6f0158627d116c98`
  for `dispatchImplementationQueue.ts` before mechanical formatting, after
  which all 7 queue/rebase tests and typecheck passed.

## targeted acceptance checks <!-- cq:wip-checkpoint -->

The seven-file acceptance selector passed 118 tests with 0 failures and 817
assertions. Changed-file ESLint, Prettier, and the full TypeScript project
build pass.

`bun run test:attestation-postgres-required` was invoked, but this confined
worker cannot create either TCP or Unix-domain sockets; PostgreSQL exited
before any test with `Operation not permitted`. The same PostgreSQL test files
compile and their offline portion passes (17 passed, 106 explicitly skipped).
The live PostgreSQL queue case is the same shared Blackbox contract that passed
against in-memory and SQLite; executing it remains an environment evidence
gap, not a green claim.

## round 1 gate rejection reproduction <!-- cq:wip-checkpoint -->

At commit `7ecae099694061b1fbb3187746ae4dc2cc7b1a24`, the focused
`dispatchAttestation.test.ts` run failed because `staged-rebase` had leaked into
the ordinary abort-reason enumeration while `abortDispatch` correctly refused
it. The focused `dispatchAttestationBackend.test.ts` run failed because
`DispatchStagedRebaseSourceError` escaped `isAttestationDomainError`. The
trusted-gate report also identified the resulting staged-rebase transport
observations; the local process-backed router suite could not be used as
evidence because this confined worker cannot bind its fixture socket.

## round 1 correction <!-- cq:wip-checkpoint -->

The compact protocol now separates ordinary abort requests from terminal abort
observations. `staged-rebase` is accepted only by the fetch-result observation
schema, while ordinary transport and `abort_dispatch` inputs remain closed over
their original reasons. Generic aborted-result typing preserves that distinction
through store, confirm, parent-gate, and transport call sites. Queue and
staged-rebase errors now inherit a dispatch-extension error recognized by the
PostgreSQL domain-error classifier; the exported-error coverage guard includes
`Implementation*Error` classes.

Mutation evidence:

- Removing `staged-rebase` from the terminal observation reasons made
  `server-only staged-rebase is observable but not an ordinary transport abort`
  fail at the fetch-result schema assertion. Restoring
  `compactDispatchProtocol.ts` to SHA-256
  `b79f14c5d6330364bb89e2755adb042b7623f1e84f562407d6ff588027e1bd7e`
  restored the pass.
- Detaching `DispatchStagedRebaseSourceError` from the extension error base made
  `EVERY error class this package exports is recognised by the predicate` fail
  with `DispatchStagedRebaseSourceError escapes isAttestationDomainError`.
- Extending the exported-error guard to include `Implementation*Error`, then
  detaching `ImplementationQueueConflictError`, made the same test fail with
  `ImplementationQueueConflictError escapes isAttestationDomainError`.
  Restoring `dispatchImplementationQueue.ts` to SHA-256
  `e23b2b61b043ae4cf493a51bb8ae1b8973a2b0c6e4f0d75123d2f1da088bd83a`
  restored both classifier checks.

The seven-file acceptance selector plus the affected attestation, backend, and
compact-protocol suites pass 281 tests with 0 failures and 2,078 assertions.
Changed-file ESLint and Prettier checks pass, as does `bun run typecheck`.

## trusted full gate <!-- cq:wip-checkpoint -->

Delegated to trusted result storage after the task tip is clean and verified.
