```json
{
  "taskId": "T6518",
  "role": "implement-worker",
  "baseCommit": "8bdc0a95efe17e69ac7e621f925574115fca3f66",
  "startedAt": "2026-09-15T18:37:39Z",
  "checkpoints": [
    { "name": "review criticism", "status": "done" },
    { "name": "cancelled authority reproduction", "status": "done" },
    { "name": "partition revision reproduction", "status": "done" },
    { "name": "qualification replay reproduction", "status": "done" },
    { "name": "successor allocation reproduction", "status": "done" },
    { "name": "required-live coverage reproduction", "status": "done" },
    { "name": "corrections", "status": "done" },
    { "name": "targeted acceptance", "status": "done" },
    { "name": "round 4 residual validation", "status": "done" },
    { "name": "round 4 targeted acceptance", "status": "done" },
    { "name": "round 5 same-attestation reproduction", "status": "done" },
    { "name": "round 5 terminal-prior correction", "status": "done" },
    { "name": "round 5 mutation evidence", "status": "done" },
    { "name": "round 5 targeted acceptance", "status": "done" },
    { "name": "round 6 guarded-rebase control reproduction", "status": "done" },
    { "name": "round 6 domain correction", "status": "done" },
    { "name": "round 6 mutation evidence", "status": "done" },
    { "name": "round 6 targeted acceptance", "status": "done" },
    { "name": "round 7 terminal queue bridge reproduction", "status": "done" },
    { "name": "round 7 queue authority correction", "status": "done" },
    { "name": "round 7 mutation evidence", "status": "done" },
    { "name": "round 7 targeted acceptance", "status": "done" },
    { "name": "round 8 terminal immutability reproduction", "status": "done" },
    { "name": "round 8 abort provenance reproduction", "status": "done" },
    { "name": "round 8 corrections", "status": "done" },
    { "name": "round 8 mutation evidence", "status": "done" },
    { "name": "round 8 targeted acceptance", "status": "done" },
    { "name": "round 9 reason-domain reproduction", "status": "done" },
    { "name": "round 9 reason-domain correction", "status": "done" },
    { "name": "round 9 mutation evidence", "status": "done" },
    { "name": "round 9 targeted acceptance", "status": "unmeasured" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## review criticism <!-- cq:wip-checkpoint -->

Five protected findings bind this correction: cancelled authority resurrection,
non-monotonic partition revisions, non-idempotent qualification replay after gate
start, successor allocation before staged-rebase source claim, and missing
required-live PostgreSQL retirement/successor coverage.

## cancelled authority reproduction <!-- cq:wip-checkpoint -->

Observed the same-authority candidate qualify after the original enrollment was
terminalized as cancelled.

## partition revision reproduction <!-- cq:wip-checkpoint -->

Observed a stale expected partition revision remain accepted after cancellation
of an older row.

## qualification replay reproduction <!-- cq:wip-checkpoint -->

Observed exact replay fail in `enqueueImplementationCandidate` with the dispatch
already `gate-running`.

## successor allocation reproduction <!-- cq:wip-checkpoint -->

Observed prepare accept a successor without the retired source's finalized
guarded-rebase binding; the shared adapter contract also observed that valid
prepare did not claim the source.

## required-live coverage reproduction <!-- cq:wip-checkpoint -->

Added retirement and successor allocation to `runAttestationStoreContract`,
which is selected by the required-live PostgreSQL command. The unchanged
implementation failed the same contract against the in-memory adapter.

Focused pre-change run: 55 passed, 5 failed. Each protected finding failed for
its expected behavioral reason.

## corrections <!-- cq:wip-checkpoint -->

Terminal non-rebase enrollment reuse is refused. Exact qualification/enqueue
replays are checked before the live gate state restriction. Abort, confirmation,
and staged-rebase claims allocate partition-wide revisions. A guarded successor
now validates and claims the retired source during the namespace-scoped prepare
transaction; enqueue only verifies that prepare-time claim. The shared adapter
contract covers retirement, reopen, successor enqueue, stable enrollment, and
second-successor refusal.

Focused post-correction run: 60 passed, 0 failed. The added confirmation guard
also failed under a deliberate stale-row-revision mutation and passed after the
partition-wide calculation was restored.

## targeted acceptance <!-- cq:wip-checkpoint -->

`bun run typecheck` and `bun run lint` passed. The exact seven-file acceptance
selector passed 125 tests with 0 failures and 832 assertions. The required-live
PostgreSQL command was attempted, but PostgreSQL could not create its configured
IPv4 listener because the sandbox denies socket creation (`Operation not
permitted`); no PostgreSQL case executed or was claimed as passing. The shared
contract selected by that command now includes retirement, successor allocation,
reopen, stable enrollment, and second-successor refusal.

## round 4 residual validation <!-- cq:wip-checkpoint -->

The protected tip retained all five prior regressions. The seven-file acceptance
selector passed 125 tests with 0 failures and 832 assertions, including cancelled
authority refusal, partition-wide revision fencing, post-gate qualification
replay, prepare-time staged-rebase source claiming, and shared adapter retirement
and successor allocation.

Current-run mutations removed the parent-gate qualification guard and the durable
staged-completion qualification field. The two same-titled regressions then failed
for their intended reasons (0 passed, 2 failed): the unqualified gate claim
resolved, and the stored row lacked the qualification. After restoration they
passed (2 passed, 0 failed). `dispatchAttestation.ts` restored to SHA-256
`07db86fb76640eb97150a427419489c8168e2cc09a12ae31c22e740e1557d8d0` and
`dispatchImplementationQueue.ts` restored to SHA-256
`35b1a58138d093a1bf6e7c78b77453e6a56147debe622aa83257521f898efd2a`.

## round 4 targeted acceptance <!-- cq:wip-checkpoint -->

The exact acceptance selector passed. The required-live PostgreSQL selector was
attempted, but this sandbox denied PostgreSQL's IPv4 listener with `Operation not
permitted`; no live database case executed or was claimed as passing. Trusted
result storage remains responsible for the canonical full gate on the final clean
tip.

## round 5 same-attestation reproduction <!-- cq:wip-checkpoint -->

The same-titled plain regression staged, qualified, and cancelled generation 1,
then prepared generation 2 through `reprepareOf` with the same attestation and
managed binding. Against the unchanged predicate, qualification incorrectly
resolved instead of rejecting with `ImplementationQueueConflictError` (0 passed,
1 failed).

## round 5 terminal-prior correction <!-- cq:wip-checkpoint -->

`terminalPrior` now excludes only the exact current `(attestationId,
generation)` handle. Earlier generations sharing the attestation ID remain
visible as terminal enrollment authority; staged-rebase retirement continues to
be admitted by its existing state exception.

## round 5 mutation evidence <!-- cq:wip-checkpoint -->

With the intended source and regression hashes recorded, the production
predicate was deliberately restored to its attestation-ID-only form. The exact
regression failed for the expected reason (qualification resolved; 0 passed,
1 failed). Restoring the intended generation-aware predicate made it pass
(1 passed, 0 failed). Restored SHA-256 values:
`dispatchImplementationQueue.ts`
`18caf550293f0bb2dd91372ea1f1b8cabd052f728c698934842151cb2a941a1b`;
`implementation-candidate-queue.test.ts`
`e8afe900a9da361c78b3abd4cbaf0ef7d7ed9c120818155908b589e964b93531`.

## round 5 targeted acceptance <!-- cq:wip-checkpoint -->

The exact seven-file acceptance selector passed 126 tests with 0 failures and
834 assertions. `bun run typecheck` and `bun run lint` also passed. Per the
round-5 operator note, the required-live PostgreSQL selector and canonical full
gate are delegated to trusted parent result storage on the final clean tip.

## round 6 guarded-rebase control reproduction <!-- cq:wip-checkpoint -->

The four named controls failed against the dispatched starting commit (0 passed,
4 failed). Three Git broker controls raised `AttestationBindingError` because
ordinary guarded-rebase prepares were made to require retired staged-rebase
source authority. The direct lineage control raised `AttestationNotFoundError`
because the exact journal-authorized source generation had already been deleted.

## round 6 domain correction <!-- cq:wip-checkpoint -->

`claimStagedRebaseSuccessor` now enters the queue-retirement extension only for
a retained source row that actually carries staged-rebase source authority.
Ordinary journal-authorized guarded rebases and exact journal recovery after
source-row deletion bypass that extension, while retained queue sources keep the
existing exact binding and single-successor checks. The four protected controls
then passed (4 passed, 0 failed).

## round 6 mutation evidence <!-- cq:wip-checkpoint -->

The intended source file hashed to SHA-256
`6dd29c8b68bbbd201c00e3a56bcb41dc1c2bd2f1bd0cb20983b83ce5ed7b960a`.
Deliberately restoring the unconditional `requireRow` plus guarded-bridge
rejection made the same four protected controls fail for the expected reasons
(0 passed, 4 failed). Restoring the queue-authority boundary made them pass
(4 passed, 0 failed), and the source returned to the same SHA-256.

## round 6 targeted acceptance <!-- cq:wip-checkpoint -->

The deferred closure passed in this run: the four ordinary guarded-rebase
controls passed, and the complete Git broker file passed 33 tests with 0
failures.

## round 7 terminal queue bridge reproduction <!-- cq:wip-checkpoint -->

The two same-titled plain regressions staged and qualified a queue enrollment,
terminalized it as either cancelled or parent-lost, and supplied a well-formed
ordinary guarded-rebase bridge. Against the starting implementation both
successor prepares resolved instead of rejecting (0 passed, 2 failed).

## round 7 queue authority correction <!-- cq:wip-checkpoint -->

`claimStagedRebaseSuccessor` now distinguishes a retained queue enrollment from
an ordinary non-queue dispatch even when no staged-rebase source binding exists.
A guarded bridge on that retained, non-retired enrollment fails closed; ordinary
non-queue bridges and source-row-deletion recovery retain their existing bypass.

## round 7 mutation evidence <!-- cq:wip-checkpoint -->

The intended source and regression hashed to SHA-256
`99a29272a3102008823d527b16a65af5232c106d3b26c0258112f53fa845ba67`
and `8cdcc7c2a2efa8bf94c2d8bb41c07c6f6f4a3171f6199733eb39739469cff4cc`.
Removing the retained-queue guard made both regressions fail because successor
prepare resolved (0 passed, 2 failed). Restoring the intended bytes returned the
same hashes and made both regressions pass (2 passed, 0 failed).

## round 7 targeted acceptance <!-- cq:wip-checkpoint -->

The two affected files passed 14 tests; the exact seven-file acceptance selector
passed 128 tests with 0 failures and 836 assertions; the complete Git broker
file passed 33 tests; the T2045 transport selector passed 1 test; and `bun run
typecheck` plus `bun run lint` passed. The packaged test file passed its static
gate (1 passed, 3 installed-only tests skipped). A direct `nix build --no-link
.#cq` could not reach the Nix daemon socket inside this sandbox (`Operation not
permitted`), so no installed-only packaged result is claimed here; the trusted
parent full gate owns that final packaged execution.

## round 8 terminal immutability reproduction <!-- cq:wip-checkpoint -->

Against the unchanged implementation, five same-titled direct-terminal reason
regressions failed because an exact retry was rejected as a stale partition
revision. Separate same-titled regressions observed `released` and
`staged-rebase-retired` controls accept a second terminal transition. The
focused pre-change run reached 64 passes and 10 expected failures overall.

## round 8 abort provenance reproduction <!-- cq:wip-checkpoint -->

Three shared-adapter contract regressions persisted queue-backed `parent-lost`,
`gate-rejected`, and `operational-abstention` aborts, reopened the backend, and
observed `mismatched-completion` in all three terminal provenance records.

## round 8 corrections <!-- cq:wip-checkpoint -->

Direct terminalization now recognizes an exact already-terminal outcome before
partition-revision fencing, returns the immutable abort result without a write,
and rejects altered requests plus `released` and `staged-rebase-retired`
controls. Queue provenance written by the general abort transition now retains
the exact dispatch abort reason. The focused post-correction run passed 74 tests
with 0 failures and 444 assertions.

## round 8 mutation evidence <!-- cq:wip-checkpoint -->

The terminal-state branch was deliberately disabled and the abort-provenance
projection was restored to the old `mismatched-completion` collapse. The same
focused set returned 64 passes and the same 10 expected failures: five stale
exact replays, two unauthorized non-active transitions, and three inaccurate
persisted reasons. Restoring both intended guards returned 74 passes and 0
failures. Restored SHA-256 values: `dispatchImplementationQueue.ts`
`4c77ae5d24e25879d07f9ca8d3d949987c57cc54f01256b3081ea3971d3bb837`;
`dispatchAttestation.ts`
`1de39a4cfe55b963ad15ea8ffded6edf8229e88d31f5420a4edd214985c29e03`.
Typecheck subsequently required the replay result to omit an absent optional
details field; that representation-only correction left the guarded behavior
unchanged and produced final `dispatchImplementationQueue.ts` SHA-256
`68e9b36c8748a543bcae6fc0311d06cea1f63c904344d6108d2e3eeac705bdaa`.

## round 8 targeted acceptance <!-- cq:wip-checkpoint -->

The exact seven-file acceptance selector passed 141 tests with 0 failures and
877 assertions on the final source bytes. `bun run typecheck` and `bun run
lint` passed. Per the round-8 operator note, the required-live PostgreSQL cases
and canonical full gate are delegated to trusted parent result storage.

## round 9 reason-domain reproduction <!-- cq:wip-checkpoint -->

Against the unchanged direct terminalization command, the three same-titled
plain regressions for `parent-lost`, `gate-rejected`, and
`operational-abstention` all failed because the promises resolved (0 passed, 3
failed). `bun run typecheck` separately reported three unused
`@ts-expect-error` directives, proving the public adapter request type admitted
all three protected reasons.

## round 9 reason-domain correction <!-- cq:wip-checkpoint -->

Stored queue terminal provenance retains the full dispatch-abort reason domain,
while the direct command again accepts only `cancelled`, `native-failure`,
`foreign-completion`, `mismatched-completion`, and `superseded`. A runtime
membership check rejects untyped or cast protected reasons before reading or
mutating the row, and an exhaustive mapping replaces the previous catch-all
`protocol-violation` conversion. The three focused regressions passed, and
`bun run typecheck` passed with the negative type assertions active.

## round 9 mutation evidence <!-- cq:wip-checkpoint -->

The intended source and regression hashes were recorded, then the request type
was deliberately widened and its runtime membership assertion removed. The
three focused regressions failed with `DispatchRefAssemblyError` instead of the
required `AttestationContractError` (0 passed, 3 failed), while typecheck again
reported all three negative assertions unused. Restoring both guards made the
runtime set pass (3 passed, 0 failed) and typecheck pass. Restored SHA-256
values: `dispatchImplementationQueue.ts`
`08c5759517822a3678b3111991862fb9eb56d5a3cb0afd791c1f45e1ec96cced`;
`implementation-candidate-queue.test.ts`
`befa9d89dbdbf674b56f1eaa30bf1c61c02d2b9d3847d281d567f96e821ab648`.

## round 9 targeted acceptance <!-- cq:wip-checkpoint -->

Unmeasured.

## trusted full gate <!-- cq:wip-checkpoint -->

Unmeasured; delegated to trusted result storage after final verification.
