```json
{
  "taskId": "T1698",
  "role": "implement-worker",
  "baseCommit": "ee0eef386ed253e44248080ec680b58488531ad7",
  "startedAt": "2026-08-07T04:22:43.000Z",
  "checkpoints": [
    { "name": "skeleton", "status": "done" },
    { "name": "k170-residual-correction", "status": "todo" },
    { "name": "claude-native-qualification", "status": "todo" },
    { "name": "pi-native-isolation", "status": "todo" },
    { "name": "tests-mutation", "status": "todo" },
    { "name": "full-gate", "status": "unmeasured" }
  ]
}
```

## skeleton <!-- cq:wip-checkpoint -->

Base verified at ee0eef38 on branch implement/T1698.
Worktree: `/home/pavel/work/safe/flakes/cq/.claude/worktrees/implement-T1698`.
Scope: T1698 (D263 Claude-native worktree qualification) + T1699 (D160 Pi-native isolation).

## k170-residual-correction <!-- cq:wip-checkpoint -->

Remove claims that K170 accepted prompt-best-effort WRITE/worktree confinement.
K170 is harness-axis (native vs shellout) + handleOnlyOutput residual only.

## claude-native-qualification <!-- cq:wip-checkpoint -->

Positive-only `claude:native` registration: structural path-scoped confinement
required. Wire worktree_manage prepare path/handle, base/HEAD preflight,
resume-by-handle, release-only-via-manager. Escape canaries + fail-closed
path/handle/base mutation tests.

## pi-native-isolation <!-- cq:wip-checkpoint -->

Same-harness `forceShellout=false` uses `createAgentSession({cwd})` with
manager-returned path. Forced/cross-harness uses registered process seam.
Close D160 when structural isolation holds; else typed incompatibility.

## tests-mutation <!-- cq:wip-checkpoint -->

Mutation evidence for new guards; claudeDispatchProtocol +
dispatchTransportRouter + crossSurface green.

## full-gate <!-- cq:wip-checkpoint -->

`cq gate run --worktree $PWD --command-cwd $PWD/nix/pkg/cq-ledgers -- bun run check`
