```json
{
  "taskId": "T2815",
  "role": "implement-worker",
  "baseCommit": "79b0b69592ab4b50f290c824a4a03b3af3159f1c",
  "startedAt": "2026-08-25T00:46:14Z",
  "checkpoints": [
    { "name": "reproduction", "status": "done" },
    { "name": "implementation", "status": "done" },
    { "name": "targeted-checks", "status": "done" },
    { "name": "mutation-evidence", "status": "done" },
    { "name": "round-1-reproduction", "status": "done" },
    { "name": "round-1-corrections", "status": "done" },
    { "name": "round-1-verification", "status": "done" },
    { "name": "round-2-reproduction", "status": "done" },
    { "name": "round-2-corrections", "status": "done" },
    { "name": "round-2-mutation-evidence", "status": "done" },
    { "name": "round-2-verification", "status": "done" },
    { "name": "round-3-reproduction", "status": "done" },
    { "name": "round-3-correction", "status": "todo" },
    { "name": "round-3-verification", "status": "unmeasured" }
  ]
}
```

## reproduction <!-- cq:wip-checkpoint -->

Before implementation, the mandated six-path `bun test` command failed because all six
recovery-seal test modules were absent. This established the missing recovery surface before
adding it.

## implementation <!-- cq:wip-checkpoint -->

Added the protected current-recovery seal and journal contract, strict maximal source
selection, namespace snapshot/retry capture, managed binding and finalized-manifest checks,
`cq dispatch-recovery seal|status`, and the lineage-cutover-fence semantic evidence guard.
The D360 fixture finalizes the corrected manifest, starts `capture-current`, completes it with
a full result commit identity, and retains `defects:D360`.

## targeted-checks <!-- cq:wip-checkpoint -->

At 2026-08-25T01:25:21Z the exact six-path acceptance command passed 28 tests with 75
expectations and zero failures. Composite TypeScript builds for ledger, ledger-mcp, and cq-cli
passed; targeted ESLint and `git diff --check` passed. The trusted parent gate remains delegated
by protocol.

## mutation-evidence <!-- cq:wip-checkpoint -->

Deliberate mutations each produced the expected nonzero targeted-test result and were restored:
generation tie ordering, incomparable closure refusal, provisional authority denial, seal
self-authentication, status digest authentication, active-lineage refusal, snapshot-change
retry, lineage-wide maximum generation, finalized-manifest membership, operator-action
semantic evidence, and canonical CLI task-id parsing. Pre/post restoration hashes matched for
each mutation batch. Final source hashes are
`ac09105001107d5afedbd250b8f2400a39a8700c91339296e7e25be783e34340` (seal),
`34418e0842f98de8f80d093a03c45f29b80ed40b9839c4eecacdc2473cf2a7cd`
(capture), `0e8793d2e28107885725a38f5408bf2c7ec77bdde2df8e5e3d3af18ed460a10e`
(operator evidence), and `fbec1f77b8b7d38e43291ceb6ff08866dfd33e84f88416efd14f71d5e8ea7b9a`
(CLI).

## round-1-reproduction <!-- cq:wip-checkpoint -->

The fail-first targeted run exited 1 with four expected assertion failures: a generation
inserted after the final reread left generation 2 selected instead of 10; a selected source's
non-empty overlay list became `[]`; unchanged-lineage replay changed `capturedAt` and the seal
digest; and locale collation selected the lowercase identifier instead of the ordinal uppercase
identifier. Separately, `git cat-file -t aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa` exited 128,
proving the D360 fixture's prior result identity was not a Git object.

## round-1-corrections <!-- cq:wip-checkpoint -->

Implemented an awaited namespace transaction nested inside the managed-worktree effect lock,
a final post-reselection snapshot check, replay of byte-identical committed authority,
canonical overlay retention from prepare through terminal envelope and recovery seed, and
locale-independent ordinal attestation ordering. The D360 fixture now creates a real descendant
commit and requires manager-authenticated release before recording task completion. The focused
recovery run passed 17 tests; the in-memory attestation adapter contract passed 46 tests, the
dispatch-attestation service suite passed 99 tests, and the composite TypeScript build passed.
Strict overlay-application schemas now also reject malformed, duplicate, noncanonical, or
extra-member applications during attestation rehydration and seed construction.

## round-1-verification <!-- cq:wip-checkpoint -->

Deliberate mutation batches all failed for the expected reason and the intended bytes were
restored before their passing reruns:

- disabling final reread, overlay propagation, and committed replay produced four recovery
  failures; restored source hash `615ac32f5f555c301bc6301e276281da2fde07f4c3f5d1dba878e616f4b1578e`;
- restoring locale collation and a generic overlay-value seed schema produced the ordinal-id
  and strict-overlay failures; restored source hash
  `b383119e2c3111287aa4fb50f9313ee81c9a305525009948827d75e56d48d834`;
- applying the async transaction journal before its body settled and weakening stored overlay
  validation produced the expected retained-row and malformed-overlay failures; restored source
  hash `eccb431b0545ef36a7cb70566af0d00aa6f93de0353b4b237cbc93ce61481f81`;
- dropping prepared/terminal overlay retention produced `[]` instead of the two canonical
  applications; restored source hash
  `284aedec4595702f9d58d5a2b135787d1e8d886ef24201892ac3e9a4564e94ed`;
- substituting the old fabricated D360 SHA made `git cat-file -t` exit 128; restored fixture hash
  `0e74a6eba5c0d3ec4ae06913865364d8ecec98dd62c14b07078cbe98dd1e8ad7`.

All corresponding restored focused reruns passed. The exact six-file acceptance command passed
33 tests with 86 expectations and zero failures. Composite TypeScript, focused ESLint, Prettier,
and `git diff --check` passed. The awaited-transaction contract also passed against the in-memory,
filesystem, and SQLite adapters; PostgreSQL was explicitly skipped because no live fixture was
configured. The trusted parent gate remains delegated by protocol.

## round-2-reproduction <!-- cq:wip-checkpoint -->

The focused fail-first run exited 1 with four expected failures: legacy persisted bytes without
`overlays` were rejected before capture, status remained committed after a prepared successor,
the serialized seed contained the exact managed-worktree handle token, and substituting the
selected source generation still passed operator-action semantic verification. The existing
D360 test stayed green while directly committing, releasing, and updating the task; that
established the criticism as a coverage defect requiring replacement of the shortcut fixture.

## round-2-corrections <!-- cq:wip-checkpoint -->

Legacy stored envelopes may omit `overlays`, but their exact pre-upgrade body is digested and
checked before the rehydrated row is normalized to an empty application list. Recovery seeds
now retain closed non-secret Git coordinates and the manager fingerprint without the registry
handle token. The committed status embeds its self-authenticating seal, binds every projected
status field to that seal, and rereads the current lineage under the managed-worktree and
namespace locks; a later active successor or any snapshot drift refuses authority. Production
CLI status opens the configured attestation backend for this check. The D360 fixture now uses a
prepared worker dispatch, materializes its input, commits through the durable Git broker,
validates and stores the broker-bound result, confirms native completion, fetches the consumed
result, and only then records task completion and releases the managed tree.

## round-2-mutation-evidence <!-- cq:wip-checkpoint -->

Seven temporary mutations produced their expected nonzero focused results and were restored:

- omitting legacy overlay normalization yielded `undefined` instead of `[]`, while bypassing the
  original-row digest comparison made the wrong-digest negative vector stop throwing; restored
  backend hash `e61e048699716e09ca858726bb25bd0c236776e2181096687858c2ee7baa692a`;
- skipping the committed-status active-generation check changed the expected `lineage-active`
  refusal into a later snapshot error; restored capture hash before formatting
  `23626a155d3ac1ad8c08fc687f5fadd1f06ab9321b2dc2ab375fa56112279f6b`;
- restoring `handleToken` to the recovery schema/projection exposed the exact
  `managed-handle-token` in serialized authority; removing the selected-generation comparison
  admitted a substituted generation; and skipping embedded-seal verification admitted a mutated
  prompt version. All three restored the seal source hash
  `d15e7e543cda44bcd762a2389857f2a6c6490a941fbca4e2b41638078eb41715`;
- routing CLI status back to journal-only status failed the production-wiring guard; restored CLI
  source hash `8f80482188ddbb1930a143d1b4547c25e2e4d8904c64de68da07aa526ff9f5ed`;
- removing the D360 result-fetch lifecycle observation failed the exact authenticated sequence;
  restored fixture hash `4583cf327e24d27ea19656f75bef6b412e5be27979c1d95afc72d72ab56b7018`.

The capture source was subsequently formatted without semantic changes; its final hash is
`cae05f5c65f99653a3acac2e4e50bf1128c263f7753479ab5cdd62cc6eb4dd06`.

## round-2-verification <!-- cq:wip-checkpoint -->

The exact six-file acceptance command passed 36 tests with 105 expectations and zero failures.
The attestation-backend serialization suite passed 50 tests with 304 expectations. The composite
TypeScript build, focused ESLint, focused Prettier check, and `git diff --check` all passed. The
trusted parent gate remains delegated by protocol.

## round-3-reproduction <!-- cq:wip-checkpoint -->

Added a SQLite-backed regression that rewrites an aborted row's actual durable body and
`row_digest` to the pre-overlays legacy shape, advances to the 24-hour retention boundary, and
then drives the public sweep operation. The fail-first run executed one test and exited 1 with
`AttestationStorageError: lost update ... 0 rows affected` from the SQLite CAS update, proving
that normalized `overlays: []` had replaced the durable legacy revision digest.

## round-3-correction <!-- cq:wip-checkpoint -->

Pending reproduction. Preserve the durable stored row digest across legacy overlay
rehydration, or atomically migrate the stored body and digest under the backend lock.

## round-3-verification <!-- cq:wip-checkpoint -->

Unmeasured. Deliberately mutate the new guard, restore the intended bytes, rerun the focused
test, the exact six-file acceptance command, and the relevant static checks.
