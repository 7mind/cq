```json
{
  "taskId": "T2052",
  "role": "implement-worker",
  "baseCommit": "ccc396d681b6df2b382ed2689b62e9bb176dd3db",
  "startedAt": "2026-08-11T04:40:02.361Z",
  "checkpoints": [
    { "name": "reproduction", "status": "done" },
    { "name": "implementation", "status": "done" },
    { "name": "focused-checks", "status": "todo" },
    { "name": "generation", "status": "done" },
    { "name": "full-gate", "status": "unmeasured" }
  ]
}
```

## reproduction <!-- cq:wip-checkpoint -->

Reproduce the missing generated/public adoption surface before implementation.

Observed exit 1 from the focused three-file test command. Both native binders
omitted `adoptWorktreePath` and `expectedHead` from their prepare request, and
the rendered implement/advance prompt omitted the pre-registry adoption path.

## implementation <!-- cq:wip-checkpoint -->

Expose prepare-only path-and-HEAD adoption while keeping lifecycle authority internal.

Claude and Pi binders now forward paired coordinates and refuse incomplete or
handle-mixed adoption before invoking the port. Public tool prose and canonical
surface sources expose only paired path-and-HEAD input; production authority
remains server-constructed.

## focused-checks <!-- cq:wip-checkpoint -->

Run focused MCP, core, stdio, T1698, T1699, and configuration parity checks.

The first post-implementation focused run observed 33 passes across the public
MCP schema/direct-stdio lifecycle and both native binder suites.

## generation <!-- cq:wip-checkpoint -->

Generate all surfaces twice and verify byte-identical output.

Both generations produced prompt catalog SHA-256
`76391c37932457668b6ac61eb9e45cb051bc066b6f116c2a6eff65c4ef330cd4`
and agents catalogue SHA-256
`66b04fd5fdcfe65a464c9733577c896b9a95bae73ed1cfea1786810177f08c02`.
The generated-authority and catalogue tests observed 49 passes.

## full-gate <!-- cq:wip-checkpoint -->

Await an explicit full-gate grant before running the mandated command.
