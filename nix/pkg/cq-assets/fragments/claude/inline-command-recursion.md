> **Inline CQ recursion (Claude).** When this workflow says to run a `CQ::<path>`
> command INLINE, invoke the mapped slash command in this session and complete it
> before resuming this workflow. Do not ask the user to invoke it.
