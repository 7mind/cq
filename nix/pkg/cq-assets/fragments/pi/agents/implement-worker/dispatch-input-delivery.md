### Dispatch input delivery (Pi — held protocol)

The Pi orchestrator passes the complete typed worker input directly in the
dispatch prompt. Use that input as the task specification described below. Do
not attempt one-shot capability retrieval: the Pi prompt/extension migration
remains held for the coordinated D186/D190 cutover.
