> **Subagent dispatch (Pi).** `CQ_SUBAGENT` means
> `dispatch_agent(agent: "<role>", task: "<complete prompt>")`. The
> cq-subagent-dispatch extension runs the role as an isolated child turn.
> Never simulate the delegated role inline.
