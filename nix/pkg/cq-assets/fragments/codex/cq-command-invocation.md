> **CQ command notation (Codex).** `CQ::<path>` names the generated skill
> `$cq-<path>`, with each `/` in `<path>` written as `-`. Preserve any following
> arguments and treat `$ARGUMENTS` as the current skill's user-supplied text.
