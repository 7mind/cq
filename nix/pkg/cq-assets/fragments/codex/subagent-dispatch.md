> **Subagent dispatch (Codex).** `CQ_SUBAGENT` means the collaboration
> `spawn_agent` transport. Pass the complete task prompt and use the isolated
> worktree path required by the workflow. Never simulate the delegated role
> inline.
