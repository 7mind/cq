> **Inline CQ recursion (Codex).** When this workflow says to run a `CQ::<path>`
> command INLINE, read the mapped `$cq-<path>` skill completely and execute it in
> this session before resuming this workflow. Do not invoke a nested skill through
> the UI or ask the user to invoke it.
