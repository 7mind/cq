[test]
# Exclude compiled JS output from test discovery; tests run from TypeScript source.
pathIgnorePatterns = ["**/dist/**"]
# D170: mirror the root gate's throwaway XDG_STATE_HOME preload so package-local
# runs are isolated from the developer's real out-of-tree ledger store.
preload = ["../../test-setup/isolate-state-dir.ts"]
