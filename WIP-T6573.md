```json
{
  "taskId": "T6573",
  "role": "implement-worker",
  "baseCommit": "3d3b263d10c5083cae87ee7b085dfcc9ec22d0d7",
  "startedAt": "2026-09-19T22:41:23Z",
  "checkpoints": [
    { "name": "public subprocess reproduction", "status": "done" },
    { "name": "managed recovery implementation", "status": "done" },
    { "name": "coordinator handoff implementation", "status": "done" },
    { "name": "workflow projections", "status": "done" },
    { "name": "focused checks", "status": "done" },
    { "name": "D512 scope transfer", "status": "done" },
    { "name": "D513 authenticated-lineage reproduction", "status": "done" },
    { "name": "supplemental fused correction", "status": "done" },
    { "name": "round 4 evidence audit", "status": "done" },
    { "name": "round 4 focused checks", "status": "done" },
    { "name": "round 5 sealed-source reproduction", "status": "done" },
    { "name": "round 5 exact bridge selection", "status": "done" },
    { "name": "round 5 focused checks", "status": "done" },
    { "name": "round 6 red-successor reproduction", "status": "done" },
    { "name": "round 6 corrective recovery fence", "status": "done" },
    { "name": "round 6 focused checks", "status": "done" },
    { "name": "round 7 own-completion reproduction", "status": "done" },
    { "name": "round 7 coordinator completion correction", "status": "done" },
    { "name": "round 7 queue-order hypothesis", "status": "done" },
    { "name": "round 7 focused checks", "status": "done" },
    { "name": "round 8 resolver admission reproduction", "status": "done" },
    { "name": "round 8 authenticated resolver admission", "status": "done" },
    { "name": "round 8 focused checks", "status": "done" },
    { "name": "round 9 manual-rebase reproduction", "status": "done" },
    { "name": "round 9 authenticated prepare composition", "status": "done" },
    { "name": "round 9 focused checks", "status": "done" },
    { "name": "round 10 case A automatic-continuation reproduction", "status": "done" },
    { "name": "round 10 case B pending-manual-conflict reproduction", "status": "done" },
    { "name": "round 10 authenticated continuation correction", "status": "done" },
    { "name": "round 10 focused checks", "status": "done" },
    { "name": "round 11 diagnostic opacity reproduction", "status": "done" },
    { "name": "round 11 safe rejection diagnostics", "status": "done" },
    { "name": "round 11 focused checks", "status": "done" },
    { "name": "round 12 guarded bridge persistence", "status": "done" },
    { "name": "round 12 composition A reproduction", "status": "done" },
    { "name": "round 12 composition B reproduction", "status": "done" },
    { "name": "round 12 composition C reproduction", "status": "done" },
    { "name": "round 12 fused correction", "status": "done" },
    { "name": "round 12 focused checks", "status": "done" },
    { "name": "round 13 retained-bridge base reproduction", "status": "done" },
    { "name": "round 13 retained-bridge correction", "status": "done" },
    { "name": "round 13 focused checks", "status": "done" },
    { "name": "round 14 old-producer correction-claim reproduction", "status": "done" },
    { "name": "round 14 claim retention and restart correction", "status": "done" },
    { "name": "round 14 focused checks", "status": "done" },
    { "name": "round 15 qualification ancestry reproduction", "status": "done" },
    { "name": "round 15 qualification ancestry correction", "status": "done" },
    { "name": "round 15 focused checks", "status": "done" },
    { "name": "round 17 qualification refusal diagnosis", "status": "done" },
    { "name": "round 17 focused checks", "status": "done" },
    { "name": "round 18 R9 queue composition", "status": "done" },
    { "name": "round 18 focused checks", "status": "done" },
    { "name": "round 20 inherited-closure reproduction", "status": "done" },
    { "name": "round 20 R12 repair composition", "status": "done" },
    { "name": "round 20 focused checks", "status": "done" },
    { "name": "round 21 cross-cut reproduction", "status": "done" },
    { "name": "round 21 disposable composition", "status": "done" },
    { "name": "round 21 focused checks", "status": "done" },
    { "name": "round 22 G219 reproduction", "status": "done" },
    { "name": "round 22 permanent correction", "status": "done" },
    { "name": "round 22 focused checks", "status": "done" },
    { "name": "round 22 final composition handoff", "status": "done" },
    { "name": "round 23 bounded public diagnostics", "status": "done" },
    { "name": "round 23 T6560 semantic reproduction", "status": "done" },
    { "name": "round 23 permanent correction", "status": "done" },
    { "name": "round 23 focused checks", "status": "done" },
    { "name": "round 24 D508 reproduction", "status": "done" },
    { "name": "round 24 authenticated closure correction", "status": "done" },
    { "name": "round 24 focused checks", "status": "done" },
    { "name": "round 25 full-history correction reproduction", "status": "done" },
    { "name": "round 25 authenticated current-recovery correction", "status": "done" },
    { "name": "round 25 mutation evidence", "status": "done" },
    { "name": "round 25 focused checks", "status": "done" },
    { "name": "round 26 legacy prepare-digest reproduction", "status": "done" },
    { "name": "round 26 cancelled recovery enrollment reproduction", "status": "done" },
    { "name": "round 26 persisted-format correction", "status": "done" },
    { "name": "round 26 mutation evidence", "status": "done" },
    { "name": "round 26 focused checks", "status": "done" },
    { "name": "round 27 authenticated multi-hop reproduction", "status": "done" },
    { "name": "round 27 correction", "status": "done" },
    { "name": "round 27 mutation evidence", "status": "done" },
    { "name": "round 27 focused checks", "status": "done" },
    { "name": "round 28 checkpoint reconciliation", "status": "done" },
    { "name": "round 28 focused checks", "status": "done" },
    { "name": "round 29 consecutive parent-loss reproduction", "status": "todo" },
    { "name": "round 29 authenticated lineage correction", "status": "todo" },
    { "name": "round 29 focused checks", "status": "todo" },
    { "name": "round 30 inherited reproduction checkpoint", "status": "done" },
    { "name": "round 30 consecutive parent-loss reproduction", "status": "todo" },
    { "name": "round 30 authenticated lineage correction", "status": "todo" },
    { "name": "round 30 focused checks", "status": "todo" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## round 30 inherited reproduction checkpoint <!-- cq:wip-checkpoint -->

Round 30 resumes from `c3a608e7270d95ddb60951d35a21144844d74267` and
preserves the inherited paired in-memory/SQLite generation-3 to generation-29
reproduction in `supervisedWorkerGateStorage.test.ts`. The test models the
operator-reported 25-intermediate history, including unenrolled parent-loss
generations, guarded-rebase retirement, exact-tip recovery, and four retained
lineage substitutions. Its pre-correction result and exact rejected edge remain
unmeasured; no product correction is claimed at this checkpoint.

## round 30 consecutive parent-loss reproduction <!-- cq:wip-checkpoint -->

The inherited paired selector exits 1 with 0 pass / 2 fail and 160 assertions.
Both in-memory and SQLite runs reach generation 28 after the guarded-rebase
bridge, then fail its `parent-lost` abort with `inherited receipt chain is stale
relative to startingCommit`. The failing call resolves the generation-28
binding's inherited pre-rebase receipts against the rebased live tip but omits
the binding's already-authenticated `receiptChainTransition` coordinates. This
is an earlier real boundary than the operator-reported generation-29
qualification refusal; the selector remains open until the latter is reached or
the corrected sequence completes. Passing the persisted transition sequence to
the existing receipt verifier clears that boundary in both backends. The next
run reaches the generation-29 hostile-lineage controls and rejects the first
substitution before gate execution, but exposes a fixture postcondition that
expects three runner requests where the modeled history has produced exactly
two (generation 3's controlled parent loss and generation 8's successful gate).

## round 30 authenticated lineage correction <!-- cq:wip-checkpoint -->

Pending the measured rejected edge. Any correction remains bounded to the
existing task, manager, source, binding, output, terminal, receipt,
prepare-digest, claim, guarded-rebase, and live-tip invariants.

## round 30 focused checks <!-- cq:wip-checkpoint -->

Pending. This dispatch is focused-only; `trusted full gate` remains parent-owned
and unmeasured.

## round 29 consecutive parent-loss reproduction <!-- cq:wip-checkpoint -->

Round 29 resumes from `eeefa833b1d2eb8ff4dcfdc9d2d0f89e7aba7e1c`.
The operator reports that final qualification still refuses the authenticated
source-generation-3 to target-generation-29 history after consecutive
unenrolled `parent-lost` intermediates. Reproduction, the rejected edge, and
all resulting correction evidence remain unmeasured at this checkpoint.

## round 29 authenticated lineage correction <!-- cq:wip-checkpoint -->

Pending the exact paired in-memory/SQLite reproduction. Any correction must
preserve the existing task, manager, source, binding, output, terminal,
receipt, prepare-digest, and live-tip checks.

## round 29 focused checks <!-- cq:wip-checkpoint -->

Pending. This dispatch is focused-only; `trusted full gate` remains parent-owned
and unmeasured.

## round 28 checkpoint reconciliation <!-- cq:wip-checkpoint -->

The stale round-23/24/25 header entries are reconciled against their recorded
bodies, the current branch bytes, and the parent-supplied R22 host evidence.
The round-23 T6560 experiment is closed as an explicit no-change disposition:
its historical measurement remains unconfirmed and is not represented as a
pass. The round-24 and round-25 check entries now distinguish their original
measurements from the later exact-current-tip host matrix. The trusted full
gate remains parent-owned and unmeasured.

## round 28 focused checks <!-- cq:wip-checkpoint -->

Completed on the current committed reconciliation bytes:

- `bun test packages/cq-config/test/wipArtifact.test.ts` exits 0 with 18 pass,
  0 fail, and 40 assertions.
- `bun run typecheck` exits 0 (`tsc -b`).
- `bun run lint` exits 0 (`eslint .`).

Before this status transition, parsing the actual artifact and applying the
task-local trusted-gate projection reported exactly one open checkpoint:
`round 28 focused checks`. After the transition, the same multiline-aware
parser reports 85 checkpoints, only `trusted full gate` in the raw open list,
and a clean task-local trusted-gate projection. The trusted parent owns the
canonical full gate.

## round 27 authenticated multi-hop reproduction <!-- cq:wip-checkpoint -->

The paired public in-memory/SQLite regression builds an enrolled parent-loss
source, cancelled current recovery, completed terminal generation 3, then 23
successive authenticated cancellation/consumed-failure rows before the passing
generation-27 recovery. Before the product correction, both final
qualifications exited 1 at `already-terminal`; the diagnostic reported source
generation 1, target generation 27, 25 intermediates, and one admitted first
hop. The runner remained at the two earlier attempts, so no final gate ran.

## round 27 correction <!-- cq:wip-checkpoint -->

The queue ancestry walk now admits both missing edges without treating a
failure as success. An exact current-recovery claim may carry an authenticated
unenrolled cancellation into a consumed-failure terminal, and that already
validated consumed failure may carry its exact ordinary continuation into a
later cancellation. Existing task, manager, source, terminal, continuation,
prepare-digest, receipt, and live-tip checks remain in force.

Queue enrollment also revalidates the stored dispatch task, branch, and input
digest before ancestry evaluation. Six table-driven substitutions across the
first repeated cancellation, first repeated failure, final cancellation, and
generation-27 target reject with unchanged row counts, no queue allocation,
and the runner still at two attempts. The restored paired selector passes 2 / 0
with 184 assertions and completes exactly one final gate.

## round 27 mutation evidence <!-- cq:wip-checkpoint -->

Four deliberate mutations failed and were restored byte-for-byte:

- Restricting consumed-failure recovery to an enrolled source restored the
  generation-27 `already-terminal` refusal (0 / 2).
- Inverting consumed-failure continuation admission restored the same refusal
  (0 / 2).
- Removing the stored task/input-digest enrollment checks made the hostile
  target-input control qualify instead of reject (0 / 2).
- Changing the final runner-count assertion from 3 to 4 failed with an observed
  count of 3 (0 / 2).

The restored queue source is SHA-256
`aba3fcc3f3d0ec9bde9c1857b72c1496df660b1a5f26e4fbbf7e1395e8ade2ca`;
the restored regression is
`46aa721d41fb77404b5ef66dc86b3d7b4ee6b1b21c2df9ef2c24ed0213e189a0`.

## round 27 focused checks <!-- cq:wip-checkpoint -->

Completed on the restored committed bytes:

- The paired short cancelled-recovery composition and generation-27
  authenticated-history selectors pass 4 / 0 with 230 assertions.
- Candidate qualification and reuse suites pass 10 / 0 with 71 assertions.
- FIFO, exact qualification replay, and lease-transition selectors pass 3 / 0
  with 12 assertions.
- `bun run typecheck` exits 0 (`tsc -b`).
- Targeted ESLint over the changed queue source and regression exits 0.

Per the operator boundary, unchanged host-only subprocess checks covered by the
parent's frozen-tip matrix were not rerun in confinement. This dispatch is
focused-only; the canonical trusted full gate was not invoked and remains
parent-owned and unmeasured.

## round 26 legacy prepare-digest reproduction <!-- cq:wip-checkpoint -->

The supplied process fixture now archives the exact producer tree at
`853eeae05037de6f8c6710c125fa00d9aa028760`, runs it against a fresh SQLite
store, then reopens those bytes through the current runtime. A same-current
producer/reopener control resolves successfully.

Before product edits, the final same-titled success regression exited 1 with
0 pass and 1 fail. It reached current-recovery seal capture and failed for the
expected reason: `retained-source-prepare-digest-mismatch`. The preceding
observation form proved that both persisted attestation rows and rejected
journal bytes remained identical and that no recovery successor was allocated.

## round 26 cancelled recovery enrollment reproduction <!-- cq:wip-checkpoint -->

The isolated in-memory and SQLite regressions qualify a focused generation 1,
cancel its live enrollment, recover generation 2, consume its controlled
`status: fail` result without enrollment, and store a successful ordinary
generation-3 continuation. Before the queue correction, both final
qualifications failed at the expected boundary with `first-hop-rejected` and
`admitted-first-hop-count=0`; the bounded diagnostic records generation 2 as
`state=none` and leaves every runner invocation count at zero.

## round 26 persisted-format correction <!-- cq:wip-checkpoint -->

The prepare-digest correction accepts exactly two formats: the current canonical
payload and the pre-`eec3c325` payload that omits only an absent
`gateRejectedCorrectionClaim`. A present claim can never use the compatibility
arm. The frozen old-producer and same-current SQLite processes now both resolve
through the same authenticated recovery path.

The queue correction authenticates both formerly missing edges. The first
requires the cancelled enrolled source, its exact queue terminal and output,
the current-recovery claim, and a fully authenticated unenrolled consumed-fail
terminal. The second requires that same consumed-fail terminal's opaque
continuation association, exact manager/receipt closure, successor input, and
reconstructed prepare digest. Five source, recovery, and continuation
substitutions fail closed on both backends. The restored selectors pass 2 / 0
with 46 assertions and observe no runner gate.

## round 26 mutation evidence <!-- cq:wip-checkpoint -->

Five deliberate product mutations failed at their intended boundaries and
were restored byte-for-byte:

- Removing the exact pre-correction-claim digest arm changed
  `dispatchAttestation.ts` from
  `d9334862dfdf47665cc0d6dea3e7643b920452e19473229345635fb6da691c7e`
  to `c9a0c2cd5aa24543badadcee3692c64e9659b3d1ef4f4fe652ad4317621e90b8`
  and restored `retained-source-prepare-digest-mismatch` (0 / 1).
- Disabling the enrolled-cancellation to consumed-failure edge changed
  `dispatchImplementationQueue.ts` from
  `49a5141860bde3e74395fabf244cda890e8f9025e96af96ac20e7ae45eeaace0`
  to `a373d1fba4841875dac584c08b8d320ab6a72dbae958f1c25d8d7f44795c461b`
  and restored `first-hop-rejected` (0 / 1, 21 assertions).
- Disabling the consumed-failure to ordinary-continuation edge changed that
  file to `7689e49fe71a479b4ec1a9cf8971d46ab2b302c81d82dd0ec4632fefbfe1a5f5`
  and restored `downstream-rejected` (0 / 1, 21 assertions).
- Bypassing the source queue-terminal guard changed it to
  `e320da403e9bc6986ea768c782e3a373c7c0405580790016877ae519c148de0c`
  and made the altered terminal-details control qualify (0 / 1, 10 assertions).
- Bypassing the consumed-failure terminal-digest guards changed it to
  `6312891c8ad2da40e325f868db911e6cf0e3820d9d57c0d74c8692205833dbb1`
  and made the altered consumed terminal qualify (0 / 1, 16 assertions).

The restored queue and attestation sources match their recorded pre-mutation
hashes. No mutation was committed.

## round 26 focused checks <!-- cq:wip-checkpoint -->

The three affected selectors pass 4 / 0 with 55 assertions: the exact legacy
prepare-digest unit guard, the frozen old-producer/current-reader SQLite
recovery, and the paired memory/SQLite cancelled-recovery composition.
`tsc -b` exits 0, and targeted ESLint over all changed TypeScript files exits
0. This dispatch is focused-only, so the canonical full gate was not invoked.

A broader optional three-file run reached 128 pass / 1 fail. The sole failure
is the pre-existing bounded-diagnostic expectation in
`implementation-candidate-queue.test.ts`: it expects `first-hop-rejected`, but
the implementation reports `downstream-rejected`. Replacing the queue source
with the exact bytes from starting commit
`250a23428882e23148190561c9aafa665eb5545f` reproduced the identical selector
failure and diagnostic, establishing that it is not introduced by round 26.
The starting source was then removed and the committed round-26 source restored
byte-for-byte before the focused checks above.

## round 25 full-history correction reproduction <!-- cq:wip-checkpoint -->

`exerciseCancelledRecoveryContinuation` now traverses the required parent-loss
recovery, staged rebase, consumed guarded successor, ordinary zero-change
continuation, cancellation, current recovery, stored and qualified worker, and
genuine red result before requesting an exact changed correction. The guarded
successor and ordinary continuation remain green; only the post-cancellation
recovered worker receives the controlled red runner result.

Before the product correction, the exact selector ran independent in-memory
and SQLite cases and exited 1 with 0 pass, 2 fail, and 100 completed assertions.
Both reached the correction request and failed for the expected reason: `the
managed task lineage is sealed for journal recovery`. No correction was
allocated and no fifth runner gate ran.

## round 25 authenticated current-recovery correction <!-- cq:wip-checkpoint -->

The recovery-fence walk now authenticates each retained journal-recovery edge
from its exact claim, source terminal, task and finalized-manifest digests,
managed-worktree fingerprint, inherited receipt digest, input digest,
reconstructed prepare request, live tip, and predecessor identity. Only the
current anchor must also match the active fence and seal; historical recovery
claims retain their own authority while the walk authenticates their exact
predecessors. A consumed guarded successor is admitted only through its exact
staged-rebase retirement record.

The correction preserves the journal seed's receipt-chain transition across
ordinary prepare, store, qualification, and queue ancestry. Closure checks
accept the guarded discontinuity only when the exact transition is retained;
ordinary receipt-prefix rules remain unchanged. Negative controls mutate the
recovery claim, task input, finalized manifest, manager binding, reconstructed
prepare digest, receipt closure, guarded bridge, epoch, source handle, and
stale tip. Each is rejected before allocating a successor or invoking another
gate.

After restoration, the paired memory and SQLite selector passes 2 / 0 with 164
assertions. The exact changed correction carries one fresh broker receipt,
qualifies, receives the fifth and final green runner result, and is consumed.
Existing post-guard-only receipt components remain transition-free unless
their retained prefix actually ends at the bridge's old result; the older case
C focused-correction regression therefore remains green.

## round 25 mutation evidence <!-- cq:wip-checkpoint -->

Five deliberate product mutations failed at their intended boundaries and
were restored byte-for-byte:

- Removing journal-recovery predecessor traversal changed
  `dispatchCapability.ts` from
  `52fed70029e0e5aeff8d9c1fa75261f88afa13cbd351d59e76be25f9c34ff476`
  to `472c62ee37173602811e81370d01f4b9f9dc0623342af90116d516235716349c`
  and restored the exact sealed-lineage correction refusal (0 / 1, 78
  assertions).
- Treating every guarded inherited component as pre-bridge changed that file
  to `223aba0e53f18eb5b2d68cba776530f2bc267858356214469a718ef957ab5231`
  and made the older case C correction fail closed (0 / 1, 29 assertions).
- Dropping persisted transition reuse at qualification changed that file to
  `9618c300396ef072758f274add67c8becae50e09c4515013ac67a855f54de080`
  and produced `inherited receipt chain diverges at entry 2` (0 / 1, 79
  assertions).
- Removing transition-aware correction admission changed
  `dispatchAttestation.ts` from and back to
  `8cbae533e38098bfbd654fea61eb9c35de243ce1cc6a143b7d069df211eaebb5`
  (mutated
  `446e1949b2e449d0b19fe183972b4f7ea1374b64fc3fe7758a5800deda547eb3`)
  and raised the exact authenticated-terminal binding error (0 / 1, 78
  assertions).
- Removing transition-aware queue ancestry changed
  `dispatchImplementationQueue.ts` from and back to
  `50deef6de9277914f96cb61f787c794e557636bf475ee37319c2c81af02e7631`
  (mutated
  `364a0c3c2e5a8e4914e4c893ee2e6ccf3038b27b0c8ff47129f38602171e3635`)
  and rejected qualification as already terminal (0 / 1, 79 assertions).

The regression file remained at
`029d786c2924a17faf5729091f9f9f0acb7b0b69d5177e50c3436a28b9a4ce06`
through those product mutations. All product hashes returned to their recorded
pre-mutation values.

## round 25 focused checks <!-- cq:wip-checkpoint -->

The task-specific recovery and older composition selectors pass 6 / 0 with
376 assertions; `tsc -b` and `eslint .` both exit 0. This is a focused-only
dispatch, so the canonical full gate was not invoked.

The mandated six-file command was also run twice. After the semantic
correction it consistently reached 137 pass / 1 skip, with every recovery
selector green, but exited 1 because three pre-existing subprocess-boundary
selectors timed out at their fixed 10s/10s/30s limits; their cleanup then
reported one `ENOENT` for an already-removed temporary bridge directory. The
same three timeouts persisted with `--max-concurrency=1`, and the first also
timed out when run alone. This confined worktree cannot provide the required
recorded-base A/B proof without creating another checkout or changing prepared
dependency topology, so the required broad focused check remains blocked.

Prompt projection generation was attempted but cannot access the Nix daemon
from this sandbox. No prompt, sidecar, or generated projection is changed by
this round.

Closure evidence supplied by the parent for exact current tip `ccd30f544d421ee8689395c5b32029ab8f39dff5`
is recorded separately from those historical attempts: the restored
long-history plus abort selectors passed 3 / 0 with 260 assertions, the related
queue matrix passed 61 / 0 with 268 assertions, the package selection passed
46 / 0 with 531 assertions, and whole-package typecheck and lint exited 0. The
parent also reported an exact-byte audit. These observations are attributed to
the parent references `20260921-R22-generation27-and-abort-green.md` and
`20260921-R22-affected-queue-matrix.md`; they were not independently rerun or
inspected from this confined worktree.

## round 24 D508 reproduction <!-- cq:wip-checkpoint -->

The immutable R16 production source hash for `dispatchCapability.ts` matched
the worktree at
`e87841d3542ae614a3052e2d8cad7c9db6c53cad4c5771e903b76c3f4a04a3e0`.
Two Behavioral-Active Effectual-GoodCommunication regressions use the public
recovery and queue boundaries on both in-memory and SQLite stores. Each
qualifies a parent-lost source, claims its current-recovery successor, advances
the protected head, retires that recovered source into a clean guarded
successor, produces a genuine red gate, and requests one exact changed
correction. One guarded red row has no fresh receipt; the other has one fresh
receipt.

Before product edits, the paired selector exited 1 with 0 pass, 2 fail, and 17
completed assertions. Both variants reached the correction request and failed
for the expected reason: `the managed task lineage is sealed for journal
recovery`. The runner observed the parent-loss attempt and the genuine red gate
only; no correction gate ran.

## round 24 authenticated closure correction <!-- cq:wip-checkpoint -->

The recovery-fence walk now permits a non-consumed guarded row only through its
unique staged-retired predecessor. That edge re-authenticates the staged source,
queue partition/enrollment/attempt/lease, task goal and finalized manifest,
managed repository/worktree binding, receipt-lineage digest, guarded bridge,
and the successor's reconstructed prepare digest before traversing the
predecessor. All other non-consumed rows remain rejected.

Receipt closure is resolved with persisted transition coordinates (including
the guarded bridge vertex when the binding has no explicit singular
transition) instead of assuming that the last inherited receipt head is the
post-rebase starting commit.

The restored paired selector passes 2 / 0 with 126 assertions. Mutation
evidence was captured and then restored byte-for-byte. Moving the non-consumed
rejection back before the staged edge changed `dispatchCapability.ts` to
`bac64465b7b0ca4669999eb6216cd611259e77ec2790b3094a45b10176179625`
and reproduced both correction refusals at `journal-recovery-required` (0 / 2,
55 assertions). Disabling the reconstructed prepare-digest comparison changed
the file to
`7633ba9337f5abd6647d1d951f10c089cc0d015234d27c0919b7de1d29629751`
and caused both hostile prepare-digest controls to allocate unauthorized
successors (0 / 2, 39 assertions). The restored production and regression
hashes are respectively
`196908cc26322039fb8ae4f94b8b09017a8a6241af542a85dbd020114f8c2685`
and `2bfdfff16440bc9e8d1682c0a139161658fd9ce3d7972865ecd179a67175c32b`.

## round 24 focused checks <!-- cq:wip-checkpoint -->

The historical round-24 selector was not rerun on the old round-24 bytes.
Later local round-25 evidence above passed the task-specific recovery and older
composition selectors 6 / 0 with 376 assertions and exited 0 for typecheck and
lint. The parent-supplied R22 current-tip matrix recorded 3 / 0 restored
long-history/abort selectors, 61 / 0 related queue tests, 46 / 0 package tests,
and exit 0 for whole-package typecheck and lint, with an exact-byte audit. This
closes the stale checkpoint without converting the unrun historical command
into a pass. The canonical full gate remains parent-owned.

## round 23 bounded public diagnostics <!-- cq:wip-checkpoint -->

The immutable R16 recovery-seal and guarded-rebase products were first imported
without modification. The branch was missing seven supporting T6576 products
for transition-aware receipt validation, stored-row decoding, transaction-bound
conflict receipts, and their public tests; only those seven immutable files
were then imported. The newer T6573 queue diagnostic regression was preserved.

Before changing recovery behavior, the public manual-successor negative
controls were strengthened to require bounded reason codes. The test failed as
expected with the generic `journal recovery cancellation does not authenticate
its exact guarded successor` message after 11 assertions. The two generic
blocks now distinguish missing evidence, source/successor identity, terminal,
successor or retained claim, input, prepare digest, managed binding, and
retained seed authority/source/task/live-tip failures. Diagnostics expose only
the bounded cause and whether the claim is retained or on the successor; they
do not expose handles, digests, receipts, capabilities, or paths.

The restored selector passes on memory and SQLite with 102 assertions. Its
negative controls exercise source and successor prepare digests, terminal and
managed binding, retained claim task/receipt identity, and retained seed
task evidence without changing journal or row counts.

## round 23 T6560 semantic reproduction <!-- cq:wip-checkpoint -->

The exact immutable R16 bytes initially reproduced a separate branch
composition omission on the second manual bridge: `inherited receipt chain
diverges at entry 3`. Importing the omitted immutable transition-aware broker
and storage products corrected that local refusal; the same public selector
then passed. Extending every zero/one/two-fresh-receipt case through two manual
guarded bridges and two journal promotions also passes with 102 assertions and
zero gates.

Those public fixtures discriminate older-producer claim persistence, manual
successor request digest, inherited bridge, fresh source/successor receipt
components, and repeated journal-seed promotion. None reproduces the retained
T6560 refusal. Per the round-23 operator boundary, no further semantic predicate
is changed until the parent obtains the new bounded reason from the exact
public retained recovery operation.

Round-28 disposition: the retained T6560 H395 case remains unconfirmed. The
checkpoint is closed without a product change because its prerequisite was not
observed and the operator explicitly excluded guess-fixing that separate
lineage. This is not a successful reproduction or a pass claim.

## round 23 permanent correction <!-- cq:wip-checkpoint -->

No round-23 product correction was authorized without the exact retained public
reason. All permanent round-22 work and finished T6576 products were preserved;
no compatibility exemption or private-state edit was made. Later T6573 rounds
addressed independently reproduced public failures without relying on the
unconfirmed T6560 predicate.

## round 23 focused checks <!-- cq:wip-checkpoint -->

The original round-23 check request was not run as one historical command.
Its public recovery coverage was subsequently exercised by the green local
round-25/26/27 selectors recorded above. For the exact current tip, the parent
supplied the R22 results recorded under round 25: 3 / 0 long-history/abort,
61 / 0 related queue, 46 / 0 package, and exit 0 for whole-package typecheck
and lint, with an exact-byte audit. This closes the stale bookkeeping entry
without claiming a pass for an unexecuted historical command.

## round 22 G219 reproduction <!-- cq:wip-checkpoint -->

The prepared worktree is verified at starting/prior result commit
`9fee4de7485cb35ba886e5bf1318d12b69ab2f3d`, on `implement/T6573`, with
`9272de7b3cd5cf8ebe41abb42c514b8421cb3f9b` as an ancestor. This round owns
the G219 queue, lower binding, dispatch-attestation, and failed-result-context
gaps identified by the parent.

The pre-product-edit probe used the recorded R21 disposable composition at
`/tmp/t6573-r21-compose.Ofb0jP`; its four product/test hashes matched the
round-21 manifest before the probe was added. The public lifecycle qualified a
parent-lost source, recovered and completed its changed worker, completed the
guarded successor, cancelled an exact zero-change ordinary continuation,
recaptured current recovery, then prepared and stored the next zero-change
worker. Native qualification failed at the intended boundary with 0 pass / 1
fail / 28 assertions:

`qualification-refusal:v1 reason=downstream-rejected ... source-generation=1
target-generation=6 intermediate-count=4 bound-intermediate-count=4
admitted-first-hop-count=2`.

The retained rows showed the missing authenticated composition exactly:
generation 3 was the staged-rebase source, generation 4 the consumed guarded
successor, generation 5 its cancelled ordinary continuation, and generation 6
the current-recovered zero-change worker. Preparation and result storage had
already succeeded; no recovery gate ran. The failure was therefore queue
ancestry admission, not recovery recapture, Git evidence, or storage.

## round 22 permanent correction <!-- cq:wip-checkpoint -->

Completed in T6573-owned paths while preserving T6576's adjacent manual
receipt-seal and guarded-materialization ownership. The queue authenticates a
consumed guarded source followed by its exact cancelled ordinary continuation,
including continuation claim, prepare and terminal digests, task/goal/manifest,
manager binding, and receipt-transition coordinates. It admits an exact
zero-change current-recovery result only when the inherited closure ends at the
sealed live tip; nonempty suffixes retain their strict task/handle/generation
and old-head ordering checks.

The lower recovery binding now retains a transition prefix while promoting to
a newer guarded bridge, so the previous bridge remains authenticated rather
than requiring byte identity with the current bridge. Recovery-fence ancestry
resolves an aborted predecessor's durable receipt closure through the guarded
bridge old result instead of inferring its tip from `startingCommit`. The
existing public manual-continuation regression now writes one fresh source
receipt before the guarded rebase and requires the broker-authenticated tip.

Negative controls change continuation claims, source generations, prepare and
terminal digests, inherited receipt order, guarded references, and request
digests. Each rejects without adding a gate or queue side effect.

## round 22 focused checks <!-- cq:wip-checkpoint -->

Completed through focused checks only. No canonical full gate was run or
requested.

- Seven public fused-lineage selectors pass with 388 assertions and zero
  failures: pending manual conflict, fresh-receipt manual continuation,
  staged-retired recovery, exact gate correction, zero-fresh transition,
  ordinary empty cancellation, and the two-bridge fresh-receipt recovery case.
- The final composition exits zero for `bun run typecheck` and targeted ESLint
  and Prettier verification over all eight composed source/test paths.
- Expected-failure policy and marker-contract checks pass 10 / 0 with 242
  assertions. Repository gate closure and its contract pass 40 / 0 with 355
  assertions after refreshing only affected digest declarations.

Mutation observations were obtained in this round and restored before the
green run. Removing continued guarded-tip queue admission changed the queue
hash to `3032d3006fda3ac7dd03d2c75bedc53a1b1ed244051d097fce67495a6f28d094`
and reproduced the generation-6 `downstream-rejected` refusal after 46
assertions. Replacing durable predecessor closure resolution with
`startingCommit` inference changed `dispatchCapability.ts` to
`ad3d898e8758b9cc5ecf72606a5bfec6dc054d0c983c2db8d1893379cd4bae5c`
and reproduced `continuation-ancestry; reason=ancestry-unverified` after nine
assertions. Before the lower-binding correction, the second manual bridge was
accepted but epoch promotion failed after 40 assertions because the newer
guarded bridge was treated as unrelated; restoration passes the complete
zero/one/two-receipt, two-bridge selector with 84 assertions.

## round 22 final composition handoff <!-- cq:wip-checkpoint -->

READY at `/tmp/t6573-r21-compose.Ofb0jP`, rooted in the immutable reviewed
source `/nix/store/j8nbq9rvdrynj97xjz0rkjznshc4kq48-cq-verified-fused-recovery-source`
and reconciled narrowly with T6576's finished round-13 receipt-seal and guarded
materialization products. No branch was imported and no other worktree was
edited. Final SHA-256 values are:

- `packages/cq-config/src/dispatchAttestation.ts`:
  `0846dfc00876e0d95749a0c239e15953c897979ff1ababb16d21b50bc3741231`;
- `packages/cq-config/src/dispatchImplementationQueue.ts`:
  `1ecb617ab6fd48b420f2438bce9fbdaced43175df97cd644b2aad4e6c42d71a4`;
- `packages/cq-config/src/index.ts`:
  `6eac209e6837db1c62cea5fd3a8f583a4ca27b4aebd70fe2ca6b2ec35e6c5d7e`;
- `packages/ledger/src/currentRecoverySeal.ts`:
  `ffe1ee6871f0580a70c5ac2854bee39725afe5e71128936bba4fbc9e7cd69bc6`;
- `packages/ledger/src/guardedRebaseContinuation.ts`:
  `3390e1e61b68c6c1ad931859f3585d798059057d2e57109dc0239291a368ad6d`;
- `packages/ledger-mcp/src/dispatchCapability.ts`:
  `e87841d3542ae614a3052e2d8cad7c9db6c53cad4c5771e903b76c3f4a04a3e0`;
- `packages/ledger-mcp/src/dispatchRecoverySeal.ts`:
  `de77a0cb06dfa4b978859b7a7d4857943e7f56ac862801e8751e2b9f516ddc3d`;
- `packages/ledger-mcp/test/supervisedWorkerGateStorage.test.ts`:
  `2a5b963d450fd3b957b388d7ddb78811f30c4bf47538215114e2014ff7e4615a`.

The sorted path/hash aggregate is
`76e1fc5d498d53534ca4a795abe3b9b687497b316063b265b2568fd3d2806461`.

## round 21 cross-cut reproduction <!-- cq:wip-checkpoint -->

Round 21 owns one disposable composition from
`/nix/store/72i7in0gak343hlb10jhh9n055jpm3xp-cq-verified-fused-recovery-source`.
Its byte-identical writable copy is `/tmp/t6573-r21-compose.Ofb0jP`. Initial
SHA-256 values were `89a95792d7d85cb0fcf2651c6ffcaf1e98e5f0b27d135ebf768b182f06f792fa`
for `dispatchRecoverySeal.ts`,
`08d595fa5a472a7bcebe4f26750d859629337967ed474958b3f420950b5be04e`
for `dispatchAttestation.ts`,
`6839a36824349ce41d9daa3db2b68b321f9f3470946de6fcb5d5069023705927`
for `dispatchImplementationQueue.ts`, and
`f6d0b547fa23aa564d5ec5aaac9b05796c4be2cfb21e3efa78ed8a6c1f4e808b`
for the disposable `supervisedWorkerGateStorage.test.ts` fixture.

Before product edits, public memory/SQLite lifecycle paths reproduced four
closed refusals:

- a cancelled current-recovery generation followed by its manual guarded
  successor failed as `intermediate-state-not-consumed`;
- an ordinary zero-change cancellation after a consumed guarded continuation
  failed as `inherited-closure-missing` with a nonempty global closure and an
  omitted empty post-transition component;
- a zero-fresh current-recovered failure passed broker validation, then
  `createDispatchRecoveryBinding` rejected its starting commit because the
  verified rebase vertex was not a receipt endpoint;
- the next ordinary candidate failed queue admission with `no-intermediate`
  because the two exact cancelled generations had never enrolled.

The existing selectors alone passed 2 / 0 before the new probes, so they did
not cover these narrower histories. The probes use public prepare, guarded
rebase, abort, recovery, result-storage, qualification, and coordination
operations; the old-producer arm is represented by a public runtime without
the current recovery journal, not by a private-row transcript.

## round 21 disposable composition <!-- cq:wip-checkpoint -->

Completed only in the disposable copy. `dispatchRecoverySeal.ts` authenticates
one cancelled journal-recovery to manual-guarded edge from its exact prepare
digest, terminal digest, manager binding, claim/bridge and receipt closure. It
also treats a missing inherited array as the empty post-transition component
only when the authenticated component is actually empty; missing nonempty
proof remains rejected.

`dispatchAttestation.ts` recognizes only `oldResultCommit` and
`rebasedStartCommit` as authenticated transition vertices and uses the same
transition-aware closure tip during recovery/continuation rehydration. It does
not manufacture a receipt. `dispatchImplementationQueue.ts` traverses exact
unenrolled cancellations only when consecutive handle identity, both terminal
digests, the manager identity, journal claim or guarded bridge, prepare digest,
task/goal/manifest, and receipt coordinates all match. The final recovered
candidate still enters the ordinary queue and receives no recovery gate.

Final manifests and stable unified-diff SHA-256 values are:

- `dispatchRecoverySeal.ts`: file
  `8da08c10060eafcf372ca3095cad197c380e614388e3be5fc2fa5081849fe792`,
  diff `9438402a45e8ad9dab4a650c55bac84b91655f5c7bdbf42a8f203eb1e0158588`;
- `dispatchAttestation.ts`: file
  `0846dfc00876e0d95749a0c239e15953c897979ff1ababb16d21b50bc3741231`,
  diff `df06eec0d783d176a0f4af488268c4ba3de31c6fc310dd12db9ebaf0595ada8e`;
- `dispatchImplementationQueue.ts`: file
  `0bcf3368789e4a636d248b7f3b4b95a3afd9f1168e98aff4786061124388ee3d`,
  diff `91531a627676e61ad09ecde5e81c1026f34eb187889d8e162f46e30a974a2305`;
- disposable fixture: file
  `c677f6fb9ceeedf9563fa1159b4fcfddfd1be30854b6746fe2a79744474c7342`,
  diff `3204e4f9a42660bef970c6a61543e608b0fdc01430523c01cf4548ce0bec3f4a`.

`diff -qr --exclude=node_modules` reports exactly those four paths. Permanent
foundation changes remain owned by T6576; G219 claim and workflow changes
remain owned by T6573.

## round 21 focused checks <!-- cq:wip-checkpoint -->

Completed through focused checks only. This dispatch did not run or request the
canonical full gate.

- The three new public memory/SQLite regressions pass 3 / 0 with 142
  assertions.
- The complete recovery-seal, dispatch-attestation/backend, and queue suites
  pass 199 / 0 with 1,350 assertions.
- `bun run typecheck`, targeted ESLint, and Prettier verification exit zero.
- Removing direct cancelled/manual composition changed the seal hash to
  `56451e7e6355a3b3928d956609d0fa12e467fbe71f6e12d1705e0c9e39d88e04`
  and restored the `intermediate-state-not-consumed` failure. Disabling only
  empty-component admission changed it to
  `f3e4cf872a087335e47709ecd0aefb49d5eb96e4370fde437bdedaa101d70962`
  and restored `inherited-closure-missing`.
- Disabling transition-vertex recognition changed `dispatchAttestation.ts` to
  `442fa208df2e2a1a1fb7f386581f180e56efe5ed0f0c4860191bf83dbcf6cfe3`
  and restored the starting-commit refusal. Disabling unenrolled-cancellation
  traversal changed the queue source to
  `6e563d20f9ac4bd606a9acb6361260735aaad94a59db464c18be1c9b9c66b088`
  and restored `first-hop-rejected`. Each file was restored to its final hash
  above before the final green selector/typecheck run.

The complete supervised-storage file produced 66 passes and one unchanged
30-second timeout in the installed-successor boundary selector. The exact
selector reproduced 0 / 1 with the same timeout on both the final composition
and a separate byte-identical immutable-source copy at
`/tmp/t6573-r21-basecheck.ZfsdIR`; it is not counted as green focused evidence.

No repository product source, full gate, Nix build, installation, deployment,
private primary store, retained-source gate, or T6576 source was changed.

## round 20 inherited-closure reproduction <!-- cq:wip-checkpoint -->

Round 20 owns one disposable repair composition rooted at the exact verified
R12 snapshot. The immutable source is
`/nix/store/81mjhn018viwq2r6zs5fl6861iglgkw0-cq-verified-fused-recovery-source`;
its fresh writable copy is `/tmp/t6573-r20-compose.JnGs7O`. Initial `diff -qr`
output is empty. Initial SHA-256 values are:

- `dispatchCapability.ts`:
  `a90e21a64adf0fa9899aeee1ad8116dd47a2bcaaeab769d5b4e7ca8703a1e4f2`;
- `dispatchRecoverySeal.ts`:
  `1daf31a1b127f9e32d6bb63a90c186ba48583a9a9297ff57a2ff2885ecc5be4c`;
- `supervisedWorkerGateStorage.test.ts`:
  `f6d0b547fa23aa564d5ec5aaac9b05796c4be2cfb21e3efa78ed8a6c1f4e808b`.

Before either product module was edited, the frozen staged-retired / cancelled
guarded fixture was changed only at its final resumed worker: it persisted one
fresh broker receipt, stored `status: fail` with `resultCommit: null`, then
qualified the native completion and expected consumption with no new gate. The
probe fixture SHA-256 is
`6e282024f3e862fc522fa63d8dec61b94c8f05e531674ce2f17c1e32f82f3085`.

The exact selector exited 1 with 0 pass, 1 fail, 63 filtered tests, and 21
successful assertions before the intended rejection. Qualification failed in
`validateGitChangeBrokerResultEvidence` with `recovery receipt chain diverges
at entry 2`; its stack reaches `dispatchCapability.ts` `consumeFailure`. Both
product SHA-256 values remained equal to the immutable R12 snapshot. The one
existing runner request is the earlier ordinary successor gate, proving the
failure occurs before any replacement gate can run.

## round 20 R12 repair composition <!-- cq:wip-checkpoint -->

Completed in `/tmp/t6573-r20-compose.JnGs7O`. The composed product changes are
limited to `dispatchCapability.ts` and `dispatchRecoverySeal.ts`; the only
other changed source is the disposable `supervisedWorkerGateStorage.test.ts`
probe. `diff -qr`, excluding copied dependency/build directories, reports
exactly those three paths.

`consumeFailure` now reconstructs the same server-authenticated context used by
`storeResult`: the dispatch base comes from the persisted worker input, and the
single/latest plus ordered receipt-chain transitions come only from the
committed journal matching the persisted recovery claim. The failed output is
then revalidated under that context before the existing locked failure
confirmation. No caller-supplied lineage is trusted and no gate is invoked.

`dispatchRecoverySeal.ts` adds only bounded diagnostics for inherited-closure
mismatch and a nonconsumed intermediate. Diagnostics contain generation,
closed state/cause values, two receipt counts, and claim/bridge presence.
Paired memory/SQLite hostile controls verify that the manager token, source and
successor attestation ids, live commit, and opaque guarded reference do not
appear. The two record casts are type-only consequences of rejecting readonly
JSON arrays before indexed access.

Final manifests:

- `dispatchCapability.ts`: SHA-256
  `ad3d898e8758b9cc5ecf72606a5bfec6dc054d0c983c2db8d1893379cd4bae5c`,
  stable diff SHA-256
  `17c282de6b5b621ce7f2ef0661acf0cb1fbbf35ec5231b301d0d26981ec7133a`;
- `dispatchRecoverySeal.ts`: SHA-256
  `89a95792d7d85cb0fcf2651c6ffcaf1e98e5f0b27d135ebf768b182f06f792fa`,
  stable diff SHA-256
  `8079c08f851a80798444280b15dc2f7c33767fb31b3cf3fde24afad0a1d78c1a`;
- probe fixture: SHA-256
  `27c32f59b51c3468fdf5a93fab23b459389f3a59cb5748d13aa5c96958541480`,
  stable diff SHA-256
  `578b29f556f716c92dc344b7dd86de7b1038ce0fbf6086c504ec5c9c778e278a`.

Two restored mutations prove the correction inputs independently. Replacing
the persisted dispatch base with the manager base made the guarded consumed
failure exit 1 with `guarded worker result diff base does not equal the
journaled ontoCommit` after 19 assertions. Omitting the authenticated receipt
transitions made the staged-retired failure exit 1 with `recovery receipt chain
diverges at entry 2` after 35 assertions. Restoring the composed bytes returned
both selectors green.

A temporary post-consumption `resolveRecovery` probe was deliberately not kept:
after the failed worker was correctly consumed, the frozen path refused it as
`the next journal recovery epoch is not one eligible terminal worker abort`.
An attempted consumed-fail v2 promotion then met the ledger's existing
committed-journal transition guard. That is a separate foundation change, not a
safe relaxation in these two temporary modules; it remains for its owning
workstream. No private row was inferred or edited.

## round 20 focused checks <!-- cq:wip-checkpoint -->

Completed through focused checks only. This dispatch did not run or request the
canonical full gate.

- The four recovery selectors pass 4 / 0 with 342 assertions: ordinary
  continuation, guarded consumed failure with manager base distinct from onto,
  staged-retired/cancelled-guarded failure consumption across memory and SQLite,
  and zero/one/multiple fresh guarded receipt components after reopen.
- The complete `dispatchRecoverySeal.test.ts` suite passes 26 / 0 with 81
  assertions.
- `bun run typecheck` exits 0.
- Prettier accepts both product modules and the disposable probe fixture.

No full suite, Nix build, installation, deployment, private-primary access,
retained-source gate, T6576 edit, or repository product-source change was
performed.

## round 18 R9 queue composition <!-- cq:wip-checkpoint -->

Round 18 owns one disposable composition rooted at the exact verified R9
snapshot. The immutable source is
`/nix/store/2l5si1f4wrgak5cdgp3796mwd0bk1q45-cq-verified-fused-recovery-source`;
its fresh writable copy is `/tmp/t6573-r18-compose.MoQrtx`. The only file to
be changed in that copy is
`packages/cq-config/src/dispatchImplementationQueue.ts`.

Initial manifest:

- immutable source SHA-256:
  `0cef36387b8986b6f485db4e034f6ca3cdfca67b6a56322f9abfc74dcdfe0a54`;
- copied source SHA-256:
  `0cef36387b8986b6f485db4e034f6ca3cdfca67b6a56322f9abfc74dcdfe0a54`;
- retained diagnostic branch source SHA-256:
  `b167a3c8c9728f09fef26f580f92a6c897df99576b69a8e2fbfe04a68e94d6ba`.

Final manifest:

- composed source SHA-256:
  `6839a36824349ce41d9daa3db2b68b321f9f3470946de6fcb5d5069023705927`;
- stable unified-diff SHA-256:
  `54d3f7f13f6ee83718b3d6a160785335a0e7232c945b49fe58b6e94ccc1801ca`;
- mode and byte count: `100644`, `113344` bytes;
- `diff -qr` against the immutable snapshot reports exactly
  `packages/cq-config/src/dispatchImplementationQueue.ts` and no other file;
- the restored R9 queue regression source SHA-256 is
  `19df791d33642bb4090c4b63b05a3804ba8d06bf2b6c012169b90c082b1ae75d`.

The queue composition factors the existing intermediate predicate only far
enough to feed the bounded refusal diagnostic. It preserves all six R9
admission arms, including the exact
`isLegacyRetiredJournalRecoveryIntermediate(candidate, intermediate,
successor, successorBinding, authority, priorEnrollment)` call. It does not
change an admission predicate or repository product source.

## round 18 focused checks <!-- cq:wip-checkpoint -->

Completed through focused checks only. This dispatch did not run or request the
canonical full gate.

- Before composition, the frozen `452b5a3e` diagnostic selectors failed 0
  pass / 3 fail at the intended missing-diagnostic assertions.
- On the final composition, the two frozen no-intermediate selectors pass.
  The frozen composed selector observes a truthful R9-specific
  `downstream-rejected` relation (one admitted ordinary-continuation first hop)
  instead of its older `first-hop-rejected` expectation, so the unmodified
  frozen selector remains 2 pass / 1 fail. Disabling predicates one at a time
  isolated that difference to R9's ordinary-continuation arm, not its legacy
  recovery arm. No semantic correction was attempted without the real
  historical generation-3 evidence.
- With only that temporary R9-specific expected diagnostic adjusted, the same
  three selectors and all token/path/receipt containment controls pass: 3
  pass, 0 fail, 18 assertions. The test source was then restored byte-for-byte
  to the immutable R9 snapshot.
- The unmodified R9 queue and SQLite-race suites pass: 23 pass, 0 fail, 92
  assertions. The `cq-config` TypeScript project build exits 0, and Prettier
  accepts the composed module.

Mutation evidence from this generation: appending the candidate manager token
outside the bounded diagnostic made exactly the two confidentiality controls
fail (1 pass / 2 fail), naming `queue-worktree-1` and
`diagnostic-manager-token`. Restoring the module returned the diagnostic
selectors to 3 pass / 0 fail and restored SHA-256
`6839a36824349ce41d9daa3db2b68b321f9f3470946de6fcb5d5069023705927`.

No Nix build, installation, deployment, private-primary access, historical
reset, retained-source gate, full suite, product-source commit, or T6576 edit
was performed. The remaining historical-generation diagnosis belongs to the
parent rebuild against this exact disposable source.

## round 17 qualification refusal diagnosis <!-- cq:wip-checkpoint -->

Round 17 starts from `2ef1061ffdca2181f4c1485da0a8880bbde20155` after the
real public runner still refused historical enrollment generation 3. The
existing memory/SQLite regression and a read-only variant both pass, so an
empty fresh receipt suffix is not a sufficient cause. This round will preserve
every refusal guard and first reproduce the retained-history relation through
public lifecycle fixtures. If no faithful fixture fails, the correction will
be limited to safe reason tags and aggregate state/generation counts on the
existing qualification refusal; it will not expose capabilities, tokens,
paths, raw rows, or receipts.

No available public lifecycle fixture reproduced the real runner's rejected
ancestry semantics. The authorized diagnostic arm therefore adds no queue
allowance and changes no refusal predicate. It factors the existing
intermediate predicate without changing its conditions, then appends one
fixed-size diagnostic to the existing `already-terminal` refusal. The closed
reason tags are `no-intermediate`, `no-bound-intermediate`,
`first-hop-rejected`, and `downstream-rejected`; the remaining fields contain
only same/different-attestation scope, queue states, terminal reasons,
generations, and aggregate counts.

The two direct public queue regressions failed first, 0 pass / 2 fail, because
the unchanged refusal omitted `qualification-refusal:v1`. After the diagnostic
was added they passed 2 / 0 with 12 assertions, including containment checks
for manager token/fingerprint, repository/common/worktree paths, and receipt
digests/object identifiers. A composed public lifecycle fixture then failed
against its deliberate placeholder classification and exposed the bounded
`first-hop-rejected` relation from superseded generation 1 through cancelled
generation 2 to generation 3. With that observed classification asserted, it
passed 1 / 0 with 6 assertions. These are Behavioral-Active Blackbox-Group
regressions with `tasks:T6573` provenance.

## round 17 focused checks <!-- cq:wip-checkpoint -->

Completed through focused checks only. This dispatch did not run or request the
canonical full gate.

- `bun test packages/ledger-mcp/test/implementation-candidate-queue.test.ts
  packages/ledger-mcp/test/implementation-candidate-queue-sqlite-race.test.ts`:
  24 pass, 0 fail, 107 assertions.
- Existing T6573 selector `a rejected correction composes through cancelled
  receipt recovery into a guarded continuation`: 1 pass, 0 fail, 22
  assertions across memory and SQLite/reopen.
- Restored diagnostic selectors: 3 pass, 0 fail, 18 assertions.
- Repository gate closure and closure contract: 40 pass, 0 fail, 355
  assertions; no closure manifest update was required.
- `bun run typecheck && bun run lint`: exit 0 (`tsc -b`, then `eslint .`).

Mutation evidence from this generation:

- Before the product edit, the two direct refusal selectors failed 0 pass / 2
  fail because the existing message omitted the requested diagnostic.
- The composed lifecycle assertion deliberately expected a placeholder reason
  and failed 0 / 1 while observing the actual bounded
  `first-hop-rejected` classification; restoring the expected tag passed 1 / 0.
- A temporary mutation appended the source manager token after the bounded
  diagnostic. The containment run failed exactly 2 assertions, naming
  `queue-worktree-1` and `diagnostic-manager-token`; the same-attestation case
  without a containment assertion remained green. Restoring the source made
  all 3 selectors pass and restored its SHA-256 to
  `b167a3c8c9728f09fef26f580f92a6c897df99576b69a8e2fbfe04a68e94d6ba`.

The final regression source SHA-256 is
`6c53a6219a3fd4441bb101d541b750bc94d44d2ce28c3da55611af5139190302`.
No queue refusal predicate, full suite, Nix build, installation, deployment,
private-state inspection, retained-source gate, or history rewrite was
performed.

## round 15 qualification ancestry reproduction <!-- cq:wip-checkpoint -->

The paired memory and SQLite/reopen public regression qualifies a genuine red
source, persists a changed correction whose gate loses its parent, commits and
cancels an ordinary current-recovery worker, recaptures it, prepares a new
worker, persists another broker commit, stages the result, and invokes native
qualification. With the live `tasks:T6573` expected-failure marker temporarily
removed, the selector exited 1 with 0 pass / 1 fail and 9 assertions at the
intended boundary: `terminal enrollment authority ...#1 cannot be resurrected`.
The marker, annotation, and inventory entry were restored; the inverted
selector and expected-failure inventory policy both pass.

The exact failing predicate is the historical rejected source's missing
composition edge to its authenticated changed correction. The later resumed
worker is a valid journal-recovery descendant of that correction, but the
intermediate traversal does not currently consider a gate-rejected correction,
so qualification mistakes the earlier terminal row for an independent reuse.

## round 15 qualification ancestry correction <!-- cq:wip-checkpoint -->

The composed-terminal traversal now admits a gate-rejected correction as an
intermediate only through the existing authenticated correction predicate. It
does not alter terminal rows or recovery claims and adds no new direct
allowance: the predicate still verifies the red terminal and qualification,
exact task/manager/repository/worktree binding, result and output evidence,
receipt-prefix closure, bridge identity, changed tip, and generation range.

The same-titled regression is plain and its annotation and inventory entry are
removed. It must now pass both memory and SQLite/reopen paths through recapture,
native qualification, green ordinary coordination, and the subsequent guarded
continuation qualification.

## round 15 focused checks <!-- cq:wip-checkpoint -->

Completed through focused checks only. This dispatch did not run or request the
canonical full gate.

- Same-title public memory and SQLite/reopen qualification progression: 1 pass,
  0 fail, 22 assertions.
- Queue and SQLite race controls, including immutable terminal enrollment and
  resurrection negatives: 23 pass, 0 fail, 92 assertions.
- Related red-correction, consumed-continuation, journal-recovery, ordinary and
  guarded continuation controls: 6 pass, 0 fail, 116 assertions.
- Expected-failure policy after removal of the live marker: 9 pass, 0 fail, 237
  assertions.
- Repository gate closure and closure contract after refreshing only the two
  existing opaque-edge declarations for the changed test source: 40 pass, 0
  fail, 355 assertions.
- `bun run typecheck && bun run lint`: exit 0 (`tsc -b`, then `eslint .`).

Mutation evidence: removing only the new intermediate
`isGateRejectedCorrectionAncestor` edge returned the original native
qualification refusal, `terminal enrollment authority ...#1 cannot be
resurrected` (0 pass / 1 fail / 9 assertions). Restoring the edge returned the
same selector to 1 pass / 0 fail / 22 assertions. The production source was
restored from the mutated SHA-256
`2042fbe933c4a1ea341ca19f5af9c62bcb9f9a6d957713ee9b7730b0a6231ee5`
to `9fdf2e8c55c3cb2fe358a589faad4c7cb34acbd4f3c69d9ce61b1f4ed8ad2c26`;
the final regression source SHA-256 is
`37dd2ef3a8b63acbb158e24f1419bde2ff02cf8fa74d56466569ed22cbb82aab`.

No full suite, Nix build, installation, deployment, private-primary access,
retired source gate, or worker-sandbox native timeout was attempted.

## round 14 old-producer correction-claim reproduction <!-- cq:wip-checkpoint -->

Round 14 starts from `0d35085e472fff05c15ab422759bf7fb89b60fbc`.
The bounded hypothesis is that an old producer can persist an authenticated
red-to-changed-to-consumed continuation without the newer correction claim,
after which the current reader rejects the retained recovery as
`continuation-ancestry/ancestry-unverified`. The first focused regression will
exercise that history through the old producer and current reader before any
product edit.

The same-title regression now consumes the changed correction, resolves its
ordinary continuation, and prepares the next worker without a guarded bridge.
Before product edits, its exact selector exited 1 with 0 pass / 1 fail and 14
assertions. The current reader refused the genuine prepared/qualified/gated
history with `stage=continuation-ancestry; reason=ancestry-unverified` at the
ordinary prepare boundary.

## round 14 claim retention and restart correction <!-- cq:wip-checkpoint -->

The correction is limited to G219 claim retention/traversal and the case-C
restart fixture; epoch and transition work belongs to T6576. The attestation
envelope, abort transition, tombstone, and SQLite codec now preserve and
validate the exact gate-rejected correction claim. Recovery-fence ancestry
validates that claim against the rejected predecessor's task, manager, gate
terminal, receipt closure, bridge digest, and changed-correction input.

For rows written by the retained r3fp producer, the reader reconstructs the
only possible claim from the predecessor and accepts it only when the stored
`prepareRequestDigest` authenticates that complete prepare request. The case-C
fixture derives such a legacy row from an actual public prepare, reopens the
SQLite backend, and then performs an ordinary continuation with no guarded
bridge. Its selector passed for both memory and SQLite: 1 pass, 0 fail, 70
assertions. The same-title narrow correction selector passed 1 / 0 with 15
assertions. `bun run typecheck` exited zero.

## round 14 focused checks <!-- cq:wip-checkpoint -->

Completed through focused checks only. This dispatch did not run or request the
canonical full gate.

- Same-title ordinary no-bridge correction continuation: 1 pass, 0 fail, 15
  assertions.
- Whole case-C memory/SQLite/reopen selector, including the r3fp-compatible
  omitted-claim row: 1 pass, 0 fail, 70 assertions.
- Attestation lifecycle/backend and dispatch-capability suites: 175 pass, 0
  fail, 1,306 assertions.
- Managed recovery, CLI parsing, and production runtime suites: 35 pass, 0
  fail, 1 declared PostgreSQL skip, 105 assertions.
- Repository gate closure and closure contract: 40 pass, 0 fail, 355
  assertions after refreshing only the changed opaque-edge declarations.
- `bun run typecheck && bun run lint`: exit 0 (`tsc -b`, then `eslint .`).

Mutation observations from this generation:

- Omitting `gateRejectedCorrectionClaim` at allocation made case C fail with
  `current correction omitted its persisted rejection claim` (0 pass / 1
  fail). Restoring the allocation returned 1 / 0.
- Inverting the legacy `prepareRequestDigest` equality made the bridge-bearing
  history fail closed at `continuation-ancestry/ancestry-unverified` (0 / 1).
  Restoring the equality returned 1 / 0.
- Inverting the nullable bridge-digest equality made the no-bridge same-title
  continuation fail closed at `continuation-ancestry/ancestry-unverified`
  (0 / 1). Restoring it returned 1 / 0.
- Rejecting the valid correction-claim codec key set made the SQLite case fail
  with `stored attestation body has malformed
  "gateRejectedCorrectionClaim"` (0 / 1). Restoring it returned 1 / 0.

The restored SHA-256 digests are
`2cecca30b418616e785791cc009246efc6a156f7192379c1d25d93062d7ebe98`
for `dispatchAttestation.ts`,
`10b690d6c062bc0df7b189bcdcd5c3a46e35443c43efe0094a68d6dc6172dac6`
for `dispatchAttestationBackend.ts`,
`b9f0c0b9e5a9f6325a29865953c25ffe468684026a9abb8b8dbf13e75f04df79`
for `dispatchCapability.ts`, and
`2aef47b9187ab0d0b3c3fd711b75228a2c77f6e66927dd4cbd3fd1f1c69f6e5c`
for the supervised-worker regression.

The requested six-file aggregate was also attempted. It reached 127 pass, 1
declared skip, and 3 native subprocess timeouts plus the known cleanup race.
Each timed-out native selector reproduced in isolation under
`SMIND_SANDBOXED=1`; no product patch was made for those environment-bound
failures. Recovery-specific cases in that aggregate, including case C and the
same-title regression, were green.

## round 13 retained-bridge base reproduction <!-- cq:wip-checkpoint -->

Round 13 starts from `eec3c325097f684c04098faa2e55bb5108901707`.
The bounded hypothesis is that successor preparation validates the successor
base against the manager binding before applying a retained guarded bridge's
distinct logical onto commit. The first check will reproduce the
retained-bridge-only path with different manager base and bridge onto commits.

The existing same-title regression is the requested case: it records the
manager base, advances the protected repository to a distinct onto commit,
removes the guarded bridge from the committed manager seal, retains the
authenticated source bridge, and prepares the successor with the bridge onto
as its logical base. This test file contains no `receiptChainTransitions`
path.

`bun test packages/ledger-mcp/test/managedDispatchRecovery.test.ts -t
'guarded-origin current recovery preserves its authenticated bridge and
logical onto'` exited zero: 1 pass, 0 fail, 5 assertions.

## round 13 retained-bridge correction <!-- cq:wip-checkpoint -->

The suspected retained-bridge ordering defect did not reproduce. Per the
operator constraint to fix only on failure, no product or test bytes were
changed in this round.

## round 13 focused checks <!-- cq:wip-checkpoint -->

Completed through focused checks only. This dispatch did not run or request the
canonical full gate.

- Retained-bridge-only logical-base regression: 1 pass, 0 fail, 5 assertions.
- `bun run typecheck`: exit 0 (`tsc -b`).
- `bun run lint`: exit 0 (`eslint .`).
- Repository gate closure and closure contract: 40 pass, 0 fail, 355
  assertions.

Fresh mutation observation: replacing the bridge-aware logical-base selection
with the manager base made the retained-bridge selector fail 0 pass / 1 fail at
the expected `input.baseCommit` rejection. Restoring the original bytes made
the selector pass 1 / 0 with 5 assertions and restored
`dispatchCapability.ts` to SHA-256
`e98387e31678cc2e7d487b69cdd7b35a7df04b7b1713ccba193c52a8444e1153`.
The mutation was temporary; no product or test bytes changed in this
generation. No full suite, Nix build, installation, deployment, private-store
inspection, retained-source gate, or parent-owned composed host run was
performed.

## public subprocess reproduction <!-- cq:wip-checkpoint -->

Added the form-(b) expected-failure regression with annotation
`tasks:T6573`, a live `test.failing` marker, and the matching inventory entry.
The installed `codex-role-dispatch` subprocess received a complete first
`staged-rebase-retired` acknowledgement, retried its revoked parent-gate
capability, and exited nonzero with
`implementation candidate coordination parent authority is invalid`.

Observed green inversion/policy commands before product edits:

`bun test packages/cq-config/test/codexRoleBoundary.test.ts -t 'installed runner hands off a retired staged-rebase conflict'`

Result: 1 pass, 0 fail (the expected-failure marker inverted the reproduced
nonzero subprocess outcome).

`bun test packages/cq-config/test/expectedFailurePolicy.test.ts -t 'committed inventory agrees'`

Result: 1 pass, 0 fail.

## managed recovery implementation <!-- cq:wip-checkpoint -->

Added `worktree_manage.operation=resolve-staged-rebase` with the exact live
manager handle, retained source dispatch, and opaque staged-source reference.
The public operation holds the manager effect lock, permits only the bound
detached rebase state, and reuses the retained-source lookup plus guarded
journal reconciler without reacquiring the lock. It returns conflict-pending,
preparation-ready, or already-bound-successor acknowledgements and never
creates a replacement journal. Replays return the same pending/preparation
authority or the exact bound successor; foreign source handles, changed opaque
references, and changed manager coordinates fail closed.

The production conflict regression now exercises pending recovery, protected
resolver continuation, terminal journal reconciliation, public preparation
authority consumption, and the ordinary successor queue path with zero gate
invocations against both the in-memory and SQLite attestation backends.

Mutation check: changing the retained-source equality guard rejected the valid
public recovery with `staged-rebase recovery source handle or reference
changed` on memory and SQLite; restoring the original bytes restored 2 passes /
0 fail. The source
file SHA-256 was restored to
`e8a604991a5ad2afef6b8b9164e6b6302c3f2a9f37399ba85e279dbfc9d8a6a9`.

## coordinator handoff implementation <!-- cq:wip-checkpoint -->

The coordinator CLI boundary now validates and preserves the complete
`empty`, `blocked`, `completed`, and `successor-queued` acknowledgements.
`staged-rebase-retired` blocked acknowledgements require their opaque
`sourceReference` and return immediately instead of polling the revoked parent
authority. Ordinary blocked polling, completion draining, and successor
identity remain explicit.

Mutation check: disabling the retired-front stop condition reproduced the
revoked-authority retry and exact
`implementation candidate coordination parent authority is invalid` failure;
restoring it returned 1 pass / 0 fail. The source file SHA-256 was restored to
`8fdab25efcbc3faba67065616ae2fc4715c3778effc0a5faa00093d5f437e37f`.

## workflow projections <!-- cq:wip-checkpoint -->

Correction round started from `b1fb928d44a54c45afc8b64127a1c532d7f6ed7e`.
`bun run gen-prompt-catalog` and `bun run gen-agents` both exited zero. The
affected agent catalogue projection was committed at
`a8f60763d0bd51257094c15efcb5d5ca0c619ad3`; no prompt-catalog byte changed.

`bun run verify-packaged-prompt-surfaces` proved generator freshness (two
generators, two byte-identical runs) and link-prompts hygiene, then could not
complete its Nix package build in this confined worker. The sandbox denies the
host Nix daemon and network substitutes. A read-only host-store evaluation
succeeded, but a writable temporary store would have to build 1,390 missing
derivations and failed at network-fetched fixed-output sources. Packaged-surface
verification therefore remains incomplete.

## focused checks <!-- cq:wip-checkpoint -->

Round 2 starts from `490568ee59c3cda4289b4e059ad5daff9d8a2b1b`.
The parent reproduced one remaining fixture failure after 113 passes and 1
skip: the restored native handoff arm passes, while the direct
`executeCodexImplementationCandidateCoordinator` call cannot find the T6573
marker/attempt paths because only the native subprocess receives them through
its environment. The bounded correction is to bind those two paths explicitly
for both invocation paths, preserving the complete native regression and direct
helper assertion.

The local worker sandbox timed out in the native subprocess before reaching the
direct helper. With only that already-parent-proven native arm temporarily
isolated, the direct helper failed in 30 ms with `T6573 paths missing` (0 pass,
1 fail). The fixture now defines one explicit environment containing the marker
and attempt paths and supplies it to both invocation paths. Repeating the same
isolated selector passed (1 pass, 0 fail); the temporary isolation was removed.
The test source changed from SHA-256
`1eab809c40961e10fa886c1d825cbfd7670cd7c3373de26ee4d1931861634cae`
to `3ed9220014b5ae7b66b1ad77d145e87d762ce7a0c218600f7699a6fae7f52cbd`.

Round 2 green focused evidence on the committed correction:

- `bun test packages/cq-config/test/codexRoleBoundary.test.ts -t 'candidate coordinator'`:
  3 pass, 0 fail.
- `bun test packages/ledger-mcp/test/implementation-candidate-stale-rebase.test.ts`:
  4 pass, 0 fail.
- `bun test packages/ledger-mcp/test/supervisedWorkerGateStorage.test.ts -t 'a fresh production coordinator replays one persisted conflict without gating or relaunching it'`:
  2 pass, 0 fail (memory and SQLite).
- `bun test packages/ledger-mcp/test/parseArgs.test.ts`: 28 pass, 0 fail.
- `bun run typecheck`: exit 0 (`tsc -b`).

The parent still owns the host-only installed-wrapper selector, packaged-surface
verification, and any final gate. This focused worker did not run or request a
full gate.

The exact six-file command exited 1 after 111 passes, 1 skip, 3 timeouts, and
1 cleanup error. The timeouts were the native registered-launch/process-provider
subprocess cases; temporary phase instrumentation showed the installed wrapper
reached dispatch planning but the nested provider/registered launch could not
advance inside this worker sandbox. The instrumentation was removed byte-for-byte.
`bun run typecheck` subsequently exited zero.

Fresh mutation observations:

- Inverting the retained source-attestation equality guard made both the memory
  and SQLite recovery cases fail with `staged-rebase recovery source handle or
  reference changed`; restoring the source returned 2 passes / 0 failures and
  SHA-256 `e8a604991a5ad2afef6b8b9164e6b6302c3f2a9f37399ba85e279dbfc9d8a6a9`.
- Disabling the retired-front stop condition made the coordinator harness retry
  once and fail with `implementation candidate coordination parent authority is
  invalid`; restoring the source returned the full handoff in one attempt and
  SHA-256 `8fdab25efcbc3faba67065616ae2fc4715c3778effc0a5faa00093d5f437e37f`.

## D512 scope transfer <!-- cq:wip-checkpoint -->

D512 is owned by G220/T6575 (`092ca0c2`) and is outside this branch. Round 4
records that transfer only; it will not reproduce or correct D512 here.

## D513 authenticated-lineage reproduction <!-- cq:wip-checkpoint -->

Round 4 added the literal `tasks:T6573` annotation, live `test.failing`
marker, and inventory entry for `guarded-origin current recovery preserves its
authenticated bridge and logical onto`. Removing the inversion reproduced the
unchanged broker rejection exactly:
`input.baseCommit: journal recovery baseCommit differs from the managed
binding` (0 pass, 1 fail). The restored marker and inventory policy both
passed.

The correction seals the source's authenticated guarded bridge while retaining
the manager's original `baseCommit`, re-verifies the terminal guarded journal,
uses its `ontoCommit` as the logical recovery base, carries the bridge into the
successor binding, and injects guarded lineage into fetched input. The same
test title is now plain; its annotation and inventory entry are removed. The
selector passes with 5 assertions after also driving a promoted successor and
one bridge-less committed epoch through the public recovery API. The
current-recovery, seal-journal, and managed-recovery group passes 45 tests / 0
failures. `bun run typecheck` exits zero.

## supplemental fused correction <!-- cq:wip-checkpoint -->

Existing bridge-less committed epochs now recover only a unique authenticated
guarded bridge from the selected source's same-attestation ancestry. Recovery
re-verifies the original snapshot and terminal identity before considering the
bridge, rejects missing, ambiguous, or foreign ancestry and mutated
journal/onto data, and promotes a successful legacy epoch to a sealed bridge.
The manager binding retains its original `baseCommit`; only the worker's
logical base becomes the authenticated `ontoCommit`.

Mutation observations on restored bytes:

- Removing the unique-bridge guard made the ambiguity assertion fail because a
  competing bridge was returned. Restoration returned 1 pass / 0 failures and
  SHA-256
  `57900e045064b843a09dafa2affe2b03244c90822d3d433674a240547ef275b6`.
- Removing the foreign-binding guard changed the observed rejection from
  `foreign managed binding` to an incidental snapshot mismatch. Restoration
  returned the six-assertion selector to green with the same source SHA-256.
- Reverting journal recovery to the manager's original base reproduced the
  exact `input.baseCommit` rejection. Restoration returned the guarded-origin
  selector to 1 pass / 0 failures and SHA-256
  `b18e42232ec37344418bae229399bc42c35d13e826e4482bd7b259047505c8ad`.
- Disabling the guarded-reference/request-digest invariant made the malformed
  bridge assertion accept an inconsistent seal. Restoration returned the
  selector to green and SHA-256
  `c3c66aca3d119e0527ccb9bc849596d4b3e77ee708bd9b4aa91e7be8fc03d81a`.

## round 4 evidence audit <!-- cq:wip-checkpoint -->

Round 4 starts from `3fb0d8327abeeb33728183c7c5064c50807da986`.
The D511 paired memory/SQLite case now closes and reopens the real SQLite
attestation backend after the pending disposition and again after terminal
resolver receipts. It then prepares the one recovered successor, commits a
receipt-backed result, stores it, runs exactly one supervised gate, confirms
completion, and verifies the retired source remains `staged-rebase` aborted
with no second successor generation. Both adapters pass with 54 assertions.

Changing the exact gate-count assertion from one to zero failed both adapter
cases with received length one. Restoring the assertion returned 2 passes / 0
failures and test SHA-256
`a9044b8dfc602673f94220fe85f7deffe3f9d48d9801989e8d7224d893427c56`.

## round 4 focused checks <!-- cq:wip-checkpoint -->

Green focused evidence on the committed correction:

- `bun test packages/ledger-mcp/test/managedDispatchRecovery.test.ts packages/ledger-mcp/test/dispatchRecoverySeal.test.ts packages/ledger/test/current-recovery-seal-journal.test.ts packages/cq-config/test/expectedFailurePolicy.test.ts packages/ledger-mcp/test/parseArgs.test.ts packages/ledger-mcp/test/productionDispatchRuntime.test.ts`:
  86 pass, 1 skip, 0 fail.
- `bun test packages/ledger-mcp/test/supervisedWorkerGateStorage.test.ts -t 'a fresh production coordinator replays one persisted conflict without gating or relaunching it'`:
  2 pass, 0 fail (memory and real SQLite reopen paths, 54 assertions).
- `bun test packages/cq-config/test/codexRoleBoundary.test.ts -t 'candidate coordinator'`:
  3 pass, 0 fail.
- `bun run typecheck`: exit 0 (`tsc -b`).
- `bun run lint`: exit 0 (`eslint .`).

The operator-specified six-file command was also run. It reached 112 passes and
1 skip, but exited 1 on the three known nested process-boundary timeouts plus
their dangling-process cleanup error: D362 at 10 seconds, the installed
staged-rebase handoff at 10 seconds, and the installed successor boundary at
30 seconds. The same changed recovery selectors pass outside that nested
subprocess group as recorded above. The parent owns host/package acceptance and
the canonical full gate.

## round 5 sealed-source reproduction <!-- cq:wip-checkpoint -->

Added the literal `tasks:T6573` annotation, live `test.failing` marker, and
inventory entry for `sealed recovery selects its exact authenticated source
bridge`. The inverted selector passed 1 / 0, and the committed inventory policy
passed 1 / 0. Replacing the marker with a plain test exited 1 with the expected
`sealed guarded bridge differs from its authenticated source ancestry`
rejection before any product edit; the live marker was then restored.

## round 5 exact bridge selection <!-- cq:wip-checkpoint -->

The sealed bridge now authenticates against the exact selected source row when
that row carries a guarded bridge. Bridge-less selected rows retain the prior
unique-ancestry requirement, so ambiguous legacy/promoted epochs still reject.
The same-titled regression is plain and green, its annotation and inventory
entry are removed, and a changed selected bridge still fails the original
journal digest check.

Mutation evidence on restored bytes:

- Removing the exact-source return reproduced the original two-bridge
  `sealed guarded bridge differs` rejection; restoration returned the sealed
  replay and legacy ambiguity selectors to 2 passes / 0 failures and source
  SHA-256 `27fd0d360313a5db9a2cc8d2e6aaf87c4b96da491db21633ee2ea006f7b93d87`.
- Substituting the older bridge in the positive identity assertion failed with
  the expected `b` versus `c` bridge diff. Replacing the changed-source
  negative control with the unchanged selected bridge failed because no error
  was thrown. Restoration returned the selector to green and test SHA-256
  `144264ef5bb704d3fea1644549b76d40bf438178d1251d3fb0a6e0234c7921a7`.

All stale opaque-edge source digests created by the earlier fused correction
were refreshed in `cq-gate-closure.json` without changing any target. The
manifest enumerator observed zero mismatches; `gateClosureRepository.test.ts`
and `gateClosure.test.ts` then passed 40 / 0.

## round 5 focused checks <!-- cq:wip-checkpoint -->

Measured through focused checks only. This dispatch did not run or request the
canonical full gate.

Final focused evidence on the correction:

- `bun test packages/ledger-mcp/test/dispatchRecoverySeal.test.ts packages/ledger-mcp/test/dispatchRecoveryEpochPromotion.test.ts packages/ledger-mcp/test/managedDispatchRecovery.test.ts packages/ledger/test/recovery-source-selection.test.ts packages/ledger/test/current-recovery-seal-journal.test.ts packages/cq-config/test/expectedFailurePolicy.test.ts`:
  73 pass, 0 fail, 524 assertions.
- `bun test packages/ledger/test/gateClosureRepository.test.ts packages/ledger/test/gateClosure.test.ts`:
  40 pass, 0 fail, 355 assertions after the source-digest refresh.
- `bun run typecheck && bun run lint`: exit 0 (`tsc -b`, then `eslint .`).

Typecheck first rejected an insufficient TypeScript narrowing for the selected
binding. Adding the explicit definedness precondition preserved runtime
semantics and made typecheck and lint green. Final source SHA-256 is
`11baa0e482e1af9e7419677294f64003a19beb3f400c0e20960a9e67d8771986`.

The parent supplied unchanged six-file host evidence at 129 pass / 0 fail with
972 assertions, including the native coordinator and real SQLite reopen path;
this worker did not rerun that host-only process boundary or any full gate.

## round 6 red-successor reproduction <!-- cq:wip-checkpoint -->

Added the literal `tasks:T6573` annotation, live `test.failing` marker, and
inventory entry for `a sealed recovery successor admits one exact changed gate
correction`. The public capability lifecycle uses a real managed Git worktree
and broker receipt: an unqueued source aborts `parent-lost`, current recovery
seals it, its first successor qualifies, and the supervised runner returns 68
passes / 1 failure. The successor remains terminal `gate-rejected`.

The inverted selector passed 1 / 0 and the committed inventory policy passed
1 / 0. Replacing the marker temporarily with the same-titled plain test exited
1 at the expected pre-allocation fence:
`sealed red successor correction refused: the managed task lineage is sealed
for journal recovery`. The live marker was restored before persistence.

## round 6 corrective recovery fence <!-- cq:wip-checkpoint -->

The lineage-fence preflight now recognizes a bare `reprepareOf` only as a
potential authority, then admits it under the managed effect lock only when it
names the exact first post-seal successor and that row is an authenticated
`gate-rejected` implementation candidate. Authorization re-verifies the
committed fence and journal, current task specification, exact goal and
finalized manifest, managed binding and guarded bridge, live tip, retained
parent-gate rejection evidence, queue terminal/qualification identity, and the
complete real broker receipt closure. All failures remain the typed
`journal-recovery-required` refusal.

The same-titled test is now plain and its annotation/inventory entry are
removed. It is classified Behavioral-Active Effectual-GoodCommunication: it
uses the public capability boundary with a real local managed Git worktree and
broker receipts, while the gate runner remains a deterministic hand-written
dummy. The unqueued sealed source stays `parent-lost`; its first queued
successor stays `gate-rejected`; foreign source, mixed authority, and
changed-spec replay allocate nothing. The accepted correction adds a fresh
broker receipt before qualification, receives exactly one green gate, and does
not mutate the red predecessor.

Mutation observations on restored bytes (each mutation made the same-titled
plain selector fail at the sealed fence, 0 pass / 1 fail; restoration returned
1 pass / 0 fail with 14 assertions): removing the preflight authority arm,
inverting the authenticated red-reason guard, inverting the current task-spec
guard, inverting the sealed receipt-prefix guard, and inverting the managed
binding-digest guard. Restored SHA-256 values are
`eee1e1ab4fc2a3ff2dc1c31d6ff07f8ae6b671a3c403604ad45e259589360648`
for `dispatchCapability.ts` and
`fdc47840a76a58a8741b04706095bc9a5d9cfc7e0b3b0b69c8c79f7de60d03fa`
for the regression source.

## round 6 focused checks <!-- cq:wip-checkpoint -->

Measured through focused checks only. This dispatch did not run or request the
canonical full gate.

Green evidence on the committed correction:

- Recovery seal, epoch promotion, managed recovery, source selection, journal,
  and expected-failure policy suites: 73 pass / 0 fail / 524 assertions.
- Existing and sealed red-correction queue selectors: 2 pass / 0 fail / 35
  assertions. They observe exactly one red gate followed by one green gate.
- Cutover fence, continuation fence, and staged-rebase source suites: 17 pass /
  0 fail / 64 assertions.
- Repository gate-closure and closure contract suites: 40 pass / 0 fail / 355
  assertions after refreshing only the three stale source-digest declarations.
- `bun run typecheck && bun run lint`: exit 0 (`tsc -b`, then `eslint .`).

The exact six-file acceptance command reached 113 passes and 1 skip but exited
1 on the same three confined nested-process timeouts recorded in earlier
rounds: D362 (10 seconds), the installed staged-rebase handoff (10 seconds),
and the installed successor boundary (30 seconds), followed by that timed-out
fixture's bridge-directory cleanup race. The new sealed red-successor selector
passed in that run. The parent owns the previously green host-only process
boundary and any final gate.

## round 7 own-completion reproduction <!-- cq:wip-checkpoint -->

Added the plain public subprocess regression `candidate coordinator returns its
own completed handle before polling foreign work`. On unchanged product code,
the first attempt returned this request's exact `completed` handle, subsequent
attempts returned a foreign `qualified` front, and the selector exited 1 after
253 ms because the wrapper returned that foreign `blocked` acknowledgement
instead of its retained completion (0 pass / 1 fail). This reproduces the D514
parent observation without treating a polling timeout as success.

## round 7 coordinator completion correction <!-- cq:wip-checkpoint -->

The installed coordinator now returns immediately only when a `completed`
acknowledgement names its requested handle. Foreign completions retain the
existing drain loop, and pre-completion blocked leases retain the bounded wait.
The public queue acquisition now resolves the sole live partition lease before
the computed FIFO front: an exact expected candidate and holder replays that
lease, while a foreign expected candidate receives the live handle as a typed
`leased` block. This preserves the exact-handoff fence without allowing an
older stable enrollment correction to mask retained completion authority.

The two plain fail-first selectors now pass independently. The wrapper case is
1 pass / 0 fail / 2 assertions and makes one subprocess call. The queue case is
1 pass / 0 fail / 3 assertions and uses the in-memory adapter contract plus the
real completion/continuation transitions, including a foreign expected-handle
rejection before exact replay.

## round 7 queue-order hypothesis <!-- cq:wip-checkpoint -->

The public in-memory queue adapter confirmed the hypothesis. A completed older
enrollment was superseded by its prepared generation-2 correction; a different
candidate then completed and retained the sole live partition lease. Qualifying
the correction restored its older admission ordinal ahead of that lease. The
plain regression exited 1 because acquisition for the retained candidate
returned the correction as a foreign `qualified` block instead of replaying the
exact live lease (0 pass / 1 fail, 2 assertions). No queue policy was changed
before this observation.

## round 7 focused checks <!-- cq:wip-checkpoint -->

Measured through focused checks only. This dispatch did not run or request the
canonical full gate.

Green final evidence:

- Coordinator live-block, foreign-drain, own-completion, and competing-lease
  selectors: 5 pass / 0 fail / 13 assertions.
- Queue and candidate-reuse suites: 26 pass / 0 fail / 138 assertions.
- Repository gate-closure and closure contract suites: 40 pass / 0 fail / 355
  assertions after refreshing only the two `codexRoleBoundary` declarations
  and two matching test declarations.
- `bun run typecheck && bun run lint`: exit 0 (`tsc -b`, then `eslint .`).

Mutation evidence on restored bytes:

- Inverting the own-completion generation equality made its selector poll and
  return the foreign qualified block (0 pass / 1 fail). Restoration returned 1
  pass / 0 fail and source SHA-256
  `ae29779fc201a9b817597dbe1ea037bdbea293edfe57c3ae7873955579b99b81`.
- Inverting the exact live-lease candidate comparison made the valid replay
  return a `leased` block (0 pass / 1 fail). Restoration returned 1 pass / 0
  fail and source SHA-256
  `43608fbda58b80ab8393a0a2798b594f3450616574f7df788f9a76aaf2baf821`.
- Disabling the foreign expected-candidate fence transferred the retained live
  lease to the foreign request (0 pass / 1 fail). Restoration returned 1 pass /
  0 fail / 3 assertions with the same queue source SHA-256.

## round 8 resolver admission reproduction <!-- cq:wip-checkpoint -->

Added the literal `tasks:T6573` annotation, live `test.failing` marker, and
inventory entry for `an authenticated sealed staged-rebase conflict admits one
resolver and guarded successor`. The paired memory/SQLite regression seals a
recoverable source, prepares its worker successor, advances the protected head,
retires that successor into a real guarded conflict, reopens SQLite, and crosses
the public `worktree_manage` observe/recover operations into resolver prepare.

The inverted selector passed 1 / 0 and the committed inventory policy passed
1 / 0. Replacing the marker temporarily with the same-titled plain test exited
1 after 10 assertions at the expected pre-allocation fence:
`sealed conflict resolver refused: journal-recovery-required: the managed task
lineage is sealed for journal recovery`. The live marker was restored before
persistence; no product source had changed.

## round 8 authenticated resolver admission <!-- cq:wip-checkpoint -->

The lineage fence now admits a conflict resolver only under the existing
manager effect lock. It requires no worker continuation authority, one unique
`staged-rebase-retired` source at exactly the fence successor generation, the
same manager/repository/queue attempt, matching guarded reference and journal
digest, unchanged protected head, a still-pending reconciled journal, and an
exact live conflict-state digest. Once the journal is terminal, only the exact
already-prepared resolver idempotency replay remains admissible; a new resolver
still fails closed.

The same-titled test is plain and its expected-failure annotation and inventory
entry are removed. It passes both memory and SQLite reopen paths with 30
assertions, then prepares the ordinary guarded worker successor without running
a gate. Foreign task, changed conflict transaction, and substituted worker
authority requests allocate no row.

Mutation observation: disabling only the new resolver admission arm returned
the exact original `journal-recovery-required` refusal (0 pass / 1 fail after
10 assertions). Restoring it returned 1 pass / 0 fail / 30 assertions. Restored
SHA-256 values are
`7dedf0f340952b34eaea3fa05742a7311055d5fc8e1ba39a99c14268dcb6eb58`
for `dispatchCapability.ts` and
`a937dc82947c4a5fb93ca342c2487c910b8d62c94eb6ef93a34c38d8e97bc3b8`
for the regression source.

## round 8 focused checks <!-- cq:wip-checkpoint -->

Measured through focused checks only. This dispatch did not run or request the
canonical full gate.

Green final evidence:

- Recovery seal, manager recovery, current-journal, cutover-fence,
  conflict-capability, and expected-failure policy suites: 64 pass / 0 fail /
  438 assertions.
- Existing public staged-conflict memory/SQLite cases plus the new sealed
  resolver case: 3 pass / 0 fail / 84 assertions.
- Repository gate-closure and closure contract suites: 40 pass / 0 fail / 355
  assertions after refreshing only the changed source declarations.
- `bun run typecheck && bun run lint`: exit 0 (`tsc -b`, then `eslint .`).

The exact installed/native six-file command remains parent-owned under the
operator instruction because its nested subprocess cases time out in this
confined worker. No child full suite, Nix build, install, deployment, or source
gate was run.

## trusted full gate <!-- cq:wip-checkpoint -->

Unmeasured. Parent-owned; this focused-only worker will not run the canonical full gate.

## round 9 manual-rebase reproduction <!-- cq:wip-checkpoint -->

Added the paired in-memory and SQLite/reopen regression for the operator-reported
path: current-seal recovery allocates a successor, that successor aborts as
`cancelled`, and one public manual guarded rebase prepares and completes the
next generation without creating a staged-retirement source. Public
`resolve-dispatch-continuation` returned its exact clean tip and opaque
continuation reference, but the following prepare exited at the expected fence
with `journal-recovery-required` (0 pass / 1 fail before product edits). One
ordinary gate ran for the completed guarded successor and no second gate ran.

The persisted form-(a) marker is annotated `tasks:T6573` and has the matching
expected-failure inventory entry pending the correction.

## round 9 authenticated prepare composition <!-- cq:wip-checkpoint -->

The retained clean-manual control remains green. Its cancelled sealed source,
terminal guarded journal, consumed worker, and public continuation now pass by
the same authenticated ancestry mechanism as the automatic case; the source's
terminal cause is not relabeled and no staged-retirement row is manufactured.

## round 9 focused checks <!-- cq:wip-checkpoint -->

The retained clean-manual control passes beside the two separate round-10
cases. Focused-only validation remained in force; the trusted full gate is
parent-owned and unmeasured here.

## round 10 case A automatic-continuation reproduction <!-- cq:wip-checkpoint -->

Added the plain paired memory/SQLite regression `case A: an automatic
staged-rebase successor retains repeated authenticated continuations`. It
independently drives current recovery into an automatic guarded conflict,
resolves it, preserves the source as `staged-rebase`, commits and consumes the
guarded successor, and resolves its public continuation authority. Before the
correction, the selector exited 1 after 18 assertions at the intended fence:
`automatic continuation one refused: journal-recovery-required: the managed
task lineage is sealed for journal recovery`.

## round 10 case B pending-manual-conflict reproduction <!-- cq:wip-checkpoint -->

Added the separate plain paired memory/SQLite regression `case B: a cancelled
sealed source admits its exact pending manual conflict resolver`. It keeps the
sealed source terminal as `cancelled`, starts one manual guarded rebase that
remains stopped in conflict, reopens SQLite, observes the conflict through
`worktree_manage`, and attempts the exact resolver prepare with no staged
retirement source. Before the correction, the combined A/B selector exited 1
with B's intended failure:
`pending manual conflict resolver refused: journal-recovery-required: the
managed task lineage is sealed for journal recovery`. B ran no gate and stopped
before conflict continuation, worker continuation, or terminal rebase.

## round 10 authenticated continuation correction <!-- cq:wip-checkpoint -->

Replaced the interrupted `fenceMaximum + 2` allowance with a durable ancestry
walk. The walk anchors at the exact current-recovery generation and follows
only server-persisted continuation claims, staged-source successor bindings,
or an adjacent guarded successor whose terminal journal, manager binding,
task specification, receipt prefix, source tip, onto commit, and bridge all
re-verify. Two consecutive public continuations pass; an unrelated generation
100 request allocates no row.

For the still-conflicted manual case, the ledger now resolves exactly one
nonterminal guarded journal for the live manager and observed conflict without
returning its path or operation id. Admission rechecks the self-authenticating
request digest, complete manager identity, conflict digest/identity/head,
original tip, onto commit, cancelled current-recovery anchor, current protected
head, and absence of staged retirement. Changed task, conflict, onto,
worktree, and substituted authority requests allocate nothing. The real-Git
journal suite also rejects a foreign manager and a duplicated pending journal.

The A/B selectors and the retained clean-manual control pass 3 / 0 with 114
assertions. `guardedRebaseContinuation.test.ts` passes 5 / 0 with 38
assertions. `bun run typecheck` and `bun run lint` both exit zero. Repository
gate closure passes 40 / 0 after refreshing only the affected source-digest
declarations.

An affected recovery-epoch regression exposed a required authentication path
through the predecessor tombstone and a v1 journal seal when no live ledger
store is present. The ancestry walk now retains tombstones for sealed-bridge
verification, derives the task-specification digest from the authenticated v1
seal input recipe on that construction path, and still considers only live
envelopes as continuation nodes.

## round 10 focused checks <!-- cq:wip-checkpoint -->

Focused-only validation completed without a full suite, Nix build,
installation, or trusted full gate:

- Case A, Case B, and retained clean-manual control: 3 pass / 0 fail / 114
  assertions.
- Recovery seal, epoch promotion, managed recovery, recovery-source,
  current-journal, guarded-rebase journal, expected-failure policy, CLI parse,
  and production runtime suites: 109 pass / 0 fail / 1 declared PostgreSQL
  construction skip / 623 assertions.
- Repository gate closure and closure contract: 40 pass / 0 fail / 355
  assertions.
- `bun run typecheck && bun run lint`: exit 0.

Fresh fail-first mutation evidence was restored before the final checks:

- Accepting duplicate pending guarded journals made the duplicate negative
  control resolve instead of reject; restoration returned its selector green
  and restored `guardedRebaseContinuation.ts` to SHA-256
  `f652f6bcaae7d6807ef7688c6297a2f7d678ffd8ae291f083f15ebdcbc23c8c8`.
- Disabling the staged-predecessor ancestry edge made Case A fail at the
  recovery fence; restoring it returned Case A green.
- Changing the adjacent manual guarded generation edge made the retained clean
  control fail at the recovery fence; restoring it returned the control green.
- Changing Case A's expected gate count from 2 to 3 failed with an observed
  count of 2; restoring the assertion returned Case A green and restored the
  regression source to SHA-256
  `cc69c770f49239d8ba9f69d3d914249346a16ea168e2b1a359e6022f464c02e6`.
- Disabling v1 seal-input authentication made epoch-promotion generation 20
  fail with `journal-recovery-required`; restoration returned 1 pass / 0 fail /
  26 assertions and restored `dispatchCapability.ts` to SHA-256
  `f8d44e21386b5271efce0593cd62cacfd234a6eb3358967baee82f3cda1fb6eb`.

## round 11 diagnostic opacity reproduction <!-- cq:wip-checkpoint -->

Added public-boundary assertions for changed continuation task specification,
an unresolved continuation reference, a changed pending-conflict snapshot,
and substituted resolver authority. The combined Case A/B selector exited 1
with both cases observing the same generic detail, `the managed task lineage is
sealed for journal recovery`, rather than their expected distinct closed
stage/reason codes. Both observed failures retained `allocated: false` (0 pass,
2 fail, 28 assertions), reproducing the diagnostic opacity before product
edits.

## round 11 safe rejection diagnostics <!-- cq:wip-checkpoint -->

The existing `journal-recovery-required` refusal now appends only closed
`stage` and `reason` codes for continuation and pending-conflict admission.
The diagnostic paths distinguish journal, manifest, anchor, snapshot,
specification, manager, bridge, ancestry, reconciliation, and caught-exception
failures. They do not include caught messages or request/store values, and the
underlying authorization predicates remain unchanged.

The same combined Case A/B selector now passes 2 / 0 with 108 assertions. It
observes four distinct public diagnostics, verifies `allocated: false`, and
checks that the serialized refusals omit the continuation reference, manager
token/path, protected head, and source tip. `bun run typecheck` and `bun run
lint` both exit zero.

## round 11 focused checks <!-- cq:wip-checkpoint -->

Completed. This dispatch is focused-only and did not invoke the canonical
full gate.

The repository closure first failed on the intentionally stale source digest.
After refreshing only the `dispatchCapability.ts` declaration and the two
matching `supervisedWorkerGateStorage.test.ts` declarations, the repository
closure and closure-contract suites pass 40 / 0 with 355 assertions.

Mutation evidence: replacing the public diagnostic suffix with the old generic
detail made both Case A and Case B fail at their distinct-detail assertions (0
pass / 2 fail / 28 assertions). Restoring the suffix returned 2 pass / 0 fail /
108 assertions and restored `dispatchCapability.ts` to SHA-256
`ac9e31a59b5e6e8e6572e24ccef1935ff8c0e8ef84e0760a41a38bd0ed385eb8`.

Final focused evidence:

- Case A, Case B, and the retained clean manual guarded continuation: 3 pass /
  0 fail / 142 assertions.
- Recovery seal, epoch promotion, managed recovery, and guarded-rebase journal
  suites: 45 pass / 0 fail / 259 assertions.
- Dispatch capability suite: 23 pass / 0 fail / 118 assertions.
- Repository gate closure and closure contract: 40 pass / 0 fail / 355
  assertions.
- `bun run typecheck && bun run lint`: exit 0 (`tsc -b`, then `eslint .`).

No full suite, Nix build, installation, deployment, private-primary
inspection, or retired source gate was run.

## round 12 guarded bridge persistence <!-- cq:wip-checkpoint -->

The guarded-rebase bridge starts from
`20cf63af87fb063b93a537521d4e6f293c356766` over base
`9272de7b3cd5cf8ebe41abb42c514b8421cb3f9b`. The dispatched correction keeps
the three public compositions separate: canonical refs-only automatic
continuation, a cancelled recovered worker with broker commits before a manual
conflict, and a consumed automatic successor whose gate-rejected continuation
requires a focused correction. Their fail-first evidence, fused correction,
and focused-only checks remain open. The trusted full gate remains parent-owned
and unmeasured.

## round 12 composition A reproduction <!-- cq:wip-checkpoint -->

The paired memory/SQLite automatic staged-rebase fixture now continues its
consumed guarded successor through the canonical refs-only request. On unchanged
product code, the selector exited 1 before allocation with
`stage=continuation-snapshot; reason=input-shape-invalid`: recovery-fence
authorization saw the caller envelope before refs assembly supplied its
validated role input.

## round 12 composition B reproduction <!-- cq:wip-checkpoint -->

The cancelled recovered worker now persists a real broker commit before the
manual guarded rebase stops in conflict. The observed journal original tip is
that receipt's new head, not the worker input's starting commit. On unchanged
product code, the paired selector exited 1 before resolver allocation with
`stage=pending-conflict-anchor; reason=anchor-source-mismatch`.

## round 12 composition C reproduction <!-- cq:wip-checkpoint -->

The separate paired composition drives a sealed staged-rebase source through
its genuine automatic guarded successor, consumes that successor after one
green runner gate, then runs an ordinary continuation through a genuine red
runner gate. Its changed `focused-only` correction names the exact rejected
continuation. On unchanged product code, preparation exited 1 at the sealed
recovery fence before allocation; the runner had been invoked exactly twice.

## round 12 fused correction <!-- cq:wip-checkpoint -->

Refs-only preparation now assembles and validates its canonical role input
before every recovery-fence authorization check; caller-envelope fingerprints
remain distinct from the server-assembled dispatch request. Cancelled anchors
may reach a pending manual conflict only when their exact inherited-plus-current
broker receipt chain closes from the authenticated starting commit to the
journal's original tip. Later gate-rejected descendants traverse the existing
continuation/staged-successor ancestry to the recovery anchor and independently
revalidate their durable receipt closure.

The attestation prepare boundary receives one server-injected
`gateRejectedCorrectionClaim` only after those checks. The claim binds the
fence, exact rejected source, result tip, receipt-lineage digest, and guarded
bridge digest into the request fingerprint. The lower allocation layer checks
the retained red terminal, manager binding, queue evidence, bridge identity,
receipt-prefix closure, and live starting tip before distinguishing the
correction from a second staged-rebase successor. It persists no caller-minted
authority and does not alter the staged source or run another recovery gate.

The paired case C control observes one green gate for the automatic guarded
successor, one real red gate for its ordinary continuation, and zero further
runner invocations for the changed focused-only correction. Changed task input,
a foreign source generation, and a stale tip all reject before allocation.

## round 12 focused checks <!-- cq:wip-checkpoint -->

Completed through focused checks only. This dispatch did not invoke the
canonical full gate.

Green evidence on restored, formatted bytes:

- Case A refs-only continuation, case B broker-advanced manual conflict, and
  case C rejected-continuation correction: 3 pass / 0 fail / 176 assertions
  across paired memory and SQLite paths.
- Recovery seal, epoch promotion, managed recovery, candidate enrollment/reuse,
  CLI parsing, production runtime, source selection, current journal, guarded
  rebase, and continuation-fence controls: 114 pass / 0 fail / 1 declared
  PostgreSQL skip / 464 assertions.
- Attestation lifecycle, staged-rebase source, and Git broker/correction suites:
  139 pass / 0 fail / 1102 assertions.
- Repository gate closure and closure contract after refreshing only the three
  affected opaque-edge source digests: 40 pass / 0 fail / 355 assertions.
- Coordinator boundary selectors: 4 pass / 0 fail / 8 assertions.
- `bun run typecheck && bun run lint`: exit 0 (`tsc -b`, then `eslint .`).

Fresh mutation observations, each restored before the green checks:

- Replacing canonical refs authorization with the raw caller envelope made
  case A fail with `stage=continuation-snapshot; reason=input-shape-invalid`.
- Restoring the cancelled anchor's old start-equality shortcut made case B fail
  with `stage=pending-conflict-anchor; reason=anchor-source-mismatch`.
- Restoring the first-successor-only generation fence made case C reject the
  exact later red continuation before allocation.
- Inverting the lower bridge-identity claim guard made case C fail with
  `AttestationBindingError` at `gateRejectedCorrectionClaim`.

The first three mutations restored `dispatchCapability.ts` to SHA-256
`95772dec322c96b440f5365556c324e6c3eefa9b097d35620e0e299090bbbd1c`;
the claim mutation restored `dispatchAttestation.ts` to SHA-256
`08ccaf4a80d1ef6f6b80320e6b078c221afa24e26d49373c1d7f4eb8d2310bda`.
The regression source remained at SHA-256
`ab2211e6eb04bb54ba454f6fb8bafb972faadb0ddccf4108a1a5d80c3cf59c66`
during mutation checks; formatting then produced the final checked bytes.

The full rendered-envelope subprocess selector was not accepted as evidence: it
reached the already documented 10-second nested-process confinement timeout.
No Nix build, installation, deployment, full suite, trusted full gate, private
store edit, or retired source gate was run.
