```json
{
  "taskId": "T1952",
  "role": "implement-worker",
  "baseCommit": "14314c56243240a91577da6af3f7d61bd4648e22",
  "startedAt": "2026-08-08T14:07:40Z",
  "checkpoints": [
    {"name": "base-verification", "status": "done"},
    {"name": "workset-graph-module", "status": "done"},
    {"name": "workset-projection", "status": "done"},
    {"name": "acceptance-tests", "status": "done"},
    {"name": "mutation-evidence", "status": "done"},
    {"name": "full-gate", "status": "done"}
  ]
}
```

## base-verification <!-- cq:wip-checkpoint -->

Verified on dispatch:
- actualWorktreePath: `/home/pavel/work/safe/flakes/cq/.claude/worktrees/019fe1af-bfd0-73d1-9d66-678faf31e4fc`
- branch: `implement/T1952`
- HEAD == startingCommit == baseCommit `14314c56243240a91577da6af3f7d61bd4648e22`
- ancestry: base is ancestor of HEAD (equal)

## workset-graph-module <!-- cq:wip-checkpoint -->

Added `packages/ledger/src/worksetGraph.ts`:
- `closeWorkset` / `canonicalizeWorksetRoots` / `buildWorksetActiveState`
- prerequisite traversal, sealed owner-edge expansion via T1951 matrix
- phase-aware draft vs finalized manifest
- explicit-milestone root asymmetry
- inactive roots remain restrictive
- `projectWorkset` delegates to G139 `projectItemDto`

## workset-projection <!-- cq:wip-checkpoint -->

`WorksetProjection = "id" | ItemProjection`; id emits refs only.

## acceptance-tests <!-- cq:wip-checkpoint -->

47 pass / 0 fail across the five acceptance files.

## mutation-evidence <!-- cq:wip-checkpoint -->

File hash `b13065574ac0ea4e2858031e95b7160a1174f2dcb4609fe5e245624b34db3907` before/after mutations (match).

| guard | mutation | fail observed | restore pass |
| --- | --- | --- | --- |
| prereq edge direction | reverse `addEdge` endpoints | fail: expected T1→Q1 got Q1→T1 | pass |
| restrictive inactive roots | `restrictive = activeRootRefs.length > 0` | fail: restrictive false | pass |
| G139 compact byte equality | compact projected as full | fail: narrative leaked into compact | pass |

## full-gate <!-- cq:wip-checkpoint -->

`cq gate run --worktree $PWD --command-cwd $PWD/nix/pkg/cq-ledgers -- bun run check`
REAL_CHECK_EXIT=0, gateDurationMs=573160.
