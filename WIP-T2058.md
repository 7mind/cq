```json
{
  "taskId": "T2058",
  "role": "implement-worker",
  "baseCommit": "8e899ed80feee0e41d44dd4489a9b5fd595cb665",
  "startedAt": "2026-08-11T13:23:00Z",
  "checkpoints": [
    { "name": "reproduction", "status": "done" },
    { "name": "implementation", "status": "done" },
    { "name": "mutation-proof", "status": "done" },
    { "name": "focused-checks", "status": "done" },
    { "name": "live-postgres", "status": "unmeasured" },
    { "name": "full-gate", "status": "unmeasured" }
  ]
}
```

## reproduction <!-- cq:wip-checkpoint -->

D312 reproduced before production changes with:

`bun test packages/ledger/test/operator-actions.test.ts --test-name-pattern 'D312 acknowledged pre-evidence'`

Observed exit 1 and `OperatorActionConflictError: Operator action OA1 conflicts with the requested materialization: expectedEvidence differs` after exact acknowledgement and before any evidence.

## implementation <!-- cq:wip-checkpoint -->

Revision-aware action/task/handoff mutation now runs through the authoritative
store transaction on in-memory, filesystem, SQLite, and PostgreSQL adapters.
The MCP, remote client, parent-only profile, generated prompt surface, ledger
schema, and operator documentation expose the revision lifecycle. D313's
camelCase lifecycle-recovery parser defect was reproduced and corrected.

## mutation-proof <!-- cq:wip-checkpoint -->

Disabling revision CAS made the stale-call selector fail 3/3, and disabling the
post-evidence guard made the same selector fail 3/3. After each restoration,
the lifecycle source and test SHA-256 values matched their pre-mutation values;
the restored selector passed 3/3.

## focused-checks <!-- cq:wip-checkpoint -->

Typecheck and lint exited 0. The operator/profile/cross-surface batch passed
69 tests with one intentional on-demand skip; MCP/direct parity passed 81 tests;
the remote/client server batch passed 26 tests with only the environment-gated
PostgreSQL contract skipped.

## live-postgres <!-- cq:wip-checkpoint -->

The tracked candidate still needs the required PostgreSQL Nix fixture check.

## full-gate <!-- cq:wip-checkpoint -->

The authoritative full gate has not run yet.
