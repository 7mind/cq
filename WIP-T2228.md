```json
{
  "taskId": "T2228",
  "role": "implement-worker",
  "baseCommit": "6ab77afd8968492bdd2e56121101075f0276aadf",
  "startedAt": "2026-08-18T09:25:09Z",
  "checkpoints": [
    { "name": "deployment-boundary preparation", "status": "done" },
    { "name": "timing calculators and deadline propagation", "status": "done" },
    { "name": "expiry cleanup and diagnostics", "status": "done" },
    { "name": "guard mutation evidence", "status": "done" },
    { "name": "targeted acceptance tests", "status": "done" },
    { "name": "live-parent selector", "status": "done" },
    { "name": "build and full gate", "status": "unmeasured" }
  ]
}
```

## deployment-boundary preparation <!-- cq:wip-checkpoint -->

The manager prepared this worktree at the sole Git dependency contribution
`6ab77afd8968492bdd2e56121101075f0276aadf`; completed external-effect
dependencies did not require a `resultCommit` to admit this dispatch.

## timing calculators and deadline propagation <!-- cq:wip-checkpoint -->

Added a checked, ordered timing basis. It derives the 300,000 ms launch phase,
3,960,000 ms store-result phase and 3960-second MCP timeout, 300,000 ms terminal
reserve, 4,560,000 ms outer reserve, and 9,611,000 ms parent window. The
installed preturn now exposes every contributing phase.

## expiry cleanup and diagnostics <!-- cq:wip-checkpoint -->

One absolute deadline now flows through broker admission, provider-control
acquire/register/share exchanges, registered-launch identity and bootstrap
handshakes, and the Codex launch boundary. Expiry abandons an unregistered
admission or settles a registered group, then drains provider/bootstrap output.

## guard mutation evidence <!-- cq:wip-checkpoint -->

Pre-correction reproduction:

`bun test packages/cq-config/test/codexRoleBoundary.test.ts packages/process-control/test/worksetEffectBroker.test.ts packages/process-control/test/processWorksetEffectAdmissionProvider.test.ts packages/ledger-mcp/test/supervisedWorkerGateStorage.test.ts -t 'D343 staged timing basis|D343 bounded child launch|D343 scaled external phases'`

exited 1 because the selector matched zero tests. The existing D343 test passed
while observing the obsolete 600,000 ms staging, 900,000 ms outer reserve, and
5,620,000 ms parent values.

Observed fail/restore controls from this run:

- Disabled timing phase-order validation: the D343 timing selector exited 1
  because `source order` was no longer thrown. Restored source SHA-256:
  `67ba3a2aa08b363b63fba7fc90254fa3d9b9748c3adaa17e53c661218a6b5277`;
  the selector then passed 1 test / 10 assertions.
- Removed the admission deadline from the broker's acquisition wait: the D343
  broker selector exited 1 because admission was released after expiry.
  Restored source SHA-256:
  `5769229ada621e75573b71bb6d755c8b4ca42c096c2f426afe9248ded7e6ae49`;
  the selector then passed 1 test / 12 assertions.
- Changed the registered cleanup assertion to expect one active admission: the
  D343 broker selector exited 1 after observing zero. Restored test SHA-256:
  `14168195c53030bed9dd8b0814113db309431cd8b6e2858977f3afa2d924924e`;
  the selector then passed 1 test / 12 assertions.
- Removed Codex-to-broker deadline propagation: the Codex D343 launch selector
  exited 1 because admission released before rejection. Restored source
  SHA-256:
  `0b7f34844254092743b4cfe78159b01f30631c3dc8e688f2f6fcdcc02cd5e9be`;
  the selector then passed 1 test / 4 assertions.
- Shortened the ledger acquisition expectation by one millisecond: the storage
  selector exited 1 after observing 3,600,000. Restored test SHA-256:
  `f8f1f63ddcbf3baa5f8623ad94ea6c0a61203104598f4c52810174c12404cea2`;
  the selector then passed 1 test / 4 assertions.
- Removed `D343 live parent` from the new test title: the live-parent selector
  exited 1 with `matched 0 tests`. Restored test SHA-256:
  `c87d0e57e7e16812eef18c8765647f661f417160d5e1008c96ecb8036a5ec69b`;
  the selector then passed with 0 pass / 1 skip / 0 fail.
- Ungated the new test (`deployedTest` to plain `test`): the live-parent
  selector ran the body and exited 1 with 1 fail inside
  `proveInstalledBootstrap`, because the installed `cq-codex-role` still emits
  the obsolete 600,000 ms staging preturn. Restored test SHA-256:
  `c87d0e57e7e16812eef18c8765647f661f417160d5e1008c96ecb8036a5ec69b`;
  the selector then passed with 0 pass / 1 skip / 0 fail.

Provider-control mutation execution is unavailable in this child sandbox
because all nested Bun stdio is suppressed; that gap is delegated to the
trusted host gate rather than reported as observed evidence.

## targeted acceptance tests <!-- cq:wip-checkpoint -->

The acceptance D343 selector exits 0 in this environment with 5 pass, 40
assertions, 0 fail — including the provider-control leg
(`D343 scaled external phases terminate and drain every expired
provider-control exchange`, 2,492 ms), which an earlier sandboxed generation
could not observe. `bun run typecheck` and `bun run lint` exit 0.

## live-parent selector <!-- cq:wip-checkpoint -->

Pre-correction reproduction:

`bun test packages/cq-config/test/codexMcpTimeoutIntegration.test.ts -t "D343 live parent"`

exited 1 with `matched 0 tests`. Added a separately selected
`CQ_TEST_DEPLOYED_CQ`-gated test whose title contains `D343 live parent`;
when enabled it proves loopback listener authority (Bun.serve on 127.0.0.1:0
plus a fetched probe), prints the installed `cq`/`cq-codex-role` paths, and
reuses the existing installed staged-parent helper. The selector now exits 0
with 0 pass / 1 skip / 0 fail without the env var; no zero-skip claim is made
before deployment.

## build and full gate <!-- cq:wip-checkpoint -->

`bun run typecheck` and `bun run lint` pass. Both required Nix build attempts
were blocked before evaluation: the default cache is outside the sandbox, and
with an in-sandbox cache the Nix daemon socket is denied. The trusted host gate
must run the Nix-backed full check.
