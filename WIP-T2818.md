```json
{
  "taskId": "T2818",
  "role": "implement-worker",
  "baseCommit": "4b459eb0db16271bee740b4468dd05524fa028c9",
  "startedAt": "2026-08-27T06:22:19Z",
  "checkpoints": [
    { "name": "reproduce D367 recovery failure", "status": "done" },
    { "name": "implement recovery-stable identity", "status": "done" },
    { "name": "reproduce D366 guarded-merge failure", "status": "done" },
    { "name": "scope guarded-merge WIP closure", "status": "done" },
    { "name": "reproduce D368 envelope detection failure", "status": "done" },
    { "name": "restrict operator envelope detection", "status": "done" },
    { "name": "prove changed guards", "status": "done" },
    { "name": "focused checks", "status": "done" },
    { "name": "refresh gate closure digest", "status": "done" },
    { "name": "prove focused closure check", "status": "done" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## reproduce D367 recovery failure <!-- cq:wip-checkpoint -->

Observed before production edits: the generation 17 → WIP → generation 18 promotion
sequence failed with `CurrentRecoverySealError` reason `journal-conflict` because the
full task digest changed. The focused run reported 27 pass, 1 fail.

## implement recovery-stable identity <!-- cq:wip-checkpoint -->

`currentRecoveryTaskEvidence` now hashes a versioned projection of immutable task
identity: id, milestone, all task fields, finalized goal reference, and exact manifest
membership. Mutable status, timestamps, author, and session are excluded.

## reproduce D366 guarded-merge failure <!-- cq:wip-checkpoint -->

Observed before production edits: an unchanged tracked `WIP-T2234.md` inherited from
integration history was rejected by `assertManagedWorktreeWipClosure` as a foreign
artifact.

## scope guarded-merge WIP closure <!-- cq:wip-checkpoint -->

Closure now obtains integration `HEAD`, verifies candidate ancestry, diffs that base to
the immutable candidate, and parses only changed root `WIP-*.md` artifacts. Unchanged
tracked artifacts are ignored; introduced and modified foreign artifacts remain denied.

## reproduce D368 envelope detection failure <!-- cq:wip-checkpoint -->

Observed before production edits on the in-memory, filesystem, and SQLite adapters:
ordinary inline prose threw `OperatorActionEnvelopeError` at creation. The focused
run reported 30 pass, 3 fail.

## restrict operator envelope detection <!-- cq:wip-checkpoint -->

Envelope parsing returns ordinary-task semantics unless the description begins with the
exact marker. Once prefixed, malformed and duplicate envelopes still fail closed.

## prove changed guards <!-- cq:wip-checkpoint -->

Mutation evidence from this run:

- Recovery identity: restored SHA-256 `ebc5fd59bcb044fcf8584c617a6b45d83b96308d7e83b10e99a06bc11db8d94e`;
  the full-task mutation hash `c3acf537c19cc4c80a627681d5e3ebeae6e48344269786bb00e20effe3d129b0`
  failed the T2345 promotion with `journal-conflict`, then the restored test passed 1/1.
- Candidate WIP filtering: restored SHA-256 `de7e66aa7d95e904712fc34976c2e22a701a403b424a3882205de4e8875c60d4`;
  removing the filter (`0e12c84d...`) rejected inherited WIP, and inverted ancestry
  (`736f075c...`) and diff-result (`3a1a73f9...`) guards each failed the focused test.
  The restored test passed 1/1.
- Operator prefix detection: restored SHA-256 `e2b22f3e7db358e6db97914acec3e1c4a79a13befbafe7c86686159aa8c1489e`;
  removing the prefix-first guard (`d63e2546...`) failed all three adapter cases, then
  the restored run passed 3/3.

## focused checks <!-- cq:wip-checkpoint -->

Final focused run: 82 pass, 0 fail, 792 expectations across the four exact test
files. `tsc -b`, ESLint on all edited TypeScript files, Prettier check, and
`git diff --check` all exited zero.

## refresh gate closure digest <!-- cq:wip-checkpoint -->

Round 1 correction: trusted gate reported the declared source digest for
`dispatchRecoveryEpochPromotion.test.ts` as stale. Comparing all 359 declared
sources found five additional stale declarations for files changed by T2818;
all six declarations now match the current source bytes.

## prove focused closure check <!-- cq:wip-checkpoint -->

Fail-first observation: the exact repository closure test failed 0 pass / 1 fail
with `source-digest-mismatch` for `dispatchRecoveryEpochPromotion.test.ts`
(declared `fd4753cd...`, actual `591b2a97...`). After refreshing every stale
T2818 declaration, the same test passed 1/1 with one expectation in 42.46s.
Manifest SHA-256 changed from `8e1d47c38c37fd03619013212025ece36cebf62ea6afdbc175de4f62b3a80527`
to `c22226e286c503700c76b317862f410484e2dd821bc420b2acf70c902d2b4f16`.

## trusted full gate <!-- cq:wip-checkpoint -->

Pending trusted parent execution.
