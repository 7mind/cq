```json
{
  "taskId": "T2110",
  "role": "implement-worker",
  "baseCommit": "09731511f5388b9aa0f3a69105bec07da490c3d1",
  "startedAt": "2026-06-14T00:00:00Z",
  "checkpoints": [
    { "name": "base-verification", "status": "done" },
    { "name": "run-acceptance-selectors", "status": "done" },
    { "name": "audit-exports-and-mcp-surfaces", "status": "done" },
    { "name": "fix-only-if-selectors-fail-for-stated-reasons", "status": "done" },
    { "name": "full-gate", "status": "todo" }
  ]
}
```

## base-verification <!-- cq:wip-checkpoint -->

- toplevel: /home/pavel/work/safe/flakes/cq/.claude/worktrees/01a01997-cb2a-71d0-ac51-70fa2f7ee67f
- branch: implement/T2110
- HEAD == startingCommit == baseCommit == 09731511f5388b9aa0f3a69105bec07da490c3d1
- baseCommit is a commit object; ancestor of HEAD. Relation: equal. Tree clean.

## run-acceptance-selectors <!-- cq:wip-checkpoint -->

From nix/pkg/cq-ledgers:
`bun test packages/ledger/test/plan-lifecycle-mcp.test.ts packages/ledger/test/workset-plan-lifecycle-direct.test.ts packages/ledger/test/workset-plan-lifecycle-stdio.test.ts packages/ledger/test/remote-ledger-client-workset-plan-lifecycle.test.ts packages/ledger-mcp/test/workset-plan-lifecycle-http.test.ts packages/ledger-mcp/test/workset-transport-parity.test.ts`
→ exit 0, "28 pass / 0 fail / Ran 28 tests across 6 files" (all six named files appear in the run header). Log: /tmp/t2110-selectors.log.

## audit-exports-and-mcp-surfaces <!-- cq:wip-checkpoint -->

Conclusion: current main ALREADY satisfies every acceptance clause; no product edits made.

1. Exactly four guarded plan-publication mutations on every advertised surface:
   - Single source `PLAN_LIFECYCLE_TOOL_SPECS` = [claim_plan, publish_plan_draft,
     release_plan_claim, finalize_plan] in packages/ledger/src/mcp/planLifecycleTools.ts:55.
   - Assembled once in `createLedgerMcpToolSpecifications` (ledgerTools.ts:1406), consumed by
     direct (`createLedgerMcpTools`), stdio (`registerLedgerStdioTools`), and HTTP
     (`createLedgerMcpServer`/`attachMcpHttp` in packages/ledger-mcp/src/main.ts) — parity is
     structural, not duplicated.
   - Canonical capability inventory `LEDGER_CAPABILITY_TOOL_NAMES`
     (packages/cq-config/src/roleToolProfiles.ts:12) lists exactly those four plan tools and no
     other plan-publication operation.
   - `RemoteLedgerClient` implements exactly claimPlan/publishPlanDraft/releasePlanClaim/
     finalizePlan (RemoteLedgerClient.ts:509-522) and nothing else plan-publication-shaped.
   - Grep for removed owned-draft publication names (publish_owned*, owned_*draft*, …) across
     all packages' src and test trees: zero hits. Transports neither advertise nor reach it.
2. Generic and owned-write handlers reject managed plan fields before store access:
   - packages/ledger/src/store/planLifecycleGuards.ts: `assertRawPlanCreateAllowed` rejects
     PLAN_MANAGED_GOAL_FIELD_NAMES on goal create and managed-goal ledgerRefs on task create;
     `assertRawPlanUpdateAllowed` rejects managed goal fields / sealed milestones / lifecycle-
     owned transitions; `assertManagedTaskTransitionAllowed` rejects draft/superseded-manifest
     task transitions.
   - Wired into EVERY backend before persistence: InMemoryLedgerStore.ts:579/609/673/703/737/947,
     SqliteLedgerStore.ts:1116/1151/1218, PostgresLedgerStore.ts:1435/1521/1639,
     AbstractLedgerStore.ts:767/809/903, plus the guarded mutation transactions
     genericMutationTransaction.ts:192/211/270 and ownedWriteTransaction.ts:131/161/189.
   - Owned-write gateway additionally rejects forged ownership fields (`assertNoForgedOwnership`,
     worksetOwnedLifecycle.ts:265) at the entry of createOwned/update/bundle handlers, and rejects
     creationKind active-current-draft|finalized-manifest with `owner-policy-denied`
     (worksetOwnedLifecycle.ts:399-406) BEFORE admission and before any store access.
3. Exported owned-write inputs cannot select either manifest creation kind:
   - `OwnedCreateInput.creationKind: WorksetOwnedWriteCreationKind`
     (worksetOwnedLifecycle.ts:124-128), a closed enum `WORKSET_OWNED_WRITE_CREATION_KINDS`
     (worksetOwnedLifecycle.ts:~783-806) that excludes both active-current-draft and
     finalized-manifest — compile-time exclusion plus the runtime `owner-policy-denied` guard.
   - Both are exported from packages/ledger/src/index.ts:260-293.
4. Guarded lifecycle owner-edge types retain both kinds:
   - `PLANNING_LIFECYCLE_CREATION_KINDS` and `WORKSET_OWNER_EDGE_KINDS` both contain
     active-current-draft and finalized-manifest (worksetOwnerEdges.ts:57-70, 96-110);
     `LifecycleCreationKind` union includes both via the planning inventory. Exported from
     index.ts:63-99.
5. Repeated legitimate publication preserves byte-stable acknowledgements (pinned green):
   - "recovers a lost claim response across retry, restart, and ordering on both transports"
     and "replays a scoped operation retry and rejects a changed one" in
     packages/ledger/test/plan-lifecycle-mcp.test.ts; idempotent replay returns the identical
     acknowledgement (replayed:true) and a changed payload conflicts.

## fix-only-if-selectors-fail-for-stated-reasons <!-- cq:wip-checkpoint -->

Not applicable: selectors passed on arrival and the audit found no gap. Zero product edits.
Only this file (WIP-T2110.md) was added.

## full-gate <!-- cq:wip-checkpoint -->

Pending: `cq gate run --worktree "$PWD" --command-cwd "$PWD/nix/pkg/cq-ledgers" -- bun run check`.
