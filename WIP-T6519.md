```json
{
  "taskId": "T6519",
  "role": "implement-worker",
  "baseCommit": "719126314750483cb306968e722bd4d6dd9e6f4d",
  "startedAt": "2026-09-16T14:02:17.795Z",
  "checkpoints": [
    { "name": "existing implementation survey", "status": "done" },
    { "name": "fail-first stored-before-qualification reproduction", "status": "done" },
    { "name": "fail-first stale-front reproduction", "status": "done" },
    { "name": "focused implementation", "status": "done" },
    { "name": "round 1 residual implementation", "status": "done" },
    { "name": "round 2 residual implementation", "status": "done" },
    { "name": "round 3 conflict head-of-line correction", "status": "done" },
    { "name": "round 4 cross-process acceptance", "status": "done" },
    { "name": "round 5 library fixture correction", "status": "done" },
    { "name": "round 6 parent continuation fixture correction", "status": "done" },
    { "name": "focused acceptance checks", "status": "done" },
    { "name": "round 7 final-base reconciliation", "status": "done" },
    { "name": "round 8 managed-binding correction", "status": "done" },
    { "name": "round 9 guarded fixture truth correction", "status": "done" },
    { "name": "round 10 D362 rendered-envelope correction", "status": "done" },
    { "name": "round 11 packaging correction", "status": "done" },
    { "name": "round 12 packaged retry and ownership correction", "status": "done" },
    { "name": "round 13 installed identity fixture correction", "status": "done" },
    { "name": "round 14 installed lifecycle fixture correction", "status": "done" },
    { "name": "round 15 consumed-continuation correction", "status": "done" },
    { "name": "round 16 terminal successor correction", "status": "done" },
    { "name": "round 17 final-base reconciliation", "status": "done" },
    { "name": "round 18 native completion and protected-ref correction", "status": "done" },
    { "name": "round 19 parent installed-package acceptance", "status": "done" },
    { "name": "round 21 parent lease-invariant acceptance", "status": "done" },
    { "name": "round 23 parent production-lifecycle acceptance", "status": "done" },
    { "name": "round 24 public parent handoff correction", "status": "done" },
    { "name": "round 25 guarded-rebase final reconciliation", "status": "done" },
    { "name": "round 26 parent fixture contract correction", "status": "done" },
    { "name": "round 27 parent successor-lifecycle acceptance", "status": "done" },
    { "name": "round 28 exact successor launch correction", "status": "done" },
    { "name": "round 29 composed production-owner scenario", "status": "unmeasured" },
    { "name": "round 30 restart contract and control restoration", "status": "unmeasured" },
    { "name": "round 31 closed installed fixture contract", "status": "todo" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## round 31 closed installed fixture contract <!-- cq:wip-checkpoint -->

Pending the parent-reproduced installed-fixture correction on final base
`719126314750483cb306968e722bd4d6dd9e6f4d`. The exact Nix fake-ledger
validator accepts the obsolete four-field successor-launch request but rejects
the production seven-field capability-free launch profile. This round will
close that fixture contract, prove the rejection before correction and the
acceptance after restoration, then run the bounded source controls, typecheck,
lint, and closure preflight. Installed execution remains parent-owned; the
`trusted full gate` checkpoint remains `unmeasured` until trusted result
storage.

## round 30 restart contract and control restoration <!-- cq:wip-checkpoint -->

The parent-observed restart defect is corrected at the exact assertion in
`supervisedWorkerGateStorage.test.ts`: after the production owner qualifies,
gates, confirms, and releases generation N+1, the restarted coordinator must
return `{ state: "empty", partitionKey, partitionRevision: 9 }`, not replay the
earlier `successor-queued` outcome. All subsequent assertions remain in the
same fixture: authentic N+1 native proof, released row, one launch/successor/
gate, terminal N, first public fetch, duplicate-read refusal, and protected-head
ancestry. The source-relative fixture still uses the real production role
boundary with a controlled external provider; it is not described as
installed-location evidence.

The displaced launcher negatives now have a focused public-boundary regression
test. It launches a real controlled subprocess with an explicit environment,
proves the private input/result/parent/Git authorities are absent from argv,
and rejects a foreign returned handle. The production launcher no longer has a
`process.env` default; the CLI composition root passes `process.env` explicitly,
while tests pass bounded environments. Removing the default before adding that
production argument made `bun run typecheck` fail with `TS2554` at the exact
call site; restoration passed.

The focused launcher selector passes 1 test with 6 assertions. A deliberate
argv leak failed at the empty-argv assertion, and changing the foreign fixture
to return the exact handle failed because the rejection promise resolved.
Restoration returned `supervisedWorkerGateStorage.test.ts` to SHA-256
`e4853d2d6f1fa3ffb48764b78da1c1cece9fdbdad005557afe3a39dd829ee2d7`
and the selector passed again. The existing worker private-stdin selector also
passes 1 test with 5 assertions.

The locally runnable qualification, scheduler, stale-rebase, head-of-line,
coordinator-race, supervised-storage, and guarded-continuation controls pass 68
tests with 1 host-only selector filtered, 0 failures, and 626 assertions.
`bun run typecheck` and `bun run lint` pass. The repository closure first
rejected the old `main.ts` digest, then passed after both changed source digests
were declared; a one-digit test-digest mutation failed at the exact path edge,
restoration returned `cq-gate-closure.json` to SHA-256
`03e90499b9843040cbab6775d1cff95b6b1509e3d066b6dff7f7f9fbe70b3d8b`,
and the closure selector passed 1 test with 1 assertion.

The exact installed-boundary selector is not rerun in this confined child
because prior attempts consistently stalled at the nested provider boundary.
Installed-package execution, required-live PostgreSQL, and the canonical
`trusted full gate` remain `unmeasured` for trusted parent result storage.

## round 29 composed production-owner scenario <!-- cq:wip-checkpoint -->

Pending the single requested composed real production-owner scenario in
`supervisedWorkerGateStorage.test.ts`. The scenario must use the existing
controlled-Codex installed boundary to carry a stale qualified generation N
through retirement and one brokered rebase, launch the exact generation N+1
successor through the real child input/result protocol, observe authentic
native completion, qualify it, run one successor gate, confirm it, and expose
its output only through the first public fetch.

The fail-first check will exercise the corrected production path before any
runtime edit. It must reject reuse of generation N and prove that restart or a
lost acknowledgement does not duplicate the child launch, successor, or gate.
No fabricated completion proof, private row mutation, test-only coordinator
kickoff, or direct gate completion is permitted. Parent-installed and
required-live PostgreSQL acceptance remain open; `trusted full gate` remains
`unmeasured`.

The unchanged corrected stale-front selector passed first (1 test, 0 failures,
9 assertions): it retired generation N, ran the guarded rebase, allocated and
launched generation N+1 once, and stopped with generation N+1 prepared and zero
gates. Requiring the successor to be consumed reproduced the remaining defect
at that exact postcondition (0 passes, 1 failure, 8 assertions reached): the
received row was `prepared` with exact-tip guarded lineage where `consumed` was
required. Retirement, rebase finalization, and allocation had already
succeeded, so the missing behavior is the composed successor boundary rather
than stale-front setup.

The corrected fixture now replaces the handle-echoing successor with the real
installed `codex-role-dispatch.ts` boundary. Its controlled Codex child uses
only public fetch/store requests, returns the exact handle-only outcome, and
lets the installed runner supply the native process observation to public
qualification and coordination. The composed postconditions require one
released generation N+1 with its authentic child/run proof, one gate, one
successor row and child launch across a lost-ack restart, terminal generation
N, and first-public-fetch materialization followed by duplicate-read refusal.
The launcher receives an explicit bounded environment rather than inheriting
`process.env`; no private attestation write, direct gate completion, or manual
successor coordinator kickoff is present.

This confined child cannot measure that installed path. Three 30-second runs
reached the spawned provider sidecar and recorded only `provider-start`; no
`provider-acquire`, Codex launch, bridge request, qualification, gate, or
assertion followed. Each run ended 0 passes / 1 timeout failure after the first
pre-launch expectation, matching the earlier nested-provider confinement
boundary rather than exposing a production assertion failure. The parent must
run this exact selector on the host.

Local controls pass: the qualification, scheduler, stale-rebase,
head-of-line, coordinator-race, and guarded-rebase files pass 36 tests with 0
failures and 155 assertions. The remainder of supervised worker storage passes
31 tests with 0 failures and 465 assertions when the single host-only selector
is filtered. `bun run typecheck`, `bun run lint`, and the exact repository
closure selector pass; closure rejected the old test digest first, then passed
1 test with 1 assertion after only the declared digest was updated. The
fail-first successor-state guard observed `prepared` instead of `consumed` at
pre-correction SHA-256
`656fd7d5ac7ec2e65e04a283dff64da1e68ec2c113109597828b94fa7802d466`;
the corrected test restores to SHA-256
`32d63eeefc2d29259dc729ecf91d83d46494a97362b384a0d0ebc598eaceaffe`,
but its behavioral pass remains unmeasured here. Parent-installed, required-live
PostgreSQL, and `trusted full gate` therefore remain open.

## round 28 exact successor launch correction <!-- cq:wip-checkpoint -->

Pending the required causal real-process reproduction: a stale qualified front
must retire generation N, prepare exact generation N+1, and then fail because
no trusted owner launches that prepared successor. The observation must report
launch count zero where one launch is required before any production edit.

The exact managed-worktree selector now reproduces that defect before the
production correction. It retired and rebased generation N, allocated the one
generation-2 successor, and then failed at the process-launch assertion with an
empty launch list where the exact successor handle was required (0 passes, 1
failure). The failure is therefore the missing owner after successor
preparation, not setup, rebase, or allocation.

The correction will make the existing trusted lifecycle own the exact prepared
successor launch and authentic native completion without exposing private
capabilities on stdout or argv. Focused process and shared-store regressions
must prove one N+1 launch, qualification, gate, confirmation, and public first
fetch; generation N remains terminal. Restart and lost-ack cuts must not create
another successor or launch, while uncertain execution remains fenced. The
canonical `trusted full gate` remains `unmeasured`.

The stale coordinator now invokes a trusted local successor launcher only
after the exact generation N+1 prepare succeeds. Production coordination
passes a capability-free launch profile, while the launcher sends the prepared
input/result/Git/parent authorities only through the child stdin envelope,
waits for the owned boundary, and accepts only its exact handle on stdout. A
lost acknowledgement or restart sees the already-bound successor and does not
launch it again; that is the execution-uncertainty fence.

The restored causal selector passes with one generation-2 process launch,
zero source gates, empty child argv, exact returned handle, and one launch
after a fresh coordinator is constructed over the same store. The coordinator
transport controls pass 3 tests with 0 failures and 6 assertions, and the
stale-rebase controls pass 4 tests with 0 failures and 26 assertions. Typecheck
passes. The real library queued-process selector cannot bind its local Bun
server in this confinement (`EADDRINUSE` for port 0), so no result is claimed
for that case yet.

The final focused shared-store/runtime run passes 96 tests with 0 failures and
671 assertions across candidate qualification, scheduling, stale rebase,
head-of-line policy, coordinator races, supervised storage, guarded-rebase
continuation, and coordinator framing. `bun run typecheck`, `bun run lint`, and
`git diff --check` pass. The exact repository closure initially rejected the
changed sources and the new dynamic spawn edge; after updating only their
declared digests and adding the `ledger-mcp/src/main.ts` spawn declaration, it
passes 1 test with 1 assertion in 37.26 seconds.

Four observed mutations failed at their intended boundaries and restored to
their pre-mutation hashes: generation N+2 was rejected in place of the exact
N+1 outcome; an altered model was visible in the coordinator request; a valid
sandbox mode resolved where malformed-profile rejection was required; and an
inverted handle comparison rejected the exact successor. The closure manifest
also rejected a one-digit mutation of the new `main.ts` digest and restored to
SHA-256 `d80dcc37c295efaa6cd5c4ac3478b66311d00be131664367a6a18cce74a162db`.
The installed rendered-envelope mutation remains unmeasured locally because
that real-process fixture times out before reaching its coordinator assertion.

The complete `codexRoleBoundary.test.ts` run records 28 passes and the same two
confinement failures documented in prior rounds: the installed runner stalls at
the nested provider boundary, and the legacy parent-finalizer fixture emits no
JSON. `codexProcessQueuedCompletion.test.ts` records two passes before its real
library fixture is refused an ephemeral local listener (`EADDRINUSE` on port
0). No installed Nix, required-live PostgreSQL, or canonical full gate was run
in this focused child. Round 28 supplies the missing production launch and
restart correction for round 27; the canonical `trusted full gate` remains
`unmeasured` for the parent-installed and required-live acceptance in trusted
result storage.

## round 27 parent successor-lifecycle acceptance <!-- cq:wip-checkpoint -->

Pending a causal real-process regression before any production edit. The
actual stale-front owner must launch its exact prepared generation N+1
successor once; the pre-correction observation must be launch count zero where
one is required. A test-only coordinator kickoff, static source-name assertion,
or fabricated native completion proof does not qualify as the reproduction.

The correction must give the prepared successor an owned trusted launch and
handoff that preserves task, managed-worktree, lease, generation, provenance,
and private-capability identity without exposing launch secrets on public
stdout or argv. The real process proof must cover N+1 launch, authentic native
completion, qualification, front-only gate, confirmation, and first public
fetch. Generation N must remain terminal and unable to gate, review, complete,
or merge.

Restart and lost-ack cuts around successor allocation, launch, and completion
must preserve exactly one successor and no duplicate child launch. Execution
uncertainty must fence the attempt rather than blindly relaunch it. The focused
round must retain every existing server-side authority and terminal-cause
check, run the causal mutation controls plus typecheck, lint, and exact closure
preflight, and leave the installed Nix and required-live PostgreSQL acceptance
to the parent. The canonical `trusted full gate` remains `unmeasured`.

## round 26 parent fixture contract correction <!-- cq:wip-checkpoint -->

The guarded bridge starts from exact round-25 result
`d69a6691cf1dc89eb602e82e82909c059a61ad50` on final base
`3f5f6b2055d69e5c9b0318acfd030763919ba570`. Step-0 verification observed the
task branch at that start, commit objects for both SHAs, and the final base as
an ancestor. No raw rebase or pre-storage full gate was run.

The supervised-storage defect reproduced causally before correction: its exact
selector passed the gate and coordinator assertions, then failed 0 passes / 1
failure at line 853 because the confirmed row had no `outputMaterializedAt`.
The corrected fixture now verifies no eager materialization, the first public
fetch returning the exact passing output, the second fetch returning
`output-already-materialized`, and a gate count that remains one. Four bounded
mutations failed at the intended checks: requiring eager materialization,
requiring a failed first-fetch result, requiring a second consumed body, and
requiring two gate requests. Exact restoration returned
`supervisedWorkerGateStorage.test.ts` to SHA-256
`868933921ce2e4df0969b6f49a92762339036d7918b448b2c4f03b5c5ccd132f`.

Source inspection confirmed the rendered-envelope fake accepted provider and
qualification commands but routed the production coordinator command to its
unexpected-command exit. The corrected fake accepts the exact coordinator
command, captures its request, returns an empty queue, and asserts ordering,
handle, holder, parent authority, and credential non-leakage. Both the
before-correction and restored local selectors timed out after 10 seconds with
0 passes / 1 failure and only three pre-launch expectations reached. A live
process-tree observation showed the nested boundary waiting on its provider
pipe before child launch, so this confinement cannot observe the new
coordinator assertions; no mutation success is claimed for that file. The
round-25 parent installed-package result of 46 passes, 0 failures, and 518
assertions remains historical source-relevant evidence, not a new-tip
measurement. The trusted storage gate must supply the new-tip observation.

The complete supervised-storage file plus qualification and scheduler controls
passed 39 tests with 0 failures and 494 assertions. `bun run typecheck` and
`bun run lint` both exited zero. The closure selector first rejected the old
manifest with exact `source-digest-mismatch`; after updating only the two
changed source digests, it passed 1 test with 0 failures and 1 assertion in
36.69 seconds. `git diff --check` passed. The canonical `trusted full gate`
remains `unmeasured` for the single trusted result-storage gate.

## round 25 guarded-rebase final reconciliation <!-- cq:wip-checkpoint -->

The server-provided guarded-rebase lineage binds pre-rebase result
`02f33f639ec8c74af7d7148a5fbb52b11a2c6406` to final base
`3f5f6b2055d69e5c9b0318acfd030763919ba570` and sealed rebased start
`ee53bef0f3cb9ccd5545ccddfaa1bf5a1c35ab1e`. Step-0 verification observed
the exact task branch and sealed start, commit objects for all three SHAs, and
the final base as an ancestor of the rebased start. The WIP header now records
that final base. No raw rebase or pre-storage full gate was run.

The parent accepted exact commit
`ee53bef0f3cb9ccd5545ccddfaa1bf5a1c35ab1e`. Its immutable Nix build exited
zero at `/nix/store/ib8imy4sawryf5979zwdf7lk6jx266lr-cq-0.0.1`; the installed
suite passed 46 tests with 0 failures and 518 assertions in 92.79 seconds,
including real public first-fetch after restart, duplicate-read refusal, a
single gate count, and guarded continuation. The required-live PostgreSQL run
passed 129 tests with 0 failures and 830 assertions in 8.76 seconds: 128
PostgreSQL cases, no PostgreSQL skip, and cleanup of its owned cluster. The
exact closure preflight passed 1 test with 0 failures and 1 assertion in 36.95
seconds. A non-overlap audit preserved all 34 task blobs and found changes only
in the three known integration-overlap files. These parent observations close
the round-23 and round-24 checkpoints. The canonical `trusted full gate`
remains `unmeasured` for the single trusted result-storage gate.

## round 24 public parent handoff correction <!-- cq:wip-checkpoint -->

Pending a causal public-boundary regression before production edits. After a
real installed boundary execution, the parent must use the returned retry
handle to fetch the exact result body once; subsequent reads must reject
duplicate delivery. The regression will also exercise restart/replay and exact
authority while proving that retry does not rerun the gate. This focused round
will run the causal controls, typecheck, lint, and the exact repository closure
test. The canonical `trusted full gate` remains `unmeasured` for trusted parent
supervision.

The real installed-boundary acceptance now performs its first parent read
through `capability.fetch(retryHandle)` after closing and reopening the SQLite
backend, then requires a consumed body, duplicate-read refusal, and an unchanged
single gate count. This is the public handoff that the prior fixture bypassed
through `observeEvidence`. Before production edits, the exact installed source
was replayed from `/nix/store/7mvgxs61rl5lsclcgyyqs4p1l26515j9-cq-0.0.1`;
the confined nested process lost its fixture input and stopped at the known
zero-`thread.started` boundary before reaching the new assertion (0 passes, 1
failure). That local failure is not causal evidence. The dispatch-supplied
parent probes remain the causal fail-first observation: the runner returned
`gate-pending`, while the later public read returned
`output-already-materialized` instead of the consumed body.

The coordinator operation now confirms the exact qualified front using its
stored native completion proof without materializing the result. Its contract
and tests use the precise `confirmQualifiedFront` name. This leaves the first
public `fetch` for the parent boundary while retaining durable confirmation,
lease release, and one-shot duplicate refusal. The focused scheduler,
head-of-line, stale-rebase, and coordinator-race selector passes 26 tests with
0 failures and 105 assertions.

Focused restored checks pass: candidate qualification is 5 tests, 0 failures,
and 16 assertions; candidate coordinator transport is 5 tests, 0 failures,
and 7 assertions; `bun run typecheck` and `bun run lint` both exit zero. The
exact `packages/ledger/test/gateClosureRepository.test.ts` selector passes 1
test after updating only the changed production and installed-acceptance source
digests. Mutating the production digest by one hex digit failed with the exact
`source-digest-mismatch`; restoration returned `cq-gate-closure.json` to
SHA-256 `9b5bb4984fb40f87a13d8ad278c37f1e6841c2b121dfec062ed5a1284cb1a39f`
and the closure selector passed again. Mutating the scheduler event assertion
from `confirm` to `confirm-fetch` failed with the expected event diff;
restoration returned that test to SHA-256
`ced45eb65f017e6d4cf0e1a3cc7f76af68d13cb03a611a0827127586f2ecb355`
and its focused selector passed 1 test with 5 assertions.

The complete local `codexRoleBoundary.test.ts` attempt recorded 33 passes and 2
failures: the installed queued-handle case timed out at the known confined
nested-process boundary, and the legacy parent-finalizer process fixture emitted
no JSON. Neither failure reached the new public handoff assertion, and neither
is claimed as an unrelated-failure proof. Consequently the installed
public-fetch assertion and its single-gate assertion have no local mutation
observation. The exact parent-installed acceptance recorded in round 25 now
supplies the missing real public first-fetch, restart, duplicate-refusal, and
single-gate evidence, so this checkpoint is `done`. No child-owned canonical,
full, or Nix gate was started, and `trusted full gate` remains `unmeasured` for
trusted result storage.

## round 23 parent production-lifecycle acceptance <!-- cq:wip-checkpoint -->

Pending fail-first reproduction of both reported production-boundary defects:
the shipped parent lifecycle does not yet drive a queued result through the
front coordinator using public authority, and the public qualifier may not
durably bind the native thread identity from the trusted observation. Preserve
the separate logical run and native thread identities, handle-only queued
return, front-only gate, and every existing terminal/fencing check. Run only
focused causal and mutation controls, typecheck, lint, and repository closure;
the canonical `trusted full gate` remains `unmeasured` for trusted parent
supervision.

The public qualifier replay reproduced the native-observation defect before
production edits: substituting only `childThreadId` resolved as an exact queued
replay instead of rejecting the altered process observation. The focused test
failed 0 passes / 1 failure with “Expected promise that rejects; Received
promise that resolved.” The real installed-runner selector again reached the
known confined nested-provider timeout before its new lifecycle assertion; that
timeout is not treated as causal reproduction. Source inspection independently
observed the shipped runner discard the queued qualification and exit without
any production coordinator owner.

The correction binds a SHA-256 digest of the complete trusted process
observation into the existing qualification digest without changing the
persisted qualification schema. An altered native thread now conflicts with
the durable proof while the separate logical expected run remains unchanged.
The coordinator command now carries the exact dispatch handle and parent-gate
authority; the server validates that authority and resolves the durable queue
partition itself. The installed runner writes the handle-only queued result,
then retains ownership while awaiting the bounded coordinator rather than
detaching work. The restored qualifier regression passes 1 test with 6
assertions, the three coordinator transport controls pass 3 tests with 5
assertions, and `bun run typecheck` passes.

The final causal selector passes the library handle-only case and public
qualification case together: 2 tests, 0 failures, and 11 assertions. The
qualification case now also proves that an invalid parent capability is
rejected before coordination and that the exact authority resolves the handle
to its yielded partition without trusting a caller-supplied partition key.
`bun run typecheck`, `bun run lint`, and `nix-instantiate --parse flake.nix`
pass. The restored repository closure resolver passes 1 test with 0 failures
in 37.11 seconds; its manifest SHA-256 is
`f4def896e7a72473b5ac87235477564d0888405a6d19209d599574dafaf9bb6d`.

Two production mutations failed at their intended assertions. Replacing the
complete process-observation digest with the synthesized logical completion
again accepted the substituted native thread. Reversing parent capability
matching advanced the invalid request to coordinator construction instead of
returning the required authority error. Restoring exact bytes returned
`dispatchCapability.ts` to SHA-256
`7e32129212acdf300d68aaeaf31ce39e5e23c4d644bc27965ebd14cc43fc3028`.
Changing the focused test's closure declaration by one hex digit failed with
the exact `source-digest-mismatch`; restoring the manifest produced the passing
closure result above.

The installed nested-process selector remains blocked by this confined
worktree's previously measured provider-pipe timeout, so it is not claimed as
passing evidence. The exact parent acceptance recorded in round 25 now supplies
the installed-package lifecycle and required-live PostgreSQL settlement. This
checkpoint is therefore `done`; the child did not start a canonical/full/Nix
gate, and `trusted full gate` remains `unmeasured`.

## round 21 parent lease-invariant acceptance <!-- cq:wip-checkpoint -->

Round 21 began pending reproduction of the reported park/yield then resume
race before production edits. The causal cases had to cover fresh and stale
partition revision and generation authority, preserve the later enrollment's
live holder and lease, reject or defer the conflicting resume observably
without implicit preemption or invented expiry, admit resume after that lease
was released, preserve the older enrollment's qualification, lineage, and
ordering, and leave independent partitions unaffected. The parent retained
the required live-PostgreSQL acceptance run; the canonical `trusted full gate`
remains `unmeasured` and must not run in this child round.

Fail-first is observed before production edits. The exact head-of-line selector
reported 16 passes and 2 failures: after A was qualified, leased, and then
parked or yielded, B was qualified and leased; a resume of A carrying B's
current partition revision incorrectly resolved instead of rejecting with the
expected `not-front` conflict. The stale-revision and stale-generation controls
passed before that point, isolating the defect to current resume authority
crossing a different enrollment's live partition lease.

The queue transaction now resolves the partition's sole live lease before
admitting an acquisition, replays only that lease's exact holder, and reports
every other acquisition as blocked by that lease. Resume rejects with
`not-front` while any enrollment retains the partition lease. It neither
preempts the holder nor infers expiry. The restored head-of-line selector
passes 18 tests with 0 failures and 51 assertions, including both dispositions,
authority controls, zero second-gate work, independent partitions, and resume
after the later lease is released.

The shared contract's in-memory and SQLite causal runs pass 4 tests with 36
assertions across independent peers and restart. Four production-guard
mutations then failed for the intended invariant violations: reversing the
resume lease check admitted A, reversing exact-holder comparison reached the
forbidden second coordinator observation, reversing lease cardinality rejected
a valid zero-lease partition as multiply leased, and reversing lease-presence
validation replaced `not-front` with the missing-authority contract error.
Restoring the intended bytes returned `dispatchImplementationQueue.ts` to
SHA-256 `bf770b91a5bdb6df3b09a687be293d559c520e041d5c6df453529e18513648a2`.
The combined restored causal and shared-store selector passes 6 tests with 58
assertions.

The bounded seven-file queue, head-of-line, qualification, scheduler,
stale-rebase, SQLite race, and coordinator-race selector passes 52 tests with
189 assertions. The complete shared attestation contract passes 116 tests with
794 assertions across the in-memory reference and SQLite. Typecheck initially
found that the independent-partition fixture used a union result without an
explicit queue-control check; adding that public-boundary narrowing made
`bun run typecheck` pass. `bun run lint` also passes.

The repository closure resolver passes with target script `check`, reachable
scripts `check`, `check:codex-installed-gate`, `check:flake-enumeration`, and
`lint`, and the expected three install roots. No closure-manifest change is
required. Local causal acceptance is complete. The canonical/full/Nix gate was
not started, and `trusted full gate` remains `unmeasured` as directed.

The parent subsequently accepted exact commit
`d82f3d75a44e80485a39878746fa0d9af68fee00`. Its required-live PostgreSQL run
passed 129 tests with 0 failures and 830 assertions: 128 PostgreSQL cases, no
skipped runnable case, 8.61 seconds, and its owned cluster closed. The parent
also exercised both original park and yield cases: each conflicting resume or
acquire remained blocked while preserving B, then A resumed after release with
exactly one live lease. This supplies the previously pending parent-owned
acceptance evidence.

Round-22 reconciliation parsed all 26 checkpoint records and found only this
checkpoint and `trusted full gate` open before closure. `bun run typecheck` and
`bun run lint` pass. The repository closure preflight passes 1 test with 0
failures. The fence-aware prose wrapping check also passes. This checkpoint is
now `done`; `trusted full gate` remains `unmeasured` for the single trusted
result-storage gate.

## round 19 parent installed-package acceptance <!-- cq:wip-checkpoint -->

The parent re-probed and accepted the installed package at exact commit
`5842b53e7e10939b37f4acf344fcc5613fa8b504`. Its immutable Nix run passed 46
tests with 0 failures and 521 assertions in 80.11 seconds, including pipe and
PTY coverage and both installed review/resolver/guarded-continuation scenarios.
The package output was `/nix/store/xhn1ajjhfqmksgqcfa7v4zhknji9ra0l-cq-0.0.1`.
No deployment occurred.

The round-19 fail-first selector reproduced the reported lifecycle failure:
the first gate-integration case exited before its intended live-gate boundary
with a missing prepared-run context, so its expected diagnostic was absent.
The direct gate and diagnostic launchers now carry explicit prepared parent-run
identities and submit their already-complete request through fixed Blob input.
The installed real-process path retains its separate correlation identity; its
pipe and PTY launchers carry the same distinct parent-run identity, while the
strict fake ledger requires the exact qualifier key set and rejects equality
with the generated child thread.

The first local rerun remained 9 passes / 5 failures because this confined Bun
process dropped the outer fixture pipe before the newline. Fixed input crossed
that boundary, after which the existing process workset provider itself timed
out under confinement (its standalone selector was 0 passes / 2 failures).
Those post-correction timeouts are not reported as passing evidence. At that
point local causal and focused checks remained pending; the later exact parent
measurements below supersede only that pending acceptance, not the historical
red observations.

Focused causal controls pass 3 tests with 10 assertions: exact qualification
replay, queued handle-only completion, and nonzero native-exit terminalization.
`bun run typecheck`, `bun run lint`, and `nix-instantiate --parse flake.nix`
pass. The repository closure preflight first failed with the intended stale
fixture digest, then passed 1 test after both exact SHA-256 entries were
updated.

The full four-file affected selector recorded 36 passes, 9 confinement
failures, and 134 assertions. The failures were the sandbox's independently
observed process-provider timeout, empty nested captured stdout, blocked
loopback bind, and the sandbox preflight's explicit `pipe-capture-lost`
verdict. The corrected two originally red files similarly reached the
provider boundary and recorded 9 passes / 5 confinement failures; no such
failure is treated as acceptance evidence.

Mutation proof for the installed qualifier guard changed its required parent
run to a non-matching value: the exact embedded guard rejected the valid
payload with `guard-exit-1`. Restoring the intended value accepted the exact
payload, restored `flake.nix` to SHA-256
`ecd3526c0840d06659718e909c776ebbc7f2ad144213fc3597b2c96b054614e6`,
and retained the explicit generated-thread inequality. The closure manifest's
fail/pass pair covers its changed digest assertions.

The parent required-live PostgreSQL run at the same exact commit passed 125
tests with 0 failures and 794 assertions: 124 PostgreSQL cases, no skipped
runnable case, and its owned cluster was closed. The parent's six affected
process-boundary files passed 59 tests with 0 failures and 485 assertions.
These exact parent observations complete the non-gate installed-package
acceptance checkpoint; they do not erase or relabel the historical local
confinement failures. The canonical `trusted full gate` remains `unmeasured`
for this generation's trusted result-storage supervision.

Round-20 reconciliation parsed all 25 checkpoint records, confirmed every
non-gate checkpoint is `done`, and confirmed `trusted full gate` remains
`unmeasured`. `bun run typecheck` and `bun run lint` pass. The exact repository
closure preflight passes 1 test with 0 failures. Multiline evidence and the
round-19 prose wrapping checks pass. This reconciliation changes only this WIP
artifact; no production guard or test assertion changed in the round.

## round 18 native completion and protected-ref correction <!-- cq:wip-checkpoint -->

Reproduce the two operator-reported boundary defects before production edits:
qualification currently accepts insufficient native-completion evidence, and
coordinator stale-base authority currently consults ambient checkout `HEAD`
instead of the persisted protected integration ref. Preserve the genuine
generated-thread path, then run the causal regressions, focused acceptance,
typecheck, lint, and repository closure. The canonical `trusted full gate`
remains `unmeasured` for parent supervision.

Fail-first qualification reported 2 passes and 2 failures: both a substituted
prepared run and a completed exit status 17 were incorrectly returned as
`queued`. The detached-checkout SQLite coordinator failed by launching a gate
from ambient `HEAD` instead of rebasing onto the unchanged `refs/heads/main`.

Qualification now derives role, correlation, generated child thread, outcome,
exit status, handle, and prompt digest only from the runner-owned registered
execution. It separately carries the prepared parent run identity; the server
matches that identity without equating it to the generated thread. Failed or
nonzero terminal observations abort before queue enrollment. The real library
process exit-17 regression passes and records no qualification.

Coordinator head observation and both initial/restart rebase revalidations now
resolve the persisted `control.partition.integrationRef`. The causal
qualification and SQLite coordinator selector passes 6 tests with 32
assertions. A transport-failed observation with exit status zero is also
terminalized as `native-failure`; it cannot qualify or reach a gate.

Round-18 mutation controls all failed on the intended violated invariant and
passed after exact restoration:

- reversing the prepared-run comparison queued a substituted registered run;
- reversing the completed-exit comparison queued exit status 17;
- removing the terminal-outcome branch queued `transport-failed`;
- replacing the persisted protected-ref lookup with ambient `HEAD` launched a
  gate before the required guarded rebase;
- reversing runner-ownership validation let a reconstructed observation reach
  the forbidden executable;
- reversing run/role validation rejected a genuine registered execution;
- reversing exact-replay policy rejected the first lost acknowledgement;
- deleting the qualifier request's `expectedRunId` and outcome guards admitted
  respectively a missing run identity and an invalid `cancelled` outcome;
- reversing the library adapter's nonzero guard attempted qualification; and
- changing one closure digest produced `source-digest-mismatch`.

Restored SHA-256 digests are
`fa89316d6597420828e7539440350d56c934b74bd31c583e3e01749a1f16c956`
for `dispatchCapability.ts`,
`7938d0787a04f00347e892eaa02855e227368c40cce4ab1b40495b87fd2800db`
for `codexRoleBoundary.ts`,
`0f39b59a9aedebde18d2700f8dd7fa4f5bb133ba4f1f692e2bcafed260032efa`
for `main.ts`, and
`f14a78f5d5c96200139af14354f348164c30fd126ae7661114fa7b9f1f12e4f2`
for `cq-gate-closure.json`. The adapter's redundant second ownership assertion
was removed after its mutation survived the already-terminal nonzero case; the
qualifier remains the single runner-registration authority.

`bun run typecheck` and `bun run lint` pass. The repository closure preflight
passes 1 test. The exact ten-file selector completed with 92 passes, 3
environment-gated skips, 4 previously recorded confinement failures, and 689
assertions: nested process pipe timeouts, empty captured child stdout, and a
blocked loopback bind. The required-live PostgreSQL probe again could not bind
`127.0.0.1` in this sandbox. Neither environment failure is reported as a
pass. Every non-gate checkpoint is complete; `trusted full gate` remains
`unmeasured` for parent supervision.

## round 17 final-base reconciliation <!-- cq:wip-checkpoint -->

Round 17 starts from guarded-rebase commit `f80bdd0c` on final base
`abcacad0`. The parent measured the installed package at the prior result
(`5a21faec`) with 46 passing tests, 0 failures, and 521 assertions; the package
output was `/nix/store/25v6sf5q8s48vzf3s8mrf2n508njf9p2-cq-0.0.1`. The
parent also measured the required-live PostgreSQL selector on this exact
rebased start with 125 passing tests, 0 failures, 792 assertions, and 0 skips,
then closed its owned cluster.

The requested four-file queue, qualification, stale-rebase, and source selector
passes 31 tests with 125 assertions. The standalone source/tombstone selector
passes 5 tests with 27 assertions. `bun run typecheck`, `bun run lint`, and the
one-test repository closure preflight all pass. No production or test guard
changed in this reconciliation; the prior causal mutations remain recorded in
their owning checkpoints.

The staged-result schema nevertheless requires generation-local mutation
evidence because the base-to-result file set includes inherited guard paths.
Changing the retained tombstone live-tip comparison from equality to inequality
admitted a substituted old result: the five-case selector reported 4 passes and
1 failure because the expected rejection resolved. Restoring equality returned
`dispatchAttestation.ts` to SHA-256
`7343af5207eb7c164644dbd315f2ed8290922ad41b0af0afd02037f67753a14b` and
the selector to 5 passes, 0 failures, and 27 assertions.

Every non-gate checkpoint is now `done`. The canonical `trusted full gate`
checkpoint remains `unmeasured` for result-storage supervision, as directed.

## round 16 terminal successor correction <!-- cq:wip-checkpoint -->

Round 16 starts from the guarded-rebase descendant at `97c0fe62`. Reproduce
the installed exact-tip successor rejection, distinguish a consumed successful
attempt from revoked or cancelled authority, and preserve the existing
continuation lineage and terminal negative controls. Focused checks remain
pending; the trusted full gate remains `unmeasured` for parent supervision.

The full in-memory lifecycle now prepares generation 1 from a consumed ordinary
generation 0, stages its exact-tip result, and attempts enrollment before
qualification. Against the inherited predicate it fails with
`ImplementationQueueConflictError(reason: "already-terminal")` naming the
consumed generation 0. The focused selector reported 0 passed / 1 failed with
4 assertions; preparation and result staging had already succeeded, isolating
the defect to successor enrollment.

The correction admits only consumed, qualified, released `gate-complete`
ancestors of the same attestation's server-bound guarded-rebase lineage.
Unrelated terminal enrollment authority still rejects, and cancelled,
parent-lost, foreign-result, foreign-manager, and staged-retirement controls
remain unchanged. Collapsed predecessors must match the retained continuation's
live tip and managed identity; paired correction generations must match the
same complete guarded bridge. The causal lifecycle now covers prepare, stage,
enroll, qualify, gate, confirm, tombstone collapse/rehydration, and a second
guarded correction; it passes with 9 assertions.

Mutation observations from this run:

- reversing the tombstone live-tip comparison admitted the substituted old
  result and failed the expected rejection;
- reversing guarded-bridge equality rejected the paired correction at prepare;
- reversing released-state eligibility reproduced the original
  `already-terminal` enrollment rejection.

After restoring the intended bytes, the five-test staged-rebase source suite
passes 27 assertions. The four-file queue/qualification/stale-rebase selector
passes 31 tests and 125 assertions. `bun run typecheck`, `bun run lint`, and the
repository closure preflight pass. Restored SHA-256 digests are
`7343af5207eb7c164644dbd315f2ed8290922ad41b0af0afd02037f67753a14b`
for `dispatchAttestation.ts`,
`28c7433f04cfb28785ba67634ce9f5aaad9fd6e1b785d2550fd4b4267ab7c2d5`
for `dispatchImplementationQueue.ts`, and
`5ccec88eb01d50fcfd4902107021db1770c600a08e259123589854cc51767fdb`
for `dispatchStagedRebaseSource.test.ts`. The installed package scenario and
canonical full gate remain delegated to the trusted parent; `trusted full gate`
therefore remains `unmeasured` in this child artifact.

## existing implementation survey <!-- cq:wip-checkpoint -->

T6518 already supplies durable candidate identities, native-completion
qualification, FIFO lease acquisition, terminal dispositions, staged-rebase
retirement, and successor fencing. The uncomposed boundary remains observable:
`createCodexProcessDispatchAdapter` and the installed role runner call
`executeCodexParentGateFinalizer` before returning the observed child outcome,
while `runPreparedDispatch` immediately confirms and fetches the row. The
task-named transport/coordinator acceptance files do not yet exist.

## fail-first stored-before-qualification reproduction <!-- cq:wip-checkpoint -->

Added the specified real lifecycle regression in
`codexProcessQueuedCompletion.test.ts`. The process child stages a valid
parent-gated output and supplies the exact native completion while a trusted
qualifier is available. On the unchanged implementation, the focused command
failed 0 passed / 1 failed with `DispatchStateConflictError`: the row remained
`gate-pending` and `runPreparedDispatch` attempted
`confirm_dispatch_completion` without invoking qualification.

## fail-first stale-front reproduction <!-- cq:wip-checkpoint -->

The starting commit had no production coordinator surface, so the first stale
fixture initially served as construction coverage rather than evidence of one
specific defect. Once composed through the real dispatch runtime it failed
before the successor correction: durable admission rejected the successor
because its Git binding retained the historical managed base rather than the
verified guarded-rebase `ontoCommit`. The same real fixture passes after the
binding correction. A prerequisite head-of-line fail-first fixture also
measured that a parked front blocked the next qualified enrollment (0 passed /
1 failed) until runnable ordering fenced parked dispositions out of the front.

## focused implementation <!-- cq:wip-checkpoint -->

First transport slice implemented: `runPreparedDispatch` now recognizes a
durably staged parent-gated row, hands the exact output digest, child identity,
provenance, and native completion to a trusted qualifier, and returns only a
`queued` handle after durable qualification. Qualification aborts remain typed
terminal outcomes. The Codex process adapter and installed runner no longer
invoke the parent finalizer before observation/qualification. The local
runnable-front coordinator and installed-runner qualification bridge remain
unfinished.

Installed-runner qualification is now composed through a dedicated bounded
local command. The trusted runtime validates the exact role/correlation,
prompt digest, handle, staged output digest, managed binding, finalized task
authority, protected ref, result commit/tree, and receipt lineage before it
persists the T6518 queue enrollment and native-completion qualification. The
installed runner calls this bridge after observing a completed registered
process and still does not invoke the parent gate. The real Git-backed focused
fixture passed 1 test with 0 failures; typecheck passed.

The focused coordinator abstraction now leases one qualified runnable front,
compares its immutable observed base with the protected head, gates and
confirms only an unchanged front using the stored native proof, and routes a
stale front through retirement, the stable task-bound `rebase` operation id,
and successor preparation with zero gate calls. Parked/yielded rows are fenced
out of runnable ordering without losing their enrollment.

Mutation observations from this run:

- bypassing the `gate-pending` queued branch failed the transport regression
  with the expected premature-confirmation state conflict;
- restoring parked rows to runnable ordering failed the head-of-line regression
  with the expected parked-front blockage;
- reversing the protected-head comparison failed both unchanged and stale
  coordinator regressions on the wrong lifecycle branches;
- reversing exact process-child correlation terminalized the valid real-Git
  qualification fixture as a foreign process completion.

The restored SHA-256 digests are `279e55c7d70412805e5fc6978dc8f558c87dd2af6faaecb100815d5ba1900a4d`
for `dispatchTransportRouter.ts`,
`a517110a3d9601606b978e6cac6f9ac98c18452262ae523088a6db205b7f5062`
for `dispatchImplementationQueue.ts`,
`14e35ef8799ae51cddd0fb7e34a830c1dccc72839ba65cc08911bd2fac44a7f7`
for `dispatchCapability.ts`, and
`ddb773e89d099809f0489b060f576aaf9f958b389c0adf17f34bbfbb4546baa8`
for `implementationCandidateQueue.ts`.

## focused acceptance checks <!-- cq:wip-checkpoint -->

The exact changed-path selector passed 27 tests with 0 failures and 92
assertions across queued transport, installed qualification, scheduler,
stale-rebase routing, head-of-line policy, three-coordinator race, and the
existing queue suite. `bun run typecheck` passed.

The ten-file acceptance selector was attempted once. The new cases and the
observable supervised/guarded controls were green, but five existing
`codexRoleBoundary` subprocess cases failed because this sandbox refuses the
fixture's process stdin write with `EPERM`; no green aggregate result is
claimed. `bun run test:attestation-postgres-required` was also attempted once;
PostgreSQL could not create its `127.0.0.1` listener (`Operation not
permitted`), so no live PostgreSQL case executed.

Residual work intentionally remains visible for the parent continuation: the
coordinator's gate/confirm/retire/rebase/successor operations are a typed local
composition boundary but are not yet wired to the production dispatch runtime;
cut-by-cut restart reconciliation, completion-unobserved recovery, and the
policy table's durable action executor remain incomplete. The installed
qualification subprocess has bounded settlement but not durable lost-ack retry.
The operator explicitly deferred the canonical full gate.

## round 1 residual implementation <!-- cq:wip-checkpoint -->

Resumed from the verified retained result at `7c150a6f`. This round will wire
the coordinator into the production dispatch runtime, add durable reconciliation
and head-of-line actions, preserve the three timing boundaries, and refresh the
affected declarations and focused checks. The trusted full gate remains
`unmeasured` pending parent final-base settlement, as explicitly required by the
operator note.

The durable head-of-line action executor is now implemented on the queue
adapter. A 16-case focused selector covers every policy disposition plus resume:
park/yield actions revoke the lease without losing enrollment, terminal actions
fence the attempt, and resume retains the qualification while the next acquire
mints a fresh lease generation. The fail-first run produced 14 missing-method
failures. The restored selector passed 16 tests with 29 assertions. Mutating
`review-question` from `park` to `yield` produced the expected two failures; the
restored source and test SHA-256 digests are
`21b2c3bddef9efab179645a2e4d61800a4cb1a7ccbf6d59c5e2990b6c34e5316`
and `8e89716c43c8a6f6f1a90ad9c6a4e79d9be9d728260a29306e7d9e027ce20d46`.

Installed-runner qualification now retries one lost acknowledgement using the
identical frozen observation payload and one total deadline. The real-process
fixture first exits after representing a durable write with no acknowledgement,
then accepts the exact replay and returns the qualified handle. It failed on the
one-shot implementation and passes with two observed invocations. Mutating the
attempt bound back to one produced the expected assertion failure. Typecheck
passes; restored SHA-256 digests are
`8d3d28081818f580a041eeb8519524e73735cf90279a1be917451169e5cfaa53`
for the source and
`b3221f37fd078e788675e0d17caa394d2bae36e9d4dae74559d8354e3c9c3056`
for the test.

The unchanged-front coordinator now observes the exact successful lease only
after gate, confirmation, and fetch. The abstract scheduler fixture first
exposed a retained `leased` state. Production composition then established that
`confirmDispatchCompletion` atomically releases the queue lease; the redundant
second release was removed and the scheduler dummy now models that transaction.

The production dispatch capability now owns the runnable-front queue adapter,
claims the parent gate only through an exact qualified lease, runs the trusted
gate under the managed-worktree effect lock, confirms with the stored native
proof and provenance, and fetches the consumed result. A real managed-worktree
fixture passes this unchanged-head path and verifies the lease is released.
Removing the trusted exact-lease checks made the fixture resolve instead of
rejecting a substituted lease; restoration made the expected rejection pass.
The restored `dispatchAttestation.ts` SHA-256 digest is
`ac525ec5c0a2348d0b51570d66c2e7cd67e11e3a513a0242624cae1faa0f33df`.

The production stale-head path now verifies the clean exact result tip, writes
the guarded-rebase intent, durably retires the source before invoking the sole
workset Git-effect broker, and prepares generation N+1 from the finalized
journal. The successor originally failed its durable admission because its Git
binding retained the historical managed base instead of the bridge
`ontoCommit`; the real fixture failed at
`gitEffectBinding.guardedRebaseBridge`. Binding the successor to the verified
`ontoCommit` makes the fixture pass with zero gate runs and one admitted
successor. Deliberately restoring the historical base reproduced that exact
failure; after restoration, the source and fixture SHA-256 digests are
`15bbe7b679bd9112e61e9c893632ece32767f4f99174468fdc4b52a13748e1ab`
and `98485002567fbe60674df1983344f575a995e407ad3fbb543cfe445cfbc938a4`.

The installed runner now invokes a dedicated bounded local coordinator command
after qualification, using the independent parent-gate window. The command
accepts only a bounded exact `{partitionKey, holderId}` object and exposes no
parent gate capability to the child. Source-bound exports were refreshed for
the new qualification acknowledgement and coordinator surface.

Current focused evidence: typecheck passes; scheduler/stale/head-of-line pass 19
tests with 46 assertions; the complete supervised worker storage file passes 31
tests with 458 assertions; queued process qualification and real installed
qualification each pass their focused case. `codexRoleBoundary.test.ts` passes
25 of 26 cases; the remaining fixture receives empty stdout twice from its Node
child and reports `parent gate emitted non-JSON stdout`. The sandbox preflight
independently reports `pipe-capture-lost`, and the gate integration fixtures
likewise cannot receive their newline-terminated request in this confinement.
No green aggregate or canonical full gate is claimed.

Residual work remains acceptance-significant. The stale-path handoff between
retirement/rebase and successor preparation is cached in process memory, so a
restart after a finalized journal does not yet reconstruct the checkpoint.
Completion-unobserved recovery is not composed, rebase conflicts do not yet
apply a durable policy action, and one installed-runner coordinator invocation
does not prove that every trailing enrollment is eventually drained after a
competing lease. These cut points require the pending parent final base and a
subsequent implementation round; this round must therefore store a truthful
partial/fail result rather than pass.

## round 2 residual implementation <!-- cq:wip-checkpoint -->

Resumed from the verified retained result at `0a7b083a`. This round will finish
restart-safe reconstruction after a finalized guarded rebase, durable conflict
recovery, completion-unobserved fenced recovery with eventual queue draining,
and the distinct qualification/queued-dwell/admitted-run timing boundaries.
The affected source-bound declarations and exact parent preflight will be
refreshed after source edits. The trusted full gate remains `unmeasured` as
required by the dispatch criticism.

Fail-first residual fixtures now reproduce two exact gaps. A coordinator built
over a durable retired-source checkpoint returned `empty` without consulting
that checkpoint, proving the in-memory stale-run map cannot survive restart.
The installed coordinator subprocess completed one runnable front but was
invoked only once, leaving its attempt counter at one instead of draining the
partition to `empty`. The same run repeated the already-recorded sandbox
pipe-capture failure; it is not used as evidence for either residual.

The restart/drain slice is now implemented. Runnable-front coordination checks
the durable retired-source checkpoint before acquiring any later enrollment,
reconciles the existing guarded-rebase journal under the stable operation id,
returns a durable blocked conflict disposition, and allocates or replays one
claimed successor without an in-memory checkpoint. Successor qualification now
carries the retired-source authority needed to preserve enrollment and ordinal.
The installed coordinator drains after completion and waits through a bounded
competing lease, while each completed front receives a fresh admitted-run
window.

Completion qualification now has its own persisted deadline. A missing proof
terminalizes as `completion-unobserved` before the next eligible enrollment is
leased; a proof arriving after that deadline is refused. Once qualified, queue
dwell does not consume or recheck the admitted-run window. The focused stale,
coordinator, timing, and typecheck selectors pass. A real managed-Git probe also
observed successful finalized-journal reconstruction and stable conflict replay;
that probe was removed from `supervisedWorkerGateStorage.test.ts` to preserve the
operator-required unchanged 31-test control file.

Focused regression controls now pass 75 tests with 584 assertions across
qualification, the durable queue, scheduler, stale recovery, head-of-line
policy, coordinator race, and the unchanged supervised-storage file. The exact
repository closure preflight initially reported each stale declaration in
turn; only the affected source/path/spawn declarations were refreshed. The
preflight now passes 1 test with 0 failures.

Mutation evidence from this run:

- bypassing the durable retired-source lookup returned `empty` instead of the
  exact successor and failed the restart selector;
- reversing expired-unqualified ordering left the first row `enqueued` and
  failed the next-enrollment lease assertion;
- reversing the late-proof boundary qualified a proof at `12:05:00.001Z` and
  failed the required `native-failure` assertion;
- returning after the first completed coordinator attempt left the installed
  attempt counter at one instead of two;
- returning on a competing lease produced `blocked` instead of the later
  completed/drained outcome;
- marking qualified queue dwell as consuming the admitted-run window failed the
  timing-basis assertion;
- disabling the production durable staged-rebase loader failed the unchanged
  real managed-Git stale-front control before successor preparation; and
- changing the declared Codex runner digest from `4712...` to `4713...` failed
  the exact closure preflight with `source-digest-mismatch`.

After restoration, source SHA-256 digests are
`0200dc6fdf5342586b1eaad9c6bf886ec78de6d91c2a2c376174cdd88256e75a`
for `implementationCandidateQueue.ts`,
`fd5f9af37d6ddec69ad8af85a08528b54652d61dac76b1a91368d37cfbb76074`
for `dispatchImplementationQueue.ts`,
`4712faa568eb4c8b2f235fa9a82ad59af46d3d6792f9f29ab82a22477d30b164`
for `codexRoleBoundary.ts`,
`a02682c462123ee7078eb5a8043271825baa2b3bc8b0921a43357747164dc812`
for `codexStagedTiming.ts`, and
`a64c1e1657c6afb2711e913d57de727a274524fe9c94d6dfe961e7040a115454`
for `dispatchCapability.ts`; the restored closure manifest digest is
`d8e61f7d9e5614c4e629771ed55b50b28ce9f901ca7d6642009eaab0b178f732`.

Additional acceptance evidence: queued process completion plus guarded-rebase
continuation and the packaged broker selector produced 7 passes, 3
environment-gated skips, and 0 failures. Per the operator note, this round did
not repeat the known Codex child pipe-capture failures or blocked PostgreSQL
listener attempt, and it did not invoke the canonical full gate. The result is
therefore a committed partial rather than a claimed pass.

## round 3 conflict head-of-line correction <!-- cq:wip-checkpoint -->

Resumed from the verified retained result at `b9916b9d`. This round will
reproduce and correct the persistent retired-conflict head-of-line blockage,
then add a production-path persisted/fresh-runtime control without weakening
durable-source reconstruction. The trusted full gate remains `unmeasured` as
required while the parent final base is pending.

The fail-first manual coordinator fixture observed the reproduced defect: a
retired source whose guarded rebase remained conflict-pending returned
`blocked` without persisting a disposition (`conflictParkCalls` was 0), so a
second coordinator could not reach the later qualified enrollment. The queue
now persists an exact conflict disposition against the retired source
authority and excludes that row from ordinary pending-source selection. The
restored selector passes 4 tests with 23 assertions; a fresh coordinator run
leases and completes the later enrollment. Typecheck passes. Production-path
persisted/fresh-runtime conflict coverage and mutation controls remain todo.

Deferred conflicts remain recoverable without regaining runnable-front
priority: only an empty ordinary acquisition consults the parked-conflict
index. A real managed-Git production fixture now stops on a conflicting rebase,
persists the disposition, reconstructs it through a fresh capability runtime
without a gate or effect relaunch, resolves it through the durable conflict
continuation, and uses a second fresh runtime to reconstruct the finalized
journal and allocate exactly one generation N+1 successor.

Mutation observations from this run:

- reversing undisposed-source selection lost the just-retired checkpoint and
  failed with `conflicted staged-rebase retirement lost its durable checkpoint`;
- disabling empty-queue deferred reconciliation returned `empty` instead of
  the exact persisted `blocked` conflict replay;
- reversing the exact source-reference authority check rejected the authentic
  retired source with `stale-lease`; and
- reversing exact disposition replay rejected the durable identical replay as
  an altered binding.

After restoration, SHA-256 digests are
`20c5ab3e757ca6c4787f682307ba89c65dca2d73b024bda3dd867c3938328650`
for `dispatchImplementationQueue.ts`,
`62cf8bfbd17e702e1f0c21a973af77b897eb4743147609f89dadda2df04f8058`
for `implementationCandidateQueue.ts`, and
`6a36ee82d808c76088a14bec7fa9566462445bddde4f01bb9031905b5219afb2`
for `supervisedWorkerGateStorage.test.ts`. The exact production-path test passes
1 test with 10 assertions. Closure refresh and final focused checks remain todo.

Final focused controls pass 76 tests with 606 assertions across the config
retirement contract, queue transitions, stale routing, head-of-line policy, and
the complete supervised-storage file. `bun run typecheck` and `bun run lint`
pass. Lint first exposed the round-1 head-of-line fixture's unused
`secondQualified` binding; removing only that unused binding made the full lint
selector green. The closure preflight first rejected the expected stale
supervised-storage source digest, then passed 1 test after refreshing that one
opaque path declaration.

Per the operator note, this round did not rerun the known sandbox child
pipe-capture failures, the unavailable PostgreSQL listener, or the canonical
full gate. Parent final-base acceptance remains exactly:

```text
bun test packages/cq-config/test/codexRoleBoundary.test.ts packages/cq-config/test/codexProcessQueuedCompletion.test.ts packages/ledger-mcp/test/implementation-candidate-qualification.test.ts packages/ledger-mcp/test/implementation-candidate-scheduler.test.ts packages/ledger-mcp/test/implementation-candidate-stale-rebase.test.ts packages/ledger-mcp/test/implementation-candidate-head-of-line.test.ts packages/ledger-mcp/test/implementation-candidate-coordinator-race.test.ts packages/ledger-mcp/test/supervisedWorkerGateStorage.test.ts packages/ledger-mcp/test/packagedCodexRoleGitBroker.test.ts packages/ledger/test/guardedRebaseContinuation.test.ts
bun run test:attestation-postgres-required
```

This round remains a committed partial/fail pending the parent final base; no
pass or trusted full-gate claim is made.

## round 4 cross-process acceptance <!-- cq:wip-checkpoint -->

Resumed from the verified retained result at `4fe4b13c`. This round will add
the still-missing three-process SQLite and required-live PostgreSQL coordinator
controls using independent database connections, a controlled temporary Git
repository, explicit process barriers, and bounded teardown. It will also
audit the installed-runner and library-adapter process qualification and
restart-cut clauses, correcting misleading in-process labels where encountered.
The trusted full gate remains `unmeasured` by operator instruction.

The shared coordinator contract now constructs a controlled repository and two
managed task worktrees, stages and qualifies two candidates, advances the
protected branch, then releases three detached Bun processes over independent
SQLite connections (or independent required-live PostgreSQL pools) through an
explicit start barrier. Exactly one process launches the real managed guarded
rebase. The source is retired with lease generation 1, one generation-2
successor is queued under the stable journal operation id, and the trailing
qualified enrollment remains untouched at lease generation 0. A separate stale
process cannot release the retired lease, and a fresh coordinator process
reconstructs the same successor without another rebase or gate launch. Process
groups have bounded TERM/KILL teardown. The SQLite selector passes 1 test, 1
filtered test, 0 failures, and 16 assertions.

The installed dispatch script no longer coordinates synchronously after durable
qualification. It emits the queued handle as its terminal transport result, so
only a later coordinator owns gate/confirm/fetch. The packaged installed
qualification lost-ack fixture now replays the exact qualification request,
asserts no gate at installed-process return, and explicitly coordinates later.
The installed guarded-rebase fixture now seeds finalized-manifest authority and
likewise leases the qualified row only after the installed boundary returns.

The library-adapter control uses an actual executable child and local transport
endpoint. It observes a real Codex JSONL completion, returns `queued`, verifies
the durable qualified row, then separately acquires the exact lease, claims and
completes the parent gate, confirms with the stored native proof, and fetches
the consumed result. The installed-runner control similarly asserts one
qualification and zero coordinator launches before the queued handle returns.

The sandbox does not deliver stdin to the installed fixture's nested workset
provider and forbids the library fixture's loopback listener. Those unchanged
confinement failures were observed once and not repeated. The two process
controls therefore remain for the unrestricted parent run; no pass is claimed
for them here. The required-live PostgreSQL selector is now part of
`bun run test:attestation-postgres-required`, but was not rerun because the
same sandbox cannot create its PostgreSQL listener. Exact focused parent
commands are:

```text
bun test packages/cq-config/test/codexRoleBoundary.test.ts -t 'installed runner returns the queued handle before any coordinator process starts'
bun test packages/cq-config/test/codexProcessQueuedCompletion.test.ts -t 'real library adapter process returns queued, then a qualified lease gates, confirms, and fetches'
bun run test:attestation-postgres-required
```

Mutation evidence: changing the trailing enrollment assertion from lease
generation 0 to 1 failed the SQLite process test at the intended queue-control
assertion. Restoring the original bytes reproduced the 16-assertion pass; the
before/after SHA-256 is
`6f416d9fd262ab09bf1084022aa818ca19ad8151e3685c8b79ed0b1be688725b`
for `implementationCandidateCoordinatorProcessContract.ts`. The sandbox-blocked
installed/library and PostgreSQL process guards have no child-run mutation
evidence; that gap is explicit rather than inferred as success.

`bun run typecheck` and `bun run lint` pass. The repository closure preflight
first reported the expected changed-source digest and new opaque path/spawn
declarations, then passed 1 test with 0 failures after the surgical manifest
refresh. Per operator instruction, the canonical full gate was not invoked.
This remains a committed partial/fail pending the parent final base.

## round 5 library fixture correction <!-- cq:wip-checkpoint -->

Resumed from the verified retained result at `f3b3d115`. The parent reproduced
the library queued-completion process fixture failure: the fixture passes a
plain temporary directory where production settlement requires a Git worktree.
This correction will create a disposable Git repository through the existing
test helpers, preserve the production guard, and rerun the real queued →
qualified lease → gate → confirm/fetch case plus discriminating controls. The
trusted full gate remains `unmeasured`; final-base settlement is parent-owned.

The local fail-first attempt was blocked one precondition earlier than the
parent observation: this sandbox refused the fixture's loopback listener with
`EADDRINUSE` (0 passed / 1 failed). The parent evidence remains the exact
Git-worktree reproduction. The fixture now initializes its disposable root as
a Git repository with the pinned test Git executable before launching the
process boundary; production settlement remains unchanged.

The restored real-process selector reaches the later listener construction but
is still refused by this sandbox (`EADDRINUSE`, 0 passed / 1 failed), so no
local end-to-end pass is claimed. The unrestricted parent should rerun:

```text
bun test packages/cq-config/test/codexProcessQueuedCompletion.test.ts -t 'real library adapter process returns queued, then a qualified lease gates, confirms, and fetches'
```

Discriminating controls are green: the in-memory queued lifecycle passed 1 test
with 5 assertions; qualification plus scheduler passed 3 tests with 10
assertions; the canonical Git-worktree gate control passed 1 test with 3
assertions. `bun run typecheck` and `bun run lint` passed. Mutating the fixture
to pass an invalid `git init` option failed at the new initialization guard as
expected; restoring it reproduced SHA-256
`3a19b797a39cf7cfc87f3fd4582c17b006579c4c4a10c7366b1dc9a460d75fed`.
The real-process passing half of that mutation proof remains parent-runnable
because loopback is unavailable here.

No PostgreSQL suite, canonical full gate, staged pass, or Nix build was run, as
instructed. This round remains a clean committed failure pending the parent
final base, with blocked reason `pending-parent-final-base`.

## round 6 parent continuation fixture correction <!-- cq:wip-checkpoint -->

Resumed from the verified retained result at `b605ba90`. The unrestricted
parent run now reaches confirmation and rejects the fixture's missing
`continuationContext`; production validation is correct. This focused
correction will reproduce the confirm/fetch contract mismatch in an in-memory
equivalent, supply the truthful locked continuation context used by existing
harnesses, and inspect the remainder of the response contract without changing
production security or settlement guards. The trusted full gate remains
`unmeasured`; final-base settlement is parent-owned.

The fixture now confirms with the manager-bound generation's locked logical
tip and exact empty receipt closure. Its assertion follows the production wire
contract: confirmation is a handle-only `{state, result}` acknowledgement, and
only the subsequent fetch contains `finalOutput`. No production validation or
settlement code changed.

The existing in-memory equivalents passed 3 tests with 25 assertions: managed
continuation rejects missing locked evidence and then persists the association;
the lifecycle confirmation is handle-only and fetch materializes the body once;
and confirmation releases the queue front while advancing its partition
revision. `bun run typecheck` and `bun run lint` passed.

The real-process assertion and its mutation cannot execute in this confinement
because the unchanged loopback precondition is unavailable. No passing or
mutation claim is made for that selector. The exact unrestricted parent command
is:

```text
bun test packages/cq-config/test/codexProcessQueuedCompletion.test.ts -t 'real library adapter process returns queued, then a qualified lease gates, confirms, and fetches'
```

No PostgreSQL suite, canonical full gate, staged pass, or Nix build was run, as
instructed. This round remains a clean committed failure pending the parent
final base, with blocked reason `pending-parent-final-base`.

## trusted full gate <!-- cq:wip-checkpoint -->

Unmeasured by instruction; trusted result storage owns the single canonical
full-gate attempt.

## round 7 final-base reconciliation <!-- cq:wip-checkpoint -->

The dispatched final base includes T6572 and activates normal pass handling.
The two historical todo flags are reconciled as completed by the later round
2 through round 6 implementation and evidence recorded above. This round will
verify the rebased implementation and affected declarations on the active
base, run focused acceptance, and leave the canonical full gate unmeasured for
trusted result storage.

The final-base closure preflight reproduced two stale declarations for the
round-5 real-process fixture: its path digest still named the pre-correction
bytes, and its new `Bun.spawnSync` Git initialization lacked a spawn edge. Only
those two declarations were refreshed. Deliberately restoring the stale path
digest failed with `source-digest-mismatch`; deliberately removing the spawn
edge failed with `source-declaration-missing`. After restoring the intended
manifest, the repository closure preflight passed 1 test with 0 failures. The
restored SHA-256 digests are
`c00968317318564a0b5c635c0bf0679fd60d16f65cc3a28dabd3ae288e20df51`
for `cq-gate-closure.json` and
`e95aff0a63e08f05193456127ddbb7f864f995d73e72c6a810b1333b9e0d4db8`
for `codexProcessQueuedCompletion.test.ts`.

`bun run typecheck` and `bun run lint` passed on the active final base. The
exact ten-file selector produced 91 passes, 3 environment-gated skips, and the
same 3 confinement failures retained in prior evidence: nested installed-runner
stdin timed out, the library fixture could not bind a loopback listener, and a
parent-finalizer child produced empty/non-JSON captured stdout. Those failures
are not reported as current passes. The unrestricted parent evidence supplied
with this round records the real-process continuation and required-live
PostgreSQL measurement; the canonical full gate remains owned by trusted result
storage.

The final focused lifecycle selector passed 63 tests with 605 assertions, 3
intentional environment-gated skips, and 0 failures across qualification,
scheduling, stale rebase, head-of-line policy, coordinator races, supervised
storage, the packaged broker, and guarded-rebase continuation. The non-confined
config boundaries passed 5 tests with 11 assertions: durable handle-only queued
return, lost-ack qualification replay, dedicated coordinator invocation,
post-completion draining, and competing-lease waiting. No production source was
changed in this reconciliation round.

## round 8 managed-binding correction <!-- cq:wip-checkpoint -->

The trusted generation-8 gate recorded 6678 passes and 9 failures. Eight
failures were reproduced in the complete `gitChangeDispatchCapability.test.ts`
selector before this correction: guarded-rebase preparation overwrote the
immutable managed-worktree binding `baseCommit` with the bridge `ontoCommit`,
so all eight manager identity checks rejected the prepared dispatch. The ninth
gate failure remains unidentified and is not attributed to this cause. This
round will preserve the manager binding, derive the queue-observed execution
base from authenticated prepared/bridge evidence, add public regressions for
the guarded broker lifecycle and rebased queue qualification, and run the
combined affected selectors. The `trusted full gate` remains `unmeasured` for
parent-owned finalization.

The unchanged full broker selector reproduced the reported signature: 25
passes and 8 failures, all at `managed worktree binding changed at baseCommit`.
The correction removes the guarded prepare's overwrite of the immutable
manager binding. The retired-successor claim now compares all immutable manager
coordinates with its source row while continuing to bind the prepared input to
the journaled `ontoCommit` and `rebasedStartCommit`. Queue qualification uses
the verified bridge `ontoCommit` as its execution base; the existing enqueue
guard still requires that value to equal the authenticated prepared input.

The new real Git-backed qualification regression failed first at the obsolete
binding-base/onto equality. After the identity correction it advanced and
failed at `observedBaseCommit`, proving the queue still used the manager base;
after the execution-base correction it passed the complete qualify → lease →
retire → guarded-successor → stage → qualify sequence. Both qualification
cases now pass with 7 assertions. The complete broker lifecycle now passes 33
tests with 167 assertions, including the new public assertion that the guarded
binding retains the manager base while its bridge retains the distinct onto
commit. Combined boundary validation and closure/type checks remain pending.

The first typecheck exposed only two local narrowing defects in the new guard
and regression (`gitEffectBinding` and the located successor row). Explicit
required-value checks replaced those implicit assumptions; the restored
`bun run typecheck` passes.

`bun run lint` passes. The repository closure preflight first rejected the
three stale opaque-edge declarations for the changed broker source and its two
real-process regressions. After refreshing only those source digests, the exact
repository closure test passed 1 test with 0 failures.

The corrected combined 11-file selector ran the complete broker file together
with every queue/boundary acceptance file: 125 passed, 3 environment-gated
skips, and 3 known confinement failures. The failures are unchanged from prior
rounds: installed-runner stdin timed out, parent-finalizer stdout was empty and
therefore non-JSON, and the real library fixture could not bind a loopback
listener. All 33 broker cases, both qualification cases, and every in-process
queue/scheduler/stale-rebase/head-of-line/coordinator/storage/continuation case
passed. Required-live PostgreSQL and the canonical full gate remain parent-run.

Controlled mutation evidence from this round:

- reintroducing `baseCommit: bridge.ontoCommit` failed the public guarded
  lifecycle at the exact retained-manager-base assertion; restoration passed;
- replacing the bridge-derived queue execution base with `binding.baseCommit`
  failed the rebased qualification at `observedBaseCommit`; restoration passed;
- restoring the obsolete successor requirement `binding.baseCommit ===
  source.ontoCommit` failed guarded successor admission; restoration passed.

The restored SHA-256 digests are
`0f95561598bab162a073fa4670d71e6c52fce0833f3aa93a3e9d21fbed49976a`
for `dispatchAttestation.ts`,
`6e1018e676bd2e62ff98bfd41c0dd66633e61ff3e1319edd1ab4cc46ccc6f7b0`
for `dispatchCapability.ts`,
`fe5282938e8409d202ba5bd38815d6a5b01247503fce62da6f5530365bff43bc`
for `gitChangeDispatchCapability.test.ts`, and
`807ce7a6fe9c7b66b2929b1e2df7ee1b4df433d8b8cdbac6c26c657c1cdb131f`
for `implementation-candidate-qualification.test.ts`.

## round 9 guarded fixture truth correction <!-- cq:wip-checkpoint -->

The generation-9 trusted gate recorded 6680 passes and 8 failures. Seven
failures were reproduced before this correction; one gate failure remains
unidentified and will not be guess-patched or used to justify an unchanged
gate rerun. This round will correct only the stale positive fixtures identified
by the operator: the attestation-store and dispatch staged-rebase fixtures must
retain the immutable manager base while their input, bridge, and queue execution
base use `ontoCommit`. The existing tombstone positive fixture will receive the
same truthful construction without weakening production guards or the negative
substitution controls.

The complete shared in-memory, SQLite, staged-rebase-source, and
single-project construction suites will be run with the corrected broker and
rebased qualification/queue regressions. All non-gate checkpoints will be
reconciled against observed evidence before result storage. The `trusted full
gate` checkpoint remains `unmeasured`; trusted result storage owns the one
canonical gate attempt for the changed candidate.

The unchanged focused fixtures reproduced three stale-construction failures:
both shared store backends and the direct successor-allocation case were
rejected at `gitEffectBinding.guardedRebaseBridge` because the fixture replaced
the immutable manager base with `ontoCommit`. Removing those substitutions made
all three pass. Applying the same truthful manager binding to the tombstone
fixture then exposed its helper's conflation of manager base and queue execution
base at `observedBaseCommit`. The helper now accepts that execution base
explicitly: initial candidates use the manager base, while guarded successors
use `ontoCommit`. The restored focused run passed 4 tests with 17 assertions.

The pre-correction SHA-256 digests were
`f21464e41fad3c1aa5f4d56b5433874fa04a464d3618f8059a5d0b45d5ab4ad9`
for `attestationStoreContract.ts` and
`fdf8d0345de21a985fd1831817e2a2e479735f07e6c5f53fd929c8e64fae8443`
for `dispatchStagedRebaseSource.test.ts`. The restored digests are
`80db3169b4563d9bc25fe3324760c7149b6c14a97587894f74e45afb1d160dde`
and `64db04ecbdd4b7d647d783b5ad8774894908553644ded20297f034d8d7dd1766`
respectively.

The four complete shared suites passed 322 tests with 2,230 assertions and no
failures across the in-memory dummy, SQLite adapter, staged-rebase source, and
all four single-project construction modes. The corrected broker lifecycle,
rebased qualification, scheduler, and stale-rebase routing passed together:
41 tests, 207 assertions, no failures. The repository closure preflight passed
without a manifest change because neither corrected fixture owns an opaque
edge. `bun run typecheck` and `bun run lint` also passed.

The exact ten-file acceptance selector completed with 92 passes, 707
assertions, 3 environment-gated skips, and the same 3 confinement failures
recorded in earlier rounds: the installed-runner fixture timed out waiting for
nested process input, the parent-finalizer child produced empty/non-JSON
stdout, and the real library adapter could not bind its loopback listener.
Every in-process qualification, scheduler, stale-rebase, head-of-line,
coordinator-race, supervised-storage, broker, and guarded-continuation case
passed. No production guard was weakened for the sandbox. The required-live
PostgreSQL selector remains delegated to the unrestricted parent as directed.

No production source changed in this correction. The truthful fixtures retain
manager base `baseCommit`, use `ontoCommit` for guarded input, bridge, and queue
execution base, and preserve the tombstone restart/replay/source-uniqueness and
negative-substitution controls. All non-gate round-9 work is complete; the
trusted full gate remains runner-owned and `unmeasured` here.

## round 10 D362 rendered-envelope correction <!-- cq:wip-checkpoint -->

The generation-10 trusted gate passed 6,687 tests and rejected one obsolete
positive assertion in `codexRenderedEnvelopeBoundary.test.ts`. This correction
will reproduce that exact D362 failure, then update the complete positive
registered-child lifecycle fixture to stage a result, observe native process
completion, qualify the queue entry, and return only the queued handle. The
fixture must retain missing-target rejection before side effects, exact target
admission before launch, child authority isolation, and the prohibition on any
direct gate or finalizer before qualification. The trusted full gate remains
runner-owned and `unmeasured`.

The unchanged fixture reproduced the exact gate rejection: 0 passes / 1
failure at the obsolete assertion that the rendered script directly contained
`parentGateCapability: invocation.parentGateCapability`. The corrected fixture
now emits a `gate-pending` staged-result acknowledgement, supplies the required
registered-process correlation, captures one exact qualification request, and
returns a `queued` acknowledgement while the public runner returns only the
dispatch handle. It asserts one target admission before child launch, native
completion identity, zero direct finalizer/coordinator commands, and exclusion
of the target, parent-gate authority, and ambient ledger credentials from both
child and qualifier transports.

A controlled missing-target mutation failed at the intended assertion before
any child side effect; restoration returned the fixture digest to
`8c5ea0c59eac8250f4bb5eebf6e42a6a0109d7b2e7f3f79424de2b7d93f6ab71`.
The positive registered subprocess cannot settle in this confined sandbox; the
unchanged neighboring installed-runner fixture independently times out at the
same nested provider boundary. This is recorded as a confinement observation,
not a passing fixture result; the complete script-dependent and static check
outcomes follow below.

The requested five-file script-dependent selector completed with 42 passes,
13 confinement failures, and 287 assertions. The failures retain the already
measured signatures: nested registered-provider timeouts, empty captured stdout
from successful Node children, lost newline-terminated child stdin, explicit
`pipe-capture-lost`, and downstream process-group observations that were never
published. The corrected D362 fixture and the unchanged installed queued-return
fixture both timed out at the same registered-provider boundary. No failure
identified a production-source regression or a distinct D362 assertion fault.

`bun run typecheck` and `bun run lint` pass. The repository closure preflight
first rejected the corrected fixture's stale opaque path digest, then passed 1
test with 0 failures after updating only its path and spawn declarations.
Changing one restored declaration digest from `...ab71` to `...ab70` failed
with the expected `source-digest-mismatch`; restoring it returned
`cq-gate-closure.json` to SHA-256
`9545681fe4de535b3abbcb7dcc2da5f116417aaa62d2af73315f69efcd64f8be`
and the closure preflight again passed 1/1. No production source changed. All
non-gate work for this correction is complete; the trusted full gate remains
runner-owned and `unmeasured`.

## round 11 packaging correction <!-- cq:wip-checkpoint -->

Generation 11 is retained at the exact guarded-rebase tip after the trusted
workspace gate passed 6,688 tests. The parent packaging check rejected the
installed implement-worker smoke fixture because the rendered child result was
staged without a registered native process observation. This round will read
the complete rejection evidence, reproduce that installed-fixture failure,
align the pipe and PTY smoke paths with the staged → observed → qualified →
handle-only lifecycle, and request the prescribed exact-commit parent package
check. The trusted full gate remains runner-owned and `unmeasured`.

The named parent handoff log was not present in the worktree or `/tmp`, and
this worker's restricted ledger profile exposes no `read_log` operation. The
dispatch criticism nevertheless carries the decisive failure signature. Source
inspection confirmed its mechanism: both installed smoke invocations omitted
`CQ_CODEX_ROLE_CORRELATION_ID`, so the production runner completed the fake
child without producing a registered observation and rejected it before
qualification. The fixture's fake ledger still implemented the obsolete
direct parent-finalizer command.

The installed pipe and PTY paths now use one exact correlation identity. Their
fake ledger accepts only the implementation-candidate qualifier, validates the
complete capability-free native observation (handle, role, correlation, child
thread, completed outcome, exit status, timestamp, and prompt digest), returns
the matching queued acknowledgement, and fails if either a parent finalizer or
coordinator is invoked before the handle is returned. The existing request
framing, workset-effect provider, handle-only stdout comparison, capability
sentinels, and PTY-open settlement checks are unchanged.

Two direct attempts to reproduce the package build were blocked before
evaluation by this child's confinement: first the Nix fetcher cache was
unwritable, then an isolated cache reached the daemon socket and received
`Operation not permitted`. Neither is claimed as package evidence. Static Nix
parsing passed. The repository closure preflight passed 1 test with 0 failures
in 37.81 seconds; `flake.nix` is not an opaque closure source, so no manifest
digest changed. The exact-commit package build and flake enumeration remain
pending from the parent-owned focused build loop.

## round 12 packaged retry and ownership correction <!-- cq:wip-checkpoint -->

The parent package run passed the pipe and PTY smoke paths, then reproduced two
focused failures in `packagedCodexRoleGitBroker.test.ts`: lost qualification
acknowledgement replay produced no effective native-completion observation, and
the guarded-rebase fixture attempted to set the library-managed
`worksetOwnerRef` through generic item mutation. This round will reproduce both
causal tests, correct the installed boundary and fixture construction without
weakening authority or replay guards, and run only the selected regressions,
typecheck, lint, and closure preflight. Exact parent packaging validation and
the trusted full gate remain pending and unmeasured.

The installed dispatch script now appends its authenticated native process
outcome immediately after child settlement and before candidate qualification.
Consequently a qualification-path exception cannot leave the trusted parent
with only a preturn record. Deliberately restoring the old ordering failed the
focused source-order probe with `native outcome must be durably observed before
candidate qualification`; restoring the correction passed and returned the
script to SHA-256
`cd5334d167a94403e98d82605843b70fb0746487ced0d777a9b8750d8f821334`.
The existing exact-request lost-ack replay regression passed 1 test with 0
failures. A nested real-script probe timed out at the sandbox's previously
recorded subprocess boundary and is not counted as causal evidence.

The installed guarded-rebase fixture no longer passes `worksetOwnerRef` or
`worksetOwnerEdgeKind` through generic mutation. It now claims a real plan,
publishes the one-task draft, records the bound go-ahead review, finalizes the
manifest through `PlanLifecycleStore`, and uses the lifecycle-allocated task
identity for the existing guarded-rebase scenario. `bun run typecheck` passes.
Focused fixture checks, lint, closure refresh/preflight, and the exact parent
package build remain pending.

The generic ownership-fence control passed its exact selector 1/1, confirming
the parent-observed `WorksetOwnershipFieldError` was the intended library
boundary rather than an authority guard to relax. The packaged broker source
selector passed its one non-installed case with 3 installed-only cases skipped;
those skips are not package acceptance evidence. `bun run lint` passed. The
closure manifest now binds the installed script digest above and fixture digest
`e384f19a7ea54fd927eed78fb84eb4f72387915c74ad71ea301148f6658aa014`.
Changing the script declaration by one hex digit failed the repository closure
preflight with the expected `source-digest-mismatch`; restoration passed 1/1
and left `cq-gate-closure.json` at SHA-256
`882a12c217e4b87974d537f6ca5c6b131b1b1c0a854356f40b5888dadf041adb`.

The focused round is complete, but the parent must rebuild the exact clean
commit and run the two installed-only cases. Per the operator instruction, no
canonical gate was started and the `trusted full gate` checkpoint remains
`unmeasured`. Parent packaging validation remains pending.

## round 13 installed identity fixture correction <!-- cq:wip-checkpoint -->

The parent package run kept the same derivation and exposed fixture identity
mismatches after the round-12 observation and ownership corrections. This
round will reproduce the focused positive installed retry and guarded-rebase
failures, align prepared child identities with the actual native process
correlations through the existing formatter/constant, and preserve every
foreign-identity negative and exact lost-ack replay assertion. Only the
selected regressions, typecheck, lint, and repository closure preflight will
run here. Parent packaging validation and the trusted full gate remain pending
and unmeasured as directed.

The unchanged fixture identities reproduced both causal mismatches in one
focused probe. The retry prepared
`t2042-packaged-child-retry/t2042-packaged-run-retry` but qualification observed
`implement-worker#t2042-installed-worker-retry/t2042-packaged-broker`; the
guarded helper likewise prepared its synthetic label child/run while observing
`implement-worker#t2151-<label>/t2151-packaged-guarded`. The positive installed
worker paths now derive the prepared child id from the shared Codex correlation
separator and reuse the same correlation variable at launch, while binding the
run id to the fake worker's actual `thread.started` value. The restored two-case
identity probe passes. Explicit foreign-identity negatives are unchanged.

The packaged broker file's available source selector passed 1 test with 19
assertions; its three installed-only cases were skipped because this worktree
has no installed role/sandbox executable inputs, so they are not counted as
package evidence. The complete in-process qualification file passed 2 tests
with 7 assertions, including the guarded-rebase manager/execution-base case.
`bun run typecheck` and `bun run lint` passed.

The repository closure preflight first rejected the stale fixture declaration
with `source-digest-mismatch`. Both its spawn and path declarations now bind
the restored fixture digest
`f3fe29d3e809f9d19664fdd265612c63d64a87f9b51f2456105fa43de6ceb116`.
A deliberate one-digit declaration mutation failed with the same expected
reason; restoration returned `cq-gate-closure.json` to SHA-256
`01e2556c046d38988a3e455477671c8ad52156823beb2cfb6e5fbba744d179cd`
and the closure preflight passed 1/1. Per the operator instruction, no canonical
gate was started. The parent must rebuild this exact clean commit and run the
installed-only retry and guarded-rebase cases; parent packaging validation and
the `trusted full gate` checkpoint remain pending and unmeasured.

## round 14 installed lifecycle fixture correction <!-- cq:wip-checkpoint -->

The parent package probe advanced both installed scenarios past the identity
checks and exposed two fixture lifecycle defects. The retry scenario never
creates durable task `T2042` before qualification, while the guarded-rebase
scenario performs a second fetch after the coordinator has already consumed
the one-shot result. This round will reproduce those exact focused failures,
seed the retry task through the supported finalized-plan lifecycle while
retaining one live ledger store for every capability and restart, and observe
the guarded result through the protected evidence-observer API without a
second fetch. Selected regressions, typecheck, lint, and repository closure
remain the only worker-owned checks. Parent packaging validation and the
trusted full gate remain pending and unmeasured as directed.

The authoritative parent package probe reproduced the unchanged source with 44
passes, 2 failures, and 376 assertions: the installed retry failed
qualification because `tasks:T2042` did not exist, and the guarded runner's
post-coordinator fetch returned `output-already-materialized`. This confined
worktree has no installed role/sandbox inputs, so its pre-correction package
selector passed the source case and skipped all three installed-only cases;
that skip is not claimed as reproduction evidence.

The two installed fixtures now share one supported finalized-plan helper. It
claims, publishes, receives a bound go-ahead review, finalizes, and marks the
lifecycle-allocated task `wip`; every worker, managed-worktree, workset-effect,
and expected gate-evidence input uses that allocated id. The live durable
ledger store is supplied to every relevant capability construction and remains
open through restart, coordination, reviewer, resolver, and protected evidence
observation. The guarded fixture reuses the same helper and store lifetime.

Both coordinator-completed worker paths now observe their durable result via
the protected non-materializing evidence observer. Review and guarded proof
consumers accept that actual observation directly; no cast to
`ConsumedDispatchResult` and no raw-row reconstruction remains. Because the
observer intentionally lacks the backend-owned one-shot materialization brand,
the obsolete positive worker-provider/native-registry assertion was removed.
The installed resolver still supplies positive provider authentication, and
the fixture retains exact lost-ack replay, foreign child/executable controls,
sandbox controls, receipt lineage, and exact-tip/correction coverage. A new
negative proves observed worker evidence cannot masquerade as a one-shot
backend materialization.

The available focused selector passed 35 tests with 3 installed-only skips and
494 assertions across the packaged source probe, qualification, and supervised
storage suites. `bun run typecheck` and `bun run lint` pass. The closure
manifest now binds fixture digest
`0370c654c4c39664eff525f906a24798b43aecf176a72d2ccfe67b02f09dd085`.
Changing its spawn declaration by one hex digit failed the repository closure
preflight with the expected `source-digest-mismatch`; restoration returned the
manifest to SHA-256
`1774e1b99de1b1a12952824cf683a54f18e78478f8d37bf0fa8e6b9b29a50e4a`,
and the preflight passed 1/1. Installed package execution remains unavailable
here, so the parent must rebuild the exact clean commit and run both corrected
installed-only scenarios. The trusted full gate remains `unmeasured`.

## round 15 consumed-continuation correction <!-- cq:wip-checkpoint -->

The parent package probe advanced the installed retry scenario through the
coordinator but found no parent-gate count artifact, and rejected the guarded
successor because the ordinary completed queued source is already consumed.
This round will reproduce those exact focused failures before changing
production behavior, distinguish consumed queue history from an unretired
staged candidate without weakening the existing guarded-rebase negatives, and
measure the real parent-side gate invocation. The trusted full gate remains
`unmeasured` as directed.

The consumed ordinary lifecycle now has a direct fail-first regression. It
stages and qualifies the worker, leases and completes the parent gate, confirms
the exact native completion, observes the resulting released/gate-complete
queue control, and then requests the guarded successor. Before the production
correction it failed with `the guarded-rebase source is not a retired
implementation queue enrollment` (0 pass / 1 fail). The successor predicate
now admits only that exact consumed, released, qualified completion and still
binds the old result, task, repository, worktree, and manager identity. The
restored regression passes, including foreign-result and foreign-manager
negatives; the staged-retirement and cancelled/parent-lost controls also pass
(25 tests, 87 assertions across the two focused files).

The retry gate-count fixture now exports `CQ_T2042_GATE_COUNT` only around the
later parent coordinator call and restores the parent environment afterward.
The existing pre-coordinator absence check remains unchanged, so the fixture
continues to distinguish zero child-side gates from exactly one real
parent-side gate. Installed package execution is unavailable in this worktree;
that assertion remains pending the parent package probe rather than being
synthesized locally.

The repository closure preflight then reproduced the expected
`source-digest-mismatch` against the changed packaged fixture. Both its opaque
spawn and path declarations now bind fixture SHA-256
`2299240cb863e44a30f2bf31b3a8825820f67665e5493a0b13722fa643f1efe9`;
the restored closure preflight passes 1/1.

The five-file bounded regression selector passed 33 tests with 3 explicit
installed-only skips and 147 assertions. Lint passed. Its concurrent typecheck
found that the consumed envelope/tombstone correlation was not preserved by a
detached union variable and that the test output helper exposed an unnecessarily
broad JSON type. The guard now narrows through the source row itself and the
helper returns its actual record type; typecheck and the 25-test queue/source
selector both pass after that correction.

Final bounded evidence is green: the five-file selector passes 33 tests with 3
installed-only skips and 147 assertions; typecheck, lint, and the repository
closure preflight pass. Deliberately changing the new released-queue predicate
to require `terminal` reproduced the original guarded-successor rejection (0
pass / 1 fail); restoration passed and returned
`dispatchAttestation.ts` to SHA-256
`f95151bd70708e635d47b865f81b9c1ecc13cc4682e65fbd573fa36e6421a10d`
and the regression fixture to
`0feddb5d59c31f4d01aba8f45165dc03d3f22674e23d790081a9de141d691d89`.
The installed retry/guarded-continuation package cases and canonical full gate
remain parent-owned and unmeasured, so this focused correction reports a fail
result until that Nix evidence is available; it does not claim task completion.
