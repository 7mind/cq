```json
{
  "taskId": "T2317",
  "role": "implement-worker",
  "baseCommit": "9263e6983ffcaf5de0f30a76f8c616ead9d12c14",
  "startedAt": "2026-08-23T06:12:11.519Z",
  "checkpoints": [
    {
      "name": "trusted full gate",
      "status": "todo"
    }
  ]
}
```

## trusted full gate <!-- cq:wip-checkpoint -->

Generation 4 resumed from the exact independently reviewed generation-3 tip.
The reviewer reproduced two canonical-path escapes: an external manifest
reached through a repository-relative `cq-gate-closure.json` symlink, and an
external `bun.lock` reached through a declared target-root symlink. Focused
fail-first tests now reproduce both cases and the equivalent manifest-declared
source escape: all three returned `resolved` instead of `invalid`. The focused
command reported 2 passes, 3 failures, and 9 assertions; the in-repository
manifest/lock symlink control passed. The resolver now canonicalizes and
repository-containment-checks the manifest before reading it, each explicit
opaque-edge source before hashing it, and each Bun lock before accepting a
target or discovered install root. The symlink selector now passes 5 tests with
12 assertions, including the in-repository controls.

Four independent guard mutations each produced the expected single-selector
failure: bypassing manifest canonicalization returned `resolved` for the
external manifest; bypassing lock canonicalization returned `resolved` for the
external target lock; bypassing explicit-source canonicalization returned
`resolved` for the external source; and rejecting all symlinks returned
`invalid` for the in-repository control. The respective mutated source hashes
were `b72a19dbaa946dee0617b5f69573941a8441c6ffbb75d2b43a1934cd0865e0b5`,
`0d469c60a0b1f1a8e2b7499070d66a1df8030408c85d7c578eeff214bdcb65fb`,
`ce762fe6967a77d75678f48ffbc1525027a71e0539585ca8e42518ab70b897a8`,
and `3d66617788747a7c6c946cf941964dad56ac88ef053ca4018b9d11d7a7ee18a2`.
Every restoration returned `gateClosure.ts` to
`617f5bb2a809813fd9c119de7635751b29eaa0dc98dd15b837d3be7f8b43cf6f`
and the exact selector to one pass. The closure manifest now declares the new
gate-closure test digest. Restoring the stale digest changed the manifest hash
from `f99b2255541cdeda73112b1fc01fb71acb7ae287ac670a16fef3c22ff24f8ecb`
to `0e464d04d3a8472261447783fe145725654a97105e0c6f8844f8022df22b17a0`
and direct real-tree resolution failed with `source-digest-mismatch` for the
exact old/new test hashes. Restoring the intended manifest hash returned
resolution to the ordered `cq-ledgers`, `cq-subagent-dispatch`, and
`ledger-status` roots. Regression verification and trusted full-gate handoff
remain todo.

Generation 3 resumed from the authoritative generation-2 tip after the trusted
parent gate reported two failures in
`packages/ledger-mcp/test/worktreeTerminalReleaseXdg.test.ts`. Both production
XDG terminal-release fixtures reached `worktree_manage prepare` without
`cq-gate-closure.json`, so preparation was refused with
`gate-closure-invalid` / `manifest-missing` instead of returning `prepared`.
This captured full-gate failure is the correction-round fail-first
reproduction. An exact-file rerun at the generation-3 starting tip confirmed
the same failure mode: 1 pass, 2 failures, and both failed prepares reported
`gate-closure-invalid` / `manifest-missing`. The fixture correction and
focused verification remain todo.

The production XDG fixture now commits a version-1 closure manifest beside its
target package and lockfile. Its target script-DAG digest is computed through
the production ledger API, and its empty opaque-edge inventory is complete for
the source-free temporary target. The exact D336 file now passes 3 tests with
47 assertions. A filename mutation changed the test source SHA-256 from
`1434c65b02f334edf18f16c672a46030521dd5a58c0d19d3bb42131525606702`
to `f1ce68fe6f728297c1fad76a592e11a9350b8e9348e8f316188fb111931ab6ca`
and restored the same 1-pass / 2-failure `manifest-missing` signal. Restoring
the intended bytes returned the source hash to `1434c65b...` and the suite to
3 passes with 47 assertions.

Final generation-3 verification passed 47 tests with 318 assertions across
the exact D336, gate-closure, and managed-worktree files. The production
resolver returned the ordered `cq-ledgers`, `cq-subagent-dispatch`, and
`ledger-status` roots. TypeScript, exact-file ESLint and Prettier, and the
multiline single-WIP identity/checkpoint invariant are green. The brokered
result-storage boundary owns the canonical full gate after this child exits.

Generation 2 resumed from the inherited six-receipt tip. Base, branch, clean
tree, and receipt-chain continuity were reverified before further checks.

Generation 2 closure mutations bypassed script-DAG and source-digest drift,
opaque and Nix declarations, dynamic-target and Bun-lock completeness,
canonical and lexical containment, static-root inclusion, unreachable-root
exclusion, and Q388 statement boundaries. Every exact selector failed once as
predicted. After restoration, the closure suite passed 11 tests with 52
assertions and restored its source/test SHA-256 digests.

Generation 2 managed-worktree mutations selected the divergent seed, installed
only the first closure root, and suppressed extension `typebox`
materialization. The focused selector failed respectively with preparation
refusal, an omitted extension install plan, and `ENOENT`; restoration returned
it to 1 pass with 5 assertions and restored both file digests.

Generation 2 final reruns passed 58 tests with 407 assertions for the closure,
managed-worktree, and MCP files, plus 37 tests with 377 assertions for adoption
and expected-failure policy. TypeScript, exact-file ESLint and Prettier,
three-root real-tree resolution, exact Q388 fixture bytes, and multiline WIP
identity checks are green.

Completed: reproduced the missing dependency, implemented and mutated the
focused regressions, completed the target-derived closure correction, and
prepared the full canonical gate for trusted result storage.

Fail-first reproduction (fresh prepared worktree, before production changes):

```text
TYPEBOX_MISSING
error: Cannot find package 'typebox' from '.../nix/pkg/pi-extensions/cq-subagent-dispatch/index.ts'
REPRO_EXIT=1
```

The broader packaged prompt-root test reached Nix first and was blocked by the
sandboxed Nix cache, so it was not counted as defect evidence. The narrowed
canonical extension import removed that unrelated precondition and failed at
the target dependency boundary.

Q388 fail-first evidence: the retained T2290 closure baseline passed 11 tests
and 49 assertions. Adding the exact one-line namespace/destructuring source to
the CommonJS alias selector then produced 0 passes and 1 failure: the resolver
returned `resolved` where the undeclared spawn edge must return `invalid`.

The minimal `;`/newline statement-boundary matcher restored that selector to 1
pass and 12 assertions. The adapted current-main closure, managed-worktree, and
worktree-MCP suites pass 58 tests with 407 assertions; TypeScript and exact-file
format checks pass. Direct resolution of the current manifest returns the
ordered `cq-ledgers`, `cq-subagent-dispatch`, and `ledger-status` Bun roots.

Guard mutations bypassed script-DAG drift, source-digest drift, opaque-edge
declarations, dynamic target completeness, Nix declarations, target locks,
canonical and lexical containment, static-root inclusion, unreachable-root
exclusion, Q388 statement boundaries, clean-target selection, all-root install,
and typebox materialization. Every exact selector failed as predicted; after
each restoration, `gateClosure.ts` returned to
`e1b2b4d8f3d1c319ff55544e39541faceaba9b8188c1d0c24480f7c61591d0b6`,
`managedWorktree.ts` to
`a56d49914fa3a6d27fa670d3089e9435d2be98aa2d8798f1f1876f53ada8099d`,
and `managedWorktree.test.ts` to
`616e816f0e19782522f6c0bb9465f319d8b167d146ab90e729dc7cff48bd7dac`.

Adoption and expected-failure policy add 37 passing tests and 377 assertions,
for 95 focused passes and 784 assertions across the two focused commands.
Exact-file ESLint, TypeScript, Prettier, real-tree closure resolution, and WIP
identity checks are green. Brokered result storage owns the canonical full gate
after this child exits; no in-child `cq gate run` is permitted on this dispatch.

The final committed-tip resolution check initially failed closed on stale
pre-format digests for `managedWorktree.ts` and `managedWorktree.test.ts`.
Advancing all four declarations to the already-recorded restored file hashes
returned direct resolution to the exact three-root closure.
