```json
{
  "taskId": "T2317",
  "role": "implement-worker",
  "baseCommit": "9263e6983ffcaf5de0f30a76f8c616ead9d12c14",
  "startedAt": "2026-08-23T06:12:11.519Z",
  "checkpoints": [
    {
      "name": "trusted full gate",
      "status": "todo"
    }
  ]
}
```

## trusted full gate <!-- cq:wip-checkpoint -->

Todo: reproduce the missing dependency, implement and mutate the focused
regressions, complete the target-derived closure correction, and delegate the
full canonical gate to trusted result storage.

Fail-first reproduction (fresh prepared worktree, before production changes):

```text
TYPEBOX_MISSING
error: Cannot find package 'typebox' from '.../nix/pkg/pi-extensions/cq-subagent-dispatch/index.ts'
REPRO_EXIT=1
```

The broader packaged prompt-root test reached Nix first and was blocked by the
sandboxed Nix cache, so it was not counted as defect evidence. The narrowed
canonical extension import removed that unrelated precondition and failed at
the target dependency boundary.

Q388 fail-first evidence: the retained T2290 closure baseline passed 11 tests
and 49 assertions. Adding the exact one-line namespace/destructuring source to
the CommonJS alias selector then produced 0 passes and 1 failure: the resolver
returned `resolved` where the undeclared spawn edge must return `invalid`.

The minimal `;`/newline statement-boundary matcher restored that selector to 1
pass and 12 assertions. The adapted current-main closure, managed-worktree, and
worktree-MCP suites pass 58 tests with 407 assertions; TypeScript and exact-file
format checks pass. Direct resolution of the current manifest returns the
ordered `cq-ledgers`, `cq-subagent-dispatch`, and `ledger-status` Bun roots.
