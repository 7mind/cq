```json
{
  "taskId": "T2317",
  "role": "implement-worker",
  "baseCommit": "9263e6983ffcaf5de0f30a76f8c616ead9d12c14",
  "startedAt": "2026-08-23T06:12:11.519Z",
  "checkpoints": [
    {
      "name": "generation 10 package-manifest symlink closure",
      "status": "done"
    },
    {
      "name": "generation 9 identifier-escape closure",
      "status": "done"
    },
    {
      "name": "generation 8 computed-member CommonJS closure",
      "status": "done"
    },
    {
      "name": "generation 7 CommonJS loading closure",
      "status": "done"
    },
    {
      "name": "generation 6 Bun configuration closure",
      "status": "done"
    },
    {
      "name": "trusted full gate",
      "status": "done"
    }
  ]
}
```

## generation 10 package-manifest symlink closure <!-- cq:wip-checkpoint -->

Generation 10 resumes from the exact reviewed generation-9 tip. Independent
review demonstrated that nearest-Bun-root discovery admits a repository-local
root whose package.json is a symlink to an external manifest because only the
lockfile is canonicalized. Exact fail-first reproduction, canonical regular-file
and repository-containment enforcement for the candidate package manifest,
focused mutation evidence, final verification, and trusted full-gate handoff
remain todo.

The focused external-package-manifest symlink regression is committed before
production correction. A repository-contained static-import source resolves to
a sibling root with an internal bun.lock and a package.json symlink to an
external regular file. The selector reported 0 passes, 1 failure, and 1
assertion because closure resolution returned resolved instead of the required
path-escape. Production correction, focused mutation evidence, final
verification, and trusted full-gate handoff remain todo.

Nearest-Bun-root discovery now canonicalizes every candidate package.json,
rejects an existing candidate whose real path escapes the repository, and
admits only canonical regular files before checking the sibling Bun lock. The
external-symlink and non-file selectors pass 2 tests with 4 assertions. Two
independent mutations produced the predicted failures: disabling the escape
classification returned bun-root-incomplete instead of path-escape, and
admitting a directory as the package manifest returned resolved instead of
invalid. Restoration returned gateClosure.ts to
e5e4fc1d5ea8e3ae7c3788d6475acefc70427b98741b85ce4cc8a3cd2b74e115
and gateClosure.test.ts to
2845d71472a7b2582467707a08a4161bc5ace0f8f143d381c1b2b2a684be5acb.
The real-tree manifest now carries that test digest for both opaque edges.
Final focused verification and trusted full-gate handoff remain todo.

Final generation-10 verification passes 35 gate-closure tests with 344
assertions and 50 managed-worktree, worktree-management, and production D336
XDG tests with 402 assertions. The exact Q388 same-line CommonJS
namespace-destructuring selector separately passes 1 test with 12 assertions.
TypeScript, exact-file ESLint, and Prettier are green. Direct real-tree closure
resolution returns the ordered cq-ledgers, cq-subagent-dispatch, and
ledger-status install roots; the manifest hash is
7ae86abdd5267527966ad599bc40725db1326a59e099a2d0111ea08882674102.
The committed tree has exactly WIP-T2317.md with the matching embedded task
identity and trusted full gate checkpoint, and no WIP-T2315.md. The trusted
parent owns the fresh canonical full gate after this child exits.

## generation 9 identifier-escape closure <!-- cq:wip-checkpoint -->

Generation 9 resumes from the exact reviewed generation-8 tip. Independent
review demonstrated that pinned Bun executes a CommonJS loader identifier
containing an ECMAScript Unicode escape while closure classification omits the
sibling root. Exact pinned-Bun fail-first reproduction; complete fixed-width
and code-point identifier-escape admission outside inert lexical regions;
valid, invalid, control, and mutation regressions; manifest refresh; focused
verification; and trusted full-gate handoff remain todo.

The exact pinned-Bun escaped-identifier reproduction is committed before any
production correction. A CommonJS source spells the executable loader as
requ\u0069re(modulePath), records the sibling-root execution marker, and
requires closure resolution to fail closed with the stable
source-identifier-escape-unsupported reason. Failure-mode measurement,
complete escape-domain controls, production correction, mutation evidence,
manifest refresh, focused verification, and trusted full-gate handoff remain
todo.

The fail-first selector is confirmed on Bun 1.3.13. The escaped CommonJS
identifier executed the sibling module and emitted
T2317_IDENTIFIER_ESCAPE_REQUIRE_EXECUTED, while closure resolution returned
resolved instead of invalid. The selector reported 0 passes, 1 failure, and 3
assertions at the intended closure-status assertion. Complete escape-domain
controls, production correction, mutation evidence, manifest refresh, focused
verification, and trusted full-gate handoff remain todo.

The production boundary now rejects executable source containing the
ECMAScript identifier Unicode-escape introducer before loader and opaque-edge
classification, with a dedicated stable reason and source-relative detail.
The same lexical mask leaves escape text in comments, quoted strings, template
text, and regular-expression literals inert while retaining executable
template expressions. Fixed-width, code-point, malformed, inert-control, and
mutation verification remain unmeasured; manifest refresh, focused
verification, and trusted full-gate handoff remain todo.

Eight independent mutations produced their predicted failures. Disabling the
guard returned resolved for the exact executable reproduction after 3
assertions. Restricting recognition to fixed-width escapes returned resolved
for the code-point case after 4 assertions; recognizing only well-formed
escapes returned resolved for the malformed case after 10. Scanning raw source
instead of the executable-code mask rejected the inert lexical control.
Substituting the generic declaration reason and omitting source-relative detail
failed their dedicated assertions. Forcing exit status 7 and changing the
sibling execution marker independently failed the two pinned-Bun execution
assertions. Restoration returned gateClosure.ts to
545756dc9672d649d3e317a10490c219333d7c231c50d5d13a98d102006b1248.
Final focused verification, manifest refresh, and trusted full-gate handoff
remain todo.

Final generation-9 verification passes 69 tests with 606 assertions across the
gate-closure, managed-worktree, and production D336 XDG files. The exact Q388
same-line CommonJS namespace-destructuring selector separately passes 1 test
with 12 assertions. TypeScript, exact-file ESLint, and Prettier are green. The
real-tree manifest now declares final test hash
435b32a133bb374ce4b17b99cbd57f3c977c739d483bc100b439a331fe20e292 for
both path and spawn edges, with manifest hash
6d9fcadac926f38679073ea39261e4651987e906472b39d56d5501c5a83ee348.
The prior test hash produced the exact source-digest-mismatch; the intended
manifest resolves the ordered cq-ledgers, cq-subagent-dispatch, and
ledger-status roots. The multiline sole-WIP identity/checkpoint invariant is
validated immediately after this checkpoint closes. The trusted parent owns
the fresh canonical full gate after this child exits.

## generation 8 computed-member CommonJS closure <!-- cq:wip-checkpoint -->

Generation 8 resumes from the exact reviewed generation-7 tip. Independent
review demonstrated that pinned Bun executes a sibling target through a
computed-member CommonJS loader while closure classification omits it because
the quoted loader property is masked. Exact fail-first reproduction, audit and
classification of the executable CommonJS loader-reference family, mutation
controls, manifest refresh, focused verification, and trusted full-gate handoff
remain todo.

The exact computed-member reproduction now fails for the reviewed reason on
Bun 1.3.13. `module["require"](modulePath)` executed a sibling-root module and
emitted `T2317_COMPUTED_MEMBER_REQUIRE_EXECUTED`, while closure resolution
returned `resolved` instead of `invalid`. The exact selector reported 0 passes,
1 failure, and 3 assertions at the closure-status assertion. Family
measurement, production correction, mutation controls, manifest refresh,
focused verification, and trusted full-gate handoff remain todo.

Pinned Bun 1.3.13 family measurement is complete. Direct, dotted-member,
quoted computed-member, variable computed-member, parenthesized direct/member,
call, apply, direct/member/computed/destructured aliases, and `createRequire`
forms executed the sibling marker. Direct, quoted computed-member,
parenthesized, and simple-alias `require.resolve` forms resolved the sibling
path without executing it. The execution-versus-resolution selector passes 1
test with 40 assertions. Production classification, mutation controls,
manifest refresh, focused verification, and trusted full-gate handoff remain
todo.

The expanded classification contracts fail before production changes. The
non-literal matrix returned `resolved` at the quoted computed-member case after
7 assertions. The literal matrix returned `resolved` but omitted the sibling
Bun root at that same class after 8 assertions. These failures establish both
the masked-property admission defect and missing static-target propagation.
Production correction, mutation controls, manifest refresh, focused
verification, and trusted full-gate handoff remain todo.

The dedicated minimal CommonJS reference scanner now normalizes executable
quoted computed properties without exposing inert strings, traces direct,
member, parenthesized, created, and simple alias/reference forms, and parses
call/apply target positions. Sound literal targets enter ordinary source and
Bun-root validation; non-literal calls enter the hashed `dynamic` declaration
contract; unresolved computed module properties and escaped/bound references
fail closed. The gate-closure suite passes 31 tests with 276 assertions;
TypeScript, exact-file ESLint, and Prettier are green. Mutation controls,
manifest refresh, broader verification, and trusted full-gate handoff remain
todo.

Twelve independent production mutations produced their predicted exact
failures. Disabling quoted `require` or `resolve` normalization made their
literal cases invalid; disabling computed-module admission returned `resolved`
after 10 assertions. Omitting parenthesis normalization made the literal
matrix invalid after 9 assertions. Disabling simple loader, destructured, or
resolver alias tracing failed after 17, 24, and 31 assertions respectively.
Treating literal call/apply targets as dynamic failed after 13 and 15
assertions. Omitting unsupported-reference admission returned `resolved` after
79 assertions, bypassing final CommonJS admission returned `resolved` for the
exact executable reproduction, and disabling code masking made inert computed
text invalid. Restoration after every mutation returned `gateClosure.ts` to
`da614206aaf783f39416025cbefa384a4905d170ff94068883b12b0a04963c25`.
Ten supported dynamic forms additionally resolve only with matching source
hashes and declared targets; source drift for every form returns
`source-digest-mismatch`. Manifest refresh, broader verification, and trusted
full-gate handoff remain todo.

The real-tree manifest now declares final test hash
`3c11043bafd213491ab53c86ef4cad7c267fbc1c86aa1a440bb5f1ae355e2b2b`
for both path and spawn edges, with manifest hash
`b87dfb02d99b5790f562ee0e900af78d2cc09321f40b15406b0037377e55a959`.
Restoring the previous test hash produced the exact
`source-digest-mismatch`; the intended manifest resolves the ordered
`cq-ledgers`, `cq-subagent-dispatch`, and `ledger-status` roots. Broader
verification and trusted full-gate handoff remain todo.

Final generation-8 verification passes 67 tests with 586 assertions across the
gate-closure, managed-worktree, and production D336 XDG files. The exact Q388
same-line CommonJS namespace-destructuring selector separately passes 1 test
with 12 assertions. TypeScript, exact-file ESLint, Prettier, and direct ordered
three-root resolution are green. The multiline sole-WIP identity/checkpoint
invariant is validated immediately after this checkpoint closes. The trusted
parent owns the fresh canonical full gate after this child exits.

## generation 7 CommonJS loading closure <!-- cq:wip-checkpoint -->

Generation 7 resumes from the exact reviewed generation-6 tip. Independent
review demonstrated that pinned Bun can execute a sibling-root module through
a variable-valued CommonJS `require(...)` while closure classification omits
that root. Exact fail-first reproduction, audit of the relevant CommonJS
execution and resolution family, fail-closed or manifest-bound correction,
literal/Q388 preservation, mutation controls, focused verification, and
trusted full-gate handoff remain todo.

The exact variable-valued `require(...)` reproduction now fails for the
reviewed reason on Bun 1.3.13. A target CommonJS source loaded and emitted
`T2317_VARIABLE_REQUIRE_EXECUTED` from a sibling Bun root, while closure
resolution returned `resolved` instead of `invalid`. The exact selector
reported 0 passes, 1 failure, and 3 assertions at the closure-status assertion.
CommonJS-family measurement, production correction, mutation controls, final
verification, and trusted full-gate handoff remain todo.

Pinned Bun 1.3.13 CommonJS-family measurement is complete. Direct
`require(variable)`, `module.require(variable)`, `require.main.require(variable)`,
and a `createRequire`-derived loader each executed the sibling-root marker.
`require.resolve(variable)` returned the sibling path without executing its
module. The execution-versus-resolution selector passes 1 test with 11
assertions. Production correction, classification controls, mutations, final
verification, and trusted full-gate handoff remain todo.

CommonJS classification now sends direct, member, main-module, and
`createRequire`-derived non-literal loaders through the existing hashed
`dynamic` declaration contract. Non-literal `require.resolve`, interpolated
templates, bare aliases, and call/apply/bind indirection also fail closed;
declared targets enter ordinary source traversal and nearest-Bun-root
validation. Literal direct, resolution, and created-loader targets remain
statically derived. The formatted gate-closure suite passes 29 tests with 160
assertions, TypeScript and exact-file ESLint pass, and the real-tree resolver
stops on the expected stale gate-closure test digest. Guard mutation, manifest
refresh, broader verification, and trusted full-gate handoff remain todo.

Lexical classification now distinguishes inert comments, quoted strings, and
template text from executable template expressions, and accepts comments as
call trivia. The real `createRequire` pattern that binds the local name
`require`, calls literal `require.resolve`, and queries `require.resolve.paths`
remains statically classified. The expanded gate-closure suite passes 30 tests
with 169 assertions; TypeScript, exact-file ESLint, and Prettier are green.
Guard mutation, manifest refresh, broader verification, and trusted full-gate
handoff remain todo.

Fourteen independent classifier mutations produced their predicted exact
failures. Bypassing CommonJS call admission returned `resolved` for the exact
executing reproduction; changing the emitted kind reported `opaque path edge`
instead of `opaque dynamic edge`. Omitting non-literal resolution, created
loaders, or indirection returned `resolved` at their corresponding table cases.
Skipping template-expression code returned `resolved` after 19 assertions;
treating an interpolated target as literal did so after 16. Disabling the code
mask made inert text invalid through both the call and indirection paths.
Disabling call trivia made the commented literal invalid; omitting all literal
propagation or only resolution/created loaders removed the expected sibling
root. Disabling the local `require` binding exemption made the real
`createRequire` pattern invalid, and disabling legacy dynamic-import admission
returned `resolved` for its exact control. Restoring every mutation returned
`gateClosure.ts` to
`8050aa625ab00e5aa0634d8c996d882cb16b3529de044c31b1c95868e1977ec3`;
the CommonJS selector passes 6 tests with 71 assertions and the legacy dynamic
selector passes 1 test with 3 assertions. Manifest refresh, broader
verification, and trusted full-gate handoff remain todo.

Final audit separated the new code-aware CommonJS scanner from the preserved
filesystem/spawn scanner: applying the new lexer to path calls exposed an
unrelated historical declaration in `probe-codex-role-dispatch.ts`, while the
surgical split restores the prior classifier boundary. Regular-expression
literals now remain inert alongside comments, strings, and template text.
Bypassing final CommonJS admission returned `resolved` for the exact executing
reproduction; disabling regular-expression masking made the inert-source
control invalid; and substituting code-aware path scanning produced the exact
unrelated `probe-codex-role-dispatch.ts` path-declaration failure. Restoring
these final mutations returned `gateClosure.ts` to
`d22f4248dd5767f78afb9b43fe96ddda930ab0d71539e39ff22c91de12ba1bcd`.
The manifest now declares final test hash
`dbc94208a822a43da9af32ee00ad033fcdcac9146266d64f97b3bae51c30c4b3`
for both path and spawn edges, with manifest hash
`ab3113d514ee0fe938410ae79f64b1a5fb7410693f9004bc18a392275c82a7c0`.
Restoring the prior test hash produced `source-digest-mismatch`; the intended
manifest resolves the ordered `cq-ledgers`, `cq-subagent-dispatch`, and
`ledger-status` roots. Broader verification and trusted full-gate handoff
remain todo.

Final generation-7 verification passes 66 tests with 437 assertions across
the gate-closure, managed-worktree, and production D336 XDG files. The exact
Q388 same-line CommonJS namespace-destructuring selector separately passes 1
test with 12 assertions. TypeScript, exact-file ESLint, Prettier, and direct
ordered three-root resolution are green. The multiline sole-WIP
identity/checkpoint invariant is validated immediately after this checkpoint
closes. The brokered result-storage boundary owns the fresh canonical full gate
after this child exits.

Pre-handoff audit found that a `createRequire`-derived loader's adjacent
`.resolve` and call/apply/bind forms need their own classification control.
Exact fail-first reproduction, correction, mutation, renewed verification, and
trusted full-gate handoff remain todo.

The exact derived-loader selector now fails at `load.resolve(variable)`: closure
resolution returned `resolved` instead of `invalid` after 13 assertions. The
following `load.call(...)` case remains unobserved until resolution admission is
corrected. Production correction, mutation, renewed verification, and trusted
full-gate handoff remain todo.

Derived loaders now classify non-literal `.resolve` and call/apply/bind
indirection as `dynamic`, while literal derived `.resolve` targets enter static
root traversal. Omitting those three guards respectively returned `resolved`
after 13 assertions, returned `resolved` after 16 assertions, and removed the
literal sibling root after 12 assertions; restoration returned
`gateClosure.ts` to
`60e1e4b3ebb34a88d0109a2ebeb6fa1adb6f64324483ed54a5fdbbf7d351212f`.
The final test hash is
`061a3edb61337cc4617772131dc749503f1c34ac5c1ed9cb88ddf9b6c38e6e8c`
and final manifest hash is
`6195b41dbe6deea752c191971fe8d71e39187e0491fa0b2dd4f8fd1cf8d69ac1`.
Renewed verification passes 66 tests with 445 assertions across closure,
managed worktree, and D336; exact Q388 passes 1 test with 12 assertions;
TypeScript, exact-file ESLint, Prettier, and ordered three-root resolution are
green. The trusted parent owns the canonical full gate after this child exits.

## generation 6 Bun configuration closure <!-- cq:wip-checkpoint -->

Generation 6 resumes from the exact reviewed generation-5 tip. Independent
review demonstrated that an applicable `bunfig.toml` test preload can execute
outside the validated closure while resolution still returns `resolved`.
Pinned-version configuration precedence measurement, exact fail-first
reproduction, production correction, focused regressions, guard mutation,
manifest refresh, and trusted full-gate handoff remain todo.

Fail-first reproduction is complete on Bun 1.3.13. A target-local
`bunfig.toml` named an external TypeScript `[test].preload`; the exact spawned
`bun test` executed it, emitted `T2317_EXTERNAL_BUNFIG_PRELOAD_EXECUTED`, and
exited 0. Closure resolution still returned `resolved` instead of the expected
`path-escape`, so the focused selector reported 0 passes, 1 failure, and 3
assertions at the intended closure assertion. Precedence measurement,
production correction, broader regressions, guard mutation, manifest refresh,
and trusted full-gate handoff remain todo.

Pinned-version precedence measurement is complete. Across direct `bun test` and
the repository-shaped `bun run check` to nested `bun test` chain, Bun 1.3.13
loaded only `$cwd/bunfig.toml`: target-local `[test].preload` executed, while
ancestor, `$HOME/.bunfig.toml`, and `$XDG_CONFIG_HOME/.bunfig.toml` test
preloads did not. A top-level `preload` likewise did not execute for either
package-script/test command. The pinned parser accepts `[test].preload` as a
string, string array, or null and also recognizes `[test].root`; unsupported
executable configuration fields must therefore be rejected explicitly rather
than ignored. Production correction, broader regressions, guard mutation,
manifest refresh, and trusted full-gate handoff remain todo.

The resolver now reads the pinned-version applicable target-local
`bunfig.toml`, parses supported string/array/null `[test].preload` and string
`[test].root` forms, and rejects unsupported top-level preload, bare-package
preload, security-scanner, static-serve-plugin, and ill-typed executable
constructs. The configuration requires a hashed path declaration whose targets
exactly match the canonical executable target set; each preload also requires
its own hashed declaration, while configured test roots enter ordinary source
and Bun-root validation. External config/preload targets fail containment.
Focused verification passes 25 tests with 104 assertions, including pinned
target-vs-ancestor precedence, internal roots, configuration/preload drift, and
external configuration symlinks. TypeScript and exact-file ESLint pass, and
Prettier has formatted both changed source files. Guard mutation, manifest
refresh, broader verification, and trusted full-gate handoff remain todo.

Fourteen production mutations each produced the predicted exact-selector
failure. Changing preload and config-symlink containment changed the observed
reason away from `path-escape`; selecting the repository ancestor instead of
the target config returned `invalid` for the pinned-precedence control. Bypassing
ill-typed preload, top-level preload, bare preload, security-scanner, or static
serve-plugin rejection changed the expected configuration reason. Changing the
config-declaration reason failed its control; bypassing exact config-target or
preload-source declarations returned `resolved`. Bypassing the existing digest
comparison returned `resolved` after configuration drift. Omitting configured
test-root scanning returned `resolved` for its undeclared opaque edge, and
omitting declared-target propagation removed the configured Bun root from the
install set. Each restoration returned `gateClosure.ts` to
`18f43906acdf0952073133d8829c380a573d07347cf860718be00480d240aac7`.
The restored suite passes 25 tests with 112 assertions; TypeScript, exact-file
ESLint, and Prettier are green. Manifest refresh, broader verification, and
trusted full-gate handoff remain todo.

The real-tree manifest now hashes `bunfig.toml`, declares its exact preload
target, hashes `test-setup/isolate-state-dir.ts`, and carries the final focused
test digest. With intended manifest hash
`8787af3eccc8e068a2f8d9bc3f627e59d3ab2f9f4b37e0150aa3919d97ef0cc1`,
direct resolution returns the ordered `cq-ledgers`,
`cq-subagent-dispatch`, and `ledger-status` roots. Changing the configuration
hash produced `source-digest-mismatch`; removing its preload target produced
`source-declaration-missing` for incomplete executable-target coverage; and
changing the preload hash produced `source-digest-mismatch`. Restoring each
mutation returned the intended manifest hash and three-root resolution.
Broader verification and trusted full-gate handoff remain todo.

Final generation-6 verification passes 61 tests with 378 assertions across
the gate-closure, managed-worktree, and production D336 XDG files. The exact
Q388 CommonJS selector separately passes 1 test with 12 assertions. TypeScript,
exact-file ESLint and Prettier, direct ordered three-root resolution, and the
multiline sole-WIP identity/checkpoint invariant are green. The brokered
result-storage boundary owns the fresh canonical full gate after this child
exits.

## trusted full gate <!-- cq:wip-checkpoint -->

Final generation-5 verification passed 56 tests with 338 assertions across
the gate-closure, managed-worktree, and D336 production XDG files. The exact
Q388 CommonJS alias selector separately passed 1 test with 12 assertions.
TypeScript, exact-file ESLint and Prettier, direct three-root real-tree
resolution, and the multiline sole-WIP identity/checkpoint invariant are
green. The brokered result-storage boundary owns the canonical full gate after
this child exits.

Generation 5 resumes from the exact reviewed generation-4 tip. The reviewer
showed that source discovery omits symlink entries before canonical containment,
although Bun executes a symlinked `*.test.ts`. Reproduction, correction,
regression coverage, guard mutation, focused verification, and trusted full-gate
handoff remain todo.

Fail-first source-symlink reproduction is complete on Bun 1.3.13. The exact
external `escaped.test.ts` symlink was executed by a child Bun test and emitted
`T2317_EXTERNAL_SOURCE_SYMLINK_EXECUTED`, after which closure resolution still
returned `resolved` instead of `path-escape`. An internal source symlink whose
canonical target contains an undeclared opaque path edge likewise returned
`resolved` instead of `source-declaration-missing`. The focused selector
reported 1 control pass, 2 failures, and 6 assertions. Production correction,
guard mutation, final verification, and trusted full-gate handoff remain todo.

Source discovery now canonicalizes every directory entry before classifying it,
fails closed when the canonical target is outside the repository, scans canonical
in-repository file and directory targets, and deduplicates canonical directories
to terminate symlink cycles. The focused source-symlink selector now passes 4
tests with 11 assertions: the prior manifest-declared control, executable
external rejection, internal ordinary edge validation plus declaration, and an
internal directory-cycle control. Guard mutation, manifest digest refresh, final
verification, and trusted full-gate handoff remain todo.

Three independent production mutations failed their exact controls. Skipping an
external canonical target changed `gateClosure.ts` to
`05d6282e59c95fe06df68f5ef2dbb15f4a7a3d6affd0d035dd225f8a5ed33f57`
and restored the external case to `resolved` (0 passes, 1 failure, 3
assertions). Restoring the former symlink skip changed it to
`6833b4c48ead4886b881fde1c11033a15322467b99847a862b7eb7cf6d396cfa`
and omitted the internal opaque edge (0 passes, 1 failure, 1 assertion).
Returning a path instead of terminating on a visited canonical directory
changed it to
`e0a5cb3ac8011e87746b7545c3e8fcea7d460e3896da9730057a5a7a79905c5e`
and made the cycle control `invalid` (0 passes, 1 failure, 1 assertion). Each
restoration returned the pre-format source to `dffc32c5...` and its exact test
to one pass. Prettier produced final source hash `7173848f...` and test hash
`fc3b96cf...`; the four-test selector still passes with 11 assertions, and
TypeScript, ESLint, and Prettier are green. Manifest digest refresh, broader
verification, and trusted full-gate handoff remain todo.

The committed-tip real-tree resolution failed closed on the stale manifest
hash `6a3236a1...`, reporting `source-digest-mismatch` between observed test hash
`fc3b96cf...` and declared `1a49b270...`. The manifest now records the observed
hash for the path edge and declares the regression's new spawn edge. With
intended manifest hash `17e553af6fc230122da123ac88ca8848e29271ad46f7bc4dbb018a81bd16afe0`,
resolution returns the ordered `cq-ledgers`, `cq-subagent-dispatch`, and
`ledger-status` roots. Changing the new spawn declaration to path changed the
manifest to `18f224d8...` and produced the expected
`source-declaration-missing` spawn failure; restoration returned the intended
hash and resolved result. Broader verification and trusted full-gate handoff
remain todo.

Final review found that external-lock behavior for static-import and
manifest-declared dependency roots uses two new propagation guards not yet
covered independently. Both focused regressions now pass with four assertions.
Bypassing lock canonicalization changed `gateClosure.ts` from `bf34581a...` to
`7037f2a9...` and made both return `resolved` (0 passes, 2 failures); restoring
the intended source hash returned both to green. Substituting
`bun-root-incomplete` at the static and declared propagation guards changed the
source hash to `855a95f68e315de8c9bd542c6ec38ad242f132afc330e0565735341ab9048c20`
and `e00d001a0a6e6b2951c0a49c1f0315076152cc9aa1cf513db80836fc0083b16b`;
each exact selector failed at its second assertion before restoration to one
pass with two assertions. Updating the closure declaration for the final test
hash produced manifest hash `6a3236a10ffa54df5874e3bcb444a4e1cba9a7e7fb5ea0583b07d569ef7ccf7f`;
restoring the prior declaration produced manifest hash `f99b2255...` and the
expected `source-digest-mismatch`, while the intended manifest again resolved
the three ordered roots. Renewed final verification is complete.

Generation 4 resumed from the exact independently reviewed generation-3 tip.
The reviewer reproduced two canonical-path escapes: an external manifest
reached through a repository-relative `cq-gate-closure.json` symlink, and an
external `bun.lock` reached through a declared target-root symlink. Focused
fail-first tests now reproduce both cases and the equivalent manifest-declared
source escape: all three returned `resolved` instead of `invalid`. The focused
command reported 2 passes, 3 failures, and 9 assertions; the in-repository
manifest/lock symlink control passed. The resolver now canonicalizes and
repository-containment-checks the manifest before reading it, each explicit
opaque-edge source before hashing it, and each Bun lock before accepting a
target or discovered install root. The symlink selector now passes 5 tests with
12 assertions, including the in-repository controls.

Four independent guard mutations each produced the expected single-selector
failure: bypassing manifest canonicalization returned `resolved` for the
external manifest; bypassing lock canonicalization returned `resolved` for the
external target lock; bypassing explicit-source canonicalization returned
`resolved` for the external source; and rejecting all symlinks returned
`invalid` for the in-repository control. The respective mutated source hashes
were `df1783fe2badeac2b5288db7d5e64dfa83691cac315b423943325c356896edf8`,
`7037f2a9fd9709d54ec60d2fb1a2f8a65f849de4ce64539a437bf3ee3b33d407`,
`0b32d12f1a93b36ef4fba5514e5ce96de889ac58a48088df7778ae3a3000cfa9`,
and `827fcaafe35e3b76b476015f5e41aa2a0344736064e98e8d6c93f5ddd7f98bbb`.
Every restoration returned `gateClosure.ts` to
`bf34581a89df9d484565f4f30b586318c5b1e8d973752a7daa872240280a93e1`
and the exact selector to one pass. Three additional wrong-reason mutations
changed the source hash to
`8e6b295eaef6e62c683c9a9c1c9c996c4d126f33980f4f8ec63928033d2fd0d7`,
`0e711fa021d7d1e94fe05ebbf5819a1c94570194b5088974fc940aec6d7ae38f`,
and `4f99631177484a2c64c192a3d8c51d251e84836436fcafb4e7265bbf8da8f38f`;
each exact selector failed at its second assertion with the substituted
`manifest-invalid`, `bun-root-incomplete`, or `edge-source-missing` reason.
Each restoration returned the source to `bf34581a...` and the selector to one
pass with two assertions. The closure manifest now declares the new
gate-closure test digest. Restoring the stale digest changed the manifest hash
from `f99b2255541cdeda73112b1fc01fb71acb7ae287ac670a16fef3c22ff24f8ecb`
to `0e464d04d3a8472261447783fe145725654a97105e0c6f8844f8022df22b17a0`
and direct real-tree resolution failed with `source-digest-mismatch` for the
exact old/new test hashes. Restoring the intended manifest hash returned
resolution to the ordered `cq-ledgers`, `cq-subagent-dispatch`, and
`ledger-status` roots. Regression verification and trusted full-gate handoff
are complete.

Final generation-4 verification passed 53 tests with 329 assertions across
the gate-closure, managed-worktree, and D336 production XDG files. The exact
Q388 CommonJS alias selector separately passed 1 test with 12 assertions.
TypeScript, exact-file ESLint and Prettier, direct three-root real-tree
resolution, and the multiline sole-WIP identity/checkpoint invariant are
green. The brokered result-storage boundary owns the canonical full gate after
this child exits.

Generation 3 resumed from the authoritative generation-2 tip after the trusted
parent gate reported two failures in
`packages/ledger-mcp/test/worktreeTerminalReleaseXdg.test.ts`. Both production
XDG terminal-release fixtures reached `worktree_manage prepare` without
`cq-gate-closure.json`, so preparation was refused with
`gate-closure-invalid` / `manifest-missing` instead of returning `prepared`.
This captured full-gate failure is the correction-round fail-first
reproduction. An exact-file rerun at the generation-3 starting tip confirmed
the same failure mode: 1 pass, 2 failures, and both failed prepares reported
`gate-closure-invalid` / `manifest-missing`. The fixture correction and
focused verification remain todo.

The production XDG fixture now commits a version-1 closure manifest beside its
target package and lockfile. Its target script-DAG digest is computed through
the production ledger API, and its empty opaque-edge inventory is complete for
the source-free temporary target. The exact D336 file now passes 3 tests with
47 assertions. A filename mutation changed the test source SHA-256 from
`1434c65b02f334edf18f16c672a46030521dd5a58c0d19d3bb42131525606702`
to `f1ce68fe6f728297c1fad76a592e11a9350b8e9348e8f316188fb111931ab6ca`
and restored the same 1-pass / 2-failure `manifest-missing` signal. Restoring
the intended bytes returned the source hash to `1434c65b...` and the suite to
3 passes with 47 assertions.

Final generation-3 verification passed 47 tests with 318 assertions across
the exact D336, gate-closure, and managed-worktree files. The production
resolver returned the ordered `cq-ledgers`, `cq-subagent-dispatch`, and
`ledger-status` roots. TypeScript, exact-file ESLint and Prettier, and the
multiline single-WIP identity/checkpoint invariant are green. The brokered
result-storage boundary owns the canonical full gate after this child exits.

Generation 2 resumed from the inherited six-receipt tip. Base, branch, clean
tree, and receipt-chain continuity were reverified before further checks.

Generation 2 closure mutations bypassed script-DAG and source-digest drift,
opaque and Nix declarations, dynamic-target and Bun-lock completeness,
canonical and lexical containment, static-root inclusion, unreachable-root
exclusion, and Q388 statement boundaries. Every exact selector failed once as
predicted. After restoration, the closure suite passed 11 tests with 52
assertions and restored its source/test SHA-256 digests.

Generation 2 managed-worktree mutations selected the divergent seed, installed
only the first closure root, and suppressed extension `typebox`
materialization. The focused selector failed respectively with preparation
refusal, an omitted extension install plan, and `ENOENT`; restoration returned
it to 1 pass with 5 assertions and restored both file digests.

Generation 2 final reruns passed 58 tests with 407 assertions for the closure,
managed-worktree, and MCP files, plus 37 tests with 377 assertions for adoption
and expected-failure policy. TypeScript, exact-file ESLint and Prettier,
three-root real-tree resolution, exact Q388 fixture bytes, and multiline WIP
identity checks are green.

Completed: reproduced the missing dependency, implemented and mutated the
focused regressions, completed the target-derived closure correction, and
prepared the full canonical gate for trusted result storage.

Fail-first reproduction (fresh prepared worktree, before production changes):

```text
TYPEBOX_MISSING
error: Cannot find package 'typebox' from '.../nix/pkg/pi-extensions/cq-subagent-dispatch/index.ts'
REPRO_EXIT=1
```

The broader packaged prompt-root test reached Nix first and was blocked by the
sandboxed Nix cache, so it was not counted as defect evidence. The narrowed
canonical extension import removed that unrelated precondition and failed at
the target dependency boundary.

Q388 fail-first evidence: the retained T2290 closure baseline passed 11 tests
and 49 assertions. Adding the exact one-line namespace/destructuring source to
the CommonJS alias selector then produced 0 passes and 1 failure: the resolver
returned `resolved` where the undeclared spawn edge must return `invalid`.

The minimal `;`/newline statement-boundary matcher restored that selector to 1
pass and 12 assertions. The adapted current-main closure, managed-worktree, and
worktree-MCP suites pass 58 tests with 407 assertions; TypeScript and exact-file
format checks pass. Direct resolution of the current manifest returns the
ordered `cq-ledgers`, `cq-subagent-dispatch`, and `ledger-status` Bun roots.

Guard mutations bypassed script-DAG drift, source-digest drift, opaque-edge
declarations, dynamic target completeness, Nix declarations, target locks,
canonical and lexical containment, static-root inclusion, unreachable-root
exclusion, Q388 statement boundaries, clean-target selection, all-root install,
and typebox materialization. Every exact selector failed as predicted; after
each restoration, `gateClosure.ts` returned to
`e1b2b4d8f3d1c319ff55544e39541faceaba9b8188c1d0c24480f7c61591d0b6`,
`managedWorktree.ts` to
`a56d49914fa3a6d27fa670d3089e9435d2be98aa2d8798f1f1876f53ada8099d`,
and `managedWorktree.test.ts` to
`616e816f0e19782522f6c0bb9465f319d8b167d146ab90e729dc7cff48bd7dac`.

Adoption and expected-failure policy add 37 passing tests and 377 assertions,
for 95 focused passes and 784 assertions across the two focused commands.
Exact-file ESLint, TypeScript, Prettier, real-tree closure resolution, and WIP
identity checks are green. Brokered result storage owns the canonical full gate
after this child exits; no in-child `cq gate run` is permitted on this dispatch.

The final committed-tip resolution check initially failed closed on stale
pre-format digests for `managedWorktree.ts` and `managedWorktree.test.ts`.
Advancing all four declarations to the already-recorded restored file hashes
returned direct resolution to the exact three-root closure.
