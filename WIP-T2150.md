```json
{
  "taskId": "T2150",
  "role": "implement-worker",
  "baseCommit": "ce735ea9572852efe7108b1051031eb241abadcb",
  "startedAt": "2026-04-25T00:00:00Z",
  "checkpoints": [
    {"name": "reproduce-d334-failing-control", "status": "todo"},
    {"name": "explore-existing-surface", "status": "todo"},
    {"name": "guarded-rebase-journal-and-reference", "status": "todo"},
    {"name": "prepare-resolve-and-inject-guardedRebaseLineage", "status": "todo"},
    {"name": "implement-worker-input-output-schema-extension", "status": "todo"},
    {"name": "role-procedure-step0-update", "status": "todo"},
    {"name": "guardedRebaseContinuation-test", "status": "todo"},
    {"name": "regenerate-projections", "status": "todo"},
    {"name": "acceptance-selectors-green", "status": "todo"},
    {"name": "replace-d334-marker", "status": "todo"},
    {"name": "full-gate-green", "status": "todo"}
  ]
}
```

## reproduce-d334-failing-control <!-- cq:wip-checkpoint -->

Run the D334 `test.failing` control in
`packages/ledger-mcp/test/gitChangeDispatchCapability.test.ts` and confirm it
fails for the expected reason (missing guarded-rebase continuation bridge).

## explore-existing-surface <!-- cq:wip-checkpoint -->

Map: `cq gate git-effect --operation rebase`, `gitConflictContinuation.ts`,
dispatch attestation backends, prepare/reprepare path, implement-worker
input/output schemas, role procedure source, D332/D333 markers.

## guarded-rebase-journal-and-reference <!-- cq:wip-checkpoint -->

Parent-minted opaque guarded-rebase journal: stable operation id journaled
under the managed handle's Git-effect lock, binding task, handle
token/fingerprint, canonical repo/common dir, worktree, branch/ref, exact
terminal old worker result, onto commit, terminal rebased head, request
digest, conflict-continuation receipt chain, outcome, timestamps.
Replay-safe: exact replay returns same reference; changed-payload reuse
rejects; restart reconciles the same journal.

## prepare-resolve-and-inject-guardedRebaseLineage <!-- cq:wip-checkpoint -->

Public prepare accepts only the opaque reference with exact reprepareOf;
server resolves it and injects closed `guardedRebaseLineage` into the worker
input (resolved reference, oldResultCommit, ontoCommit, rebasedStartCommit,
exact-tip mode). Persist the verified bridge through dispatch Git binding,
prepare digest, every backend, and parent-gate state.

## implement-worker-input-output-schema-extension <!-- cq:wip-checkpoint -->

Input: closed server-injected `guardedRebaseLineage`. Output: closed
`gitLineage` discriminant — ordinary: non-empty complete receipt chain;
guarded: fresh post-rebase suffix only. Reject caller-injected lineage.

## role-procedure-step0-update <!-- cq:wip-checkpoint -->

Step 0: baseCommit == ontoCommit, HEAD == startingCommit ==
rebasedStartCommit; initial bridge round priorResultCommit == oldResultCommit
with exact ancestry exemption; later corrections equal-or-precede HEAD.
exact-tip/no-new-commit mode: resultCommit == rebasedStartCommit, empty
suffix, WIP-skeleton/new-receipt exemption; corrections keep early
persistence and non-empty contiguous suffix. filesTouched from
ontoCommit..resultCommit diff.

## guardedRebaseContinuation-test <!-- cq:wip-checkpoint -->

Create `packages/ledger/test/guardedRebaseContinuation.test.ts`: terminal
worker → guarded rebase → broker restart → exact rebased reprepare →
pre-deadline stage → parent-owned gate → confirm/fetch. Controls: conflict,
restart, exact replay, changed-payload, nonterminal journal,
omission/substitution/foreign/stale, pre-rebase receipt, mode spoofing.

## regenerate-projections <!-- cq:wip-checkpoint -->

`bun run gen-prompt-catalog && bun run gen-agents` — byte-fresh catalogues.

## acceptance-selectors-green <!-- cq:wip-checkpoint -->

All acceptance selectors pass; configured PostgreSQL selectors do not skip.

## replace-d334-marker <!-- cq:wip-checkpoint -->

Replace D334 `test.failing` with same-titled plain test; remove annotation
and inventory entry. D332/D333 remain green. No D332 marker reintroduced.

## full-gate-green <!-- cq:wip-checkpoint -->

`cq gate run --worktree "$PWD" --command-cwd "$PWD/nix/pkg/cq-ledgers" -- bun run check`
exits zero.
