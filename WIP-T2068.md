```json
{
  "taskId": "T2068",
  "role": "implement-worker",
  "baseCommit": "a475dc3d789f5cf1a08e447368b1d1322f08ce4e",
  "startedAt": "2026-08-12T11:17:50Z",
  "checkpoints": [
    { "name": "installed-boundary reproduction", "status": "done" },
    { "name": "installed-boundary implementation", "status": "done" },
    { "name": "focused and static checks", "status": "done" },
    { "name": "full gate", "status": "unmeasured" }
  ]
}
```

## installed-boundary reproduction <!-- cq:wip-checkpoint -->

`nix build --no-link .#cq` failed in the installed wrapper check with
`cq-codex-role did not settle before PTY EOF (exit 124)`. The helper resolved
the newline-framed request but left the PTY stream flowing, which retained the
installed process until the ten-second guard terminated it.

## installed-boundary implementation <!-- cq:wip-checkpoint -->

The bounded reader now pauses the input stream when it settles, allowing the
installed process to exit while its PTY remains open. Dispatch parsing also
maps malformed or non-object JSON to one bounded diagnostic that cannot echo
capability bytes. The focused framing suite passed 2 tests / 4 assertions.

## focused and static checks <!-- cq:wip-checkpoint -->

Focused suites passed 91 tests / 695 assertions across real-Git reconciliation,
managed adoption, request framing, sandbox preflight, parent gate attestation,
and catalogue regeneration. The installed Nix boundary also passed after the
fix. Removing `input.pause()` reproduced PTY timeout exit 124; restoring digest
`a878d83f2fbeee5e164b159ded27c90eb578e67c38d620319f4c12ccf4f01ec1`
passed. Restoring raw `JSON.parse` reproduced the engine-specific malformed
diagnostic; restoring digest
`d956da00853af56ab0d73f3b2af0a42d5864c81ff131b73a1c32951b526a5d5d`
passed. `bun run typecheck` and `bun run lint` both exited zero.

## full gate <!-- cq:wip-checkpoint -->

Unmeasured.
