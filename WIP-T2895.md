```json
{
  "taskId": "T2895",
  "role": "implement-worker",
  "baseCommit": "676a44dd9bb05dd1e98fbf46a2102cc5ef69c275",
  "startedAt": "2026-08-29T13:29:05Z",
  "checkpoints": [
    { "name": "baseline inspection", "status": "done" },
    { "name": "defect reproduction", "status": "done" },
    { "name": "bootstrap state machine", "status": "done" },
    { "name": "service status", "status": "done" },
    { "name": "generated workflow", "status": "done" },
    { "name": "targeted acceptance", "status": "done" },
    { "name": "changed guard mutation evidence", "status": "done" },
    { "name": "round 1 gate guard correction", "status": "done" },
    { "name": "round 2 atomic activation materialization", "status": "done" },
    { "name": "round 3 closure manifest refresh", "status": "done" },
    { "name": "round 4 bootstrap task write protection", "status": "done" },
    { "name": "round 5 production baseline fixture", "status": "done" },
    { "name": "trusted full gate", "status": "unmeasured" },
    { "name": "final verification", "status": "done" }
  ]
}
```

## baseline inspection <!-- cq:wip-checkpoint -->

Verified `implement/T2895` at the dispatched base, then inspected the existing
activation, protected-review, transport, runtime, remote-client, and generated
prompt surfaces. The requested bootstrap and service-status operations and four
named test files are absent at the starting commit.

## defect reproduction <!-- cq:wip-checkpoint -->

Added the specified bootstrap behavior contract before production changes.
`bun test packages/ledger/test/implementation-evidence-bootstrap.test.ts`
discovers three tests and fails all three at the expected boundary:
`TypeError: service.advanceEvidenceBootstrap is not a function`.

## bootstrap state machine <!-- cq:wip-checkpoint -->

The journal state machine replay-fences both phases, admits only the mapped
historical task, binds the immutable startup build to the completed evidence
task, and materializes the canonical activation action/handoff without
consulting the packaged audit registry. `prepare_dispatch` now requires and
validates that exact opaque admission for the fresh round-zero historical
worker; an omitted, foreign, stale-head, or continuation-coupled ref rejects
before allocation. The focused direct/dispatch/HTTP/CLI group passes 33/33.

## service status <!-- cq:wip-checkpoint -->

Implemented the management-only service-derived status and the configured
RemoteLedgerClient CLI path. Direct, authenticated HTTP, production-runtime,
and CLI checks pass 17/17. The CLI rejects caller-asserted identity, old
protocols, incomplete operation inventories, and missing bounded finalized
review outcome support without inspecting a local store or Git checkout.

## generated workflow <!-- cq:wip-checkpoint -->

The source prompt now consumes service-derived status, advances both bootstrap
phases, passes the opaque historical-dispatch authority to `prepare_dispatch`,
forbids a third Git task, and orders the historical deployment activation
proof. Its seven focused contract tests pass. Both requested generators ran
twice through a direct evaluation of the repository's pure `assets.nix`
projection; the second-run SHA-256 pairs were byte-identical. The prompt
catalog projection stayed unchanged and the generated agent catalogue records
the new prompt bytes.

## targeted acceptance <!-- cq:wip-checkpoint -->

Initial exact command: 114 discovered tests across 9 existing files; 76 passed,
20 skipped, and 18 failed. All 18 failures have the same sandbox-local
`Bun.serve({ port: 0 })` `EADDRINUSE` signature in the pre-existing remote
client contract. The requested bootstrap, service-status transport/HTTP, and
CLI status files were not discovered because they do not exist yet.

After implementation, the 14-file named acceptance set discovered 126 tests:
88 passed, 20 integration tests skipped, and the same 18 socket-bind cases
failed with the baseline signature. All named tests that do not open a local
listening socket are green, including the new direct, HTTP, CLI, bootstrap,
runtime, review, operator-action, prompt, and workset contracts. TypeScript,
ESLint, and the 42-test management tool inventory suite are green.

A workspace sweep also exposed two deterministic repository drifts before the
sandbox socket/process failures cascaded: the public response matrix omitted
the new tools, and the normalized tool-surface artifact was stale. Both are
updated. The focused documentation suite passes 4/4 and the measured surface
suite passes 2/2 with byte-current evidence, an exact 56-tool inventory, and a
strict G93 maximum reduced from 1621 to 1591 tokens (below the 1622 budget).
The final restored-byte focus discovered 64 tests: 26 passed, 20 remote
integration tests skipped, and the same 18 sandbox listener failures remained.
The bootstrap, CLI, stdio parity, and typed remote-client contracts are green;
the stdio suite contributes 11 passes and 647 total focused assertions remain
effective around the sandboxed adapter failures. TypeScript and ESLint are
green after the final edit batch.

## changed guard mutation evidence <!-- cq:wip-checkpoint -->

Each row is an observation from this run. `before` and `restored` are SHA-256
digests of the mutated file; equality proves byte restoration.

| guard                        | mutation and focused command                        | expected failure | restored pass | before / restored                                                         |
| ---------------------------- | --------------------------------------------------- | ---------------- | ------------- | ------------------------------------------------------------------------- |
| activation action key        | accept the obsolete key; bootstrap test             | 2 failed         | 3 passed      | `d35260cd064f95271338b05fb47e304f24e0d04e4bc6164dcfbaa4b27edb0f41` / same |
| exact operation inventory    | omit service-status; transport + CLI tests          | 3 failed         | 6 passed      | `d35260cd064f95271338b05fb47e304f24e0d04e4bc6164dcfbaa4b27edb0f41` / same |
| immutable startup build      | substitute live head; status transport test         | 1 failed         | 1 passed      | `d35260cd064f95271338b05fb47e304f24e0d04e4bc6164dcfbaa4b27edb0f41` / same |
| protected dispatch admission | allow omitted ref; dispatch test                    | 1 failed         | 1 passed      | `37b6e5cf4e0d0a6248e241c6e5534f9b19ffe8ac1eefa046b9de4445a9d83293` / same |
| management-only HTTP surface | remove status from management profile; HTTP test    | 1 failed         | 1 passed      | `1871a82624458a6619993e53fb165678beb1fbd7506ae7a8076b6dd011999c1a` / same |
| orchestration authority      | remove bootstrap-ref field from prompt; prompt test | 2 failed         | 7 passed      | `26bbdef51d5c3cb237dd1a66ef7d5a33ee7ef4320959999e74aa8c6b6acb8903` / same |
| management inventory size    | change 56 to 55; MCP inventory test                 | 1 failed         | 1 passed      | `ca94e654571b97f9ab2f51908a57a1bab878477a8ab5600c8f4a54611d00ad9e` / same |
| public response matrix       | omit service-status documentation row               | 1 failed         | 1 passed      | `99265fa90c234c63a081c45e475d7eec184d577bd7b692fddbbe585711fff747` / same |
| measured G93 ceiling         | change measured 1591 ceiling to 1590                | 1 failed         | 1 passed      | `00d1ec63c53b2d33e5f1bd32c4562ada1e58482a12e20bc2913f3539749ce65b` / same |
| persistent restart replay    | restart with a fresh journal store; bootstrap test  | 1 failed         | 1 passed      | `6532febfdf075b220fbcc95a9171d4aff904269501540ed678020c86236e4682` / same |
| strict caller identity       | treat repository-head as merely unknown; CLI test   | 1 failed         | 1 passed      | `5019146fa4155403c338f969fac73c81e3710cd785687a088abe5b212bd38b37` / same |
| typed remote routing         | route service status to activation status; client test | 1 failed      | 1 passed      | `50a1e39756019704d9578ae928988f54298e54c93af30355c05ae5e8a35ee637` / same |
| stdio operation parity       | omit service-status invocation; parity test         | 2 failed         | 2 passed      | `f1f234d84fe3a4081e6588b802bd3e23f18ee684502c48a8cb077dc76f132846` / same |
| role inventory assertion     | change 56 to 55; role-profile test                  | 1 failed         | 1 passed      | `e595af3a92e4b670939f3e3a5ece4d5f3be0ace38983c8b586d967881f482d95` / same |

The first operation-inventory mutation exposed a circular test oracle because
the fixture imported the production inventory. The fixture and direct
transport assertion now carry the closed expected inventory; the repeated
mutation then failed at both client and transport boundaries as required.

## round 1 gate guard correction <!-- cq:wip-checkpoint -->

Correction round started from the retained result commit. Reproduce and repair
only the supervised gate's measured breaking-surface target and repository
gate-closure manifest guards, then delegate the trusted full gate again.

The exact two-file focused command discovered four tests: two passed and two
failed. The breaking-surface contract failed because
`roleCapabilityUnionPreserved` and `targetRoleToolUnionMatchesInventory` were
false. The closure contract failed with `source-digest-mismatch` for
`scripts/measure-tool-surface.ts`, proving both reported supervised-gate
failures before their checked-in evidence is regenerated.

Added the bootstrap and service-status operations to the measured target's
post-baseline inventory, regenerated the canonical target, and refreshed every
declared closure edge whose source bytes changed in the retained candidate.
The exact focused command then passed 4/4. A second target generation retained
SHA-256 `1b3dc65201c0fc5ba610ae6ba4edb2d115e53e530d68fd621fa562df749e6574`.

Round-1 restored-byte mutation evidence:

| guard | mutation | expected failure | restored pass | before / restored |
| ----- | -------- | ---------------- | ------------- | ----------------- |
| measured target inventory | substitute the service-status name only in `publicToolInventory`; exact breaking-surface selector | `targetRoleToolUnionMatchesInventory: false` | 1 passed | `1b3dc65201c0fc5ba610ae6ba4edb2d115e53e530d68fd621fa562df749e6574` / same |
| repository gate closure | restore the stale dynamic source digest for `measure-tool-surface.ts`; closure selector | `source-digest-mismatch` | 1 passed | `2750f16b5b1d0fb36aa2c89be1a1f132272aa7a3923d1f3bad7e8e9992bf3c4f` / same |

## trusted full gate <!-- cq:wip-checkpoint -->

Unmeasured; delegated to trusted result storage after final verification.

## round 5 production baseline fixture <!-- cq:wip-checkpoint -->

Correction round starts from the retained round-4 result commit. Reproduce the
criticized absence of an executable fixture rooted at the exact production
baseline, then add the minimum production-envelope fixture that drives the
generated command and management profile through bounded disapproval,
continuation correction, protected completion, release, and the required
user-action stop. The trusted full gate remains delegated to parent
supervision.

The exact focused selector now reproduces the supplied criticism before any
fixture implementation: it discovers one test and fails with `ENOENT` for
`packages/ledger-mcp/test/fixtures/t2895/production-bootstrap-v1.json`. A
repository-wide search also finds no reference to baseline
`5342f4050891231a4b41e6d0278c62c87568d16b`, excluding a differently named
existing fixture.

Implemented a version-1 fixture manifest whose four artifact digests bind the
exact baseline source command, generated prompt catalogue, generated command
surface, and management profile. The executable fixture clones the real
repository at that exact commit, finalizes a new G176 manifest through the XDG
plan lifecycle, verifies only the freshly allocated `t-evidence` task is ready,
and uses the baseline management inventory for every pre-deployment call.

The fixture then exercises a filesystem-backed attestation journal, managed
worktree, two brokered worker generations, and runner-supervised gate storage.
The first protected adapter result carries a complete disapproval despite exit
17; finalization returns its exact known criticism and excludes the private
adapter diagnostic. The correction consumes only that bounded outcome through
the retained continuation, gates, receives approval, and crosses protected
completion prepare, ff-only merge, terminal recording, and guarded release.
It creates one `user-action-required` deployment handoff and no historical task
dispatch. After an explicit service reconstruction at the merged result commit,
the fixture invokes `advance_implementation_evidence_bootstrap`, admits only the
fresh historical mapping, exact-replays the opaque reference, and rejects wrong
head and wrong-manifest envelopes without creating an operator action.

The exact focused selector passes 1/1 with one aggregate assertion in 68.35s.

The repository-closure selector then reproduced the expected
`source-digest-mismatch` for the changed runtime test. After refreshing its two
declared edges it exposed the new fixture runner's undeclared opaque path edge;
the manifest now hashes that runner and closes both its spawn boundary and its
exact CLI/JSON paths. The same selector passes 1/1 in 49.06s.

Round-5 restored-byte mutation evidence:

| guard | mutation | expected failure | restored pass | before / restored |
| ----- | -------- | ---------------- | ------------- | ----------------- |
| production fixture aggregate | require three supervised gate runs instead of the observed two; exact focused selector | `3` expected versus `2` received, 1 failed | 1 passed in 51.68s | `66f0bef8f794ef2edad514cbb3b6fc83a1dfa0a7dfbe481b389c7f23e4cd1c0a` / same |
| repository gate closure | reintroduce the stale runtime-test digest on its spawn edge; exact closure selector | `source-digest-mismatch`, 1 failed | 1 passed in 50.02s | `8818ff0a86b922fb2ef5247d7eeb29362fc0d8eed5344fe27f4677432eed52b5` / same |

The exact 13-file acceptance command discovered 135 tests: 97 passed, 20 real
PostgreSQL integration tests skipped, and 18 unchanged sandbox-local remote
client cases failed before contract execution because
`Bun.serve({ port: 0 })` returned `EADDRINUSE`. The new production fixture and
all non-socket acceptance selectors passed. `bun run typecheck` and focused
ESLint both exit zero.

The exact prompt generator was blocked before generation because confined Nix
could not write `/home/pavel/.cache/nix/fetcher-cache-v4.sqlite`. The existing
daemon-free `nix-instantiate` shim evaluated the same pure `assets.nix`
attributes, after which both actual Bun generator entry points ran twice. The
two passes were byte-identical and left no Git diff: `promptCatalog.gen.ts` =
`3f4412ea8a5f319bca9df07ad8f48c6ac59753fbd652509aec154f8374249c29` and
`agentsCatalogue.gen.ts` =
`2e828a6df54544cc3887588ae84aa485a4f977dd951eb3e8bcc96c81fbbd9498`.

## round 4 bootstrap task write protection <!-- cq:wip-checkpoint -->

Correction round starts from the retained round-3 result commit. Reproduce the
criticized generic terminal-write path for a bootstrap-admitted historical
task, then protect that task at the generic-write boundary and require
activation-handoff authority to bind its protected completion to a distinct
historical result commit. The trusted full gate remains delegated to parent
supervision.

The public-boundary regression discovered five tests: three passed and two
failed for the criticized reasons. Both the protected store wrapper and the
management service terminalization guard fulfilled a forged generic `done`
write after historical bootstrap admission. Activation handoff also resolved
and materialized authority when the historical task projection had no matching
protected completion. The normal protected review/completion fixture remained
green, excluding fixture setup and verdict validation as failure causes.

Historical-dispatch records now activate only their single admitted historical
task at both generic terminalization guards. Activation handoff requires one
exact admission for the goal, manifest, three-task mapping, evidence result,
and historical task; it then requires one recorded protected completion bound
to the projected historical result and rejects reuse of the evidence/startup
commit. The same five-test focus passes 5/5 after the correction.

The contract was separated into exact admission, recorded completion, result
binding, and distinct-build cases; the final focused file passes 8/8. Each
production guard was then removed or bypassed independently and restored:

| guard | mutation and exact selector | expected failure | restored pass | before / restored |
| ----- | --------------------------- | ---------------- | ------------- | ----------------- |
| bootstrap task activation | remove historical bootstrap predicate; `bootstrap admission protects` | both generic writes fulfilled, 1 failed | 1 passed | `d4152c9f18ee2dc209b23391403ee21b991525532ff8f18a78c70668f5efdc52` / same |
| exact admission binding | bypass admission cardinality; `exact historical bootstrap admission` | promise resolved, 1 failed | 8 passed | `d4152c9f18ee2dc209b23391403ee21b991525532ff8f18a78c70668f5efdc52` / same |
| protected completion binding | bypass completion cardinality/result comparison; `recorded protected historical completion` and `binds the projected result` | each promise resolved, 1 failed each | 8 passed | `d4152c9f18ee2dc209b23391403ee21b991525532ff8f18a78c70668f5efdc52` / same |
| distinct historical result | remove evidence-build comparison; `result distinct` | wrong protected-completion failure, 1 failed | 8 passed | `d4152c9f18ee2dc209b23391403ee21b991525532ff8f18a78c70668f5efdc52` / same |

The repository-closure selector reproduced `source-digest-mismatch` for the
changed `implementationEvidence.ts` bytes. Updating only that manifest edge
made the selector pass 1/1. Reintroducing the stale digest failed with the same
signature; restoring SHA-256
`db66e46f989272c3f662d8fd1778f5231b58ed3680cb1ad197415543c0c67504`
returned the selector to 1/1.

The exact 13-file acceptance command discovered 134 tests: 96 passed, 20 real
PostgreSQL integration tests skipped, and the same 18 sandbox-local remote
client cases failed before contract execution because `Bun.serve({ port: 0 })`
returned `EADDRINUSE`. All changed bootstrap, runtime, activation, operator
action, review, transport, HTTP, CLI, prompt, and command-guard contracts are
green. `bun run typecheck` and the full ESLint traversal exit zero.

The Nix daemon socket is unavailable inside the child sandbox. A temporary
daemon-free `nix-instantiate` shim evaluated the same three pure `llmAssets`
attributes from `assets.nix`; both actual Bun generator entry points then ran
twice. Replay SHA-256 values were byte-identical:
`promptCatalog.gen.ts` =
`3f4412ea8a5f319bca9df07ad8f48c6ac59753fbd652509aec154f8374249c29` and
`agentsCatalogue.gen.ts` =
`2e828a6df54544cc3887588ae84aa485a4f977dd951eb3e8bcc96c81fbbd9498`.
Neither pass changed the worktree.

## round 3 closure manifest refresh <!-- cq:wip-checkpoint -->

Correction round starts from the retained round-2 result commit. Reproduce the
trusted gate's exact repository-closure failure, refresh only the closure
manifest source digests invalidated by the atomic activation materialization
correction, and run the focused closure selector. The trusted full gate remains
delegated to parent supervision.

The exact repository-closure selector discovered one test and failed with the
trusted gate's `source-digest-mismatch`: the declared digest for
`operator-actions.test.ts` was `b8b5205c...`, while the retained atomicity test
bytes are `dbb79da9...`. The round-2 change set intersects the closure inventory
at only that source, so the manifest now updates that single digest. The exact
selector then passes 1/1.

Round-3 restored-byte mutation evidence:

| guard | mutation | expected failure | restored pass | before / restored |
| ----- | -------- | ---------------- | ------------- | ----------------- |
| repository gate closure | reintroduce the stale operator-action test digest; exact closure selector | `source-digest-mismatch`, 1 failed | 1 passed | `cbd373a31b282fe7b33ec004906b5f158556d3e24fcd99adca7f5ee1f0ebdfe9` / same |

## round 2 atomic activation materialization <!-- cq:wip-checkpoint -->

Correction round starts from the retained round-1 result commit. Reproduce the
criticized split-write state under a handoff-create failure, then move the
pending operator action and its single user-action-required handoff behind one
protected atomic operation. Prove both failure and restored pass before
delegating the trusted full gate again.

The focused reproduction discovered one test and failed at its old-state
assertion: after the injected handoff-create failure, `operatorActions:OA1`
remained `pending` while `handoffs:HO1` was absent. This is the criticized
partial commit, not a setup or import failure.

`materializeOperatorAction` now re-reads task/action/handoff authority and
creates any missing pair inside the adapter's existing multi-ledger owned-write
transaction. The focused reproduction passes 1/1, the full operator-action
file passes 34/34 across in-memory, filesystem, and SQLite adapters, and
`bun run typecheck` exits zero.

Added filesystem persistence failpoints at each of the three materialization
write boundaries. After restart, every boundary recovered to the complete old
state (neither item) and an ordinary retry produced the complete new state
(one pending action and one user-action-required handoff); no mixed state was
observable. The final production/bootstrap/operator-action focus passes 51/51,
TypeScript exits zero, and focused ESLint exits zero.

The exact 13-file acceptance command discovered 129 tests: 91 passed, 20 real
PostgreSQL integration tests skipped, and the same 18 sandbox-local remote
client cases failed before contract execution because
`Bun.serve({ port: 0 })` returned `EADDRINUSE`. This is unchanged from the
starting and prior-round observations. The pure `assets.nix` projection ran
inside the sandbox, and both requested generators ran twice with zero Git diff;
canonical flake evaluation remains delegated to the trusted gate because the
Nix daemon socket is outside this sandbox.

Round-2 restored-byte mutation evidence:

| guard | mutation | expected failure | restored pass | before / restored |
| ----- | -------- | ---------------- | ------------- | ----------------- |
| transaction-local handoff failure | redirect only the injected owned-write handoff failpoint; exact atomic-pair selector | promise resolved instead of rejecting | 1 passed | `53211dbafbabd2dbeb3d5521e6f023e78ec0b693b10fda493e3a1809e34fd5c7` / same |
| filesystem complete-pair recovery | expect a drained handoff after each persistence fault; exact restart selector | 3 failed with user-action-required vs drained | 3 passed | `dbb79da99b6895c914358756013b40605bc88c28d870324e5f7ebae325014943` / same |

## final verification <!-- cq:wip-checkpoint -->

Verified the committed task tip is a commit on `implement/T2895`, the tree is
clean, and the dispatched base is its ancestor. Both historical evidence
commits (`5342f4050891231a4b41e6d0278c62c87568d16b` and
`00ebe7e4982cbf9d847b5713c65f5fa46b6bda72`) are ancestors. The repair merge
`7cee985a088e7112bc4465b7b750ed32e824b29a` has exact second parent
`00ebe7e4982cbf9d847b5713c65f5fa46b6bda72`, and its tree equals its first
parent's tree. The trusted full gate remains deliberately unmeasured for parent
result-storage supervision.

Round 1 reverified a clean committed correction tip, dispatched-base and
retained-candidate ancestry, both required historical ancestors, and the exact
no-tree-change repair-merge parent/tree invariants. The trusted full gate
remains unmeasured pending parent supervision.

Round 2 verified a clean committed atomicity-correction tip on
`implement/T2895`, with the dispatched base and retained round-1 result in its
ancestry. Both required historical commits remain ancestors. The repair merge
still has `00ebe7e4982cbf9d847b5713c65f5fa46b6bda72` as its exact second parent,
and its tree still equals its first parent's tree. The trusted full gate remains
unmeasured pending parent supervision of the final WIP-only tip.

Round 3 verified a clean committed closure-correction tip on
`implement/T2895`, with the dispatched base and retained round-2 result in its
ancestry. Both required historical commits remain ancestors. The repair merge
still has `00ebe7e4982cbf9d847b5713c65f5fa46b6bda72` as its exact second parent,
and its tree still equals its first parent's tree. The exact closure selector
passes 1/1. The trusted full gate remains unmeasured pending parent supervision
of the final WIP-only tip.

Round 5 verified the committed production-fixture tip on `implement/T2895`,
with the dispatched base and retained round-4 result in its ancestry. The exact
production baseline and published historical result remain ancestors; the
repair merge's second parent and unchanged-tree invariants remain intact. The
trusted full gate remains unmeasured pending parent supervision of the final
WIP-only tip.
