```json
{
  "taskId": "T2149",
  "role": "implement-worker",
  "baseCommit": "d6360b55b21fc7800d9ae8aa3c37b68a0dc821d0",
  "startedAt": "2026-08-19T00:00:00.000Z",
  "checkpoints": [
    { "name": "Step 0 base verification", "status": "done" },
    { "name": "Fail-first reproduction via D332 control", "status": "done" },
    { "name": "Prepare-boundary lineage-free advanced-tip rejection", "status": "done" },
    { "name": "D332 marker -> plain test; annotation + inventory entry removed; policy count 8 -> 7", "status": "done" },
    { "name": "Packaged codex broker retries adopt reprepareOf", "status": "done" },
    { "name": "Provider qualification retains inherited receipt prefix", "status": "done" },
    { "name": "Mutation evidence for changed guards", "status": "done" },
    { "name": "Focused acceptance selectors green", "status": "done" },
    { "name": "Full gate (cq gate run -- bun run check)", "status": "todo" }
  ]
}
```

## Step 0 base verification <!-- cq:wip-checkpoint -->
Verified 2026-08-19: toplevel
`/home/pavel/work/safe/flakes/cq/.claude/worktrees/01a0167f-4520-74e9-95a4-c0380fe31dc9`,
branch `implement/T2149`, HEAD == startingCommit == baseCommit
`d6360b55b21fc7800d9ae8aa3c37b68a0dc821d0` (`git cat-file -t` = commit,
`merge-base --is-ancestor` exit 0, relation equal), clean tree. Round 0, no
priorResultCommit, held dispatch protocol (no gitChangeCapability): confined
commit path, no gitReceipts in the result.

## Fail-first reproduction via D332 control <!-- cq:wip-checkpoint -->
Done: flipped the D332 `test.failing` to plain `test` (also dropping the
annotation — the final intended form) and ran the focused selector. RED with
the exact defect signature: "D332 observed lineage-free advanced-tip
acceptance: prepare allocated attestation att_1t3k6_L5AAcOFRwjKjE4P0ZNVFtiaXB3
generation 1 with a broker Git capability at managed tip
7462d76775e7584efeb84f02a7d32cb056edfa55 without reprepareOf lineage". Not a
setup/Git/timeout failure: prepare returned accepted with a minted
gitChangeCapability.

## Prepare-boundary lineage-free advanced-tip rejection <!-- cq:wip-checkpoint -->
Done: `packages/ledger-mcp/src/dispatchCapability.ts` prepare. A lineage-free
(`reprepareOf` absent) brokered implement-worker prepare now requires
`input.startingCommit === input.baseCommit`; otherwise it returns
`dispatchPreLaunchRejection("invalid-launch-envelope", "reprepareOf", ...)`
BEFORE `prepareDispatchOn`, so no attestation/capability is allocated. The
repareOf codex path (T2119: prior-binding field equality, terminal-generation
resolution, `resolveInheritedGitChangeReceipts` lineage) is unchanged; the
conflict-resolver branch is untouched. Discriminator verified against the
T2081 "descendant dispatch base" fixture (lineage-free re-based correction
round with baseCommit == startingCommit == advanced tip stays admissible) and
D334 (rebased tip + reprepareOf still routes to the T2150 receipt-lineage
rejection). After the fix the plain D332 test passes.

## D332 marker -> plain test; annotation + inventory entry removed; policy count 8 -> 7 <!-- cq:wip-checkpoint -->
Done: `gitChangeDispatchCapability.test.ts` carries the same-titled plain
test with no `// expected-failure: defects:D332` annotation;
`expectedFailureInventory.ts` lost the D332 entry (D334 entry retained);
`expectedFailurePolicy.test.ts` committed-inventory test now expects exactly
seven markers `[defects:D334, 6x tasks:T826]`. Policy suite: 9 pass / 0 fail.

## Packaged codex broker retries adopt reprepareOf <!-- cq:wip-checkpoint -->
Done: `packagedCodexRoleGitBroker.test.ts` — the round-1 criticism retry now
passes `reprepareOf: handle` (round-0, consumed) and asserts the complete
4-receipt inherited-plus-current chain (prefix ends at `firstResultCommit`,
current generation continues there); the round-2 cancel control names
`reprepareOf: retryHandle`; the deadline control names
`reprepareOf: cancelledPrepared.handle` (distinct generations 3 and 4 off the
same attestation id). The worker fixture already prepends
`inheritedGitReceipts`. Installed-gate tests skip locally (env unset); they
run in the gate's `nix build .#cq` installCheck.

## Mutation evidence for changed guards <!-- cq:wip-checkpoint -->
Done (hashes recorded pre/post, restored bytes match):
1. Production guard mutated to typeof-only (lineage clause removed): D332
   plain test RED with the exact acceptance observation (managed tip
   aacb28951b5d83a57bc7b3c58e9f4d6ebc869d52). Restored
   sha256 bad9575cf928edeb6f537b592f309bdec982a8c7611869f079026c5a121f43c7,
   D332 green.
2. Inverted equality (`===` baseCommit): suite RED in the fixture beforeAll
   (initial lineage-free prepare at base rejected) — guard load-bearing in
   both directions. Restored.
3. Policy `toHaveLength(7)` -> `(8)`: committed-inventory test RED
   ("expected 8, received 7"). Restored,
   sha256 f8785deb055bc46dfb0b815e4a85ac5b8d551860544c94525ff40f9aa3180f71.
4. Inventory D332 entry re-added without a live marker: committed-inventory
   test RED (scanner bidirectionality). Restored,
   sha256 6e56073c3bdd287e80780e7b1c85d42ca4effccfecada4be1fc8876715123abb.
5. D332 fail-first (plain test before the production fix) is the
   marker-flip guard evidence; recorded above.
Packaged-test reprepareOf edits: no local mutation signal possible (installed
gate skips without CQ_TEST_CODEX_ROLE_EXECUTABLE); verified by typecheck and
the installed-gate nix build.

## Provider qualification retains inherited receipt prefix <!-- cq:wip-checkpoint -->
Fail-first captured by the first gate run (REAL_CHECK_EXIT=1, nix installCheck):
"Codex provider gitReceipts[0] breaks its dispatch chain" from
`authenticateCodexProviderGateObservation` — the qualification required every
receipt to carry the dispatch's own generation, which an inherited prefix
cannot. Fix in `packages/cq-config/src/nativeDispatchQualification.ts`:
entries may name any generation in [1, handle.generation] of the same
attestationId, non-decreasing (prefix-before-current), still contiguous and
terminating at resultCommit; at least one current-generation entry is
required; and the prior-consumed check now requires the leading segment to
canonically equal the prior result's complete receipt chain with the first
current-generation receipt's oldHead == prior resultCommit. Verified by
`nix build --no-link .#cq` exit 0 (installCheck runs the installed
packagedCodexRoleGitBroker + gitChangeDispatchCapability suites with the real
wrapped executables).

## Focused acceptance selectors green <!-- cq:wip-checkpoint -->
Done: the ten acceptance files pass (254 pass / 0 fail / 1 environmental
skip — the livePgTest hub-construction selector; `CQ_TEST_PG_URL` is unset
here, identical to base). `bun run typecheck` clean.
`packagedCodexRoleGitBroker.test.ts`: 1 pass / 2 installed skips locally.
