```json
{
  "taskId": "T1953",
  "role": "implement-worker",
  "baseCommit": "dfdc62241e2406ba3bb38df89b601a40724d812b",
  "startedAt": "2026-08-08T14:57:00Z",
  "checkpoints": [
    { "name": "grounding", "status": "done" },
    { "name": "kind-inventory", "status": "done" },
    { "name": "admission-contract", "status": "done" },
    { "name": "effect-protocol", "status": "done" },
    { "name": "admin-admission", "status": "done" },
    { "name": "acceptance-tests", "status": "done" },
    { "name": "mutation-evidence", "status": "done" },
    { "name": "full-gate", "status": "todo" }
  ]
}
```

## grounding <!-- cq:wip-checkpoint -->

Base verified on `implement/T1953` at
`dfdc62241e2406ba3bb38df89b601a40724d812b`. Built on T1951/T1952 owner edges +
closure graph.

## kind-inventory <!-- cq:wip-checkpoint -->

`worksetEffectAdmission.ts` enumerates plan-lifecycle mutations, generic/owned
writes, external effects, and administrative destructive ops; maps each to an
admission form; seals forbidden patterns and termination reasons. Inventory
test keeps process-control broker kinds byte-identical.

## admission-contract <!-- cq:wip-checkpoint -->

In-memory `WorksetAdmissionCoordinator` implements ledger-mutation and
external-effect forms, exclusive set (epoch+1, revoke in-flight), latch hooks,
invalid-replacement atomicity, target exclusion, one-shot admissions.

## effect-protocol <!-- cq:wip-checkpoint -->

`worksetEffectProtocol.ts` stage machine + opaque provider surface; cleanup-
before-release for all five termination reasons; integrates with coordinator
provider in race fixture.

## admin-admission <!-- cq:wip-checkpoint -->

Opaque management authority mint; all five admin kinds refuse without it; wait
for brokered effect close before destructive phase; exclusive hold revokes
mid-flight admits.

## acceptance-tests <!-- cq:wip-checkpoint -->

48/48 pass:
`workset-effect-admission-contract`, `workset-effect-kind-inventory`,
`worksetEffectProtocol`, `workset-admin-admission`.

## mutation-evidence <!-- cq:wip-checkpoint -->

| guard | mutate | fail observation | restore hash |
| --- | --- | --- | --- |
| external settle-before-release | skip settled check | expected process-group-not-settled missing | SRC sha match |
| management authority | skip isTrusted check | expected management-authority-required missing | SRC sha match |
| protocol close-before-settle | skip settled stage check | test failed (provider cleanup error) | PROTO sha match |

SRC `f54942f6…00b78`, PROTO `54583eb6…42ab1` unchanged after restore.
Post-restore acceptance 48/48.

## full-gate <!-- cq:wip-checkpoint -->

todo
