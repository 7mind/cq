```json
{
  "taskId": "T6558",
  "role": "implement-worker",
  "baseCommit": "3d3b263d10c5083cae87ee7b085dfcc9ec22d0d7",
  "startedAt": "2026-09-16T17:14:38Z",
  "checkpoints": [
    { "name": "retained artifact reproduction", "status": "done" },
    { "name": "packaged provenance implementation", "status": "done" },
    { "name": "focused verification", "status": "done" },
    { "name": "real shipped entrypoint probe correction", "status": "done" },
    { "name": "installed probe runtime prerequisites", "status": "done" },
    { "name": "post-rebase package acceptance", "status": "done" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## retained artifact reproduction <!-- cq:wip-checkpoint -->

Added the plain `D430 keeps startup build identity immutable across repository
HEAD movement` regression against retained output
`/nix/store/h0b861ap8lg30p7jl0a0wra86vnjjpfz-cq-0.0.1`. Before any production
change, the two service constructions reported distinct live commits from one
unchanged artifact and the test failed at the intended equality assertion:
expected `158e7d4dd078b7fded19436c6d3ff04cd6708ce7`, received
`63d1d0c7421d962f78c319f34ab1100a25c8c214`. The separate same-process
stability control passed. Command result: 1 pass, 1 fail, exit 1.

## packaged provenance implementation <!-- cq:wip-checkpoint -->

Added a generated `buildProvenance.gen.ts` module to the staged MCP closure and
made `flake.nix` reject missing, dirty, or non-full `self.rev`. The production
service resolves `startupBuildCommit` from that immutable module; live Git is
used only by the existing `repositoryHead` dependency. Source-workspace tests
use an explicit trusted injection which cannot replace a present packaged
identity. Standalone MCP, embedded TUI, embedded web, and embedded status keep
their shipped construction calls free of that injection.

The non-skippable install check now runs an installed-closure probe. A local
preflight with a simulated generated module observed build commit
`aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa`, distinct first/second repository
heads, and all four constructor labels. Substituting a different generated
full SHA failed with `installed provenance module does not match the derivation
source revision`. The parent owns the actual `nix build .#cq` after final-base
reconciliation.

## focused verification <!-- cq:wip-checkpoint -->

The final exact focused acceptance command discovered 29 tests and passed 29
with 90 expectations, followed by green `tsc -b` and `eslint .` runs.
`git diff --check` and `nix-instantiate --parse flake.nix` are also green.
`XDG_CACHE_HOME=/tmp/cq-t6558-nix-cache nix eval --raw .#cq.pname` reached the
Nix daemon boundary but confinement denied its socket with `Operation not
permitted`; the exact parent-owned remaining package command is
`nix build .#cq`, whose install check runs the non-skippable provenance probe.

Mutation observations (all affected production files restored to their
pre-mutation SHA-256):

| guard | mutation | expected failure | restored digest |
| --- | --- | --- | --- |
| immutable startup identity | derive build identity from live `git rev-parse HEAD` | D430 regression: expected injected `a…a`, received temporary first HEAD | `76db32e53fc8af7332d34ca3f2db810946dd70c850a625e1ad8dc84aaf5f9214` |
| missing/dirty/malformed/substituted provenance | bypass all selector guards | 4 targeted controls failed for the intended missing throws | `c527329a8a5658aff1df911e8bea9fe68432723c12764299321b84ef001fe251` |
| shipped constructor injection fence | inject a source-workspace commit in standalone MCP | shipped-constructor contract failed on the injected option | `3b73299b2683e95aafdf852168f24739c9a05f7e99ebc62dd928f9008344b843` |
| clean flake revision | remove the `dirtyRev` rejection | derivation contract failed on the absent clean-tree predicate | `b340d8747c33bcaada748973386bb503f377bacc4943fc886aa6c2ea8a58cafe` |
| derivation/module binding | substitute a different generated full SHA | installed probe rejected the derivation/source mismatch | `4e4e064faa3987fc2eaf3d866c26b63c93cf9d685c4d18423a7fdd0a7e0137cf` |

## trusted full gate <!-- cq:wip-checkpoint -->

Reserved for trusted parent finalization.

## real shipped entrypoint probe correction <!-- cq:wip-checkpoint -->

Round 2 will replace the private single-service four-label probe with public
status observations from the actual standalone MCP, embedded TUI, embedded
web, and status construction paths. Focused tests and guard mutations remain
unmeasured for this correction.

Fail-first measurement: the new public-status regression ran with Bun 1.3.13;
the existing eight controls passed and the shipped-constructor regression
failed at the intended boundary because
`createStandaloneImplementationEvidenceService` was absent (`undefined`
instead of `function`).

Implemented path-specific factories used by the standalone MCP, embedded TUI,
embedded web, and status production paths. The installed probe now constructs
each factory once and observes the public `evidenceServiceStatus()` before and
after advancing a temporary repository. A simulated generated provenance
module passed with immutable `aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa` across
all four factories while the live repository head changed from
`ba67ac25a799bdd0de33ff4a7d07ac4e45de728b` to
`f60435a694bcffa2b772db70a56554aec0a6802b`. The probe command no longer
reassigns `HOME`; the unrelated smoke-check assignment remains intact.

The exact focused acceptance command passed 29 tests / 103 expectations and
`tsc -b` exited zero. `eslint .`, `git diff --check`, and
`nix-instantiate --parse flake.nix` also exited zero. The operator explicitly
deferred `nix build .#cq` and the canonical full gate until the parent has the
final base.

Round-2 mutation observations (all affected files restored to their intended
SHA-256):

| guard | mutation | expected failure | restored digest |
| --- | --- | --- | --- |
| distinct shipped factories | replaced the embedded-TUI factory with the standalone factory | probe rejected fewer than four distinct constructors | `8d50343fb290a04563ae37a07202a1f73b2813b3ee65efcce6155c2d5d284d79` |
| packaged identity matches derivation | generated `a…a` but supplied expected `b…b` | standalone MCP rejected the substituted identity | `4e4e064faa3987fc2eaf3d866c26b63c93cf9d685c4d18423a7fdd0a7e0137cf` |
| live repository observation | compared the post-advance status with the first HEAD | probe rejected the stale repository head | `8d50343fb290a04563ae37a07202a1f73b2813b3ee65efcce6155c2d5d284d79` |
| public constructor regression | made the embedded-TUI factory throw before service construction | the shipped-constructor regression failed at that factory | `3e0d42ac88ad13e4399320a4deda55aa2a06651ca854a24f9e792848cab1ba1d` |

## installed probe runtime prerequisites <!-- cq:wip-checkpoint -->

Round 3 reproduces and corrects the installed-artifact probe's Nix runtime
boundary. The parent-built derivation reached the direct Bun probe and failed
before provenance assertions because `git` was absent from that process's
`PATH` (`ENOENT` at `probeImplementationEvidenceBuildProvenance.ts:21`). The
correction must make Git available to that exact install-check phase without a
host-PATH fallback, preserve the non-skippable four-factory assertions, and
check the probe's other runtime prerequisites.

The focused derivation guard was added before the production correction. Its
fail-first run executed 9 tests: the existing 8 provenance regression/controls
passed, and the new assertion failed because the cq derivation did not contain
`nativeBuildInputs = [ pkgs.bun pkgs.makeWrapper pkgs.git ];` (exit 1, 8 pass,
1 fail, 41 expectations). Inspection of the probe found no other external
runtime executable: Bun is invoked by its absolute Nix store path, while the
probe itself spawns only `git`.

Added `pkgs.git` to the cq derivation's `nativeBuildInputs`, which places the
Nix-provided executable on `PATH` for the direct Bun install-check probe as
well as the existing build phases. The focused provenance file then passed 9
tests / 42 expectations, including the unchanged D430 regression, paired
negative controls, four shipped-constructor control, and derivation guard.

The exact focused acceptance command passed 29 tests / 104 expectations and
`tsc -b` exited zero. `eslint .`, `nix-instantiate --parse flake.nix`, and
`git diff --check` also exited zero.

Historical operator package evidence closes this runtime-prerequisite
checkpoint: commit `84ccf59b8a4fe953217da215a2b29256e9919cfb` built
`/nix/store/5089k24fw6sldswn5qq3m2xnjcd8i80b-cq-0.0.1`; its installed checks
passed 46 tests / 0 failures / 516 assertions, and the installed provenance
probe exercised all four actual public service factories. This is evidence for
that historical package only. It does not prove the current
`08ea21b3fe5ac2da03d5226b0ff4645176c490de` tip or a future rebased candidate.
Exact-candidate package acceptance remains part of the parent-owned trusted
full-gate checkpoint, whose status stays `unmeasured`.

Round-3 mutation observation (affected files restored to their intended
SHA-256):

| guard | mutation | expected failure | restored digest |
| --- | --- | --- | --- |
| install-check Git runtime | removed `pkgs.git` from cq `nativeBuildInputs` | derivation guard failed with 8 pass / 1 fail because the required input declaration was absent | `flake.nix`: `9957b19853926846ccfce05070203c615522760532b3baabc6e74e7402ce3576`; test: `86c0940eeb12c48c3ee20c1504123115cf28af8c5c94078bb07061c941fd12d8` |

## post-rebase package acceptance <!-- cq:wip-checkpoint -->

Round 5 is the guarded bridge onto
`3d3b263d10c5083cae87ee7b085dfcc9ec22d0d7`. Focused constructor checks and
the exact-candidate `nix build .#cq` installed-artifact probe are unmeasured.
The trusted parent gate remains separately unmeasured.

The exact focused acceptance command passed 30 tests / 109 expectations and
`tsc -b` exited zero at committed candidate
`d7ce02322b6ac2fba3eeaa86f74a356a308f8cb1`. The required exact-candidate
package acceptance did not reach evaluation or build: plain `nix build .#cq`
failed opening the confined process's unwritable user fetcher cache, and a
retry with `XDG_CACHE_HOME=/tmp/cq-t6558-nix-cache` failed connecting to
`/nix/var/nix/daemon-socket/socket` with `Operation not permitted`. Therefore
no current package output, install-check result, or installed provenance probe
result exists, and this checkpoint remains `unmeasured` rather than green.

Subsequent parent-owned package evidence closes this checkpoint for the exact
`67f078c9eb1d20afca49ab1a31ab6660934cabe4` candidate. The retained report
`logs/20260919T235600Z-T6558-exact-67f-package.md` records a green
`nix build .#cq`: the installed checks passed 46 tests / 0 failures / 531
assertions, including the immutable-provenance probe through all four shipped
entrypoints. This is historical evidence for the `67f` package candidate; the
reserved parent-owned `trusted full gate` checkpoint remains `unmeasured` for
trusted finalization after this WIP-only commit.
