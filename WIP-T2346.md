```json
{
  "taskId": "T2346",
  "role": "implement-worker",
  "baseCommit": "ca2dee1b586544e5c5b92e4d2057ea8aac5523e3",
  "startedAt": "2026-08-27T20:37:49Z",
  "checkpoints": [
    { "name": "exploration", "status": "done" },
    { "name": "reproduction", "status": "done" },
    { "name": "implementation", "status": "done" },
    { "name": "mutation evidence", "status": "done" },
    { "name": "targeted checks", "status": "done" },
    { "name": "round 1 gate corrections", "status": "done" },
    { "name": "round 2 catalogue closure", "status": "done" },
    { "name": "round 3 evidence handoff", "status": "done" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## exploration <!-- cq:wip-checkpoint -->

Mapped the existing T2345 protected evidence journal, reviewer-attempt state
machine, management-only MCP registration, RemoteLedgerClient, production
runtime, prompt catalogue, role-tool matrix, CLI dispatcher, and backend store
construction. The existing implementation had no historical-audit or evidence-
activation records or operations.

## reproduction <!-- cq:wip-checkpoint -->

Added a Behavioral-Active Blackbox-Atomic contract for the distinct protected
audit and activation collections. Before implementation,
`bun test packages/ledger/test/implementation-evidence-audit.test.ts` failed
one test because the evidence snapshot contained only live-review panels,
attempts, and completions; all five required historical/activation collections
were absent.

## implementation <!-- cq:wip-checkpoint -->

Added the versioned protected audit/panel/attempt/requirement/activation model,
strict packaged-manifest resolution, authenticated native and adapter audit
attempts, all-abstain fallback, exact-cohort arm/apply/status operations, the
read-only implementation-auditor sidecar and role source, management tool and
RemoteLedgerClient surfaces, production runtime seams, and the bounded CLI
status probe, canonical D303/D340/D343 and finalized-key D347 fixtures, and
complete three-surface prompt/catalog integration.

## mutation evidence <!-- cq:wip-checkpoint -->

Two deliberate guard mutations failed for the expected reasons and passed
after exact restoration:

- Inverting the complete ordered audit-attempt comparison made the valid
  atomic activation case reject and the malformed cohort case resolve. The
  audit selector changed from 3 pass to 1 pass / 2 fail, then returned to 3
  pass. `implementationEvidence.ts` restored to SHA-256
  `7ffbee87f591a02f114949b81186a4165fb083e83e27ef2b9bfbd7e9f152bebf`.
- Allowing additional auditor input properties admitted caller-authored
  `worktreePath`, `workerResult`, `evidence`, and `taskRefs`. The prompt contract
  changed from 3 pass to 2 pass / 1 fail, then returned to 3 pass. The sidecar
  restored to SHA-256
  `fb6d2cdc7a02c093710b3236c8e632c4967ea38c72009540d94edbfa98293dd7`.

## targeted checks <!-- cq:wip-checkpoint -->

The exact nine-file acceptance selector passed 53 tests with 314 assertions.
TypeScript and ESLint passed. Related prompt-roster, generated-agent, model-
resolution, and configuration checks also passed; a confined Codex boundary
probe could not bind an ephemeral local port and is left to the trusted full
gate outside the child sandbox.

## trusted full gate <!-- cq:wip-checkpoint -->

Unmeasured in the child. The brokered dispatch delegates the canonical full
gate to trusted result storage after the final clean commit is handed off.

## round 1 gate corrections <!-- cq:wip-checkpoint -->

Reproduced the profiler's stale 1,405-token expectation (observed 1,621) and
the missing implementation-auditor fixture entry. Refreshed the canonical
T1326 target and normalized T1331 after-artifact, added the eight protected
audit/activation operations to the target inventory, and raised the strict
threshold to 1,622. The profiler passed after restoration; the focused role
record invariant passed. The local aggregate check reached unrelated
loopback/process and Nix-cache confinement failures; trusted result storage
will rerun the canonical gate outside this child boundary.

## round 2 catalogue closure <!-- cq:wip-checkpoint -->

The trusted gate reported coupled checked-in catalogue, sidecar-pin, rendered
surface, parity, and closure-manifest omissions. The correction closes the
implementation-auditor dispatch edge, adds its historical schema pin and Pi
fallback projection, removes the concrete goal identifier from canonical
implement/advance prose, refreshes the 54-tool response documentation, and
refreshes the managed closure digests. The focused catalogue suite passed 60
tests. Deliberately removing the auditor edge made the closed-set assertion
fail with the expected missing edge; exact restoration returned the selector to
7 passing tests and restored its SHA-256 to
`26e6ee2dc374783362b30c192ade9770f143aa230f6e7ac8cd056382fdd45977`.

## round 3 evidence handoff <!-- cq:wip-checkpoint -->

This round does not change the round-2 correction. It will rerun the focused
catalogue and transport checks and submit mutation evidence with the strict
`{ mutation, observed, restored }` row shape required by result storage.

The focused transport parity run instead reproduced a real fixture omission:
the matrix advertised 54 management tools but invoked only 46. It now seeds
authenticated historical-audit attempts and invokes all eight audit and
activation tools over direct and stdio transports. Omitting the fallback
attempt from the ordered cohort made both transport cases reject application;
the exact restored test SHA-256 is
`3ba905bc6f3959ed06a0d0e17ad9858e446ada79f9c54a7a8f3612d882b47a94`.
The focused four-file selector then passed 64 tests with 1,353 assertions.
