```json
{
  "taskId": "T1306",
  "role": "implement-worker",
  "baseCommit": "2cc42a7991d6086655f4bd7bd902a8e0e6116baa",
  "startedAt": "2026-08-07T12:00:00.000Z",
  "checkpoints": [
    { "name": "skeleton", "status": "done" },
    { "name": "worktree-manage-module", "status": "done" },
    { "name": "capability-inventory-wiring", "status": "done" },
    { "name": "schema-and-contract-tests", "status": "done" },
    { "name": "dual-transport-tests", "status": "done" },
    { "name": "full-gate", "status": "unmeasured" }
  ]
}
```

## skeleton <!-- cq:wip-checkpoint -->

Base verified. Dependencies installed.

## worktree-manage-module <!-- cq:wip-checkpoint -->

Added `worktreeManageTools.ts`: flat `operation: prepare|release` schema,
authoritative `dependencyTaskSnapshotReaderFromStore`, injected
`WorktreeManageCapability` (repository root + lifecycle + adapters).

## capability-inventory-wiring <!-- cq:wip-checkpoint -->

`worktree_manage` in LEDGER_CAPABILITY_TOOL_NAMES (31 tools), response
contracts, direct/stdio/`createLedgerMcpServer` wiring with `repositoryRoot`.

## schema-and-contract-tests <!-- cq:wip-checkpoint -->

Schema tests cover mixed fields, unknown keys, invalid UUID/commit/handle,
caller-supplied dependency evidence. Mutation evidence captured for the
dependency-evidence guard.

## dual-transport-tests <!-- cq:wip-checkpoint -->

Direct and stdio lifecycle matrix green: fresh prepare, resume-required,
resume-by-handle, base/dep refusal, dirty/WIP release refusal, idempotent
release.

## full-gate <!-- cq:wip-checkpoint -->

Pending `cq gate run … bun run check`.
