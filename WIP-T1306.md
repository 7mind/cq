```json
{
  "taskId": "T1306",
  "role": "implement-worker",
  "baseCommit": "2cc42a7991d6086655f4bd7bd902a8e0e6116baa",
  "startedAt": "2026-08-07T12:00:00.000Z",
  "checkpoints": [
    { "name": "skeleton", "status": "done" },
    { "name": "worktree-manage-module", "status": "todo" },
    { "name": "capability-inventory-wiring", "status": "todo" },
    { "name": "schema-and-contract-tests", "status": "todo" },
    { "name": "dual-transport-tests", "status": "todo" },
    { "name": "full-gate", "status": "unmeasured" }
  ]
}
```

## skeleton <!-- cq:wip-checkpoint -->

Base verified at `2cc42a7991d6086655f4bd7bd902a8e0e6116baa`. Worktree
`/home/pavel/work/safe/flakes/cq/.claude/worktrees/implement-T1306`.
Dependencies installed under `nix/pkg/cq-ledgers`.

## worktree-manage-module <!-- cq:wip-checkpoint -->

Add `worktreeManageTools.ts`: flat `operation: prepare|release` schema,
authoritative dependency-reader from ledger store, injected repository root /
lifecycle core / command adapters, typed prepare/release acknowledgements.

## capability-inventory-wiring <!-- cq:wip-checkpoint -->

Register `worktree_manage` in LEDGER_CAPABILITY_TOOL_NAMES, response contracts,
direct/stdio/`createLedgerMcpServer` injection from repository root.

## schema-and-contract-tests <!-- cq:wip-checkpoint -->

Reject mixed prepare/release fields, unknown fields, invalid UUID/commit/handle,
and caller-supplied dependency evidence.

## dual-transport-tests <!-- cq:wip-checkpoint -->

Direct and stdio: fresh prepare, resume-required, resume-by-handle, typed
base/dependency refusal, guarded release refusal, idempotent release; identical
normalised acknowledgements.

## full-gate <!-- cq:wip-checkpoint -->

`cq gate run --worktree "$PWD" --command-cwd "$PWD/nix/pkg/cq-ledgers" -- bun run check`
