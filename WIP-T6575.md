```json
{
  "taskId": "T6575",
  "role": "implement-worker",
  "baseCommit": "3d3b263d10c5083cae87ee7b085dfcc9ec22d0d7",
  "startedAt": "2026-09-20T01:41:00Z",
  "checkpoints": [
    { "name": "reproduce native fail qualification", "status": "done" },
    { "name": "reproduce cancelled-source recovery", "status": "done" },
    { "name": "implement consumed fail qualification", "status": "done" },
    { "name": "implement cancelled-source recovery", "status": "done" },
    { "name": "prove changed guards", "status": "done" },
    { "name": "focused checks", "status": "done" },
    { "name": "reproduce correction controls", "status": "done" },
    { "name": "correct recovery fixture and closure manifest", "status": "done" },
    { "name": "prove correction guards", "status": "done" },
    { "name": "correction focused checks", "status": "done" },
    { "name": "reproduce composed ancestry rejection", "status": "done" },
    { "name": "implement persisted composed ancestry", "status": "done" },
    { "name": "prove composed ancestry guards", "status": "done" },
    { "name": "round 3 focused checks", "status": "done" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## reproduce native fail qualification <!-- cq:wip-checkpoint -->

The live `tasks:T6575` progression test stores a schema-valid failure with a
durable broker receipt. Unsuppressed execution failed at the pass-only
qualification guard: `only a passing broker-verified worker result can enter
the runnable queue`.

## reproduce cancelled-source recovery <!-- cq:wip-checkpoint -->

The live `tasks:T6575` progression test derives generation 18 from a committed
generation-17 journal, aborts it as `cancelled`, and attempts epoch promotion.
Unsuppressed execution failed with `source-not-found`: the successor was not
considered an eligible terminal worker abort.

## implement consumed fail qualification <!-- cq:wip-checkpoint -->

Native qualification now validates the exact process/provenance and the failure's
manager-bound receipt evidence under the worktree lock, then atomically consumes
the staged failure with a handle-only acknowledgement. Exact replay is bound to
the persisted observation digest. Tests cover memory, SQLite close/reopen, body
fetch, replay, altered observations, receipt substitutions, and zero queue/gate
or successor effects.

## implement cancelled-source recovery <!-- cq:wip-checkpoint -->

The version-1 aborted current-recovery source admits the explicit `cancelled`
cause. Capture, strict seal/status parsing, filesystem journal round-trip, and
committed epoch promotion retain that cause and the existing identity/fence/
receipt checks.

## prove changed guards <!-- cq:wip-checkpoint -->

Deliberate mutations produced the expected failures, then exact restoration
returned each file to its committed SHA-256 digest:

- bypassing the failure-status branch produced the original pass-only queue
  rejection; the focused failure-consumption test then passed after restoration
  (`dispatchCapability.ts` `b6cf15bb...`).
- inverting the persisted observation-digest comparison rejected the exact
  replay; the same test passed after restoration (`dispatchAttestation.ts`
  `b8ad428e...`).
- bypassing broker receipt normalization made substitution/reordering controls
  fail with later continuation errors instead of the expected manager-bound
  journal errors; both controls passed after restoration
  (`dispatchCapability.ts` `b6cf15bb...`).
- rejecting the consumed acknowledgement made the native qualifier report a
  foreign acknowledgement; its test passed after restoration
  (`codexRoleBoundary.ts` `3d08846d...`).
- deleting `cancelled` from the capture eligibility set selected generation 2
  instead of 9 and rejected epoch promotion; both tests passed after restoration
  (`dispatchRecoverySeal.ts` `a8a9b865...`).
- deleting `cancelled` from the shared recovery schema caused both in-memory and
  filesystem adapters to reject the cause; both passed after restoration
  (`currentRecoverySeal.ts` `508c7999...`).

## focused checks <!-- cq:wip-checkpoint -->

Green worker checks:

- `bun run typecheck`: exit 0.
- `bun run lint`: exit 0.
- six in-process/paired suite files: 163 pass, 1 environment-gated PostgreSQL
  skip, 0 fail.
- five intentional-failure consumption/receipt cases: 5 pass, 0 fail.
- native consumed-acknowledgement boundary case: 1 pass, 0 fail.
- expected-failure marker/inventory policy: 10 pass, 0 fail.

The installed wrapper's host-native execution is parent-owned. Directly running
its installed-runner cases in this confined child cannot reach the workset
provider and times out; no green result is claimed for that path here.

## reproduce correction controls <!-- cq:wip-checkpoint -->

The exact correction command reported 6 pass and 2 fail. The recovery fixture's
negative control used the newly eligible `cancelled` cause and therefore did not
throw. Repository closure resolution rejected only the stale declared digest for
`dispatchCapability.ts` (`69c81b9e...` declared, `b6cf15bb...` observed).

## correct recovery fixture and closure manifest <!-- cq:wip-checkpoint -->

The recovery selector now retains the malformed-digest and genuinely excluded
`gate-rejected` negative controls and adds an explicit allowed-`cancelled`
control. Closure declarations now carry the exact current SHA-256 bytes for
every changed source already present as an opaque edge; targets and guards are
unchanged. The three focused suites reported 48 pass and 0 fail.

## prove correction guards <!-- cq:wip-checkpoint -->

Three deliberate mutations failed for their intended reasons and exact bytes
were restored:

- replacing the excluded `gate-rejected` control with `cancelled` produced the
  expected "function did not throw" failure; restoration passed 1/1.
- replacing the admitted `cancelled` control with `gate-rejected` produced the
  expected `abortReason` schema rejection; restoration passed 1/1.
- replacing the declared `dispatchCapability.ts` digest with zeros produced the
  exact source-digest mismatch; the manifest was restored to SHA-256
  `b413fa38...` and the selector test to `da274432...`.

## correction focused checks <!-- cq:wip-checkpoint -->

Green correction checks:

- selector plus closure repository/target suites: 48 pass, 0 fail.
- `bun run typecheck`: exit 0.
- `bun run lint`: exit 0.

No production source changed in this correction round. The trusted parent owns
the sole new exact-candidate canonical gate.

## reproduce composed ancestry rejection <!-- cq:wip-checkpoint -->

Two literal `tasks:T6575` progression tests were run unsuppressed before product
edits. Both failed at `dispatchImplementationQueue.ts:1092` with
`terminal enrollment authority on <attestation>#1 cannot be resurrected`:

- a consumed pass was continued into a genuine gate rejection, after which an
  ordinary changed correction could not qualify;
- a qualified source became `parent-lost`, its journal-derived successor was
  explicitly cancelled, and the next public current-seal recovery successor
  could not qualify.

With their annotations, markers, and inventory entries restored, the two tests
and expected-failure policy reported 2 pass, 0 fail.

## implement persisted composed ancestry <!-- cq:wip-checkpoint -->

Queue enrollment now composes exact persisted continuation claims with the
existing rejected-gate correction proof instead of treating every earlier
terminal enrollment as unrelated. Abort transitions retain the claim.

Current-journal recovery prepare also persists a capability-free claim binding
the committed fence and seal, selected terminal source and cause, lineage
maximum, task/goal/finalized manifest, manager fingerprint, live tip, and
receipt closure. Enrollment accepts an earlier terminal queue only when that
claim, the selected source row, manager/bridge identity, receipt prefix, and
changed successor suffix all validate. Missing and substituted claims fail
closed. The corrected tests pass in memory and across SQLite close/reopen.

## prove composed ancestry guards <!-- cq:wip-checkpoint -->

Five deliberate mutations failed for their intended reasons before exact-byte
restoration:

- dropping continuation-claim retention rejected the composed correction with
  `rejected continuation lost its persisted source claim`; restoration passed
  the case (`dispatchAttestation.ts` `c72a3796...`; mutated `52ee5f1f...`).
- omitting the journal recovery claim rejected the SQLite successor with
  `sealed successor lost its persisted journal claim`; restoration passed
  (`dispatchCapability.ts` `43c200c8...`; mutated `189eebe7...`).
- changing the persisted claim schema version rejected SQLite reopen with a
  malformed-claim error; restoration passed
  (`dispatchAttestationBackend.ts` `d4b43e29...`; mutated `ef6f6b84...`).
- bypassing all journal-claim validation made the substitution control resolve
  instead of reject; restoration passed
  (`dispatchImplementationQueue.ts` `6a8b9592...`; mutated `1ef1429e...`).
- replacing the declared capability-source digest with zeros produced the
  exact repository source-digest mismatch; restoration retained the corrected
  digest (`cq-gate-closure.json` was `8166637f...` before final test-edge
  digest updates; mutated `5ce443c3...`).

## round 3 focused checks <!-- cq:wip-checkpoint -->

The changed ancestry and recovery surfaces are green:

- the two corrected progression cases: 2 pass, 0 fail;
- dispatch/recovery/source-selection and marker-policy suites: 177 pass,
  0 fail;
- closure repository and target suites: 40 pass, 0 fail;
- `bun run typecheck`: exit 0;
- `bun run lint`: exit 0.

The specified nine-file acceptance command did not complete green: 245 tests
passed, one skipped, four failed, and one asynchronous error escaped. The
installed successor/coordinator case timed out after 30 seconds, two installed
role-boundary cases timed out after 10 seconds, and parent finalization received
non-JSON child stdout. All four failing selectors reproduced when isolated;
the installed successor/coordinator test again timed out after 30 seconds, the
rendered-envelope and queued-handle tests again timed out after 10 seconds, and
the parent finalizer again received non-JSON stdout. These process-boundary
failures preclude a passing result; no unrelated timeout workaround was added.

## trusted full gate <!-- cq:wip-checkpoint -->

Unmeasured. Parent-owned final validation remains at this checkpoint.
