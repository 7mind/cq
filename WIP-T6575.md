```json
{
  "taskId": "T6575",
  "role": "implement-worker",
  "baseCommit": "3d3b263d10c5083cae87ee7b085dfcc9ec22d0d7",
  "startedAt": "2026-09-20T01:41:00Z",
  "checkpoints": [
    { "name": "reproduce native fail qualification", "status": "done" },
    { "name": "reproduce cancelled-source recovery", "status": "done" },
    { "name": "implement consumed fail qualification", "status": "done" },
    { "name": "implement cancelled-source recovery", "status": "done" },
    { "name": "prove changed guards", "status": "done" },
    { "name": "focused checks", "status": "done" },
    { "name": "reproduce correction controls", "status": "todo" },
    { "name": "correct recovery fixture and closure manifest", "status": "todo" },
    { "name": "prove correction guards", "status": "todo" },
    { "name": "correction focused checks", "status": "todo" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## reproduce native fail qualification <!-- cq:wip-checkpoint -->

The live `tasks:T6575` progression test stores a schema-valid failure with a
durable broker receipt. Unsuppressed execution failed at the pass-only
qualification guard: `only a passing broker-verified worker result can enter
the runnable queue`.

## reproduce cancelled-source recovery <!-- cq:wip-checkpoint -->

The live `tasks:T6575` progression test derives generation 18 from a committed
generation-17 journal, aborts it as `cancelled`, and attempts epoch promotion.
Unsuppressed execution failed with `source-not-found`: the successor was not
considered an eligible terminal worker abort.

## implement consumed fail qualification <!-- cq:wip-checkpoint -->

Native qualification now validates the exact process/provenance and the failure's
manager-bound receipt evidence under the worktree lock, then atomically consumes
the staged failure with a handle-only acknowledgement. Exact replay is bound to
the persisted observation digest. Tests cover memory, SQLite close/reopen, body
fetch, replay, altered observations, receipt substitutions, and zero queue/gate
or successor effects.

## implement cancelled-source recovery <!-- cq:wip-checkpoint -->

The version-1 aborted current-recovery source admits the explicit `cancelled`
cause. Capture, strict seal/status parsing, filesystem journal round-trip, and
committed epoch promotion retain that cause and the existing identity/fence/
receipt checks.

## prove changed guards <!-- cq:wip-checkpoint -->

Deliberate mutations produced the expected failures, then exact restoration
returned each file to its committed SHA-256 digest:

- bypassing the failure-status branch produced the original pass-only queue
  rejection; the focused failure-consumption test then passed after restoration
  (`dispatchCapability.ts` `b6cf15bb...`).
- inverting the persisted observation-digest comparison rejected the exact
  replay; the same test passed after restoration (`dispatchAttestation.ts`
  `b8ad428e...`).
- bypassing broker receipt normalization made substitution/reordering controls
  fail with later continuation errors instead of the expected manager-bound
  journal errors; both controls passed after restoration
  (`dispatchCapability.ts` `b6cf15bb...`).
- rejecting the consumed acknowledgement made the native qualifier report a
  foreign acknowledgement; its test passed after restoration
  (`codexRoleBoundary.ts` `3d08846d...`).
- deleting `cancelled` from the capture eligibility set selected generation 2
  instead of 9 and rejected epoch promotion; both tests passed after restoration
  (`dispatchRecoverySeal.ts` `a8a9b865...`).
- deleting `cancelled` from the shared recovery schema caused both in-memory and
  filesystem adapters to reject the cause; both passed after restoration
  (`currentRecoverySeal.ts` `508c7999...`).

## focused checks <!-- cq:wip-checkpoint -->

Green worker checks:

- `bun run typecheck`: exit 0.
- `bun run lint`: exit 0.
- six in-process/paired suite files: 163 pass, 1 environment-gated PostgreSQL
  skip, 0 fail.
- five intentional-failure consumption/receipt cases: 5 pass, 0 fail.
- native consumed-acknowledgement boundary case: 1 pass, 0 fail.
- expected-failure marker/inventory policy: 10 pass, 0 fail.

The installed wrapper's host-native execution is parent-owned. Directly running
its installed-runner cases in this confined child cannot reach the workset
provider and times out; no green result is claimed for that path here.

## reproduce correction controls <!-- cq:wip-checkpoint -->

Pending the exact two-file reproduction requested for this correction round.

## correct recovery fixture and closure manifest <!-- cq:wip-checkpoint -->

Pending fixture and manifest corrections only; production code is unchanged.

## prove correction guards <!-- cq:wip-checkpoint -->

Pending deliberate failure and restoration evidence for the new controls.

## correction focused checks <!-- cq:wip-checkpoint -->

Pending source-selection, closure repository/target, typecheck, and lint checks.

## trusted full gate <!-- cq:wip-checkpoint -->

Unmeasured. Parent-owned final validation remains at this checkpoint.
