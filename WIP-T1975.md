```json
{
  "taskId": "T1975",
  "role": "implement",
  "baseCommit": "b8694e8ca0277f32c80b9171c3e80b85d9e768fe",
  "startedAt": "2026-08-10T07:40:43Z",
  "checkpoints": [
    { "name": "postgres-guarded-factory", "status": "todo" },
    { "name": "export-surface", "status": "todo" },
    { "name": "shared-contract-postgres", "status": "todo" },
    { "name": "focused-postgres-cases", "status": "todo" },
    { "name": "fault-and-isolation-cases", "status": "todo" },
    { "name": "targeted-tests", "status": "unmeasured" },
    { "name": "full-gate", "status": "unmeasured" }
  ]
}
```

## postgres-guarded-factory <!-- cq:wip-checkpoint -->

Wire T1961 `createWorksetGuardedLedger` over `PostgresLedgerStore` +
`createPostgresWorksetStore` with closed-graph `isTargetAdmitted`.

## export-surface <!-- cq:wip-checkpoint -->

Export `createPostgresWorksetGuardedLedger` (+ options type) from package index.

## shared-contract-postgres <!-- cq:wip-checkpoint -->

`workset-generic-mutation-postgres.test.ts` runs shared contract unchanged
(env-gated on `CQ_TEST_PG_URL`).

## focused-postgres-cases <!-- cq:wip-checkpoint -->

Good-Communication: status/reference updates, exact-root unarchive, archive
sweeps, restart, peer invalidation, tenant isolation, NOTIFY-after-commit.

## fault-and-isolation-cases <!-- cq:wip-checkpoint -->

`workset-generic-mutation-postgres-faults.test.ts`: failed mutation leaves
tenant unchanged, set waits on admitted mutations, cross-server races,
rollback/zero-mutation.

## targeted-tests <!-- cq:wip-checkpoint -->

Acceptance selector tests under `CQ_TEST_PG_URL`.

## full-gate <!-- cq:wip-checkpoint -->

`cq gate run … bun run check` with `CQ_TEST_PG_URL`.
