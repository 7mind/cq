```json
{
  "taskId": "T1972",
  "role": "implement-worker",
  "baseCommit": "b8694e8ca0277f32c80b9171c3e80b85d9e768fe",
  "startedAt": "2026-08-04T12:00:00.000Z",
  "checkpoints": [
    {"name": "createFsWorksetGuardedLedger", "status": "todo"},
    {"name": "exports", "status": "todo"},
    {"name": "fs-contract-tests", "status": "todo"},
    {"name": "fs-fault-tests", "status": "todo"},
    {"name": "targeted-checks", "status": "unmeasured"},
    {"name": "full-gate", "status": "unmeasured"}
  ]
}
```

## createFsWorksetGuardedLedger <!-- cq:wip-checkpoint -->

Wire WorksetGuardedLedger over FsLedgerStore + createFsWorksetStore with closed-graph isTargetAdmitted (parity with createInMemoryWorksetGuardedLedger).

## exports <!-- cq:wip-checkpoint -->

Export factory + options type from package index.

## fs-contract-tests <!-- cq:wip-checkpoint -->

workset-generic-mutation-fs.test.ts: shared contract + restart/competing/update/unarchive/archive cases.

## fs-fault-tests <!-- cq:wip-checkpoint -->

workset-generic-mutation-fs-faults.test.ts: write/rename failures, replacement races, zero-mutation before I/O.

## targeted-checks <!-- cq:wip-checkpoint -->

bun test acceptance selectors.

## full-gate <!-- cq:wip-checkpoint -->

cq gate run bun run check.
