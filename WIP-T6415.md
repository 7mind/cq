```json
{
  "taskId": "T6415",
  "role": "implement-worker",
  "baseCommit": "abd095511ddfcaf13a0d040b049cd401d39fdde8",
  "startedAt": "2026-09-02T18:34:12Z",
  "checkpoints": [
    { "name": "map legacy utility ownership", "status": "done" },
    { "name": "retarget repository utilities", "status": "done" },
    { "name": "prior criticism durability", "status": "done" },
    { "name": "targeted acceptance", "status": "done" },
    { "name": "closure declaration", "status": "done" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## map legacy utility ownership <!-- cq:wip-checkpoint -->

Grounded the prepared task branch and its verified base commit.

## retarget repository utilities <!-- cq:wip-checkpoint -->

Removed the retired importer, moved normalization to the management-selected
SQLite path and its focused test to `SqliteLedgerStore`, and regenerated the
sample through an in-memory store plus the backup exporter.

## prior criticism durability <!-- cq:wip-checkpoint -->

The actual normalize command initially updated one item but left no in-tree
backup because `close()` cancelled its queued export. A focused command-level
regression test now reproduces the configured XDG/in-tree path and verifies
that the command drains the backup before closing it.

## targeted acceptance <!-- cq:wip-checkpoint -->

The focused normalization and backup suites passed 22 active tests (3 known
Postgres cases skipped). Sample seeding, bootstrap regeneration, retired-file
checks, and the task-local retired-provider audit passed cleanly.

## targeted checks <!-- cq:wip-checkpoint -->

## closure declaration <!-- cq:wip-checkpoint -->

The trusted gate reported the test's Bun.spawn call as undeclared. The closure
entry records the source path and its measured content hash. Its focused test
failed with a deliberately incorrect digest, then passed after restoration.

## trusted full gate <!-- cq:wip-checkpoint -->
