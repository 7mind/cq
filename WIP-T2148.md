```json
{
  "taskId": "T2148",
  "role": "implement-worker",
  "baseCommit": "fb8ea129ac06a2b7e7c708102de15a17de0ea551",
  "startedAt": "2026-08-18T19:05:00.000Z",
  "checkpoints": [
    { "name": "Step 0 base verification", "status": "done" },
    { "name": "D332 form-(a) control (marker + annotation + inventory)", "status": "done" },
    { "name": "D334 form-(a) control (marker + annotation + inventory)", "status": "done" },
    { "name": "Inventory policy test update (six -> eight live markers)", "status": "done" },
    { "name": "Fail-first observations (D332 acceptance, D334 receipt-lineage rejection)", "status": "done" },
    { "name": "T2119 D333 plain control stays green", "status": "done" },
    { "name": "Mutation evidence for changed guards", "status": "done" },
    { "name": "Focused three-file bun test green", "status": "done" },
    { "name": "Full gate (cq gate run -- bun run check)", "status": "todo" }
  ]
}
```

## Step 0 base verification <!-- cq:wip-checkpoint -->
Verified 2026-08-18: toplevel
`/home/pavel/work/safe/flakes/cq/.claude/worktrees/01a0162f-0ff7-749b-a8a0-384ee8a223d6`,
branch `implement/T2148`, HEAD == startingCommit == baseCommit
`fb8ea129ac06a2b7e7c708102de15a17de0ea551` (`git cat-file -t` = commit,
`merge-base --is-ancestor` exit 0, relation equal), clean tree. Round 0, no
priorResultCommit, held dispatch protocol (no gitChangeCapability): confined
commit path, no gitReceipts in the result.

## D332 form-(a) control (marker + annotation + inventory) <!-- cq:wip-checkpoint -->
Done: `test.failing` "D332 rejects a lineage-free implement-worker retry at an
advanced managed-worktree tip [Behavioral-Progression
Blackbox-GoodCommunication]" in `gitChangeDispatchCapability.test.ts` with
immediately preceding `// expected-failure: defects:D332`; same-titled
inventory entry (ledgerRef defects:D332). Fixture: brokered generation 1
commits one durable receipt at the managed tip and aborts; the round-1 retry
at that advanced tip omits reprepareOf. The control asserts rejection with no
handle/prepared/allocated authority.

## D334 form-(a) control (marker + annotation + inventory) <!-- cq:wip-checkpoint -->
Done: `test.failing` "D334 accepts an exact guarded-rebase continuation after
broker restart [Behavioral-Progression Blackbox-GoodCommunication]" with
`// expected-failure: defects:D334`; same-titled inventory entry (ledgerRef
defects:D334). Production-layout fixture (initial branch main,
`.claude/worktrees/.cq-managed-registry`, cq.toml fs backend, wip task item),
real `cq gate git-effect --operation rebase` rewrites the managed branch onto
the advanced main; after a broker restart the exact reprepare at the rebased
tip must be accepted and complete the public lifecycle; rejection allocates
no authority.

## Inventory policy test update (six -> eight live markers) <!-- cq:wip-checkpoint -->
Done: `EXPECTED_FAILURE_INVENTORY` gained the D332/D334 entries
(DISPATCH_CAPABILITY_FILE const); the committed-inventory test now expects
eight markers in scanner order [defects:D332, defects:D334, 6x tasks:T826]
(ledger-mcp sorts before ledger under localeCompare; D332 line < D334 line).

## Fail-first observations (D332 acceptance, D334 receipt-lineage rejection) <!-- cq:wip-checkpoint -->
Done (both controls temporarily flipped to plain `test`):
- D332: `D332 observed lineage-free advanced-tip acceptance: prepare allocated
  attestation att_1t3k6_L5AAcOFRwjKjE4P0ZNVFtiaXB3 generation 1 with a broker
  Git capability at managed tip bda7b373d581ff3d7967fb72902a4224769117c4
  without reprepareOf lineage`.
- D334: `D334 observed guarded-rebase receipt-lineage rejection at rebased tip
  e4fef3dcd6ff3ee73aa2ce65f533e306c8c35d04: prior-generation receipt
  inheritance failed: durable broker receipt
  t2148-d334-guarded-rebase-round-0-change does not belong to resultCommit`.
Both signatures are the defect observations, not setup/Git/timeout/schema
failures. Markers restored; file hash c73e7830... verified pre/post mutation.

## T2119 D333 plain control stays green <!-- cq:wip-checkpoint -->
Done: "rehydrates a prepared worker's inherited receipt prefix after a broker
restart" passes unchanged (focused run, 66 pass / 0 fail across the three
acceptance files); no edits to it or to `dispatchAttestationBackend.test.ts`.

## Mutation evidence for changed guards <!-- cq:wip-checkpoint -->
Done:
1. D332 test.failing->test: failed with the D332 acceptance observation (above).
2. D334 test.failing->test: failed with the D334 receipt-lineage observation.
3. Policy toHaveLength(8)->(9): committed-inventory test failed as expected.
4. D332 annotation deleted: scanner rejected with "expected-failure marker
   lacks an immediately preceding annotation" at line 1167.
All restorations hash-verified (capability test c73e7830..., policy test
05bbbee1..., inventory 79a3167b...).

## Focused three-file bun test green <!-- cq:wip-checkpoint -->
Done: `bun test packages/ledger-mcp/test/gitChangeDispatchCapability.test.ts
packages/cq-config/test/dispatchAttestationBackend.test.ts
packages/cq-config/test/expectedFailurePolicy.test.ts` -> 66 pass, 0 fail,
exit 0 (22.12s).

## Full gate (cq gate run -- bun run check) <!-- cq:wip-checkpoint -->
Todo: `cq gate run --worktree "$PWD" --command-cwd "$PWD/nix/pkg/cq-ledgers"
-- bun run check` from the worktree root; capture REAL_CHECK_EXIT, tail, and
gateDurationMs.
