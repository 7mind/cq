```json
{
  "taskId": "T2148",
  "role": "implement-worker",
  "baseCommit": "fb8ea129ac06a2b7e7c708102de15a17de0ea551",
  "startedAt": "2026-08-18T19:05:00.000Z",
  "checkpoints": [
    { "name": "Step 0 base verification", "status": "done" },
    { "name": "D332 form-(a) control (marker + annotation + inventory)", "status": "todo" },
    { "name": "D334 form-(a) control (marker + annotation + inventory)", "status": "todo" },
    { "name": "Inventory policy test update (six -> eight live markers)", "status": "todo" },
    { "name": "Fail-first observations (D332 acceptance, D334 receipt-lineage rejection)", "status": "todo" },
    { "name": "T2119 D333 plain control stays green", "status": "todo" },
    { "name": "Mutation evidence for changed guards", "status": "todo" },
    { "name": "Focused three-file bun test green", "status": "todo" },
    { "name": "Full gate (cq gate run -- bun run check)", "status": "todo" }
  ]
}
```

## Step 0 base verification <!-- cq:wip-checkpoint -->
Verified 2026-08-18: toplevel
`/home/pavel/work/safe/flakes/cq/.claude/worktrees/01a0162f-0ff7-749b-a8a0-384ee8a223d6`,
branch `implement/T2148`, HEAD == startingCommit == baseCommit
`fb8ea129ac06a2b7e7c708102de15a17de0ea551` (`git cat-file -t` = commit,
`merge-base --is-ancestor` exit 0, relation equal), clean tree. Round 0, no
priorResultCommit, held dispatch protocol (no gitChangeCapability): confined
commit path, no gitReceipts in the result.

## D332 form-(a) control (marker + annotation + inventory) <!-- cq:wip-checkpoint -->
Todo: in `packages/ledger-mcp/test/gitChangeDispatchCapability.test.ts`, a
`test.failing` titled "D332 rejects a lineage-free implement-worker retry at an
advanced managed-worktree tip [Behavioral-Progression
Blackbox-GoodCommunication]" with an immediately preceding
`// expected-failure: defects:D332` annotation; same-titled inventory entry
with ledgerRef `defects:D332`. Setup: brokered generation 1 commits a durable
receipt and aborts; a same-task retry at that advanced managed tip omits
reprepareOf and must be rejected with no attestation/capability allocated.
Uncorrected baseline is expected to accept (D332 observation).

## D334 form-(a) control (marker + annotation + inventory) <!-- cq:wip-checkpoint -->
Todo: same file, `test.failing` titled "D334 accepts an exact guarded-rebase
continuation after broker restart [Behavioral-Progression
Blackbox-GoodCommunication]" with `// expected-failure: defects:D334`;
same-titled inventory entry. Setup: real task-bound guarded rebase via
`cq gate git-effect --operation rebase` on a production-layout fixture, then
exact reprepare at the rebased tip. Uncorrected baseline rejects with
`prior-generation receipt inheritance failed: durable broker receipt ... does
not belong to resultCommit` (D334 observation).

## Inventory policy test update (six -> eight live markers) <!-- cq:wip-checkpoint -->
Todo: extend `EXPECTED_FAILURE_INVENTORY` with the D332/D334 entries and update
the "committed inventory agrees bidirectionally" test in
`packages/cq-config/test/expectedFailurePolicy.test.ts` from six T826 markers
to eight markers (D332, D334, then six tasks:T826 in scanner order:
ledger-mcp sorts before ledger under localeCompare).

## Fail-first observations (D332 acceptance, D334 receipt-lineage rejection) <!-- cq:wip-checkpoint -->
Todo: run both controls as plain tests (temporary probe) on this baseline and
capture: D332 -> prepare accepts/allocates without reprepareOf lineage;
D334 -> prepare rejects at `input.startingCommit` with the receipt-lineage
signature. Restore `test.failing` afterwards.

## T2119 D333 plain control stays green <!-- cq:wip-checkpoint -->
Todo: confirm "rehydrates a prepared worker's inherited receipt prefix after a
broker restart" (generation-1 -> restart -> generation-2 correction, exact
inherited prefix plus one new receipt) passes unchanged; no edits to it or to
`dispatchAttestationBackend.test.ts`.

## Mutation evidence for changed guards <!-- cq:wip-checkpoint -->
Todo: flip each new `test.failing` to `test` and capture the expected failure
signature; break the inventory-count assertion and capture its failure; hash
affected files before/after restoration.

## Focused three-file bun test green <!-- cq:wip-checkpoint -->
Todo: `bun test packages/ledger-mcp/test/gitChangeDispatchCapability.test.ts
packages/cq-config/test/dispatchAttestationBackend.test.ts
packages/cq-config/test/expectedFailurePolicy.test.ts` exits zero from
`nix/pkg/cq-ledgers`.

## Full gate (cq gate run -- bun run check) <!-- cq:wip-checkpoint -->
Todo: `cq gate run --worktree "$PWD" --command-cwd "$PWD/nix/pkg/cq-ledgers"
-- bun run check` from the worktree root; capture REAL_CHECK_EXIT, tail, and
gateDurationMs.
