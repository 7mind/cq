```json
{
  "taskId": "T1957",
  "role": "implement",
  "baseCommit": "46b434ef81008c8955ed66cd12afd9a5fd3b6d77",
  "startedAt": "2026-08-09T00:55:00Z",
  "checkpoints": [
    {"name": "grounding", "status": "done"},
    {"name": "schema-and-persistence", "status": "done"},
    {"name": "sqlite-workset-store", "status": "done"},
    {"name": "contract-factory", "status": "done"},
    {"name": "effectual-sqlite-tests", "status": "done"},
    {"name": "schema-contention-extensions", "status": "done"},
    {"name": "mutation-evidence", "status": "done"},
    {"name": "full-gate", "status": "unmeasured"}
  ]
}
```

## grounding <!-- cq:wip-checkpoint -->

T1957: Persist project roots and admissions in SQLite on SqliteLedgerStore.

Acceptance tests green locally:
- workset-store-contract + workset-store-sqlite + workset-effect-admission-sqlite + sqlite-schema + sqlite-connection-contention

## schema-and-persistence <!-- cq:wip-checkpoint -->

SCHEMA_VERSION 3→4. Tables: workset_state, workset_admissions, workset_exclusive. Marker-only migrate like v2→v3.

## sqlite-workset-store <!-- cq:wip-checkpoint -->

createSqliteWorksetStore + SqliteLedgerStore.worksetStore() accessor (snapshot name collision with LedgerStore).

## contract-factory <!-- cq:wip-checkpoint -->

workset-store-sqlite.test.ts runs runWorksetStoreContract unchanged.

## effectual-sqlite-tests <!-- cq:wip-checkpoint -->

workset-effect-admission-sqlite.test.ts: peer durability, set waits, stale reclaim, crash cleanup, cross-process child.

## schema-contention-extensions <!-- cq:wip-checkpoint -->

sqlite-schema asserts workset tables + v4. Contention suite unchanged/pass.

## mutation-evidence <!-- cq:wip-checkpoint -->

| guard | mutate | fail | restore hash match | pass |
| SCHEMA_VERSION=4 | =5 | schema+seed tests | YES | YES |
| epoch+1 | epoch+0 | advances epoch | YES | YES |
| isPidAlive reclaim | if(true)continue | stale reclaim | YES | YES |

## full-gate <!-- cq:wip-checkpoint -->

unmeasured
