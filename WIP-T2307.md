```json
{
  "taskId": "T2307",
  "role": "implement-worker",
  "baseCommit": "1bb8721723eb916c45827694d4219f3f180fdc67",
  "startedAt": "2026-08-22T20:42:19Z",
  "checkpoints": [
    { "name": "reproduction", "status": "done" },
    { "name": "resolver", "status": "done" },
    { "name": "worktree-management", "status": "done" },
    { "name": "acceptance", "status": "done" },
    { "name": "gate-delegation", "status": "todo" }
  ]
}
```

## reproduction <!-- cq:wip-checkpoint -->

The fresh base exercised the resolver and existing external-effect closure
without the abandoned task's manager-allocation regression. The fresh task
therefore restores that missing behavioral guard through its own brokered
commit chain.

## resolver <!-- cq:wip-checkpoint -->

The pure resolver preserves external-effect identity and transitive Git
contributions while requiring each Git-producing contribution to be a valid,
contained commit. The focused resolver suite is green.

## worktree-management <!-- cq:wip-checkpoint -->

The new regression covers missing, malformed, missing-object, non-commit, and
non-contained Git contributions. Every case refuses before worktree allocation,
installation, or live-state creation. The successful operator-action path
proves exactly one install and one live worktree allocation.

## acceptance <!-- cq:wip-checkpoint -->

The four-file acceptance suite is green. Mutation probes changed the invalid
dependency allocation count from zero to one and the valid prepare install
count from one to two; each focused test failed with the expected count before
the exact bytes were restored and the focused test passed.

## gate-delegation <!-- cq:wip-checkpoint -->

Pending.
