```json
{
  "taskId": "T2816",
  "role": "implement-worker",
  "baseCommit": "0f0f63a5ebbc1b54a0cabf468128d1a8479a28dd",
  "startedAt": "2026-08-25T06:17:00Z",
  "checkpoints": [
    { "name": "ground implementation surface", "status": "done" },
    { "name": "reproduce missing lineage fence", "status": "done" },
    { "name": "implement cutover fence", "status": "done" },
    { "name": "prove changed guards", "status": "done" },
    { "name": "run acceptance selectors", "status": "done" },
    { "name": "verify clean committed tip", "status": "done" },
    { "name": "correct tool-surface evidence regression", "status": "done" },
    { "name": "correct continuation validation ordering", "status": "done" },
    { "name": "restore closed-reason stage coverage", "status": "done" },
    { "name": "rerun focused regressions", "status": "done" },
    { "name": "trusted full gate", "status": "unmeasured" },
    { "name": "reproduce round-two full-gate failures", "status": "done" },
    { "name": "correct response-contract documentation", "status": "done" },
    { "name": "correct legacy adoption description", "status": "done" },
    { "name": "prove round-two changed guards", "status": "done" },
    { "name": "rerun round-two focused selectors", "status": "done" },
    { "name": "reproduce round-three fence bypasses", "status": "done" },
    { "name": "correct cached prepare fence ordering", "status": "done" },
    { "name": "preserve committed fence across recapture", "status": "done" },
    { "name": "prove round-three changed guards", "status": "done" },
    { "name": "rerun round-three focused selectors", "status": "done" },
    { "name": "reproduce round-four full-gate failures", "status": "done" },
    { "name": "preserve durable cached replay", "status": "done" },
    { "name": "restore conflict-resolver preparation", "status": "done" },
    { "name": "correct conflict continuation stdin boundary", "status": "done" },
    { "name": "prove round-four changed guards", "status": "done" },
    { "name": "rerun round-four focused selectors", "status": "done" },
    { "name": "reproduce round-five backend wrapper fence bypass", "status": "done" },
    { "name": "reproduce round-five public prevalidation bypass", "status": "done" },
    { "name": "reproduce round-five backend matrix gap", "status": "done" },
    { "name": "enforce backend wrapper fence and lock", "status": "done" },
    { "name": "move public fence preflight before validation", "status": "done" },
    { "name": "add in-memory filesystem git-object sqlite matrix", "status": "done" },
    { "name": "prove round-five changed guards", "status": "done" },
    { "name": "rerun round-five focused selectors", "status": "done" },
    { "name": "reproduce round-six generic backend contract failure", "status": "done" },
    { "name": "separate manager-bound fence authority", "status": "done" },
    { "name": "prove round-six changed guard", "status": "done" },
    { "name": "rerun round-six focused selectors", "status": "done" },
    { "name": "reproduce round-seven trusted-authority bypass", "status": "done" },
    { "name": "establish Git-object fence contract", "status": "done" },
    { "name": "correct round-seven fence authority", "status": "done" },
    { "name": "prove round-seven changed guards", "status": "done" },
    { "name": "rerun round-seven focused selectors", "status": "done" },
    { "name": "record round-eight gate retry", "status": "done" },
    { "name": "reconfirm round-eight focused selectors", "status": "todo" },
    { "name": "verify round-eight clean tip", "status": "todo" }
  ]
}
```

## ground implementation surface <!-- cq:wip-checkpoint -->

The existing committed recovery seal validates a stabilized attestation snapshot and
persists journal authority, but no `DispatchLineageCutoverFence` type, durable fence
store, or prepare-time `journal-recovery-required` refusal exists.

## reproduce missing lineage fence <!-- cq:wip-checkpoint -->

The exact five-file acceptance selector exited 1 before correction because none of
the required fence test files existed. Bun searched 1106 files and reported that
all five filters matched no tests.

## implement cutover fence <!-- cq:wip-checkpoint -->

Committed recovery journals now embed an authenticated `journal-only` fence keyed
by namespace, task, managed fingerprint, and source attestation lineage. Direct
and legacy prepare paths check it under the shared managed-worktree effect lock;
only the exact recovery seed plus fence capability receives a max+1 reservation.
Guarded terminal worktree release removes the journal/fence.

## prove changed guards <!-- cq:wip-checkpoint -->

Six deliberate source mutations failed for the expected reason and passed after
byte-for-byte restoration: exact recovery seed authorization, pre-backend fence
refusal, max+1 reservation, committed-journal fence persistence, recovery-journal
seed authentication, and guarded-release journal removal. The restored source
SHA-256 digests were `8992b62199f8159c9f64ba6ff992e97b6bba99dfb28965860863a8639839a25f`,
`7937843d5342c551532a022f3b035d8c87a4f6a15d120c2333c94fefce23d353`,
`236c6d2474e4e6039a81607834e9d6f42a392d6937bb3d5cde5fd447140ea064`,
`8dffd9865e86e81e11d57c7f93edb1813a435fa44a776db1639f0367a7fa66b1`,
`af803650800c1fe304d43841fa295fe790ca2e07e0a00a2a985b109405323814`, and
`42a18ddd25216630d43bf78e5a997ed27c37e955999402cf42bbd73271c459ce`.

## run acceptance selectors <!-- cq:wip-checkpoint -->

The exact five-file non-PostgreSQL selector passes 16 tests; the focused
managed-worktree regression selector passes 33 tests, and `bun run typecheck`
passes. Required-live PostgreSQL mode was invoked without a DSN and failed
explicitly with the required `CQ_TEST_PG_URL` diagnostic; no live PostgreSQL
DSN is available in this child.

The round-one rerun again passes all 16 acceptance tests. Required-live
PostgreSQL mode again fails explicitly in both named files because this child
has no DSN. The corrected tree also passes `bun run typecheck` and
`bun run lint`.

## verify clean committed tip <!-- cq:wip-checkpoint -->

Commit `c4c41351470be1fd254e0970600d38041f30dd0f` was observed as a commit
object on `implement/T2816`, with an empty porcelain status and verified
ancestry from the full base SHA. The final WIP-closure commit is reverified
before result construction.

## correct tool-surface evidence regression <!-- cq:wip-checkpoint -->

The canonical normalized artifact now reflects the recovery-preparation schema.
Compact worktree and prepare descriptions retain the typed contract while the
largest full surface measures 11,781 tokens against the 11,782-token baseline;
all profiler budgets pass and the maximum G93 counterfactual is 1,237 tokens.

## correct continuation validation ordering <!-- cq:wip-checkpoint -->

Structural exclusivity for guarded rebase, legacy recovery, continuation, and
journal recovery is now checked before caller fingerprinting or managed
worktree resolution. The PATH-stripped mixed-continuation regression returns
the typed rejection without starting Git.

## restore closed-reason stage coverage <!-- cq:wip-checkpoint -->

The T976 exhaustive test assigns `journal-recovery-required` to the prepare
stage, restoring equality with the declared closed rejection-reason set.

## rerun focused regressions <!-- cq:wip-checkpoint -->

The exact three-file reproduction ran 68 tests: 65 passed and the three
trusted-gate regressions failed. The profiler reported a stale normalized
artifact and `allSurfacesSmaller=false`; the mixed continuation test attempted
`git rev-parse --show-toplevel` under a PATH with no Git; the T976 exhaustive
set lacked only `journal-recovery-required`. The current measured full surface
is 11,865 tokens against the 11,782-token historical baseline, so preserving
the strict shrink invariant requires at least 84 tokens of real surface
reduction before regenerating evidence.

After correction, the same three-file selector passes 68 tests. Deliberate
counterfactual mutations reproduced the Git `ENOENT`, the missing closed-set
member, and the profiler expectation mismatch; restoration produced passes and
identical SHA-256 hashes `e06e25bc725ed36ef2feef017ca8dc49a2ccce793faab90e38bc207c08f97383`,
`4430d001b44fd3815b536af12db60177424d7e838eea2d8f257e5de5c94262ea`,
and `62c44848da16d6e11939cc7305d3ea5f48c22b092406c91cd15e1c1054bc9e41`.

## trusted full gate <!-- cq:wip-checkpoint -->

The trusted full gate remains delegated to the result-storage boundary after a
clean, verified correction commit.

## reproduce round-five backend wrapper fence bypass <!-- cq:wip-checkpoint -->

The direct manager-bound wrapper regression resolved instead of rejecting when
called with only its clock and random source. It inserted one attestation row;
the new fail-closed expectation reported `Expected promise that rejects`.

## reproduce round-five public prevalidation bypass <!-- cq:wip-checkpoint -->

The production managed-worktree/journal fixture committed a fence, then an empty
idempotency key returned `invalid-launch-envelope` at `idempotencyKey` instead of
the expected `journal-recovery-required` fence refusal. The focused two-file run
reported 24 passes and these two expected regression failures.

## reproduce round-five backend matrix gap <!-- cq:wip-checkpoint -->

An exact five-file symbol inventory observed `InMemoryAttestationBackend=2`,
`FsAttestationBackend=0`, `GitObjectLedgerBackend=0`, and
`SqliteAttestationBackend=0`, confirming the requested adapter matrix is absent.

## enforce backend wrapper fence and lock <!-- cq:wip-checkpoint -->

Manager-bound `prepareDispatchOn` now rejects omitted fence/lock dependencies
before opening an attestation transaction. The public binding passes a real
journal lookup while its allocation/replay operation holds the matching managed
effect lock. Non-manager-bound roles retain the plain backend dependency shape.

## move public fence preflight before validation <!-- cq:wip-checkpoint -->

The public path resolves a registry-only lineage identity without consulting Git,
then checks the journal fence under the shared effect lock before validating
idempotency, timeout, or mutually exclusive continuation fields. Full mutable Git
binding validation remains after structural validation and before allocation.

## add in-memory filesystem git-object sqlite matrix <!-- cq:wip-checkpoint -->

One shared behavioral contract now runs allocation and fence refusal against the
in-memory, filesystem, and SQLite attestation adapters. The real Git-object
ledger backend is instantiated separately and its intentional pre-registration
attestation exclusion is asserted. The exact-file inventory is now 2/1/1/1 for
in-memory/filesystem/Git-object/SQLite, and the matrix file passes eight tests.

## prove round-five changed guards <!-- cq:wip-checkpoint -->

Four counterfactual mutations failed for the expected reasons: disabling the
manager-bound dependency check allowed allocation instead of rejection;
bypassing the early public preflight returned `invalid-launch-envelope`;
making the registry-only exact match reject its one match produced the same
ordering failure; and bypassing `withLineageLock` removed the observed
`lock-enter`/`lock-exit` events. Restored SHA-256 hashes are
`66407adc5f53674c6229af10fef6798afbbec8577f369132a8006e8b340099be`,
`ccdf806c3dabe0393a1f36b7531742a6a20bec87c14d109de1a3e30260a5a275`, and
`eaca16916067a7063c79d10ff09e36da08c4906a49a5f94e4fb516c8fad0dca5`.

## rerun round-five focused selectors <!-- cq:wip-checkpoint -->

The exact five-file acceptance selector passes 21 tests with 74 expectations.
The public/direct regression pair passes 26 tests; the attestation and operator
recovery compatibility selector passes 101 tests. `bun run typecheck` and
`bun run lint` pass. Required-live PostgreSQL mode was invoked and both named
files failed explicitly because `CQ_TEST_PG_URL` is absent. The trusted full
gate remains delegated to result storage.

## reproduce round-two full-gate failures <!-- cq:wip-checkpoint -->

The exact two-file selector exited 1 with 16 passes and the two reported
failures. The response-contract matrix retained stale compact response cells
for `prepare_dispatch` and `worktree_manage`; the worktree schema description
lacked the required literal `legacy` despite preserving the paired adoption
coordinates.

## correct response-contract documentation <!-- cq:wip-checkpoint -->

The exhaustive README matrix now uses the exact live response cells for
`prepare_dispatch` and `worktree_manage`; its compact allowlist and strict
equality validation remain unchanged.

## correct legacy adoption description <!-- cq:wip-checkpoint -->

The public worktree description now explicitly qualifies the paired prepare-only
adoption coordinates as `legacy`. Removing one redundant article kept the full
surface at 11,781 tokens against the 11,782-token frozen baseline.

## prove round-two changed guards <!-- cq:wip-checkpoint -->

Counterfactual reversion of the README cell and removal of `legacy` produced
the expected two failures with 16 unrelated passes. Restoring the intended
bytes returned SHA-256 hashes
`35f7ddd5a8958018e82db43591b9f0f4c0a387879d0961698230c0baccf6cfa5`
and `2312d640725ded8a3a9b0ee0f78b70f349373c6d6d0c1423124dc7cc9551fb9e`.

## rerun round-two focused selectors <!-- cq:wip-checkpoint -->

The response documentation, worktree manager, and tool-surface selector passes
20 tests with 351 expectations. The regenerated normalized artifact has SHA-256
`d6928eeb7c856c55c4ed1da8089a98969b8b17ea895c2273b43e7ff6d1fe0dc2`.
The exact five-file fence acceptance selector also passes all 16 tests. The
required-live PostgreSQL selector was invoked with `CQ_TEST_REQUIRE_PG=1` and
failed explicitly in both files because no `CQ_TEST_PG_URL` DSN is available.
The trusted full gate remains delegated to result storage.

## reproduce round-three fence bypasses <!-- cq:wip-checkpoint -->

The two production-level regressions ran 33 tests: 31 passed and both new
tests failed for the cited mechanisms. Same-key refs replay returned the
previously accepted handle and raw capabilities instead of the committed
fence refusal. Successor recapture replaced the committed journal with
provisional state and reached the simulated crash hook instead of rejecting
the demotion with `journal-conflict`.

## correct cached prepare fence ordering <!-- cq:wip-checkpoint -->

Refs and inline managed coordinates are now resolved before task validation.
Under the managed lineage lock, the binding is revalidated, the durable fence
is checked, and only an unfenced request may replay a cached prepare. The final
cache/allocation operation repeats binding and fence checks under the same lock.

## preserve committed fence across recapture <!-- cq:wip-checkpoint -->

Journal transitions now permit provisional replacement only while the current
journal is also provisional. Once committed fence authority exists, successor
recapture cannot demote it before validation or another durability boundary.

## prove round-three changed guards <!-- cq:wip-checkpoint -->

Disabling refs-coordinate preflight reproduced cached acceptance with raw
capabilities instead of `journal-recovery-required`; restoration passed the
single regression and returned `dispatchCapability.ts` to SHA-256
`ec88e723260c765d4c5a42e850aa92d6c0cc072174ae2adcb88c15d0839e73ce`.
Re-enabling committed-to-provisional replacement reproduced the simulated
crash after fence demotion; restoration passed the single regression and
returned `currentRecoverySeal.ts` to SHA-256
`eba978098a4d1f5c4468f77bdb00b19901c45e7b8560b882b8e8c1104c6c7eb4`.

## rerun round-three focused selectors <!-- cq:wip-checkpoint -->

The two corrected production test files pass 33 tests with 140 expectations.
The exact five-file fence acceptance selector passes 16 tests with 55
expectations. `bun run typecheck` and `bun run lint` pass. Required-live
PostgreSQL mode was invoked for both named files and failed explicitly because
no `CQ_TEST_PG_URL` DSN is available. The trusted full gate remains delegated
to result storage.

## reproduce round-four full-gate failures <!-- cq:wip-checkpoint -->

The exact two-file selector exited 1 with 13 passes and the two reported
failures. The accepted generation-two continuation replay instead returned
`invalid-launch-envelope` because its mutable manager handle had disappeared.
The conflict-resolver fixture prepared without `gitConflictCapability`, causing
the test's pre-launch contract check to throw.

## preserve durable cached replay <!-- cq:wip-checkpoint -->

Cached worker replay now resolves its Git binding from the durable attestation
row. That binding supplies the lineage lock and fence identity when the live
manager registry is unavailable, so the exact replay succeeds without moving
the fence check behind the cache.

## restore conflict-resolver preparation <!-- cq:wip-checkpoint -->

Early coordinate preflight is restricted to implement-worker requests, and
locked resolver revalidation retains detached-rebase admission. The resolver
fixture now prepares with `gitConflictCapability`; its test proceeds into the
authorized continuation path.

## correct conflict continuation stdin boundary <!-- cq:wip-checkpoint -->

The restored resolver contract exposed a masked Bun 1.3.13 compatibility
failure: `node:child_process` discarded the supplied `Uint8Array`, so Git
hashed the empty blob. The conflict broker now uses the repository's established
`Bun.spawn` byte-input pattern, preserving the hermetic index and quarantine.

## prove round-four changed guards <!-- cq:wip-checkpoint -->

Three deliberate counterfactual mutations failed for the expected reasons and
passed after restoration: removing the durable binding fallback restored the
worker's `invalid-launch-envelope`; disabling detached resolver revalidation
removed `gitConflictCapability`; dropping broker stdin restored the empty-blob
continuation failure. Restored SHA-256 digests are
`78a80e59464f0143c8e86acb88139900fa68239e5f04656afda3cd489c342702`
and `89817dad86aeef0004bfd6f3757782f6a4c1bef4a28e043889e6facf2bacc681`.

## rerun round-four focused selectors <!-- cq:wip-checkpoint -->

The complete affected three-file selector passes 22 tests with 184
expectations. The exact five-file fence acceptance selector passes 16 tests
with 55 expectations. `bun run typecheck` and `bun run lint` pass. The
required-live PostgreSQL selector was invoked for both named files and failed
explicitly because no `CQ_TEST_PG_URL` DSN is available. The trusted full gate
remains delegated to result storage.

## reproduce round-six generic backend contract failure <!-- cq:wip-checkpoint -->

The unchanged in-memory contract selector for `parent-owned gate staging,
reclaim, stale-epoch refusal, and exact replay survive restarts` exited 1 with
zero passes and the expected `AttestationContractError: deps: manager-bound
prepare requires a lineage fence guard and lock` from `prepareDispatchOn`.

## separate manager-bound fence authority <!-- cq:wip-checkpoint -->

`prepareDispatchOn` now requires an explicit `backend` or `manager-bound`
dependency mode. Ordinary store contracts use backend mode even when they
persist a Git-effect binding; the production wrapper and fence-aware paths use
manager-bound mode, which still requires the fence guard and shared lock. Both
reported in-memory contract selectors now pass, and TypeScript accepts every
updated call site.

## prove round-six changed guard <!-- cq:wip-checkpoint -->

Setting the mode-derived `managerBound` guard to false made the direct
fail-closed regression resolve after allocating instead of rejecting; the
selector exited 1 with `Expected promise that rejects`. Restoring the intended
expression passed the regression and returned `dispatchAttestationBackend.ts`
to SHA-256
`afe4ccfe46d2104f33a9f2e4e5dea12f2d47e74ea5de21879623d6257c2cc1db`.

## rerun round-six focused selectors <!-- cq:wip-checkpoint -->

The complete in-memory, filesystem, and SQLite/XDG AttestationStore contracts
pass 150 tests with 1,066 expectations. The exact five-file T2816 selector
passes 21 tests with 74 expectations; `bun run typecheck` and `bun run lint`
pass. Required-live PostgreSQL mode was invoked and both named files failed
explicitly because `CQ_TEST_PG_URL` is absent. The trusted full gate remains
delegated to result storage.

## reproduce round-seven trusted-authority bypass <!-- cq:wip-checkpoint -->

The committed manager-resolved `gitEffectBinding` plus caller-selected backend-mode
counterexample ran nine tests: eight passed and the regression resolved instead of
rejecting, then reported `Expected promise that rejects`. The accepted operation
inserted a row, reproducing the allocation bypass before any correction. The test
file changed from SHA-256
`e59a6fb5ec93e2e907cfd2d00ee237b6401b56dd7505c005c86df8250030ea4e` to
`d3f780e834bc860621c767cb38eb250d20c08c3fb37e9321ae64dbda42c234b4`;
the uncorrected authority boundary is
`afe4ccfe46d2104f33a9f2e4e5dea12f2d47e74ea5de21879623d6257c2cc1db`.

## establish Git-object fence contract <!-- cq:wip-checkpoint -->

The production Git-object ledger has whole-ledger orphan-ref CAS, but no dispatch
attestation adapter: its construction guard rejects registration because the
attestation transaction requires row-level compare-and-set and a matching
cross-process write lock. The selector now calls this an explicit matrix
incompatibility rather than behavioral fence coverage. Implementing a Git-object
attestation adapter would change the production backend contract and is outside
this correction; the stated four-adapter fence matrix therefore remains
incompatible with the declared production support set.

## correct round-seven fence authority <!-- cq:wip-checkpoint -->

`prepareDispatchOn` now treats a request carrying the documented manager-resolved
`gitEffectBinding` as manager-bound regardless of caller-selected dependency mode.
Contradictory backend dependencies are rejected before the backend transaction;
valid manager dependencies still run the fence guard under the lineage lock. The
generic AttestationStore contract harness supplies an identity manager boundary
for its bound restart/recovery fixtures, preserving all 150 in-memory,
filesystem, and SQLite/XDG contract tests.

## prove round-seven changed guards <!-- cq:wip-checkpoint -->

Disabling the trusted-request discriminator made the regression allocate and fail
with `Expected promise that rejects` (zero passes, one failure). Restoring the
intended expression passed the regression and returned
`dispatchAttestationBackend.ts` byte-for-byte to SHA-256
`81af9a08be97205ecc6d5caee49bd98f0f5e37caee3ff027ac2a5d9bdbeaba30`.

## rerun round-seven focused selectors <!-- cq:wip-checkpoint -->

The exact five-file T2816 selector passes 22 tests with 75 expectations. The
complete in-memory, filesystem, and SQLite/XDG AttestationStore contracts pass
150 tests with 1,066 expectations, and the production dispatch-capability file
passes 22 tests with 115 expectations. `bun run typecheck` and `bun run lint`
pass. Required-live PostgreSQL mode was invoked and both named files failed
explicitly because `CQ_TEST_PG_URL` is absent, so no live PostgreSQL behavior was
observed. The trusted full gate remains delegated to result storage.

## record round-eight gate retry <!-- cq:wip-checkpoint -->

The inherited round-eight criticism reports one load-sensitive T1625 orchestration
timeout in the parent full gate and an immediate isolated pass. No T2816 source
correction is indicated. This round will preserve the committed authority correction,
reconfirm the focused T2816 selectors, and delegate one canonical full-gate retry to
the trusted result-storage boundary.

## reconfirm round-eight focused selectors <!-- cq:wip-checkpoint -->

Pending this round's exact focused selector observations.

## verify round-eight clean tip <!-- cq:wip-checkpoint -->

Pending final commit-object, clean-tree, and ancestry verification.
