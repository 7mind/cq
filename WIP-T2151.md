```json
{
  "taskId": "T2151",
  "role": "implement-worker",
  "baseCommit": "65e9f8a813c767e43127593d240471b401041e17",
  "startedAt": "2026-08-19T02:40:00Z",
  "checkpoints": [
    {
      "name": "step0-base-verification",
      "status": "done"
    },
    {
      "name": "reproduce-flow-gaps",
      "status": "todo"
    },
    {
      "name": "canonical-advance-flow-wiring",
      "status": "todo"
    },
    {
      "surface-fragment-wiring",
      "status": "todo"
    },
    {
      "cli-help-examples",
      "status": "todo"
    },
    {
      "installed-broker-fixture-lineage-arm",
      "status": "todo"
    },
    {
      "packaged-e2e-and-correction-controls",
      "status": "todo"
    },
    {
      "regenerate-catalogues",
      "status": "todo"
    },
    {
      "mutation-proofs",
      "status": "todo"
    },
    {
      "acceptance-selectors-green",
      "status": "todo"
    },
    {
      "full-gate-green",
      "status": "todo"
    }
  ]
}
```

## step0-base-verification <!-- cq:wip-checkpoint -->

VERIFIED: worktree
`/home/pavel/work/safe/flakes/cq/.claude/worktrees/01a017c0-f1ac-7cc1-b4c3-781f3b33db73`,
branch `implement/T2151`, HEAD == startingCommit == baseCommit
`65e9f8a813c767e43127593d240471b401041e17` (relation: equal), clean tree,
round 0 (no priorResultCommit). Legacy dispatch: no gitChangeCapability;
in-child full gate at the end.

## reproduce-flow-gaps <!-- cq:wip-checkpoint -->

Gap hypothesis: the rendered implement/advance artifacts (claude/codex/pi)
run the rebase WITHOUT `--operation-id` (no journal, no reference minted),
never capture `CQ_GUARDED_REBASE_REFERENCE`, and never pass
`guardedRebase`/`reprepareOf` on the post-rebase redispatch — so the T2150
bridge is unreachable from every flow surface. Reproduce by adding the
conformance pins first and observing them fail on the current source.

## canonical-advance-flow-wiring <!-- cq:wip-checkpoint -->

Wire `nix/pkg/cq-assets/commands/cq/implement/advance.md` §7: stable
operation id, machine-readable reference capture, exact reference retention,
redispatch contract (reference + exact reprepareOf only; server materializes
the lineage), coordinate reset (baseCommit=ontoCommit,
startingCommit=rebasedStartCommit, priorResultCommit=exact pre-rebase result
on the initial bridge round), server-resolved exact-tip/no-new-commit mode,
result consumed only after the parent gate, fresh reviewers, existing ff-only
guarded merge, fail-closed on missing/malformed/mismatched handoff.

## surface-fragment-wiring <!-- cq:wip-checkpoint -->

Codex `implement-dispatch-workflow.md`: brokered redispatch mechanics
(prepare carries reprepareOf + guardedRebase; caller never writes
guardedRebaseLineage; consumed result must echo gitLineage exactly; fresh
supervisedGateEvidence required). Claude/Pi: same canonical contract; a
prepare refusal stops the continuation closed (no raw-Git fallback, no
caller-minted lineage, no reuse of pre-rebase authority).

## cli-help-examples <!-- cq:wip-checkpoint -->

CLI help/examples for the guarded rebase arm; gateRun.test.ts parse pins for
`--operation-id` (round-trip; merge+operation-id rejected; malformed
rejected).

## installed-broker-fixture-lineage-arm <!-- cq:wip-checkpoint -->

Extend `packages/ledger-mcp/test/fixtures/codexBrokerWorker.ts`: on
`guardedRebaseLineage` input verify the bindings (baseCommit==ontoCommit,
startingCommit==rebasedStartCommit, priorResultCommit==oldResultCommit on the
initial bridge round); exact-tip mode makes no git_commit call, writes no WIP,
reports resultCommit==rebasedStartCommit with an empty suffix and exact
gitLineage; correction mode keeps early persistence and a non-empty
contiguous suffix.

## packaged-e2e-and-correction-controls <!-- cq:wip-checkpoint -->

New installedGateTest: terminal worker → guarded rebase via the installed CLI
(reference captured from stdout) → broker + parent-runner restart →
no-new-commit worker result at the exact rebased tip → parent-owned gate →
fresh review → ff-only merge via the installed CLI; paired correction control
advances the rebased tip with early persistence and a non-empty suffix.
Negative controls: dropped reference, changed operation id/payload,
substituted lineage coordinates, unbound prior result exemption, no-new
result outside exact-tip mode, skipped mutation evidence.

## regenerate-catalogues <!-- cq:wip-checkpoint -->

`bun run gen-prompt-catalog && bun run gen-agents` — byte-fresh catalogues
after source edits.

## mutation-proofs <!-- cq:wip-checkpoint -->

Each added/changed guard: make it fail, capture, restore intended bytes
(sha256-verified), capture pass. Record the table in the result
`mutationTable` and here.

## acceptance-selectors-green <!-- cq:wip-checkpoint -->

`bun run gen-prompt-catalog && bun run gen-agents && bun test
packages/cq-config/test/workset-git-command-surfaces.test.ts
packages/cq-config/test/promptSourceHygiene.test.ts
packages/cq-config/test/crossSurfaceDispatchConformance.test.ts
packages/ledger-mcp/test/packagedCodexRoleGitBroker.test.ts
packages/cq-cli/test/gateRun.test.ts` exits zero.

## full-gate-green <!-- cq:wip-checkpoint -->

`cq gate run --worktree "$PWD" --command-cwd "$PWD/nix/pkg/cq-ledgers" -- bun run check`
exits zero; REAL_CHECK_EXIT=0 preserved with the verbatim tail and
gateDurationMs.
