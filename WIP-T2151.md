```json
{
  "taskId": "T2151",
  "role": "implement-worker",
  "baseCommit": "65e9f8a813c767e43127593d240471b401041e17",
  "startedAt": "2026-08-19T02:40:00Z",
  "checkpoints": [
    {
      "name": "step0-base-verification",
      "status": "done"
    },
    {
      "name": "reproduce-flow-gaps",
      "status": "done"
    },
    {
      "name": "canonical-advance-flow-wiring",
      "status": "done"
    },
    {
      "surface-fragment-wiring",
      "status": "todo"
    },
    {
      "cli-help-examples",
      "status": "todo"
    },
    {
      "installed-broker-fixture-lineage-arm",
      "status": "todo"
    },
    {
      "packaged-e2e-and-correction-controls",
      "status": "todo"
    },
    {
      "regenerate-catalogues",
      "status": "todo"
    },
    {
      "mutation-proofs",
      "status": "todo"
    },
    {
      "acceptance-selectors-green",
      "status": "todo"
    },
    {
      "full-gate-green",
      "status": "todo"
    }
  ]
}
```

## step0-base-verification <!-- cq:wip-checkpoint -->

VERIFIED: worktree
`/home/pavel/work/safe/flakes/cq/.claude/worktrees/01a017c0-f1ac-7cc1-b4c3-781f3b33db73`,
branch `implement/T2151`, HEAD == startingCommit == baseCommit
`65e9f8a813c767e43127593d240471b401041e17` (relation: equal), clean tree,
round 0 (no priorResultCommit). Legacy dispatch: no gitChangeCapability;
in-child full gate at the end.

## reproduce-flow-gaps <!-- cq:wip-checkpoint -->

REPRODUCED (commit 92767acc): the new conformance pins fail on the unwired
source with exactly the documented violation set — every rendered
implement/advance artifact (claude/codex/pi) reports missing-operation-id,
missing-reference-capture, missing-reprepare-binding,
missing-server-materialization, missing-coordinate-reset,
missing-ancestry-exemption-limit, missing-exact-tip-mode,
missing-correction-persistence, missing-fresh-gate-and-review,
missing-authority-reset, missing-fail-closed; the codex brokered-redispatch
mechanics pin fails; the catalogue capture pin fails. 34 sibling tests pass —
the failure is the missing-reference/missing-operation-id flow gap, not a
harness fault.

Gap hypothesis: the rendered implement/advance artifacts (claude/codex/pi)
run the rebase WITHOUT `--operation-id` (no journal, no reference minted),
never capture `CQ_GUARDED_REBASE_REFERENCE`, and never pass
`guardedRebase`/`reprepareOf` on the post-rebase redispatch — so the T2150
bridge is unreachable from every flow surface. Reproduce by adding the
conformance pins first and observing them fail on the current source.

## canonical-advance-flow-wiring <!-- cq:wip-checkpoint -->

DONE: `commands/cq/implement/advance.md` §7 — Guarded rebase authority block:
stable operation id + verbatim reuse on lost response, machine-readable
`CQ_GUARDED_REBASE_REFERENCE=cq-guarded-rebase:v1:<64 lowercase hex>` capture
and sole-authority retention, redispatch bound by `reprepareOf` + exact
`guardedRebase` reference only (server materializes `guardedRebaseLineage`;
caller-supplied lineage always rejected), coordinate reset
(baseCommit=ontoCommit, startingCommit=rebased tip, priorResultCommit=exact
pre-rebase result on the initial bridge round), ancestry-exemption limit,
server-resolved exact-tip/no-new-commit mode vs correction persistence,
consume-only-after-parent-gate + fresh reviewers + ff-only merge, fail-closed
on missing/malformed/mismatched handoff. Conflict arm replays the identical
command to reconcile the journal before redispatch.

Wire `nix/pkg/cq-assets/commands/cq/implement/advance.md` §7: stable
operation id, machine-readable reference capture, exact reference retention,
redispatch contract (reference + exact reprepareOf only; server materializes
the lineage), coordinate reset (baseCommit=ontoCommit,
startingCommit=rebasedStartCommit, priorResultCommit=exact pre-rebase result
on the initial bridge round), server-resolved exact-tip/no-new-commit mode,
result consumed only after the parent gate, fresh reviewers, existing ff-only
guarded merge, fail-closed on missing/malformed/mismatched handoff.

## surface-fragment-wiring <!-- cq:wip-checkpoint -->

DONE: codex `implement-dispatch-workflow.md` gains the Guarded-rebase
redispatch paragraph (prepare names reprepareOf + guardedRebase; never place
guardedRebaseLineage or journal coordinates in caller input; accept a consumed
guarded result only with exact gitLineage echo, fresh suffix, diff-derived
filesTouched, fresh runner-owned supervisedGateEvidence). Claude/Pi inherit
the canonical contract; their prepare boundary rejects the reference and the
canonical fail-closed stop applies.

Codex `implement-dispatch-workflow.md`: brokered redispatch mechanics
(prepare carries reprepareOf + guardedRebase; caller never writes
guardedRebaseLineage; consumed result must echo gitLineage exactly; fresh
supervisedGateEvidence required). Claude/Pi: same canonical contract; a
prepare refusal stops the continuation closed (no raw-Git fallback, no
caller-minted lineage, no reuse of pre-rebase authority).

## cli-help-examples <!-- cq:wip-checkpoint -->

DONE: `cq --help` git-effect text now states the consumption contract
(reference retained as sole rebase authority; passed verbatim with the exact
reprepareOf handle; the server materializes the lineage). gateRun.test.ts
gains three pins: operation-id round-trips into the typed rebase request; the
trusted runner rejects merge+operation-id and a malformed operation id before
any store access.

CLI help/examples for the guarded rebase arm; gateRun.test.ts parse pins for
`--operation-id` (round-trip; merge+operation-id rejected; malformed
rejected).

## installed-broker-fixture-lineage-arm <!-- cq:wip-checkpoint -->

DONE: `codexBrokerWorker.ts` — closed-shape lineage validation
(baseCommit==ontoCommit, startingCommit==rebasedStartCommit, HEAD==rebased
head, inherited suffix begins at the rebased head), exact-tip mode (bridge
round binds priorResultCommit==oldResultCommit; NO WIP write, NO git_commit
call, empty suffix, exact rebased tip, diff-derived filesTouched), correction
mode (early WIP skeleton commit first, non-empty contiguous suffix, test-path
change carrying mutationTable). Installed-instruction pin extended to require
the exact-tip exemption + gitLineage contract in the packaged role body.
directGit probe factored and reused by both arms.

Extend `packages/ledger-mcp/test/fixtures/codexBrokerWorker.ts`: on
`guardedRebaseLineage` input verify the bindings (baseCommit==ontoCommit,
startingCommit==rebasedStartCommit, priorResultCommit==oldResultCommit on the
initial bridge round); exact-tip mode makes no git_commit call, writes no WIP,
reports resultCommit==rebasedStartCommit with an empty suffix and exact
gitLineage; correction mode keeps early persistence and a non-empty
contiguous suffix.

## packaged-e2e-and-correction-controls <!-- cq:wip-checkpoint -->

DONE (installed, in the real nix build): new installedGateTest executes the
full chain — terminal worker (installed boundary) → advance main → guarded
rebase via the installed CLI with exactly one CQ_GUARDED_REBASE_REFERENCE line
captured → exact replay returns the same reference without re-running the
effect / changed payload under the same id rejects → broker + parent-runner
restart → prepare rejections (dropped reference at input.startingCommit,
substituted base at input.baseCommit, unbound prior result at
input.priorResultCommit, caller-minted lineage at input.guardedRebaseLineage)
→ exact-tip bridge round (no WIP, no git_commit, empty suffix,
resultCommit==rebased tip, exact gitLineage echo, diff-derived filesTouched)
→ parent-owned gate (runner-owned supervisedGateEvidence bound to the rebased
tip) → fresh sandboxed review → paired correction (early WIP commit first,
non-empty contiguous suffix, test-path change with mutationTable) → fresh
non-sandboxed review re-running the gate → ff-only merge via the installed
CLI; main lands at the correction tip; worktree clean. Gate-run counter
proves one fresh parent-owned gate per round (plus the reviewer re-run).
Build evidence: /nix/store/z3lfh3w6dj7cnlr8ndv6v73s1aglvl9j-cq-0.0.1
installCheck, 20 pass / 0 fail (e2e 19.5s).

New installedGateTest: terminal worker → guarded rebase via the installed CLI
(reference captured from stdout) → broker + parent-runner restart →
no-new-commit worker result at the exact rebased tip → parent-owned gate →
fresh review → ff-only merge via the installed CLI; paired correction control
advances the rebased tip with early persistence and a non-empty suffix.
Negative controls: dropped reference, changed operation id/payload,
substituted lineage coordinates, unbound prior result exemption, no-new
result outside exact-tip mode, skipped mutation evidence.

## regenerate-catalogues <!-- cq:wip-checkpoint -->

DONE (first pass): `bun run gen-prompt-catalog && bun run gen-agents`;
agentsCatalogue.gen.ts embeds the new flow text; promptCatalog.gen.ts is
content-independent of command bodies (unchanged). Will re-run after any
further source edits.

`bun run gen-prompt-catalog && bun run gen-agents` — byte-fresh catalogues
after source edits.

## mutation-proofs <!-- cq:wip-checkpoint -->

DONE (each: hash before → mutate → expected failure captured → byte-restore →
hash verified → pass captured):
1. advance.md drop ` --operation-id <stableRebaseOperationId>` →
   crossSurface T2151 suite + workset-git suite fail with
   missing-operation-id (2 tests failed).
2. advance.md substitute the reference-capture token →
   missing-reference-capture (2 tests failed).
3. codex fragment drop the reprepareOf/guardedRebase binding sentence →
   "T2151 pins the codex brokered redispatch mechanics" fails (1 fail).
4. implement-worker.md rename rebasedStartCommit → same pin fails (1 fail).
5. gateGitEffect.ts drop merge+operation-id rejection → "rejects a merge
   carrying an operation id" fails (12 pass / 1 fail).
6. gateGitEffect.ts drop OPERATION_ID pattern check → "rejects a malformed
   guarded-rebase operation id" fails (12 pass / 1 fail).
7. gateGitEffect.ts print CQ_GUARDED_REBASE= instead of
   CQ_GUARDED_REBASE_REFERENCE= (probe commit 59835338, reverted in 4f60c56b)
   → installed packaged e2e fails at `expect(referenceLines).toHaveLength(1)`
   with 0 (nix build installCheck). Revert build reproduces the green state
   (identical output path z3lfh3w6...).
Restore hashes: advance.md 2f80b52f…, codex fragment 16403c3d…,
implement-worker.md 15a1f15d…, gateGitEffect.ts e7afb98f… — all verified
byte-identical after restore.

Each added/changed guard: make it fail, capture, restore intended bytes
(sha256-verified), capture pass. Record the table in the result
`mutationTable` and here.

## acceptance-selectors-green <!-- cq:wip-checkpoint -->

`bun run gen-prompt-catalog && bun run gen-agents && bun test
packages/cq-config/test/workset-git-command-surfaces.test.ts
packages/cq-config/test/promptSourceHygiene.test.ts
packages/cq-config/test/crossSurfaceDispatchConformance.test.ts
packages/ledger-mcp/test/packagedCodexRoleGitBroker.test.ts
packages/cq-cli/test/gateRun.test.ts` exits zero.

## full-gate-green <!-- cq:wip-checkpoint -->

`cq gate run --worktree "$PWD" --command-cwd "$PWD/nix/pkg/cq-ledgers" -- bun run check`
exits zero; REAL_CHECK_EXIT=0 preserved with the verbatim tail and
gateDurationMs.
