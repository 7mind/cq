```json
{
  "taskId": "T2230",
  "role": "implement-worker",
  "baseCommit": "271d9e6e6230012784e7667b6b02ee76b33cc94e",
  "startedAt": "2026-08-18T13:57:40.000Z",
  "checkpoints": [
    { "name": "Step 0 base verification", "status": "done" },
    { "name": "Behavior-preserving settlement seam", "status": "todo" },
    { "name": "Four D342 plain controls fail-first", "status": "todo" },
    { "name": "§6a form (a) live marker phase", "status": "todo" },
    { "name": "All-settled cleanup correction", "status": "todo" },
    { "name": "Marker teardown to plain controls", "status": "todo" },
    { "name": "Mutation evidence", "status": "todo" },
    { "name": "Full gate", "status": "todo" }
  ]
}
```
## Step 0 base verification <!-- cq:wip-checkpoint -->
Verified 2026-08-18: toplevel `/home/pavel/work/safe/flakes/cq/.claude/worktrees/01a0150a-fa75-7c50-8cc5-554d67859ce2`,
branch `implement/T2230`, HEAD == startingCommit == baseCommit `271d9e6e6230012784e7667b6b02ee76b33cc94e`
(`git cat-file -t` = commit, `merge-base --is-ancestor` exit 0, relation equal), clean tree.

## Behavior-preserving settlement seam <!-- cq:wip-checkpoint -->
Planned: `createNodeSupervisedWorkerGateRunner(settlement)` exported from `@cq/ledger`; production
singleton explicitly supplies real `settleWorktreeGateCommands` + `settleProcessGroups`. No behavior change.

## Four D342 plain controls fail-first <!-- cq:wip-checkpoint -->
Planned scenarios (15,000 ms admission / 5,000 ms execution / 90,000 ms marker / 300,000 ms per-test):
worktree-only rejection; root-only rejection; direct-root survivors retaining identifier list;
both arms rejecting with both diagnostics. Fail-first evidence pending.

## §6a form (a) live marker phase <!-- cq:wip-checkpoint -->
Pending: annotations `// expected-failure: defects:D342`, four inventory entries, policy expectation 6→10.

## All-settled cleanup correction <!-- cq:wip-checkpoint -->
Pending: unconditional-finally settlement of both arms, all-settled capture, bounded diagnostics,
original deadline error retained as `cause`.

## Marker teardown to plain controls <!-- cq:wip-checkpoint -->
Pending: same-titled plain tests, annotations/entries/expectation removed, policy back to six tasks:T826.

## Mutation evidence <!-- cq:wip-checkpoint -->
Pending: short-circuit-arm / dropped-cause / dropped-survivor mutations observed failing, restored.

## Full gate <!-- cq:wip-checkpoint -->
Pending: `cq gate run --worktree "$PWD" --command-cwd "$PWD/nix/pkg/cq-ledgers" -- bun run check`.
