```json
{
  "taskId": "T2230",
  "role": "implement-worker",
  "baseCommit": "271d9e6e6230012784e7667b6b02ee76b33cc94e",
  "startedAt": "2026-08-18T13:57:40.000Z",
  "checkpoints": [
    { "name": "Step 0 base verification", "status": "done" },
    { "name": "Behavior-preserving settlement seam", "status": "done" },
    { "name": "Four D342 plain controls fail-first", "status": "done" },
    { "name": "§6a form (a) live marker phase", "status": "done" },
    { "name": "All-settled cleanup correction", "status": "done" },
    { "name": "Marker teardown to plain controls", "status": "done" },
    { "name": "Mutation evidence", "status": "done" },
    { "name": "Full gate", "status": "todo" }
  ]
}
```
## Step 0 base verification <!-- cq:wip-checkpoint -->
Verified 2026-08-18: toplevel `/home/pavel/work/safe/flakes/cq/.claude/worktrees/01a0150a-fa75-7c50-8cc5-554d67859ce2`,
branch `implement/T2230`, HEAD == startingCommit == baseCommit `271d9e6e6230012784e7667b6b02ee76b33cc94e`
(`git cat-file -t` = commit, `merge-base --is-ancestor` exit 0, relation equal), clean tree.

## Behavior-preserving settlement seam <!-- cq:wip-checkpoint -->
Done: `createNodeSupervisedWorkerGateRunner(settlement)` exported from `@cq/ledger`; production
singleton explicitly supplies real `settleWorktreeGateCommands` + `settleProcessGroups`.
`runAdmittedNodeSupervisedWorkerGate` calls the injected arms; control flow unchanged.
Real helpers + seam types re-exported via `@cq/ledger` barrel (ledger-mcp has no process-control dep).
tsc -b clean; existing tests unchanged.

## Four D342 plain controls fail-first <!-- cq:wip-checkpoint -->
`bun test packages/ledger-mcp/test/supervisedWorkerGateStorage.test.ts -t 'D342'` (plain, pre-fix):
0 pass / 4 fail, each ~5.1 s (≪ 300,000 ms), no Bun/marker/admission timeout, no launch failure.
Named final assertion mismatches observed:
1. worktree-only rejection — `expect(rootArm.calls).toBe(1)` received 0 (root cleanup skipped).
2. root-only rejection — message is raw "D342 injected root-arm settlement rejection";
   no "registered-root settlement rejected" diagnostic, deadline cause lost.
3. direct-root survivors — message is bare "supervised worker gate exceeded its host execution
   deadline"; no "registered-root survivors:" identifier list.
4. both arms rejecting — `expect(rootArm.calls).toBe(1)` received 0 (second diagnostic suppressed).
No leaked `sleep 86400`/bootstrap processes after the run (pgrep clean).

## §6a form (a) live marker phase <!-- cq:wip-checkpoint -->
Done: four `test.failing` with immediately preceding `// expected-failure: defects:D342`;
four inventory entries (file `…/ledger-mcp/test/supervisedWorkerGateStorage.test.ts`);
policy expectation temporarily raised 6→10 (`4× defects:D342` then `6× tasks:T826`, scan order).
Guard probe: with expectation still at six the policy test failed `toHaveLength(6)` receiving 10.
Live-marker focused runs: four inverted controls pass; policy suites pass (10 markers).

## All-settled cleanup correction <!-- cq:wip-checkpoint -->
Done in `runAdmittedNodeSupervisedWorkerGate`: the race outcome is captured (original error retained),
both settlement arms run exactly once in the unconditional finally via `captureSettlementArm`
(all-settled: one arm's rejection/survivors never suppress the other). On a gate error with
diagnostics, the run throws `supervised worker gate cleanup failed: <bounded diagnostics>` with the
original deadline error as `cause`; with clean settlement the original error is rethrown unchanged.
Success path fails closed (`left an unsettled process group`) on any arm rejection, signaled
worktree group, or survivor. Bounded diagnostics: per-arm message limit 200 chars, survivor list
limit 8. Post-fix focused run: four same-titled plain controls pass (66 expects; each ~5.1 s).
Full file 16/16; ledger package 3332 pass / 0 fail; policy suites back to exactly six tasks:T826
markers (inventory + policy test byte-identical to base `271d9e6e`).

## Marker teardown to plain controls <!-- cq:wip-checkpoint -->
Done: same-titled plain tests restored; all four annotations, inventory entries, and the
temporary ten-marker expectation removed (`git diff` vs base for both policy files is empty).

## Mutation evidence <!-- cq:wip-checkpoint -->
sha256 before mutations: supervisedWorkerGate.ts 64d3491855a8…, supervisedWorkerGateStorage.test.ts
2fd5b949460f…; identical after every restoration (git status clean).
- M1 short-circuited root arm when the worktree arm rejected: scenarios 1+4 failed
  `rootArm.calls` expected 1 received 0; scenarios 2+3 still passed. Restored.
- M2 dropped `{ cause: originalError }`: all four failed `expect(cause).toBeInstanceOf(Error)`. Restored.
- M3 dropped the `registered-root survivors:` diagnostic: scenario 3 failed
  `toContain("registered-root survivors:")` receiving the bare deadline message. Restored.
- Policy guard: six-marker expectation against ten live markers failed `toHaveLength(6)` receiving 10
  (observed during the live marker phase); ten-expectation passed; six-expectation green after teardown.
- Fail-first phase (the defect itself as mutant): four named mismatches recorded above.
Post-restore focused run: 4 pass / 0 fail, 66 expects.

## Full gate <!-- cq:wip-checkpoint -->
Pending: `cq gate run --worktree "$PWD" --command-cwd "$PWD/nix/pkg/cq-ledgers" -- bun run check`.
