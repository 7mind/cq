```json
{
  "taskId": "T2299",
  "role": "implement-worker",
  "baseCommit": "53288556933229a64b161e002f8737f78fec624a",
  "startedAt": "2026-08-21T12:47:00Z",
  "checkpoints": [
    { "name": "fail-first reproduction", "status": "done" },
    { "name": "shared WIP closure policy", "status": "done" },
    { "name": "terminal release authority", "status": "done" },
    { "name": "adapter and MCP acceptance", "status": "done" },
    { "name": "targeted and full checks", "status": "unmeasured" }
  ]
}
```

## fail-first reproduction <!-- cq:wip-checkpoint -->

Observed the real supervised result-storage path attach green runner-owned evidence at an
immutable managed tip. The subsequent release refused with `status: refused`,
`reason: wip-open`, and `openCheckpoints: [trusted full gate]` (1 test, 1 expected failure).

## shared WIP closure policy <!-- cq:wip-checkpoint -->

Implemented one virtual-closure policy shared by guarded merge and release. Runner evidence is
stored out of tree against the exact task, handle token/fingerprint, repository, worktree, branch,
and result commit. Focused controls pass for exact projection and deny foreign T2234/T2235,
malformed, missing, stale, moved-tip, open, non-green, and substituted-coordinate cases.

## terminal release authority <!-- cq:wip-checkpoint -->

Applied reviewed commit `ebce64b4c7bc31e1e9486d1284f92ce0cd8fd820` without its obsolete WIP
artifact. The SQLite/XDG lifecycle, shared admission contract, expected-failure inventory,
and all requested store adapters passed: 100 pass, 2 PostgreSQL skips, 0 fail.

## adapter and MCP acceptance <!-- cq:wip-checkpoint -->

The production XDG test now drives prepare/fetch/git-commit/store/confirm over linked MCP,
finalizes a runner-owned green gate for the immutable WIP tip, performs the guarded fast-forward
merge while the task is `wip`, transitions it to `done`, and releases the directory, branch, and
live registry record under restrictive roots. The focused production control passes (25 expects).

## targeted and full checks <!-- cq:wip-checkpoint -->

The exact selector reproduced three gate-run failures because the confined Bun process has PGID
`1`, which the settlement path rejected even though it only compares the caller group and never
signals it. The correction accepts positive caller PGIDs while preserving strict validation for
registered groups that may be signaled. The corrected selector and trusted full gate remain
unmeasured.
