```json
{
  "taskId": "T1207",
  "role": "implement-worker",
  "baseCommit": "56f50619d3d64dd42b4adf5e06fe0fd3250358bb",
  "startedAt": "2026-08-12T13:29:00Z",
  "checkpoints": [
    { "name": "inspect adopted candidate", "status": "done" },
    { "name": "measure expected-failure contract", "status": "done" },
    { "name": "prove guards and run gate", "status": "todo" }
  ]
}
```

## inspect adopted candidate <!-- cq:wip-checkpoint -->

Observed an adopted, untracked candidate test at the dispatch worktree. It uses
temporary out-of-tree fixtures and `Bun.spawnSync([process.execPath, "test",
fixturePath])`; measurement and guard evidence remain unmeasured.

## measure expected-failure contract <!-- cq:wip-checkpoint -->

The test uses two out-of-tree fixtures and the child runtime selected by
`process.execPath`; it does not let the outer suite collect either fixture.
The measurement is a specified, Behavioral-Active blackbox group test: Bun's
public test command runs against fixture files and the parent only observes its
exit code and captured streams.

## runtime

```text
bun --version:
1.3.13

process.execPath:
/nix/store/9ari7qcwfwkqcnr9kpf9fhbshmyhp98k-bun-1.3.13/bin/bun
```

## fixture-A

```text
exit code: 0

stdout:
bun test v1.3.13 (bf2e2cec)

stderr:

../../../../../../../../../../../../tmp/cq-expected-failure-marker-report-g45GMp/fixture-a.test.ts:
(pass) expected failure marker contract

 1 pass
 0 fail
 1 expect() calls
Ran 1 test across 1 file. [24.00ms]
```

## fixture-B

```text
exit code: 1

stdout:
bun test v1.3.13 (bf2e2cec)

stderr:

../../../../../../../../../../../../tmp/cq-expected-failure-marker-report-g45GMp/fixture-b.test.ts:
(fail) expected failure marker contract
  ^ this test is marked as failing but it passed. Remove `.failing` if tested behavior now works

 0 pass
 1 fail
 1 expect() calls
Ran 1 test across 1 file. [25.00ms]
```

## fixture diff

```text
--- fixture-a.test.ts
+++ fixture-b.test.ts
@@ -1,5 +1,5 @@
 import { expect, test } from "bun:test";
 
 test.failing("expected failure marker contract", () => {
-  expect(true).toBe(false);
+  expect(true).toBe(true);
 });
```

This provides one changed hunk: only the body assertion differs. The committed
test obtains both exit codes from `Bun.spawnSync` results, asserts A equals
zero, B does not equal zero, and they differ; its output assertion uses the
captured child-B output. A temporary `it.failing` type-reference also passed
`bunx tsc -b`, while the committed file directly type-checks `test.failing`.

Independent corroboration (not used in place of the measurement):
`packages/ledger/test/archive-dependency-invariants.test.ts:60` documents the
same inversion contract, and it has six currently failing `test.failing`
bodies at lines 206, 225, 253, 275, 296, and 312.

## prove guards and run gate <!-- cq:wip-checkpoint -->

The pre-mutation SHA-256 of the test file was
`bb3142b32625d3a445dc7c0412def5d195ee000196588c1d948f305f0eddfcc5`.

Temporary type-error probe (`const typecheckReachability: string = 1;`) made
`bunx tsc -b` exit 2 with this output; it was then reverted and the SHA-256
returned to the pre-mutation value.

```text
packages/cq-config/test/expectedFailureMarkerContract.test.ts(8,7): error TS2322: Type 'number' is not assignable to type 'string'.
```

Temporary inverted-expectation probe (`expect(childA.exitCode).not.toBe(0)`) made
the exact targeted test exit 1 with this output; it was then reverted and the
SHA-256 returned to the pre-mutation value.

```text
bun test v1.3.13 (bf2e2cec)

packages/cq-config/test/expectedFailureMarkerContract.test.ts:
52 | 
53 |   const childA = runFixture(fixtureAPath);
54 |   const childB = runFixture(fixtureBPath);
55 | 
56 |   expect(fixtureA.replace("toBe(false)", "toBe(true)")).toBe(fixtureB);
57 |   expect(childA.exitCode).not.toBe(0);
                                   ^
error: expect(received).not.toBe(expected)

Expected: not 0

      at <anonymous> (/home/pavel/work/safe/flakes/cq/.claude/worktrees/implement-T1207/nix/pkg/cq-ledgers/packages/cq-config/test/expectedFailureMarkerContract.test.ts:57:31)
(fail) bun inverts test.failing outcomes [62.00ms]

 0 pass
 1 fail
 2 expect() calls
Ran 1 test across 1 file. [96.00ms]
```

After restoration, `bunx tsc -b`, the exact Bun test, and exact-file ESLint
all exited 0. The required foreground full gate remains pending.
