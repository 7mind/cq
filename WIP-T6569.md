```json
{
  "taskId": "T6569",
  "role": "implement-worker",
  "baseCommit": "c476b3b91922daadb93995400b544502655c2c35",
  "startedAt": "2026-09-16T20:24:12Z",
  "checkpoints": [
    { "name": "fail-first semantic regressions", "status": "done" },
    { "name": "operation-aware policy and gateway", "status": "done" },
    { "name": "focused cross-adapter checks", "status": "unmeasured" },
    { "name": "required PostgreSQL task-only harness", "status": "unmeasured" },
    { "name": "postgres scope reproduction", "status": "todo" },
    { "name": "postgres scope correction", "status": "todo" },
    { "name": "focused local checks", "status": "todo" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## fail-first semantic regressions <!-- cq:wip-checkpoint -->

Added same-titled form-(c) D442 cases to the shared adapter contract. Before
production edits, the cases observed `creation-denied`, `target-excluded`, and
`archive-terminal-items-denied` with zero mutation. Matching empty-root
operations succeeded as discriminating controls. Bun 1.3.13 ran the in-memory
contract: 25 pass, 0 fail, 102 assertions.

## operation-aware policy and gateway <!-- cq:wip-checkpoint -->

Implemented exported ledger/semantic operation clauses and effective question
delta classification. Idea create/update/reopen/unarchive/terminal archive now
remain available under restrictive roots. Pure question answers admit only the
three specified effective deltas. Terminal archive enumerates exact affected
refs before mutation, exempts idea refs, and atomically rejects excluded
non-idea refs. Durable read scope remains separate from admission targets.

## focused cross-adapter checks <!-- cq:wip-checkpoint -->

`tsc -b` and ESLint pass. The exact 15-file focused selector executed 303
tests: 301 passed; the two HTTP MCP cases failed before behavior at
`Bun.serve({ port: 0 })` with `EADDRINUSE`. Running the HTTP file alone produced
the same bind failure. In-memory, SQLite, direct, stdio, web, TUI, inventory,
conformance, question-precondition, effect-admission, and wrapper cases passed.
Mutation probes disabled idea creation, question-answer classification, idea
archive exemption, durable target loading, web full-field shape, and TUI narrow
shape; each named case failed for the expected reason and passed after exact
hash restoration.

## required PostgreSQL task-only harness <!-- cq:wip-checkpoint -->

Implemented `--task-only`, unit-proved that it runs exactly one JUnit-validated
task stage and no full check, and proved required mode with an empty DSN exits
1 before skips. The live wrapper could not provision its isolated cluster in
this sandbox: its loopback reservation failed with `Failed to listen at
127.0.0.1`; no live PostgreSQL evidence is claimed. A mutation of the task-only
branch caused the wrapper case to invoke the forbidden second stage; restoration
passed with the original hash.

## postgres scope reproduction <!-- cq:wip-checkpoint -->

Pending focused reproduction from the parent-recorded required-live PostgreSQL failures.

## postgres scope correction <!-- cq:wip-checkpoint -->

Pending correction of the PostgreSQL bound scope/admission model and discriminating coverage.

## focused local checks <!-- cq:wip-checkpoint -->

Pending focused non-network checks permitted by the round-1 operator note.

## trusted full gate <!-- cq:wip-checkpoint -->

Unmeasured by instruction in this focused round; parent final-base settlement
must precede the single trusted canonical gate.
