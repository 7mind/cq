```json
{
  "taskId": "T6569",
  "role": "implement-worker",
  "baseCommit": "17d58923bd9dd53ee3d61fddfe6de64f5f93c611",
  "startedAt": "2026-09-16T20:24:12Z",
  "checkpoints": [
    { "name": "fail-first semantic regressions", "status": "done" },
    { "name": "operation-aware policy and gateway", "status": "done" },
    { "name": "focused cross-adapter checks", "status": "done" },
    { "name": "required PostgreSQL task-only harness", "status": "done" },
    { "name": "postgres scope reproduction", "status": "done" },
    { "name": "postgres scope correction", "status": "done" },
    { "name": "binding ownership correction", "status": "done" },
    { "name": "focused local checks", "status": "done" },
    { "name": "binding lookalike reproduction", "status": "done" },
    { "name": "authentic fixed verifier correction", "status": "done" },
    { "name": "focused correction checks", "status": "done" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## fail-first semantic regressions <!-- cq:wip-checkpoint -->

Added same-titled form-(c) D442 cases to the shared adapter contract. Before
production edits, the cases observed `creation-denied`, `target-excluded`, and
`archive-terminal-items-denied` with zero mutation. Matching empty-root
operations succeeded as discriminating controls. Bun 1.3.13 ran the in-memory
contract: 25 pass, 0 fail, 102 assertions.

## operation-aware policy and gateway <!-- cq:wip-checkpoint -->

Implemented exported ledger/semantic operation clauses and effective question
delta classification. Idea create/update/reopen/unarchive/terminal archive now
remain available under restrictive roots. Pure question answers admit only the
three specified effective deltas. Terminal archive enumerates exact affected
refs before mutation, exempts idea refs, and atomically rejects excluded
non-idea refs. Durable read scope remains separate from admission targets.

## focused cross-adapter checks <!-- cq:wip-checkpoint -->

`tsc -b` and ESLint pass. The exact 15-file focused selector executed 303
tests: 301 passed; the two HTTP MCP cases failed before behavior at
`Bun.serve({ port: 0 })` with `EADDRINUSE`. Running the HTTP file alone produced
the same bind failure. In-memory, SQLite, direct, stdio, web, TUI, inventory,
conformance, question-precondition, effect-admission, and wrapper cases passed.
Mutation probes disabled idea creation, question-answer classification, idea
archive exemption, durable target loading, web full-field shape, and TUI narrow
shape; each named case failed for the expected reason and passed after exact
hash restoration.

Parent final-base validation retained the 301 non-network passes and ran both
HTTP MCP cases successfully (2 passed, 0 failed). The final candidate therefore
has passing direct, stdio, HTTP MCP, web full-field, TUI answer-only, in-memory,
and SQLite evidence without repeating the unchanged child selectors.

## required PostgreSQL task-only harness <!-- cq:wip-checkpoint -->

Implemented `--task-only`, unit-proved that it runs exactly one JUnit-validated
task stage and no full check, and proved required mode with an empty DSN exits
1 before skips. The live wrapper could not provision its isolated cluster in
this sandbox: its loopback reservation failed with `Failed to listen at
127.0.0.1`; no live PostgreSQL evidence is claimed. A mutation of the task-only
branch caused the wrapper case to invoke the forbidden second stage; restoration
passed with the original hash.

Parent final-base validation ran the four-file required-live PostgreSQL
selector against its owned isolated cluster: 52 passed, 0 failed, 1,092
assertions, 52 PostgreSQL cases, and 0 skips. Task-only JUnit evidence reported
no automatic full-check result (`full=null`), and the owned cluster stopped
after the run.

## postgres scope reproduction <!-- cq:wip-checkpoint -->

Bun 1.3.13 ran the focused D442 binding regression before production edits. A
gateway-issued live admission with semantic targets `[]` and PostgreSQL row
scope `["ideas:I1"]` failed at `genericRowOperation.ts:43-45` with
`WorksetAdmissionError(code="caller-minted-admission")`, matching the parent
required-live PostgreSQL observation.

## postgres scope correction <!-- cq:wip-checkpoint -->

The gateway now mints an immutable WeakSet-bound admission/scope descriptor.
PostgreSQL verifies that descriptor identity, a still-live generic-write
admission, and its durable project-scoped roots/epoch while retaining the
separate exact row-loading scope. The focused regression admits distinct
semantic/row targets and rejects both substituted row scope and a live foreign
admission. Removing the binding predicate made the substituted plan load;
restoring it passed with exact pre/post file hashes. Refreshed the exact opaque
digests for `postgresRequiredGate.ts` and the changed conformance test. A
one-digit digest mutation produced `source-digest-mismatch`; exact restoration
passed the repository closure check.

## binding ownership correction <!-- cq:wip-checkpoint -->

Replaced the module-level mutable admission-binding registry with an opaque,
per-gateway owner and frozen verifier token injected into PostgreSQL row
resolution. A same-titled D442 regression proved the old registry accepted a
still-live descriptor from another gateway, while the corrected owner rejects
it without weakening exact scope/admission identity, live-authority checks, or
durable project roots/epoch validation. Removing only the new ownership guard
reproduced the same `loaded` failure; exact source-hash restoration passed.

The changed conformance file passes 8 tests with 28 assertions. `tsc -b`,
selected ESLint, and the repository closure check pass. The closure digest was
updated for the changed conformance bytes; `implementationEvidence.ts` remains
unchanged at SHA-256
`e56b14602f07bb7d18ee63ea934c77be8c1194a12f7eb98777cac06edd57d7ca`.

## focused local checks <!-- cq:wip-checkpoint -->

Bun 1.3.13 ran the changed conformance and required-wrapper files: 17 pass,
0 fail, 116 assertions. `tsc -b`, selected ESLint, and the repository closure
test pass. The unchanged prior-round evidence remains 301 non-network passes
and the two HTTP cases blocked before behavior by sandbox loopback binding.
No live PostgreSQL pass is claimed in this sandbox. Parent validation command:

`bun run check:postgres-required -- --task-only bun test packages/ledger/test/workset-generic-mutation-postgres.test.ts packages/ledger/test/workset-generic-mutation-postgres-faults.test.ts`

## binding lookalike reproduction <!-- cq:wip-checkpoint -->

Bun 1.3.13 ran the changed conformance file before production edits: 7 passed,
1 failed, 27 assertions. The authentic binding plus cloned row scope retained
the expected `caller-minted-admission` rejection, while
`Object.assign(Object.create(Object.getPrototypeOf(binding)), { owns: () => true })`
incorrectly authenticated the same cloned scope and returned `kind: "loaded"`.
The failure is therefore specific to the overridable verifier path.

## authentic fixed verifier correction <!-- cq:wip-checkpoint -->

Replaced the callback-bearing token with frozen, key-gated construction and
fixed class verifiers over private per-token `WeakSet` state. Authenticity no
longer depends on `instanceof`, a caller-overridable `owns` method, or a token
constructor that accepts an arbitrary verifier. The immutable module-private
key protects construction and registration without mutable module state.

Mutation probes started and ended with source SHA-256
`c33be3f4155b65052cb54dcaf6116f98b5452dca170f1ce2c6c7e6dba0b70ac8`.
Reintroducing an overridable `owns` branch made the prototype lookalike return
`kind: "loaded"` (7 pass, 1 fail). Removing the private-constructor check made
the captured-constructor assertion observe no throw (7 pass, 1 fail). Exact
restoration passed all 8 cases with 30 assertions.

## focused correction checks <!-- cq:wip-checkpoint -->

Bun 1.3.13 passed the changed conformance selector: 8 tests, 30 assertions.
`tsc -b`, selected ESLint, and the repository closure test pass; the closure
manifest records test SHA-256
`754f64bf94913c1d463be89cdd75148d7cfff8a36481f87697783f09d86f5f9b`.
`implementationEvidence.ts` remains unchanged at SHA-256
`e56b14602f07bb7d18ee63ea934c77be8c1194a12f7eb98777cac06edd57d7ca`.
No live PostgreSQL or canonical full-gate run was performed in this correction
round, as required by the operator handoff.

## trusted full gate <!-- cq:wip-checkpoint -->

Unmeasured by instruction. Parent final-base settlement passed its closure
probe (1 passed, 0 failed); result storage owns the single trusted canonical
gate for this exact final candidate.
