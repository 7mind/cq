```json
{
  "taskId": "T2837",
  "role": "implement-worker",
  "baseCommit": "b048276a6b945286a7044215c078d03e9f3fd4c9",
  "startedAt": "2026-08-27T12:36:43Z",
  "checkpoints": [
    { "name": "D370 fail-first regression", "status": "done" },
    { "name": "D370 authentic nativeCompletion migration", "status": "done" },
    { "name": "D359 fail-first regression", "status": "done" },
    { "name": "guarded bootstrap-repair lifecycle", "status": "done" },
    { "name": "targeted checks and mutation evidence", "status": "done" },
    { "name": "round-two gate criticism corrections", "status": "todo" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## D370 fail-first regression <!-- cq:wip-checkpoint -->

The production-shaped generation-17 consumed-FAIL fixture carries the exact native-completion
proof and terminal digest emitted by `confirmDispatchCompletion`. Before the production correction,
the focused selector exited 1 with the expected `CurrentRecoverySealError` reason
`journal-conflict` at the obsolete two-field terminal-digest comparison (0 pass, 1 fail).

## D370 authentic nativeCompletion migration <!-- cq:wip-checkpoint -->

Pre-scheme consumed-FAIL migration now requires the retained native-completion proof and recomputes
the same terminal digest as the production consumed transition: output digest, child id, run id,
and completion timestamp. The exact T2345 generation-17 authority migrates and seals; mutated proof
and terminal-digest controls reject without changing the committed journal. The complete focused
file passed 10 tests with 90 assertions.

## D359 fail-first regression <!-- cq:wip-checkpoint -->

The production-shaped in-memory lifecycle has one managed building goal, one finalized blocked
task, and one owned high-severity root-caused infrastructure defect. All six prior action
predicates are false; ordinary follow-up rejects with `implementation-active` and leaves the
snapshot byte-equivalent. The new repair-action assertion then failed as expected because
`pBootstrapRepair` was absent (0 pass, 1 fail, 9 assertions).

## guarded bootstrap-repair lifecycle <!-- cq:wip-checkpoint -->

An owned root-caused defect that blocks the sole active finalized task is now re-exposed through
`pSeed` when no canonical repair lineage is active. The existing atomic defect-to-fix-goal bundle
now returns its already-sealed correction goal on replay and concurrent claims, so it never
allocates competing repair identities. A live correction goal hands progress to normal plan and
implement predicates; if the correction lineage terminates first, the blocker becomes actionable
again until the defect itself reaches a terminal status. Ordinary follow-up still rejects and the
blocked goal, task status, finalized manifest, and task fields remain byte-equivalent.

The shared dual-test contract passed on the in-memory, filesystem, SQLite, and Git-object adapters;
the Postgres leg was explicitly environment-gated. Together with the D359 predicate suite, the
focused run passed 27 tests, skipped 1 unavailable Postgres leg, and made 141 assertions. Typecheck
passed.

## targeted checks and mutation evidence <!-- cq:wip-checkpoint -->

The final focused matrix passed 42 tests, skipped the explicitly environment-gated Postgres leg,
and made 242 assertions across the D370 recovery file, the D359 lifecycle, and the in-memory,
filesystem, SQLite, and Git-object coordination-bundle adapters. TypeScript project references and
targeted ESLint checks passed. Direct evaluation of `llmAssets.promptCatalogProjection` against the
locked nixpkgs source produced the 25-role projection, and the generated agent catalogue now carries
the D359 bootstrap-repair instructions.

Mutation probes were run against the formatted final source and restored before the passing run:

- D370 terminal authentication: source SHA-256
  `7461763f6054ec90a34db31d26013a018229066f48ea43b9e0bf8be7a8a350ff`; weakening the exact
  native-completion terminal-digest check produced
  `5e2f3082314902d8acfae39aee7ed40fa36f677ccd3ad501bebd1b5b1951414a` and made the mutated-proof
  control resolve instead of reject. Restoration returned the original hash and both D370 selectors
  passed.
- D359 repair exposure: predicates SHA-256
  `a8ca7680f21d6d1ce2c34afbfe58db8ed9976a7b483b1247d41d01cd32ea7093`; disabling the bootstrap
  exception produced `0ffa6d00338eec0cd1d53f0b735700e878dac63b3da8f8e50cf541d9a45a04cd` and changed the
  expected `pSeed` verdict from true to false. Restoration returned the original hash.
- D359 correction-lineage suppression: the same restored predicates hash; disabling the active
  fix-goal check produced `1fab076ac24f129c71835d3461a8d7a7e8b23fc0b333be95f572ab9a77391cea` and exposed `pSeed`
  while the repair goal was live. Restoration returned the original hash.
- D359 sole-active-task boundary: the same restored predicates hash; weakening exact cardinality
  produced `4f08d59ea4452fedfed0f730cf622c73449a72decba16b4cfc4533e813a14b3e` and incorrectly exposed
  `pSeed` with two active finalized tasks. Restoration returned the original hash.
- Guarded replay/concurrency: lifecycle SHA-256
  `9fa86e8fe76cbb9737f703252ce893e851dc48e3a24db173a09b57b567fb18e2`; disabling the canonical
  existing-goal return produced `df41af0dba4ab05f764933dafa61732a19a1df63a8dc243c7b142ef5da0b59df` and the eight
  concurrent claims returned eight goal identities instead of one. Restoration returned the
  original hash and the selector passed.

## round-two gate criticism corrections <!-- cq:wip-checkpoint -->

Remove the CQ-development identifier from canonical prompt prose, cover the added `create_item`
call in the response-policy inventory, regenerate prompt catalogue and profiler evidence, refresh
the repository gate-closure manifest after the final bytes, and run the focused provenance,
profiler, response-policy, and closure selectors.

## trusted full gate <!-- cq:wip-checkpoint -->

Reserved for parent-owned supervised full-gate finalization.
