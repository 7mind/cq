```json
{
  "taskId": "T2047",
  "role": "implement-worker",
  "baseCommit": "89a0b5558bd002bb493d5be47e71980cd4a58c2e",
  "startedAt": "2026-08-10T20:16:27Z",
  "checkpoints": [
    { "name": "reproduction", "status": "done" },
    { "name": "implementation", "status": "done" },
    { "name": "verification", "status": "unmeasured" }
  ]
}
```

## reproduction <!-- cq:wip-checkpoint -->

`bun test packages/ledger/test/worktree-manage-mcp.test.ts --test-name-pattern 'accepts a manager-issued v2 handle'` failed with `handle.version: Invalid input: expected 1` (0 pass, 1 fail), confirming that the MCP boundary rejects the required v2 adopted handle for the expected reason.

## implementation <!-- cq:wip-checkpoint -->

Added a shared closed v1/v2 handle union. Fresh v1 retains UUIDv7 basename placement; adopted v2 retains an opaque UUIDv7 registry identity and binds task T to `.claude/worktrees/implement-T` plus branch `implement/T`. Core, MCP, native qualification, Claude/Pi binding, and T1698/T1699 paths consume the same validator.

## verification <!-- cq:wip-checkpoint -->

Run focused tests, mutation checks for every changed guard, and the full repository gate.
