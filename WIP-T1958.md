```json
{
  "taskId": "T1958",
  "role": "implement-worker",
  "baseCommit": "46b434ef81008c8955ed66cd12afd9a5fd3b6d77",
  "startedAt": "2026-04-09T00:00:00.000Z",
  "checkpoints": [
    {"name": "schema-and-types", "status": "todo"},
    {"name": "postgres-workset-store", "status": "todo"},
    {"name": "integration-postgres-ledger-store", "status": "todo"},
    {"name": "contract-and-focused-tests", "status": "todo"},
    {"name": "mutation-evidence-and-gate", "status": "todo"}
  ]
}
```

## schema-and-types <!-- cq:wip-checkpoint -->

Add tenant-scoped Postgres tables:
- `workset_roots` (project_key PK, roots_json, epoch)
- `workset_admissions` durable rows with host/process identity, kind, target, epoch, heartbeat, optional process-group registration

Never use connection-lifetime advisory locks for admissions.

## postgres-workset-store <!-- cq:wip-checkpoint -->

Implement `createPostgresWorksetStore` / Postgres adapter implementing `WorksetStore`:
- setRoots transactional under tenant serialization
- durable INSERT/DELETE admissions
- stale recovery via process-identity fence + heartbeat
- stuck-record fail-closed diagnostic
- LISTEN/NOTIFY coherence advance

## integration-postgres-ledger-store <!-- cq:wip-checkpoint -->

Wire WorksetStore onto PostgresLedgerStore (or expose factory used by it).

## contract-and-focused-tests <!-- cq:wip-checkpoint -->

- workset-store-contract (in-memory always)
- workset-store-postgres (skip-if-no-PG)
- workset-effect-admission-postgres
- workset-postgres-disconnect
- ensure existing postgres-coherence-watcher + multi-writer-stress still pass

## mutation-evidence-and-gate <!-- cq:wip-checkpoint -->

Prove changed guards fail then pass; full `cq gate run`.
