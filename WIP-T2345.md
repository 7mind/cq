```json
{
  "taskId": "T2345",
  "role": "implement-worker",
  "baseCommit": "79b0b69592ab4b50f290c824a4a03b3af3159f1c",
  "startedAt": "2026-08-24T04:10:57Z",
  "checkpoints": [
    { "name": "reproduction", "status": "done" },
    { "name": "implementation", "status": "done" },
    { "name": "targeted-checks", "status": "done" },
    { "name": "mutation-evidence", "status": "done" },
    { "name": "harvest-recovery", "status": "done" },
    { "name": "child-verification", "status": "done" },
    { "name": "generation-3-revalidation", "status": "done" },
    { "name": "generation-4-gate-corrections", "status": "done" },
    { "name": "generation-5-parent-gate-corrections", "status": "done" },
    { "name": "generation-6-installed-gate-correction", "status": "done" },
    { "name": "generation-7-reviewer-matrix-correction", "status": "done" },
    { "name": "generation-8-review-corrections", "status": "done" },
    { "name": "generation-9-parent-gate-corrections", "status": "done" },
    { "name": "generation-10-review-corrections", "status": "done" },
    { "name": "generation-11-parent-gate-correction", "status": "done" },
    { "name": "generation-13-review-corrections", "status": "done" },
    { "name": "generation-14-review-corrections", "status": "todo" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## reproduction <!-- cq:wip-checkpoint -->

Added blackbox contract probes for the required merge `completionRef` and stable
operation id. Before implementation, `bun test packages/cq-cli/test/gateRun.test.ts`
failed three merge-journal assertions because `--completion-ref` was unknown and
an unjournaled merge was accepted.

## implementation <!-- cq:wip-checkpoint -->

Added a versioned protected review/completion journal with durable filesystem
and strict in-memory adapters, authenticated reviewer-attempt receipts,
pre-merge evidence binding, crash recovery, merge admission, canonical
acknowledgements, race-safe terminal recording, and generic-write denial. The
direct, stdio, and Streamable HTTP tool surfaces expose the seven protected
operations. The implementation prompt and generated agent catalogue require the
journal-first protocol. The command parser and trusted Git-effect binding now
discriminate merge from rebase and bind the completion ref plus operation id.

## targeted-checks <!-- cq:wip-checkpoint -->

The focused CLI, process-control, evidence, adapter, transport, prompt-contract,
and recovery suites pass (41 tests). TypeScript project references compile.
`gen-prompt-catalog` and `gen-agents` completed; local evaluation used the
locked source hashes through an isolated local Nix store because the worker
sandbox cannot reach the host daemon socket.

## mutation-evidence <!-- cq:wip-checkpoint -->

Observed mutations:

- Removing `completionRef` from merge binding identity made the substitution
  test fail; restoring the intended bytes returned it to green.
- Disabling the merge completion-ref parser guard made the unjournaled-merge
  test fail; restoring the guard returned it to green.
- Removing each new orchestration-prompt invariant made its contract probe fail;
  restoring the canonical prompt returned all five prompt probes to green.

## harvest-recovery <!-- cq:wip-checkpoint -->

Generation 2 verified the prepared branch, starting commit, and base ancestry,
then preserved the parent-timeout harvest. The recovered bytes contain the
generic implementation-result write guard, additive-schema compatibility
fixtures, and the tool-surface baseline comparison correction. The inherited
`WIP-T2322.md` artifact was removed as directed.

## child-verification <!-- cq:wip-checkpoint -->

The exact 13-file acceptance selector passed 92 tests with 0 failures and 478
expectations. The harvested compatibility and tool-surface selector passed 17
tests with 0 failures, 2 explicit PostgreSQL-environment skips, and 269
expectations. `gen-prompt-catalog` and `gen-agents` each completed twice from
the locked T2345 projection bytes, leaving the tracked output byte-identical.

## trusted full gate <!-- cq:wip-checkpoint -->

Unmeasured in the child. The brokered dispatch delegates the canonical full
gate to trusted result storage after the final clean commit is handed off.

## generation-11-parent-gate-correction <!-- cq:wip-checkpoint -->

## generation-13-review-corrections <!-- cq:wip-checkpoint -->

## generation-14-review-corrections <!-- cq:wip-checkpoint -->

Reproducing the remaining production authority, receipt-binding, supersession,
and external-execution durability findings before correction.

Round 2 preserved the inherited branch and reproduced four surviving review
findings before correction: two different external-execution operation IDs
could overwrite a receipt; a gate-less approval with an arbitrary merge base
was accepted; an activated task could be terminalized as `abandoned` through
the generic store; and an unchanged prepared journal could be superseded.
The focused probes also showed that a failed ff-only process caused the journal
wrapper to throw before settling the underlying admission.

An adapter attempt now permits one immutable trusted execution and rejects any
new execution after finalization. Approved verdicts require a positive rerun
gate duration and coherent base ancestry. The generic guard rejects both task
terminal statuses, supersession requires a changed rebased result plus fresh
attempt set, and the wrapper settles the underlying admission before recording
`merged`, retaining `merge-started` when HEAD remains at the prepared head.
The 17-test focused evidence selector passes; the full 13-file acceptance
selector passes 103 tests with 506 expectations. TypeScript references and
ESLint pass. A counterfactual disabling the new transition checks produced
four expected failures; restoring the source returned
`implementationEvidence.ts` to SHA-256
`94fd2930b90dcec355a04aaf11d4b99911d7866b6147696afe85656e774a7048`. The prompt
generator could not open the host Nix fetcher cache from confinement and did
not write output; the final generation check remains delegated to the trusted
parent gate.

Round 1 verified the prepared worktree, branch, exact starting commit, prior
result ancestry, base commit object, and base ancestry. The parent reports one
remaining Markdown response-matrix assertion: the document contains the 46
trusted-management rows but the assertion compares them with the 39-name
ordinary inventory. The MCP documentation test already used the management
inventory and passed, but the parent diagnostic identified a separate rendered
Markdown guard in `ledger-web/test/Markdown.test.tsx`. A focused reproduction
failed exactly 1 of 5 tests with 39 expected rows and 46 received rows.

The rendered Markdown guard now uses `MANAGEMENT_LEDGER_TOOL_NAMES` for its row
count, column count, and ordered tool-name assertions. The focused selector
passes all 5 tests with 28 expectations. A fresh counterfactual restored the
ordinary inventory and reproduced the same 39-versus-46 failure; restoring the
intended bytes passed all 5 tests and returned the file to SHA-256
`2fe08ac80793f7462f95b3ef13dcdd1ab482c330c6ce909a3ea967adf7a87359`.
The exact 13-file acceptance selector passed 99 tests with 498 expectations.
TypeScript project references and the repository ESLint suite pass.
Both generators completed twice through the retained task-scoped locked Nix
projections and left generated output byte-identical. The direct prompt
generator invocation failed before writing because confinement denies the host
Nix fetcher cache; no success claim is made for that invocation. The final
trusted full gate remains delegated to result storage.

## generation-10-review-corrections <!-- cq:wip-checkpoint -->

Round 1 verified the prepared worktree, branch, exact starting commit, prior
result ancestry, base commit object, and base ancestry. The embedded TUI
conformance probe reproduced the 46-name trusted-management inventory being
compared with the 39-name ordinary inventory. A documentation-first assertion
also reproduced the stale `closed 39-tool matrix` text.

The standalone runtime reproduction captured the exact pre-connect stderr:
under `CQ_HARNESS=codex`, eager evidence-service construction resolved the
reviewer panel and aborted on a minimal `cq.toml` without `[harness.codex]`.
Reviewer roster resolution now remains fail-closed but occurs at the first
evidence operation, where the roster is actually snapshotted. Construction no
longer prevents stdio or HTTP from binding. Shared dispatch conformance now
takes an explicit expected inventory; embedded TUI/web select all 46 management
names while standalone surfaces retain the ordinary inventory. Focused runtime,
review-attempt, evidence, parity, TUI, and documentation checks pass.

The final 18-file selector, including all 13 acceptance files, passed 127 tests
with one explicit PostgreSQL-environment skip and 1,340 expectations. TypeScript
project references and ESLint pass. Both generators completed twice using the
retained task-scoped locked Nix projections and left generated output
byte-identical. The direct generator invocation could not write the host Nix
fetcher cache from confinement; no generator-success claim is made for that
failed invocation.

Fresh counterfactuals failed for the intended reasons and were restored:
selecting an empty expected embedded inventory exposed all 46 surplus names;
restoring the README's stale 39 count failed the 46-tool documentation guard;
eager reviewer resolution reproduced the exact pre-connect `[harness.codex]`
startup failure; accepting an empty roster minted an invalid zero-attempt panel;
and bypassing configured roster resolution admitted that panel instead of
preserving the fail-closed config error. Restored SHA-256 digests are
`41dd9c3433ecbbcf3449c770bb6f2237a03d23f47d3823fd81b1811186543e7f`,
`edcc61450244c4fb9da9ca83e81566ddaec7259e60ac3ffc5a81d70a21769fb3`,
`5e59aeb9473e5703eac95d570d42a6b9dbd95ff8deda898f0f80dd457fb998e7`,
and `5f2e488274d0d98529b3b6ae3df1dfe7feec45e5e076a816078d2894009f703d`.
The final trusted full gate remains delegated to result storage.

## generation-9-parent-gate-corrections <!-- cq:wip-checkpoint -->

Round 1 verified the prepared worktree, branch, exact starting commit, prior
result ancestry, base commit object, and base ancestry. The trusted parent gate
reported 6,753 passing tests and 29 failures. Diagnosis and correction of the
reported legacy-lifecycle, management-surface, and D336 fingerprint regressions
were reproduced before correction. Four backend task-adoption cases and the
bounded workset scenario failed at the unconditional core task guard; four
ordinary server inventory assertions exposed the seven management-only tools;
and D336 failed because its second one-shot worker-result fetch no longer
returned the evidence bytes bound at completion preparation.

Protection now composes the protected sidecar with production ledger stores.
Direct and generic-transaction mutations consult append-only activation state,
so legacy task lifecycle remains valid before a panel exists while activated
tasks still reject generic terminalization and post-completion mutation. The
ordinary inventory contains 39 operations, the trusted-management inventory
contains all 46, and profile filtering retains management-only implementation
operations only on management-bound surfaces. D336 re-observes immutable
dispatch evidence instead of consuming the public one-shot fetch twice.

The focused implementation, legacy lifecycle, inventory, transport parity,
D336, response-contract, and tool-surface checks pass. TypeScript project
references and ESLint pass. The exact 13-file acceptance selector passes 98
tests with 497 expectations. `gen-prompt-catalog` and `gen-agents` each ran
twice through the retained task-scoped locked Nix projections and left both
generated outputs byte-identical.

Fresh counterfactuals failed for the intended reasons and were restored:
disabling activation lookup admitted a forged direct terminalization; retaining
only the seven protected names emptied the ordinary instruction inventory;
filtering a management full profile removed all seven protected operations;
removing the compact-instruction management arm expanded the canonical prompt
to a 47-line inventory; and replacing immutable dispatch observation with the
one-shot fetch reproduced D336's `worker evidence changed` failure. Restored
source SHA-256 digests are
`20116e601b294b04bf27fe5ef44311272ae317a0599b8c92d9cbc3cb3509464a`,
`a59defc8e44bd77429d5ad723a3a58c63038802b2599af693ebca7c28eb6520a`,
`6eaf1dff48760f9c18b353d05050e5d827843bdfa2a465bef3abf3e6188ee429`,
and `8493d14b76e001dfe95f0eb1289806c86dd7d67c24d9943579fdbece5ff32e62`.
The final trusted full gate remains delegated to result storage.

## generation-8-review-corrections <!-- cq:wip-checkpoint -->

Generation 8 verified the inherited receipt chain, prepared branch, exact
starting commit, prior result commit, base commit object, and base ancestry.
It reproduced and corrected all seven reviewer findings: production service
wiring, management-only exposure, merge replay/recovery, repository-wide merge
exclusion, atomic terminal recording and revalidation, reviewer-attempt
validation, and store-bound generic authority protection.

Focused regression probes now reproduce the reported defects. The six-file
selector reported 8 failures: external execution was allowed before prepare;
substituted adapter identity and malformed approval semantics authenticated;
prepared-at-result recovery was not recordable; observe/direct surfaces
advertised protected operations; evidence-bearing reviews remained generically
mutable; and a failed paired task transition left its review behind. The race
probe initially named the fixture's store field incorrectly; that test defect
was corrected before persistence and is not claimed as product evidence.

The correction now constructs one production evidence service and injects it
into both standalone transports. Native evidence is re-observed without
materializing a capability; configured external reviewers run through the
selected trusted harness process. Ordinary direct, stdio, and HTTP sessions no
longer advertise the seven protected operations, while management-bound parity
still covers the complete 46-tool inventory. Remote management clients expose
typed parity for all seven operations.

Completion recording now revalidates the exact worker output, task authority,
ordered reviewer receipts, fallback justification, result/base/start
coordinates, clean worktree, Git receipt chain, acceptance, and gate binding.
Repository merge-start admission is atomic, every prepared/result-head recovery
state is replayable, and the terminal review plus task update share one backend
transaction. A real SQLite trigger failpoint observed rollback of both writes
and a successful exact retry. The focused correction selector passes 19 tests;
the final 13-file implementation selector passes 62 tests with 745 expectations
after updating its parity fixture to request the management surface explicitly.
TypeScript project references compile, ESLint passes, and the tool-surface
profiler's two budget tests pass. Both catalogue generators were invoked but
could not reach the host Nix daemon from the confined worker; their tracked
outputs remain unchanged and the trusted parent gate retains the authoritative
generator check.

Fresh counterfactuals failed for the intended reasons and were restored: an
approval-semantics substitution authenticated a malformed verdict; removing
merge-started exclusion admitted both repositories; removing pre-record
revalidation resolved a stale completion; changing the direct terminalization
guard admitted the forged task transition; trusting every HTTP authority
exposed protected tools to the ordinary token; inverting receipt-tree equality
rejected the valid Git chain; and removing HTTP evidence injection failed the
two-transport wiring contract. Restored source hashes are
`ff2d178ca4e999556009466812740325e1e5e729f06778059cd862142137f83d`,
`0348ae78bf7532d6dfdf59b8bf07c60061f7ba524d38794679e482ccf997b0e2`,
`9d156255d0514ac46d712d6b19e7e616d57d8d83ad5d6921abba2c209d962872`,
and `45a58a7d23119cc8a8c5b028bc7b289863b6fd0476280cf10c2b450f77b2eee8`.

## generation-7-reviewer-matrix-correction <!-- cq:wip-checkpoint -->

Generation 7 verified the complete inherited receipt chain, prepared branch,
exact starting commit, base commit object, and base ancestry. The trusted parent
reports that the source gate is green at 6,771 tests and the installed gate has
one remaining failure: the reviewer-matrix fixture compares rich configured
reviewer entries directly with summary rows. A focused Bun counterexample
reproduced the exact failure: the received rows retained authenticated
`dispatch` and `consumed` fields that the expected summary intentionally omits.
The matrix now retains the rich run type and projects its six summary fields
before the exact comparison. The source fixture selector passes 1 test with 3
intentional installed-environment skips and 19 expectations; TypeScript project
references and focused ESLint both pass. A direct rich-row counterexample
failed with the expected six surplus `dispatch`/`consumed` lines. A mutation of
the installed assertion could not reach that guard in the child: the staged
installed process fixture terminated first because its fake Codex received EOF,
so no installed-pass claim is made here. The protected result-storage boundary
will run the canonical Nix install check and full gate against the final commit.

## generation-6-installed-gate-correction <!-- cq:wip-checkpoint -->

Generation 6 verified the inherited receipt chain, prepared branch, exact
starting commit, base commit object, and base ancestry. The source suite is
green at 6,771 tests; the trusted parent reports that the installed
`packagedCodexRoleGitBroker.test.ts` guarded-rebase fixture still uses the
legacy unjournaled merge invocation. The fixture now retains its consumed
round-2 reviewer dispatch, prepares and finalizes the opaque protected review
attempt, prepares completion against the pre-merge head, passes the exact
completion ref and stable merge operation id to the installed Git-effect
boundary, and validates the canonical merge acknowledgement.

The focused source guard plus the 13-file acceptance selector pass 93 tests
with 3 installed-environment skips and 497 expectations. TypeScript project
references and ESLint pass. A counterfactual that dropped the completion ref
from the final merge failed the new multiline source guard; restoration passed
and returned `packagedCodexRoleGitBroker.test.ts` to SHA-256
`3cd6644382815bda1f42da002d61592e6cd0ff910f42253fbd1d41da1bff9830`.
The installed runtime case is delegated to the trusted full gate because the
child sandbox cannot impersonate a writable `/nix/store` derivation identity.

## generation-4-gate-corrections <!-- cq:wip-checkpoint -->

Generation 4 verified the inherited receipt chain, prepared branch, exact
starting commit, base commit object, and base ancestry. The parent canonical
gate reported 6702 passing and 69 failing tests. Diagnosis and correction of
those observed failures remains in progress. A focused six-file reproduction
reported 61 failures caused by the overbroad generic task-create guard. The
protection now activates only after a protected review panel exists and is
enforced at the generic `update_item` boundary; direct seed and legacy setup
remain valid. The evidence/workset correction selector passes 6 tests. The CQ
response-policy inventory, eligible-column baseline, and canonical ledger docs
were updated, and their selector plus the tool-surface target passes 198 tests.
The original six-file failing selector now passes 260 tests with one explicit
PostgreSQL-environment skip. The exact acceptance selector passes 92 tests;
TypeScript project references and ESLint pass. Both generators completed twice
through an isolated local Nix store using the locked input source hashes and
left the tracked tree byte-identical.

The generation-4 counterfactual changed the generic boundary's terminal status
match from `done` to `wip`. The active-task denial test then failed because the
forged completion resolved instead of rejecting. Restoring the intended match
passed all 3 evidence tests and restored `ledgerTools.ts` to SHA-256
`45b2552984b01773c572f053db7a98effb4ef270ada796e8d381da93edf6787c`;
the test stayed at `2fe3ac464686c76961b99d08a388abfd34df5b05512e80e6f4a920edc0b097ac`.

## generation-5-parent-gate-corrections <!-- cq:wip-checkpoint -->

Generation 5 verified the inherited receipt chain, prepared branch, exact
starting commit, base commit object, and base ancestry. The parent canonical
gate converged to 6,766 passing and 5 failing tests. The required corrections
are the stale stdio parity invocation matrix for all seven protected evidence
operations, the D336 fixture's journal-bound completion preparation and merge
coordinates, the Pi usable-verdict rule in generated source and surface, and
the canonical tool-surface target. The corrected five-file selector passes 23
tests with 658 expectations: both stdio prefixes invoke all 46 tools, D336
prepares/finalizes protected review and completion evidence before the real
journal-bound merge, both Pi verdict rules are restored, and the regenerated
target passes its canonical JSON, capability-union, and inventory guards. Both
prompt generators completed through a task-scoped writable Nix store backed by
the read-only host store.

Generation-5 counterfactuals removed `record_implementation_completion` from
the parity matrix, omitted D336's `--completion-ref`, weakened the first Pi
usable-verdict qualification, and removed the completion-recording operation
from the generated target source. They respectively failed both 46-tool
inventory cases, the real merge CLI with `--completion-ref is required`, the
named Pi prompt contract, and the public-inventory target contract. Restoration
passed each focused selector and returned the affected files to SHA-256
`13661d03400022ab3e90dc2e7e8cf8961226406058dca265fd4ba1b4c8134d3f`,
`0daf43d73e67f68e41e4d687a38f94b5da7837d394f3cbde2137c713f1345326`,
`96365768c3fe743aad5e790b65f3645333f19bafbd37d4f9a0759312d2ac4375`,
and `e8d97806fc91056f51bbd1cd0edbf7648fdc4b166da23bbac06c03ebe8d61675`;
the regenerated target returned to
`f4385fe7cfec97b96ea7c6d4e3c89278afcb6b2725f0e9022c967cfa91bb7d14`.
The exact 13-file acceptance selector passes 92 tests with 478 expectations.
TypeScript project references and ESLint pass. A second prompt-catalog,
agent-catalogue, and tool-target generator pass preserved exact SHA-256 bytes
`10e3995c2e630c272f1990dab7c1a40be5798d8067638194f78d5666b09ad6fc`,
`1cd22bff43a940852e927f810adb92784aa3eca4f05a7cf654ea50fd24bb633c`,
and `f4385fe7cfec97b96ea7c6d4e3c89278afcb6b2725f0e9022c967cfa91bb7d14`.
The trusted parent remains responsible for the canonical full gate.

## generation-3-revalidation <!-- cq:wip-checkpoint -->

Generation 3 resumed the exact clean generation-2 tip after the trusted parent
gate finalization failure. Base, branch, starting commit, receipt continuity,
and ancestry are verified. The exact 13-file acceptance selector passed 92
tests with 0 failures and 478 expectations. Both generators completed from the
recovered task-scoped Nix projection and left tracked output byte-identical.
The compatibility and tool-surface selector passed 17 tests with 0 failures,
2 explicit PostgreSQL-environment skips, and 269 expectations. A second run of
both generators again left the tree byte-identical. TypeScript project-reference
compilation and the repository ESLint suite completed successfully. Fresh
generation-3 counterfactuals proved that removing completionRef from merge
binding identity, bypassing the merge completion-ref parser guard, or restoring
the legacy unjournaled merge prompt makes its focused guard test fail; each
restored source passed and matched its exact pre-mutation SHA-256 digest.
