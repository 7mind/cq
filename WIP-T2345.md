```json
{
  "taskId": "T2345",
  "role": "implement-worker",
  "baseCommit": "79b0b69592ab4b50f290c824a4a03b3af3159f1c",
  "startedAt": "2026-08-24T04:10:57Z",
  "checkpoints": [
    { "name": "reproduction", "status": "done" },
    { "name": "implementation", "status": "todo" },
    { "name": "targeted-checks", "status": "unmeasured" },
    { "name": "mutation-evidence", "status": "unmeasured" },
    { "name": "final-verification", "status": "unmeasured" }
  ]
}
```

## reproduction <!-- cq:wip-checkpoint -->

Added blackbox contract probes for the required merge `completionRef` and stable
operation id. Before implementation, `bun test packages/cq-cli/test/gateRun.test.ts`
failed three merge-journal assertions because `--completion-ref` was unknown and
an unjournaled merge was accepted.

## implementation <!-- cq:wip-checkpoint -->

The command parser and trusted Git-effect binding now discriminate merge from
rebase. Merge requests require an opaque completion ref and stable operation id;
both coordinates participate in trusted binding validation and re-resolution.

## targeted-checks <!-- cq:wip-checkpoint -->

The focused CLI and process-control suites pass (24 tests).

## mutation-evidence <!-- cq:wip-checkpoint -->

Observed mutations:

- Removing `completionRef` from merge binding identity made the substitution
  test fail; restoring the intended bytes returned it to green.
- Disabling the merge completion-ref parser guard made the unjournaled-merge
  test fail; restoring the guard returned it to green.

## final-verification <!-- cq:wip-checkpoint -->

Unmeasured.
