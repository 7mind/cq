```json
{
  "taskId": "T2080",
  "role": "implement-worker",
  "baseCommit": "0ec99e8a686decb6e3476f1668dd468404d53fda",
  "startedAt": "2026-08-12T17:54:48Z",
  "checkpoints": [
    { "name": "base verification", "status": "done" },
    { "name": "guard reproductions", "status": "done" },
    { "name": "policy implementation", "status": "done" },
    { "name": "mutation controls", "status": "done" },
    { "name": "targeted checks", "status": "done" },
    { "name": "full gate", "status": "unmeasured" }
  ]
}
```

## base verification <!-- cq:wip-checkpoint -->

Prepared branch, starting commit, commit object, and base ancestry matched the dispatch input.

## guard reproductions <!-- cq:wip-checkpoint -->

The exact focused test exited 1 because the canonical implement prompt lacked `## 6a. Expected-failure tasks`.

## policy implementation <!-- cq:wip-checkpoint -->

Added the scanner, deterministic enumerator, typed inventory, six annotations, §6a, five scoped role blocks, and the convention pointer. The focused suite reports 7 pass / 0 fail and targeted lint/typecheck pass. Generated surfaces remain pending.

## mutation controls <!-- cq:wip-checkpoint -->

The exact focused command failed after each restored mutation class: §6 bytes, unannotated marker, orphan annotation, marker-without-inventory, inventory-without-marker, deleted role block, deleted convention pointer, and provenance inserted into all five canonical role blocks. It returned to 7 pass / 0 fail after restoration.

## targeted checks <!-- cq:wip-checkpoint -->

The focused suite, targeted lint/typecheck, single generator pass, and all five requested Nix prompt-surface checks passed. The canonical implement prompt diff against the dispatched base remains additive-only.

## full gate <!-- cq:wip-checkpoint -->

Pending.
