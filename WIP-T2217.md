```json
{
  "taskId": "T2217",
  "role": "implement-worker",
  "baseCommit": "47a26cc785ad29e5393a2ea7d82aacbe2aa543b5",
  "startedAt": "2026-08-18T00:15:33Z",
  "checkpoints": [
    { "name": "fail-first", "status": "done" },
    { "name": "implementation", "status": "done" },
    { "name": "mutation-proof", "status": "done" },
    { "name": "targeted-checks", "status": "done" },
    { "name": "full-gate", "status": "done" }
  ]
}
```

## fail-first <!-- cq:wip-checkpoint -->

Observed at `47a26cc785ad29e5393a2ea7d82aacbe2aa543b5` with:

`bun test packages/ledger/test/worktree-manage-mcp.test.ts -t "D346 preserves"`

The single selected test failed for the expected reason: `worktree_manage`
returned `refused` where the completed strict operator-action dependency must
permit `prepared`. No production source had been changed.

## implementation <!-- cq:wip-checkpoint -->

Implemented a closed `git-producing` / `external-effect` snapshot discriminator,
derived from the strict leading v1 operator-action envelope. Resolution now
returns the complete satisfying dependency closure separately from Git-only
result commits. Active and archived item snapshots share the classifier, and
task-adoption fencing retains every satisfying node plus parsed action identity.

## mutation-proof <!-- cq:wip-checkpoint -->

Four deliberate controls failed for the expected reason and were restored to
their exact pre-mutation SHA-256 digests:

- changing `external-effect` classification to `git-producing` failed both the
  classifier control and the real D346 worktree lifecycle;
- omitting the external-effect node from the satisfying closure failed the
  resolver closure and adoption-snapshot controls;
- bypassing missing-commit validation made the generic completed dependency
  report `ready` instead of `result-commit-missing`;
- replacing the parsed action key with a constant made the action-key fence
  publish instead of return `stale`.

The restored source digests are
`5aa384d4f4849b6f0abf4fe597ee8ebb5fc22fdd0bb235c674aa732a03a95fba`
for `dependencyResultCommits.ts` and
`c32e2de57bf6513117c41a143648208ba21717f8bf00d0ecee0edfe3289c5925`
for `taskAdoptionEligibility.ts`.

## targeted-checks <!-- cq:wip-checkpoint -->

The four-file acceptance command passed 58 tests and 347 assertions:

`bun test packages/ledger/test/dependencyResultCommits.test.ts packages/ledger/test/task-adoption-eligibility.test.ts packages/ledger/test/worktree-manage-mcp.test.ts packages/ledger/test/managedWorktree.test.ts`

## full-gate <!-- cq:wip-checkpoint -->

The brokered dispatch assigns the authoritative full gate to the trusted
result-storage boundary after this exact clean tip is handed off. The local
diagnostic reached green TypeScript and ESLint phases; effectful tests then hit
the child sandbox's expected process, socket, Git-object, Nix-cache, and
temporary-directory denials. That non-authoritative attempt was terminated and
settled before the trusted handoff.
