```json
{
  "taskId": "T2044",
  "role": "implement-worker",
  "baseCommit": "783f6b67ec38e8d595f80aede3d5c5aaa83d7a7d",
  "startedAt": "2026-08-11T10:31:31.631Z",
  "checkpoints": [
    { "name": "authority-reproduction", "status": "done" },
    { "name": "sandbox-reproduction", "status": "done" },
    { "name": "preturn-reproduction", "status": "done" },
    { "name": "correction", "status": "done" },
    { "name": "mutation-proof", "status": "done" },
    { "name": "focused-package-gates", "status": "done" },
    { "name": "full-gate", "status": "unmeasured" }
  ]
}
```

## authority-reproduction <!-- cq:wip-checkpoint -->

The real installed derivation failed with 9 pass / 2 fail / 242 assertions.
Calling the exported release recorder on a structural clone made authentication
succeed, and calling the exported installed-test attester with locally asserted
booleans likewise made authentication succeed. Both required rejection.

## sandbox-reproduction <!-- cq:wip-checkpoint -->

Changing the probe to `git update-ref refs/heads/cq-direct-git-probe
<valid-starting-commit>` made the installed fixture fail with `direct Git ref
mutation unexpectedly succeeded`. The prior nonexistent-object probe therefore
measured Git object validation, not provider confinement.

## preturn-reproduction <!-- cq:wip-checkpoint -->

The same installed proof observed neither an effective preturn record nor an
independently trusted expected derivation identity on the runner-owned result.
The local installed-only package test also proved that no parent-runnable
`check:codex-installed-gate` entrypoint existed.

## correction <!-- cq:wip-checkpoint -->

The top-level manager and installed-test authority factories are gone. The
installed runner now binds an independently supplied derivation identity and
reads a one-shot preturn/outcome record emitted by the packaged wrapper. Gate
authentication derives its claims from runner-owned native/process executions,
backend-owned consumed/aborted results, distinct backend instances, a
manager-owned release, and real Codex sandbox controls for both roles and both
routes. TypeScript and the focused source tests pass; the installed Nix arm is
next.

The first installed run exposed that Codex's built-in `:workspace` profile
admits the valid ref update for a linked worktree. The corrected control uses a
named profile with the exact managed worktree writable and the repository Git
metadata explicitly read-only. The same valid update now exits nonzero and
leaves the ref absent in the linked worktree while the isolated unsandboxed
positive control succeeds.

## mutation-proof <!-- cq:wip-checkpoint -->

Four real installed-package mutations failed for the expected predicate: a
manager-release ownership bypass made `fabricatedReleaseRejected` false; an
expected-derivation bypass made `substitutedExpectedIdentityRejected` false;
changing the explicit Git-metadata permission from read to write raised
`sandbox admitted direct Git metadata mutation`; and suppressing backend-instance
restart attribution removed `restart` from the exact failure-control set.
Restoration hashes matched the pre-mutation hashes for every production file.

## focused-package-gates <!-- cq:wip-checkpoint -->

`bun run typecheck`, `bun run lint`, and the focused source selectors passed.
The real `nix build --no-link --print-build-logs .#cq` installed arm passed 11
tests with 261 assertions and no failures, including the installed worker and
resolver gate.

## full-gate <!-- cq:wip-checkpoint -->

Awaiting an explicit parent grant after the corrected installed gate is green.
