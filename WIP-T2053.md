```json
{
  "taskId": "T2053",
  "role": "implement-worker",
  "baseCommit": "16a792b3372d7b00f7fd9e93232a1c10b3c71595",
  "startedAt": "2026-08-11T06:02:00Z",
  "checkpoints": [
    { "name": "reproduction", "status": "done" },
    { "name": "implementation", "status": "done" },
    { "name": "guard-mutation", "status": "done" },
    { "name": "targeted-checks", "status": "done" },
    { "name": "full-gate", "status": "todo" },
    { "name": "final-verification", "status": "todo" }
  ]
}
```

## reproduction <!-- cq:wip-checkpoint -->

`bun test packages/ledger/test/predicates.test.ts -t 'operator-action tasks never enter the worker-ready predicate'` failed as expected on both FsLedgerStore and InMemoryLedgerStore: the strict-envelope task appeared in `pImplement` as `T1`, proving the existing flow would select the worker-only arm.

## implementation <!-- cq:wip-checkpoint -->

Implemented the strict envelope parser, canonical pending/acknowledged/verified action ledger, deterministic restart-safe action/handoff materialization, identity acknowledgement, bounded append-only shell evidence, failure-to-pending, verified-only task completion, and a separate parent action predicate. The typed action remains the sole completion authority; operator tasks cannot acquire a worker status or erase/swap their envelope.

## guard-mutation <!-- cq:wip-checkpoint -->

Observed five fail-first mutations and restored exact intended bytes:

- routing an operator task into `pImplement` failed on both filesystem and in-memory predicate fixtures (2 failures); restored hash `f5d26e4e5c64f03b6a16ed343031bde85a49f5bd4d50361a588af49c56abd732` and 2 tests passed;
- counting wrong-identity exit-zero evidence verified prematurely on all three stores (3 failures);
- bypassing verified-only completion allowed generic task completion on all three stores (3 failures);
- accepting underscores in the strict action key admitted a malformed envelope on all three stores (3 failures);
- allowing an operator task to enter `wip` acquired a worker execution owner on all three stores (3 failures).

The lifecycle source hashes restored to `0f76a27b52c963bbeab4c5e8935b26c8ab18b70e44eddea936aeb12eebd5079c` (`operatorActions.ts`) and `593db13a003a0ad6987ff0f70376b8522fca5bd5827a218899bdec1f955bf300` (`core.ts`); all 8 lifecycle tests passed. Removing the cross-surface `never pImplement` invariant failed its generation guard; source and generated catalogue restored to hashes `e77f666ec62d4b71b1631f1e84aea1f12e29868b649bf51673777212b7d2704b` and `76909d34118230ef5415ce181b243664b36d2fe7c6f81548b4450d8d602c6091`, then the guard passed.

## targeted-checks <!-- cq:wip-checkpoint -->

Generated both committed prompt surfaces. `tsc -b` passes. Focused lifecycle, predicate, CLI gate, direct/stdio, remote client, canonical-schema, response-contract, docs, and all-surface generation suites pass: 127 + 53 + 211 tests in the principal runs, plus the remote and MCP/docs batches. Initial targeted failures exposed readonly tuple inference, a parity snapshot count, and stale canonical YAML; each was corrected and its exact suite rerun green.

## full-gate <!-- cq:wip-checkpoint -->

The granted foreground gate ran once at tip `a6cd9c47d1c6e2da35a49620ed0b4e8a0c040cbe`: `REAL_CHECK_EXIT=1`, 674163 ms, 5647 pass / 263 skip / 24 fail across 5934 tests. Full output: `/tmp/exchange/logs/cq-t2053-check.log`. Seventeen task-caused failures covered prompt provenance, generated profiler/target drift, public inventory, columns, response-policy call sites, post-fixture canonical-ledger initialization, and shifted prompt projections; each exact group now passes after correction. The remaining seven failures require the parent-owned package-local Pi dependency restoration. A green full-gate rerun still requires a new exclusive grant.

## final-verification <!-- cq:wip-checkpoint -->

Pending clean-tree, commit-object, and ancestry verification.
