```json
{
  "taskId": "T6521",
  "role": "implement-worker",
  "baseCommit": "cffbce1a2118b46b73b8c30d0d3168d3a7fec90a",
  "startedAt": "2026-09-19T09:40:00Z",
  "checkpoints": [
    { "name": "repository grounding", "status": "done" },
    { "name": "legacy queue upgrade and classification", "status": "done" },
    { "name": "canonical local executor flow and remote refusal", "status": "done" },
    { "name": "worker result status and WIP gate guards", "status": "done" },
    { "name": "focused-check contracts", "status": "done" },
    { "name": "cancellation cleanup and diagnostics", "status": "done" },
    { "name": "required live PostgreSQL and capstone coverage", "status": "done" },
    { "name": "canonical sources and generated catalogues", "status": "done" },
    { "name": "targeted acceptance checks", "status": "done" },
    { "name": "round 1 qualification assertion corrections", "status": "done" },
    { "name": "round 1 durable gate single-flight", "status": "done" },
    { "name": "round 1 rollout production wiring", "status": "done" },
    { "name": "round 1 required PostgreSQL rollout matrix", "status": "done" },
    { "name": "round 1 composed capstone evidence", "status": "done" },
    { "name": "round 1 diagnostics and restart races", "status": "done" },
    { "name": "round 1 focused acceptance checks", "status": "done" },
    { "name": "round 2 durable cross-process gate ownership", "status": "done" },
    { "name": "round 2 owner cancellation settlement", "status": "done" },
    { "name": "round 2 rollout and composed-flow evidence", "status": "done" },
    { "name": "round 2 focused acceptance checks", "status": "done" },
    { "name": "round 3 stale rebase successor capstone", "status": "done" },
    { "name": "round 3 correction recovery controls", "status": "done" },
    { "name": "round 3 retrievable gate diagnostics", "status": "done" },
    { "name": "round 3 bootstrap publication settlement", "status": "done" },
    { "name": "round 3 PostgreSQL rollout matrix", "status": "done" },
    { "name": "round 3 acceptance closure and focused checks", "status": "done" },
    { "name": "round 4 rejected-gate correction recovery", "status": "done" },
    { "name": "round 4 host gate environment isolation", "status": "done" },
    { "name": "round 4 focused checks", "status": "done" },
    { "name": "round 5 stale binding rollout correction", "status": "done" },
    { "name": "round 5 fixture authority corrections", "status": "done" },
    { "name": "round 5 focused checks", "status": "unmeasured" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## repository grounding <!-- cq:wip-checkpoint -->

Prepared worktree, branch, starting commit, base object, and base ancestry verified.

## legacy queue upgrade and classification <!-- cq:wip-checkpoint -->

Added the versioned `g213-t4` rollout contract. Pre-queue live worker rows are
processed by durable staging time and handle, every managed worktree is
protected before disposition, compatible rows enter the canonical queue, and
only an exact trusted child/provenance/output completion promotes a row to
qualified. Incompatible rows receive a durable parked disposition;
`gate-running` rows receive `execution-uncertain` without fabricated
completion evidence. Exact completed-green evidence is adopted only when the
output, result commit, worktree binding, and supervised-gate evidence digests
all match. The focused in-memory contract passes 2 tests with 13 assertions.

## canonical local executor flow and remote refusal <!-- cq:wip-checkpoint -->

The fail-first remote construction test observed a runnable worker row being
allocated, qualification reaching the task-ledger check, and Git reaching the
repository-root check. The corrected boundary returns typed
`executor-unavailable` before allocation and rejects qualification, acquisition,
parent gate, and Git before backend/filesystem effects. Read-only status remains
available. The focused contract passes 3 tests with 8 assertions; existing
runtime coverage passes 25 tests with one environment-gated PostgreSQL case.
The repository TypeScript build is green after this checkpoint.

## worker result status and WIP gate guards <!-- cq:wip-checkpoint -->

Fail-first schema checks observed `fail` with a non-null commit, `pass` with a
null commit, and both staged equivalents being accepted. The corrected ordinary
and staged schemas reject all four malformed writes while preserving the valid
fail/null and pass/full-SHA arms. The focused schema suite passes 21 tests with
39 assertions.

Fail-first managed-worktree coverage observed an open task-local
`implementation` checkpoint resolve through parent finalization and invoke the
gate runner. The corrected supervisor scans the exact task WIP before the host
runner, virtually closes only `trusted full gate`, and rejects the open
checkpoint with zero runner invocations. The focused case passes with 1 test and
3 assertions. A separate pre-existing successor test timed out and later read a
removed fixture directory during the initial broad run; it is not evidence for
this correction.

## focused-check contracts <!-- cq:wip-checkpoint -->

The canonical resolver output now carries non-empty typed `focusedChecks`, and
the implementation resolver and worker prompts limit child execution to focused
checks. The parent-owned host finalizer remains the sole canonical full-gate
boundary. Resolver schema and capability fixtures exercise the typed payload;
the capstone asserts one parent gate for the complete implementation flow.

## cancellation cleanup and diagnostics <!-- cq:wip-checkpoint -->

The fail-first blocking-runner regression observed authenticated cancellation
remain blocked behind the parent gate's managed-worktree effect lock. Gate
claim and completion now retain the lock, while host execution runs outside it;
authenticated cancellation settles before the runner is released. Concurrent
finalizers still share one active run and one gate epoch. Rollout dispositions
retain a structured diagnostic artifact alongside their stable detail digest,
so parked and execution-uncertain rows remain inspectable.

## required live PostgreSQL and capstone coverage <!-- cq:wip-checkpoint -->

The canonical capstone covers staged legacy adoption through queue coordination
and the single parent-gate boundary. The same rollout contract has an
environment-gated production PostgreSQL adapter case. Its required-live command
was attempted in this sandbox, but PostgreSQL could not create its IPv4 socket
on `127.0.0.1` (`Operation not permitted`); the ordinary selector passes three
in-memory cases and reports the live case as skipped when no DSN is supplied.

## canonical sources and generated catalogues <!-- cq:wip-checkpoint -->

Updated the canonical role and command sources, regenerated the agent
catalogue, and documented the versioned `g213-t4` rollout contract and its
operational dispositions.

## targeted acceptance checks <!-- cq:wip-checkpoint -->

The final focused selectors report 35 passes, one environment-gated PostgreSQL
skip, and 80 assertions across the schema, rollout, remote-refusal, capstone,
and conflict-capability files. The changed supervisor selectors report 9
passes and 47 assertions. `bun run typecheck`, `bun run lint`, prompt catalogue
generation, agent catalogue generation, and multiline `git diff --check` all
exit zero; regeneration produces no additional diff.

Deliberate guard mutations produced the expected failures and were restored to
their pre-mutation SHA-256 values: worker status/commit correlation
(`21e9ff5271a582b055f3edfc432d25e259bcb4340fc221fbd7f442a4c1f2231a`),
resolver focused-check cardinality
(`8614e91889f1c2f61b0e33410e89e7c37c729032f56913b06ef9179f88da345d`),
rollout ordering, exact completion, completed-green evidence, and diagnostic
retention
(`a3b402791ca211090697839c7d83d3dde7a081f6a66b3ac5c9413011647fdcd9`),
remote executor refusal, cancellation lock scope, and gate single-flight
(`e1d653a7a0840a15fe2dd59b7217622d387b473f6936c6ff1b9c01ca1a55e18b`),
task-local WIP closure
(`b0e1f82a600386ab9ac836fe03489c4346fcec88078699dfadd2e96624696122`),
and settled-front gate cardinality
(`a10f34bc1652f9e30bab39c962d4252b79f75c749ca14c5cddea1b0e2991c59e`).

## round 1 qualification assertion corrections <!-- cq:wip-checkpoint -->

Reproduced six focused failures: local qualification fixtures omitted both the
repository root and implementation-evidence authority required by the canonical
executor boundary. Added those authorities to the in-process and peer-process
fixtures without weakening remote refusal. The exact qualification and
coordinator-race selector now reports 7 passes, 0 failures, and 37 assertions.

## round 1 durable gate single-flight <!-- cq:wip-checkpoint -->

The same-dispatch regression failed first on memory and two SQLite connections:
two independently constructed capabilities each invoked the blocked gate, and
the first owner lost epoch 1 to epoch 2. A process-wide key now binds namespace,
repository, managed handle, dispatch, and generation before the durable claim,
so peers share one fenced finalization. Durable abort observes the persisted
epoch and signals only its exact owner; the node runner observes that signal,
settles both process-group arms, and releases admission afterward. Focused D489
and cancellation checks pass 3 tests with 11 assertions, and TypeScript builds.
The broader selector still reproduces the separately tracked deterministic
bootstrap-versus-parent cleanup timeout; that checkpoint remains open.

## round 1 rollout production wiring <!-- cq:wip-checkpoint -->

Local XDG runtime construction now completes `g213-t4` before exposing queue
execution and reports its disposition counts. Resolution requires repository,
task-ledger, and implementation-evidence authority; reauthenticates broker
receipts; leaves staged rows unqualified without an exact completion; accepts
completed-green rows only with exact runner evidence and the protected managed
gate projection; and holds the managed-worktree effect lock through resolution
and disposition. A pre-resolution row digest is compare-and-set at enrollment
or parking. The production runtime adoption case and its skip-wiring mutation
failed and passed as expected; the async row-change regression likewise failed
before the digest fence and passes after it. The rollout/runtime/remote selector
passes 10 tests with 42 assertions (two environment-gated PostgreSQL skips), and
TypeScript builds.

## round 1 required PostgreSQL rollout matrix <!-- cq:wip-checkpoint -->

The required-live script now includes `attestation-queue-upgrade.test.ts` and
therefore fails when its PostgreSQL case skips. The case now covers the complete
rollout disposition set, skipped prepared/consumed/aborted states, protected
ordering, and reopen preservation; the same required selector includes the
existing acquisition/fencing and staged-rebase successor process contracts.

## round 1 composed capstone evidence <!-- cq:wip-checkpoint -->

Completed by the round-2 public local-XDG composition and concrete transition
counts recorded below.

## round 1 diagnostics and restart races <!-- cq:wip-checkpoint -->

The missing D504 integration reproduction now drives the registered-launch
boundary with a real detached bootstrap identity and a controlled release
publisher. Its fail-first form returned at the deadline, removed the protocol
directory, and left the still-owned publication to observe the vanished path.
The corrected boundary does not start publication after expiry and, once
started, waits for its settlement before parent cleanup. The focused case
passes with 5 assertions; `registeredLaunch.ts` and its guard restore to
SHA-256 `0329540589d6ecb5378bd6773770421fe4e4e99f4c2358848a478f37bdfafb13`
and `ec6089bc736837de6bb5d318b641606a39e7677332d7ef570b950ea5200e2e9f`.

The successor process test also exposed a confined-runner limitation rather
than queue behavior: the sandbox rejects Bun pipe `send(2)` with `EPERM`, and
the existing process-provider contract times out under the same confinement.
The trusted parent gate remains responsible for that real-process selector.

Generic first-runner failure details are now redacted with the shared secret
policy and truncated on a 1,024-byte UTF-8 boundary before durable
`parent-lost` publication. The fail-first case retained the raw API-key-shaped
value and exceeded the byte bound; the corrected case keeps the live thrown
error unchanged while durable row inspection and terminal retrieval return the
same bounded `[REDACTED:api-key]` diagnostic. The focused case passes with 7
assertions.

## round 1 focused acceptance checks <!-- cq:wip-checkpoint -->

Completed by the round-2 focused selector and production-call mapping below.
The trusted parent owns package, live-PostgreSQL, and full-gate execution.

## round 2 durable cross-process gate ownership <!-- cq:wip-checkpoint -->

Prepared branch `implement/T6521` at `cfb5c1d4` was verified as a descendant of
the dispatched base. A two-process SQLite regression failed first with two gate
invocations while the first runner was blocked; the first owner then lost epoch
1 to epoch 2. Finalization now holds the durable managed-worktree effect lock
across claim, gate execution, settlement, and completion, and the process-global
finalization map was removed. The focused selector passes four cases (memory
peers, SQLite peers, independent SQLite processes, and authenticated
cancellation) with 15 assertions; the independent process case records one gate
invocation and two exact `result-stored` outcomes.

## round 2 owner cancellation settlement <!-- cq:wip-checkpoint -->

Authenticated cancellation intent is now durable and epoch-bound. The gate
owner observes it outside the worktree terminalization lock, settles the runner,
and only then publishes the abort while still holding the serialized effect
boundary. A second Bun process ran the production registered-process runner;
the parent cancelled it through a separate SQLite connection and observed both
the registered root PID and process group absent at the first durable aborted
row. The owner and cancelling parent returned the same `cancelled` terminal
result. Deliberately changing the process-settlement assertion failed with
`processAbsent: true` versus the mutated `false`; restoration returned the file
to SHA-256 `d92d17ab6a5f3b65572731bd0aaf53754d4f45a8c22e1ff234eeccfd9966f85e`
and the focused case passes with 5 assertions.

## round 2 rollout and composed-flow evidence <!-- cq:wip-checkpoint -->

The capstone now exercises the actual local-XDG public path: production rollout,
qualification, acquisition, one trusted gate, completion/consumption, fetch
delivery, and empty replay. Its observed counts are enqueue 1, qualification 1,
admission 1, retirement 0, rebase 0, gate 1, completion 1, and duplicate gate 0.
The same runtime dependency seam is available only to the local construction;
remote construction remains metadata-only. A deliberate gate-count mutation
failed with observed 1 versus mutated 2, then restored to SHA-256
`7f8c72e3143d4caeff7abbc1763f1bd534a4c9ae8c30423bb502e539b10e3d38`.

The required-live PostgreSQL rollout case now defines five live dispositions
(unqualified, qualified, execution-uncertain, completed-green, incompatible),
checks stable protected ordering, and verifies prepared, consumed, and aborted
rows remain byte-identical across backend reopen. The required harness already
combines it with PostgreSQL acquisition/fencing and staged-rebase successor
process races. Live execution remains unmeasured here because the confined
sandbox cannot bind the required PostgreSQL socket; the parent owns that gate.

## round 2 focused acceptance checks <!-- cq:wip-checkpoint -->

`bun run typecheck` and `bun run lint` exit zero. Exact focused cases for
cross-process gate ownership/cancellation, redacted diagnostics, production
local-XDG rollout, capstone composition, in-memory rollout, and remote executor
refusal report 19 passes, two environment-gated PostgreSQL skips, zero failures,
and 82 assertions. An earlier broad name expression also selected the previously
documented confined successor-bootstrap case, which timed out and produced the
same removed bridge-directory follow-on. The acceptance aggregate additionally
cannot open Nix's home cache or loopback listeners in this sandbox
(`unable to open ... fetcher-cache-v4.sqlite`; Bun `EADDRINUSE` on port 0).
Those environment-denied checks are delegated to trusted parent execution.
Multiline `git diff --check` is clean.

## round 3 stale rebase successor capstone <!-- cq:wip-checkpoint -->

The public local-XDG capstone now advances the protected branch after staging a
real candidate, retires the stale source, executes the admitted guarded rebase,
materializes an exact-tip successor, qualifies it, and runs one parent gate.
After closing and reconstructing the runtime, it retrieves the same durable
gate evidence, rejects both retired-source and tampered-commit authorities,
records one protected review without rerunning the gate, reserves the exact
candidate, performs `git merge --ff-only`, records completion idempotently, and
delivers the successor output.

## round 3 correction recovery controls <!-- cq:wip-checkpoint -->

An adapter-backed control yields a deterministic-red front, admits and releases
the next eligible enrollment, resumes the corrected front under a fresh lease
generation, and runs exactly one correction gate. The composed stale flow
observes enqueue 2, qualification 2, admission 2, retirement 1, rebase 1, gate
1, review 1, evidence preservation 1, evidence rejection 2, merge 1,
completion 1, delivery 1, eager-source gate 0, and post-fast-forward gate 0.
The runtime restart occurs between successor completion and evidence delivery.

## round 3 retrievable gate diagnostics <!-- cq:wip-checkpoint -->

The host gate now writes JUnit to a unique runner-owned temporary path and
prioritizes the first failing testcase before cascading console failures.
Persisted rejection details apply shared secret redaction and the existing byte
bound. The deterministic-red contract fetches the exact terminal diagnostic,
contains no original secret, and proves repeated finalization does not invoke
the runner again. The D500 mutation replaced the JUnit failure prefix and failed
on the missing first assertion; restoration returned
`supervisedWorkerGate.ts` to SHA-256
`cb4d905737f08ed3ece91e374beecb38464278a8a76b270437c8297c8f27b679`.

## round 3 bootstrap publication settlement <!-- cq:wip-checkpoint -->

The D504 test now launches the real command bootstrap, waits for its actual
deadline exit, and holds publication both before and after the atomic rename.
The authenticated parent retains the protocol directory through publication
settlement and removes it only after its operation settles; an unauthenticated
bootstrap still self-cleans. The fail-first case observed the child delete the
directory before its live parent published. The full registered-launch file
passes 19 tests with 91 assertions. Final source and test SHA-256 values are
`2895b2db001a09490ae408ae7719e42322ab83b2c4e5fa663a0d0036b64c04a4`
and `155b9d2615f7b80ceb4e49e9976fe3de520c5b0a9c16653e79819e5f549eab6a`.

## round 3 PostgreSQL rollout matrix <!-- cq:wip-checkpoint -->

The required PostgreSQL case retains its real pre-rollout row layout, complete
five-disposition matrix, terminal-row digest preservation, protected ordering,
and reopen checks. Completed-green rows now reach `result-stored` only through
the minted parent-gate capability, authenticated claim, and epoch-bound green
completion; fabricated row replacement is gone. The required harness also
contains acquisition/fencing and successor-process coverage. Live execution is
still delegated to the trusted parent because this sandbox has no PostgreSQL
DSN; the same focused selector passes four in-memory cases and reports the one
live case as skipped. The disposition mutation failed with observed
`adopted-completed-green` versus the mutated value and restored SHA-256
`7f661daaaab71608766dc420a61a165b3db20f963a2dfac644e31c008896d1a3`.

## round 3 acceptance closure and focused checks <!-- cq:wip-checkpoint -->

The focused round-3 selector reports 11 passes, one environment-gated
PostgreSQL skip, zero failures, and 71 assertions. The capstone alone passes
four tests with 24 assertions; focused ESLint, `tsc -b`, and multiline
`git diff --check` exit zero. The cheap repository-closure test passes after
refreshing the audited inherited and round-3 opaque-edge digests and adding the
new runner, capstone, and diagnostic path/spawn declarations. Its deliberate
digest mutation failed closed and restored the manifest to SHA-256
`70110101b37ee5310274dbf878d1c70cdfb63244ba1504518191515b64a09b8a`.
The capstone gate-count mutation observed gate 1 versus mutated 2 and restored
SHA-256
`51f7f9b466b2136483357ca87b76911429d76333d1d4d0a4da236029249086be`.

## round 4 rejected-gate correction recovery <!-- cq:wip-checkpoint -->

Fail-first production composition ran an actual qualified parent gate to
`gate-rejected`, refused an unchanged intermediate generation without invoking
the gate, then committed a changed correction through the broker. Native
qualification failed at `dispatchImplementationQueue.ts:980` because
`terminalPrior` classified the genuine red source as immutable. The regression
retains the red row, an intervening unqualified aborted generation, and requires
one fresh green gate plus an empty replay. Queue admission now recognizes only a
genuine qualified `gate-rejected` ancestor with matching stored diagnostics,
task, repository, worktree, manager binding, receipt prefix, and a non-empty
contiguous changed suffix. The production composition passes with one red source
gate, zero unchanged or unqualified-intermediate gates, one correction gate, no
replay gate, and all three generations retained. Before the successful
qualification, the same composition now rejects a foreign repository binding,
forged rejection diagnostics, and a substituted receipt-lineage digest.

## round 4 host gate environment isolation <!-- cq:wip-checkpoint -->

Fail-first coverage launched the production node gate with only coordinator
identity variables added. The child observed the leaked values and exited 1
with zero passes, while the fixture also pins preservation of PostgreSQL, Node,
test, ledger-command, and Codex-executable settings. The host launch now removes
only the three dispatch invocation identity and observation variables before
spawning the canonical gate. The production subprocess regression passes.

## round 4 focused checks <!-- cq:wip-checkpoint -->

The corrected D508 production composition passes 1 test with 21 assertions;
the D506 production subprocess passes 1 test with 1 assertion. The immutable
terminal queue suite passes 21 tests with 83 assertions. The audited repository
closure passes 1 test after updating the path/spawn digests for the changed test
and host-gate source; its final manifest SHA-256 is
`f204d5ed7f19ce62a74e8269d0295dd3e758b6dbbe62228b7bf668aa36b95cbd`.
`tsc -b`, ESLint, and base-to-worktree multiline `git diff --check` exit zero.

Mutation observations from this run:

- removing the non-empty changed-result and strict receipt-prefix guards let the
  unchanged correction qualify; the D508 test failed at the expected rejection
  (0 pass, 1 fail), then passed after restoration;
- disabling the exact manager-binding guard let the foreign repository binding
  qualify (0 pass, 1 fail); disabling the rejection-details digest guard did the
  same for forged diagnostics; disabling both receipt-lineage digest comparisons
  did the same for a substituted lineage. The source restored to SHA-256
  `468038cf92c193d54c924004acc228b4243f04f5a3d932a97497ac265d3c8aa6`,
  and the expanded test restored to
  `0623eabb46a75ba629fbdf124c751320d918990cc30eed667f9e16c292413772`;
- omitting correlation-id scrubbing made the host subprocess exit 1 with zero
  passes and one failure; restoration passed and returned
  `supervisedWorkerGate.ts` to SHA-256
  `e5bc4a0af213a53d99b64420e4ad21a59d157ff2c1f4c167fea40db16505c34f`;
- substituting a stale audited test digest made the repository-closure check fail
  with `source-digest-mismatch`; restoration passed before the later intentional
  coverage update refreshed the manifest to its final digest above.

The trusted parent owns the full and required-live gates.

## round 5 stale binding rollout correction <!-- cq:wip-checkpoint -->

The real local runtime constructor was seeded with both a valid legacy row and
a released managed-worktree binding. It failed first at startup with `managed
worktree binding is no longer live`. The protection port now distinguishes an
exact protected execution from the known absent binding; the latter is parked
through the rollout compare-and-set path with `worktreeProtected: false`, and
its incompatibility resolver is never called. Exact changed-binding checks and
unexpected-error propagation remain intact. The focused runtime case passes
with one adoption, one parked row, 12 assertions, and no runnable stale row.

Disabling the absent-binding branch reproduced the same startup failure with 0
passes and 1 failure. Restoration returned the runtime source and regression
test to SHA-256 `295e7b4362d8231afd0b1ed1781c68b63e83b17c7e6960dacd381e1e1b7972bd`
and `956270afdedc43ed9f4b0ce66f10ca918650e455d0e866e640ed8cf6aaca4d1c`;
the focused case then passed again.

## round 5 fixture authority corrections <!-- cq:wip-checkpoint -->

Both coordinator process constructors now declare the explicit `local-xdg`
executor mode while retaining their in-memory implementation-evidence stores.
The SQLite three-process contract passes with one test and 18 assertions. Both
guarded-rebase packaged-role constructors now receive the genuine evidence
store created by the local XDG ledger plus explicit local mode, so candidate
authority controls are available before review and completion. That installed
selector is unavailable in this sandbox because its three packaged executable
variables are absent; the trusted parent owns its installed acceptance run.

## round 5 focused checks <!-- cq:wip-checkpoint -->

Unmeasured; this round will record fail-first observations, focused test counts,
typecheck and lint results, and restored guard hashes. The trusted parent owns
the package aggregate, required-live PostgreSQL harness, and native full gate.

## trusted full gate <!-- cq:wip-checkpoint -->

Unmeasured; delegated to trusted result storage after the final clean commit.
