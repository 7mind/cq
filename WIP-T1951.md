```json
{
  "taskId": "T1951",
  "role": "implement-worker",
  "baseCommit": "3629552d8f3f8f96f85bbce0e738396a34197499",
  "startedAt": "2026-08-04T00:00:00.000Z",
  "checkpoints": [
    {"name": "policy-module", "status": "done"},
    {"name": "ownership-metadata-guard", "status": "done"},
    {"name": "schema-fields", "status": "done"},
    {"name": "acceptance-tests", "status": "done"},
    {"name": "full-gate", "status": "todo"}
  ]
}
```

## policy-module <!-- cq:wip-checkpoint -->

`packages/ledger/src/worksetOwnerEdges.ts` — total allow/deny table over CANONICAL_LEDGERS × planning/implementation creation inventories; fixtures; legacy inference.

## ownership-metadata-guard <!-- cq:wip-checkpoint -->

`assertWorksetOwnershipFieldsAbsent` wired into `applyCreateItem`/`applyUpdateItem`. Mutation evidence: fail without throw, pass after restore (hash match).

## schema-fields <!-- cq:wip-checkpoint -->

Optional `worksetOwnerRef` / `worksetOwnerEdgeKind` on every canonical schema via `WORKSET_OWNERSHIP_SCHEMA_FIELDS`.

## acceptance-tests <!-- cq:wip-checkpoint -->

`workset-owner-edge-table.test.ts` + `workset-owner-edge-legacy.test.ts` — 21 pass.

## full-gate <!-- cq:wip-checkpoint -->

Pending `cq gate run` / `bun run check`.
