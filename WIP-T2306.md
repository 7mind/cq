```json
{
  "taskId": "T2306",
  "role": "implement-worker",
  "baseCommit": "319f5184bf388b2dda4a6dec92c12e20b8c91ba0",
  "startedAt": "2026-08-21T20:37:28Z",
  "checkpoints": [
    { "name": "reproduction", "status": "done" },
    { "name": "recovery-binding", "status": "done" },
    { "name": "implement-flow", "status": "done" },
    { "name": "denial-regressions", "status": "done" },
    { "name": "postgres-required", "status": "done" },
    { "name": "gate-delegation", "status": "done" },
    { "name": "generation-2-verification", "status": "unmeasured" }
  ]
}
```

## reproduction <!-- cq:wip-checkpoint -->

The black-box recovery regression failed before implementation because the
recovery API did not exist, then passed after the implementation.

## recovery-binding <!-- cq:wip-checkpoint -->

Terminal recovery is bound to the exact managed handle, terminal outcome, live
tip, and durable receipt closure and survives adapter restart and collapse.

## implement-flow <!-- cq:wip-checkpoint -->

Worktree management returns only an opaque reference; worker prepare resolves
the prior generation and verified receipt lineage server-side.

## denial-regressions <!-- cq:wip-checkpoint -->

Foreign, forged, ambiguous, nonterminal, unbound, expired, and mismatched
recovery attempts fail before dispatch allocation.

## postgres-required <!-- cq:wip-checkpoint -->

The required command provisions an isolated PostgreSQL instance, requires the
live suite, and tears the instance down on every exit path. The confined worker
proved its fail-closed startup and cleanup path; TCP binding is host-gated.

## gate-delegation <!-- cq:wip-checkpoint -->

The exact task suite, broker suite, typecheck, and lint are green. The canonical
full gate is delegated to trusted result storage for this committed tip.

## generation-2-verification <!-- cq:wip-checkpoint -->

Inherited implementation and receipt lineage are present. Generation 2 will
rerun the acceptance checks and persist any required corrections before trusted
gate delegation.
