```json
{
  "taskId": "T2306",
  "role": "implement-worker",
  "baseCommit": "319f5184bf388b2dda4a6dec92c12e20b8c91ba0",
  "startedAt": "2026-08-21T20:37:28Z",
  "checkpoints": [
    { "name": "reproduction", "status": "done" },
    { "name": "recovery-binding", "status": "done" },
    { "name": "implement-flow", "status": "done" },
    { "name": "denial-regressions", "status": "done" },
    { "name": "postgres-required", "status": "todo" },
    { "name": "full-gate", "status": "unmeasured" }
  ]
}
```

## reproduction <!-- cq:wip-checkpoint -->

Observed the new black-box recovery regression fail before implementation:
`Export named 'discoverDispatchRecovery' not found`, with 0 pass / 1 fail.

## recovery-binding <!-- cq:wip-checkpoint -->

The attestation store retains an opaque, exact manager-binding recovery association
through terminal collapse and validates receipt closure against the live tip.

## implement-flow <!-- cq:wip-checkpoint -->

Parent-lost worker aborts persist recovery atomically; worktree management resolves
the retained handle, and prepare derives reprepare identity and receipts server-side.

## denial-regressions <!-- cq:wip-checkpoint -->

Focused tests deny foreign namespaces, forged references and handles, nonterminal or
unbound dispatches, expired authority, ambiguity, binding/tip mismatch, and incomplete
receipt closure without allocating a dispatch.

## postgres-required <!-- cq:wip-checkpoint -->

The required command provisions and tears down an isolated PostgreSQL instance. Its
startup failure path removed its temporary cluster; this confined worker cannot bind
the required TCP listener (`Operation not permitted`), so the live leg remains todo.

## full-gate <!-- cq:wip-checkpoint -->

Trusted full gate has not been delegated.
