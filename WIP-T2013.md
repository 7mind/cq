```json
{
  "taskId": "T2013",
  "role": "implement-worker",
  "baseCommit": "f150292fad2036eef3080d9a4d3e966e64323217",
  "startedAt": "2026-08-07T18:00:00.000Z",
  "checkpoints": [
    { "name": "skeleton", "status": "done" },
    { "name": "t1307-worker-schema-prompt", "status": "done" },
    { "name": "t1308-reviewer-schema-prompt", "status": "done" },
    { "name": "t1309-orchestrator-fragments", "status": "done" },
    { "name": "tests-mutation", "status": "done" },
    { "name": "gen-catalogue", "status": "done" },
    { "name": "full-gate", "status": "done" }
  ]
}
```

## skeleton <!-- cq:wip-checkpoint -->

Base verified at `f150292f`. Implementing G121 harness batch T1307+T1308+T1309
as one serialized commit (T2013 / K168).

## t1307-worker-schema-prompt <!-- cq:wip-checkpoint -->

- implement-worker sidecar v4→v5
- required `baseVerification` discriminated union on output
- full-SHA `baseCommit`/`startingCommit`/`priorResultCommit`
- Step 0 evidence-only prompt; removed install/worktree lifecycle ownership
- G122 early skeleton + incremental retained

## t1308-reviewer-schema-prompt <!-- cq:wip-checkpoint -->

- implement-reviewer sidecar v5→v6
- required `resultCommitEvidence` + `baseAncestry` verified|unresolvable
- approve requires both verified arms with full SHAs
- agent prompt + portable implement-review mirror updated

## t1309-orchestrator-fragments <!-- cq:wip-checkpoint -->

- implement/advance: worktree_manage prepare before wip/dispatch; retain handle;
  resume-required recovery; guarded release only; no raw worktree lifecycle
- claude/codex/pi implement-dispatch-workflow fragments updated

## tests-mutation <!-- cq:wip-checkpoint -->

g121HarnessBatch.test.ts + fixture updates. Mutation evidence:
- worker Step 0 marker fail-then-restore (sha restored)
- advance raw `git worktree add .claude/...` fail-then-restore (sha restored)
- schema pass rejects unresolvable baseVerification; fail accepts closed reasons

## gen-catalogue <!-- cq:wip-checkpoint -->

`bun run gen-agents` wrote agentsCatalogue.gen.ts (24 roles).

## full-gate <!-- cq:wip-checkpoint -->

REAL_CHECK_EXIT=0, gateDurationMs=538139, 5218 pass / 0 fail.
