```json
{
  "taskId": "T2322",
  "role": "implement-worker",
  "baseCommit": "83de47ffb2ff04c6a908eea999e45a1f7cf36d51",
  "startedAt": "2026-08-24T00:41:00Z",
  "checkpoints": [
    { "name": "fail-first controls", "status": "done" },
    { "name": "unique task-state resolver", "status": "done" },
    { "name": "terminal release integration", "status": "done" },
    { "name": "effect-coordinate revalidation", "status": "done" },
    { "name": "Task-1 marker conversion", "status": "done" },
    { "name": "targeted checks", "status": "done" },
    { "name": "round-2 artifact cleanup", "status": "todo" },
    { "name": "trusted full gate", "status": "unmeasured" }
  ]
}
```

## fail-first controls <!-- cq:wip-checkpoint -->

`bun test packages/ledger-mcp/test/worktreeTerminalReleaseXdg.test.ts`
failed with 3 pass / 3 fail. Archived-only release failed with `Item not
found in ledger tasks: T350`; active+archived ambiguity incorrectly released;
two archives were rejected for the same active-only lookup reason instead of
the required total-match ambiguity.

## unique task-state resolver <!-- cq:wip-checkpoint -->

`resolveUniqueTaskState` enumerates all active task groups and every advertised
archive, filters exact task ids, and rejects any total other than one.

## terminal release integration <!-- cq:wip-checkpoint -->

`worktree_manage(release)` resolves unique active-or-archived authority after
registry, handle/task, and canonical-disposition validation, then compares the
authoritative status before minting release authority.

## effect-coordinate revalidation <!-- cq:wip-checkpoint -->

Terminal-binding coordinate resolution uses the shared resolver and compares
the current status with the bound disposition. Ordinary effects retain the
active-only `assertLiveTask` path.

## Task-1 marker conversion <!-- cq:wip-checkpoint -->

Converted the D350 progression marker to the same-titled plain test and removed
its annotation plus inventory entry.

## targeted checks <!-- cq:wip-checkpoint -->

`bun test packages/ledger/test/workset-git-effect-gate.test.ts
packages/ledger-mcp/test/worktreeTerminalReleaseXdg.test.ts`: 14 pass, 0 fail,
124 assertions.

Mutation controls deliberately inverted exact-id filtering, complete archive
enumeration, unique-match cardinality, release status equality, and runner
bound-disposition equality. Each selected test failed under mutation and passed
after restoration. Restored SHA-256 values: resolver
`ff663023683608493f8d61a29764144454f89b717ffaaf063100889b06054420`,
release path
`99923be5c4fd091f8e6bfa9329da1bba502b5061dd514ab9a50c82d4191c24a4`,
runner
`2d228b286b0f8bd066041c9e380f3bdf6049b65e4148ebdc582d052d7381d733`.

## round-2 artifact cleanup <!-- cq:wip-checkpoint -->

Remove only the inherited stray `WIP-T2321.md`; retain this canonical task
artifact and every implementation, control, inventory, and mutation-evidence
change from the preceding round.

## trusted full gate <!-- cq:wip-checkpoint -->

Delegated to the trusted result-storage boundary after implementation.
