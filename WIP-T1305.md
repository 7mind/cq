```json
{
  "taskId": "T1305",
  "role": "implement-worker",
  "baseCommit": "fe6103b2c99fad4cbee75d3713971ce50240139a",
  "startedAt": "2026-08-07T00:00:00.000Z",
  "checkpoints": [
    { "name": "skeleton", "status": "done" },
    { "name": "core-api", "status": "done" },
    { "name": "tests-mutation", "status": "done" },
    { "name": "full-gate", "status": "todo" }
  ]
}
```

## skeleton <!-- cq:wip-checkpoint -->

Base verified at fe6103b2. WIP skeleton created.

## core-api <!-- cq:wip-checkpoint -->

Added `@cq/ledger` `managedWorktree.ts`:
- `prepareManagedWorktree` (fresh / resume / handle-free resume-required)
- `releaseManagedWorktree` (dirty / WIP-open / terminal / idempotent)
- install plan build+validate (`bun install --frozen-lockfile` + `BUN_INSTALL_CACHE_DIR` under CQ cache root)
- UUIDv7 worktree ids under `.claude/worktrees/`
- registry-backed opaque handles with tamper/foreign/traversal fail-closed
- fault injector seams before irreversible boundaries
- exports via `packages/ledger/src/index.ts`

## tests-mutation <!-- cq:wip-checkpoint -->

`managedWorktree.test.ts`: 14 real-repo tests, all green.
Mutation evidence:
1. Disabled missing-`BUN_INSTALL_CACHE_DIR` guard → test failed (expected)
2. Disabled WIP-open refuse → release released instead of refused
3. Disabled live-tree short-circuit → duplicate prepare no longer resume-required
Restored bytes; sha256 matched pre-mutation; full file 14 pass.

## full-gate <!-- cq:wip-checkpoint -->

TODO: `cq gate run … bun run check` green.
